---
title: "VideoTIR: Accurate Understanding for Long Videos with Efficient Tool-Integrated Reasoning"
title_zh: VideoTIR：通过高效工具集成推理实现长视频的准确理解
authors: "Zhe Gao, Shiyu Shen, Taifeng Chai, Weinong Wang, Haotian Xu, Xing W, Wenbin Li, Qi Fan, Yang Gao, Dacheng Tao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25021v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于长视频理解的高效工具集成推理
tldr: 针对多模态大模型在长视频理解中存在的幻觉和效率问题，本文提出VideoTIR框架。该框架利用强化学习驱动模型调用多层级工具包（涵盖视频段、图像及区域），通过Zero-RL和SFT冷启动策略，使模型能精准定位关键视觉信息。此外，引入TAGPO优化算法和沙盒轨迹合成框架，显著提升了工具调用效率和数据质量，在多个长视频问答基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有模型在处理长视频时因视觉与文本标记不平衡易产生幻觉，且现有的工具调用方法受限于高质量数据匮乏和调用路径僵化。
method: 提出VideoTIR框架，结合强化学习与多层级工具包，并利用TAGPO算法优化工具调用效率及沙盒框架合成高质量训练轨迹。
result: 在三个长视频问答基准测试上的实验证明，该方法在理解准确性和推理效率方面均优于现有技术。
conclusion: VideoTIR通过强化学习有效整合了多层级视觉工具，为解决长视频理解中的幻觉问题和提升推理效率提供了一种新颖且高效的方案。
---

## 摘要
现有的多模态大语言模型（MLLMs）在长视频理解（LVU）中经常面临幻觉问题，这主要是由于文本和视觉标记（tokens）之间的不平衡造成的。观察到 MLLMs 能够很好地处理短视觉输入，最近的长视频理解研究通过将海量视觉数据自动解析为可由 MLLMs 有效处理的可管理片段来缓解幻觉。基于监督微调（SFT）的工具调用方法可以实现这一目的，但它们通常需要大量的细粒度、高质量数据，且受限于固定的工具调用轨迹。我们提出了一种新颖的 VideoTIR，它利用强化学习（RL）来鼓励正确使用全面的多级工具包，从而实现高效的长视频理解。VideoTIR 探索了零样本强化学习（Zero-RL）和 SFT 冷启动，使 MLLMs 能够检索并关注有意义的视频片段、图像或区域，从而准确且高效地增强长视频理解。为了减少冗余的工具调用，我们提出了工具包动作分组策略优化（TAGPO），通过逐步奖励分配和失败回放（rollouts）的重用，提高了调用过程的效率。此外，我们开发了一个基于沙盒的轨迹合成框架，用于生成高质量的轨迹数据。在三个长视频问答（QA）基准测试上的广泛实验证明了我们方法的有效性和高效性。

## Abstract
Existing Multimodal Large Language Models (MLLMs) often suffer from hallucinations in long video understanding (LVU), primarily due to the imbalance between textual and visual tokens. Observing that MLLMs handle short visual inputs well, recent LVU works alleviate hallucinations by automatically parsing the vast visual data into manageable segments that can be effectively processed by MLLMs. SFT-based tool-calling methods can serve this purpose, but they typically require vast amounts of fine-grained, high-quality data and suffer from constrained tool-calling trajectories. We propose a novel VideoTIR that leverages Reinforcement Learning (RL) to encourage proper usage of comprehensive multi-level toolkits for efficient long video understanding. VideoTIR explores both Zero-RL and SFT cold-starting to enable MLLMs to retrieve and focus on meaningful video segments/images/regions, enhancing long video understanding both accurately and efficiently. To reduce redundant tool-calling, we propose Toolkit Action Grouped Policy Optimization (TAGPO), which enhances the efficiency of the calling process through stepwise reward assignment and reuse of failed rollouts. Additionally, we develop a sandbox-based trajectory synthesis framework to generate high-quality trajectories data. Extensive experiments on three long-video QA benchmarks demonstrate the effectiveness and efficiency of our method.

---

## 论文详细总结（自动生成）

### VideoTIR：通过高效工具集成推理实现长视频的准确理解

#### 1. 核心问题与整体含义
论文针对多模态大语言模型（MLLM）在处理**长视频理解（LVU）**时面临的两个核心挑战：
*   **幻觉问题**：由于视觉标记（tokens）数量远超文本标记，模型难以在海量信息中精准定位，导致推理出错。
*   **工具调用效率低下**：现有的工具集成推理（TIR）方法要么依赖复杂的外部流程，要么缺乏高质量的微调数据，导致工具调用冗余（过度使用）或无效（误用）。
**整体含义**：VideoTIR 旨在通过强化学习（RL）驱动模型自主、高效地调用多层级内部工具（如视频检索、帧定位、局部放大），实现对长视频的精准“按需取样”理解。

#### 2. 方法论
VideoTIR 提出了一个多轮交互推理框架，核心包含以下三个部分：
*   **多层级分级工具包（Hierarchical Toolkit）**：
    *   **全局工具（Browsing Tool）**：通过提高分辨率和采样率进行粗略浏览。
    *   **局部工具链**：包含**片段检索器**（定位时间段）、**帧检索器**（定位关键帧）和**缩放检索器**（定位空间区域），模拟人类从粗到细的观察过程。
*   **工具包动作分组策略优化（TAGPO）**：
    *   这是一种改进的强化学习算法。它将相同工具的调用进行分组，并引入**逐步奖励分配机制**。
    *   **核心逻辑**：对成功轨迹中的冗余调用进行惩罚（通过衰减系数），对失败轨迹中的盲目重复进行抑制，从而鼓励模型用最少的工具调用次数获取最关键的信息。
*   **沙盒轨迹合成框架**：
    *   利用外部强模型（如 GLM-4.5V）在模拟沙盒中探索工具调用路径，生成高质量的“思考-调用-反馈-回答”轨迹数据，用于 SFT 冷启动，解决长视频工具调用数据匮乏的问题。

#### 3. 实验设计
*   **数据集/基准测试**：
    *   **MVBench**：涵盖 20 个任务，评估时空推理能力。
    *   **Video-MME**：全面的视频理解基准，包含短、中、长（达 1 小时）视频。
    *   **LongVideoBench**：专注于中长视频的事件感知和关系理解。
*   **对比方法**：包括 InternVL2、Qwen2-VL、Qwen2.5-VL（基座模型）、Video-R1、Video-XL 以及专门的工具集成方法如 Video-MTR 和 LongVT-RL。
*   **基座模型**：选用 Qwen2.5-VL-3B 和 7B 版本。

#### 4. 资源与算力
论文详细列出了使用的算力资源：
*   **SFT 阶段**：使用 8 台 NVIDIA 4090 或 2 台 AMD MI308X。
*   **RL 阶段**：7B 模型使用 8 台 NVIDIA H20（配合 VeRL 框架），3B 模型使用 4 台 AMD MI308X。
*   **推理评估**：使用 8 台 NVIDIA 4090 或 4 台 AMD MI308X。
*   **训练参数**：最大轮数设为 4，每样本采样 8 条轨迹，视频初始采样 32 帧。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准上进行了完整测试，涵盖了从短视频到小时级长视频的场景。
*   **数据准备**：筛选了约 4.5k-9k 条 RL 训练样本，并合成了约 17k 条 SFT 轨迹数据。
*   **消融实验**：针对 TAGPO 算法与标准 GRPO 的效率对比、SFT 策略（正向微调 vs 随机噪声微调）、工具路由的合理性等进行了详细分析。
*   **评价**：实验设计较为充分，通过跨模型规模（3B/7B）和跨硬件平台（NVIDIA/AMD）的验证，表现出较好的客观性和鲁棒性。

#### 6. 主要结论与发现
*   **性能领先**：VideoTIR 在长视频任务上显著优于基座模型，甚至在输入帧数更少的情况下，通过工具检索达到了比全量输入更高的准确率。
*   **效率提升**：TAGPO 算法相比传统 RL 显著加快了收敛速度，有效减少了模型在推理时的冗余工具调用。
*   **长视频优势**：视频越长，VideoTIR 的优势越明显，因为它能通过内部工具链克服 MLLM 上下文窗口对视觉标记的限制。

#### 7. 优点
*   **内生工具设计**：不依赖复杂的外部 API，而是利用模型自身的编码能力设计检索工具，降低了交互开销。
*   **RL 优化创新**：TAGPO 解决了 TIR 领域普遍存在的“工具滥用”问题，使模型推理更加简洁。
*   **数据合成方案**：提出的沙盒合成框架为缺乏细粒度标注的长视频领域提供了一种有效的自动化数据生产路径。

#### 8. 不足与局限
*   **基座模型依赖**：3B 等小规模模型在没有 SFT 冷启动的情况下难以直接进行 Zero-RL，说明该方法对模型初始的指令遵循能力有一定要求。
*   **推理延迟**：虽然减少了标记数量，但多轮交互（Multi-turn）本身会带来额外的推理延迟，论文未详细对比端到端的实时性表现。
*   **复杂场景覆盖**：对于极度复杂的长视频（如多线叙事），工具检索的准确性仍受限于模型最初的“全局扫描”质量。

（完）
