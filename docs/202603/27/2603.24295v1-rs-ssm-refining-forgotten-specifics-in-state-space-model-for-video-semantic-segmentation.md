---
title: "RS-SSM: Refining Forgotten Specifics in State Space Model for Video Semantic Segmentation"
title_zh: RS-SSM：细化状态空间模型中被遗忘细节的视频语义分割方法
authors: "Kai Zhu, Zhenyu Cui, Zehua Zang, Jiahuan Zhou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24295v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于时空视频语义分割的高效状态空间模型
tldr: 针对状态空间模型（SSM）在视频语义分割中因固定状态空间压缩而导致细节信息丢失的问题，本文提出了RS-SSM模型。该模型通过设计通道振幅感知器（CwAP）提取特定信息分布，并利用遗忘门信息精炼器（FGIR）自适应反转遗忘门矩阵，从而补偿压缩过程中丢失的时空细节。实验证明，RS-SSM在四个主流基准测试上达到了SOTA性能，且保持了极高的计算效率。
selection_source: fresh_fetch
motivation: 状态空间模型在压缩过程中不可避免地会遗忘特定的时空细节，限制了其在像素级视频语义分割中的表现。
method: 提出了包含通道振幅感知器（CwAP）和遗忘门信息精炼器（FGIR）的RS-SSM，通过反转遗忘门来精炼和补充丢失的特定信息。
result: 在四个视频语义分割基准数据集上均取得了领先的性能，并兼顾了推理效率。
conclusion: RS-SSM通过有效恢复SSM中被遗忘的细节信息，显著提升了视频语义分割的时空建模能力。
---

## 摘要
最近，状态空间模型通过线性复杂度的状态空间压缩展示了高效的视频分割性能。然而，视频语义分割（VSS）需要像素级的时空建模能力，以维持语义对象分割的时间一致性。虽然状态空间模型在状态空间压缩过程中可以保留共性语义信息，但固定大小的状态空间不可避免地会遗忘特定细节信息，这限制了模型进行像素级分割的能力。为了解决上述问题，我们提出了一种用于视频语义分割的细化细节状态空间模型（RS-SSM），该模型对遗忘的时空细节进行互补细化。具体而言，设计了一个通道级幅度感知器（CwAP）来提取并对齐状态空间中细节信息的分布特征。此外，还提出了一个遗忘门信息细化器（FGIR），根据细节信息分布自适应地反转并细化状态空间模型中的遗忘门矩阵。因此，我们的 RS-SSM 利用反转的遗忘门来互补地细化状态空间压缩过程中遗忘的细节信息，从而增强了模型的时空像素级分割能力。在四个 VSS 基准数据集上的广泛实验表明，我们的 RS-SSM 在保持高计算效率的同时，达到了最先进的性能。代码可在 https://github.com/zhoujiahuan1991/CVPR2026-RS-SSM 获取。

## Abstract
Recently, state space models have demonstrated efficient video segmentation through linear-complexity state space compression. However, Video Semantic Segmentation (VSS) requires pixel-level spatiotemporal modeling capabilities to maintain temporal consistency in segmentation of semantic objects. While state space models can preserve common semantic information during state space compression, the fixed-size state space inevitably forgets specific information, which limits the models' capability for pixel-level segmentation. To tackle the above issue, we proposed a Refining Specifics State Space Model approach (RS-SSM) for video semantic segmentation, which performs complementary refining of forgotten spatiotemporal specifics. Specifically, a Channel-wise Amplitude Perceptron (CwAP) is designed to extract and align the distribution characteristics of specific information in the state space. Besides, a Forgetting Gate Information Refiner (FGIR) is proposed to adaptively invert and refine the forgetting gate matrix in the state space model based on the specific information distribution. Consequently, our RS-SSM leverages the inverted forgetting gate to complementarily refine the specific information forgotten during state space compression, thereby enhancing the model's capability for spatiotemporal pixel-level segmentation. Extensive experiments on four VSS benchmarks demonstrate that our RS-SSM achieves state-of-the-art performance while maintaining high computational efficiency. The code is available at https://github.com/zhoujiahuan1991/CVPR2026-RS-SSM.

---

## 论文详细总结（自动生成）

以下是对论文 **《RS-SSM: Refining Forgotten Specifics in State Space Model for Video Semantic Segmentation》** 的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：视频语义分割（VSS）要求模型在处理长序列时既能保持时空一致性，又能精确捕捉像素级的细节（如物体边界、纹理）。
*   **核心痛点**：新兴的状态空间模型（SSM，如 Mamba）虽然具有线性计算复杂度，适合处理长视频，但其**固定大小的状态空间压缩机制**会导致“细节遗忘”。模型倾向于保留宏观的共性语义（如全局结构），而丢失了对分割至关重要的微调细节（Specifics）。
*   **研究动机**：开发一种能够“找回”并“细化”这些被遗忘细节的 SSM 架构，在不破坏高效性的前提下提升像素级分割精度。

### 2. 方法论
RS-SSM 采用了**双路径 SSM 结构**，核心思想是通过分析主路径遗忘的信息，在辅助路径中进行针对性补偿。
*   **通道振幅感知器 (CwAP)**：
    *   利用 **2D 快速傅里叶变换 (FFT)** 将特征图转换到频域。
    *   通过分析频率能量分布，量化不同通道中“细节信息”（高频成分）的占比，生成频谱特征 $F_t$。
    *   引入**通道信息损失 ($L_{ci}$)**，利用余弦相似度强制模型在不同样本间对齐细节信息的通道分布，使模型能稳定地识别哪些通道富含细节。
*   **遗忘门信息精炼器 (FGIR)**：
    *   **自适应反转**：根据主路径 SSM 的遗忘门矩阵 $A$，计算其反转版本。如果主路径对某个通道进行了强力压缩（遗忘），FGIR 则在细化路径中降低压缩强度。
    *   **动态插值**：结合 CwAP 提取的频谱特征，动态调整反转权重，引导模型重点修复那些包含丰富细节但被主路径遗忘的信息。
*   **算法流程**：输入视频帧 -> 编码器提取特征 -> 线性投影 -> **CwAP 提取细节分布** -> **FGIR 引导双路径 SSM 建模（主路径+细节细化路径）** -> 解码器输出分割掩码。

### 3. 实验设计
*   **数据集**：在四个主流 VSS 基准上进行了验证：
    1.  **VSPW**：大规模野外视频场景（124 类）。
    2.  **NYUv2**：室内环境视频。
    3.  **CamVid**：城市驾驶场景。
    4.  **Cityscapes**：城市街道高分辨率视频。
*   **对比方法**：
    *   **Transformer 类**：SegFormer, Mask2Former。
    *   **视频专用 Transformer**：CFFM, MRCFA, MPVSS。
    *   **SSM 类**：TV3S（此前的 SOTA）。
*   **评价指标**：mIoU（精度）、Params（参数量）、GFLOPs（计算量）、FPS（推理速度）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **软件框架**：基于 MMSegmentation 开发。
*   **训练细节**：使用 AdamW 优化器，初始学习率 $6 \times 10^{-5}$，采用 Poly 学习率策略。输入尺寸根据数据集不同设定（如 480x480 或 512x1024）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从轻量级（MiT-B1, Swin-T）到大规模（MiT-B5, Swin-S）的多种骨干网络。
*   **消融实验**：对比了单路径 SSM、双路径普通 SSM、无 CwAP 引导的 RS-SSM 等变体，验证了每个模块的增量贡献。
*   **客观性**：在不同分辨率和不同应用场景（室内 vs 室外）下均表现出一致的领先地位，且在相同 Backbone 下与 SOTA 方法进行了公平对比。

### 6. 主要结论与发现
*   **性能领先**：在 VSPW 数据集上，使用 MiT-B5 时 mIoU 达到 51.6%，超越了此前最强的 TV3S（49.8%）。
*   **高效性**：RS-SSM 在提升精度的同时，计算开销反而更低。例如在 MiT-B5 下，比 TV3S 减少了 8.2 GFLOPs，FPS 提升了 6.2。
*   **细节恢复**：可视化结果证实，RS-SSM 能够更精确地勾勒出细小物体和复杂边缘，有效解决了 SSM 的模糊问题。

### 7. 优点
*   **理论结合巧妙**：将信号处理中的频域分析（FFT）与 SSM 的遗忘机制结合，理论支撑扎实。
*   **即插即用且轻量**：CwAP 和 FGIR 模块设计精简，几乎不增加推理负担，却显著提升了特征表达能力。
*   **兼顾速度与精度**：成功保持了 SSM 的线性复杂度优势，是长视频实时分割的理想方案。

### 8. 不足与局限
*   **频域假设限制**：方法高度依赖“高频即细节”的假设，在光照剧烈变化或存在高频噪声（如雨雪天气）的视频中，细节提取的准确性可能受影响。
*   **超参数敏感性**：损失函数中的平衡因子 $\lambda_i$ 以及高频带选择数量 $k_h$ 可能需要针对不同数据集进行微调。
*   **应用范围**：目前主要集中在语义分割，尚未探讨在实例分割或全景分割等更复杂任务中的表现。

（完）
