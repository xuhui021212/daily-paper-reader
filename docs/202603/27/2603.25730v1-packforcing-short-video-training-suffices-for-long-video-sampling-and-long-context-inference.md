---
title: "PackForcing: Short Video Training Suffices for Long Video Sampling and Long Context Inference"
title_zh: "PackForcing: 短视频训练足以支持长视频采样与长上下文推理"
authors: "Xiaofeng Mao, Shaohao Rui, Kaining Ying, Bo Zheng, Chuanhao Li, Mingmin Chi, Kaipeng Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25730v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于长上下文推理和长视频采样的效率 KV 缓存策略
tldr: PackForcing 针对自回归视频扩散模型在长视频生成中面临的 KV 缓存增长、重复和误差累积问题，提出了一种创新的三分区 KV 缓存管理策略。该框架通过将历史上下文划分为 Sink、压缩的 Mid 和全分辨率 Recent 令牌，并结合动态 top-k 选择与时间 RoPE 调整，实现了在单 GPU 上生成高质量 2 分钟视频。该研究证明了仅需短视频训练即可实现卓越的长视频推理和 24 倍时间外推能力。
selection_source: fresh_fetch
motivation: 旨在解决自回归视频生成中 KV 缓存线性增长导致的内存瓶颈、时间重复以及长序列推理中的误差累积问题。
method: 提出三阶段 KV 缓存策略，通过双分支网络对中间帧进行 32 倍时空压缩，并利用动态 top-k 选择和时间 RoPE 调整来维持长程一致性。
result: 在单块 H200 GPU 上实现了仅 4GB 固定 KV 缓存下的 2 分钟长视频生成，并在 VBench 上取得了领先的时间一致性和动态性评分。
conclusion: 证明了通过分层上下文压缩和位置编码调整，短视频训练的监督足以支持高质量、长时长的视频合成与推理。
---

## 摘要
自回归视频扩散模型已取得显著进展，但在长视频生成过程中仍面临 KV 缓存（KV-cache）线性增长难以处理、时间重复以及误差累积等瓶颈。为了应对这些挑战，我们提出了 PackForcing，这是一个通过创新的三分区 KV 缓存策略高效管理生成历史的统一框架。具体而言，我们将历史上下文分为三类：(1) Sink token，以全分辨率保留早期锚点帧以维持全局语义；(2) Mid token，通过融合渐进式 3D 卷积与低分辨率 VAE 重编码的双分支网络实现大规模时空压缩（token 减少 32 倍）；(3) Recent token，保持全分辨率以确保局部时间连贯性。为了在不牺牲质量的前提下严格限制内存占用，我们为 Mid token 引入了动态 top-k 上下文选择机制，并配合连续时间 RoPE 调整（Temporal RoPE Adjustment），以极低的开销无缝重新对齐由丢弃 token 引起的位置间隙。得益于这种基于原则的分层上下文压缩，PackForcing 能够在单块 H200 GPU 上生成分辨率为 832x480、帧率为 16 FPS 的连贯 2 分钟视频。它实现了仅 4 GB 的受限 KV 缓存，并支持卓越的 24 倍时间外推（从 5 秒到 120 秒），无论是零样本运行还是仅在 5 秒片段上训练都能有效工作。在 VBench 上的广泛实验结果表明，该模型达到了最先进的时间一致性（26.07）和动态程度（56.25），证明了短视频监督足以实现高质量的长视频合成。

## Abstract
Autoregressive video diffusion models have demonstrated remarkable progress, yet they remain bottlenecked by intractable linear KV-cache growth, temporal repetition, and compounding errors during long-video generation. To address these challenges, we present PackForcing, a unified framework that efficiently manages the generation history through a novel three-partition KV-cache strategy. Specifically, we categorize the historical context into three distinct types: (1) Sink tokens, which preserve early anchor frames at full resolution to maintain global semantics; (2) Mid tokens, which achieve a massive spatiotemporal compression (32x token reduction) via a dual-branch network fusing progressive 3D convolutions with low-resolution VAE re-encoding; and (3) Recent tokens, kept at full resolution to ensure local temporal coherence. To strictly bound the memory footprint without sacrificing quality, we introduce a dynamic top-$k$ context selection mechanism for the mid tokens, coupled with a continuous Temporal RoPE Adjustment that seamlessly re-aligns position gaps caused by dropped tokens with negligible overhead. Empowered by this principled hierarchical context compression, PackForcing can generate coherent 2-minute, 832x480 videos at 16 FPS on a single H200 GPU. It achieves a bounded KV cache of just 4 GB and enables a remarkable 24x temporal extrapolation (5s to 120s), operating effectively either zero-shot or trained on merely 5-second clips. Extensive results on VBench demonstrate state-of-the-art temporal consistency (26.07) and dynamic degree (56.25), proving that short-video supervision is sufficient for high-quality, long-video synthesis. https://github.com/ShandaAI/PackForcing

---

## 论文详细总结（自动生成）

以下是对论文《PackForcing: Short Video Training Suffices for Long Video Sampling and Long Context Inference》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：自回归视频扩散模型在生成长视频时面临三大瓶颈：
    1.  **内存爆炸**：KV 缓存（KV-cache）随视频长度线性增长，生成分钟级视频需消耗上百 GB 显存。
    2.  **误差累积**：预测误差随时间迭代复合，导致语义漂移和质量下降。
    3.  **时间重复**：模型容易陷入循环或生成静态画面。
*   **核心目标**：实现一种在有限显存（如单块 GPU）下，能够通过短视频训练（5秒）生成高质量、长时长（2分钟）且具有时间连贯性的视频生成框架。

### 2. 方法论：PackForcing 框架
该论文提出了**三分区 KV 缓存策略**，将历史上下文解耦为不同功能的三个部分：
*   **Sink Tokens（锚点令牌）**：以全分辨率保留最早生成的 8 帧。它们作为全局语义锚点，防止长序列生成中的语义漂移。
*   **Compressed Mid Tokens（压缩中间令牌）**：
    *   **双分支压缩网络**：融合了 **HR 分支**（渐进式 3D 卷积提取细粒度结构）和 **LR 分支**（低分辨率 VAE 重编码捕捉全局语义），实现 128 倍体积压缩（Token 数量减少 32 倍）。
    *   **动态上下文选择**：基于查询-键（Query-Key）亲和力评分，仅检索 Top-K 个最相关的压缩块参与计算，而非盲目处理全部历史。
*   **Recent Tokens（近期令牌）**：保持全分辨率，确保局部时间转换的平滑性和高保真度。
*   **关键技术细节**：
    *   **时间 RoPE 调整（Temporal RoPE Adjustment）**：针对 Token 丢弃导致的位置编码不连续问题，提出一种仅针对时间轴的增量旋转操作，以极低开销重新对齐位置间隙。
    *   **双分辨率切换**：在生成当前块时同步计算其压缩版本，隐藏压缩延迟。

### 3. 实验设计
*   **数据集/场景**：训练使用 VidProM 数据集；评估使用从 MovieGen 选取的 128 个文本提示词。
*   **Benchmark**：采用 **VBench** 和 **VBench-Long** 评估体系，涵盖动态程度、运动平滑度、整体一致性、图像质量、审美质量、主体一致性和背景一致性 7 个维度。
*   **对比方法**：包括 CausVid、LongLive、Self-Forcing、Rolling Forcing 和 Deep Forcing 等最先进的自回归视频生成基线。
*   **评估时长**：重点对比了 60 秒和 120 秒的生成效果。

### 4. 资源与算力
*   **模型底座**：基于 Wan2.1-T2V-1.3B 开源模型。
*   **硬件环境**：推理在单块 **NVIDIA H200** 或 **A100-80GB** GPU 上完成。
*   **训练细节**：在约 5 秒（20 个潜变量帧）的短片段上训练 3000 次迭代，Batch Size 为 8。
*   **显存优化**：通过 PackForcing，2 分钟视频生成的 KV 缓存被严格限制在约 **4 GB**（相比原始需求的 138 GB 降低了 97% 以上）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 60s 和 120s 的大规模定量对比，并提供了 10s 间隔的 CLIP 分数轨迹分析。
*   **消融实验**：针对 Sink 尺寸、压缩分支（HR vs LR）、丢弃策略（FIFO vs 动态选择）以及 RoPE 调整的有效性进行了详尽测试。
*   **分析深度**：通过经验性分析（注意力模式图）证明了历史 Token 需求是稀疏且跨越全局的，从而支撑了其三分区设计的合理性。实验设计客观、对比公平，涵盖了从短到长的外推能力验证。

### 6. 主要结论与发现
*   **短训长推**：证明了仅需 5 秒的短视频监督，即可实现 24 倍的时间外推（生成 120 秒视频）。
*   **内存恒定**：通过分层压缩，注意力上下文大小保持恒定，实现了 O(1) 的注意力复杂度。
*   **性能领先**：在 VBench 动态程度（56.25）和时间一致性上达到 SOTA，且 CLIP 分数随时间下降最慢，表现出极强的抗漂移能力。

### 7. 优点
*   **极高的压缩效率**：32 倍的 Token 压缩率远超同类方法，显著降低了长视频生成的硬件门槛。
*   **创新的位置对齐**：Temporal RoPE Adjustment 巧妙解决了缓存管理中的位置断裂问题。
*   **动态性强**：相比于为了维持一致性而牺牲运动的基线（如 LongLive），PackForcing 在保持一致性的同时保留了丰富的动态效果。

### 8. 不足与局限
*   **主体一致性微降**：在追求高动态性的过程中，主体一致性评分略低于某些极其保守的基线（如 LongLive）。
*   **固定压缩率**：目前的压缩比例是固定的，可能无法针对不同复杂度的场景（如极简 vs 极繁）进行自适应调整。
*   **分辨率限制**：实验主要集中在 832x480 分辨率，尚未深入探讨在 1080p 或更高分辨率下的时空压缩表现。

（完）
