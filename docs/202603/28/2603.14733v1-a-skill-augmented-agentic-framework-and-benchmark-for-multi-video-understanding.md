---
title: A Skill-augmented Agentic Framework and Benchmark for Multi-Video Understanding
title_zh: 一种用于多视频理解的技能增强智能体框架与基准
authors: "Yue Zhang, Liqiang Jing, Jia Li, Yapeng Tian, Xinya Du, Yunhui Guo, Vibhav Gogate"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14733v1"
tags: ["query:lvu"]
score: 8.0
evidence: 多视频理解的智能体框架和基准测试
tldr: 针对多视频理解中存在的推理受限、信息丢失及缺乏跨视频协调等问题，本文提出了MVX-Bench基准测试，涵盖11类视觉任务。同时，提出了一种名为SAMA的技能增强型智能体框架，通过集成视觉工具、特定任务技能和冲突感知验证机制，实现了迭代式结构化推理。实验证明SAMA在多视频理解任务上优于现有开源模型及GPT。
selection_source: carryover_cache
motivation: 现有多视频理解方法在处理跨视频协调、身份匹配和细粒度辨析方面存在局限，且缺乏全面的评估基准。
method: 提出了SAMA框架，通过整合视觉工具、任务特定技能和冲突感知验证机制，实现多视频的迭代式结构化推理。
result: "SAMA在包含1,442个问题的MVX-Bench基准测试中表现优异，显著超越了强力开源基准模型和GPT。"
conclusion: 本研究通过新基准和智能体框架，有效解决了多视频理解中的结构化推理难题，提升了复杂场景下的分析能力。
---

## 摘要
多模态大语言模型在单视频理解方面已取得显著进展，但其跨多视频的推理能力仍然有限。现有方法通常将多个视频拼接为单个输入并进行直接推理，这引入了训练与推理不匹配、帧压缩导致的信息损失以及缺乏显式跨视频协作等问题。同时，现有的多视频基准主要侧重于事件级比较，而对身份级匹配、细粒度辨别和结构化多步推理的探索不足。为了填补这些空白，我们推出了 MVX-Bench，这是一个多视频跨维度基准，它将 11 个经典计算机视觉任务重新构建为一个统一的多视频问答框架，包含来自不同真实世界数据集的 4,255 个视频上的 1,442 个问题。我们进一步提出了 SAMA，一种用于多视频理解的技能增强智能体框架，它集成了视觉工具、特定任务技能和冲突感知验证机制，以实现迭代式和结构化推理。实验结果表明，SAMA 在 MVX-Bench 上的表现优于强开源基准模型和 GPT，消融实验验证了技能设计和冲突解决的有效性。

## Abstract
Multimodal Large Language Models have achieved strong performance in single-video understanding, yet their ability to reason across multiple videos remains limited. Existing approaches typically concatenate multiple videos into a single input and perform direct inference, which introduces training-inference mismatch, information loss from frame compression, and a lack of explicit cross-video coordination. Meanwhile, current multi-video benchmarks primarily emphasize event-level comparison, leaving identity-level matching, fine-grained discrimination, and structured multi-step reasoning underexplored. To address these gaps, we introduce MVX-Bench, a Multi-Video Cross-Dimension Benchmark that reformulates 11 classical computer vision tasks into a unified multi-video question-answering framework, comprising 1,442 questions over 4,255 videos from diverse real-world datasets. We further propose SAMA, a Skill-Augmented Agentic Framework for Multi-Video Understanding, which integrates visual tools, task-specific skills, and a conflict-aware verification mechanism to enable iterative and structured reasoning. Experimental results show that SAMA outperforms strong open-source baselines and GPT on MVX-Bench, and ablations validate the effectiveness of skill design and conflict resolution.

---

## 论文详细总结（自动生成）

### 论文总结：A Skill-augmented Agentic Framework and Benchmark for Multi-Video Understanding

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLMs）虽然在单视频理解上表现出色，但在处理**多视频（Multi-Video）**场景时面临严峻挑战。现有的主流做法是将多个视频帧拼接后直接输入模型，这导致了三个主要问题：
*   **训练-推理不匹配**：模型大多在单视频数据上训练，难以处理跨视频的协调逻辑。
*   **信息丢失**：为了适应上下文窗口，多视频输入往往需要剧烈的帧压缩。
*   **缺乏显式协调**：模型难以在多个视频流之间进行身份对齐、细粒度辨别和多步逻辑推理。
此外，现有的多视频基准测试多集中在简单的事件比较，缺乏对身份匹配、艺术风格抽象及复杂逻辑推理（如反事实推理）的深度评估。

#### 2. 方法论：SAMA 框架
论文提出了 **SAMA（Skill-Augmented Agentic Framework）**，这是一个专门为多视频理解设计的智能体框架，其核心由两层组成：

*   **智能体核心（Agent Core）**：
    *   **规划器（Planner）**：采用纯文本 LLM（如 GPT-5.2）作为大脑，负责任务分解和工具调用决策。
    *   **工具箱（Tools）**：集成 8 种工具，包括感知类（Video Reader, Temporal Grounding）、检测类（Scene Graph, Object Tracker）和其他类（Visual Similarity, Subtitle Retriever）。
    *   **技能库（Skills）**：这是 SAMA 的关键创新。技能是针对特定任务（如跨视频比较、计数、时间定位）的结构化行为指南，规定了工具调用的顺序、触发条件和停止准则，弥补了 LLM 无法高效编排工具的缺陷。
*   **外部验证层（External Verification Layer）**：
    *   **冲突检测与自适应重读**：当不同工具（如 VLM 感知与目标检测器）输出矛盾时，触发自适应重读机制，引导 Planner 调用更多工具进行验证。
    *   **证据聚合**：维护一个结构化存储字典，记录并蒸馏每个视频的验证证据，防止长上下文导致的遗忘。

#### 3. 实验设计
*   **Benchmark (MVX-Bench)**：
    *   涵盖 **11 个任务**：计数、伪造检测、动作匹配、行人重识别、艺术风格识别、多视角推理、视频相似度、反事实推理、可废止蕴含（Defeasible Entailment）、序列推理、空间关系。
    *   规模：包含 **1,442 个问题**，涉及 **4,255 个视频**，数据源自 11 个真实的计算机视觉数据集。
    *   格式：统一转化为多选题（MCQ）形式。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-5.2。
    *   **开源模型**：涵盖 Large (≥14B)、Medium (7B-14B)、Small (<7B) 三个梯度的 20 个模型，如 InternVL3.5 系列、Qwen2.5-VL 系列、Gemma-3 系列等。

#### 4. 资源与算力
论文**未明确说明**具体的训练算力（如 GPU 型号、数量或训练时长）。这主要是因为 SAMA 是一个**推理侧的智能体框架**，它通过调用现有的预训练模型（如 Qwen2.5-VL-7B 作为 Video Reader）和 API（如 GPT-5.2 作为 Planner）来工作，而非从头训练一个大型多模态模型。

#### 5. 实验数量与充分性
*   **实验规模**：在 11 个任务维度上对 20 个基准模型进行了全面测试，样本量（1,442）足以支撑统计意义上的结论。
*   **消融实验**：针对“技能设计（Skills）”和“冲突检测（Conflict Detection）”模块进行了专门的消融研究（样本量 350），验证了各组件的增益。
*   **公平性**：所有模型均在相同的多选题框架下评估，SAMA 使用的视觉骨干仅为 7B 规模，与其对比的包括 38B 甚至闭源巨型模型，体现了实验的客观性。

#### 6. 主要结论与发现
*   **多视频理解远未解决**：即使是表现最好的 SAMA，整体准确率也仅为 52.7%，许多任务（如反事实推理、空间关系）接近随机水平。
*   **规模效应非万能**：增大模型参数能提升视觉关联任务，但在逻辑推理任务上提升微乎其微。
*   **智能体框架的优越性**：SAMA 仅使用 7B 的视觉骨干，通过结构化编排，性能提升了 **20.1%**，并超越了 GPT-4o 和 InternVL3.5-38B。
*   **技能与验证的重要性**：显式的技能引导和冲突解决机制是提升推理可靠性的核心。

#### 7. 优点
*   **任务维度全面**：MVX-Bench 突破了简单的事件对齐，引入了身份、风格、取证和高阶逻辑推理，是目前最全面的多视频基准之一。
*   **模块化设计**：SAMA 框架不依赖于特定的视觉模型，具有很强的可扩展性和通用性。
*   **冲突处理机制**：引入了跨工具的冲突检测，有效解决了 VLM 在细粒度感知（如计数、颜色）上的幻觉问题。

#### 8. 不足与局限
*   **评估形式单一**：目前仅限于多选题（MCQ），未能涵盖开放式生成或自由对话场景。
*   **推理成本**：作为智能体框架，SAMA 需要多次迭代调用 LLM 和视觉工具，其推理延迟和计算成本显著高于单次推理模型。
*   **依赖 Planner 能力**：框架性能高度依赖于底层文本 LLM（如 GPT-5.2）的规划能力，若 Planner 逻辑出错，整个流程会失效。

（完）
