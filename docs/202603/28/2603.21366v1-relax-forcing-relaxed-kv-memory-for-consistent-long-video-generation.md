---
title: "Relax Forcing: Relaxed KV-Memory for Consistent Long Video Generation"
title_zh: Relax Forcing：用于一致性长视频生成的松弛 KV 内存
authors: "Zengqun Zhao, Yanzuo Lu, Ziquan Liu, Jifei Song, Jiankang Deng, Ioannis Patras"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21366v1"
tags: ["query:lvu"]
score: 8.0
evidence: 用于一致性长视频生成的松弛KV内存
tldr: 针对自回归视频扩散模型在长视频生成中出现的时序退化问题，本文提出 Relax Forcing 机制。研究发现长视频生成的瓶颈在于时序记忆的使用方式而非容量。该方法将 KV 缓存分解为 Sink（全局稳定）、Tail（短期连续）和动态选择的 History（结构化运动引导）三个功能角色，有效减少了误差累积并提升了运动动力学表现，在 VBench-Long 评测中展现了更优的连贯性与效率。
selection_source: fresh_fetch
motivation: 现有的自回归视频生成模型在长时生成时面临严重的性能退化，且单纯增加记忆长度并不能有效解决运动不一致的问题。
method: 提出 Relax Forcing 结构化记忆机制，通过将时序上下文分解为 Sink、Tail 和动态 History 三部分，选择性地利用最相关的历史信息。
result: 在 VBench-Long 实验中，该方法显著提升了长视频的运动动力学和时序一致性，同时降低了注意力计算开销。
conclusion: 结构化的时序记忆管理是实现可扩展长视频生成的关键，能有效补充现有的自回归训练策略并提升生成质量。
---

## 摘要
自回归 (AR) 视频扩散最近已成为长视频生成的一种极具前景的范式，能够实现超越双向模型限制的因果合成。为了解决训练与推理不匹配的问题，研究者提出了一系列自强制 (self-forcing) 策略，通过在训练期间以模型自身的预测为条件来提高推演稳定性。虽然这些方法显著减轻了曝光偏差 (exposure bias)，但由于渐进的时间退化，将生成长度扩展到分钟级仍然具有挑战性。在这项工作中，我们证明了这种局限性主要不是由内存不足引起的，而是由推理过程中时间内存的使用方式决定的。通过实证分析，我们发现增加内存并不能持续改善长时程生成，且历史上下文的时间位置会显著影响运动动力学，而对视觉质量的影响微乎其微。这些发现表明，时间内存不应被视为同质缓冲区。受此启发，我们引入了 Relax Forcing，这是一种用于 AR 扩散的结构化时间内存机制。Relax Forcing 不再关注密集的生成历史，而是将时间上下文分解为三个功能角色：用于全局稳定性的 Sink（锚点）、用于短期连续性的 Tail（尾部）以及用于结构化运动引导的动态选择 History（历史），并有选择地仅整合最相关的过去信息。这种设计在保留运动演化的同时，减轻了外推过程中的误差累积。在 VBench-Long 上的实验表明，Relax Forcing 在减少注意力开销的同时，改善了运动动力学和整体时间一致性。我们的结果表明，结构化时间内存对于可扩展的长视频生成至关重要，是对现有基于强制 (forcing-based) 训练策略的补充。

## Abstract
Autoregressive (AR) video diffusion has recently emerged as a promising paradigm for long video generation, enabling causal synthesis beyond the limits of bidirectional models. To address training-inference mismatch, a series of self-forcing strategies have been proposed to improve rollout stability by conditioning the model on its own predictions during training. While these approaches substantially mitigate exposure bias, extending generation to minute-scale horizons remains challenging due to progressive temporal degradation. In this work, we show that this limitation is not primarily caused by insufficient memory, but by how temporal memory is utilised during inference. Through empirical analysis, we find that increasing memory does not consistently improve long-horizon generation, and that the temporal placement of historical context significantly influences motion dynamics while leaving visual quality largely unchanged. These findings suggest that temporal memory should not be treated as a homogeneous buffer. Motivated by this insight, we introduce Relax Forcing, a structured temporal memory mechanism for AR diffusion. Instead of attending to the dense generated history, Relax Forcing decomposes temporal context into three functional roles: Sink for global stability, Tail for short-term continuity, and dynamically selected History for structural motion guidance, and selectively incorporates only the most relevant past information. This design mitigates error accumulation during extrapolation while preserving motion evolution. Experiments on VBench-Long demonstrate that Relax Forcing improves motion dynamics and overall temporal consistency while reducing attention overhead. Our results suggest that structured temporal memory is essential for scalable long video generation, complementing existing forcing-based training strategies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Relax Forcing: Relaxed KV-Memory for Consistent Long Video Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自回归（AR）视频扩散模型在生成长视频（如分钟级）时，面临严重的**时序退化**问题，表现为语义漂移（Identity 改变）或运动动力学丧失（画面变得僵硬、重复）。
*   **研究背景**：虽然现有的“自强制”（Self Forcing）训练策略缓解了曝光偏差，但长视频外推的瓶颈在于推理阶段如何利用**时间记忆（KV Cache）**。
*   **核心动机**：作者发现，单纯增加记忆长度并不能持续提升质量，反而可能引入冗余并抑制运动。时间记忆不应被视为同质的缓冲区，不同位置的历史帧对生成稳定性和运动演化起着不同的功能性作用。

### 2. 方法论：Relax Forcing 机制
该方法是一种**无需训练（Training-free）**的结构化稀疏内存机制，核心思想是将 KV 缓存分解为三个功能角色：
*   **Sink（全局锚点）**：保留视频开头的固定帧（如前 2 帧），用于维持全局身份一致性和场景稳定性。
*   **Tail（短期上下文）**：保留紧邻当前生成块的最近帧（如最后 1 帧），确保动作的短期连贯性。
*   **History（动态中程结构）**：从中间候选区域动态选择最具代表性的帧。
    *   **动态选择算法**：计算候选帧与 Sink 的相似度（稳定性得分 $S$）以及与 Tail 的相似度（冗余得分 $R$）。
    *   **松弛得分公式**：$r(h) = S(h) - \lambda R(h)$。通过该得分进行 Top-K 排名，选择既能对齐全局背景又能避免与近期动作重复的帧。
*   **混合位置编码调整**：针对非连续的内存结构，对 Tail 使用绝对位置编码，而对 Sink 和 History 使用相对于 Tail 的相对位置编码，防止模型因位置索引混乱而无法利用长程信息。

### 3. 实验设计
*   **数据集与场景**：使用 **VBench-Long** 基准测试，包含从 MovieGen 提取并经 Qwen2.5-7B 优化的 128 个复杂提示词。
*   **生成时长**：评估了 30 秒和 60 秒的长视频生成效果。
*   **对比方法**：
    *   **训练相关方法**：CausVid, Self Forcing, Rolling Forcing, LongLive。
    *   **推理优化方法**：Deep Forcing（基于注意力评分压缩内存）。
*   **评估指标**：主体一致性、背景一致性、审美质量、成像质量、运动平滑度、动态程度（Dynamic Degree）以及人工偏好评分。

### 4. 资源与算力
*   **硬件环境**：推理吞吐量（FPS）评估是在单张 **NVIDIA H100 GPU** 上进行的。
*   **训练时长**：由于该方法是**无需训练（Training-free）**的插件式机制，它直接应用于预训练好的 Self Forcing 模型上，因此不涉及额外的训练算力消耗。
*   **推理效率**：相比全量注意力的 Baseline，Relax Forcing 将有效注意力长度从 21 帧减少到 7 帧，实现了约 **1.26倍的端到端加速**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 30s 和 60s 两个时长进行了全量对比实验。
    *   进行了详尽的**消融实验**，包括：内存组件（Sink/Tail/History）的必要性、历史帧选择策略（随机 vs 固定 vs 动态）、候选池大小的影响、以及超参数 $\lambda$ 的敏感度分析。
    *   包含 128 个提示词的多轮生成（每组 5 次重复以确保鲁棒性）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅提供了定量指标，还引入了“漂移-重复平衡”（Balance）指标和人工双盲评测，从多维度验证了稀疏结构化内存的优越性。

### 6. 主要结论与发现
*   **内存并非越多越好**：密集的历史信息会引入冗余，导致模型“偷懒”生成微小运动，从而降低视频的动态感。
*   **结构化胜过压缩**：相比于 Deep Forcing 等基于注意力权重的压缩方法，基于功能角色（Sink/Tail/History）的显式划分能更好地平衡稳定性与运动演化。
*   **性能提升**：在 VBench-Long 上，Relax Forcing 在动态程度（Dynamic Degree）上提升了 **66.8%**，整体得分提升了 **5.7%**，且在 60 秒超长生成中表现出极强的鲁棒性。

### 7. 优点
*   **即插即用**：无需重新训练模型，可直接提升现有自回归视频生成系统的长视频能力。
*   **高效性**：通过稀疏化 KV 缓存，显著降低了长序列生成的计算开销和内存占用。
*   **运动增强**：有效解决了长视频生成中常见的“画面静止”或“动作僵硬”问题，使视频在分钟级别仍能保持自然的运动演化。

### 8. 不足与局限
*   **依赖基础模型**：作为一种推理策略，其上限仍受限于底层自回归扩散模型的建模能力。
*   **超参数依赖**：虽然实验证明 $\lambda$ 相对鲁棒，但在极端不同的视频风格（如极速运动 vs 静态风景）下，可能仍需要微调内存配比（Sink/Tail 的数量）。
*   **复杂场景挑战**：对于涉及多个复杂实体交互的长视频，仅靠 1-2 帧的历史帧选择可能不足以捕捉所有关键的结构化运动引导。

（完）
