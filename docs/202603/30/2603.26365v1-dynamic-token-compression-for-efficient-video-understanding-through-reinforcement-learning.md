---
title: Dynamic Token Compression for Efficient Video Understanding through Reinforcement Learning
title_zh: 通过强化学习实现高效视频理解的动态 Token 压缩
authors: "Shida Wang, YongXiang Hua, Zhou Tao, Haoyu Cao, Linli Xu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26365v1"
tags: ["query:lvu"]
score: 9.0
evidence: 通过自适应令牌压缩实现高效视频理解
tldr: "针对多模态大模型在视频理解中面临的计算开销大和冗余Token导致的性能下降问题，本文提出了SCORE框架。该框架利用强化学习训练自适应Token压缩策略，通过引入包含帧间残差的“惊喜增强”状态表示来捕捉动态信息。实验表明，SCORE在保持99.5%性能的同时实现了16倍的预填充加速，为长视频理解提供了高效且可扩展的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26365v1/fig-001.webp\", \"caption\": \"Fig. 2: SCORE pipeline. A lightweight visual compressor is inserted between the frozen vision encoder and the frozen LLM. During training, we optimize a surpriseaugmented Bernoulli gating policy with group rollouts and an accuracy–sparsity reward. During inference, we use deterministic global top-K selection to meet a target budget.\", \"page\": 6, \"index\": 1, \"width\": 723, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2603.26365v1/fig-002.webp\", \"caption\": \"Fig. 1: (a) Visual token redundancy induces context rot in video MLLMs. (b) A comparision of existing token compression methods and SCORE.\", \"page\": 2, \"index\": 2, \"width\": 577, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2603.26365v1/fig-003.webp\", \"caption\": \"Table 1: Main results on Qwen2.5-VL [2] across three video understanding benchmarks. We compare SCORE with the Vanilla baseline and state-of-the-art token reduction methods at varying retention ratios (R). The best results in each block are\", \"page\": 10, \"index\": 3, \"width\": 719, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2603.26365v1/fig-004.webp\", \"caption\": \"Fig. 5: Qualitative visualization of SCORE token masks. Top two rows: six pseudo-video inputs constructed from static images, where SCORE concentrates kept tokens on salient objects and action-related regions while pruning static backgrounds. Bottom two rows: two real-video examples, showing temporally adaptive masks that shift as motion and scene content evolve.\", \"page\": 14, \"index\": 4, \"width\": 719, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.26365v1/fig-005.webp\", \"caption\": \"Fig. 4: Pseudo-video samples for curriculum warm-up. We construct short clips by repeating each sampled image for several frames and concatenate their captions into a single training target. This synthesis produces near-zero inter-frame residuals within repeated segments and sharp changes at image boundaries, offering high-contrast temporal signals for learning redundancy-aware token pruning.\", \"page\": 9, \"index\": 5, \"width\": 725, \"height\": 271}]"
motivation: 现有的视频Token压缩方法多依赖启发式规则，难以适应下游任务且无法有效处理视频中的冗余与动态变化。
method: 提出SCORE框架，利用轻量级策略网络和强化学习，结合帧间残差的惊喜增强状态表示来实现自适应Token压缩。
result: "在多个基准测试中显著优于现有方法，仅保留10%的Token即可维持99.5%的性能，并实现16倍的推理加速。"
conclusion: SCORE通过强化学习实现了高效的动态Token压缩，平衡了视频理解的计算效率与模型性能。
---

## 摘要
多模态大语言模型在视频理解方面展现了卓越的能力，但由于海量的视觉 Token 冗余，面临着极高的计算成本以及由“上下文腐烂”导致的性能下降。现有的压缩策略通常依赖于启发式方法或固定变换，这些方法往往与下游任务目标脱节，限制了其适应性和有效性。为解决这一问题，我们提出了 SCORE（基于强化学习的惊奇度增强 Token 压缩），这是一个学习自适应 Token 压缩策略的统一框架。SCORE 引入了一个轻量级策略网络，该网络以惊奇度增强的状态表示为条件，其中结合了帧间残差，以显式捕捉时间动态和运动显著性。我们使用带有分裂优势估计器的分组强化学习方案来优化该策略，并通过从静态伪视频到真实动态视频的两阶段课程学习来确保训练稳定性。在多种视频理解基准测试上的广泛实验表明，SCORE 显著优于现有的最先进基准方法。值得注意的是，SCORE 在 10% 的保留率下实现了 16 倍的预填充加速，同时保留了 99.5% 的原始性能，为高效的长视频理解提供了一个可扩展的解决方案。

## Abstract
Multimodal Large Language Models have demonstrated remarkable capabilities in video understanding, yet face prohibitive computational costs and performance degradation from ''context rot'' due to massive visual token redundancy. Existing compression strategies typically rely on heuristics or fixed transformations that are often decoupled from the downstream task objectives, limiting their adaptability and effectiveness. To address this, we propose SCORE (Surprise-augmented token COmpression via REinforcement learning), a unified framework that learns an adaptive token compression policy. SCORE introduces a lightweight policy network conditioned on a surprise-augmented state representation that incorporates inter-frame residuals to explicitly capture temporal dynamics and motion saliency. We optimize this policy using a group-wise reinforcement learning scheme with a split-advantage estimator, stabilized by a two-stage curriculum transferring from static pseudo-videos to real dynamic videos. Extensive experiments on diverse video understanding benchmarks demonstrate that SCORE significantly outperforms state-of-the-art baselines. Notably, SCORE achieves a 16x prefill speedup while preserving 99.5% of original performance at a 10% retention ratio, offering a scalable solution for efficient long-form video understanding.

---

## 论文详细总结（自动生成）

### 论文总结：SCORE —— 通过强化学习实现高效视频理解的动态 Token 压缩

#### 1. 核心问题与整体含义（研究动机和背景）
现代多模态大语言模型（MLLMs）在处理长视频时面临两大挑战：
*   **计算开销巨大**：视觉编码器产生的 Token 数量极多，由于自注意力机制的平方复杂度，导致推理延迟和内存占用极高。
*   **上下文腐烂（Context Rot）**：海量的冗余 Token（如静态背景、重复动作）会淹没关键语义信息，导致模型在处理长序列时性能反而下降。
现有的压缩方法（如池化、启发式剪枝）往往与下游任务脱节，缺乏内容感知能力。本文提出的 **SCORE** 旨在通过强化学习（RL）训练一个轻量级的自适应压缩策略，在大幅减少 Token 数量的同时，保持甚至提升模型的理解能力。

#### 2. 方法论：核心思想与关键技术
SCORE 框架在冻结的视觉编码器和 LLM 之间插入一个轻量级的 **Token 级压缩器**。
*   **核心思想**：利用强化学习直接优化下游任务的生成质量，使压缩器学会识别哪些 Token 对回答问题最重要。
*   **关键技术细节**：
    *   **惊奇度增强的状态表示（Surprise-augmented State）**：输入不仅包含原始 Token 嵌入 $X_t$，还引入了帧间残差 $\Delta X_t = X_t - X_{t-1}$。这种“惊奇信号”能显式捕捉运动显著性和时间动态，打破时间上的对称性。
    *   **伯努利采样策略**：轻量级 MLP 输出每个 Token 的保留概率，通过伯努利分布采样生成离散的掩码（Mask）。
    *   **分组强化学习与分裂优势估计器（Split-advantage Estimator）**：
        *   针对每个视频进行 $K$ 次采样（Group Rollouts），以降低梯度方差。
        *   将采样结果分为“安全区”（性能达标）和“惩罚区”（性能下降）。在安全区鼓励更高稀疏度，在惩罚区则施加权重惩罚以恢复性能。
    *   **两阶段课程学习**：
        1.  **伪视频预热**：使用静态图像拼接成的伪视频进行训练，利用其极高的帧间对比度让模型快速学会识别冗余。
        2.  **真实视频微调**：在真实视频数据集上精调，处理更细微的运动和噪声。

#### 3. 实验设计
*   **基础模型**：Qwen2.5-VL (7B) 和 LLaVA-Video。
*   **评测基准（Benchmarks）**：
    *   **Video-MME**：全面的视频分析评测。
    *   **MLVU**：多任务长视频理解。
    *   **LVBench**：极长视频理解基准。
*   **对比方法**：包括 DyCoke (CVPR'25)、HoliTom (NeurIPS'25)、VidCom 2 (EMNLP'25) 和 FastVID (NeurIPS'25) 等最先进的训练/非训练压缩方法。
*   **保留率设置**：测试了 10%、25% 和 40% 三种不同的 Token 保留比例。

#### 4. 资源与算力
*   **教师模型**：使用了 Qwen3-VL-235B 和 Qwen2.5-VL-72B 来生成高质量的训练标注（Dense Captions）。
*   **训练数据**：包含约 10K 伪视频样本和 57K 真实视频-文本对（来自 Koala-36M 和 LLaVA-Video-178K）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长，但提到压缩器本身是一个极轻量级的 MLP，推理时的额外计算开销占比小于 1%。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准上进行了详尽测试，涵盖了从短视频到 1 小时以上长视频的场景。
*   **消融实验**：针对“课程学习策略”和“惊奇度增强（残差信号）”分别做了对比实验，验证了各组件的有效性。
*   **客观性**：通过在不同架构（Qwen 和 LLaVA）上进行实验，证明了方法的通用性；采用多种保留率进行对比，展示了性能-效率的权衡曲线。实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **高效性**：在 10% 的保留率下，实现了 **16.2 倍** 的预填充（Prefill）加速，且保留了 99.5% 的原始性能。
*   **性能提升**：在 25% 的保留率下，SCORE 的平均得分（58.9）甚至**超过了不压缩的原始模型**（57.3），证明了去除冗余 Token 能有效缓解“上下文腐烂”问题。
*   **自适应性**：定性分析显示，SCORE 能自动将 Token 分配给运动物体和关键动作区域，而剪枝掉背景等静态冗余。

#### 7. 优点
*   **端到端优化**：不同于启发式方法，SCORE 直接优化下游任务的奖励，使压缩更具语义相关性。
*   **创新的状态编码**：引入帧间残差作为“惊奇度”信号，极大地提高了模型对动态变化的敏感度。
*   **训练稳定性**：通过分裂优势估计器和课程学习，成功解决了强化学习在巨大动作空间（数万个 Token 的选择）中难以收敛的问题。

#### 8. 不足与局限
*   **训练依赖教师模型**：需要强大的教师模型生成高质量标注作为奖励信号，这增加了训练准备阶段的成本。
*   **固定保留率限制**：虽然推理时可以手动设置保留率，但策略网络在训练时可能对特定的稀疏度范围有偏好。
*   **实时性挑战**：虽然预填充速度大幅提升，但强化学习训练过程本身比启发式方法更复杂，且需要针对不同模型重新训练压缩器。

（完）
