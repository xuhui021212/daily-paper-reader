---
title: "Low-Rank-Modulated Functa: Exploring the Latent Space of Implicit Neural Representations for Interpretable Ultrasound Video Analysis"
title_zh: 低秩调制 Functa：探索隐式神经表示的潜空间以进行可解释的超声视频分析
authors: "Julia Wolleb, Cristiana Baloescu, Alicia Durrer, Hemant D. Tagare, Xenophon Papademetris"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25951v1"
tags: ["query:medvid"]
score: 10.0
evidence: 使用低秩调制隐式神经表示进行可解释的超声视频分析
tldr: 本研究针对超声视频分析，提出了一种名为Low-Rank-Modulated Functa (LRM-Functa) 的新型隐式神经表示架构。该方法通过在时间分辨潜空间中引入低秩调制约束，使潜空间呈现出清晰的周期性轨迹，显著增强了模型的可解释性。LRM-Functa不仅能实现高效的视频压缩，还能在无需额外训练的情况下，通过潜空间遍历实现心脏关键帧的无监督检测，并在射血分数预测和肺部超声分类等任务中展现出优异的性能和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25951v1/fig-001.webp\", \"caption\": \"Fig. 3. For k = 2, we visualize the raw trajectories ϕt over time. LRM-Functao exhibits spiral-like trajectories, whereas LRM-Functab collapses the trajectory to a line. Projecting ϕt onto the principal motion direction p and filtering yields the 1D signal sfilt, which enables direct interpretable identification of ED and ES frames.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.25951v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of our LRM-Functa framework. A) Our approach builds on VidFuncta, where each ultrasound video is represented by a video-level modulation vector v and time-resolved modulation vectors ϕ = {ϕt}Tt=1. (B) Pairwise cosine similarity of ϕ over time reveals a clear low-rank pattern. (C) Motivated by this observation, we constrain the latent modulation space to a low-rank subspace Bβ . (D/E) This structured latent space enables efficient compression-reconstruction and cardiac phase analysis.\", \"page\": 2, \"index\": 2, \"width\": 725, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.25951v1/fig-003.webp\", \"caption\": \"Table 1. Mean ± standard deviation for the ED and ES MAE on the EchoNet-Dynamic and the OOD POCUS test sets. All methods are unsupervised.\", \"page\": 8, \"index\": 3, \"width\": 716, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2603.25951v1/fig-004.webp\", \"caption\": \"Table 2. Mean ± std of downstream performance on V̂, evaluated on the reconstructed EchoNet-Dynamic and LUS test sets using 5-fold cross-validation.\", \"page\": 8, \"index\": 4, \"width\": 721, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.25951v1/fig-005.webp\", \"caption\": \"Fig. 2. Proposed architecture for low-rank adaptation of the modulation vectors. Each video V is encoded into a video-level representation v and time-resolved frame representations ϕt, which are projected onto a rank-k subspace Bβ to produce modulation vectors mt = v+Bβϕt. These vectors shift the activations of a shared MLP Mθ, trained to reconstruct pixel intensities ẑ at each coordinate (x, y), shown as a red cross.\", \"page\": 4, \"index\": 5, \"width\": 725, \"height\": 363}]"
motivation: 现有的基于Functa的隐式神经表示模型在处理超声视频时，其潜空间缺乏结构化特征和临床可解释性。
method: 提出LRM-Functa架构，通过在时间维度的潜空间调制向量中实施低秩自适应，以捕捉视频中的周期性时空模式。
result: 该方法在无监督检测心脏舒张末期和收缩末期帧方面优于前人研究，且在极高压缩比下仍能保持准确的临床指标预测能力。
conclusion: LRM-Functa为超声视频分析提供了一个紧凑、可解释且通用的框架，成功实现了高效数据表示与深度临床洞察的结合。
---

## 摘要
隐式神经表示 (INR) 已成为连续图像表示学习的一种强大框架。在基于 Functa 的方法中，每张图像被编码为一个潜调制向量，用于调节共享的 INR，从而实现强大的重建性能。然而，相应潜空间的结构和可解释性在很大程度上仍未得到探索。在这项工作中，我们研究了用于超声视频的基于 Functa 模型的潜空间，并提出了低秩调制 Functa (LRM-Functa)，这是一种在时间分辨潜空间中强制执行调制向量低秩自适应的新型架构。当应用于心脏超声时，生成的潜空间表现出结构清晰的周期性轨迹，有助于时间模式的可视化和解释。可以遍历潜空间以采样新帧，揭示心动周期中的平滑过渡，并能够在无需额外模型训练的情况下直接读取舒张末期 (ED) 和收缩末期 (ES) 帧。我们证明了 LRM-Functa 在无监督 ED 和 ES 帧检测方面优于先前的方法，同时将每个视频帧压缩到低至秩 k=2，且不牺牲在射血分数预测上的竞争性下游性能。在心脏床旁超声数据集的分布外帧选择以及肺部超声 B 线分类上的评估证明了我们方法的泛化能力。总的来说，LRM-Functa 为超声视频分析提供了一个紧凑、可解释且可泛化的框架。

## Abstract
Implicit neural representations (INRs) have emerged as a powerful framework for continuous image representation learning. In Functa-based approaches, each image is encoded as a latent modulation vector that conditions a shared INR, enabling strong reconstruction performance. However, the structure and interpretability of the corresponding latent spaces remain largely unexplored. In this work, we investigate the latent space of Functa-based models for ultrasound videos and propose Low-Rank-Modulated Functa (LRM-Functa), a novel architecture that enforces a low-rank adaptation of modulation vectors in the time-resolved latent space. When applied to cardiac ultrasound, the resulting latent space exhibits clearly structured periodic trajectories, facilitating visualization and interpretability of temporal patterns. The latent space can be traversed to sample novel frames, revealing smooth transitions along the cardiac cycle, and enabling direct readout of end-diastolic (ED) and end-systolic (ES) frames without additional model training. We show that LRM-Functa outperforms prior methods in unsupervised ED and ES frame detection, while compressing each video frame to as low as rank k=2 without sacrificing competitive downstream performance on ejection fraction prediction. Evaluations on out-of-distribution frame selection in a cardiac point-of-care dataset, as well as on lung ultrasound for B-line classification, demonstrate the generalizability of our approach. Overall, LRM-Functa provides a compact, interpretable, and generalizable framework for ultrasound video analysis. The code is available at https://github.com/JuliaWolleb/LRM_Functa.

---

## 论文详细总结（自动生成）

以下是对论文《Low-Rank-Modulated Functa: Exploring the Latent Space of Implicit Neural Representations for Interpretable Ultrasound Video Analysis》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：隐式神经表示（INR）在图像重建方面表现优异，但在处理超声视频时，其潜空间（Latent Space）缺乏结构化特征和临床可解释性。
*   **研究动机**：超声视频（如心脏、肺部）具有极高的时空冗余性，临床诊断依赖于特定的时间相位（如舒张末期 ED 和收缩末期 ES）。现有的 INR 方法（如 Functa）主要关注重建质量，而忽略了如何利用潜空间进行临床分析。作者旨在构建一种既能高效压缩又能提供直观、可解释轨迹的视频表示模型。

### 2. 方法论：LRM-Functa 架构
*   **核心思想**：在 VidFuncta 框架的基础上，引入**低秩自适应（Low-Rank Adaptation）**约束。将每一帧的调制向量限制在一个低维子空间内，从而强制模型捕捉主要的周期性运动模式。
*   **关键技术细节**：
    *   **调制公式**：第 $t$ 帧的调制向量 $m_t$ 定义为 $m_t = v + B_\beta \phi_t$。其中 $v$ 是视频级潜向量（全局特征），$B_\beta$ 是一个可学习的秩为 $k$ 的子空间基矩阵，$\phi_t$ 是帧特定的低维更新系数（秩 $k$ 远小于原始维度 $q$）。
    *   **变体设计**：
        *   **LRM-Functa basic (LRM-Functa b)**：随机初始化子空间基矩阵。
        *   **LRM-Functa ortho (LRM-Functa o)**：对基矩阵施加正交约束（通过 Frobenius 范数损失实现），使潜空间轨迹更具几何结构感。
    *   **相位提取算法**：通过对低维轨迹 $\phi_t$ 进行主成分分析（PCA）并投影，提取出一维周期信号，利用峰值和谷值直接识别 ED 和 ES 帧，无需额外监督训练。

### 3. 实验设计
*   **数据集**：
    1.  **EchoNet-Dynamic**：超过 10,000 个四腔心心脏超声视频，标注有射血分数（EF）和 ED/ES 帧。
    2.  **POCUS (分布外测试)**：17 个来自不同医院、不同切面（两腔心长轴）的床旁超声视频。
    3.  **LUS (肺部超声)**：200 个肺部超声片段，标注有 B 线（病理特征）。
*   **对比方法（Benchmarks）**：
    *   **INR 类**：COIN++、MedFuncta、VidFuncta。
    *   **传统深度学习类**：卷积自动编码器（Convolutional Autoencoder）。
*   **评估指标**：重建质量（PSNR, SSIM3D）、相位检测误差（MAE）、下游任务性能（EF 预测的 $R^2$、B 线分类的 AUROC）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A5000 GPU (24GB)**。
*   **训练时长**：每个模型训练 80,000 次迭代，耗时约 **20 小时**。
*   **软件环境**：Python 3.11, PyTorch 2.4.1。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从极高压缩比（秩 $k=2$）到高保真（秩 $k=512$）的多种配置。
*   **充分性评价**：实验设计非常充分。不仅验证了基础的重建能力，还通过跨数据集（POCUS）验证了**泛化性**，通过不同器官（心脏 vs 肺部）验证了**通用性**，并通过下游临床任务验证了**实用性**。对比实验涵盖了当前主流的医疗 INR 模型，结果具有客观性。

### 6. 主要结论与发现
*   **潜空间结构化**：LRM-Functa o 在 $k=2$ 时能产生清晰的螺旋状周期轨迹，而 LRM-Functa b 则产生直线往复轨迹。
*   **高效压缩**：在极低秩（$k=2$）下，LRM-Functa 是唯一能保持稳定重建质量的方法，且仍能准确预测射血分数（$R^2=0.67$）。
*   **无监督相位检测**：在无需任何标签的情况下，该方法在 ED/ES 帧检测上的表现优于所有对比方法，甚至接近全监督模型的水平。
*   **强泛化力**：在分布外（OOD）的心脏切面上，INR 类方法（尤其是 LRM-Functa）表现出比卷积自动编码器更强的鲁棒性。

### 7. 优点与亮点
*   **可解释性极强**：通过将高维视频压缩至 2D 潜空间，临床医生可以直接“看到”心动周期的轨迹，并能通过遍历轨迹生成平滑的中间帧。
*   **无需标签的临床洞察**：能够直接从学习到的表示中提取生理指标（ED/ES），降低了对昂贵医疗标注的依赖。
*   **端到端一致性**：模型在实现高压缩率的同时，保留了对下游诊断至关重要的细微特征。

### 8. 不足与局限
*   **空间分辨率限制**：实验中视频被下采样至 $112 \times 112$，这可能导致某些微小的解剖结构（如左心室肥厚）在重建中丢失。
*   **训练成本**：每个模型 20 小时的训练时间对于实时应用或大规模数据集的快速迭代仍是一个挑战。
*   **超参数敏感性**：虽然 $k=2$ 表现良好，但针对不同器官（如肺部超声的 B 线检测）仍需要较高的秩（如 $k=512$）来保留细节，缺乏一种自适应选择秩的机制。

（完）
