---
title: "GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding"
title_zh: GazeQwen：面向流式视频理解的轻量级注视条件 LLM 调制
authors: "Trong Thang Pham, Hien Nguyen, Ngan Le"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25841v1"
tags: ["query:lvu"]
score: 9.0
evidence: 流式视频理解的多模态注视条件调制
tldr: "GazeQwen 是一种轻量级视线调节多模态大模型，通过紧凑的重采样器将视线特征注入 LLM 解码器层，解决了现有模型难以利用视线信息理解视频的问题。在 StreamGaze 基准测试中，其准确率达 63.9%，显著超越 GPT-4o，证明了在模型内部注入视线信息比扩大规模或优化提示词更有效。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25841v1/fig-001.webp\", \"caption\": \"Figure 1. GazeQwen overview. A small gaze resampler (∼1– 5 M parameters) encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into four evenly-spaced LLM decoder layers via forward hooks.\", \"page\": 2, \"index\": 1, \"width\": 496, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.25841v1/fig-002.webp\", \"caption\": \"Table 1. Results on the StreamGaze benchmark. We report accuracy on all 10 task types. Baseline numbers are from [11]; GazeQwen is our evaluation on the Qwen2.5-VL-7B backbone. Overall is the mean over the 10 tasks shown. Best per-task (excluding Human) is bolded.\", \"page\": 4, \"index\": 2, \"width\": 1026, \"height\": 566}]"
motivation: 现有的多模态大模型即使通过视觉叠加或文本描述也无法有效利用视线信息来理解视频。
method: 提出一种参数高效的隐藏状态调制方法，利用紧凑的视线重采样器将视线特征作为残差注入 LLM 解码器层。
result: "在 StreamGaze 基准测试中，GazeQwen 达到了 63.9% 的准确率，比 GPT-4o 高出 10.5 个百分点。"
conclusion: 研究表明，学习在 LLM 内部何处注入视线信息比扩大模型规模或设计更好的提示词更为有效。
---

## 摘要
当前的多模态大语言模型 (MLLM) 无法有效地利用眼动注视信息进行视频理解，即使注视线索是通过视觉叠加或文本描述提供的。我们介绍了 GazeQwen，这是一种参数高效的方法，通过隐藏状态调制使开源 MLLM 具备注视感知能力。其核心是一个紧凑的注视重采样器（约 1-5 M 可训练参数），它将 V-JEPA 2.1 视频特征与源自注视点的点位编码相结合，并产生加性残差，通过前向钩子注入到选定的 LLM 解码器层中。一个可选的第二训练阶段向 LLM 添加了低秩自适应器 (LoRA) 以实现更紧密的集成。在 StreamGaze 基准测试的所有 10 个任务中，GazeQwen 达到了 63.9% 的准确率，比使用注视作为视觉提示的相同 Qwen2.5-VL-7B 主干网络提高了 16.1 个百分点，比 GPT-4o 提高了 10.5 个百分点，是所有测试的开源和私有模型中的最高分。这些结果表明，学习在 LLM 中的何处注入注视信息比扩展模型规模或设计更好的提示词更有效。所有代码和检查点均可在 https://github.com/phamtrongthang123/gazeqwen 获取。

## Abstract
Current multimodal large language models (MLLMs) cannot effectively utilize eye-gaze information for video understanding, even when gaze cues are supplied via visual overlays or text descriptions. We introduce GazeQwen, a parameter efficient approach that equips an open-source MLLM with gaze awareness through hidden-state modulation. At its core is a compact gaze resampler (~1-5 M trainable parameters) that encodes V-JEPA 2.1 video features together with fixation-derived positional encodings and produces additive residuals injected into selected LLM decoder layers via forward hooks. An optional second training stage adds low-rank adapters (LoRA) to the LLM for tighter integration. Evaluated on all 10 tasks of the StreamGaze benchmark, GazeQwen reaches 63.9% accuracy, a +16.1 point gain over the same Qwen2.5-VL-7B backbone with gaze as visual prompts and +10.5 points over GPT-4o, the highest score among all open-source and proprietary models tested. These results suggest that learning where to inject gaze within an LLM is more effective than scaling model size or engineering better prompts. All code and checkpoints are available at https://github.com/phamtrongthang123/gazeqwen .

---

## 论文详细总结（自动生成）

以下是对论文《GazeQwen: Lightweight Gaze-Conditioned LLM Modulation for Streaming Video Understanding》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在增强现实（AR）和具身智能应用中，用户的**眼动注视（Eye-gaze）**是理解其意图和关注点的关键线索。然而，现有的多模态大模型（MLLM）即使通过文本坐标、视觉覆盖或显著性图等方式获取注视信息，也无法有效利用这些数据，表现远逊于人类。
*   **核心问题**：如何让 MLLM 在不进行大规模重训练或架构更改的情况下，深度融合并理解流式视频中的动态注视信息？
*   **整体含义**：论文提出了一种名为 **GazeQwen** 的轻量级调制方法，证明了在 LLM 内部隐藏状态中注入注视信号，比单纯增加模型规模或优化提示词（Prompt Engineering）更为有效。

### 2. 论文提出的方法论
*   **核心思想**：通过一个紧凑的“注视重采样器”（Gaze Resampler）将视频特征与注视坐标融合，生成残差信号，并利用 PyTorch 的前向钩子（Forward Hooks）将其注入到 LLM 的特定解码层中。
*   **关键技术细节**：
    *   **特征提取**：使用冻结的 **V-JEPA 2.1** 作为视觉编码器，因为它比 LLM 自带的视觉编码器保留了更多细粒度的时空细节。
    *   **注视编码**：采用 DETR 风格的正弦位置编码（Sinusoidal Positional Encoding）处理归一化的注视坐标，能够平滑地映射稀疏的注视点。
    *   **注视重采样器**：包含 32 个可学习的潜变量（Latents），通过交叉注意力机制处理视频特征。特别之处在于将注视编码添加到 **Key（键）** 中，从而引导注意力分布向注视区域倾斜，而不改变 Value（值）中的视觉语义。
    *   **隐藏状态调制**：在 LLM 的 4 个等间距解码层（第 6, 13, 20, 27 层）注入加性残差。
    *   **两阶段训练**：第一阶段仅训练重采样器（1-5M 参数）；第二阶段加入 LoRA 适配器（约 3.5M 参数）进行联合微调，使 LLM 更好地适应注入的信号。

### 3. 实验设计
*   **数据集与基准**：使用 **StreamGaze** 基准测试，包含 285 个第一视角视频、8,521 个注视条件下的问答对。
*   **任务分类**：涵盖 10 类任务，分为“过去”（推理历史）、“现在”（当前上下文）和“前瞻性”（预测未来/提醒）三大类。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 4 (Sonnet/Opus)。
    *   **开源模型**：Qwen2.5-VL, InternVL3.5, VITA 1.5 等。
    *   **流式视频模型**：ViSpeak, Dispider, VideoLLM-online 等。
    *   **注视专用模型**：AssistGaze。

### 4. 资源与算力
*   **算力需求**：论文明确指出该方法是**单 GPU 可训练的（Single-GPU training）**，具有极高的参数效率。
*   **训练细节**：使用 AdamW 优化器，学习率为 $3 \times 10^{-4}$，共训练 20 个 epoch，梯度累积步数为 8。
*   **具体型号**：文中未明确给出具体的 GPU 型号（如 A100 或 H100），但强调了其轻量级特性（总可训练参数仅约 5-9M）。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同的任务维度上进行了全面评估，并与超过 10 种主流模型进行了对比。
*   **消融实验**：针对四个关键设计轴进行了充分的消融研究：(G) 注视编码方式、(B) 视觉主干网络选择、(S) 层间参数共享策略、(A) LLM 适配方式。
*   **客观性**：采用了 70/15/15 的视频级分割以防止数据泄露，实验设计严谨，对比基准统一。

### 6. 论文的主要结论与发现
*   **性能突破**：GazeQwen 达到了 **63.9%** 的总准确率，比 GPT-4o 高出 **10.5%**，比同底座的 Qwen2.5-VL（使用视觉提示）高出 **16.1%**。
*   **注视的作用域**：注视信息在“物体识别（困难）”、“物体追踪”和“出现提醒”等空间敏感型任务中提升最显著（最高达 +34.1%），但在简单的物体识别或纯时间推理任务中提升有限。
*   **内部调制优于外部提示**：学习如何将注视信息注入 LLM 内部表示，比简单地在输入端提供坐标或热力图要有效得多。

### 7. 优点
*   **参数高效**：仅需极少量的可训练参数（<10M），即可让 7B 规模的模型超越闭源巨型模型。
*   **模块化设计**：注视模块可以像“插件”一样随时挂载或卸载，无需重新加载 LLM 权重，不影响模型原有的通用能力。
*   **架构无关**：通过 Hook 注入，不需要修改 LLM 的源代码，易于迁移到其他开源模型。

### 8. 不足与局限
*   **依赖外部注视数据**：模型性能高度依赖于高质量的眼动追踪输入，在缺乏此类传感器的实际场景中无法直接应用。
*   **任务局限性**：目前仅在第一视角（Egocentric）视频理解上验证了效果，对于第三视角或其他类型的视频理解能力尚待验证。
*   **实时性细节**：虽然针对流式视频设计，但论文未详细给出推理延迟（Latency）的具体毫秒级数据。

（完）
