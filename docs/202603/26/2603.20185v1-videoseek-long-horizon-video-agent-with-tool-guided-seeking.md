---
title: "VideoSeek: Long-Horizon Video Agent with Tool-Guided Seeking"
title_zh: VideoSeek：具有工具引导寻踪能力的长时程视频智能体
authors: "Jingyang Lin, Jialian Wu, Jiang Liu, Ximeng Sun, Ze Wang, Xiaodong Yu, Jiebo Luo, Zicheng Liu, Emad Barsoum"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20185v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于理解和推理的长时视频智能体
tldr: "VideoSeek 是一种针对长视频理解的智能体，旨在解决传统方法过度依赖密集采样导致的计算高昂问题。它通过“思考-行动-观察”循环，利用视频逻辑流主动寻找关键证据，而非穷举解析。该方法在减少 93% 帧数的情况下，在 LVBench 等基准测试中显著提升了性能，实现了高效且精准的长视频推理。"
selection_source: fresh_fetch
motivation: 传统视频智能体依赖密集采样导致计算成本极高，且缺乏对视频逻辑流的主动探索能力。
method: 提出 VideoSeek 框架，通过“思考-行动-观察”循环和多粒度观察工具包，实现查询驱动的视频证据主动搜寻。
result: "在四个挑战性基准测试中表现优异，尤其在 LVBench 上比基座模型提升 10.2 分，且仅使用其 7% 的帧数。"
conclusion: 通过利用视频逻辑流和工具引导的搜寻机制，VideoSeek 证明了在大幅降低计算开销的同时，可以显著增强长视频的理解与推理能力。
---

## 摘要
视频智能体模型在具有挑战性的视频-语言任务中取得了显著进展。然而，大多数智能体方法仍然严重依赖于对密集采样视频帧的贪婪解析，导致了高昂的计算成本。我们提出了 VideoSeek，这是一种长时程视频智能体，它利用视频逻辑流主动寻找对回答至关重要的证据，而不是穷举式地解析整个视频。这一见解使得模型在保持甚至提高其视频理解能力的同时，能够使用更少的帧。VideoSeek 在“思考-行动-观察”循环中运行，并配备了一个精心设计的工具包，用于收集多粒度的视频观测。这种设计实现了对累积观测结果的查询感知探索，并支持实际的视频理解与推理。在四个具有挑战性的视频理解和推理基准测试上的实验表明，VideoSeek 在使用比之前的视频智能体和独立大型多模态模型（LMMs）少得多的帧的情况下，实现了极高的准确率。值得注意的是，VideoSeek 在 LVBench 上相比其基础模型 GPT-5 实现了 10.2 个百分点的绝对提升，同时减少了 93% 的帧使用量。进一步的分析强调了利用视频逻辑流、强大的推理能力以及工具包设计的互补作用的重要性。

## Abstract
Video agentic models have advanced challenging video-language tasks. However, most agentic approaches still heavily rely on greedy parsing over densely sampled video frames, resulting in high computational cost. We present VideoSeek, a long-horizon video agent that leverages video logic flow to actively seek answer-critical evidence instead of exhaustively parsing the full video. This insight allows the model to use far fewer frames while maintaining, or even improving, its video understanding capability. VideoSeek operates in a think-act-observe loop with a well-designed toolkit for collecting multi-granular video observations. This design enables query-aware exploration over accumulated observations and supports practical video understanding and reasoning. Experiments on four challenging video understanding and reasoning benchmarks demonstrate that VideoSeek achieves strong accuracy while using far fewer frames than prior video agents and standalone LMMs. Notably, VideoSeek achieves a 10.2 absolute points improvement on LVBench over its base model, GPT-5, while using 93% fewer frames. Further analysis highlights the significance of leveraging video logic flow, strong reasoning capability, and the complementary roles of toolkit design.

---

## 论文详细总结（自动生成）

### VideoSeek: 具有工具引导寻踪能力的长时程视频智能体 —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
长视频理解（Long-Horizon Video Understanding）面临的核心挑战是**计算效率与理解深度之间的矛盾**。现有的视频智能体（Video Agents）通常采用“密集采样”或“全量解析”的策略，将视频转化为长文档或结构化存储，这导致计算成本随视频长度呈线性甚至指数级增长。
**研究动机**源于对人类行为的观察：人类在回答视频相关问题时，很少从头到尾看完，而是利用视频的**逻辑流（Logic Flow）**（如时间顺序、因果关系）进行主动搜寻。论文提出了 **VideoSeek**，旨在通过模拟人类“先看大概、再扫区间、最后定格”的寻踪行为，在极低视觉预算下实现高效的长视频推理。

#### 2. 论文提出的方法论
VideoSeek 采用 **ReAct（Reasoning and Acting）** 架构，将视频理解建模为一个迭代的“思考-行动-观察”循环。

*   **核心思想**：不预先处理全量视频，而是根据查询（Query）和已有的观察结果，动态决定下一步该看哪里、怎么看。
*   **关键技术细节**：
    *   **思考模型（Thinking LLM）**：作为大脑，负责推理当前证据是否充足，并规划下一步行动。
    *   **多粒度工具包（Toolkit）**：
        1.  `<overview>`：全局概览，均匀采样极少量帧（如16帧），构建初步故事线。
        2.  `<skim>`：粗略扫描，针对候选区间进行低成本探测，缩小搜索范围。
        3.  `<focus>`：精细检查，以高帧率（如1 FPS）观察短片段，捕捉关键细节（如文字、微小动作）。
*   **算法流程**：
    1.  初始化：输入查询和视频元数据。
    2.  循环：LLM 生成“思考（Thought）” -> 选择“工具（Action）” -> 获取“观测（Observation）”。
    3.  终止：当 LLM 认为证据足以回答或达到最大轮次（N=20）时，输出最终答案。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LVBench**：极端长视频理解（平均 67 分钟）。
    *   **Video-MME**：综合性多模态长视频评估。
    *   **LongVideoBench**：长上下文交错视频语言理解。
    *   **Video-Holmes**：复杂的视频逻辑推理（如悬疑片线索连接）。
*   **对比方法**：
    *   **独立 LMMs**：GPT-4o, Gemini 1.5 Pro, Gemini 2.0 Flash, Qwen2.5-VL-72B, 以及基座模型 GPT-5。
    *   **视频智能体**：VideoAgent, VideoTree, DrVideo, VCA, MR. Video, DVD。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件训练资源（如 GPU 型号、数量）。
*   **实现细节**：由于 VideoSeek 是一个基于 API 的 Agent 框架，其核心开销在于调用大型多模态模型（如 GPT-5）的推理成本。
*   **效率指标**：论文重点强调了**帧数（Frames）**的节省。例如，在 LVBench 上，VideoSeek 仅处理约 27-92 帧，而对比方法 DVD 处理了 8074 帧。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准测试上进行了全面评估，涵盖了从 3 分钟到 60 分钟不等的视频长度。
*   **消融实验**：
    *   验证了工具包中每个组件（Overview/Skim/Focus）的必要性。
    *   测试了不同底层 LLM（GPT-5, o4-mini, GPT-4.1）对智能体表现的影响。
    *   分析了字幕（Subtitles）对逻辑流引导的作用。
*   **评价**：实验设计客观、公平，通过对比“相同帧数下的性能”和“相同性能下的帧数”，充分证明了该方法的高效性。

#### 6. 论文的主要结论与发现
*   **高效性**：VideoSeek 在处理帧数仅为同类智能体（如 DVD）的 **1/300** 时，依然能达到甚至超越其准确率。
*   **性能提升**：在 LVBench 上，VideoSeek 比基座模型 GPT-5 提升了 **10.2%** 的准确率，同时减少了 **93%** 的帧使用量。
*   **逻辑流的重要性**：引入字幕能显著降低帧数需求（从 92 帧降至 27 帧），证明了文本逻辑流能极大地引导视觉搜索。
*   **推理能力挂钩**：智能体的表现高度依赖于底层 LLM 的推理能力，较弱的模型（如 GPT-4.1）容易出现过早停止或判断失误。

#### 7. 优点
*   **拟人化搜索**：模仿人类的认知策略，避免了暴力解析，具有极高的计算性价比。
*   **模型无关性**：作为一个框架，它可以适配任何强大的 LMM 作为后端。
*   **多粒度协同**：工具包设计合理，实现了从全局到局部的平滑过渡。
*   **长视频处理能力**：在小时级视频上表现出极强的鲁棒性。

#### 8. 不足与局限
*   **意外事件检测难**：对于缺乏逻辑预兆的突发异常（如监控中的随机事故），基于逻辑流的搜寻可能无法准确定位。
*   **推理延迟**：虽然处理帧数少，但多次“思考-行动”的往返循环（Turn-based）可能增加总体的推理时延（Runtime）。
*   **依赖性风险**：如果底层 LLM 在初始阶段对视频逻辑理解偏差，可能会导致智能体在错误的搜索路径上“越走越远”。

（完）
