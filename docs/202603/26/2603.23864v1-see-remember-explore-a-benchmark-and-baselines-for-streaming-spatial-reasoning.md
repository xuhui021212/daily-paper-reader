---
title: "See, Remember, Explore: A Benchmark and Baselines for Streaming Spatial Reasoning"
title_zh: 观察、记忆、探索：流式空间推理的基准测试与基准模型
authors: "Yuxi Wei, Wei Huang, Qirui Chen, Lu Hou, Xiaojuan Qi"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23864v1"
tags: ["query:lvu"]
score: 9.0
evidence: 长时程流式视频空间推理基准测试
tldr: 本研究针对具身智能体在空间理解中缺乏流式推理和主动感知能力的问题，提出了S3-Bench基准测试。该基准涵盖1万多个场景，要求模型在流式视频中根据实时观测回答空间问题，并支持通过主动探索获取缺失信息。同时，研究者提出了AMF-VLM模型，通过记忆折叠技术压缩长程观测，并结合主动探索动作提升推理精度。实验证明，该方法在模拟和真实场景中均显著优于现有模型，为长程流式空间推理提供了有效的评估框架和基准方案。
selection_source: fresh_fetch
motivation: 现有的空间视觉语言模型大多采用离线评估，忽视了具身智能体在实际部署中所需的长程流式推理和主动感知能力。
method: 提出了包含模拟与真实双域数据的S3-Bench基准，并开发了支持记忆折叠压缩和主动探索动作的AMF-VLM模型。
result: "AMF-VLM在模拟和真实评估集上分别实现了8.8%和13.3%的性能提升，并展现出良好的跨基准迁移能力。"
conclusion: 通过引入流式推理基准和主动感知机制，本研究为提升具身智能体在复杂动态环境下的空间推理能力提供了新路径。
---

## 摘要
空间理解是具身智能体的基础，然而大多数空间视觉语言模型（VLM）和基准测试仍处于离线状态——即对预录制的输入进行事后问答评估，忽略了两个对部署至关重要的要求：长时程流式推理以及在当前视野不足时的主动感知。为了填补这一空白，我们推出了 S3-Bench，这是一个带有主动探索的流式空间问答基准测试套件。在该套件中，查询在时间上被定位到特定的时间戳，并且必须仅使用截至该时刻可用的观测结果进行回答。S3-Bench 采用了双域设计，结合了具有可控轨迹和探索动作的可扩展模拟器，以及捕捉了实际传感伪影的真实世界流式视频，以进行严格的泛化评估。总体而言，它涵盖了超过 1 万个场景和 2.6 万条轨迹，并设有专门的训练集（S3-Train）和评估集（S3-Eval）。我们进一步提出了 AMF-VLM，它通过以下方式支持有限计算下的流式空间推理：(i) 记忆折叠（memory folding），将长时程观测压缩为紧凑的结构化记忆；(ii) 主动探索，输出明确的动作（如移动/旋转/扫描）以在回答前获取缺失的证据。大量实验表明，与使用相同训练数据的模型相比，我们的方法在 S3-Eval 的模拟和真实拆分集上分别实现了 8.8% 和 13.3% 的提升，同时对标准空间基准测试保持了具有竞争力的迁移能力。

## Abstract
Spatial understanding is fundamental for embodied agents, yet most spatial VLMs and benchmarks remain offline-evaluating post-hoc QA over pre-recorded inputs and overlooking two crucial deployment-critical requirements: long-horizon streaming inference and active perception when the current view is insufficient. To address this gap, we introduce S3-Bench, a benchmark suite for streaming spatial question answering with active exploration, where queries are temporally grounded to specific timestamps and must be answered using only observations available up to that moment. S3-Bench adopts a dual-domain design, combining a scalable simulator with controllable trajectories and exploration actions, and real-world streaming videos that capture practical sensing artifacts for rigorous generalization evaluation. Overall, it spans 10K+ scenes and 26K+ trajectories, with dedicated training (S3-Train) and evaluation (S3-Eval) splits. We further propose AMF-VLM, which supports streaming spatial reasoning under bounded computing via (i) memory folding, which compresses long-horizon observations into compact structured memory, and (ii) active exploration, which outputs explicit actions (e.g. move/rotate/scan) to acquire missing evidence before answering. Extensive experiments demonstrate that, compared to models using identical training data, our approach yields improvements of 8.8% and 13.3% on the simulated and real splits of S3-Eval, respectively, while maintaining competitive transferability to standard spatial benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《See, Remember, Explore: A Benchmark and Baselines for Streaming Spatial Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的空间视觉语言模型（VLM）大多处于“离线”模式，即在观看完完整视频后进行事后问答。这忽视了具身智能体在实际部署中的两个关键需求：**长时程流式推理**（随时间推移持续更新空间认知）和**主动感知**（当当前视野不足以回答问题时，主动采取行动获取信息）。
*   **研究背景**：具身智能体（如机器人）在导航和交互中必须实时处理不断增长的观测流，且受限于计算资源（KV Cache 爆炸）和遮挡问题。论文旨在填补离线评估与实时具身需求之间的鸿沟。

### 2. 方法论：核心思想与关键技术
论文提出了一个基准测试套件 **S3-Bench** 和一个基准模型 **AMF-VLM**。

*   **S3-Bench 基准设计**：
    *   **双域设计**：结合了可控的**模拟器**（支持自由轨迹生成和主动探索动作）和**真实世界视频**（涵盖 ScanNet 等数据集，捕捉真实的传感伪影）。
    *   **时间锚定（Temporally Grounded）**：每个查询都关联特定时间戳，模型只能利用该时刻之前的观测数据，符合因果律约束。
*   **AMF-VLM 模型架构**：
    *   **记忆折叠（Memory Folding）**：为了应对长序列导致的计算开销和遗忘，模型定期（如每10秒）将历史观测压缩为结构化的文本记忆（描述实体、属性和空间关系），并与稀疏的关键帧结合，维持紧凑的世界状态。
    *   **主动探索机制（Active Exploration）**：当模型判断当前信息不足以回答问题时，会输出 JSON 格式的动作指令（如“左转45度”、“向前移动2米”）。模拟器执行动作后获取新视野，模型再整合新证据给出最终答案。
    *   **流式处理策略**：采用分层流式机制，保留初始 Token 作为“注意力锚点（Attention Sinks）”，并使用连续旋转位置编码（RoPE）防止 ID 溢出。

### 3. 实验设计
*   **数据集/场景**：
    *   **S3-Train**：包含约 60 万条训练数据。
    *   **S3-Eval**：包含约 1000 个场景和 2 万个经过人工精修的问答对。
    *   **数据来源**：模拟数据基于 3D-FRONT；真实数据整合了 ScanNet、ScanNet++ 和 ARKitScenes。
*   **对比方法**：
    *   **闭源模型**：GPT-4 (文中注为 5.2)、Gemini 1.5 Flash/Pro。
    *   **开源模型**：LLaVA-Video、LLaVA-OneVision、Qwen2-VL (文中注为 Qwen3-VL)、InternVL2.5。
    *   **空间/流式专用模型**：VLM-3R、Cambrian-S、Streaming-VLM。
*   **评估指标**：多选题使用准确率（Accuracy），数值估计题使用平均相对准确率（MRA）。

### 4. 资源与算力
*   **算力设备**：训练在 **NVIDIA B200 GPU** 上进行；评估和延迟测量在 **NVIDIA H800 GPU** 上完成。
*   **训练细节**：模型基于 Qwen3-VL-8B（注：可能指代 Qwen2-VL 的后续或特定版本），冻结视觉编码器，训练 1 个 Epoch，Batch Size 为 16。
*   **推理效率**：论文展示了延迟分析，AMF-VLM 在处理长视频（如 128 帧）时，延迟保持在 0.5s 左右，远低于标准 VLM 的 8.5s。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10k+ 场景和 26k+ 轨迹，规模庞大。
*   **实验维度**：
    *   **主实验**：分别在模拟（Sim）和真实（Real）子集上进行了全面对比。
    *   **消融实验**：验证了主动探索、记忆折叠和 3D 编码器各自的贡献。
    *   **盲测（Blind Testing）**：去除视觉输入以验证模型是否依赖语言先验（结果显示性能大幅下降，证明了视觉理解的必要性）。
    *   **跨基准迁移**：在 VSI-Bench、Blink 和 MMSI 等标准空间基准上进行了测试。
*   **评价**：实验设计非常充分且客观，通过引入“盲测”和“延迟分析”增强了评估的严谨性。

### 6. 主要结论与发现
*   **性能领先**：AMF-VLM 在 S3-Eval 的模拟和真实部分分别比同类模型提升了 **8.8%** 和 **13.3%**。
*   **流式优势**：传统的离线 VLM 在处理长流式输入时，由于 KV Cache 增长和长程遗忘，表现迅速下滑，而记忆折叠能有效缓解此问题。
*   **主动感知的必要性**：在存在遮挡或视野受限的场景下，主动探索动作能显著提升空间推理的准确性。
*   **泛化能力**：在模拟数据上训练的模型在真实世界视频中依然表现出良好的泛化性能。

### 7. 优点：亮点总结
*   **首创性**：首次形式化了 VLM 的“流式空间理解”任务，更贴近具身智能的实际应用。
*   **闭环交互**：将“动作输出”引入 VLM 推理流程，实现了从被动观察到主动感知的转变。
*   **效率平衡**：通过记忆折叠技术，在有限的计算预算下实现了长时程的空间记忆。
*   **高质量数据**：构建了包含 3D 几何引导轨迹和时间锚定标注的大规模数据集。

### 8. 不足与局限
*   **真实域的主动性受限**：在真实世界评估（Real Split）中，由于使用的是预录制视频，模型无法像在模拟器中那样真正自由地执行任意动作，只能在固定轨迹上评估。
*   **记忆折叠的依赖性**：记忆折叠高度依赖于模型生成准确文本摘要的能力，若摘要出现幻觉，可能会导致长程推理的连锁错误。
*   **模拟与现实的差距**：尽管引入了真实数据，但主动探索策略主要在模拟器中训练，在复杂多变的真实物理环境中的鲁棒性仍待进一步验证。

（完）
