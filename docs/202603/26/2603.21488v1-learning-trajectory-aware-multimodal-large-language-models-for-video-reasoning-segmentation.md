---
title: Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation
title_zh: 学习用于视频推理分割的轨迹感知多模态大语言模型
authors: "Jingnan Luo, Mingqi Gao, Jun Liu, Bin-Bin Gao, Feng Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21488v1"
tags: ["query:lvu"]
score: 9.0
evidence: 基于多模态大模型的视频推理分割
tldr: 本文针对视频推理分割任务中动态场景下轨迹感知不足的问题，提出了TrajSeg框架。该框架基于多模态大模型，引入了双向文本-轨迹对齐机制（包括定位和描述指令），显著增强了模型对物体轨迹的理解。通过帧级内容集成模块和统一掩码解码器，TrajSeg实现了简化且端到端的训练流程。实验表明，该方法在多个视频分割基准测试中均取得了SOTA性能，为视频推理分割提供了高效的统一解决方案。
selection_source: fresh_fetch
motivation: 现有视频推理分割方法依赖单向隐式对齐，在处理剧烈动态视频时难以准确感知物体轨迹。
method: 提出TrajSeg框架，通过双向文本-轨迹对齐增强感知，并利用帧级内容集成模块和统一掩码解码器实现端到端分割。
result: 在参考和推理视频分割数据集上，TrajSeg在所有指标上均优于现有的视频推理分割方法。
conclusion: 该研究证明了双向对齐和轨迹感知在提升多模态大模型视频推理分割性能方面的有效性。
---

## 摘要
多模态大语言模型（MLLMs）的繁荣激发了对视频推理分割的需求，该任务旨在根据人类指令对视频对象进行分割。先前的研究依赖于单向且隐式的文本-轨迹对齐，在面对剧烈的视频动态时难以进行轨迹感知。在这项工作中，我们提出了 TrajSeg，这是一个基于 MLLMs 构建的简单且统一的框架。具体而言，我们引入了双向文本-轨迹对齐，其中 MLLMs 接收定位导向（文本到轨迹）和描述导向（轨迹到文本）的指令。通过这种方式，MLLMs 可以从增强的对应关系中获益，并更好地感知视频中的对象轨迹。从轨迹生成掩码是通过帧级内容整合（FCI）模块和统一掩码解码器实现的。前者将 MLLM 解析的轨迹级 token 适配到特定帧的信息，而后者将所有帧的分割统一到一个单一结构中，使所提出的框架得以简化并实现端到端训练。在指代和推理视频分割数据集上的广泛实验证明了 TrajSeg 的有效性，其在所有指标上均优于现有的视频推理分割方法。代码将在 https://github.com/haodi19/TrajSeg 公开。

## Abstract
The prosperity of Multimodal Large Language Models (MLLMs) has stimulated the demand for video reasoning segmentation, which aims to segment video objects based on human instructions. Previous studies rely on unidirectional and implicit text-trajectory alignment, which struggles with trajectory perception when faced with severe video dynamics. In this work, we propose TrajSeg, a simple and unified framework built upon MLLMs. Concretely, we introduce bidirectional text-trajectory alignment, where MLLMs accept grounding-intended (text-to-trajectory) and captioning-intended (trajectory-to-text) instructions. This way, MLLMs can benefit from enhanced correspondence and better perceive object trajectories in videos. The mask generation from trajectories is achieved via a frame-level content integration (FCI) module and a unified mask decoder. The former adapts the MLLM-parsed trajectory-level token to frame-specific information. The latter unifies segmentation for all frames into a single structure, enabling the proposed framework to be simplified and end-to-end trainable. Extensive experiments on referring and reasoning video segmentation datasets demonstrate the effectiveness of TrajSeg, which outperforms all video reasoning segmentation methods on all metrics. The code will be publicly available at https://github.com/haodi19/TrajSeg.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **TrajSeg**，一个旨在解决视频推理分割（Video Reasoning Segmentation）挑战的统一框架。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视频推理分割方法（根据复杂人类指令分割视频目标）主要面临两大挑战：**动态性（Dynamicity）**和**时空一致性（Spatial-temporal consistency）**。
*   **研究动机**：
    *   **感知不足**：先前方法依赖单向的“文本到轨迹”对齐，模型仅通过最终分割结果隐式学习，难以在剧烈运动的视频中准确感知目标轨迹。
    *   **流程复杂且非最优**：现有方法通常采用“分割+跟踪”的两阶段流水线，关键帧和非关键帧使用不同的模型处理，导致训练复杂且缺乏端到端的协同优化。
*   **整体含义**：TrajSeg 旨在通过增强轨迹感知和统一模型结构，实现一个简单、高效且端到端可训练的视频推理分割框架。

### 2. 方法论
TrajSeg 由四个核心组件组成：MLLM、轨迹编码器、帧级内容集成（FCI）模块和统一掩码生成器。
*   **双向文本-轨迹对齐**：
    *   除了传统的**定位任务**（文本 $\rightarrow$ 轨迹），引入了**描述任务**（轨迹 $\rightarrow$ 文本）。
    *   通过让 MLLM 描述给定的目标轨迹，迫使模型显式理解目标的动态属性和时空演变，从而增强对轨迹的感知能力。
*   **轨迹编码器**：利用 ROI-Align 从视觉特征中提取目标特征，并将其线性投影为 MLLM 可理解的轨迹表示。
*   **帧级内容集成（FCI）模块**：
    *   MLLM 输出的轨迹级 Token 虽富含语义，但缺乏帧内空间细节。
    *   FCI 模块利用**交叉注意力机制**，将轨迹 Token 与特定帧的视觉特征融合，生成具有帧特异性的目标嵌入。
*   **统一掩码生成器**：
    *   **统一结构**：将关键帧（基于指令分割）和非关键帧（基于记忆跟踪）的分割任务统一在同一个解码器结构中。
    *   **灵活提示**：关键帧使用 FCI 增强的 Token 作为提示；非关键帧则利用包含历史掩码和特征的“记忆库”进行查询，确保时空一致性。

### 3. 实验设计
*   **数据集**：
    *   **指代视频分割（RVOS）**：Ref-YouTube-VOS, Ref-DAVIS, MeViS。
    *   **推理视频分割**：ReVOS（包含指代和推理子集）。
*   **Benchmark（基准指标）**：采用 Jaccard 指数（J，区域准确度）、F-measure（F，轮廓准确度）及其平均值（J&F）。
*   **对比方法**：对比了传统的 RVOS 方法（如 ReferFormer, MTTR, ReferDINO）和基于 MLLM 的推理分割方法（如 TrackGPT, VISA, VideoLISA）。

### 4. 资源与算力
*   **算力配置**：使用了 **4 张 GPU** 进行训练。
*   **训练细节**：
    *   使用 DeepSpeed 训练引擎和 AdamW 优化器。
    *   **预训练阶段**：在图像数据集上训练 10 个 epoch。
    *   **主训练阶段**：在视频数据集上进行混合训练，持续 14 个 epoch。
    *   **模型基础**：基于 LLaVA-7B 和 SAM-2 的解码器构建。
*   **注**：文中未明确指出具体的 GPU 型号（如 A100 或 H100），但根据 7B 参数量和 80 的 Batch Size 推测为高性能计算卡。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流视频分割基准上进行了详尽测试。
*   **消融实验**：
    *   验证了双向对齐对推理能力的提升。
    *   验证了 FCI 模块对空间精度恢复的作用。
    *   探讨了关键帧数量（1, 5, 10）对性能和时空稳定性的影响。
*   **充分性与公平性**：实验涵盖了从简单指代到复杂推理的多种场景，并与同等参数规模（如 LLaVA-7B）的 SOTA 方法进行了直接对比，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：TrajSeg 在所有视频推理分割指标上均优于现有方法，尤其在处理长视频和复杂动态场景时表现突出。
*   **双向学习的价值**：引入“轨迹到文本”的描述任务能显著增强模型对复杂指令下目标的识别能力。
*   **统一架构的优势**：统一的掩码生成器不仅简化了部署流程，还通过端到端训练提升了时空一致性，且对关键帧数量具有较强的鲁棒性。

### 7. 优点
*   **创新性对齐**：通过双向对齐解决了 MLLM 在视频领域“感知弱”的痛点。
*   **架构简洁**：摒弃了复杂的“分割+独立跟踪器”模式，实现了真正的端到端视频推理分割。
*   **泛化能力强**：在需要常识推理（如“不愿分享玩具的猫”）和动态推理（如“向右移动的目标”）的场景下均表现优异。

### 8. 不足与局限
*   **歧义处理**：在面对极其模糊或涉及多层嵌套逻辑的指令时，模型仍可能出现定位错误（如视觉相似目标的混淆）。
*   **采样限制**：受限于计算资源，训练时采样的帧数有限，这可能限制了模型捕捉极长时序依赖的能力。
*   **推理深度**：目前主要依赖静态提示对齐，缺乏类似强化学习（如 GRPO）的深度迭代推理机制来处理极高难度的逻辑任务。

（完）
