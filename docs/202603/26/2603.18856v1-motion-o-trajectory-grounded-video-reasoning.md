---
title: "Motion-o: Trajectory-Grounded Video Reasoning"
title_zh: Motion-o：基于轨迹定位的视频推理
authors: "Bishoy Galoaa, Shayda Moezzi, Xiangyu Bai, Sarah Ostadabbas"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18856v1"
tags: ["query:lvu"]
score: 9.0
evidence: 基于轨迹的视频推理和运动模式
tldr: 针对视频推理中物体运动轨迹理解不明确的问题，本文提出了Motion-o模型。该模型通过引入空间-时间-轨迹（STT）推理，将物体在观测点间的运动显式化。研究者构建了增强的轨迹接地数据集，并设计了“运动思维链”（MCoT），利用特定标签描述物体的方向、速度和缩放变化。Motion-o在不改变模型架构的前提下，通过特定的奖励函数强化了视觉证据推理，显著提升了时空定位和轨迹预测能力。
selection_source: fresh_fetch
motivation: 现有视频推理模型往往忽略了物体在连续观测之间的具体运动模式，导致轨迹理解隐含且难以验证。
method: 引入Motion-o模型和运动思维链（MCoT），通过特定标签显式描述物体运动，并利用增强的轨迹数据集和奖励函数进行训练。
result: 实验结果显示，Motion-o在时空接地和轨迹预测方面取得了显著提升，同时保持了与现有框架的完全兼容性。
conclusion: 将显式运动推理引入视觉语言模型是实现基于证据的视频理解的关键步骤。
---

## 摘要
近期研究在视频推理方面取得了实质性进展，许多模型利用时空证据链来增强其推理能力。同时，越来越多的数据集和基准测试提供了结构化标注，旨在支持和评估此类推理。然而，关于物体在观察结果之间“如何”移动的推理却很少受到关注：以往的研究未能通过连接连续的观察结果来阐明运动模式，导致轨迹理解处于隐式状态且难以验证。我们将这种缺失的能力形式化为时空轨迹（STT）推理，并引入了 Motion-o，这是一种针对视觉语言模型的以运动为中心的视频理解扩展，使轨迹变得显式且可验证。为了实现运动推理，我们还引入了一个轨迹定位数据集工件，通过数据增强扩展了稀疏的关键帧监督，从而产生更密集的边界框轨迹和更强的轨迹级训练信号。最后，我们提出了运动思维链（MCoT），这是一种结构化的推理路径，通过离散的 <motion/> 标签总结每个物体的方向、速度和（速度）尺度变化，将定位的观察结果显式地连接成轨迹。为了训练 Motion-o，我们设计了一个奖励函数，迫使模型直接基于视觉证据进行推理，且无需修改架构。实验结果表明，Motion-o 在提升时空定位和轨迹预测能力的同时，保持了与现有框架的完全兼容，确立了运动推理作为循证视频理解的关键扩展。代码见 https://github.com/ostadabbas/Motion-o。

## Abstract
Recent research has made substantial progress on video reasoning, with many models leveraging spatio-temporal evidence chains to strengthen their inference capabilities. At the same time, a growing set of datasets and benchmarks now provides structured annotations designed to support and evaluate such reasoning. However, little attention has been paid to reasoning about \emph{how} objects move between observations: no prior work has articulated the motion patterns by connecting successive observations, leaving trajectory understanding implicit and difficult to verify. We formalize this missing capability as Spatial-Temporal-Trajectory (STT) reasoning and introduce \textbf{Motion-o}, a motion-centric video understanding extension to visual language models that makes trajectories explicit and verifiable. To enable motion reasoning, we also introduce a trajectory-grounding dataset artifact that expands sparse keyframe supervision via augmentation to yield denser bounding box tracks and a stronger trajectory-level training signal. Finally, we introduce Motion Chain of Thought (MCoT), a structured reasoning pathway that makes object trajectories through discrete \texttt{<motion/>} tag summarizing per-object direction, speed, and scale (of velocity) change to explicitly connect grounded observations into trajectories. To train Motion-o, we design a reward function that compels the model to reason directly over visual evidence, all while requiring no architectural modifications. Empirical results demonstrate that Motion-o improves spatial-temporal grounding and trajectory prediction while remaining fully compatible with existing frameworks, establishing motion reasoning as a critical extension for evidence-based video understanding. Code is available at https://github.com/ostadabbas/Motion-o.

---

## 论文详细总结（自动生成）

### Motion-o: 基于轨迹定位的视频推理论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
当前的视频语言模型（VLM）在视频推理中虽然能提供时空证据（如“在某秒、某位置有一个物体”），但往往忽略了物体在不同观测点之间**如何移动**的动态过程。现有的模型通常将运动理解视为隐式的插值，导致推理过程缺乏可验证性，且容易依赖文本先验（Static Priors）而非视觉证据。
本文提出了 **Spatial-Temporal-Trajectory (STT) 推理** 框架，旨在将物体的运动轨迹显式化。通过引入 **Motion-o**，研究者试图填补“空间-时间-轨迹”这一三位一体推理能力的空白，使模型能够明确说出物体的移动方向、速度和缩放变化。

#### 2. 方法论：核心思想与关键技术
Motion-o 的核心在于将运动作为一种显式的推理算子，其主要技术细节包括：
*   **运动思维链（Motion Chain of Thought, MCoT）**：在推理链中引入结构化的 `<motion/>` 标签。该标签包含四个离散属性：`obj`（物体名称）、`dir`（方向，如 N/S/E/W/STAT）、`speed`（速度，如 slow/moderate/fast）和 `scale`（缩放，反映物体靠近或远离，如 approaching/stable/receding）。
*   **轨迹接地数据增强**：针对现有数据集关键帧标注稀疏的问题，利用插值技术生成密集的边界框轨迹，并自动计算运动原语（Motion Primitives）作为监督信号。
*   **两阶段训练流程**：
    1.  **监督微调（SFT）**：在 Qwen2.5-VL-7B 基础上，教模型学习 MCoT 的语法格式。
    2.  **强化学习（RL）**：采用 **GSPO（Group Sequence Policy Optimization）** 算法。
*   **轨迹奖励函数**：
    *   **轨迹奖励 ($r_{traj}$)**：根据预测标签与真实轨迹的匹配度打分（包含方向、速度、缩放的加权得分）。
    *   **视觉接地奖励 ($r_{ground}$)**：引入**双链验证（Dual-Chain Verification）**。通过对比原始视频和“冻结帧”视频（去除运动信息）的输出，惩罚那些在没有运动证据时仍能产生运动描述的行为，从而抑制模型的文本幻觉。

#### 3. 实验设计
*   **数据集与场景**：
    *   **V-STAR**：评估时空推理（What/When/Where）。
    *   **VideoMME & WorldSense**：评估通用视频理解能力。
    *   **TVGBench, MVBench, MotionBench**：评估时间定位和运动专项推理。
*   **Benchmark 与对比方法**：
    *   对比了闭源模型（GPT-4o, Gemini-2-Flash）和多种开源模型（Video-LLaMA3, LLaVA-Video, InternVL-2.5, Open-o3 Video 等）。
    *   重点对比了基座模型 Open-o3 Video，验证 MCoT 带来的增量收益。

#### 4. 资源与算力
论文明确列出了实验所用的算力资源：
*   **GPU 型号**：NVIDIA H200 (140GB)。
*   **数量与时长**：
    *   **SFT 阶段**：使用 1 台 H200，训练 3 个 epoch。
    *   **RL 阶段**：使用 2 台 H200 进行训练。
*   训练数据量：RL 阶段使用了约 34k 样本，SFT 阶段根据模型变体使用 7.8k 至 22k 样本。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流视频理解基准上进行了测试，涵盖了从基础问答到复杂时空定位的多个维度。
*   **消融实验**：针对标注密度（稀疏 vs 密集）、标签表示形式（离散 bin vs 连续数值）、奖励函数组件（是否包含 $r_{ground}$）进行了详细消融。
*   **客观性与公平性**：实验采用了统一的 vLLM 框架进行评估，并对基座模型进行了重测，确保了对比的公正性。通过双链验证奖励的设计，客观地评估了模型对视觉证据的依赖程度。

#### 6. 主要结论与发现
*   **性能提升**：Motion-o 在 V-STAR 上的 mAM 指标提升了约 2.9-3.9 点，在 VideoMME 上提升了约 6.1 点。
*   **运动推理的价值**：显式地要求模型输出运动标签，不仅提升了运动相关的问答准确率，还反哺了空间定位（Where）和时间定位（When）的精度。
*   **离散化的必要性**：消融实验显示，使用离散的类别标签（如“快/慢”）远优于连续数值，后者会导致模型推理崩溃。
*   **视觉依赖性**：视觉接地奖励能有效减少模型根据常识“盲猜”运动模式的现象。

#### 7. 优点（亮点）
*   **架构无关性**：无需修改模型底层架构，通过格式规范和 RL 即可实现能力的增强，具有很强的通用性。
*   **可解释性**：`<motion/>` 标签为视频推理提供了可验证的中间步骤，使用户能理解模型是如何理解物体动态的。
*   **创新的奖励设计**：双链验证（对比原始 vs 冻结帧）是解决多模态模型“捷径学习”和幻觉问题的有效手段。

#### 8. 不足与局限
*   **定位精度受限**：模型依赖于 VLM 自身的定位能力，而 Qwen 等模型并非原生检测器，在复杂场景下边界框可能不够精细（如文中图 S7 所示）。
*   **运动描述粒度**：目前的运动属性（方向、速度、缩放）仍较为基础，难以描述复杂的非线性轨迹或多物体间的精细互动。
*   **计算开销**：虽然推理时开销较小，但 RL 阶段需要生成多个候选链并进行双链验证，训练成本相对较高。

（完）
