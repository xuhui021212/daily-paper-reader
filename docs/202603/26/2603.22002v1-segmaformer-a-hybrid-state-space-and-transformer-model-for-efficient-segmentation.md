---
title: "SegMaFormer: A Hybrid State-Space and Transformer Model for Efficient Segmentation"
title_zh: SegMaFormer：一种用于高效分割的状态空间与 Transformer 混合模型
authors: "Duy D. Nguyen, Phat T. Tran-Truong"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22002v1"
tags: ["query:medvid"]
score: 9.0
evidence: 用于 3D 医学分割的 Mamba 与 Transformer 混合架构
tldr: 针对3D医学图像分割中Transformer模型计算复杂度高、参数量大的问题，本文提出SegMaFormer。该模型采用轻量化混合架构，在编码器的高分辨率阶段使用Mamba模块以降低开销并捕捉空间上下文，在低分辨率阶段保留自注意力机制以精细化特征表示，并结合旋转位置编码增强空间感知。实验表明，该模型在保持高性能的同时，参数量减少了高达75倍，显著提升了计算效率。
selection_source: fresh_fetch
motivation: 旨在解决现有Transformer模型在处理3D医学影像时计算开销巨大且参数冗余的问题。
method: 提出一种分层混合架构，在早期高分辨率阶段应用Mamba层，在后期低分辨率阶段应用自注意力机制，并引入广义旋转位置编码。
result: 在Synapse、BraTS和ACDC等基准测试中，该模型以极低的参数量（最高减少75倍）和计算量达到了与SOTA模型相当的分割精度。
conclusion: SegMaFormer证明了结合Mamba与Transformer的混合架构能有效平衡3D医学图像分割的性能与效率。
---

## 摘要
Transformer 和基于 Mamba 的架构的出现通过实现全局上下文建模，显著推进了 3D 医学图像分割，而这种能力在传统的卷积神经网络 (CNN) 中往往受限。然而，最先进的 Transformer 模型通常伴随着巨大的计算复杂度和参数量，这对于体积数据而言代价极高，且由于标注医学影像数据集的匮乏而进一步加剧。为了解决这些局限性，本研究提出了 SegMaFormer，这是一种轻量级的混合架构，它在分层体积编码器中协同了 Mamba 和 Transformer 模块，以实现高效的长程依赖建模。该模型策略性地在早期的高分辨率阶段采用基于 Mamba 的层，以在捕获关键空间上下文的同时降低计算开销，并将自注意力机制保留给后期的低分辨率阶段，以精细化特征表示。此外，该设计还引入了广义旋转位置嵌入以增强空间感知能力。尽管结构紧凑，SegMaFormer 在三个公共基准数据集（Synapse、BraTS 和 ACDC）上均取得了具有竞争力的性能，其 Dice 系数与规模大得多的模型相当。实验结果表明，与当前最先进的模型相比，我们的方法将参数量减少了高达 75 倍，并大幅降低了 FLOPs，为 3D 医学图像分割提供了一种高效且高性能的解决方案。

## Abstract
The advent of Transformer and Mamba-based architectures has significantly advanced 3D medical image segmentation by enabling global contextual modeling, a capability traditionally limited in Convolutional Neural Networks (CNNs). However, state-of-the-art Transformer models often entail substantial computational complexity and parameter counts, which is particularly prohibitive for volumetric data and further exacerbated by the limited availability of annotated medical imaging datasets. To address these limitations, this work introduces SegMaFormer, a lightweight hybrid architecture that synergizes Mamba and Transformer modules within a hierarchical volumetric encoder for efficient long-range dependency modeling. The model strategically employs Mamba-based layers in early, high-resolution stages to reduce computational overhead while capturing essential spatial context, and reserves self-attention mechanisms for later, lower-resolution stages to refine feature representation. This design is augmented with generalized rotary position embeddings to enhance spatial awareness. Despite its compact structure, SegMaFormer achieves competitive performance on three public benchmarks (Synapse, BraTS, and ACDC), matching the Dice coefficient of significantly larger models. Empirically, our approach reduces parameters by up to 75x and substantially decreases FLOPs compared to current state-of-the-art models, establishing an efficient and high-performing solution for 3D medical image segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《SegMaFormer: A Hybrid State-Space and Transformer Model for Efficient Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D 医学图像分割需要全局上下文建模，但现有的 Transformer 模型计算复杂度高（随序列长度呈平方增长）且参数量巨大，难以在资源受限的临床环境（如发展中地区的医院）中部署。
*   **研究动机**：
    *   CNN 感受野有限，难以捕捉长程依赖。
    *   Transformer 虽然擅长全局建模，但在处理高分辨率 3D 体积数据时，Token 数量爆炸导致计算开销不可接受。
    *   新兴的状态空间模型（Mamba/SSM）具有线性复杂度，但在医学影像任务中仍需精细设计以发挥潜力。
    *   旨在开发一种既能保持高精度，又能极大幅度压缩参数量和计算量的轻量化模型。

### 2. 方法论：核心思想与关键技术
SegMaFormer 采用了一种分层体积编码器架构，结合了 Mamba 的效率和 Transformer 的精细建模能力：
*   **混合编码器设计**：
    *   **早期阶段（高分辨率）**：使用 **Mamba 模块**。由于此时 Token 序列极长，利用 Mamba 的线性复杂度 $O(N)$ 显著降低计算负担，同时捕捉初步的空间上下文。
    *   **后期阶段（低分辨率）**：使用 **自注意力（Self-Attention）模块**。经过下采样后序列长度 $N$ 减小，此时应用注意力机制计算开销可控，且能利用其强大的全局依赖建模能力精细化特征。
*   **3D 旋转位置嵌入 (3D-RoPE)**：在重叠补丁嵌入（Overlapped Patch Embedding）后引入 3D-RoPE，为模型提供旋转一致的空间位置线索，增强了 Mamba 和 Transformer 对复杂解剖结构的感知。
*   **改进的 Mamba 块**：引入了受 GRU 启发的门控机制，动态调节信息流，加速训练收敛并保留数据相关的局部偏差。
*   **全 MLP 解码器**：放弃传统的逐级 3D 卷积解码，采用轻量化的线性层进行多尺度特征融合，避免过度参数化。

### 3. 实验设计
*   **数据集**：
    1.  **BraTS (2016/2017)**：脑肿瘤分割（MRI 模态），评估 WT、ET、TC 三个子区域。
    2.  **Synapse**：多器官腹部 CT 分割，评估 8 个器官的 Dice 分数。
    3.  **ACDC**：自动心脏诊断（MRI 模态），评估左心室、右心室和心肌。
*   **基准模型 (Benchmarks)**：对比了包括 nnFormer、TransUNet、UNETR、Swin-UNETR、SegFormer3D 以及最新的 U-Mamba 等多种 SOTA 模型。
*   **评价指标**：Dice 系数、参数量 (Params)、计算量 (GFLOPs)。

### 4. 资源与算力
*   **硬件配置**：实验在双显卡 **NVIDIA RTX 4060Ti GPU** 上完成。
*   **软件框架**：基于 PyTorch 2.8 和 nnU-Net 框架。
*   **训练细节**：使用 AdamW 优化器，初始学习率 3e-4，采用余弦退火衰减和线性预热（Warm-up）。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流公开基准数据集上进行了完整的训练和测试。
*   **对比充分性**：对比对象涵盖了纯 CNN、纯 Transformer、混合架构以及最新的 Mamba 系模型，跨度较大。
*   **客观性**：模型未依赖任何外部预训练数据（从零训练），确保了架构设计本身有效性的公平对比。
*   **不足**：文中未详细展示消融实验（如单独移除 Mamba 或 3D-RoPE 的影响），虽然提及了 3D-RoPE 的作用，但缺乏定量消融数据支撑。

### 6. 主要结论与发现
*   **极高的效率**：SegMaFormer 仅有 **2.02M 参数**，相比 nnFormer (150.5M) 减少了约 **75 倍**，计算量仅为 15.2 GFLOPs。
*   **性能持平**：在参数量极小的情况下，其 Dice 分数在多个数据集上与规模大得多的模型（如 Swin-UNETR）相当，甚至在 BraTS 和 Synapse 上超越了基准模型 SegFormer3D。
*   **位置编码的重要性**：引入 3D-RoPE 有效弥补了 Mamba 在空间归纳偏置上的不足，特别是在心脏 (ACDC) 等对空间结构敏感的任务中表现明显。

### 7. 优点与亮点
*   **轻量化巅峰**：在保持 3D 医学图像分割精度不掉队的前提下，将参数量压缩到了极致（2M 级别），极具实用价值。
*   **架构创新**：合理分配了 Mamba 和 Transformer 的应用阶段，解决了 3D 数据 Token 序列过长导致的计算瓶颈。
*   **无需预训练**：在不使用 ImageNet 等大规模预训练的情况下，依然能取得优异结果，降低了对数据的依赖。

### 8. 局限性与不足
*   **小器官分割挑战**：实验结果显示，模型在 Synapse 数据集的小器官（如胆囊、胰腺）上表现略逊于超大规模模型，说明轻量化可能牺牲了部分细微特征的捕捉能力。
*   **边界处理**：在处理解剖结构边缘锐利的区域时仍有提升空间。
*   **消融实验缺失**：论文中缺乏对混合架构各组件贡献度的详细消融分析，难以量化 Mamba 块与 Transformer 块各自的增益比例。

（完）
