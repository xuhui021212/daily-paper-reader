---
title: "ForeSea: AI Forensic Search with Multi-modal Queries for Video Surveillance"
title_zh: ForeSea：面向视频监控的多模态查询 AI 取证搜索
authors: "Hyojin Park, Yi Li, Janghoon Cho, Sungha Choi, Jungsoo Lee, Taotao Jing, Shuai Zhang, Munawar Hayat, Dashan Gao, Ning Bi, Fatih Porikli"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22872v1"
tags: ["query:lvu"]
score: 10.0
evidence: 长时监控视频的多模态查询问答
tldr: 针对长视频监控中跨摄像头目标搜索难题，本文提出了首个支持多模态查询（图像+文本）的基准测试集 ForeSeaQA，并开发了 ForeSea 系统。该系统采用三阶段流水线：通过跟踪过滤无关片段、多模态嵌入索引以及 VideoLLM 检索与推理，显著提升了复杂取证场景下的检索准确率和时间定位精度。
selection_source: fresh_fetch
motivation: 现有的视频监控搜索方法难以处理复杂的多模态查询，且缺乏能够评估长视频中时间推理和精确定位的基准测试。
method: 提出 ForeSea 系统，包含目标跟踪过滤、多模态嵌入索引以及基于 VideoLLM 的三阶段检索与问答框架。
result: "在 ForeSeaQA 基准上，ForeSea 相比之前的 VideoRAG 模型在准确率上提升了 3.5%，在时间交并比（IoU）上提升了 11.0。"
conclusion: ForeSea 是首个专为复杂多模态查询和精确时间定位设计的视频取证搜索系统，为智能安防领域提供了新的评估标准和技术方案。
---

## 摘要
尽管经过数十年的研究，在长时、多摄像机视频中寻找特定目标仍然是监控领域的一大挑战。先前的方法（如跟踪流水线、基于 CLIP 的模型和 VideoRAG）需要大量的人工过滤，仅能捕捉浅层属性，且在时序推理方面表现不佳。现实世界的搜索本质上是多模态的（例如，结合人物图像询问“此人何时加入打斗？”），但这一设定尚未得到充分探索。此外，目前还缺乏合适的基准来评估这种基于多模态查询的视频问答设定。为填补这一空白，我们推出了 ForeSeaQA，这是一个专门为结合图像和文本查询的视频问答（Video QA）设计的全新基准，并包含关键事件的时间戳标注。该数据集由长时监控录像和多样化的多模态问题组成，能够对现实取证条件下的检索、时序定位（temporal grounding）和多模态推理进行系统性评估。不仅限于该基准，我们还提出了 ForeSea，这是一个具有三阶段、即插即用流水线的 AI 取证搜索系统：(1) 跟踪模块过滤无关素材；(2) 多模态嵌入模块对剩余片段进行索引；(3) 在推理过程中，系统检索前 K 个候选片段，供视频大语言模型（VideoLLM）回答查询并定位事件。在 ForeSeaQA 上，ForeSea 的准确率比之前的 VideoRAG 模型提高了 3.5%，时序交并比（temporal IoU）提高了 11.0。据我们所知，ForeSeaQA 是首个支持具有精确时序定位的复杂多模态查询的基准，而 ForeSea 是首个专为在该设定下表现出色而构建的 VideoRAG 系统。

## Abstract
Despite decades of work, surveillance still struggles to find specific targets across long, multi-camera video. Prior methods -- tracking pipelines, CLIP based models, and VideoRAG -- require heavy manual filtering, capture only shallow attributes, and fail at temporal reasoning. Real-world searches are inherently multimodal (e.g., "When does this person join the fight?" with the person's image), yet this setting remains underexplored. Also, there are no proper benchmarks to evaluate those setting - asking video with multimodal queries. To address this gap, we introduce ForeSeaQA, a new benchmark specifically designed for video QA with image-and-text queries and timestamped annotations of key events. The dataset consists of long-horizon surveillance footage paired with diverse multimodal questions, enabling systematic evaluation of retrieval, temporal grounding, and multimodal reasoning in realistic forensic conditions. Not limited to this benchmark, we propose ForeSea, an AI forensic search system with a 3-stage, plug-and-play pipeline. (1) A tracking module filters irrelevant footage; (2) a multimodal embedding module indexes the remaining clips; and (3) during inference, the system retrieves top-K candidate clips for a Video Large Language Model (VideoLLM) to answer queries and localize events. On ForeSeaQA, ForeSea improves accuracy by 3.5% and temporal IoU by 11.0 over prior VideoRAG models. To our knowledge, ForeSeaQA is the first benchmark to support complex multimodal queries with precise temporal grounding, and ForeSea is the first VideoRAG system built to excel in this setting.

---

## 论文详细总结（自动生成）

以下是对论文《ForeSea: AI Forensic Search with Multi-modal Queries for Video Surveillance》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在长达数小时、跨多个摄像头的监控视频中，如何高效、准确地搜索特定目标（如特定人物及其行为）。
*   **研究背景**：
    *   **现有技术局限**：传统的跟踪流水线需要大量人工过滤；基于 CLIP 的检索仅能处理浅层属性；现有的 VideoRAG（检索增强生成）系统通常只支持文本查询，且缺乏精细的时序推理能力。
    *   **现实需求**：实际取证往往是**多模态**的（例如：拿着一张嫌疑人照片问“这个人什么时候参与了打斗？”），但目前缺乏支持这种“图像+文本”混合查询的基准测试和系统。
*   **整体含义**：本文旨在通过引入首个多模态取证基准数据集（ForeSeaQA）和一个三阶段的 AI 搜索系统（ForeSea），解决长视频监控中的复杂查询与精确时序定位问题。

### 2. 方法论：核心思想与技术细节
ForeSea 采用了一个**三阶段、即插即用**的 VideoRAG 流水线：
1.  **视频数据库构建（跟踪与过滤）**：
    *   使用人像跟踪模块（如 ByteTrack + YOLO）将原始长视频分割成以人为中心的短片段（Clips）。
    *   这种“以人为中心”的策略极大地缩小了搜索空间，过滤了无关的背景信息。
2.  **多模态嵌入与索引**：
    *   使用多模态编码器（基于 GCL/VISTA）将视频片段映射到统一的图像-文本嵌入空间。
    *   **关键点**：该空间允许系统直接比较文本查询、图像查询或“图像+文本”混合查询，避免了传统方法中将所有内容转化为文本描述带来的信息损失。
3.  **查询回答与推理（VideoLLM）**：
    *   **检索**：根据查询向量从数据库中检索 Top-K 个相关片段。
    *   **增强**：在片段帧上绘制边界框（Bounding Box）或在 Prompt 中加入坐标，显式引导模型关注目标。
    *   **生成**：将检索到的片段输入视频大语言模型（如 VideoLLaMA3），生成带有精确时间戳（时序定位）的回答。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **ForeSeaQA（本文提出）**：基于 UCF-Crime 监控视频构建，包含 1,041 个精心策划的问题，涵盖 6 类任务：搜索（Search）、活动（Activity）、事件（Event）、时序（Temporal）、计数（Counting）和异常（Anomaly）。
    *   **通用基准**：为了验证泛化性，还在 VideoMME、MLVU 和 LongVideoBench 等通用长视频基准上进行了测试。
*   **对比方法**：
    *   **原生 VideoLLM**：LLaVA-OneVision, Qwen2.5-VL (7B/72B), InternVL3, VideoLLaMA3。
    *   **RAG 基准**：VideoRAG, T*。

### 4. 资源与算力
*   **模型规模**：主要实验基于 7B 参数规模的模型（如 VideoLLaMA3-7B），同时也对比了 72B 的大模型。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了推理延迟：ForeSea 的端到端延迟约为 **2.6 秒**，优于其他 RAG 基准（5.2-7.6 秒），证明了其高效性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ForeSeaQA 上进行了全方位的对比实验，区分了“纯文本查询”和“多模态查询”两种模式。
    *   进行了详细的**消融实验**，探讨了 Top-K 取值、空间引导方式（裁剪、覆盖、坐标注入）对性能的影响。
    *   在 3 个外部通用长视频数据集上验证了泛化能力。
*   **充分性与客观性**：实验设计较为充分，不仅关注准确率（Accuracy），还引入了时序交并比（Temporal IoU）来衡量定位精度。对比对象涵盖了当前最先进的开源模型，评估过程相对客观。

### 6. 主要结论与发现
*   **性能领先**：在 ForeSeaQA 上，ForeSea 的准确率达到 66.0%，时序 IoU 达到 13.6%，显著优于之前的 VideoRAG 模型。
*   **多模态挑战**：实验发现，现有的 VideoLLM 在处理“图像+文本”混合查询时性能普遍下降，而 ForeSea 表现出更强的鲁棒性。
*   **归纳偏置的有效性**：在监控领域，以人为中心的检索是一种极强的归纳偏置，能显著提升搜索任务的性能。
*   **效率优势**：通过检索减少了输入给 LLM 的帧数，ForeSea 在保持高精度的同时，推理速度比全量处理视频的模型更快。

### 7. 论文亮点
*   **首创性**：提出了首个支持多模态查询且具有精确时序标注的监控视频问答基准（ForeSeaQA）。
*   **实用性**：系统设计考虑了现实取证流程，支持“以图搜人”并给出视频证据。
*   **即插即用**：框架不依赖特定的模型架构，可以随 VideoLLM 或编码器的升级而提升。

### 8. 不足与局限
*   **任务局限性**：系统高度依赖人像跟踪，对于不涉及人物的异常（如火灾、车辆事故等）可能覆盖不足。
*   **计数任务瓶颈**：实验显示，所有模型在“计数（Counting）”任务上表现均较差，说明复杂时序逻辑的精确统计仍是 VideoLLM 的难题。
*   **跟踪质量依赖**：如果初始跟踪模块失败（如严重遮挡或光照极差），后续的检索和推理将受到直接影响。

（完）
