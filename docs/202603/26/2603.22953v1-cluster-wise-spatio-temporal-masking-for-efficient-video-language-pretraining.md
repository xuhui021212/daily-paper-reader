---
title: Cluster-Wise Spatio-Temporal Masking for Efficient Video-Language Pretraining
title_zh: 基于聚类的时空掩码用于高效视频-语言预训练
authors: "Weijun Zhuang, Yuqing Huang, Weikang Meng, Xin Li, Ming Liu, Xiaopeng Hong, Yaowei Wang, Wangmeng Zuo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22953v1"
tags: ["query:lvu"]
score: 9.0
evidence: 通过掩码实现长程时间视频建模的高效架构
tldr: 针对大规模视频-语言预训练中计算成本高、高掩码率导致信息丢失及帧间冗余引起的时间信息泄露等问题，本文提出 ClusterSTM 策略。该方法通过帧内聚类将视觉标记划分为语义独立的簇，并保留簇内时间密度最高的标记，以在大幅降低计算量的同时保留核心时空语义。结合新引入的视频-文本相关性重构目标，ClusterSTM 在多个视频多模态任务上刷新了高效预训练模型的性能记录。
selection_source: fresh_fetch
motivation: 现有的掩码视觉建模在视频预训练中面临高掩码率下的信息严重丢失和帧间相关性导致的时间信息泄露挑战。
method: 提出 ClusterSTM 策略，通过帧内聚类划分语义簇并保留高时间密度标记，并引入视频-文本相关性重构目标以增强多模态语义对齐。
result: 在视频-文本检索、视频问答和视频字幕生成等多个基准测试中，ClusterSTM 均取得了优于现有高效视频-语言模型的 SOTA 性能。
conclusion: ClusterSTM 通过创新的聚类掩码机制和多模态重构目标，实现了高效且高性能的视频-语言预训练。
---

## 摘要
大规模视频-语言预训练在多模态任务中实现了强大的泛化能力，但往往伴随着极高的计算成本。尽管掩码视觉建模（masked visual modeling）的最新进展有助于缓解这一问题，但仍面临两个根本性局限：高掩码率下的严重视觉信息丢失，以及由帧间相关性引起的时间信息泄漏。为应对这些挑战，我们提出了 ClusterSTM，一种用于高效视频-语言预训练的基于聚类的时空掩码策略。ClusterSTM 首先进行帧内聚类，将视觉 token 划分为多个语义独立的聚类，然后通过保留每个聚类中具有最高时间密度的 token 来执行基于聚类的掩码。我们的掩码策略确保保留的 token 在捕捉整体视频内容的同时，展现出强的时间相关性。此外，我们引入了一种视频-文本相关性重构目标，在传统的视觉重构之外对齐高层多模态语义。在多个基准测试上的广泛实验表明，ClusterSTM 在视频-文本检索、视频问答和视频字幕生成任务中取得了卓越的性能，在高效视频-语言模型中确立了新的最先进水平。

## Abstract
Large-scale video-language pretraining enables strong generalization across multimodal tasks but often incurs prohibitive computational costs. Although recent advances in masked visual modeling help mitigate this issue, they still suffer from two fundamental limitations: severe visual information loss under high masking ratios and temporal information leakage caused by inter-frame correlations. To address these challenges, we propose ClusterSTM, a Cluster-Wise Spatio-Temporal Masking strategy for efficient video-language pretraining. ClusterSTM first performs intra-frame clustering to partition visual tokens into multiple semantically independent clusters, then conducts cluster-wise masking by retaining the token with the highest temporal density within each cluster. Our masking strategy ensure that the retained tokens capture holistic video content while exhibit strong temporal correlation. Additionally, we introduce a video-text relevance reconstruction objective that aligns high-level multimodal semantics beyond conventional visual reconstruction. Extensive experiments across multiple benchmarks demonstrate that ClusterSTM achieves superior performance on video-text retrieval, video question answering, and video captioning tasks, establishing a new state-of-the-art among efficient video-language models.

---

## 论文详细总结（自动生成）

### 论文总结：Cluster-Wise Spatio-Temporal Masking for Efficient Video-Language Pretraining (ClusterSTM)

#### 1. 核心问题与整体含义
视频-语言预训练（VLP）虽然强大，但计算成本极高。现有的掩码视觉建模（MVM）方法虽然能提高效率，但存在两个痛点：
*   **视觉信息丢失严重**：在高掩码率（如 90%）下，随机掩码容易丢失关键的背景或细微语义信息。
*   **时间信息泄露**：由于视频帧间存在高度冗余，模型可以轻易通过相邻帧预测出被掩码的部分，导致学习到的表示不够鲁棒。
**ClusterSTM** 的核心目标是在保持高掩码率（高效率）的同时，通过智能选择保留的 token 来解决上述信息丢失与泄露问题。

#### 2. 方法论
该论文提出了两个核心模块：
*   **基于聚类的时空掩码（Cluster-Wise Spatio-Temporal Masking）**：
    1.  **帧内聚类**：对每一帧的视觉 token 使用密度峰值聚类（DPC）算法，将其划分为多个语义独立的簇。这确保了保留的 token 能覆盖视频的整体内容（包括前景和背景）。
    2.  **基于时间密度的选择**：在每个簇内，计算每个 token 与相邻帧 token 的语义距离，定义“时间密度”。算法保留每个簇中时间密度最高的 token。这种策略确保了保留的 token 在时间轴上具有强相关性，从而迫使模型去学习更深层的时空动态，而非简单的帧间插值。
*   **视频-文本相关性重构（Video-Text Relevance Reconstruction）**：
    *   不同于传统的像素或特征重构，该方法引入了**掩码相关性建模（MRM）**。利用教师模型（SigLIP）生成视频 token 与文本特征的细粒度相关性矩阵，作为学生模型的重构目标。这增强了模型在高层多模态语义上的对齐能力。

#### 3. 实验设计
*   **预训练数据集**：WebVid-2M（2.5M 视频-文本对）和 CC3M（3M 图像-文本对），总计约 5M 规模。
*   **下游任务与 Benchmark**：
    *   **视频-文本检索**：MSRVTT, DiDeMo, ActivityNet, MSVD。
    *   **视频问答（Video QA）**：TGIF (Action/Transition/Frame), MSRVTT-MC/QA, ActivityNet-QA, MSVD-QA。
    *   **视频字幕生成（Video Captioning）**：MSRVTT, MSVD。
*   **对比方法**：包括 UMT, STM, Singularity, VIOLET, ALPRO 等高效预训练模型，以及 InternVideo, CLIP4Clip 等大规模预训练模型。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练配置**：预训练 10 个 epoch。
*   **批大小（Batch Size）**：视频流为 1024，图像流为 4096。
*   **优化器**：AdamW，基础学习率为 1e-4。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 大类任务、超过 10 个主流数据集。
*   **消融实验**：非常充分。分别针对掩码策略（帧级、随机、管状、聚类）、掩码比例（图像 75%/视频 90% 等不同组合）、重构目标（像素、HOG、特征、相关性矩阵）以及预训练损失函数进行了详细对比。
*   **客观性**：通过在相同预训练数据规模（5M）下对比 SOTA 模型，证明了方法的优越性；同时展示了在更少数据下达到甚至超过某些大规模（如 138M 数据）模型的效果，体现了实验的公平性与说服力。

#### 6. 主要结论与发现
*   **聚类掩码的有效性**：相比随机掩码，基于聚类的选择能更全面地捕捉视频场景，显著提升检索和问答性能。
*   **时间密度解决泄露**：保留时间一致性强的 token 能有效缓解时间信息泄露，增强模型对视频上下文的理解。
*   **多模态重构优于单模态**：重构“视频-文本相关性”比单纯重构视觉特征更有利于跨模态任务。
*   **效率与性能平衡**：在 90% 的极高视频掩码率下，ClusterSTM 依然刷新了多项任务的 SOTA 记录。

#### 7. 优点
*   **创新性掩码逻辑**：将聚类与时间密度结合，从几何和时间两个维度优化了 token 选择。
*   **高效率**：极高的掩码率大幅降低了计算开销，适合大规模工业应用。
*   **多模态对齐增强**：MRM 损失函数巧妙地利用了教师模型的多模态知识来指导学生模型的重构学习。

#### 8. 不足与局限
*   **教师模型依赖**：该方法依赖于一个预训练良好的教师模型（如 SigLIP）来生成掩码指导和重构目标，这增加了流水线的复杂性。
*   **聚类开销**：虽然论文强调效率，但在预训练过程中对海量视频帧进行实时聚类（DPC 算法）会带来额外的 CPU/GPU 计算负担，文中未详细量化这部分开销。
*   **应用限制**：主要针对短视频片段，对于需要极长程依赖（如数小时的视频）的任务，该掩码策略的有效性尚待验证。

（完）
