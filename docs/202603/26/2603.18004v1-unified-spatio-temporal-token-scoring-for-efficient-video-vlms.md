---
title: Unified Spatio-Temporal Token Scoring for Efficient Video VLMs
title_zh: 针对高效视频视觉语言模型的统一时空 Token 评分
authors: "Jianrui Zhang, Yue Yang, Rohun Tripathi, Winson Han, Ranjay Krishna, Christopher Clark, Yong Jae Lee, Sangho Lee"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18004v1"
tags: ["query:lvu"]
score: 9.0
evidence: 视频视觉语言模型的高效时空令牌评分
tldr: 针对视频视觉语言模型（VLM）中时空冗余导致的计算开销问题，本文提出了一种统一的时空标记评分（STTS）模块。该模块无需文本条件或标记合并，即可在ViT和LLM中同步进行视觉标记剪枝。通过辅助损失学习时间评分并利用LLM梯度进行空间评分，STTS在保持性能基本不变的情况下，显著提升了长短视频问答任务的训练和推理效率，尤其在处理长视频时表现优异。
selection_source: fresh_fetch
motivation: 现有的视频VLM剪枝方法通常仅局限于ViT或LLM内部，且往往依赖复杂的文本条件，难以兼顾效率与多模态任务的适配性。
method: 提出STTS模块，通过辅助损失进行时间维度评分，并结合LLM下游梯度进行空间维度评分，实现跨架构的视觉标记剪枝。
result: "在13个视频问答任务中，STTS剪枝了50%的视觉标记，使训练和推理效率提升了62%，而平均性能仅下降0.7%。"
conclusion: STTS是一种简单且有效的统一剪枝技术，能够显著降低视频VLM的计算成本，为高效的长视频理解提供了新方案。
---

## 摘要
Token 剪枝对于提高视觉语言模型 (VLMs) 的计算效率至关重要，特别是在时间冗余普遍存在的视频任务中。先前的方法通常要么 (1) 仅在视觉 Transformer (ViT) 内部针对动作识别和物体分割等单模态感知任务进行 Token 剪枝，而不适应下游视觉语言任务；要么 (2) 仅在 LLM 内部进行剪枝，同时保持 ViT 输出完整，这通常需要复杂的文本条件 Token 选择机制。在本文中，我们引入了时空 Token

## Abstract
Token pruning is essential for enhancing the computational efficiency of vision-language models (VLMs), particularly for video-based tasks where temporal redundancy is prevalent. Prior approaches typically prune tokens either (1) within the vision transformer (ViT) exclusively for unimodal perception tasks such as action recognition and object segmentation, without adapting to downstream vision-language tasks; or (2) only within the LLM while leaving the ViT output intact, often requiring complex text-conditioned token selection mechanisms. In this paper, we introduce Spatio-Temporal Token Scoring (STTS), a simple and lightweight module that prunes vision tokens across both the ViT and the LLM without text conditioning or token merging, and is fully compatible with end-to-end training. By learning how to score temporally via an auxiliary loss and spatially via LLM downstream gradients, aided by our efficient packing algorithm, STTS prunes 50% of vision tokens throughout the entire architecture, resulting in a 62% improvement in efficiency during both training and inference with only a 0.7% drop in average performance across 13 short and long video QA tasks. Efficiency gains increase with more sampled frames per video. Applying test-time scaling for long-video QA further yields performance gains of 0.5-1% compared to the baseline. Overall, STTS represents a novel, simple yet effective technique for unified, architecture-wide vision token pruning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **STTS (Spatio-Temporal Token Scoring)** 的方法，旨在解决视频视觉语言模型（Video VLMs）中由于视频帧数多、Token 序列过长导致的巨大计算开销问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：视频理解需要处理大量帧，每帧又包含数百个 Patch Token，导致计算量随帧数增加呈二次方增长。
*   **研究动机**：现有的 Token 剪枝方法存在局限性：
    1.  **ViT 内部剪枝**：多用于动作识别等单模态任务，不适应下游的视觉语言推理。
    2.  **LLM 内部剪枝（Post-ViT）**：虽然减少了 LLM 的负担，但 ViT 编码器仍需处理所有原始帧，计算瓶颈依然存在。
    3.  **复杂性**：许多方法依赖复杂的文本条件选择或 Token 合并算法，难以端到端训练。
*   **整体含义**：STTS 提出了一种简单、轻量且统一的方案，在 ViT 内部进行剪枝，从而同时加速视觉编码和语言推理过程。

### 2. 论文提出的方法论
STTS 是一个插入在 ViT 层间的插件模块，核心思想是通过**空间显著性**和**时间冗余性**两个维度对 Token 进行评分并剔除低分 Token。
*   **核心组件**：
    1.  **评分器 (Scorer)**：由 Token 池化层和 3 层 MLP 组成。它接收当前帧与前一帧的特征拼接，捕捉时空上下文。
    2.  **空间评分（偏置注入）**：将评分作为注意力偏置（Attention Bias）注入到随后的 ViT 层中。这使得模型能通过下游 LLM 的梯度回传，自动学会识别哪些空间区域对回答问题更重要（无需文本输入）。
    3.  **时间评分（辅助损失）**：引入辅助的 MSE 损失，强制评分器预测与相邻帧之间的余弦相似度。相似度越高（冗余越大），得分越低。
    4.  **打包算法 (Packing Algorithm)**：由于不同帧剪枝程度不同（非均匀），论文采用“首次下降自适应 (First-fit descending)”算法将剩余 Token 重新打包成紧凑的张量，以实现真正的硬件加速。

### 3. 实验设计
*   **基准模型**：以 **Molmo2** (SigLIP-So400M + Qwen3-4B) 为基础架构。
*   **数据集/场景**：涵盖了 13 个视频问答（Video QA）基准测试，分为短视频（如 NextQA, MVBench）和长视频（如 VideoMME, LongVideoBench, MLVU）。
*   **对比方法**：
    *   主流模型：Qwen3-VL-4B, InternVL3.5-8B, PLM-8B。
    *   剪枝基准：随机剪枝 (Random)、启发式剪枝 (Heuristic)、Token 合并 (ToMe)。
*   **测试时间缩放 (Test-Time Scaling, TTS)**：在推理时增加采样帧数，利用剪枝节省的预算处理更多视觉信息。

### 4. 资源与算力
*   **硬件**：实验在单台配备 **8 张 NVIDIA H100 GPU** 的节点上进行。
*   **训练细节**：在视频 QA 数据子集上微调了 6,250 步，有效 Batch Size 为 128。
*   **效率提升**：在 50% 剪枝率下，训练吞吐量提升了 **1.62x**，推理速度提升了 **1.61x**（在 256 帧设置下加速比可达 **2.2x** 以上）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 13 个不同的视频基准上进行了测试，实验覆盖面广。
*   **消融实验**：非常充分。作者探讨了：
    1.  剪枝比例（30% 到 90%）对性能的影响。
    2.  剪枝插入的 ViT 深度（第 0, 1, 2, 3 层）的影响。
    3.  有无辅助损失（Aux Loss）的对比。
    4.  学习到的评分与简单启发式规则的对比。
*   **公平性**：通过设置“100% 剪枝（纯文本推理）”作为底线，客观评估了视觉信息对模型性能的真实贡献。

### 6. 论文的主要结论与发现
*   **高效性**：STTS 在剪掉 **50%** 视觉 Token 的情况下，13 个任务的平均性能仅下降 **0.7%**，且效率提升约 **62%**。
*   **性能增益**：在 30% 剪枝率下，部分任务性能甚至优于原始基准，说明剪枝起到了“去噪”作用。
*   **长视频优势**：通过测试时间缩放（TTS），在相同计算预算下处理更多帧，长视频 QA 性能提升了 0.5%~1%。
*   **层深敏感性**：在 ViT 极早期（如第 0 层）剪枝会显著损害性能，建议在第 3 层左右进行。

### 7. 优点
*   **架构统一**：一个模块同时解决了 ViT 和 LLM 的计算瓶颈。
*   **无需文本条件**：相比需要文本-图像交叉注意力的剪枝方法，STTS 更简单、计算开销更低。
*   **硬件友好**：通过 Packing 算法解决了稀疏张量无法加速的痛点。
*   **可解释性强**：可视化结果显示，模型能准确识别并保留动态前景（如游戏角色），剪掉静态背景。

### 8. 不足与局限
*   **层深依赖**：虽然论文确定了第 3 层效果较好，但对于不同规模或不同预训练目标的 ViT，最佳插入位置可能需要重新调整。
*   **任务局限**：主要集中在视频问答（QA）任务，尚未在视频生成或实时目标检测等任务中验证。
*   **辅助损失权重**：虽然引入了辅助损失，但对于空间和时间评分权重的平衡可能存在超参数敏感性。

（完）
