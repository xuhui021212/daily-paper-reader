---
title: "OmniFM: Toward Modality-Robust and Task-Agnostic Federated Learning for Heterogeneous Medical Imaging"
title_zh: OmniFM：面向异构医学影像的模态鲁棒与任务无关联邦学习
authors: "Meilin Liu, Jiaying Wang, Jing Shan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21660v1"
tags: ["query:medvid"]
score: 8.0
evidence: 异构医学影像的任务无关联邦学习
tldr: 针对医疗影像联邦学习中任务耦合度高及模态异构性强的问题，本文提出OmniFM框架。该框架利用频域中低频成分的跨模态一致性，通过全局频谱知识检索、交叉注意力融合及频谱提示技术，实现了对分类、分割、超分辨率等多种任务的统一支持。实验证明，OmniFM在处理异构模态数据时表现优异，显著提升了联邦学习的鲁棒性与泛化能力。
selection_source: fresh_fetch
motivation: 现有的联邦学习框架受限于特定任务架构，且在面对医疗机构间多样的影像模态时表现脆弱。
method: 提出OmniFM框架，利用频域低频成分的跨模态一致性，通过频谱知识检索、注意力融合和频谱提示来对齐异构特征。
result: 实验结果显示，OmniFM在处理模态内和跨模态异构性方面均优于现有最先进的联邦学习基准。
conclusion: OmniFM成功实现了模态鲁棒且任务无关的联邦学习，为异构医疗影像协作分析提供了统一且高效的方案。
---

## 摘要
联邦学习（FL）已成为协作式医学图像分析中一种极具前景的范式，然而现有框架仍与特定任务的主干网络紧密耦合，且在异构成像模态下表现脆弱。这些限制阻碍了实际部署，因为不同机构的模态分布差异巨大，且必须支持多种下游任务。为解决这一局限性，我们提出了 OmniFM，这是一个模态无关且任务无关的联邦学习框架，它在无需重新设计优化流水线的情况下，统一了分类、分割、超分辨率、视觉问答和多模态融合的训练。OmniFM 基于一个关键的频域见解：低频光谱分量表现出强大的跨模态一致性，并编码了模态不变的解剖结构。据此，OmniFM 集成了 (i) 全局光谱知识检索以注入全局频率先验，(ii) 嵌入级交叉注意力融合以对齐表示，以及 (iii) 前缀-后缀光谱提示以共同调节全局和个性化线索，并由稳定聚合的光谱近端对齐（Spectral-Proximal Alignment）目标进行联合正则化。在真实世界数据集上的实验表明，OmniFM 在模态内和跨模态异构性方面始终优于最先进的联邦学习基准，在微调和从头训练设置下均取得了卓越的结果。

## Abstract
Federated learning (FL) has become a promising paradigm for collaborative medical image analysis, yet existing frameworks remain tightly coupled to task-specific backbones and are fragile under heterogeneous imaging modalities. Such constraints hinder real-world deployment, where institutions vary widely in modality distributions and must support diverse downstream tasks. To address this limitation, we propose OmniFM, a modality- and task-agnostic FL framework that unifies training across classification, segmentation, super-resolution, visual question answering, and multimodal fusion without re-engineering the optimization pipeline. OmniFM builds on a key frequency-domain insight: low-frequency spectral components exhibit strong cross-modality consistency and encode modality-invariant anatomical structures. Accordingly, OmniFM integrates (i) Global Spectral Knowledge Retrieval to inject global frequency priors, (ii) Embedding-wise Cross-Attention Fusion to align representations, and (iii) Prefix-Suffix Spectral Prompting to jointly condition global and personalized cues, together regularized by a Spectral-Proximal Alignment objective that stabilizes aggregation. Experiments on real-world datasets show that OmniFM consistently surpasses state-of-the-art FL baselines across intra- and cross-modality heterogeneity, achieving superior results under both fine-tuning and training-from-scratch setups.

---

## 论文详细总结（自动生成）

以下是对论文《OmniFM: Toward Modality-Robust and Task-Agnostic Federated Learning for Heterogeneous Medical Imaging》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的医学影像联邦学习（FL）框架存在两个主要瓶颈：
    1.  **任务耦合性高（Task-Specific）**：通常采用“一机一用”设计，针对分类、分割或超分辨率等不同任务需要重新设计优化流水线和聚合策略。
    2.  **模态异构脆弱性（Modality-Fragile）**：不同医疗机构使用的成像设备（如MRI、CT、PET、病理切片）产生的图像特征分布差异巨大，导致局部模型在聚合时产生严重的优化漂移（Optimization Drift）。
*   **研究动机**：开发一个**任务无关（Task-Agnostic）**且**模态鲁棒（Modality-Robust）**的统一联邦学习框架，使一套流水线能同时支持多种医学影像任务，并能有效处理跨机构的模态差异。

### 2. 方法论：核心思想与关键技术
OmniFM 的核心思想基于一个频域见解：**图像的低频光谱分量在不同模态间具有高度一致性，编码了模态不变的解剖结构。**

*   **关键技术组件**：
    1.  **全局光谱知识检索 (GSKR)**：服务器维护一个全局光谱知识库，存储各客户端上传的光谱嵌入。客户端通过余弦相似度检索最相关的 Top-k 全局光谱原型，以引入全局先验。
    2.  **嵌入级交叉注意力融合 (ECA)**：利用交叉注意力机制，将检索到的全局光谱上下文注入到主干网络的表示嵌入中，使模型更关注模态不变的解剖特征而非模态特定的纹理。
    3.  **前缀-后缀光谱提示 (PSP)**：在 Token 序列中，将融合的光谱特征作为“前缀”注入，将客户端特定的可学习 Token 作为“后缀”，兼顾全局一致性与局部个性化。
    4.  **光谱近端对齐 (SPAlign)**：在损失函数中加入正则项，强制局部光谱嵌入靠近检索到的全局原型质心，从优化层面缓解模态引起的漂移。
*   **算法流程**：客户端提取图像特征并进行快速傅里叶变换（FFT）获取光谱嵌入 -> 从服务器检索全局原型 -> 通过 ECA 和 PSP 增强特征 -> 执行任务头预测 -> 利用 SPAlign 更新局部参数 -> 上传光谱嵌入更新服务器知识库。

### 3. 实验设计
*   **数据集与场景**：
    *   **分类**：MedMNIST-v2（包含结肠病理、皮肤镜、血细胞显微镜等，约11万样本）。
    *   **超分辨率**：BreaKHis（乳腺癌组织病理图像，涉及x2, x4, x8缩放）。
    *   **视觉问答 (VQA)**：SLAKE, VQA-RAD, VQA-Med (2019-2021)，涉及 LLaVA 模型的微调。
    *   **分割**：FeTS2022（多机构胶质瘤 MRI 数据）。
    *   **多模态融合**：哈佛医学院脑部影像（MRI, PET, CT, SPECT）。
*   **对比方法 (Benchmarks)**：
    *   通用 FL 算法：FedAvg, FedProx, SCAFFOLD, FedDyn。
    *   个性化 FL 算法：Ditto, MOON, FedRep, FedPer, GPFL。
    *   针对 VQA 的特定方法：FedDAT, PromptFL, F3OCUS。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **模型细节**：提到了使用了 ResNet-18（分类）、UNet（分割）、LLaVA-1.5（VQA，结合 CLIP ViT-B/32 和 LLaMA-3.2-3B），并使用了 LoRA 进行参数高效微调。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 5 大类医学影像任务，每类任务都设置了不同的异构场景（如 Hard/Medium/Extra 场景）。
*   **消融实验**：针对 GSKR、ECA、PSP 和 SPAlign 四个核心组件分别做了消融，验证了各部分的必要性。
*   **充分性评价**：实验设计非常全面，覆盖了从传统视觉任务到大语言模型微调的多种范式。通过跨模态和模态内异构性的多维度对比，证明了框架的通用性。实验对比了超过 10 种主流联邦学习基准，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：OmniFM 在所有测试任务中均显著优于现有最先进（SOTA）的联邦学习方法，尤其是在模态异构性极强的场景下。
*   **任务通用性**：证明了无需针对特定任务修改优化流水线，仅通过频域对齐即可实现多任务支持。
*   **稳定性**：光谱近端对齐（SPAlign）能有效稳定聚合过程，减少通信轮数中的震荡，加快收敛。

### 7. 优点
*   **创新视角**：巧妙利用频域（FFT）的低频特性解决医学影像的模态异构问题，避开了复杂的空间域特征对齐。
*   **高复用性**：解耦了任务逻辑与联邦优化逻辑，为实际医疗场景中“一库多用”的联邦部署提供了可行方案。
*   **隐私友好**：仅上传轻量级的光谱嵌入而非模型参数或原始数据，降低了通信开销并增强了隐私保护。

### 8. 不足与局限
*   **计算开销**：虽然光谱嵌入较轻量，但在每个训练样本上执行 FFT 和交叉注意力计算可能会增加客户端的计算负担。
*   **超参数敏感性**：尽管论文做了敏感性分析，但 Top-k 检索值和对齐系数 $\lambda$ 在不同任务间仍需微调以达到最优。
*   **极端模态缺失**：实验主要集中在常见的医学影像模态，对于极低分辨率或噪声极大的特殊成像模态（如某些超声设备）的鲁棒性尚待验证。

（完）
