---
title: "MemCam: Memory-Augmented Camera Control for Consistent Video Generation"
title_zh: MemCam：面向一致性视频生成的记忆增强型摄像机控制
authors: "Xinhang Gao, Junlin Guan, Shuhan Luo, Wenzhuo Li, Guanghuan Tan, Jiacheng Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26193v1"
tags: ["query:lvu"]
score: 9.0
evidence: 记忆增强的长视频生成与上下文压缩
tldr: 针对交互式视频生成中长视频场景一致性差的问题，本文提出MemCam。该方法将历史帧视为外部记忆，通过上下文压缩模块将其编码为紧凑表示，并利用共视性选择动态检索最相关的历史帧作为条件。MemCam在保持动态相机控制下的场景一致性方面表现优异，显著超越了现有SOTA方法，尤其擅长处理大角度旋转的长视频生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26193v1/fig-001.webp\", \"caption\": \"Fig. 3: Qualitative Comparison Results. (a) and (b) are evaluated on the Context-as-Memory dataset, and (c) and (d) on RealEstate10K. MemCam achieves superior performance in scene memory retention and overall generation quality. In contrast, other methods exhibit varying degrees of scene inconsistency due to insufficient utilization of contextual information.\", \"page\": 4, \"index\": 1, \"width\": 1062, \"height\": 716}, {\"url\": \"assets/figures/arxiv/2603.26193v1/fig-002.webp\", \"caption\": \"TABLE I: Quantitative Comparison Results. We evaluate on two datasets with 90° and 360° round-trip benchmarks. ↑ means higher is better, ↓ means lower is better. Best results are in bold, second best are underlined.\", \"page\": 5, \"index\": 2, \"width\": 752, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2603.26193v1/fig-003.webp\", \"caption\": \"TABLE II: Ablation on Context Selection Strategy.\", \"page\": 5, \"index\": 3, \"width\": 412, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2603.26193v1/fig-004.webp\", \"caption\": \"Fig. 2: Methodology. (Left) Overview of MemCam: the Context Compressor encodes historical frames selected via co-visibility into compact representations, which are concatenated with the noisy prediction sequence and fed into the DiT Block. (Right) Illustration of co-visibility computation between predicted and historical camera FOVs.\", \"page\": 3, \"index\": 4, \"width\": 1074, \"height\": 375}]"
motivation: 现有视频生成方法在动态相机控制下，因上下文信息有限，难以在长视频生成过程中维持场景的连贯性与一致性。
method: 提出一种记忆增强方法，通过压缩历史帧并基于共视性动态检索相关上下文，为相机控制提供持续的一致性引导。
result: 实验表明MemCam在场景一致性指标上显著优于现有基准和开源SOTA模型，特别是在涉及大幅度相机旋转的长视频场景中。
conclusion: MemCam通过有效的记忆管理和上下文检索机制，成功解决了长视频生成中的一致性难题，提升了交互式视频生成的质量。
---

## 摘要
交互式视频生成在场景模拟和视频创作方面具有巨大潜力。然而，由于上下文信息有限，现有方法在动态摄像机控制下的长视频生成过程中，往往难以保持场景一致性。为了应对这一挑战，我们提出了 MemCam，这是一种记忆增强型交互式视频生成方法，它将先前生成的帧视为外部记忆，并将其作为上下文条件，以实现具有高场景一致性的可控摄像机视角。为了实现更长且更相关的上下文，我们设计了一个上下文压缩模块，将记忆帧编码为紧凑表示，并采用基于共视性的选择来动态检索最相关的历史帧，从而在丰富上下文信息的同时减少计算开销。在交互式视频生成任务上的实验表明，MemCam 在场景一致性方面显著优于现有的基线方法以及开源的最先进方法，特别是在具有大角度摄像机旋转的长视频场景中。

## Abstract
Interactive video generation has significant potential for scene simulation and video creation. However, existing methods often struggle with maintaining scene consistency during long video generation under dynamic camera control due to limited contextual information. To address this challenge, we propose MemCam, a memory-augmented interactive video generation approach that treats previously generated frames as external memory and leverages them as contextual conditioning to achieve controllable camera viewpoints with high scene consistency. To enable longer and more relevant context, we design a context compression module that encodes memory frames into compact representations and employs co-visibility-based selection to dynamically retrieve the most relevant historical frames, thereby reducing computational overhead while enriching contextual information. Experiments on interactive video generation tasks show that MemCam significantly outperforms existing baseline methods as well as open-source state-of-the-art approaches in terms of scene consistency, particularly in long video scenarios with large camera rotations.

---

## 论文详细总结（自动生成）

### MemCam: 记忆增强型摄像机控制实现一致性视频生成

#### 1. 核心问题与整体含义（研究动机和背景）
在交互式视频生成领域，现有的扩散模型（如 CameraCtrl, DFoT 等）在处理**长视频序列**和**大幅度相机旋转**时，往往难以维持场景的一致性。主要原因在于模型缺乏有效的“记忆”机制：当相机视角在旋转一圈后回到初始位置时，模型由于上下文窗口有限或缺乏显式历史参考，容易产生内容漂移或幻觉，导致前后场景不匹配。本文提出的 **MemCam** 旨在通过将历史生成的帧作为外部记忆，并结合动态检索与压缩技术，解决长程视频生成中的场景一致性难题。

#### 2. 方法论：核心思想与关键技术
MemCam 的核心思想是将历史帧作为上下文条件注入到 Diffusion Transformer (DiT) 架构中。

*   **上下文压缩模块 (Context Compression Module)：**
    *   为了降低计算开销并引入更多历史信息，设计了一个卷积神经网络来替代原始模型的 patchify 层。
    *   该模块在保持时间维度不变的情况下，将空间维度的长和宽各压缩 2 倍（面积压缩 4 倍），从而生成更紧凑的 Token 表示。
    *   使用旋转位置编码 (RoPE) 来处理变长的上下文输入。
*   **相机控制 (Camera Control)：**
    *   在每个 DiT Block 中集成了一个单层 MLP（Camera Encoder）。
    *   将相机参数（3×4 矩阵平铺为 12 维向量）映射到特征空间，并以元素相加的方式注入到主特征中。
*   **基于共视性的记忆选择 (Co-Visibility Selection)：**
    *   **核心逻辑：** 并非盲目使用所有历史帧，而是计算当前预测帧与历史帧之间的视场 (FOV) 重叠度。
    *   **算法流程：** 采用蒙特卡洛采样 3D 点的方法，计算两个相机位姿下的交并比 (IoU)。公式定义为可见点集的交集除以并集。
    *   **检索机制：** 在推理时，为每个预测帧动态检索共视性得分最高的历史帧作为上下文。

#### 3. 实验设计
*   **数据集：** 
    *   主要在 **Context-as-Memory** 数据集（包含 100 个场景，每个场景约 7901 帧）上训练和评估。
    *   在 **RealEstate10K** 数据集上进行零样本 (Zero-shot) 泛化能力测试。
*   **Benchmark（评估基准）：**
    *   **90° 往返生成：** 相机右转 90° 再转回原点（共 153 帧）。
    *   **360° 往返生成：** 相机顺时针旋转 360° 再逆时针转回原点（共 609 帧），极度考验记忆能力。
*   **对比方法：** 
    *   **I2V (Base)：** 仅以首帧为条件的基准模型。
    *   **DFoT：** 基于固定长度上下文窗口的方法。
    *   **GeometryForcing (GF)：** 结合显式 3D 重建约束的方法。
*   **评估指标：** PSNR, SSIM, LPIPS（衡量重建一致性）以及 FVD（衡量视频整体质量）。

#### 4. 资源与算力
*   **基础模型：** 基于 Wan2.1 1.3B 开源模型构建。
*   **硬件：** 使用了 **2 张 NVIDIA H20 GPU**。
*   **训练参数：** 迭代次数为 20,000 次，学习率 $1 \times 10^{-5}$，Batch size 为 16。
*   **推理：** 采用 50 步去噪采样。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了定量对比实验（两个数据集、两种旋转场景）、定性视觉对比以及详细的消融实验。
*   **消融实验：** 
    *   对比了不同的上下文选择策略（Recent, Random, TopK, Ours），证明了基于共视性的动态选择在长程任务中的优越性。
    *   验证了压缩模块的有效性，对比了不同上下文长度（19, 38, 76 帧）下的性能与推理耗时。
*   **客观性：** 实验设计涵盖了从短程到极长程的挑战，对比了当前主流的开源 SOTA 方法，评估维度全面，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **场景一致性显著提升：** 在 360° 往返测试中，MemCam 的 PSNR 和 SSIM 指标大幅领先，证明其能准确“找回”并重建几百帧前的场景。
*   **压缩的必要性：** 上下文压缩模块在几乎不损失质量的前提下，将推理速度提升了约 5 倍（相比不压缩且使用同等上下文长度的情况）。
*   **检索策略的关键性：** 简单的“最近帧”策略在 360° 旋转中会失效，而基于几何共视性的检索是维持长程一致性的核心。

#### 7. 优点（亮点）
*   **无需显式 3D 重建：** 相比 GeometryForcing 等方法，MemCam 避免了复杂的 3D 模块带来的误差累积和计算负担。
*   **高效的上下文管理：** 通过压缩和动态检索，在有限的计算资源下实现了极长视频的上下文感知。
*   **强大的泛化性：** 在 RealEstate10K 上的零样本表现证明了该架构具有良好的通用性。

#### 8. 不足与局限
*   **推理速度：** 尽管有压缩模块，但由于需要处理双向注意力机制，当前的推理速度（约 4.47s/frame）对于实时交互应用仍显缓慢。
*   **训练规模：** 训练数据集仅包含 100 个场景，虽然模型表现优异，但在更复杂、更多样化的真实世界场景下的鲁棒性仍有待更大规模数据的验证。
*   **蒸馏技术缺失：** 论文提到未来计划探索扩散蒸馏技术以进一步加速生成过程。

（完）
