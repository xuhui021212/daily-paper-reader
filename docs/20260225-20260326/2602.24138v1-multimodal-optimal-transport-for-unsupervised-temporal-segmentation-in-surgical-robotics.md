---
title: Multimodal Optimal Transport for Unsupervised Temporal Segmentation in Surgical Robotics
title_zh: 手术机器人中无监督时序分割的多模态最优传输
authors: "Omar Mohamed, Edoardo Fazzari, Ayah Al-Naji, Hamdan Alhadhrami, Khalfan Hableel, Saif Alkindi, Cesare Stefanini"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.24138v1"
tags: ["query:medvid"]
score: 10.0
evidence: 手术视频中手术阶段和步骤识别的无监督时序分割
tldr: 针对手术机器人视频分段任务，本文提出了一种名为TASOT的无监督多模态最优传输方法。该方法通过结合视频帧的视觉特征和自动生成的文本语义信息，利用非平衡Gromov-Wasserstein公式实现时序一致的动作对齐。TASOT无需大规模手术特定预训练，在多个基准数据集上显著超越了现有的零样本方法，证明了利用标准视觉和文本表示即可实现精细的手术理解。
selection_source: fresh_fetch
motivation: 现有的手术阶段识别方法过度依赖昂贵的大规模标注数据预训练，本文旨在探索无需重度预训练的无监督解决方案。
method: 提出TASOT框架，将时序动作分割建模为多模态最优传输问题，通过加权融合视觉相似度和文本语义成本，并结合时序约束进行优化。
result: "在StrasBypass70、Cholec80等多个手术数据集上，TASOT相比现有零样本方法取得了最高达23.7%的显著性能提升。"
conclusion: 该研究表明，通过有效整合视频中已有的视觉和文本信息，无需复杂的预训练管线即可实现高效的手术动作分割。
---

## 摘要
从视频中识别手术阶段和步骤是计算机辅助干预中的一个基本问题。最近的方法越来越多地依赖于在数千个标记的手术视频上进行大规模预训练，随后将其零样本迁移到特定的手术过程中。虽然这种策略行之有效，但会产生巨大的计算和数据收集成本。在这项工作中，我们质疑这种繁重的预训练是否真的必要。我们提出了文本增强动作分割最优传输（TASOT），这是一种用于手术阶段和步骤识别的无监督方法，它通过整合直接从视频中生成的文本信息，对动作分割最优传输（ASOT）进行了扩展。TASOT 将时序动作分割建模为一个多模态最优传输问题，其中匹配成本被定义为视觉成本和基于文本的成本的加权组合。视觉项捕捉帧级外观相似性，而文本项提供互补的语义线索，两者通过时序一致的非平衡 Gromov-Wasserstein 公式进行联合正则化。这种设计实现了视频帧与手术动作之间的有效对齐，无需针对手术的特定预训练或外部大规模网络监督。我们在多个基准手术数据集上评估了 TASOT，并观察到其相对于现有的零样本方法有持续且显著的提升，包括 StrasBypass70 (+23.7)、BernBypass70 (+4.5)、Cholec80 (+16.5) 和 AutoLaparo (+19.6)。这些结果表明，通过利用标准视觉和文本表示中已经存在的信息，可以实现细粒度的手术理解，而无需诉诸日益复杂的预训练流水线。代码将在 https://github.com/omar8ahmed9/TASOT 上发布。

## Abstract
Recognizing surgical phases and steps from video is a fundamental problem in computer-assisted interventions. Recent approaches increasingly rely on large-scale pre-training on thousands of labeled surgical videos, followed by zero-shot transfer to specific procedures. While effective, this strategy incurs substantial computational and data collection costs. In this work, we question whether such heavy pre-training is truly necessary. We propose Text-Augmented Action Segmentation Optimal Transport (TASOT), an unsupervised method for surgical phase and step recognition that extends Action Segmentation Optimal Transport (ASOT) by incorporating textual information generated directly from the videos. TASOT formulates temporal action segmentation as a multimodal optimal transport problem, where the matching cost is defined as a weighted combination of visual and text-based costs. The visual term captures frame-level appearance similarity, while the text term provides complementary semantic cues, and both are jointly regularized through a temporally consistent unbalanced Gromov-Wasserstein formulation. This design enables effective alignment between video frames and surgical actions without surgical-specific pretraining or external web-scale supervision. We evaluate TASOT on multiple benchmark surgical datasets and observe consistent and substantial improvements over existing zero-shot methods, including StrasBypass70 (+23.7), BernBypass70 (+4.5), Cholec80 (+16.5), and AutoLaparo (+19.6). These results demonstrate that fine-grained surgical understanding can be achieved by exploiting information already present in standard visual and textual representations, without resorting to increasingly complex pre-training pipelines. The code will be available at https://github.com/omar8ahmed9/TASOT.

---

## 论文详细总结（自动生成）

以下是对论文《Multimodal Optimal Transport for Unsupervised Temporal Segmentation in Surgical Robotics》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无需大规模手术特定预训练（Pre-training）和昂贵的人工标注（Dense Annotations）的情况下，实现对手术视频的高效时序分割（识别手术阶段和步骤）。
*   **研究背景**：当前主流方法依赖于在数千个标注视频上进行预训练或复杂的零样本（Zero-shot）迁移，计算成本极高。本文质疑这种重度预训练的必要性，探索是否能通过无监督学习和多模态信息融合来达到甚至超越现有性能。

### 2. 方法论：TASOT 框架
TASOT（Text-Augmented Action Segmentation Optimal Transport）扩展了原有的 ASOT 方法，核心思想是将时序分割建模为一个**多模态最优传输（Optimal Transport, OT）问题**。
*   **字幕生成流水线**：利用大语言模型（Gemini 2.0 Flash）对视频窗口（如 5 分钟片段）生成自然语言描述，获取手术动作的语义线索。
*   **多模态特征提取**：
    *   **视觉**：使用 DINOv3 提取视频帧特征。
    *   **文本**：使用 CLIP 编码生成的字幕，并将其与对应的时间帧对齐。
*   **多模态最优传输公式**：
    *   定义两个成本矩阵：视觉成本（帧与原型间的余弦距离）和文本成本（文本嵌入与原型间的距离）。
    *   **加权融合**：通过权重参数 $\beta$ 将两者结合，形成统一的匹配成本。
    *   **正则化对齐**：采用非平衡 Gromov-Wasserstein 公式，并引入时序先验（Temporal Prior），强制要求动作在时间上具有单调性和一致性。
*   **伪标签学习**：通过 OT 求解得到的传输方案作为伪标签，指导模型进行自监督表示学习。

### 3. 实验设计
*   **数据集**：
    *   **Cholec80**：胆囊切除术视频，包含 7 个阶段。
    *   **AutoLaparo**：腹腔镜子宫切除术，包含 7 个阶段。
    *   **MultiBypass140**：来自两个中心（Strasbourg 和 Bern）的胃旁路手术，包含 12 个阶段和 46 个细粒度步骤。
*   **Benchmark 与对比方法**：
    *   主要对比**零样本（Zero-shot）方法**：如 MIL-NCE、CLIP-SVL、SurgVLP、HecVL 和 PeskaVLP。
    *   评估指标：**Segmental F1 Score**（主要衡量分割的质量和一致性）。
*   **对齐方式**：使用匈牙利算法（Hungarian matching）将无监督聚类结果与真实标签对齐。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **模型组件**：使用了 Gemini 2.0 Flash（API 调用）、DINOv3 和 CLIP 等预训练模型作为特征提取器，这些模型本身具有较高的参数量，但 TASOT 框架本身的训练属于轻量级自监督优化。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流手术基准数据集上进行了测试，涵盖了不同的手术类型和医疗中心。
*   **消融实验**：
    *   对比了仅视觉、仅文本、特征拼接与 TASOT 多模态成本融合的效果。
    *   测试了不同文本编码器（CLIP vs Gemma）的影响。
    *   分析了聚类数量 $k$ 对结果的影响。
*   **充分性评价**：实验设计较为充分，通过跨中心（MultiBypass）的验证展示了模型的鲁棒性。对比对象涵盖了近年来最先进的零样本手术视频理解模型，具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：TASOT 在所有数据集上均显著优于现有的零样本方法。例如，在 StrasBypass70 上 F1 提升了 **+23.7**，在 Cholec80 上提升了 **+16.5**。
*   **无需特定预训练**：证明了利用通用的视觉（DINOv3）和文本（CLIP）表示，结合最优传输理论，即可实现深度的手术语义理解，无需在数万小时的手术视频上进行昂贵的领域特定预训练。
*   **聚类数量的关键性**：实验发现，如果能根据每个视频动态调整聚类数量 $k$（而非固定总类别数），性能会进一步大幅提升，甚至在某些指标上接近有监督学习水平。

### 7. 优点
*   **高效性**：摆脱了对手术领域大规模标注数据的依赖，降低了技术门槛。
*   **多模态融合创新**：首次将文本语义线索引入无监督最优传输框架，解决了纯视觉方法在手术场景中因遮挡、相似解剖结构导致的歧义。
*   **通用性**：该框架不仅限于手术机器人，理论上可推广至任何具有时序逻辑的长视频动作分割任务。

### 8. 不足与局限
*   **固定聚类数限制**：目前模型假设每个视频包含固定数量的动作类别，这与实际手术中步骤可能缺失或重复的情况不符，导致部分视频出现过度分割。
*   **细粒度步骤挑战**：虽然在阶段识别上表现优异，但在极细粒度的“步骤（Steps）”分割上，由于动作持续时间短且视觉差异极小，分割结果仍存在一定的碎片化现象。
*   **依赖外部 LLM**：字幕生成依赖于 Gemini 等高性能大模型，这在离线或隐私敏感的医疗环境中可能存在部署限制。

（完）
