---
title: Do You See What I Am Pointing At? Gesture-Based Egocentric Video Question Answering
title_zh: 你看到我指的东西了吗？基于手势的第一视角视频问答
authors: "Yura Choi, Roy Miles, Rolandos Alexandros Potamias, Ismail Elezi, Jiankang Deng, Stefanos Zafeiriou"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12533v1"
tags: ["query:lvu"]
score: 9.0
evidence: 基于手势的第一视角视频问答
tldr: 本研究针对第一视角AI助手在理解用户指向手势方面的不足，提出了EgoPointVQA数据集和HINT模型。EgoPointVQA包含4400个合成与真实视频，涵盖多种指示性推理任务。HINT通过引入3D手部关键点编码的意图标记，为模型提供显式的空间和时间上下文。实验表明，HINT-14B在多项任务中表现优异，显著超越了现有最先进模型，为手势驱动的视频问答提供了新思路。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在第一视角视频中难以准确推断细粒度的指向意图，且缺乏相关的手势丰富数据集。
method: 提出了包含4400个视频的EgoPointVQA数据集，并开发了HINT模型，通过交织3D手部关键点编码的意图标记来增强空间和时间上下文理解。
result: "HINT-14B在6项任务上的平均准确率达到68.1%，比SOTA模型InternVL3-14B高出6.6%。"
conclusion: 通过结合专门的数据集和显式的手势意图编码，可以显著提升第一视角视频中基于手势的问答性能。
---

## 摘要
理解并根据用户的手势回答问题对于下一代第一视角（egocentric）AI 助手至关重要。然而，由于缺乏丰富的手势数据，且从第一视角视频中推断细粒度指向意图的能力有限，当前的多模态大语言模型（MLLMs）在处理此类任务时面临困难。为解决这一问题，我们推出了 EgoPointVQA，这是一个针对基于手势的第一视角问答的数据集和基准测试，包含跨多个指示性推理任务的 4000 个合成视频和 400 个真实世界视频。在此基础上，我们进一步提出了手部意图标记（Hand Intent Tokens, HINT），它使用现成的重建模型对源自 3D 手部关键点的标记进行编码，并将其与模型输入交错，从而为解释指向意图提供明确的时空上下文。我们展示了我们的模型在不同骨干网络和模型规模下均优于其他模型。特别是，HINT-14B 在 6 个任务上的平均准确率达到 68.1%，比最先进的 InternVL3-14B 高出 6.6%。为了进一步促进开放研究，我们将发布代码、模型和数据集。项目页面：https://yuuraa.github.io/papers/choi2026egovqa

## Abstract
Understanding and answering questions based on a user's pointing gesture is essential for next-generation egocentric AI assistants. However, current Multimodal Large Language Models (MLLMs) struggle with such tasks due to the lack of gesture-rich data and their limited ability to infer fine-grained pointing intent from egocentric video. To address this, we introduce EgoPointVQA, a dataset and benchmark for gesture-grounded egocentric question answering, comprising 4000 synthetic and 400 real-world videos across multiple deictic reasoning tasks. Built upon it, we further propose Hand Intent Tokens (HINT), which encodes tokens derived from 3D hand keypoints using an off-the-shelf reconstruction model and interleaves them with the model input to provide explicit spatial and temporal context for interpreting pointing intent. We show that our model outperforms others in different backbones and model sizes. In particular, HINT-14B achieves 68.1% accuracy, on average over 6 tasks, surpassing the state-of-the-art, InternVL3-14B, by 6.6%. To further facilitate the open research, we will release the code, model, and dataset. Project page: https://yuuraa.github.io/papers/choi2026egovqa

---

## 论文详细总结（自动生成）

### 论文总结：Do You See What I Am Pointing At? Gesture-Based Egocentric Video Question Answering

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着增强现实（AR/VR）设备和智能眼镜（如 Meta Ray-Ban）的普及，AI 助手需要具备理解用户注意力方向的能力。在自然交互中，用户常使用指向手势配合指示性代词（如“这个是什么？”、“那个能吃吗？”）。
*   **核心挑战**：现有的多模态大语言模型（MLLM）在处理此类任务时表现糟糕。
*   **原因分析**：一是缺乏包含丰富手势与第一视角交互的训练数据；二是现有架构通常将视觉信息全局编码，缺乏显式机制来解析手部动作所代表的细粒度空间意图。
*   **研究目标**：建立专门的基准测试，并提出一种能显式理解手势指向意图的模型架构。

#### 2. 论文提出的方法论：HINT (Hand Intent Tokens)
论文提出了一种双流处理架构，将手势信息作为一种独立的模态引入模型：
*   **核心思想**：利用现成的 3D 手部重建模型提取关键点，并将其转化为 LLM 可理解的“意图标记”（Tokens），与视觉标记交织输入。
*   **关键技术细节**：
    1.  **3D 手部姿态提取**：使用 WiLoR 模型从每一帧中提取 21 个 3D 手部关键点坐标。
    2.  **关键点适配器 (Keypoint Adapter)**：一个轻量级的 MLP 模块。它将展平后的关键点向量（63维）投影到 LLM 的嵌入空间，生成“手部意图标记”（$H_t$）。
    3.  **帧-关键点交织 (Interleaving)**：将 $H_t$ 插入到对应视频帧的视觉标记（Visual Tokens）之后。为了减少噪声，仅在手部检测置信度高于阈值 $\tau$（设定为 0.5）时插入标记。
    4.  **训练策略**：使用 LoRA 对视觉编码器和 LLM 进行微调，使模型学会将手势标记与视觉场景中的特定区域及文本中的指示代词进行关联。

#### 3. 实验设计
*   **数据集：EgoPointVQA**
    *   **合成数据**：利用 AI2-THOR 模拟器生成 4000 个视频，通过逆向运动学（IK）确保虚拟人物精准指向目标物体。
    *   **真实数据**：使用 Meta Ray-Ban 智能眼镜采集 400 个真实场景视频，涵盖室内外多种环境。
    *   **任务分类**：包含 6 类推理任务：识别（Reference）、计数（Counting）、空间关系（Spatial）、时间顺序（Temporal）、属性（Attribute）和功能反馈（Feedback）。
*   **Benchmark 与对比方法**：
    *   对比了 15 个模型，包括闭源模型（GPT-5, GPT-4o）和开源模型（Qwen3-VL, InternVL2.5/3, LLaVA-OneVision 等）。
    *   包含专门针对第一视角优化的模型（如 EgoGPT）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到使用 AdamW 优化器，余弦学习率调度，Batch Size 为 32，仅训练 1 个 epoch。
*   **推理开销**：在 InternVL3-8B 骨干网络上，加入 HINT 模块后，32 帧视频的推理时间从 2.58s 仅增加到 2.84s，且 HINT 标记占总 token 数不足 1%，计算开销极低。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 300 个真实世界视频（672 个 QA 对）上进行了详尽的基准测试。
    *   **消融实验**非常充分：测试了有无微调（SFT）、有无 HINT 标记、不同数据比例（合成 vs 真实）、不同的手势建模方式（如在图像上画箭头、输入文本坐标等）、以及手部检测阈值 $\tau$ 的影响。
    *   **泛化性验证**：在 Video-MME、MVBench 等通用视频理解榜单上进行了测试，证明引入手势能力不会导致通用能力的“灾难性遗忘”。
*   **客观性**：引入了人类表现对比（人类准确率 95.9%）和数据集偏差分析（如“盲测”模型是否能仅靠文本猜出答案），确保了实验的严谨性。

#### 6. 论文的主要结论与发现
*   **性能领先**：HINT 显著提升了模型在所有任务上的表现。HINT-14B 达到了 68.1% 的平均准确率，比基座模型 InternVL3-14B 提升了 6.6%。
*   **手势标记的必要性**：实验证明，单纯在数据集上微调（SFT）提升有限，显式的 3D 手部关键点编码对于解决“指向哪里”的问题至关重要。
*   **任务难度差异**：时间顺序（Temporal）任务最具挑战性，要求模型跨帧跟踪多个手势的先后顺序。
*   **合成数据的价值**：大规模合成数据为模型提供了精准的几何对齐监督，是提升性能的关键。

#### 7. 优点
*   **首创性**：填补了第一视角视频中“手势驱动问答”这一重要交互场景的空白。
*   **架构优雅**：HINT 模块极其轻量，不改变 LLM 核心架构，易于迁移到不同的多模态骨干网络。
*   **数据质量高**：结合了高精度的合成数据和多样化的真实世界数据，并提供了细致的任务分类。

#### 8. 不足与局限
*   **依赖外部模型**：HINT 的效果高度依赖于前端手部重建模型（WiLoR）的准确性。在运动模糊或严重遮挡下，性能会受限。
*   **动态场景挑战**：在快速头部转动（Viewpoint Drift）时，模型难以稳定关联手势与目标物体。
*   **交互单一**：目前主要针对单手指向手势，尚未涵盖更复杂的双手交互或动态手势（如挥手、抓取）。
*   **应用限制**：模型目前仍需离线处理 32 帧采样，对于实时 AR 助手的超低延迟需求仍有优化空间。

（完）
