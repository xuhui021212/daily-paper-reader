---
title: RARE disease detection from Capsule Endoscopic Videos based on Vision Transformers
title_zh: 基于 Vision Transformers 的胶囊内镜视频罕见病检测
authors: "X. Gao, C. Chien, G. Liu, A. Manullang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.18045v1"
tags: ["query:medvid"]
score: 10.0
evidence: 使用Transformer进行胶囊内窥镜视频疾病检测
tldr: 本文针对胶囊内镜视频（CEV）的多标签分类挑战，提出了一种基于Vision Transformer的检测方法。研究通过微调Google ViT-B/16模型，实现了对包括解剖部位（如胃、小肠）和病理特征（如活动性出血、息肉）在内的17类标签的自动识别。该方法在Gastro竞赛数据集上进行了验证，为胶囊内镜影像的自动化分析提供了技术参考。
selection_source: fresh_fetch
motivation: 旨在解决胶囊内镜视频中解剖结构与多种罕见疾病特征的自动化多标签分类难题。
method: 基于224x224分辨率的Google Vision Transformer (ViT-B/16) 预训练模型进行微调，用于识别17种不同的解剖和病理标签。
result: "在包含三个视频的测试集上，模型实现的整体mAP@0.5为0.0205，mAP@0.95为0.0196。"
conclusion: Vision Transformer可应用于胶囊内镜的多标签分类任务，但在处理极具挑战性的罕见疾病检测时仍需进一步优化以提高精度。
---

## 摘要
本研究针对胶囊内镜视频（CEV）多标签分类的 Gastro 竞赛。针对该任务，对基于 Transformers 的深度学习网络进行了微调。基础模型采用分辨率为 224 x 224 的 Google Vision Transformer (ViT) batch16。总共对 17 个标签进行了分类，包括口腔、食管、胃、小肠、结肠、Z线、幽门、回盲瓣、活动性出血、血管扩张、血液、糜烂、红斑、血红素、淋巴管扩张、息肉和溃疡。在包含三个视频的测试数据集上，总体的 mAP @0.5 为 0.0205，而总体的 mAP @0.95 为 0.0196。

## Abstract
This work is corresponding to the Gastro Competition for multi-label classification from capsule endoscopic videos (CEV). Deep learning network based on Transformers are fined-tune for this task. The based online mode is Google Vision Transformer (ViT) batch16 with 224 x 224 resolutions. In total, 17 labels are classified, which are mouth, esophagus, stomach, small intestine, colon, z-line, pylorus, ileocecal valve, active bleeding, angiectasia, blood, erosion, erythema, hematin, lymphangioectasis, polyp, and ulcer. For test dataset of three videos, the overall mAP @0.5 is 0.0205 whereas the overall mAP @0.95 is 0.0196.

---

## 论文详细总结（自动生成）

以下是对论文《RARE disease detection from Capsule Endoscopic Videos based on Vision Transformers》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在长达 8 小时、包含约 50,000 帧图像且质量较低（分辨率低、存在气泡、食物残渣等干扰）的胶囊内镜视频（VCE）中，自动识别解剖区域和病理特征。
*   **研究背景**：胃肠道（GI）诊断极具挑战性。传统的单标签分类无法满足临床需求，本研究旨在开发一种**多标签分类模型**，同时识别 8 种解剖部位（如食管、小肠、幽门等）和 9 种病理发现（如活动性出血、息肉、溃疡等），共计 17 个类别。

### 2. 论文提出的方法论
*   **核心模型**：采用了基于 Transformer 架构的 **Vision Transformer (ViT)**。具体为 Google 的 ViT-B/16 预训练模型，输入分辨率调整为 224×224 像素。
*   **关键技术：半监督随机欠采样（Semi-supervised Random Under-sampling）**：
    *   **应对极度不平衡的数据**：原始数据中“结肠”标签超过 187 万条，而“Z线”仅有 122 条。
    *   **多标签优先策略**：采用类似决策树的降序筛选逻辑（从拥有 5 个标签的图像到 1 个标签的图像）。
    *   **样本平衡**：保留所有包含 4 个和 5 个标签的稀有样本，对仅含 1-3 个标签的样本进行随机采样，最终使每个类别的训练样本量平衡在 3,000 左右。

### 3. 实验设计
*   **数据集**：使用 ICPR 2026 RARE-VISION 竞赛提供的胶囊内镜数据集。
*   **标签体系**：17 类标签（8 种解剖 + 9 种病理）。
*   **Benchmark**：竞赛官方提供的测试集（包含 3 个完整的视频）。
*   **对比方法**：本文主要展示了基于 ViT 的微调效果，未在文中详细列出与其他模型（如 ResNet 或 Swin Transformer）的横向对比实验。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及训练时长。
*   **代码实现**：作者提供了 GitHub 仓库链接，暗示了实验的可复现性。

### 5. 实验数量与充分性
*   **实验规模**：针对 17 个类别进行了统一的模型训练与验证。训练集包含 42,234 张图像，验证集包含 10,890 张图像。
*   **充分性评价**：
    *   **数据处理充分**：针对极度不平衡的数据集设计了详细的采样策略。
    *   **实验深度不足**：作为竞赛论文，缺乏消融实验（例如：不同 Patch 大小的影响、不同采样比例的影响）以及与其他 SOTA 模型的性能对比。

### 6. 论文的主要结论与发现
*   **性能指标**：在测试集上的整体 **mAP@0.5 为 0.0205**，**mAP@0.95 为 0.0196**。
*   **视频间差异**：模型在不同视频上的表现差异巨大。视频 001 的 mAP@0.5 达到 0.0596，而视频 003 几乎为 0。
*   **结论**：ViT 能够处理胶囊内镜的多标签分类任务，但面对极度不平衡和低质量的医疗影像，检测精度仍有巨大提升空间。

### 7. 优点
*   **采样策略科学**：针对医疗数据中常见的“长尾分布”问题，提出了优先保留多标签样本的欠采样方法，有效防止了模型被多数类（如结肠、小肠）淹没。
*   **任务全面**：不仅关注疾病检测，还将解剖标志物纳入分类体系，更符合临床实际诊断流程。

### 8. 不足与局限
*   **性能指标较低**：mAP 数值极低（约 2%），说明模型在实际应用中的可靠性尚不足，可能存在大量漏诊或误诊。
*   **缺乏时序信息**：胶囊内镜是视频流数据，本文将其作为独立图像处理，忽略了帧与帧之间的时序关联，这对于识别某些解剖部位至关重要。
*   **泛化能力存疑**：在三个测试视频中表现不一，说明模型对特定视频环境（如光照、清洁度）的鲁棒性较弱。

（完）
