---
title: Towards Long-Form Spatio-Temporal Video Grounding
title_zh: 迈向长视频时空定位
authors: "Xin Gu, Bing Fan, Jiali Yao, Zhipeng Zhang, Yan Huang, Cheng Han, Heng Fan, Libo Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23294v1"
tags: ["query:lvu"]
score: 9.0
evidence: 分钟至小时级视频中的长时空视频定位
tldr: 针对现有视频时空定位（STVG）仅限于短视频的问题，本文提出了长视频时空定位（LF-STVG）任务及ART-STVG架构。该方法将视频视为流式输入，利用自回归Transformer顺序处理帧，并通过带选择策略的空间与时间记忆库建模长程上下文。此外，采用级联时空设计，利用细粒度空间线索辅助复杂的时间定位。实验表明，该方法在长视频定位上显著优于SOTA，且在短视频任务中表现出色。
selection_source: fresh_fetch
motivation: 现有STVG方法难以处理长视频中极长的时空跨度和大量无关信息的干扰，且全量处理帧的方式计算开销巨大。
method: 提出一种自回归Transformer架构，通过流式处理、记忆库选择策略以及级联的时空解码器实现高效的长视频定位。
result: 在LF-STVG数据集上显著超越了现有最先进方法，同时在传统短视频STVG任务中也达到了极具竞争力的水平。
conclusion: ART-STVG通过自回归处理和有效的记忆管理机制，为长视频中的时空目标定位提供了一种高效且准确的解决方案。
---

## 摘要
在真实场景中，视频时长可能跨越数分钟甚至数小时。然而，现有的时空视频定位（STVG）研究在给定文本查询的情况下，主要集中在定位几十秒（通常少于一分钟）短视频中的目标，这限制了其在现实世界中的应用。在本文中，我们探索了长视频时空定位（LF-STVG），旨在定位长视频中的目标。与短视频相比，长视频包含更长的时间跨度和更多的无关信息，这使得一次性处理所有帧的现有 STVG 方法难以应对。为了应对这一挑战，我们为 LF-STVG 提出了一种自回归 Transformer 架构，称为 ART-STVG。与需要整个视频序列一次性做出预测的传统 STVG 方法不同，ART-STVG 将视频视为流式输入并按顺序处理帧，从而能够高效处理长视频。为了建模时空上下文，我们设计了空间和时间记忆库，并将其应用于解码器。由于来自不同时刻的记忆并不总是与当前帧相关，我们引入了简单而有效的记忆选择策略，为解码器提供更相关的信息，从而显著提高了性能。此外，我们提出了一种级联时空设计，将空间解码器连接到时间解码器，而不是并行的空间和时间定位，从而允许细粒度的空间线索辅助长视频中复杂的时间定位。在新扩展的 LF-STVG 数据集上的实验表明，ART-STVG 显著优于现有最先进的方法，同时在传统的短视频 STVG 上也取得了具有竞争力的性能。

## 速览
**TLDR**：针对现有视频时空定位（STVG）方法难以处理长视频的问题，本文提出了长时视频时空定位（LF-STVG）任务及ART-STVG架构。该架构采用自回归Transformer，将视频视为流式输入进行序列化处理，并引入时空记忆库及选择策略来捕捉长程上下文。此外，通过级联设计利用细粒度空间线索辅助复杂的时域定位。实验证明，该方法在长视频定位上显著优于现有技术，且在短视频任务中也极具竞争力。 \
**Motivation**：现有STVG方法主要针对短视频，难以应对长视频中巨大的时间跨度和海量的无关干扰信息。 \
**Method**：提出自回归Transformer架构，利用流式输入处理、时空记忆库选择策略以及级联的时空解码器设计来增强长程建模。 \
**Result**：在扩展的长视频STVG数据集上表现显著优于SOTA方法，同时在传统短视频任务中也达到了极具竞争力的水平。 \
**Conclusion**：ART-STVG通过自回归处理和记忆增强机制，为长视频场景下的高效、精准时空定位提供了有效的解决方案。

---

## Abstract
In real scenarios, videos can span several minutes or even hours. However, existing research on spatio-temporal video grounding (STVG), given a textual query, mainly focuses on localizing targets in short videos of tens of seconds, typically less than one minute, which limits real-world applications. In this paper, we explore Long-Form STVG (LF-STVG), which aims to locate targets in long-term videos. Compared with short videos, long-term videos contain much longer temporal spans and more irrelevant information, making it difficult for existing STVG methods that process all frames at once. To address this challenge, we propose an AutoRegressive Transformer architecture for LF-STVG, termed ART-STVG. Unlike conventional STVG methods that require the entire video sequence to make predictions at once, ART-STVG treats the video as streaming input and processes frames sequentially, enabling efficient handling of long videos. To model spatio-temporal context, we design spatial and temporal memory banks and apply them to the decoders. Since memories from different moments are not always relevant to the current frame, we introduce simple yet effective memory selection strategies to provide more relevant information to the decoders, significantly improving performance. Furthermore, instead of parallel spatial and temporal localization, we propose a cascaded spatio-temporal design that connects the spatial decoder to the temporal decoder, allowing fine-grained spatial cues to assist complex temporal localization in long videos. Experiments on newly extended LF-STVG datasets show that ART-STVG significantly outperforms state-of-the-art methods, while achieving competitive performance on conventional short-form STVG.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Long-Form Spatio-Temporal Video Grounding》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：现有的时空视频定位（STVG）研究主要集中在短视频（通常小于1分钟，如HCSTVG和VidSTG数据集）。然而，在监控、检索等实际应用中，视频往往长达数分钟甚至数小时。
*   **核心问题**：
    1.  **计算瓶颈**：现有模型通常一次性处理所有视频帧，面对长视频时会导致显存爆炸。
    2.  **信息冗余**：长视频包含大量无关信息，现有模型难以在海量干扰中精准捕捉目标事件。
    3.  **长程建模**：如何在长序列中保持目标的一致性并准确界定时间边界。
*   **研究动机**：提出长视频时空定位（LF-STVG）任务，并开发一种能够高效处理流式输入且具备长程记忆能力的架构。

### 2. 方法论
论文提出了 **ART-STVG**（Auto Regressive Transformer for LF-STVG），其核心思想是将视频视为流式输入，逐帧进行自回归预测。
*   **多模态编码器**：
    *   使用 ResNet-101 提取 2D 外观特征，VidSwin 提取 3D 运动特征，RoBERTa 提取文本特征。
    *   通过 Self-Attention Encoder 进行多模态特征融合。
*   **记忆增强解码器（核心技术）**：
    *   **空间记忆库（Spatial Memory Bank）**：存储历史帧的查询特征。采用**文本引导选择策略**，计算记忆与文本的相似度，选取前 $N_s$ 个最相关的记忆来辅助当前帧定位。
    *   **时间记忆库（Temporal Memory Bank）**：存储时间查询特征。采用**事件边界引导策略**，通过计算相邻记忆的余弦相似度识别事件切换点，仅选择与当前帧属于同一事件的记忆，避免无关事件干扰。
*   **级联时空设计**：
    *   不同于传统的并行预测，ART-STVG 先由空间解码器预测 Box，再通过 **RoI Pooling** 提取该区域的细粒度运动特征输入时间解码器。这种“空间辅助时间”的设计增强了复杂场景下的时间边界判定。

### 3. 实验设计
*   **数据集扩展**：由于缺乏 LF-STVG 基准，作者将 HCSTVG-v2 的验证集扩展为三个版本：**LF-STVG-1min、3min、5min**（基于原始 YouTube 视频而非拼接剪辑）。
*   **Benchmark**：以 HCSTVG-v2 原始短视频数据集作为 SF-STVG 的基准。
*   **对比方法**：对比了 TubeDETR, STCAT, CG-STVG, TA-STVG, STVGFormer 等近年来最先进的（SOTA）一阶段模型。
*   **评价指标**：m_tIoU（时间定位）、m_vIoU（时空综合）、vIoU@R（成功率）。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA A100 GPU** 上运行。
*   **算力优势**：
    *   **显存占用**：ART-STVG 仅需 **7.9G** 显存，而对比方法（如 TA-STVG 和 CG-STVG）在处理相同任务时需要 **25.1G - 25.9G**。这证明了自回归架构在处理长视频时的极高资源效率。
    *   **推理速度**：处理 64 帧约需 1.09 秒，虽然比非自回归模型稍慢，但在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个长视频数据集上进行了全方位对比。
    *   在原始短视频数据集上验证了通用性。
    *   **消融实验**：针对空间记忆、时间记忆、记忆选择策略、级联设计、超参数 $N_s$、训练视频长度等进行了 6 组以上的详细消融实验。
*   **充分性评价**：实验设计非常充分。作者不仅证明了模型在长视频上的优越性，还客观地展示了其在短视频任务中依然保持着极具竞争力的性能（仅略低于最新的 TA-STVG）。

### 6. 主要结论与发现
*   **长视频性能飞跃**：在 3min 和 5min 的长视频中，ART-STVG 的表现远超现有模型。例如在 3min 数据集上，m_tIoU 比之前的 SOTA 提升了约 9%。
*   **记忆选择的必要性**：实验发现，如果不加选择地使用所有历史记忆，性能反而会下降（尤其是时间维度），这证明了过滤无关事件信息在长视频理解中的关键作用。
*   **级联结构的有效性**：通过空间定位引导时间定位，比并行预测更能应对长视频中复杂的动作变化。

### 7. 优点
*   **架构创新**：首次将自回归 Transformer 引入 STVG 领域，从根本上解决了长视频处理的显存瓶颈。
*   **策略精巧**：提出的基于事件边界的时间记忆选择策略非常符合人类观察长视频的逻辑。
*   **实用性强**：低显存需求使其更易于在实际生产环境（如边缘计算设备）中部署。

### 8. 不足与局限
*   **推理延迟**：由于是逐帧自回归处理，其推理速度（1.09s）慢于并行处理的 TubeDETR（0.23s）等模型，无法实现超实时处理。
*   **极端场景挑战**：在目标事件极短（如 300s 视频中仅 3s 目标）或背景干扰物极度相似（如双胞胎或相同制服）时，模型仍存在定位漂移的风险。
*   **训练偏差**：模型主要在 20s 的短视频上训练，虽然展现了良好的泛化性，但若能配合长视频训练集，性能可能还有进一步提升空间。

（完）
