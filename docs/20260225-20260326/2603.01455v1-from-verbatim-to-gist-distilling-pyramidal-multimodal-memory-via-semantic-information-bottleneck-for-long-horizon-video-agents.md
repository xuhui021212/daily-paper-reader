---
title: "From Verbatim to Gist: Distilling Pyramidal Multimodal Memory via Semantic Information Bottleneck for Long-Horizon Video Agents"
title_zh: 从逐字记录到要义：通过语义信息瓶颈为长时程视频智能体蒸馏金字塔式多模态记忆
authors: "Niu Lian, Yuting Wang, Hanshu Yao, Jinpeng Wang, Bin Chen, Yaowei Wang, Min Zhang, Shu-Tao Xia"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01455v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于长时视频的金字塔多模态记忆架构
tldr: 针对多模态大模型在长视频理解中面临的上下文限制和记忆冗余问题，本文受模糊痕迹理论启发，提出了MM-Mem架构。该架构通过金字塔式存储（感官缓冲、情节流、符号图谱）将细粒度感知逐步蒸馏为高层语义，并利用语义信息瓶颈（SIB）目标和SIB-GRPO算法优化记忆压缩与关键信息保留。实验证明，MM-Mem在多项长视频基准测试中表现优异，实现了高效的记忆管理与检索。
selection_source: fresh_fetch
motivation: 现有的长视频理解方法在视觉冗余导致的延迟与文本描述导致的细节丢失之间难以平衡，且缺乏类似人类认知的动态记忆机制。
method: 提出金字塔式多模态记忆架构MM-Mem，结合语义信息瓶颈优化目标和熵驱动的自顶向下检索策略，实现从逐字感知到语义要义的层级化蒸馏。
result: 在4个主流基准测试的离线和流式任务中，MM-Mem均取得了显著的性能提升，验证了其在长时程视频智能体中的有效性和鲁棒性。
conclusion: MM-Mem通过模拟人类认知的层级化记忆组织方式，成功解决了长视频理解中的效率与精度矛盾，为构建高效视频智能体提供了新方案。
---

## 摘要
尽管多模态大语言模型在短期推理方面表现出色，但由于上下文窗口有限以及静态记忆机制无法反映人类的认知效率，它们在长时程视频理解方面仍面临挑战。现有的范式通常处于两个极端：以视觉为中心的方法通过密集的视觉累积导致高延迟和冗余，而以文本为中心的方法则因过度的字幕生成而面临细节丢失和幻觉问题。为了弥补这一差距，我们提出了 MM-Mem，这是一种基于模糊痕迹理论（Fuzzy-Trace Theory）的金字塔式多模态记忆架构。MM-Mem 将记忆分层结构化为感官缓冲器（Sensory Buffer）、情境流（Episodic Stream）和符号图式（Symbolic Schema），实现了将细粒度的感知痕迹（逐字记录）逐步蒸馏为高层语义图式（要义）。此外，为了管理记忆的动态构建，我们推导出了一个语义信息瓶颈目标，并引入了 SIB-GRPO 来优化记忆压缩与任务相关信息保留之间的权衡。在推理过程中，我们设计了一种熵驱动的自顶向下记忆检索策略，该策略首先尝试抽象的符号图式，并在不确定性较高时逐步“向下钻取”至感官缓冲器和情境流。在 4 个基准测试上的广泛实验证实了 MM-Mem 在离线和流式任务中的有效性，展示了强大的泛化能力，并验证了受认知启发的记忆组织的有效性。代码可在 https://github.com/EliSpectre/MM-Mem 获取。

## 速览
**TLDR**：针对多模态大模型在长视频理解中面临的上下文窗口限制和记忆效率低的问题，本文提出MM-Mem架构。该架构受模糊痕迹理论启发，构建了由感官缓冲、情节流和符号图式组成的金字塔式记忆体系，通过语义信息瓶颈（SIB）机制将细粒度感知信息提炼为高层语义。实验证明，该方法在离线和流式任务中均表现优异，有效平衡了记忆压缩与关键信息保留。 \
**Motivation**：现有的长视频理解方法在视觉冗余导致的延迟与文本描述导致的细节丢失之间难以平衡，缺乏类似人类认知的动态记忆机制。 \
**Method**：提出金字塔式记忆架构MM-Mem，利用语义信息瓶颈目标和SIB-GRPO算法实现从逐字感知到主旨语义的逐级蒸馏，并配合熵驱动的自顶向下检索策略。 \
**Result**：在4个基准测试上的实验结果显示，MM-Mem在处理长时程视频任务时具有显著的性能提升、鲁棒的泛化能力和高效的记忆组织。 \
**Conclusion**：MM-Mem通过模拟人类认知的记忆分层与提炼机制，为长视频智能体提供了一种兼顾效率与准确性的多模态记忆管理新方案。

---

## Abstract
While multimodal large language models have demonstrated impressive short-term reasoning, they struggle with long-horizon video understanding due to limited context windows and static memory mechanisms that fail to mirror human cognitive efficiency. Existing paradigms typically fall into two extremes: vision-centric methods that incur high latency and redundancy through dense visual accumulation, or text-centric approaches that suffer from detail loss and hallucination via aggressive captioning. To bridge this gap, we propose MM-Mem, a pyramidal multimodal memory architecture grounded in Fuzzy-Trace Theory. MM-Mem structures memory hierarchically into a Sensory Buffer, Episodic Stream, and Symbolic Schema, enabling the progressive distillation of fine-grained perceptual traces (verbatim) into high-level semantic schemas (gist). Furthermore, to govern the dynamic construction of memory, we derive a Semantic Information Bottleneck objective and introduce SIB-GRPO to optimize the trade-off between memory compression and task-relevant information retention. In inference, we design an entropy-driven top-down memory retrieval strategy, which first tries with the abstract Symbolic Schema and progressively "drills down" to the Sensory Buffer and Episodic Stream under high uncertainty. Extensive experiments across 4 benchmarks confirm the effectiveness of MM-Mem on both offline and streaming tasks, demonstrating robust generalization and validating the effectiveness of cognition-inspired memory organization. Code is available at https://github.com/EliSpectre/MM-Mem.

---

## 论文详细总结（自动生成）

以下是对论文《From Verbatim to Gist: Distilling Pyramidal Multimodal Memory via Semantic Information Bottleneck for Long-Horizon Video Agents》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文针对多模态大语言模型（MLLM）在**长时程视频理解**中的瓶颈问题展开研究。
*   **核心矛盾**：现有的长视频处理方法存在两个极端：
    *   **视觉中心化**：通过密集采样积累视觉特征，导致计算冗余、高延迟且难以捕捉长程语义。
    *   **文本中心化**：将视频转化为纯文本描述，虽然高效但属于有损压缩，容易丢失关键视觉细节并引发幻觉。
*   **研究动机**：受人类认知心理学中的**模糊痕迹理论（Fuzzy-Trace Theory, FTT）**启发，人类记忆并非单一记录，而是由“逐字痕迹”（Verbatim，细节）和“要义痕迹”（Gist，抽象意义）并行组成的。论文旨在构建一种能够平衡细节与抽象语义的金字塔式多模态记忆架构。

### 2. 论文提出的方法论
论文提出了 **MM-Mem** 架构，核心包含三个维度：
*   **金字塔记忆结构**：
    1.  **感官缓冲层（Sensory Buffer）**：存储细粒度的视觉证据（关键子片段、时间戳、原始视觉特征）。
    2.  **情节流层（Episodic Stream）**：通过对感官条目进行合并、丢弃或新增，形成紧凑的事件级摘要。
    3.  **符号图式层（Symbolic Schema）**：利用知识图谱结构存储高层语义抽象（实体、关系、全局原型）。
*   **语义信息瓶颈（SIB）与 SIB-GRPO**：
    *   基于**信息瓶颈理论**，推导出在有限预算下保留最大任务相关语义的目标函数。
    *   引入 **SIB-GRPO**（语义信息瓶颈组相对策略优化）算法，利用强化学习训练记忆管理器，在构建情节流时自动剔除冗余并保留关键信息。
*   **熵驱动的自顶向下检索**：
    *   推理时，智能体首先查询最抽象的“符号图式”。
    *   计算预测结果的**熵（不确定性）**。若熵值较高（不确定），则逐步“向下钻取”至情节流甚至感官缓冲层，调取视觉细节进行验证。

### 3. 实验设计
*   **数据集与场景**：
    *   **Video-MME**：涵盖短、中、长三种长度的综合视频评估。
    *   **MLVU**：多任务长视频理解基准。
    *   **VStream-QA**：在线流式视频理解（包含第一人称和电影视角）。
    *   **HD-EPIC++**：论文自建的、具有高度详细标注的第一人称长视频数据集。
*   **对比方法**：
    *   **闭源模型**：Gemini 1.5 Pro/Flash, GPT-4o/4V。
    *   **开源模型**：Qwen2-VL-72B, LLaVA-Video-72B, VideoLLaMA 3 等。
    *   **智能体系统**：Vgent, VideoRAG, VideoMiner, VideoTree 等。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 80GB GPU** 上进行。
*   **模型底座**：使用 **Qwen3-VL-8B** 作为基础多模态模型。
*   **训练细节**：使用 SWIFT 框架进行 LoRA 微调，SIB-GRPO 训练设置为 3 个 epoch，batch size 为 8。
*   **算力说明**：文中未明确给出总训练时长，但提到了使用 A100 集群进行强化学习训练。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准测试上进行了全面评估，涵盖了离线长视频和在线流式视频两种模式。
*   **消融实验**：非常充分。分别验证了 SIB-GRPO、金字塔层级（感官/情节/符号）、视觉记忆 vs 文本记忆的各自贡献。
*   **客观性**：通过 t-SNE 拓扑可视化展示了记忆空间的解耦效果，并使用了 GPT-4o-mini 作为客观评判器（Judge）处理开放式问题，实验设计遵循了学术界的标准流程，对比公平。

### 6. 论文的主要结论与发现
*   **性能领先**：MM-Mem 在所有开源模型和智能体系统中达到了 SOTA（最先进）水平，甚至在某些指标上超越了参数量大得多的 72B 模型和闭源模型（如 Gemini 1.5 Pro）。
*   **记忆解耦的有效性**：实验证明，将视觉细节（逐字）与文本抽象（要义）分离，能有效缓解视觉冗余带来的认知过载。
*   **自适应检索的优势**：熵驱动的策略允许模型在简单问题上快速响应，在复杂问题上深度挖掘，实现了精度与效率的平衡。

### 7. 优点（亮点）
*   **认知启发**：将心理学中的模糊痕迹理论成功转化为可计算的层级记忆模型，具有很强的理论支撑。
*   **动态管理**：不同于以往静态的记忆系统，SIB-GRPO 允许模型根据任务需求动态压缩和更新记忆。
*   **多模态对齐**：通过符号图谱将视觉特征与文本语义深度绑定，增强了跨模态推理能力。

### 8. 不足与局限
*   **计算开销**：虽然推理高效，但金字塔记忆的**构建阶段**（尤其是多层级提取和强化学习训练）相比扁平化模型具有更高的计算成本。
*   **感知依赖**：系统的表现高度依赖于底层视觉编码器和字幕生成器的质量，若初始感知存在严重偏差，高层记忆也会受损。
*   **无监督泛化**：目前的 SIB-GRPO 依赖于任务相关的奖励信号，在完全无监督或开放域的“终身学习”场景下的表现仍需进一步探索。

（完）
