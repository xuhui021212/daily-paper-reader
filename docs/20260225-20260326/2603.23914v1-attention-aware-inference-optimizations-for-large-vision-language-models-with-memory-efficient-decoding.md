---
title: Attention-aware Inference Optimizations for Large Vision-Language Models with Memory-efficient Decoding
title_zh: 针对具有内存高效解码的大型视觉语言模型的注意力感知推理优化
authors: "Fatih Ilhan, Gaowen Liu, Ramana Rao Kompella, Selim Furkan Tekin, Tiansheng Huang, Zachary Yahn, Yichang Xu, Ling Liu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23914v1"
tags: ["query:lvu"]
score: 9.0
evidence: 长上下文任务中大型视觉语言模型的内存高效解码
tldr: 针对大型视觉语言模型（VLM）在处理长序列（如高分辨率图像或视频）时面临的推理内存开销大、效率低的问题，本文提出了 AttentionPack 框架。该框架通过利用 KV 矩阵的隐式低秩结构进行多头注意力压缩，并结合令牌特定的注意力感知解压机制，显著提升了解码阶段的内存效率。实验表明，该方法在保持输出质量的同时，可实现高达 8 倍的内存优化，支持更大批量推理和更长上下文。
selection_source: fresh_fetch
motivation: 视觉语言模型在处理包含大量视觉令牌的长上下文任务时，解码过程中的内存开销和推理延迟成为了主要瓶颈。
method: 提出了 AttentionPack 框架，利用 KV 缓存的隐式低秩结构进行压缩，并采用令牌特定的注意力感知机制进行高效解压。
result: 在多个基准测试中实现了高达 8 倍的内存效率提升，显著增加了推理批量大小并降低了延迟，同时保持了模型性能。
conclusion: AttentionPack 为大模型推理提供了高效的内存管理方案，且能与量化、算子融合等技术结合，进一步增强资源受限环境下的性能。
---

## 摘要
大型视觉语言模型（VLM）在多模态推理方面取得了显著成功，但由于解码过程中的内存开销，其推理时间效率仍面临重大挑战，特别是当 VLM 的查询和回答包含长序列的视觉和文本标记（tokens）时。本文提出了 AttentionPack，这是一个专为大型视觉语言模型量身定制的自适应且注意力感知的优化框架，旨在提高解码过程中的内存效率，重点解决由于视觉输入和交互数量增加而带来的挑战，特别是在涉及多张高分辨率图像或视频的长上下文任务中。AttentionPack 的创新之处体现在两个方面：(i) 我们引入了一种多头注意力压缩方法，通过利用隐式低秩结构来经济地存储键（key）和值（value）矩阵；(ii) 我们开发了一种特定于标记（token-specific）的注意力感知解压缩机制，以减少延迟开销。在多个基准测试上的实验结果表明，AttentionPack 将内存效率提高了多达 8 倍，在保持模型输出质量或支持更长上下文长度以实现卓越检索性能的同时，实现了更高的批处理大小和更快的批处理推理。我们还报告了 AttentionPack 与逐出（eviction）、量化和算子融合（kernel fusion）相结合的有效性，展示了在资源受限环境下进一步的效率提升。

## Abstract
Large Vision-Language Models (VLMs) have achieved remarkable success in multi-modal reasoning, but their inference time efficiency remains a significant challenge due to the memory overhead during decoding, especially when the query and answer of VLMs consist of long sequences of visual and text tokens. This paper presents AttentionPack, an adaptive and attention-aware optimization framework tailored for large vision-language models with improving memory-efficiency during decoding, focusing on addressing the challenges due to the increased high number of visual inputs and interactions, particularly in long-context tasks with multiple high-resolution images or videos. AttentionPack is novel in two aspects: (i) We introduce a multi-head attention compaction method for economically storing key and value matrices by exploiting the implicit low-rank structure, and (ii) we develop a token-specific attention-aware decompression mechanism to reduce latency overhead. Experimental results on multiple benchmarks demonstrate that AttentionPack improves memory efficiency by up to 8x, enabling higher batch sizes and faster batch inference while preserving the model output quality or longer context lengths for superior retrieval performance. We also report the effectiveness of AttentionPack combined with eviction, quantization and kernel fusion, showing further efficiency gains for resource-limited environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AttentionPack** 的推理优化框架，旨在解决大型视觉语言模型（VLM）在处理长上下文（如高分辨率图像和视频）时，因 KV 缓存（Key-Value Cache）过大导致的内存瓶颈和推理延迟问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型在解码阶段需要存储过去所有标记（tokens）的键（Key）和值（Value）向量，即 KV 缓存。随着视觉输入（如多图或长视频）增加，KV 缓存的大小呈线性增长，导致 GPU 显存溢出，并迫使系统频繁进行低效的内存交换（I/O 绑定），严重限制了推理速度和批处理大小（Batch Size）。
*   **研究背景**：现有的优化方法如“令牌逐出”（Eviction）会丢失信息，而“量化”（Quantization）在处理离群值时存在挑战。本文观察到视觉标记的 KV 矩阵在隐藏维度上具有显著的**隐式低秩结构**，这为无损或近无损的压缩提供了可能。

### 2. 方法论
AttentionPack 提出了两个核心技术模块：
*   **多头注意力压缩（Multi-head Compression）**：
    *   **核心思想**：利用奇异值分解（SVD）对 KV 缓存进行降维。
    *   **关键细节**：不同于传统对每个注意力头独立压缩的方法，AttentionPack 将多个头的 KV 矩阵合并后进行 SVD，以捕获跨头的共享信息，从而实现更高的压缩率。
    *   **流程**：在预填充（Prefill）阶段后，将视觉和文本标记分开处理，分别计算低秩分量和解压矩阵。
*   **注意力感知解压缩（Attention-aware Decompression）**：
    *   **核心思想**：并非所有标记在解码时都同等重要。
    *   **技术细节**：通过滑动平均（Moving Average）追踪每个标记的注意力分数（Importance Score）。在解码步骤中，仅对高分标记使用全秩解压，对低分标记使用极低秩解压，从而大幅减少计算量（FLOPs）。
*   **算法流程**：预填充 -> SVD 压缩存储 -> 解码时根据重要性局部解压 -> 计算注意力 -> 更新重要性分数 -> 周期性重新压缩。

### 3. 实验设计
*   **模型**：LLaVA-1.5 (7B/13B), QwenVL-Chat-7B, VideoLLaVA-7B, 以及支持 GQA 的 Qwen3VL-8B。
*   **数据集**：
    *   **图像问答**：A-OKVQA（常识）、OCR-VQA（文本识别）、MMMU（多学科推理）。
    *   **视频问答**：MSVD-QA、MSRVTT-QA。
*   **对比方法（Baselines）**：Full KV Caching（基准）、FastV（令牌跳过）、Scissorhands/H2O（令牌逐出）、Minicache（深度压缩）、KVQuant（量化）。

### 4. 资源与算力
*   **硬件使用**：论文提到在 **NVIDIA RTX 3060** 上进行了延迟测量和算子监控。
*   **基础设施**：使用了佐治亚理工学院的 PACE 高级计算环境资源。
*   **说明**：文中未明确给出总训练时长或总 GPU 小时数，因为该研究侧重于**推理阶段**的即插即用优化，而非从头训练模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流基准数据集，测试了从 7B 到 13B 不同规模的模型，并针对图像和视频两种模态进行了验证。
*   **消融实验**：详细分析了压缩秩（Rank）的大小、解压比例、多头合并的影响，以及与量化、逐出技术的兼容性。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还详细报告了显存减少比例和吞吐量提升，证明了方法在不同场景下的鲁棒性和公平性。

### 6. 主要结论与发现
*   **显存优化**：在 LLaVA 和 VideoLLaVA 上分别实现了约 **5.1倍** 和 **8.1倍** 的 KV 缓存减小。
*   **性能保持**：在压缩率高达 8 倍的情况下，模型在问答任务上的准确率下降极小，甚至在某些 OCR 任务中因压缩过滤了噪声而有所提升。
*   **吞吐量提升**：由于显存占用降低，支持更大的 Batch Size，批处理推理吞吐量提升了 **50% - 74%**。
*   **兼容性**：该方法可与 4-bit 量化、令牌逐出和 FlashAttention 算子融合技术完美结合，进一步压榨性能。

### 7. 优点与亮点
*   **低秩挖掘**：敏锐捕捉到视觉标记在注意力机制中的冗余性，通过跨头 SVD 实现了比单头压缩更高的效率。
*   **动态解压**：引入注意力感知机制，平衡了计算开销与信息完整性。
*   **底层优化**：提供了**融合算子（Fused Kernel）**实现，将解压与注意力计算合并，减少了 HBM 与 SRAM 之间的数据传输，实际加速效果显著。

### 8. 不足与局限
*   **计算开销**：SVD 分解本身具有一定的计算成本，虽然在长序列中被节省的 I/O 时间抵消，但在极短序列推理中可能优势不明显。
*   **文本压缩限制**：实验发现文本标记对压缩更敏感，因此主要针对视觉标记进行高倍压缩，对纯文本长文本任务的优化幅度相对较小。
*   **超参数依赖**：压缩秩 $R$ 和重要性衰减因子 $\alpha$ 需要根据具体模型和任务进行微调，缺乏完全自动化的参数选择机制。

（完）
