---
title: Event-Anchored Frame Selection for Effective Long-Video Understanding
title_zh: 面向高效长视频理解的事件锚定帧选择
authors: "Wang Chen, Yongdong Luo, Yuhui Zeng, Luojun Lin, Tianyu Xie, Fei Chao, Rongrong Ji, Xiawu Zheng"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.00983v1"
tags: ["query:lvu"]
score: 9.0
evidence: 长视频理解中的高效帧选择
tldr: 针对长视频理解中帧冗余和上下文窗口限制的问题，本文提出了一种名为EFS的事件锚定帧选择方法。该方法摒弃了传统的均匀采样，采用分层、感知事件的流程：利用DINO嵌入将视频划分为语义事件段，在段内提取查询相关的锚点帧，并通过自适应MMR算法进行全局优化。作为一种无需训练的即插即用模块，EFS显著提升了现有大视觉语言模型在VideoMME等基准测试上的表现。
selection_source: fresh_fetch
motivation: 传统的均匀采样忽略了视频的结构化语义，导致长视频理解中关键信息丢失且冗余严重。
method: 提出EFS流水线，通过自监督特征划分事件段，结合查询相关性提取锚点，并利用MMR算法平衡覆盖度与多样性。
result: "在LLaVA-Video-7B上应用后，VideoMME、LongVideoBench和MLVU的准确率分别提升了4.7%、4.9%和8.8%。"
conclusion: EFS作为一种高效的免训练帧选择方案，通过结构化先验显著增强了大型视觉语言模型处理长视频的能力。
---

## 摘要
海量的帧冗余和有限的上下文窗口使得高效的帧选择对于利用大型视觉语言模型（LVLM）进行长视频理解至关重要。然而，主流方法采用扁平化采样范式，将视频视为帧的无结构集合。在本文中，我们提出了事件锚定帧选择（EFS），这是一种分层的、事件感知的流水线。利用自监督 DINO 嵌入，EFS 首先将视频流划分为视觉同质的时间段，这些时间段作为语义事件的代理。在每个事件内部，它随后选择与查询最相关的帧作为锚点。这些锚点作为结构先验，引导使用自适应最大边际相关性（MMR）方案的全局细化阶段。该流水线确保最终的关键帧集在事件覆盖率、查询相关性和视觉多样性方面实现联合优化。作为一种无需训练、即插即用的模块，EFS 可以无缝集成到现有的 LVLM 中，在具有挑战性的视频理解基准测试中取得显著提升。具体而言，当应用于 LLaVA-Video-7B 时，EFS 在 VideoMME、LongVideoBench 和 MLVU 上的准确率分别提高了 4.7%、4.9% 和 8.8%。

## 速览
**TLDR**：针对长视频理解中帧冗余和上下文限制问题，本文提出了一种名为EFS的事件锚定帧选择方法。该方法摒弃了传统的扁平采样，采用分层、感知事件的流程：利用DINO嵌入将视频划分为语义事件段，在段内提取查询相关的锚点帧，并通过自适应MMR算法进行全局优化。作为一种无需训练的即插即用模块，EFS显著提升了主流大视觉语言模型在VideoMME等长视频基准测试上的表现。 \
**Motivation**：现有的长视频采样方法多为扁平化处理，忽略了视频的事件结构，导致关键信息丢失或冗余。 \
**Method**：EFS通过自监督特征划分视频事件段，在段内选取查询相关帧作为锚点，并利用自适应最大边界相关性（MMR）进行全局精炼。 \
**Result**：在LLaVA-Video-7B上，EFS在VideoMME、LongVideoBench和MLVU基准上分别实现了4.7%、4.9%和8.8%的准确率提升。 \
**Conclusion**：EFS证明了通过结构化事件感知和查询引导的帧选择，可以在不增加训练成本的情况下大幅增强长视频理解能力。

---

## Abstract
Massive frame redundancy and limited context window make efficient frame selection crucial for long-video understanding with large vision-language models (LVLMs). Prevailing approaches, however, adopt a flat sampling paradigm which treats the video as an unstructured collection of frames. In this paper, we introduce Event-Anchored Frame Selection (EFS), a hierarchical, event-aware pipeline. Leveraging self-supervised DINO embeddings, EFS first partitions the video stream into visually homogeneous temporal segments, which serve as proxies for semantic events. Within each event, it then selects the most query-relevant frame as an anchor. These anchors act as structural priors that guide a global refinement stage using an adaptive Maximal Marginal Relevance (MMR) scheme. This pipeline ensures the final keyframe set jointly optimizes for event coverage, query relevance, and visual diversity. As a training-free, plug-and-play module, EFS can be seamlessly integrated into off-the-shelf LVLMs, yielding substantial gains on challenging video understanding benchmarks. Specifically, when applied to LLaVA-Video-7B, EFS improves accuracy by 4.7%, 4.9%, and 8.8% on VideoMME, LongVideoBench, and MLVU, respectively.

---

## 论文详细总结（自动生成）

以下是对论文《Event-Anchored Frame Selection for Effective Long-Video Understanding》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长视频理解面临海量帧冗余与大视觉语言模型（LVLM）有限上下文窗口之间的矛盾。
*   **研究动机**：现有的帧选择方法多采用“扁平采样（Flat Sampling）”范式（如均匀采样或简单的 Top-K 检索），将视频视为无结构的帧集合。这种方式忽略了视频内在的叙事节奏和事件结构，容易导致关键事件丢失或引入大量语义重复的冗余帧，从而限制了模型在复杂时间推理任务中的表现。

### 2. 方法论：事件锚定帧选择（EFS）
EFS 是一种分层、感知事件且无需训练的流水线，包含以下四个关键步骤：
*   **信号获取**：
    *   **语义信号**：利用 BLIP2-ITM 计算每一帧与用户查询（Query）的相关性得分。
    *   **视觉信号**：利用 DINOv2 提取帧特征，计算相邻帧间的时间相似度曲线。
*   **视觉事件划分**：将相似度曲线的局部极小值视为视觉边界，将视频划分为若干“事件段”。若段数过多，则通过聚类合并相似段，最终保留 $M$ 个宏观事件。
*   **事件锚点定位**：在每个事件段内，选取与查询相关性得分最高的帧作为该事件的“锚点（Anchor）”，确保初步筛选的帧集在覆盖所有事件的同时与查询高度相关。
*   **锚点引导的全局精炼**：采用**自适应最大边际相关性（MMR）**算法。该算法以锚点集为基础，动态计算多样性阈值（基于视频自身的相似度统计分布），从候选帧中补充既相关又具有视觉多样性的帧，形成最终的关键帧集合。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **VideoMME**：全方位多模态视频分析基准（平均时长 17 分钟）。
    *   **LongVideoBench (LVB)**：长上下文交错视频理解基准（平均时长 12 分钟）。
    *   **MLVU**：多任务长视频理解基准（平均时长 11 分钟）。
*   **对比模型**：
    *   **LVLM 基座**：LLaVA-OneVision-7B, Qwen2.5-VL-7B, LLaVA-Video-7B。
    *   **采样策略对比**：Uniform Sampling（均匀采样）、Top-K、BOLT、KFC、AKS 等。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA A800 GPU (80GB)** 和 Intel Xeon Silver 4314 CPU (16核) 的服务器上运行。
*   **效率分析**：论文提供了详细的时间开销表。预处理（视频读取、特征提取）是主要耗时项（长视频约需 124 秒），而 EFS 核心选择逻辑非常轻量，仅占总预处理时间的不到 1%。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流长视频基准测试，测试了 3 种不同的 7B 级 LVLM 模型，并对比了 4 种以上的先进采样策略。
*   **消融实验**：非常充分。作者针对事件划分策略（随机/聚类/DINO）、锚点初始化方式、自适应 MMR 的有效性、特征提取模型（CLIP vs DINOv2）以及超参数（事件数 $M$、松弛因子 $\alpha$）均进行了详细的消融研究。
*   **客观性**：实验在统一的帧预算（如 8/16/32/64 帧）下进行对比，确保了公平性。

### 6. 主要结论与发现
*   **性能提升显著**：在 LLaVA-Video-7B 上，EFS 相比均匀采样在 VideoMME 上提升 4.7%，在 MLVU 上提升达 8.8%。
*   **小模型逆袭**：搭载 EFS 的 7B 模型在某些指标上甚至超越了 GPT-4o 或 GPT-4o-mini。
*   **结构化先验的重要性**：实验证明，感知视频的事件结构比单纯的语义检索更能有效提升长视频的推理准确性。
*   **通用性**：EFS 作为一个即插即用模块，在不同架构的 LVLM 上均表现出稳定的增益。

### 7. 优点
*   **免训练（Training-free）**：无需对现有 LVLM 进行微调，部署成本低。
*   **分层感知**：通过“事件划分-锚点定位-全局精炼”的逻辑，兼顾了覆盖度、相关性和多样性。
*   **自适应性**：MMR 阈值根据视频内容动态调整，能够适应动作片、纪录片等不同节奏的视频。

### 8. 不足与局限
*   **预处理开销**：依赖 DINOv2 和 BLIP2 进行离线特征提取，对于实时性要求极高的场景（如实时直播流分析）存在延迟压力。
*   **超参数敏感性**：虽然提出了自适应机制，但初始事件数 $M$ 等超参数仍需根据数据集进行一定程度的微调以达到最优效果。
*   **上游模型依赖**：EFS 的效果高度依赖于 DINOv2 和 BLIP2 的特征质量，若上游模型在某些特定领域（如极暗环境、医学影像）表现不佳，会直接影响帧选择质量。

（完）
