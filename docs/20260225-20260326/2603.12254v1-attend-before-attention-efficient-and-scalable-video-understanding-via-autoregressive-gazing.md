---
title: "Attend Before Attention: Efficient and Scalable Video Understanding via Autoregressive Gazing"
title_zh: 先关注后注意：通过自回归凝视实现高效且可扩展的视频理解
authors: "Baifeng Shi, Stephanie Fu, Long Lian, Hanrong Ye, David Eigen, Aaron Reite, Boyi Li, Jan Kautz, Song Han, David M. Chan, Pavlo Molchanov, Trevor Darrell, Hongxu Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12254v1"
tags: ["query:lvu"]
score: 10.0
evidence: 通过自回归凝视实现高效且可扩展的视频理解，将视觉标记减少 4-100 倍
tldr: 针对多模态大模型在处理长视频和高分辨率视频时面临的计算冗余问题，本文提出 AutoGaze 模块。该模块通过自回归方式在进入视觉编码器前剔除冗余补丁，仅保留极少数关键多尺度补丁。AutoGaze 实现了 4x-100x 的视觉 Token 减少和高达 19x 的加速，支持 1K 帧 4K 分辨率视频理解，并在 VideoMME 等基准测试中取得领先性能。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理视频时对所有像素均等对待，导致在处理长且高分辨率视频时存在巨大的时空冗余和计算瓶颈。
method: 提出轻量级 AutoGaze 模块，结合自回归预测与强化学习，在指定误差阈值内选择最少的多尺度补丁来重构视频并保留关键信息。
result: "该方法显著减少了视觉 Token 数量并实现高达 19 倍的加速，在 VideoMME 达到 67.0% 的准确率，并在新提出的 HLVid 4K 长视频基准上表现出色。"
conclusion: AutoGaze 有效解决了视频理解中的效率与可扩展性难题，为超长、超高清视频的高效多模态理解提供了极具潜力的技术路径。
---

## 摘要
多模态大语言模型（MLLMs）推动了通用视频理解的发展，但在处理长视频和高分辨率视频时面临挑战——尽管存在显著的时空冗余，它们在视觉 Transformer（ViTs）或 LLMs 中仍平等地处理每一个像素。我们引入了 AutoGaze，这是一个轻量级模块，可在 ViT 或 MLLM 处理之前移除冗余补丁（patches）。AutoGaze 通过下一标记预测和强化学习进行训练，能够自回归地选择一组最小的多尺度补丁，在用户指定的误差阈值内重建视频，从而在保留信息的同时消除冗余。实验表明，AutoGaze 将视觉标记减少了 4 到 100 倍，并将 ViTs 和 MLLMs 的速度提升了高达 19 倍，使 MLLMs 能够扩展到 1000 帧 4K 分辨率的视频，并在视频基准测试中取得了优异成绩（例如在 VideoMME 上达到 67.0%）。此外，我们推出了 HLVid：首个包含 5 分钟 4K 分辨率视频的高分辨率长视频问答基准测试。在该基准测试中，使用 AutoGaze 扩展的 MLLM 比基线模型提升了 10.1%，并比之前的最优 MLLM 提升了 4.5%。项目主页：https://autogaze.github.io/。

## 速览
**TLDR**：针对多模态大模型在处理长视频和高分辨率视频时面临的计算冗余问题，本文提出 AutoGaze 模块。该模块通过自回归方式在进入视觉编码器前剔除冗余补丁，仅保留极少数关键多尺度补丁。AutoGaze 实现了 4x-100x 的视觉 Token 减少和高达 19x 的加速，支持 1K 帧 4K 分辨率视频理解，并在 VideoMME 等基准测试中取得领先性能。 \
**Motivation**：现有的多模态大模型在处理视频时对所有像素平等对待，导致在处理长且高分辨率视频时存在巨大的时空冗余和计算瓶颈。 \
**Method**：提出轻量级 AutoGaze 模块，利用自回归预测和强化学习，在指定误差阈值内选择最少的多尺度补丁来重构视频并保留关键信息。 \
**Result**：该方法将视觉 Token 减少了 4 到 100 倍，推理加速高达 19 倍，并在 VideoMME 及新提出的 HLVid 4K 长视频基准上显著提升了性能。 \
**Conclusion**：AutoGaze 有效解决了视频理解中的效率与可扩展性难题，为超长、超高清视频的高效多模态理解提供了极具潜力的解决方案。

---

## Abstract
Multi-modal large language models (MLLMs) have advanced general-purpose video understanding but struggle with long, high-resolution videos -- they process every pixel equally in their vision transformers (ViTs) or LLMs despite significant spatiotemporal redundancy. We introduce AutoGaze, a lightweight module that removes redundant patches before processed by a ViT or an MLLM. Trained with next-token prediction and reinforcement learning, AutoGaze autoregressively selects a minimal set of multi-scale patches that can reconstruct the video within a user-specified error threshold, eliminating redundancy while preserving information. Empirically, AutoGaze reduces visual tokens by 4x-100x and accelerates ViTs and MLLMs by up to 19x, enabling scaling MLLMs to 1K-frame 4K-resolution videos and achieving superior results on video benchmarks (e.g., 67.0% on VideoMME). Furthermore, we introduce HLVid: the first high-resolution, long-form video QA benchmark with 5-minute 4K-resolution videos, where an MLLM scaled with AutoGaze improves over the baseline by 10.1% and outperforms the previous best MLLM by 4.5%. Project page: https://autogaze.github.io/.

---

## 论文详细总结（自动生成）

### 论文结构化总结：Attend Before Attention (AutoGaze)

#### 1. 核心问题与整体含义（研究动机和背景）
现代多模态大语言模型（MLLMs）在处理视频时，通常对每一帧的每一个像素进行均等计算。然而，视频在时空维度上存在极大的冗余（如静态背景、低细节区域）。这种“全量处理”模式导致模型在面对**长持续时间**（数分钟）和**高分辨率**（4K）视频时，计算开销呈爆炸式增长，ViT（视觉编码器）成为了严重的效率瓶颈。
论文的核心动机是模仿人类视觉的“凝视（Gazing）”机制：在进入昂贵的视觉处理流程之前，先通过一个轻量级模块识别并仅提取关键信息补丁，从而实现高效且可扩展的视频理解。

#### 2. 方法论：核心思想与关键技术
论文提出了 **AutoGaze**，一个仅有 3M 参数的轻量级自回归模块。
*   **核心思想**：在 ViT 处理之前，自回归地选择一组最小的多尺度补丁（Patches），这些补丁配合历史信息足以在用户指定的误差阈值内重构当前帧。
*   **关键技术细节**：
    *   **自回归架构**：由卷积编码器和 Transformer 解码器组成。它逐帧感知，并根据历史帧和已选补丁，像 LLM 生成单词一样生成补丁索引。
    *   **多尺度凝视**：支持不同分辨率（如 32px 到 224px）的补丁选择。简单区域用粗尺度，细节区域（如文字、小物体）用细尺度。
    *   **自动停止机制**：模型带有一个重构损失预测头，当预测到当前已选补丁足以重构该帧（低于设定阈值）时，自动停止采样并跳到下一帧。
    *   **两阶段训练流程**：
        1.  **NTP 预训练**：在通过贪心搜索生成的“最优凝视序列”数据集上进行下一标记预测训练。
        2.  **RL 后训练**：使用强化学习（简化版 GRPO 算法），以重构质量作为奖励信号，进一步优化采样策略。

#### 3. 实验设计
*   **数据集与场景**：涵盖了通用视频问答（VideoMME, MVBench）、长视频理解（LongVideoBench, EgoSchema, MLVU）以及 OOD（分布外）测试（如机器人操作、CCTV 监控、风格迁移视频）。
*   **新基准测试 (HLVid)**：论文推出了首个**高分辨率长视频问答基准**，包含 268 个 QA 对，视频时长达 5 分钟，分辨率为 4K，必须依靠 1K-2K 级别的分辨率感知才能正确回答。
*   **对比方法**：
    *   **模型对比**：InternVL3.5, Qwen2.5-VL, GPT-4o, Gemini 1.5 Pro, VideoChat-Flash 等。
    *   **技术对比**：与多种 Token 削减技术（如 Token Merging, FastV, LongVU 等）进行消融对比。

#### 4. 资源与算力
*   **训练数据规模**：收集了约 800K 段视频，其中 250K 用于预训练。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号（如 H100 或 A100）及总算力消耗。但提到了训练参数：预训练 150 epochs，RL 阶段 3 epochs，Batch Size 为 256。
*   **模型规模**：AutoGaze 模块极小（3M 参数），相比于 8B 或更大的 MLLM 基座，其引入的额外计算开销几乎可以忽略不计。

#### 5. 实验数量与充分性
实验设计非常**充分且客观**：
*   **多维度评估**：不仅测试了下游任务的准确率，还详细测试了 ViT 和 LLM 的端到端推理延迟（Latency）。
*   **消融实验**：针对训练阶段（NTP vs RL）、多尺度设计、多 Token 预测数量、重构损失阈值等关键变量进行了详尽的消融分析。
*   **公平性**：在对比 Token 削减方法时，确保了所有方法在相同的平均 Token 压缩率下进行性能对比。

#### 6. 主要结论与发现
*   **极高的压缩率**：AutoGaze 可将视觉 Token 减少 **4x 到 100x**。对于 30 FPS 的 4K 视频，仅需约 1% 的补丁即可保持性能。
*   **显著的加速**：使 ViT 推理加速高达 **19x**，MLLM 整体加速达 **10x**。
*   **性能提升**：在 HLVid 基准上，通过 AutoGaze 扩展到 1K 帧/4K 分辨率的 NVILA 模型比原始基线提升了 **10.1%**，超越了 Qwen2.5-VL 等强力模型。
*   **泛化能力**：AutoGaze 展现了极强的 OOD 泛化性，能处理从未见过的语义（如机器人抓取）和视觉风格。

#### 7. 优点（亮点）
*   **Attend Before Attention**：突破了以往方法只在 LLM 侧减 Token 的局限，直接解决了 ViT 阶段的计算瓶颈。
*   **多尺度自适应**：能够根据内容复杂度动态分配分辨率，这比固定网格的采样更符合视觉逻辑。
*   **即插即用**：作为一个轻量级前端，它可以集成到现有的各种预训练 ViT 和 MLLM 中，无需对基座模型进行大规模重绘。

#### 8. 不足与局限
*   **相机运动处理**：模型目前对相机平移（Panning）产生的冗余处理不够完美，可能会重复采样在逻辑上已存在的背景。
*   **物理直觉缺失**：重构模型（VideoMAE）是因果的但缺乏物理常识，无法根据物理规律（如重力）预测未来帧的精细运动。
*   **实时性挑战**：虽然大幅加速了推理，但在极高分辨率下，自回归采样过程本身仍存在一定的序列依赖开销。

（完）
