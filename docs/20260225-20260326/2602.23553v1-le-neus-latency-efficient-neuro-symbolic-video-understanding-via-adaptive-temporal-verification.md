---
title: "LE-NeuS: Latency-Efficient Neuro-Symbolic Video Understanding via Adaptive Temporal Verification"
title_zh: LE-NeuS：通过自适应时序验证实现低延迟的神经符号视频理解
authors: "Shawn Liang, Sahil Shah, Chengwei Zhou, SP Sharan, Harsh Goel, Arnab Sanyal, Sandeep Chinchali, Gourav Datta"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23553v1"
tags: ["query:lvu"]
score: 10.0
evidence: 长视频问答的高效延迟神经符号框架
tldr: 针对长视频问答中神经符号方法推理延迟过高的问题，本文提出LE-NeuS框架。该框架通过CLIP引导的两阶段自适应采样减少视觉冗余，并利用批处理命题检测实现并行推理。在保持神经符号方法高准确率优势的同时，将推理延迟从基准的90倍降至约10倍，在LongVideoBench等基准测试中表现优异，显著提升了长视频理解的实用性。
selection_source: fresh_fetch
motivation: 现有的神经符号视频理解方法因密集的序列化命题检测导致推理延迟极高，严重限制了其在延迟敏感场景下的应用。
method: 采用CLIP引导的自适应采样跳过语义冗余帧，并结合批处理命题检测技术实现VLM推理的并行化。
result: "在保持10%以上准确率增益的同时，将推理延迟从原始神经符号方法的90倍缩减至约10倍。"
conclusion: LE-NeuS通过算法优化成功平衡了神经符号视频理解的精度与效率，为长视频理解的高效部署提供了可行方案。
---

## 摘要
神经符号方法在长视频问答（LVQA）中，通过将时序推理建立在形式化验证的基础上，显著提升了准确率。然而，现有方法会产生极高的延迟开销，比基础视觉语言模型（VLM）提示慢多达 90 倍，导致其在延迟敏感的边缘部署中难以应用。我们提出了 LE-NeuS，这是一个低延迟的神经符号框架，它在保留时序逻辑引导的视频理解所带来的准确率优势的同时，大幅降低了推理延迟。我们的核心见解是，主要的计算瓶颈源于自动机构建过程中，跨视频帧进行的顺序且密集的命题检测。我们通过两项原则性优化来解决这一问题：(1) CLIP 引导的两阶段自适应采样，利用视觉冗余跳过语义相似的帧，同时保留时序边界；(2) 批处理命题检测，在时序窗口内实现 VLM 推理的并行化。在理论上，我们推导出了作为视频长度、命题复杂度和采样密度函数的延迟界限，确立了实现延迟效率的条件。在实验上，在部署于 NVIDIA H100 GPU 的 LongVideoBench 和 Video-MME 基准测试中，LE-NeuS 将延迟差距从 90 倍缩小到约 10 倍，同时在时序复杂的查询中保持了超过 10% 的准确率提升。

## 速览
**TLDR**：针对长视频问答（LVQA）中神经符号方法推理延迟过高的问题，本文提出LE-NeuS框架。该框架通过CLIP引导的两阶段自适应采样减少视觉冗余，并利用批处理命题检测实现并行推理。在保持高准确率的同时，将推理延迟从基准的90倍降低至约10倍，显著提升了长视频理解的效率，使其更适用于延迟敏感的边缘部署场景。 \
**Motivation**：现有的神经符号视频理解方法因密集的序列化命题检测导致推理延迟极高，严重限制了其实际应用。 \
**Method**：采用CLIP引导的两阶段自适应采样跳过冗余帧，并结合批处理命题检测来并行化VLM推理过程。 \
**Result**：在多个基准测试中，LE-NeuS将延迟差距从90倍缩减至10倍，同时在复杂查询上保持了10%以上的准确率优势。 \
**Conclusion**：该研究通过算法优化成功平衡了神经符号推理的准确性与效率，为高效长视频理解提供了可行方案。

---

## Abstract
Neuro-symbolic approaches to long-form video question answering (LVQA) have demonstrated significant accuracy improvements by grounding temporal reasoning in formal verification. However, existing methods incur prohibitive latency overheads, up to 90x slower than base VLM prompting, rendering them impractical for latency-sensitive edge deployments. We present LE-NeuS, a latency-efficient neuro-symbolic framework that preserves the accuracy benefits of temporal logic-guided video understanding while drastically reducing inference latency. Our key insight is that the dominant computational bottleneck arises from sequential and dense proposition detection across video frames during automaton construction. We address this through two principled optimizations: (1) CLIP guided two-stage adaptive sampling that exploits visual redundancy to skip semantically similar frames while preserving temporal boundaries, and (2) batched proposition detection that parallelizes VLM inference across temporal windows. Theoretically, we derive latency bounds as a function of video length, proposition complexity, and sampling density, establishing conditions under which latency efficiency is achievable. Empirically, on LongVideoBench and Video-MME benchmarks deployed on NVIDIA H100 GPUs, LE-NeuS reduces the latency gap from 90x to approximately 10x while maintaining >10% accuracy gains on temporally complex queries.

---

## 论文详细总结（自动生成）

### LE-NeuS：通过自适应时序验证实现低延迟的神经符号视频理解

#### 1. 核心问题与研究动机
*   **研究背景**：长视频问答（LVQA）要求模型具备语义对齐、长程时序推理和组合推理能力。现有的视觉语言模型（VLM）在处理长视频时，由于上下文窗口限制，往往采取激进的抽样，导致丢失关键时序细节。
*   **核心问题**：虽然神经符号（Neuro-Symbolic）方法（如 NeuS-QA）通过将查询转化为时序逻辑（TL）并进行形式化验证，显著提升了推理准确率，但其计算开销极大。由于需要对视频帧进行密集的顺序命题检测，其延迟比基础 VLM 高出近 90 倍，难以在边缘设备或实时场景中部署。
*   **研究动机**：开发一种既能保留神经符号方法的高准确率和可解释性，又能大幅降低推理延迟的高效框架。

#### 2. 方法论
LE-NeuS 提出了三项核心优化技术，旨在打破自动机构建过程中的计算瓶颈：
*   **CLIP 引导的两阶段自适应采样**：
    *   **阶段 1：语义相关性过滤**。利用轻量级的 CLIP 图像-文本编码器计算视频帧与查询命题之间的相似度，剔除与任务无关的背景内容。
    *   **阶段 2：视觉冗余消除**。在保留的候选帧中，通过计算相邻帧的 CLIP 视觉特征相似度，跳过视觉重复的帧，仅保留具有显著变化的“关键帧”。
*   **并行/批处理命题检测**：改变了基准方法中“一个命题调用一次 VLM”的顺序模式。LE-NeuS 将多个命题与同一视觉上下文组合成批次（Batch），在单次 VLM 前向传播中完成检测，充分利用 GPU 的并行计算能力。
*   **多段感兴趣帧（FoI）检索策略**：不同于以往只提取单个连续窗口的方法，LE-NeuS 根据模型检测结果提取多个不相连的逻辑一致片段。这种方式在固定的 VLM 输入预算下，提高了证据帧的密度。
*   **理论支撑**：论文推导了延迟界限公式，证明了当视频长度增加时，LE-NeuS 的延迟呈亚线性增长，而基准方法呈线性增长。

#### 3. 实验设计
*   **数据集**：
    *   **LongVideoBench**：主要评估长视频（最长 60 分钟）的复杂推理能力（T3E, E3E, T3O, O3O 子集）。
    *   **Video-MME**：侧重时序推理类别。
    *   **MLVU**：使用 Needle QA（测试长上下文检索）和 Ego Reasoning（测试动作序列理解）。
*   **对比方法**：
    *   **基础 VLM**：InternVL2.5-8B, Qwen2.5-VL-7B, VideoLLaMA3-7B, LLaVA-Video-7B。
    *   **结构化推理框架**：VideoTree, VideoAgent, T*, LVNet。
    *   **基准神经符号方法**：NeuS-QA。

#### 4. 资源与算力
*   **硬件平台**：所有实验和延迟测量均在单张 **NVIDIA H100 GPU** 上完成。
*   **模型配置**：使用 InternVL2-8B 作为命题检测器，GPT-4o 用于查询到时序逻辑（LQ2TL）的翻译。
*   **时长说明**：文中未明确给出总训练时长（因为该方法主要是推理侧的优化），但详细记录了推理延迟：例如 60 分钟视频的推理时间从 957.8s 降至 70.01s。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流长视频基准测试，使用了 4 种不同的 SOTA VLM 作为骨干网络。
*   **消融实验**：专门针对批处理检测、自适应采样和多段检索进行了消融研究，验证了各组件对延迟降低和准确率提升的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还系统分析了不同视频长度下的延迟表现，并提供了形式化的理论证明，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **延迟大幅降低**：LE-NeuS 实现了 **12.53 倍** 的全局加速，将平均推理延迟从约 550 秒缩减至 42 秒左右。
*   **准确率提升**：在 LongVideoBench 上，LE-NeuS 比 NeuS-QA 基准提升了约 **5.21%** 的准确率，在 Video-MME 的时序推理子集上提升了 **12.07%**。
*   **效率优势**：随着视频长度增加，LE-NeuS 的加速效果愈发明显（在 1 小时视频上达到 13.66 倍加速），证明了其处理超长视频的潜力。

#### 7. 优点
*   **高效性**：成功解决了神经符号方法在视频领域“好用但太慢”的痛点。
*   **形式化保证**：保留了时序逻辑的严谨性，使得推理过程具有可解释性和逻辑一致性。
*   **即插即用**：作为一种框架，它可以适配各种主流的 VLM 骨干网络。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了完备的延迟复杂度理论分析。

#### 8. 不足与局限
*   **感知层依赖**：系统的最终表现仍高度依赖于底层 VLM（如 InternVL2）对原子命题检测的准确性。
*   **采样风险**：虽然自适应采样能跳过冗余，但在极端情况下（如极短且关键的动作），CLIP 可能会因语义理解不足而误删关键帧。
*   **翻译成本**：依赖 GPT-4o 进行自然语言到时序逻辑的翻译，这在离线或完全本地化的部署中可能存在成本或隐私限制。

（完）
