---
title: Stateful Token Reduction for Long-Video Hybrid VLMs
title_zh: 长视频混合 VLM 的有状态 Token 缩减
authors: "Jindong Jiang, Amala Sanjay Deshmukh, Kateryna Chumachenko, Karan Sapra, Zhiding Yu, Guilin Liu, Andrew Tao, Pavlo Molchanov, Jan Kautz, Wonmin Byeon"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2603.00198v1"
tags: ["query:lvu"]
score: 9.0
evidence: 通过令牌缩减加速长视频视觉语言模型
tldr: "本文针对长视频混合架构（Attention+Mamba）视觉语言模型，提出了一种状态感知的Token缩减方法。通过分析发现Token重要性在层间具有稀疏性但缺乏稳定性，据此设计了由低到高的渐进式缩减策略及统一的语言感知评分机制。该方法在保留25%视觉Token的情况下，实现了约4倍的预填充加速，并在长视频理解任务中保持了极高的精度。"
selection_source: fresh_fetch
motivation: 现有Token缩减技术主要针对纯Transformer架构，无法直接有效处理结合了Mamba等线性状态空间块的混合架构长视频模型。
method: 提出了一种渐进式缩减方案，并利用隐式注意力代理为Attention和Mamba块构建了统一的语言感知Token评分机制。
result: "在仅保留25%视觉Token的激进压缩下，实现了3.8至4.2倍的预填充加速，且模型精度接近原始基准。"
conclusion: 该研究验证了在混合架构VLM中进行全层Token缩减的高效性，为长视频理解的高效推理提供了有力支持。
---

## 摘要
Token 缩减是加速长视频视觉语言模型 (VLM) 的有效方法，但现有方法大多针对密集 Transformer 设计，未直接考虑将注意力机制与线性时间状态空间块（如 Mamba）交错的混合架构。我们研究了针对混合视频 VLM 的查询条件 Token 缩减，并通过两个属性分析了缩减行为：层级稀疏性（有多少 Token 捕捉到了与查询相关的信息）和重要性稳定性（Token 重要性排名是否随深度保持一致）。尽管 Token 重要性在每一层内是稀疏的，但重要 Token 的集合会随层数变化，因此激进的早期剪枝是不可靠的。受此启发，我们提出了一种由低到高的渐进式缩减方案，以及一种针对注意力块和 Mamba 块（对 Mamba 使用隐式注意力代理）的统一语言感知评分机制，从而实现了混合模型中的全层 Token 缩减。在激进的压缩设置下（保留 25% 的视觉 Token），我们的方法在测试时实现了显著的预填充加速（3.8-4.2 倍），且准确率接近基线水平；在缩减条件下进行轻量级微调可进一步提升在长上下文视频基准测试上的性能。

## 速览
**TLDR**：本文针对长视频混合架构（Attention+Mamba）视觉语言模型，提出了一种状态感知的Token缩减方法。通过分析发现Token重要性在层间具有稀疏性但缺乏稳定性，据此设计了渐进式缩减策略和统一的语言感知评分机制。该方法在大幅减少视觉Token的同时，显著提升了预填充速度并保持了高水平的视频理解性能。 \
**Motivation**：现有Token缩减技术主要针对Transformer架构，无法直接有效处理结合了Mamba等线性状态空间块的混合架构长视频模型。 \
**Method**：提出了一种从低到高的渐进式缩减方案，并利用隐式注意力代理为Attention和Mamba块构建了统一的语言感知评分机制。 \
**Result**：在仅保留25%视觉Token的情况下，实现了3.8至4.2倍的预填充加速，且在长视频基准测试中表现出接近基准的准确率。 \
**Conclusion**：该研究验证了在混合架构VLM中实施全层Token缩减的有效性，为高效长视频理解提供了新的技术路径。

---

## Abstract
Token reduction is an effective way to accelerate long-video vision-language models (VLMs), but most existing methods are designed for dense Transformers and do not directly account for hybrid architectures that interleave attention with linear-time state-space blocks (e.g., Mamba). We study query-conditioned token reduction for hybrid video VLMs and analyze reduction behavior through two properties: layerwise sparsity (how many tokens capture query-relevant information) and importance stability (whether token-importance rankings persist across depth). Although token importance is sparse within each layer, the set of important tokens changes across layers, so aggressive early pruning is unreliable. Motivated by this, we propose a low-to-high progressive reduction schedule and a unified language-aware scoring mechanism for both attention and Mamba blocks (using an implicit-attention proxy for Mamba), enabling all-layer token reduction in hybrids. Under an aggressive compression setting (retaining 25% of visual tokens), our approach delivers substantial prefilling speedups (3.8--4.2x) with near-baseline accuracy at test time, and light finetuning under reduction further improves performance on long-context video benchmarks.

---

## 论文详细总结（自动生成）

### 论文总结：长视频混合 VLM 的有状态 Token 缩减（Stateful Token Reduction）

#### 1. 论文的核心问题与整体含义
随着视频理解任务向长时程（Long-horizon）发展，视觉 Token 的数量呈爆炸式增长，导致推理（尤其是预填充阶段）计算成本极高。现有的 Token 缩减技术（如剪枝、合并）主要针对纯 Transformer 架构设计。
**核心问题**：如何针对结合了注意力机制（Attention）与线性时间状态空间块（Mamba）的**混合架构（Hybrid Architecture）**视频 VLM 进行高效的 Token 缩减？论文探讨了混合模型在处理长视频时，Token 的重要性分布及其在层间的稳定性，旨在实现大幅加速的同时保持理解精度。

#### 2. 论文提出的方法论
论文提出了一种针对混合架构的**有状态 Token 缩减方案**，核心思想包括：
*   **统一的查询条件评分机制（Query-Conditioned Scoring）**：
    *   **注意力层**：利用标准的文本到视觉（Text-to-Vision）注意力权重作为重要性得分。
    *   **Mamba 层**：推导出一种**隐式注意力代理（Implicit-attention proxy）**。通过分析 Mamba 的选择性扫描公式，提取输入投影 $b$（类比 Key）和输出投影 $c$（类比 Query）的点积作为对齐分数，从而在非注意力块中也能识别关键 Token。
*   **由低到高的渐进式缩减策略（Low-to-High Progressive Schedule）**：
    *   **分析发现**：虽然单层内 Token 重要性是稀疏的，但层间稳定性（Rank Stability）较差，尤其是早期层。
    *   **核心逻辑**：在模型早期层保留较多 Token，允许 Mamba 的循环状态（Recurrent State）充分累积信息；随着深度增加，逐步加大缩减力度。这种“先积累后压缩”的策略利用了 Mamba 状态作为“压缩记忆”的特性。
*   **缩减调度函数**：采用了阶梯衰减（Step Decay）和 Sigmoid 曲线衰减两种形式来控制各层的 Token 保留比例。

#### 3. 实验设计
*   **模型对象**：
    *   混合架构：**Nemotron-Nano-V2 VL 12B**（包含 Mamba-2、Attention 和 MLP 块）。
    *   Transformer 架构（对比组）：**Qwen3-VL 8B**。
*   **数据集与 Benchmark**：
    *   **VideoMME**：全方位视频分析基准。
    *   **LongVideoBench (LongVB)**：针对长上下文交织视频语言理解。
    *   **LVBench**：极长视频（30分钟至2小时）理解基准。
*   **对比维度**：对比了无缩减基线、仅注意力层缩减、仅 Mamba 层缩减、全层缩减，以及不同的缩减比例（25%、35%、50%）。

#### 4. 资源与算力
*   **硬件使用**：论文在延迟测量部分明确提到了使用单张 **NVIDIA A100 GPU**。
*   **训练细节**：训练分为四个阶段（从对齐到长上下文微调），最大视频帧数从 32 帧扩展到 128 帧。
*   **算力总量**：文中未明确给出总的 GPU 小时数或集群规模，但详细描述了多阶段微调的样本量（如 Stage 1 使用了约 1250 万个样本）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括：
    1.  不同缩减模式（1st Attn, All Attn, Mamba+Attn 等）的对比。
    2.  不同缩减类型（基于查询的选择 vs. 平均池化）的对比。
    3.  不同架构（Hybrid vs. Transformer）在相同缩减比例下的鲁棒性对比。
*   **充分性评价**：实验设计较为充分且客观。通过跨架构对比，揭示了混合模型由于具备“循环记忆状态”，在激进缩减下比纯 Transformer 表现更稳健，这一发现具有较强的说服力。

#### 6. 主要结论与发现
*   **混合架构的优势**：混合模型对 Token 缩减的容忍度显著高于 Transformer。在保留 25% Token 时，混合模型精度接近基线，而 Transformer 精度大幅下降。
*   **加速效果**：在激进的 25% 保留率下，实现了 **3.8x 至 4.2x** 的预填充（TTFT）加速。
*   **训练增强**：虽然测试时直接缩减（Training-free）效果已不错，但配合轻量级微调（Train-time reduction），模型性能甚至能超过无缩减的基线。
*   **内存优化**：Token 缩减显著降低了长视频推理时的显存占用，解决了长视频输入导致的 OOM（显存溢出）问题。

#### 7. 优点
*   **架构创新**：首次深入研究了混合架构（Mamba+Transformer）中的 Token 缩减问题，填补了该领域的空白。
*   **理论支撑**：通过稀疏性和层间稳定性的定量分析来指导缩减策略的设计，而非纯经验主义。
*   **实用性强**：提出的 Mamba 隐式注意力评分机制计算开销小，且能显著提升长视频推理效率。

#### 8. 不足与局限
*   **评分开销**：虽然缩减了后续计算，但计算 Mamba 层的隐式注意力分数本身会带来一定的额外开销（约占总推理时间的 20% 左右）。
*   **模型局限**：实验主要集中在 8B-12B 规模的模型，尚未验证在超大规模（如 70B+）混合模型上的表现。
*   **任务偏差**：虽然在长视频 QA 上表现优异，但对于需要极高空间细节（如微小物体检测）的任务，激进的 Token 缩减可能仍存在风险。

（完）
