---
title: Recurrent Reasoning with Vision-Language Models for Estimating Long-Horizon Embodied Task Progress
title_zh: 基于视觉语言模型的循环推理用于估计长程具身任务进度
authors: "Yuelin Zhang, Sijie Cheng, Chen Li, Zongzhao Li, Yuxin Huang, Yang Liu, Wenbing Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17312v1"
tags: ["query:lvu"]
score: 9.0
evidence: 循环推理框架，通过不断演化的思维链维持长视频的全局上下文
tldr: 针对具身智能体在长程任务中进度估计难、计算开销大的问题，本文提出 R2VLM 模型。该模型采用循环推理框架，通过处理局部视频片段并维护一个不断演进的思维链（CoT）来记录任务分解和完成状态，从而在降低计算成本的同时增强了对复杂时间依赖的推理能力。在 ALFRED 和 Ego4D 数据集上的实验证明，该方法在进度估计及下游任务中均达到了 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理长视频任务进度估计时，往往忽视了复杂的推理潜力且计算成本过高。
method: 提出一种循环推理框架，利用演进的思维链（CoT）迭代处理局部视频片段，以维护全局上下文并记录任务步骤状态。
result: 在 ALFRED 和 Ego4D 数据集上取得了领先的进度估计性能，并有效提升了策略学习和奖励建模等下游任务的效果。
conclusion: R2VLM 通过结合循环机制与显式推理，为长程具身任务的进度感知提供了一种高效且泛化性强的解决方案。
---

## 摘要
准确估计任务进度对于具身智能体规划和执行长程、多步任务至关重要。尽管取得了显著进展，现有的基于视觉语言模型（VLM）的方法主要利用其视频理解能力，而忽略了其复杂的推理潜力。此外，使用 VLM 处理长视频轨迹在实际部署中计算成本过高。为了应对这些挑战，我们提出了循环推理视觉语言模型（$\text{R}^2$VLM）。该模型采用循环推理框架，迭代处理局部视频片段，并通过不断演进的思维链（CoT）维持全局上下文。该思维链明确记录了任务分解、关键步骤及其完成状态，使模型能够推理复杂的时间依赖关系。这种设计在保留核心推理能力的同时，避免了处理长视频的高昂成本。我们在来自 ALFRED 和 Ego4D 的大规模自动生成数据集上训练了 $\text{R}^2$VLM。在进度估计及下游应用（包括进度增强的策略学习、强化学习的奖励建模和主动辅助）上的广泛实验表明，$\text{R}^2$VLM 具有强大的性能和泛化能力，在长程任务进度估计方面达到了新的最先进水平。模型和基准测试已在 Hugging Face 上公开。

## 速览
**TLDR**：针对具身智能体在长程任务中进度估计难、计算开销大的问题，本文提出了 R2VLM 模型。该模型采用循环推理框架，通过迭代处理局部视频片段并维护一个不断演化的思维链（CoT）来记录任务分解与完成状态，从而在降低计算成本的同时增强了对复杂时间依赖的推理能力。实验证明其在进度估计及下游任务中达到了 SOTA 水平。 \
**Motivation**：现有的视觉语言模型在处理长视频任务进度估计时，往往忽略了推理潜力且计算成本过高。 \
**Method**：提出一种循环推理视觉语言模型（R2VLM），通过迭代处理视频片段并利用演化思维链（CoT）维护全局上下文。 \
**Result**：在 ALFRED 和 Ego4D 数据集上的实验表明，R2VLM 在进度估计和下游应用（如策略学习、奖励建模）中均取得了领先性能。 \
**Conclusion**：R2VLM 有效结合了循环机制与思维链推理，为长程具身任务的进度感知提供了高效且准确的解决方案。

---

## Abstract
Accurately estimating task progress is critical for embodied agents to plan and execute long-horizon, multi-step tasks. Despite promising advances, existing Vision-Language Models (VLMs) based methods primarily leverage their video understanding capabilities, while neglecting their complex reasoning potential. Furthermore, processing long video trajectories with VLMs is computationally prohibitive for real-world deployment. To address these challenges, we propose the Recurrent Reasoning Vision-Language Model ($\text{R}^2$VLM). Our model features a recurrent reasoning framework that processes local video snippets iteratively, maintaining a global context through an evolving Chain of Thought (CoT). This CoT explicitly records task decomposition, key steps, and their completion status, enabling the model to reason about complex temporal dependencies. This design avoids the high cost of processing long videos while preserving essential reasoning capabilities. We train $\text{R}^2$VLM on large-scale, automatically generated datasets from ALFRED and Ego4D. Extensive experiments on progress estimation and downstream applications, including progress-enhanced policy learning, reward modeling for reinforcement learning, and proactive assistance, demonstrate that $\text{R}^2$VLM achieves strong performance and generalization, achieving a new state-of-the-art in long-horizon task progress estimation. The models and benchmarks are publicly available at \href{https://huggingface.co/collections/zhangyuelin/r2vlm}{huggingface}.

---

## 论文详细总结（自动生成）

以下是对论文《Recurrent Reasoning with Vision-Language Models for Estimating Long-Horizon Embodied Task Progress》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：具身智能体（Embodied Agents）在执行长程（Long-horizon）、多步骤任务时，准确估计任务进度对规划和决策至关重要。
*   **核心挑战**：
    1.  **推理能力不足**：现有基于视觉语言模型（VLM）的方法多侧重于视频理解，忽略了对复杂时间依赖关系的逻辑推理。
    2.  **计算开销巨大**：直接处理长视频轨迹会导致显存和计算成本呈线性或指数级增长，难以在实际场景中实时部署。
*   **研究目标**：开发一种既能利用 VLM 推理潜力，又能高效处理长视频流的进度估计模型。

### 2. 论文提出的方法论：$\text{R}^2$VLM
*   **核心思想**：引入**循环推理（Recurrent Reasoning）**框架。模型不再一次性处理全量视频，而是迭代处理局部视频片段（Video Snippets），并通过一个不断演进的**思维链（Chain of Thought, CoT）**来维护全局上下文。
*   **关键技术细节**：
    *   **循环机制**：在每个时间步 $t$，模型输入当前的视频片段 $v_t$ 和上一步生成的历史思维链 $c_{t-1}$，输出更新后的思维链 $c_t$ 和进度估计值 $p_t$。
    *   **CoT 设计**：思维链明确记录任务分解、已完成步骤、待处理步骤及其完成状态，充当了模型的“历史记忆”。
    *   **两阶段训练**：
        1.  **监督微调 (SFT)**：学习任务分解和基础推理模式。
        2.  **强化学习 (RL)**：使用 PPO 算法优化。设计了多种奖励函数：格式奖励、分箱奖励（Bin Reward）、平均绝对误差奖励（MAE Reward）、**改进奖励（Improvement Reward，鼓励新回合比旧回合更准确）**以及完成奖励。
    *   **自动数据生成**：利用大模型将 ALFRED 和 Ego4D 的专家轨迹转化为包含视频片段、进度标签和 CoT 的训练数据，并加入“干扰任务描述”以增强鲁棒性。

### 3. 实验设计
*   **数据集与场景**：
    *   **ALFRED**：模拟环境下的具身任务数据集（清洁、烹饪等）。
    *   **Ego4D**：真实世界的第一视角视频数据集，环境更复杂、噪声更多。
*   **Benchmark 与指标**：
    *   $p_{mae}$：进度估计的平均绝对误差。
    *   $\Delta p_{mae}$：进度增量的误差（用于评估奖励建模能力）。
    *   $bin$：步骤级完成状态的准确率。
    *   $acc$：整体任务完成判断的准确率。
*   **对比方法**：
    *   **API 模型**：GPT-5、Gemini-2.5-Pro、Qwen3-VL-Plus。
    *   **开源模型**：MiniCPM-V-2.6、InternVL3、Qwen2.5-VL (7B/72B)。
    *   **特定方法**：LIV、ROVER、GVL。

### 4. 资源与算力
*   **硬件环境**：使用 **8 张 GPU** 进行全参数微调（Full-parameter fine-tuning）。
*   **软件框架**：基于 LLaMA-Factory 进行 SFT，基于 Verl 进行 RL 训练。
*   **训练时长**：
    *   ALFRED：SFT 训练 8 个 epoch（约 15k 步），RL 训练 1.5 个 epoch。
    *   Ego4D：SFT 训练 8 个 epoch（约 16k 步），RL 训练 4 个 epoch。
*   **推理效率**：实验证明 $\text{R}^2$VLM 的推理时间不随视频长度增加而增长，保持常数级延迟。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含约 2.5 万条轨迹、超过 25 万个对话元组的大规模数据集。
*   **实验维度**：
    *   **主实验**：在两个不同性质（模拟 vs 真实）的数据集上验证 SOTA 性能。
    *   **消融实验**：验证了视频片段输入 vs 全量输入、不同奖励函数组件的有效性。
    *   **下游应用**：在策略学习、在线强化学习奖励建模、主动辅助三个实际场景中进行了验证。
*   **客观性**：对比了从 7B 到 72B 的多种模型，并进行了跨域泛化测试，实验设计较为全面、公平。

### 6. 论文的主要结论与发现
*   **性能领先**：$\text{R}^2$VLM 在所有指标上均显著优于现有模型，尤其是在长程任务中。
*   **RL 的必要性**：单纯的 SFT 难以处理复杂的长程推理，RL 阶段（尤其是改进奖励）显著提升了模型的自我纠错能力。
*   **效率优势**：循环推理框架解决了 VLM 处理长视频时的注意力分散和计算冗余问题。
*   **下游价值**：该模型提供的进度信号能有效提升具身智能体策略的成功率，并能作为高质量的奖励函数。

### 7. 优点
*   **架构创新**：将循环神经网络的思想与 VLM 的 CoT 推理结合，优雅地解决了长视频处理的效率与逻辑一致性问题。
*   **闭环训练**：通过 RL 进一步精炼推理链，使模型具备了动态修正历史错误的能力。
*   **实用性强**：不仅关注指标，还通过三个下游任务证明了模型在具身智能领域的实际应用潜力。
*   **数据质量**：自动化的 CoT 生成流水线和干扰项设计为该领域提供了高质量的训练资源。

### 8. 不足与局限
*   **环境敏感性**：在真实世界数据集（Ego4D）上的表现虽优于基准，但相比模拟环境（ALFRED）仍有明显下降，说明模型对复杂背景和非规范动作的鲁棒性仍有提升空间。
*   **初始依赖**：模型依赖于初始的任务分解，如果第一步分解错误，后续循环推理可能存在偏差风险。
*   **实时性限制**：虽然推理时间恒定，但 7B 模型的推理延迟对于某些极高频响应的机器人任务可能仍显不足。

（完）
