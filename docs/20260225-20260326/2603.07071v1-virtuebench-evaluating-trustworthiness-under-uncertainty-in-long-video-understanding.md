---
title: "VirtueBench: Evaluating Trustworthiness under Uncertainty in Long Video Understanding"
title_zh: VirtueBench：评估长视频理解中不确定性下的可信度
authors: "Xueqing Yu, Bohan Li, Yan Li, Zhenheng Yang"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07071v1"
tags: ["query:lvu"]
score: 9.0
evidence: 长视频理解可信度评估基准
tldr: 针对长视频理解中因采样限制导致关键帧缺失、模型倾向于盲目猜测而非诚实拒绝的问题，本文提出了VirtueBench基准。该基准通过构建多层级采样并区分可答与不可答情况，评估了25个主流视觉语言模型的可靠性。研究发现模型在不确定性下的拒绝表现差异巨大，且高度依赖提示词引导，强调了开发可信模型的重要性。
selection_source: fresh_fetch
motivation: 现有的长视频理解评估忽略了采样不足导致的不确定性，导致模型倾向于通过猜测获取高分而非诚实回答。
method: 提出了VirtueBench基准，通过设置不同的帧采样水平并标注可答性，来评估模型在信息缺失时是否能正确拒绝回答。
result: "对25个模型的评估显示，拒绝准确率差异极大（0%到70%不等），且大多数模型在没有明确提示时拒绝意愿显著下降。"
conclusion: 评估结果揭示了当前模型在处理不确定性时的不可靠性，呼吁建立更注重可靠性和诚实性的多模态评估体系。
---

## 摘要
最近的视觉语言模型（VLMs）在多模态理解任务中取得了显著进展，但它们在长视频理解方面的评估仍然不可靠。由于帧输入受限，回答问题所需的关键帧可能会在模型的输入中缺失。然而，在这种不确定性下如实拒绝回答的模型会被标记为错误，而那些进行猜测的模型可能会巧合地产生正确答案，从而获得具有欺骗性的高准确率，这导致了误导性的评估结果，并鼓励模型进行猜测而非诚实响应。为了解决这一问题，我们推出了 VirtueBench，这是一个专门设计用于评估不确定性下模型可信度的基准测试。VirtueBench 为每个视频构建了多个帧采样级别，并提供了区分可回答和不可回答情况的基准真相（ground truths）。对 25 个开源和商业 VLM 的评估揭示了不同模型系列之间截然不同的拒绝行为，拒绝准确率从表现最好模型的 70% 以上到表现最差模型的接近 0% 不等。此外，当提示词没有明确要求时，大多数模型的拒绝率会大幅下降。这些发现强调了在强调可靠性和可信度的基准测试和排行榜引导下，开发用于多模态理解的可信 VLM 的必要性。

## 速览
**TLDR**：针对长视频理解中因关键帧缺失导致模型盲目猜测而非诚实拒绝的问题，本文提出了 VirtueBench 评测基准。该基准通过构建多层级帧采样，区分可回答与不可回答的情况，旨在评估视觉语言模型（VLM）在不确定性下的可信度。研究发现现有模型在拒绝回答方面的表现差异巨大，且高度依赖提示词引导，强调了开发可靠 VLM 的必要性。 \
**Motivation**：现有的长视频理解评估忽略了模型在关键信息缺失时盲目猜测导致的虚高准确率问题。 \
**Method**：提出了 VirtueBench 基准，通过设置不同的帧采样率来制造信息不确定性，并要求模型区分可回答与不可回答的情境。 \
**Result**：对 25 个模型的评估显示，拒绝回答的准确率差异极大（0% 到 70% 以上），且大多数模型在缺乏明确指令时倾向于猜测而非拒绝。 \
**Conclusion**：评估结果揭示了当前 VLM 在处理不确定性时的不可信问题，呼吁建立更注重可靠性和诚实性的评估体系。

---

## Abstract
Recent Vision-Language Models (VLMs) have made remarkable progress in multimodal understanding tasks, yet their evaluation on long video understanding remains unreliable. Due to limited frame inputs, key frames necessary for answering the question may be missing from the model's input. However, models that truthfully refuse to answer under such uncertainty are marked as incorrect, while those that guess may coincidentally produce the correct answer and thus obtain deceptively higher accuracy, leading to misleading evaluation results and encouraging models to guess rather than respond honestly. To address this issue, we introduce VirtueBench, a benchmark explicitly designed to assess model trustworthiness under uncertainty. VirtueBench constructs multiple frame-sampling levels for each video and provides ground truths that distinguish between answerable and unanswerable cases. Evaluations on 25 open-source and commercial VLMs reveal distinct refusal behaviors across different model families, with refusal accuracy ranging from over 70% in the best models to nearly 0% in the worst. Moreover, most models exhibit a substantial drop in refusal when the prompt does not explicitly require them to do so. These findings highlight the need for developing trustworthy VLMs for multimodal understanding, guided by benchmarks and leaderboards that emphasize reliability and trustworthiness.

---

## 论文详细总结（自动生成）

### VirtueBench：长视频理解中不确定性下的可信度评估基准总结

#### 1. 核心问题与研究动机
*   **核心问题**：当前视觉语言模型（VLM）在处理长视频时，受限于输入帧数（通常仅能处理 256-512 帧），导致回答问题所需的关键信息可能在采样过程中丢失。
*   **研究动机**：
    *   **评估偏见**：现有基准测试通常只区分对错。如果模型因信息缺失而诚实地拒绝回答，会被判定为错误；而如果模型盲目猜测并碰巧猜对，则会获得虚高的准确率。
    *   **诱导不诚实**：这种评估机制变相鼓励模型进行猜测而非诚实表达不确定性，导致模型在实际应用中不可靠。
    *   **缺乏基准**：目前缺乏专门评估模型在信息不足时是否能“知之为知之，不知为不知”的评测工具。

#### 2. 核心方法论
*   **核心思想**：通过构建**多层级帧采样（Multi-level Frame Sampling）**，人为制造信息缺失的情境，并标注每个采样层级下的可答性（Answerability）。
*   **关键技术细节**：
    *   **多级采样**：将视频统一采样为 64、128、256、512 和 1024 帧五个层级。
    *   **开放式转换**：将原始基准中的多选题（MCQ）转换为开放式问题（OE），以消除模型通过排除法或随机猜测获胜的可能性。
    *   **人工标注流水线**：标注员需针对每个采样层级判断信息是否充足。若不足，标注为“视频未提供足够信息”；若充足，则提供具体答案及证据时间戳。
*   **评估流程（LLM-as-a-judge）**：
    *   **第一阶段：拒绝检测**。判断模型是否表达了“信息不足”。
    *   **第二阶段：准确性验证**。对于可答题，验证语义一致性；对于不可答题，只有当模型选择“拒绝回答”时才判定为正确。

#### 3. 实验设计
*   **数据集来源**：整合了 MLVU、LVBench、LongVideoBench、MovieChat、Video-MME 和 ALLVB 等主流长视频基准。
*   **数据规模**：最终筛选并标注了 1,328 个高质量实例，涵盖 901 个原始视频。
*   **对比方法**：评估了 **25 个主流 VLM**，包括：
    *   **开源模型**：Qwen2.5-VL 系列、Qwen3-VL 系列、InternVL3.5 系列、LLaVA-Video、Mimo-VL 等。
    *   **商业模型**：GPT-4o、GPT-5 (preview/chat)、Gemini-2.5-Flash。
*   **评估维度**：整体准确率、感知（Perception）与推理（Reasoning）准确率、拒绝子集（Refusal）与非拒绝子集（Non-refusal）准确率。

#### 4. 资源与算力
*   **算力说明**：论文主要侧重于**评估（Evaluation）**而非模型训练。文中未详细列出评估过程消耗的具体 GPU 型号、数量或总时长。
*   **推理配置**：提到在推理时将最大像素限制在 512 × 512，以平衡视觉细节与模型处理能力。

#### 5. 实验数量与充分性
*   **实验规模**：对 25 个模型在 5 个不同采样层级上进行了全面测试，实验组数超过 100 组（模型 × 层级）。
*   **消融实验**：进行了**提示词消融（Prompt Ablation）**，对比了“带有诚实指令”与“无明确指令”下模型的表现。
*   **充分性评价**：实验设计非常充分且客观。通过多层级采样对比，清晰地展示了模型性能随信息完整度变化的曲线，揭示了现有模型普遍存在的“过度猜测”问题。

#### 6. 主要结论与发现
*   **拒绝行为差异巨大**：不同家族的模型表现迥异。Qwen 系列和商业模型（GPT-4o, Gemini）表现出较强的拒绝意识；而 LLaVA-Video、InternVL 等模型几乎从不拒绝，倾向于盲目猜测。
*   **规模与推理效应**：模型参数量越大，或经过强化学习（RL）/ 具备思考模式（Thinking mode）的模型，通常拒绝准确率更高，表现更可信。
*   **提示词敏感性**：大多数模型在没有明确要求“诚实”的情况下，拒绝率会大幅下降，表明模型默认被优化为“答题者”模式而非“诚实助手”。
*   **长视频挑战**：随着帧数增加，虽然信息更全，但模型处理长序列的压力增大，整体准确率并未如预期般在所有模型中稳步上升。

#### 7. 优点与亮点
*   **填补空白**：首次系统性地提出了评估长视频理解中“不确定性”和“可信度”的基准。
*   **标注严谨**：采用了多级采样+人工校验+时间戳证据的严苛标注流程，确保了 Ground Truth 的可靠性。
*   **揭示本质**：通过实验证明了当前 VLM 评估中存在的“虚假繁荣”，为未来开发更稳健、更诚实的多模态模型指明了方向。

#### 8. 不足与局限
*   **采样上限**：最高采样仅为 1024 帧，对于数小时的超长视频，信息丢失依然严重，未能覆盖极端长上下文的情况。
*   **Judge 模型偏见**：依赖 GPT-4o 作为裁判，可能存在 LLM 固有的评估偏见（如偏好长文本或特定格式）。
*   **数据源限制**：数据主要源于现有基准的二次加工，可能继承了原始数据在场景分布上的局限性。

（完）
