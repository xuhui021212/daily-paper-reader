---
title: "Towards Multimodal Lifelong Understanding: A Dataset and Agentic Baseline"
title_zh: 迈向多模态终身理解：一个数据集与智能体基线
authors: "Guo Chen, Lidong Lu, Yicheng Liu, Liangrui Dong, Lidong Zou, Jixin Lv, Zhenquan Li, Xinyi Mao, Baoqi Pei, Shihao Wang, Zhiqi Li, Karan Sapra, Fuxiao Liu, Yin-Dong Zheng, Yifei Huang, Limin Wang, Zhiding Yu, Andrew Tao, Guilin Liu, Tong Lu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05484v1"
tags: ["query:lvu"]
score: 10.0
evidence: 月度跨度的多模态终身视频理解数据集
tldr: 针对现有视频理解数据集缺乏自然日常生活特性的问题，本文提出了MM-Lifelong数据集，包含181.1小时跨越日、周、月尺度的视频。研究发现现有MLLM在长视频中存在工作记忆瓶颈和全局定位失效。为此，作者提出了递归多模态智能体（ReMA），通过动态内存管理和递归信念状态更新，显著提升了长周期视频的理解能力，并建立了严谨的评估基准。
selection_source: fresh_fetch
motivation: 现有视频数据集多为密集剪辑，无法模拟真实世界中稀疏且长期的日常生活场景。
method: 提出了递归多模态智能体（ReMA），利用动态内存管理迭代更新递归信念状态以处理长时视频。
result: ReMA在处理月度尺度的长视频理解任务上显著优于现有的端到端多模态大模型和智能体基准。
conclusion: 本研究通过新数据集和算法框架，揭示并解决了长时多模态理解中的记忆瓶颈与定位失效问题。
---

## 摘要
尽管视频理解数据集的规模已扩展至数小时，但它们通常由密集拼接的片段组成，与自然、无脚本的日常生活有所不同。为了弥补这一差距，我们推出了 MM-Lifelong，这是一个专为多模态终身理解设计的数据集。该数据集包含 181.1 小时的视频素材，按天、周、月三个尺度进行组织，以捕捉不同的时间密度。广泛的评估揭示了当前范式中的两种关键失效模式：端到端多模态大语言模型（MLLMs）因上下文饱和而面临工作记忆瓶颈，而代表性的智能体基线在处理稀疏的月度时间线时会出现全局定位崩溃。为解决这些问题，我们提出了递归多模态智能体（ReMA），它采用动态记忆管理来迭代更新递归信念状态，性能显著优于现有方法。最后，我们建立了旨在隔离时间与领域偏差的数据集划分方式，为监督学习和分布外泛化的未来研究提供了严谨的基础。

## 速览
**TLDR**：针对现有视频理解数据集缺乏自然非脚本化日常生活场景的问题，本文提出了MM-Lifelong数据集，涵盖181.1小时跨越日、周、月尺度的视频。研究发现现有MLLM存在工作记忆瓶颈，而智能体基线在长周期定位中易崩溃。为此，提出了递归多模态智能体（ReMA），通过动态内存管理和递归信念状态更新，显著提升了长时多模态理解能力，并建立了严谨的评估基准。 \
**Motivation**：现有视频数据集多为密集拼接的短片，无法模拟真实世界中长周期、非脚本化的日常生活理解需求。 \
**Method**：提出了递归多模态智能体（ReMA），利用动态内存管理机制迭代更新递归信念状态，以应对长时视频处理。 \
**Result**：实验表明ReMA在处理月度尺度的稀疏时间线时显著优于现有的端到端多模态大模型和智能体基线。 \
**Conclusion**：本研究通过新数据集和ReMA模型，为解决多模态终身理解中的记忆饱和与全局定位失效问题提供了重要基础。

---

## Abstract
While datasets for video understanding have scaled to hour-long durations, they typically consist of densely concatenated clips that differ from natural, unscripted daily life. To bridge this gap, we introduce MM-Lifelong, a dataset designed for Multimodal Lifelong Understanding. Comprising 181.1 hours of footage, it is structured across Day, Week, and Month scales to capture varying temporal densities. Extensive evaluations reveal two critical failure modes in current paradigms: end-to-end MLLMs suffer from a Working Memory Bottleneck due to context saturation, while representative agentic baselines experience Global Localization Collapse when navigating sparse, month-long timelines. To address this, we propose the Recursive Multimodal Agent (ReMA), which employs dynamic memory management to iteratively update a recursive belief state, significantly outperforming existing methods. Finally, we establish dataset splits designed to isolate temporal and domain biases, providing a rigorous foundation for future research in supervised learning and out-of-distribution generalization.

---

## 论文详细总结（自动生成）

### 论文总结：迈向多模态终身理解（MM-Lifelong）

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前的视频理解研究正从短片段转向长视频，但现有的长视频数据集（如 Ego4D, Video-MME）大多是密集的视频剪辑，其物理时间跨度（$T_{span}$）基本等于观察时长（$T_{dur}$）。
*   **研究动机**：真实的“终身理解”具有**高度稀疏性**（$T_{span} \gg T_{dur}$），即在数天或数月的跨度中，模型只能观察到片段性的信息，需要跨越巨大的时间鸿沟（如睡眠、未记录的间隙）来构建连贯的认知。
*   **核心挑战**：端到端多模态大模型（MLLMs）面临“工作记忆瓶颈”，即上下文饱和导致的性能下降；而现有的智能体方法在处理月度跨度的稀疏时间线时，容易出现“全局定位崩溃”。

#### 2. 论文提出的方法论：递归多模态智能体（ReMA）
为了克服上述瓶颈，作者提出了 **ReMA (Recursive Multimodal Agent)**，其核心思想是将长视频流转化为结构化的、由语言增强的“信念状态”。
*   **感知阶段（Perception Phase）**：将视频切分为固定长度（如5分钟）的片段，通过 `MMInspect` 工具提取多模态摘要，并利用 `MemManage` 动态整合进全局记忆库（Memory Bank）。
*   **控制阶段（Control Phase）**：LLM 控制器（如 GPT-5）根据用户查询进行迭代推理。
    *   **MMInspect**：主动对特定时间段进行细粒度视觉重检。
    *   **MemManage**：通过合并重叠或相似信息，防止记忆爆炸，保持记忆库简洁。
    *   **MemSearch**：在语言空间内进行两阶段检索（向量搜索 + LLM 重排序），聚合跨越数小时或数天的离散事件。
*   **递归机制**：智能体通过“推理-行动-观察”循环，不断修正和深化对视频内容的理解，直到生成最终答案。

#### 3. 实验设计
*   **数据集（MM-Lifelong）**：包含 181.1 小时视频，分为三个尺度：
    *   **Day（天级）**：游戏《黑神话：悟空》的完整流程，追踪技能与装备演变。
    *   **Week（周级）**：第一视角（EgoLife）记录的日常生活。
    *   **Month（月级）**：长达 51 天的户外直播，包含极高的视觉方差和时间稀疏性。
*   **Benchmark 与指标**：
    *   **Answer Accuracy**：基于 GPT-5 的语义准确性评分。
    *   **Reference Grounding (Ref@N)**：引入量化时间交并比，评估模型在长达百小时视频中定位证据线索的精度。
*   **对比方法**：
    *   **端到端模型**：GPT-5, Qwen3-VL (235B/30B), Video-XL-2, Eagle-2.5, Nemotron-v2。
    *   **智能体基线**：VideoMind, LongVT, DeepVideoDiscovery (DVD)。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 硬件型号、数量及总训练时长。
*   **模型使用**：实验主要基于 API 调用（如 GPT-5, GPT-4o-mini）和开源模型推理（如 Qwen3-VL 系列）。由于 ReMA 采用的是一种 Agentic 框架，其核心开销在于推理时的多轮 API 调用和视觉编码器的特征提取，而非从头开始的大规模预训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对 1289 个高质量 QA 对进行了测试，涵盖 11 个认知类别（计数、因果推理、状态变化等）。
    *   进行了多维度的消融实验，包括递归深度（Round 0-16）、感知粒度（2min 到 Full Video）、不同控制器骨干（MLLM vs. Text-only LLM）的对比。
*   **充分性与公平性**：实验设计较为充分。通过建立严谨的 Train/Val/Test 划分（隔离了领域和时间偏差），并引入人类评估（Human Full）作为上限，确保了对比的客观性。特别是对“LLM-as-a-Judge”的一致性验证（F1 达 99.39%），证明了自动评估的可靠性。

#### 6. 主要结论与发现
*   **工作记忆瓶颈**：端到端 MLLMs 随着输入帧数增加，性能会先升后降，存在明显的上下文饱和与噪声累积现象。
*   **定位崩溃**：传统视频智能体在处理月度跨度的稀疏数据时，无法有效定位长程线索。
*   **ReMA 的优越性**：通过将视觉信息转化为语言空间的记忆，ReMA 在准确率和定位精度（Ref@300）上均显著超过端到端模型和现有智能体。
*   **多模态对齐的重要性**：实验发现，使用 MLLM 作为智能体控制器比纯文本 LLM 效果更好，因为多模态预训练增强了指令遵循和规划的稳定性。

#### 7. 优点
*   **任务定义新颖**：首次严格区分了“观察时长”与“物理跨度”，定义了真正的“终身理解”任务。
*   **数据集质量高**：采用“线索锚定（Clue-Grounded）”标注，不仅有答案，还有支撑答案的视觉证据区间，便于评估定位能力。
*   **框架实用性强**：ReMA 框架不依赖于无限上下文硬件，通过动态内存管理解决了长时流处理的实际工程问题。

#### 8. 不足与局限
*   **主体多样性有限**：每个尺度（天/周/月）仅追踪一个主要主体（一个游戏角色、一个佩戴者、一个主播），缺乏大规模的人群多样性。
*   **未观察间隙的建模**：虽然测试了跨越间隙的推理，但尚未深入探讨“未记录期间发生的现实世界演变”如何系统性地影响模型对后续观察的推断。
*   **成本问题**：作为智能体基线，ReMA 依赖多轮推理和视觉重检，在处理超长视频时的推理延迟和 API 成本可能较高。

（完）
