---
title: Spectral Rectification for Parameter-Efficient Adaptation of Foundation Models in Colonoscopy Depth Estimation
title_zh: 结肠镜深度估计中基础模型参数高效适配的频谱校正
authors: "Xiaoxian Zhang, Minghai Shi, Lei Li"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15374v1"
tags: ["query:medvid"]
score: 9.0
evidence: 结肠镜深度估计的参数高效适配
tldr: 针对结肠镜单目深度估计中基础模型泛化性差的问题，本文提出SpecDepth框架。研究发现性能下降源于频域统计偏移，即结肠镜图像缺乏模型依赖的高频边缘信息。SpecDepth通过可学习的小波分解模块进行自适应频谱校正，在保留预训练模型几何表征的同时增强高频特征。实验表明，该方法在C3VD和SimCol3D数据集上达到SOTA性能，证明了频谱对齐在医疗影像适配中的有效性。
selection_source: fresh_fetch
motivation: 基础模型在结肠镜深度估计中表现不佳，主因是结肠镜图像与自然图像在频域存在统计偏移，缺乏模型所需的强高频纹理。
method: 提出SpecDepth框架，利用可学习的小波分解模块对特征图进行自适应频谱校正，显式建模并放大衰减的高频分量。
result: 在C3VD和SimCol3D公开数据集上分别实现了0.022和0.027的绝对相对误差，达到了当前最先进的性能水平。
conclusion: 通过直接解决频谱失配问题，可以高效地将视觉基础模型适配到专业的医疗成像任务中。
---

## 摘要
准确的单目深度估计对于结肠镜检查中的病变定位和导航至关重要。在自然图像上训练的基础模型无法直接泛化到结肠镜检查。我们发现核心问题并非语义鸿沟，而是频域中的统计偏移：结肠镜图像缺乏这些模型进行几何推理所依赖的强高频边缘和纹理梯度。为此，我们提出了 SpecDepth，这是一个参数高效的适配框架，在适应结肠镜领域的同时，保留了预训练模型鲁棒的几何表示。其核心创新是一个自适应频谱校正模块，该模块利用可学习的小波分解来显式建模并放大特征图中衰减的高频分量。与可能扭曲高层语义特征的传统微调不同，这种针对性的底层调整使输入信号与基础模型的原始归纳偏置重新对齐。在公开的 C3VD 和 SimCol3D 数据集上，SpecDepth 分别以 0.022 和 0.027 的绝对相对误差达到了最先进的性能。我们的工作表明，直接解决频谱失配是将视觉基础模型适配到专业医学成像任务的一种高效策略。代码将在论文被录用后公开。

## Abstract
Accurate monocular depth estimation is critical in colonoscopy for lesion localization and navigation. Foundation models trained on natural images fail to generalize directly to colonoscopy. We identify the core issue not as a semantic gap, but as a statistical shift in the frequency domain: colonoscopy images lack the strong high-frequency edge and texture gradients that these models rely on for geometric reasoning. To address this, we propose SpecDepth, a parameter-efficient adaptation framework that preserves the robust geometric representations of the pre-trained models while adapting to the colonoscopy domain. Its key innovation is an adaptive spectral rectification module, which uses a learnable wavelet decomposition to explicitly model and amplify the attenuated high-frequency components in feature maps. Different from conventional fine-tuning that risks distorting high-level semantic features, this targeted, low-level adjustment realigns the input signal with the original inductive bias of the foundational model. On the public C3VD and SimCol3D datasets, SpecDepth achieved state-of-the-art performance with an absolute relative error of 0.022 and 0.027, respectively. Our work demonstrates that directly addressing spectral mismatches is a highly effective strategy for adapting vision foundation models to specialized medical imaging tasks. The code will be released publicly after the manuscript is accepted for publication.

---

## 论文详细总结（自动生成）

这篇论文针对结肠镜单目深度估计任务，提出了名为 **SpecDepth** 的参数高效适配框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：结肠镜检查是结直肠癌筛查的金标准，但受限于操作者的经验和图像的3D感知缺失。单目深度估计（MDE）能增强手术导航和病变定位。
*   **核心问题**：虽然在自然图像上预训练的“基础模型”（如 DINOv2、Depth Anything）具有强大的几何先验，但直接迁移到结肠镜图像时表现不佳。
*   **核心发现**：作者指出，性能下降的主因并非传统的“语义鸿沟”，而是**频域统计偏移（Spectral Collapse）**。结肠镜图像由于组织特性和光学限制，严重缺乏自然图像中常见的强高频边缘和纹理梯度，导致基础模型无法激活其几何推理能力。

### 2. 方法论：核心思想与关键技术
SpecDepth 的核心思想是通过**频谱校正**来重新对齐输入信号与基础模型的归纳偏置。
*   **混合适配编码器（Hybrid Adaptation）**：
    *   采用 DINOv2 (ViT-Small) 作为骨干。
    *   **策略**：冻结前 $m$ 个 Transformer 块以保留通用的底层视觉原语（如边缘、频率模式）；微调深层块以学习结肠镜特有的语义（如粘膜褶皱、反光伪影）。
*   **门控小波频谱校正（Gated-Wavelet Spectral Rectification, GWT）**：
    *   在解码器中引入离散小波变换（DWT），将特征图分解为四个方向子带：LL（低频近似）、LH（垂直边缘）、HL（水平结构）、HH（对角纹理）。
    *   **核心技术**：通过可学习的全局标量权重 $\omega$ 对子带进行重加权，显式放大被衰减的高频分量，从而恢复边界信息。
*   **多约束优化策略（Multi-Constraint Objective）**：
    *   **尺度不变损失 ($L_{scale}$)**：处理全局尺度不确定性。
    *   **梯度匹配损失 ($L_{grad}$)**：强制深度断裂处与真实几何对齐，提升边界清晰度。
    *   **边缘感知平滑损失 ($L_{smooth}$)**：在纹理稀疏区域抑制由频谱放大可能引入的噪声。

### 3. 实验设计
*   **数据集**：
    *   **C3VD**：真实的临床结肠镜数据集，具有通过2D-3D注册获得的精确深度真值。
    *   **SimCol3D**：大规模合成数据集，用于验证鲁棒性。
*   **Benchmark 与对比方法**：
    *   对比了通用基础模型（Depth Anything V2）、任务特定模型（EcoDepth, Marigold）以及结肠镜专用方法（Col3D-MTL+SSL, MonoPCC）。
*   **泛化性测试**：在 Polyp-Size（息肉场景）和 SERV-CT（腹腔镜手术场景）上进行了 **Zero-shot（零样本）** 迁移实验。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练细节**：
    *   C3VD 训练约 10 个 epoch，SimCol3D 训练约 30 个 epoch。
    *   Batch size 为 16，使用 AdamW 优化器，学习率为 $5 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了两个主流数据集的定量评估、详尽的消融实验（验证 GWT、多约束损失、混合适配策略的有效性）、损失函数权重敏感性分析、3D 点云重建可视化以及跨领域的零样本泛化实验。
*   **充分性评价**：实验设计非常充分且客观。通过对比不同微调策略（全量微调 vs LoRA vs 混合策略）以及在完全未见过的腹腔镜数据上测试，有力地证明了该方法并非过拟合特定数据集，而是抓住了内窥镜图像的本质物理特性。

### 6. 主要结论与发现
*   **频谱校正是关键**：直接微调基础模型而不处理频谱失配是不够的。通过小波模块放大高频信号，可以有效“唤醒”预训练模型中潜伏的几何感知能力。
*   **性能领先**：SpecDepth 在 C3VD 上实现了 0.022 的 AbsRel（绝对相对误差），相比 Depth Anything V2 基准降低了约 79% 的误差。
*   **参数高效**：在仅增加极少量参数（从 24.8M 到 26.0M）的情况下，实现了性能的巨大飞跃。

### 7. 优点与亮点
*   **视角新颖**：从频域统计偏移的角度解释基础模型在医疗影像中的失效，避开了复杂的语义对齐。
*   **即插即用**：门控小波模块设计精巧，不改变预训练骨干的结构，易于集成。
*   **泛化能力强**：在零样本迁移实验中，SpecDepth 能够准确保持息肉的凸面轮廓和手术器械的边缘，显著优于其他 SOTA 模型。

### 8. 不足与局限
*   **静态假设**：模型目前假设场景是静态的，未显式处理结肠镜中的动态组织形变、流体运动或器械交互带来的时间一致性问题。
*   **架构依赖**：主要在 Transformer 架构上验证，其频谱校正逻辑在 CNN 或扩散模型中的适用性尚待进一步探索。
*   **外部验证受限**：由于外部数据集（如 Polyp-Size）缺乏深度真值，跨领域测试主要停留在定性视觉分析，缺乏定量指标。
*   **监督需求**：仍依赖有监督的深度标签进行适配训练，在完全缺乏标注的场景下应用受限。

（完）
