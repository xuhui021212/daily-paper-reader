---
title: "Show Me When and Where: Towards Referring Video Object Segmentation in the Wild"
title_zh: 展示何时何地：迈向野外环境下的指代视频物体分割
authors: "Mingqi Gao, Jinyu Yang, Jingnan Luo, Xiantong Zhen, Jungong Han, Giovanni Montana, Feng Zheng"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14300v1"
tags: ["query:lvu"]
score: 9.0
evidence: 使用未修剪长视频的指代视频对象分割新基准
tldr: 本研究针对现有指代视频对象分割（RVOS）数据集仅包含修剪视频、无法反映真实挑战的问题，提出了面向野外场景的新设定。作者构建了包含1120个长视频的YoURVOS基准数据集，要求模型同时解决“何时”与“何处”出现目标的问题。同时提出了OMFormer模型，通过对象级多模态交互实现高效的时空定位。实验证明该模型在处理目标缺失帧时显著优于传统方法，推动了RVOS向实际应用迈进。
selection_source: fresh_fetch
motivation: 现有RVOS数据集多为精简视频且目标始终存在，忽视了真实场景中目标在时间维度上出现与消失的挑战。
method: 建立了包含大量长视频和多样化场景的YoURVOS基准，并提出利用对象级多模态Transformer（OMFormer）进行全局时空定位。
result: 实验显示传统RVOS方法在处理目标缺失帧时表现不佳，而OMFormer在YoURVOS基准上展现出更强的鲁棒性。
conclusion: YoURVOS数据集和OMFormer模型为实现更具实用价值的野外指代视频对象分割提供了重要的基准和基准方案。
---

## 摘要
指代视频物体分割（RVOS）因其广泛的应用，近期在计算机视觉领域引起了极大关注。现有的 RVOS 设置包含精心剪辑的视频，其中文本指代的物体始终出现在所有帧中，但这未能充分反映该任务在现实中的挑战。这种简化的设置仅要求 RVOS 方法预测物体的位置（where），而无需展示物体何时（when）出现。在这项工作中，我们引入了一种面向野外 RVOS 的新设置。为此，我们利用 YouTube 未剪辑视频收集了一个新的 RVOS 基准数据集——YoURVOS，它包含 1,120 个野外视频，其时长和场景数量是现有数据集的 7 倍。我们的新基准挑战 RVOS 方法不仅要展示物体在视频中的位置，还要展示其出现的时间。为了建立基准，我们提出了物体级多模态 Transformer（OMFormer）来应对这些挑战，其特点是通过编码物体级多模态交互来实现高效且全局的时空定位。我们证明了以往的 VOS 方法在我们的 YoURVOS 基准上表现不佳，特别是在目标缺失帧增加的情况下，而我们的 OMFormer 始终表现良好。我们的 YoURVOS 数据集提供了一个重要的基准，将推动 RVOS 方法在实际应用中的进步。

## Abstract
Referring video object segmentation (RVOS) has recently generated great popularity in computer vision due to its widespread applications. Existing RVOS setting contains elaborately trimmed videos, with text-referred objects always appearing in all frames, which however fail to fully reflect the realistic challenges of this task. This simplified setting requires RVOS methods to only predict where objects, with no need to show when the objects appear. In this work, we introduce a new setting towards in-the-wild RVOS. To this end, we collect a new benchmark dataset using Youtube Untrimmed videos for RVOS - YoURVOS, which contains 1,120 in-the-wild videos with 7 times more duration and scenes than existing datasets. Our new benchmark challenges RVOS methods to show not only where but also when objects appear in videos. To set a baseline, we propose Object-level Multimodal TransFormers (OMFormer) to tackle the challenges, which are characterized by encoding object-level multimodal interactions for efficient and global spatial-temporal localisation. We demonstrate that previous VOS methods struggle on our YoURVOS benchmark, especially with the increase of target-absent frames, while our OMFormer consistently performs well. Our YoURVOS dataset offers an imperative benchmark, which will push forward the advancement of RVOS methods for practical applications.

---

## 论文详细总结（自动生成）

### 论文总结：Show Me When and Where: Towards Referring Video Object Segmentation in the Wild

#### 1. 核心问题与整体含义（研究动机和背景）
指代视频物体分割（RVOS）旨在根据自然语言描述定位并分割视频中的特定物体。然而，现有的 RVOS 基准（如 Ref-YouTube-VOS, MeViS）主要基于**精心修剪（Trimmed）**的短视频，目标物体通常在所有帧中都存在。
*   **研究动机**：现实场景中的视频通常是**未修剪（Untrimmed）**的，目标物体可能仅在视频的特定时间段出现，或者在多个不连续的片段中出现。
*   **核心问题**：现有方法过度依赖“目标始终存在”的假设，只需解决“在哪（Where）”的问题。本论文提出 RVOS 应该同时解决**“何时（When）”**（时间定位）和**“何处（Where）”**（空间分割）的问题，以适应野外（In-the-wild）的复杂环境。

#### 2. 方法论：核心思想与关键技术
论文提出了 **OMFormer（Object-level Multimodal TransFormers）**，其核心思想是利用**物体级（Object-level）**特征而非密集的像素级特征来进行长程时空建模，从而提高处理长视频的效率。

*   **核心流程**：
    1.  **空间多模态编码**：使用视觉骨干网络提取多尺度特征，并通过互注意力机制（Cross-Attention）增强视觉与文本特征的关联。
    2.  **空间多模态解码**：将文本特征转化为语言查询（Language Queries），在单帧内聚合目标相关上下文，生成物体级特征。
    3.  **时空多模态解码（关键创新）**：引入**序列与相关性解码器（SRD）**。它不直接处理海量的像素特征，而是对物体级特征进行时空关联，预测物体在每一帧的**相关性（Relevance）**。
    4.  **时空联合分割**：通过三个专用头（Span Head, Sequence Head, Relevance Head）预测目标的出现跨度（Start/End Time）和序列置信度。最终输出仅在预测的时间跨度内保留分割掩码，其余帧置为空。
*   **损失函数**：结合了分类损失、框回归损失、掩码损失（DICE + Focal）、时间跨度损失（KL 散度）和相关性损失。

#### 3. 实验设计
*   **新基准数据集 YoURVOS**：
    *   来源：从 YouTube-8M 中检索 Ref-YouTube-VOS 的原始未修剪长视频。
    *   规模：1,120 个视频，平均时长 90 秒（是现有数据集的 7 倍），包含 5,276 条描述。
    *   特点：高**目标无关帧率（TI Rate）**，平均 72.63% 的帧不包含目标物体。
*   **对比方法**：对比了 22 种代表性方法，包括 ReferFormer, UNINEXT, R2-VOS, 以及基于大模型的 VISA, VideoLISA, SAM-V2 等。
*   **评价指标**：空间维度使用 $J\&F$（区域与轮廓精度），时间维度引入 $tIoU$（时间交并比）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 V100）及数量。
*   **训练细节**：模型在图像数据集上预训练 12 个 epoch，随后在 YoURVOS 上微调 10 个 epoch。
*   **效率对比**：文中通过图表展示了 OMFormer 在训练内存占用和推理速度（FPS）上的优势，强调其物体级交互比传统的密集特征交互更节省显存。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 YoURVOS 测试集上进行了大规模的基准测试（22 种方法）。
    *   针对 **TI Rate（目标缺失程度）** 进行了分组实验，验证模型在不同干扰强度下的鲁棒性。
    *   进行了详尽的**消融实验**，涵盖了物体级建模、时间跨度预测（Span）、相关性预测（Rel）以及解码器结构的设计。
*   **充分性评价**：实验设计较为充分且客观。通过将现有 SOTA 方法在 YoURVOS 上重新微调并对比，证明了性能提升并非单纯来自训练数据，而是源于架构对时间定位的处理能力。

#### 6. 主要结论与发现
*   **现有方法的局限**：所有现有的 RVOS 方法在面对未修剪视频时性能均大幅下降，尤其是随着目标缺失帧（TI Rate）的增加，其 $tIoU$ 几乎呈线性下降。
*   **OMFormer 的优势**：OMFormer 在 $J\&F$ 和 $tIoU$ 指标上均显著优于现有方法。在 ResNet-50 后端下，$J\&F$ 达到 33.7，比最接近的竞争对手高出约 6-7 个点。
*   **时间定位的重要性**：在长视频中，准确预测“何时出现”对于提升整体分割质量至关重要。

#### 7. 优点（亮点）
*   **数据集前瞻性**：填补了 RVOS 领域缺乏长视频、未修剪视频基准的空白，更贴近实际应用（如监控、长视频剪辑）。
*   **高效的架构设计**：OMFormer 巧妙地避开了长视频中密集特征计算量巨大的问题，通过物体级特征实现了全局时空建模。
*   **任务定义扩展**：将 RVOS 从单纯的空间分割扩展到了时空联合定位。

#### 8. 不足与局限
*   **长文本理解能力**：在处理包含复杂动作序列的长描述（如“先做 A 再做 B”）时，模型仍会出现对应错误。
*   **多干扰物挑战**：当视频中存在多个相似物体且背景复杂时，模型容易产生误报（False Positive）。
*   **计算瓶颈**：虽然比像素级方法高效，但随着视频长度进一步增加（如直播流），物体级特征的显存占用仍有优化空间。
*   **算力信息缺失**：未提供具体的硬件环境和训练耗时，不利于其他研究者评估复现成本。

（完）
