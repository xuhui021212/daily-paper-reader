---
title: "EndoVGGT: GNN-Enhanced Depth Estimation for Surgical 3D Reconstruction"
title_zh: EndoVGGT：用于手术三维重建的 GNN 增强深度估计
authors: "Falong Fan, Yi Xie, Arnis Lektauers, Bo Liu, Jerzy Rozenblit"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24577v1"
tags: ["query:medvid"]
score: 9.0
evidence: 手术3D重建和机器人感知的深度估计
tldr: 针对手术机器人感知中软组织3D重建面临的低纹理、高光及遮挡挑战，本文提出EndoVGGT框架。该框架引入变形感知图注意力模块（DeGAT），通过动态构建特征空间语义图来捕捉长程相关性，有效传播结构线索并增强全局一致性。实验表明，该方法在SCARED数据集上显著提升了重建保真度，并展现出卓越的零样本跨域泛化能力。
selection_source: fresh_fetch
motivation: 传统固定拓扑方法难以处理手术场景中因低纹理、高光和器械遮挡导致的几何不连续及非刚性变形恢复问题。
method: 提出EndoVGGT框架，利用变形感知图注意力模块（DeGAT）动态构建特征空间语义图，以捕捉组织区域间的长程几何关联。
result: "在SCARED数据集上，该方法将PSNR和SSIM分别提升了24.6%和9.1%，并在跨数据集测试中展现出极强的零样本泛化能力。"
conclusion: 动态特征空间建模能有效学习领域无关的几何先验，显著增强了手术场景下3D重建的鲁棒性与全局一致性。
---

## 摘要
可变形软组织的精确三维重建对手术机器人感知至关重要。然而，低纹理表面、镜面高光和手术器械遮挡往往会破坏几何连续性，这对现有的固定拓扑方法提出了挑战。为此，我们提出了 EndoVGGT，这是一个以几何为中心的框架，配备了变形感知图注意力（DeGAT）模块。DeGAT 并非使用静态空间邻域，而是动态构建特征空间语义图，以捕捉相干组织区域之间的远程相关性。这使得结构线索能够在遮挡情况下实现鲁棒传播，从而增强全局一致性并改善非刚性变形恢复。在 SCARED 数据集上的广泛实验表明，我们的方法显著提高了保真度，相比之前的最先进技术，PSNR 提升了 24.6%，SSIM 提升了 9.1%。至关重要的是，EndoVGGT 在未见过的 SCARED 和 EndoNeRF 领域表现出强大的零样本跨数据集泛化能力，证实了 DeGAT 学习到了与领域无关的几何先验。这些结果突显了动态特征空间建模对于一致的手术三维重建的有效性。

## Abstract
Accurate 3D reconstruction of deformable soft tissues is essential for surgical robotic perception. However, low-texture surfaces, specular highlights, and instrument occlusions often fragment geometric continuity, posing a challenge for existing fixed-topology approaches. To address this, we propose EndoVGGT, a geometry-centric framework equipped with a Deformation-aware Graph Attention (DeGAT) module. Rather than using static spatial neighborhoods, DeGAT dynamically constructs feature-space semantic graphs to capture long-range correlations among coherent tissue regions. This enables robust propagation of structural cues across occlusions, enforcing global consistency and improving non-rigid deformation recovery. Extensive experiments on SCARED show that our method significantly improves fidelity, increasing PSNR by 24.6% and SSIM by 9.1% over prior state-of-the-art. Crucially, EndoVGGT exhibits strong zero-shot cross-dataset generalization to the unseen SCARED and EndoNeRF domains, confirming that DeGAT learns domain-agnostic geometric priors. These results highlight the efficacy of dynamic feature-space modeling for consistent surgical 3D reconstruction.

---

## 论文详细总结（自动生成）

这是一份关于论文《EndoVGGT: GNN-Enhanced Depth Estimation for Surgical 3D Reconstruction》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
手术机器人的感知核心在于对可变形软组织进行精确的三维重建。然而，手术场景具有极大的挑战性：
*   **环境复杂性**：软组织表面纹理稀疏、存在镜面高光。
*   **动态干扰**：手术器械的频繁遮挡会破坏几何连续性。
*   **模型局限**：现有的通用大模型（LRMs）多基于刚性物体训练，直接用于非刚性手术场景时会导致拓扑结构断裂；而基于 NeRF 或高斯泼溅（Gaussian Splatting）的方法通常需要针对每个新场景进行耗时的逐场景优化（Per-scene optimization），缺乏泛化性。

**核心动机**：开发一种无需逐场景优化、能够处理非刚性变形并对器械遮挡具有鲁棒性的通用手术三维重建框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **EndoVGGT** 框架，其核心是**变形感知图注意力模块（DeGAT）**。

*   **核心思想**：利用动态构建的特征空间语义图来捕捉组织区域间的长程相关性，而非依赖固定的空间邻域。这使得模型能够跨越器械遮挡，从相似的组织区域提取结构线索。
*   **关键技术细节**：
    *   **动态图构建**：针对每一帧图像，计算 Patch Token 之间的余弦相似度，选取 K 个最近邻（K-NN，实验设定 K=9）构建动态图。
    *   **注意力聚合**：采用 GAT 架构，通过 LeakyReLU 激活的投影矩阵计算注意力权重，并利用 ELU 激活函数进行残差更新，确保训练稳定性。
    *   **多层级集成方案**：研究了将 DeGAT 集成在 Token 层级（调节相机 Token）、注意力层级（注入偏置矩阵）和特征层级（Transformer 前后的残差算子）的效果。
    *   **损失函数**：组合了相机位姿损失（$L_{cam}$）和深度损失（$L_{depth}$）。深度损失包含回归项、基于不确定性的置信度加权项（$L_{unc}$）以及空间梯度一致性项（$L_{grad}$）。
    *   **几何投影**：通过预测的深度图和相机参数，将像素点反投影回三维空间，形成稠密点云。

### 3. 实验设计
*   **数据集**：
    *   **SCARED**：真实的达芬奇机器人手术数据，提供深度真值。
    *   **EndoSLAM**：包含离体和合成序列，用于基础训练。
    *   **EndoNeRF**（Cutting & Pulling 子集）：用于评估极端变形和拓扑变化下的鲁棒性。
*   **Benchmark 与对比方法**：
    *   **通用模型**：VGGT（原始版本）。
    *   **手术专用模型**：EndoSurf（基于 NeRF）、EndoLRMGS（基于 LRM 和高斯泼溅）。
*   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量、内存占用或具体的训练总时长。
*   **实现细节**：提到了使用 DINOv2 作为冻结的骨干网络提取特征，并使用了 DPT 头进行深度预测。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：对比了 DeGAT 在不同层级（Token, Attention, Feature）的集成效果，并对邻居数量 $K$ 进行了详细的参数敏感性分析（$K=2$ 到 $18$）。
    *   **性能对比**：在 SCARED 和 EndoNeRF 多个子集上进行了详尽的定量对比。
    *   **泛化性测试**：进行了“零样本跨数据集”实验（在 EndoSLAM 训练，在 SCARED/EndoNeRF 测试）。
*   **充分性评价**：实验设计较为全面，涵盖了从模块有效性到系统泛化性的各个维度。通过可视化分析（如邻域聚合的可视化）直观展示了 DeGAT 处理遮挡的能力，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在 SCARED 数据集上，EndoVGGT 相比原始 VGGT，PSNR 从 14.06 提升至 34.35（提升 144%），SSIM 提升了 9.1%。
*   **DeGAT 的位置至关重要**：实验发现，在 Transformer 块之前应用特征层级的 DeGAT 效果最好，能有效建立稳定的局部几何结构。
*   **强大的泛化能力**：在零样本测试中，EndoVGGT 在未见过的变形场景下依然保持了高保真度（SSIM 达 0.915），证明其学习到了领域无关的几何先验，而非仅仅拟合特定场景的纹理。
*   **鲁棒性**：DeGAT 能够成功“桥接”被器械遮挡的组织区域，保持深度的连续性。

### 7. 优点（亮点）
*   **动态拓扑建模**：突破了固定空间邻域的限制，利用语义相似性处理非刚性变形，非常契合手术场景。
*   **前馈推理效率**：相比需要逐场景优化的 NeRF 类方法，EndoVGGT 是前馈网络，推理速度更快，更具临床实时应用潜力。
*   **理论支撑**：附录中提供了关于 DeGAT 稳定性（范数边界）和置信度学习凸性的数学证明，增强了方法的严谨性。

### 8. 不足与局限
*   **时间连续性**：目前模型主要基于单帧或短序列推理，尚未显式建模长程视频流的时间一致性，可能在极高速运动下出现深度抖动。
*   **算力细节缺失**：未提供训练成本和推理延迟的具体数据，难以评估其在嵌入式手术设备上的部署难度。
*   **极端光照处理**：虽然 DeGAT 增强了鲁棒性，但在极端严重的镜面反射（完全覆盖组织特征）情况下，语义相似性检索可能会失效。

（完）
