---
title: Investigating a Policy-Based Formulation for Endoscopic Camera Pose Recovery
title_zh: 探究一种基于策略的内窥镜相机位姿恢复建模方法
authors: "Jan Emily Mangulabnan, Akshat Chauhan, Laura Fleig, Lalithkumar Seenivasan, Roger D. Soberanis-Mukul, S. Swaroop Vedula, Russell H. Taylor, Masaru Ishii, Gregory D. Hager, Mathias Unberath"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20045v1"
tags: ["query:medvid"]
score: 10.0
evidence: 从视频序列中恢复内窥镜相机姿态的基于策略的公式
tldr: 针对内窥镜手术中传统几何方法在低纹理和光照变化下鲁棒性差的问题，本文提出一种基于策略的相机位姿恢复方法。该方法模仿专家行为，通过当前状态预测短时相对运动，无需显式几何表示。在尸体鼻窦内窥镜实验中，该方法在平移精度和低纹理鲁棒性上优于传统几何基准，为内窥镜导航提供了新思路。
selection_source: fresh_fetch
motivation: 传统的基于特征匹配和几何优化的内窥镜位姿恢复方法在低纹理和光照剧烈变化的环境下容易失效。
method: 提出一种基于策略的公式，通过模仿专家行为直接预测短时相对运动，且在推理时不依赖显式的几何表示。
result: 在鼻窦内窥镜评估中，该方法实现了最低的平均平移误差，并在低纹理条件下表现出更强的鲁棒性。
conclusion: 学习型运动策略是解决内窥镜相机位姿恢复中几何方法局限性的一种可行且有效的替代方案。
---

## 摘要
在内窥镜手术中，外科医生通过结合先验知识来解释术中场景不断变化的视觉外观，从而持续定位内窥镜视野相对于解剖结构的位置。基于视觉的导航系统试图通过直接从内窥镜视频中恢复相机位姿来复制这种能力，但大多数方法并未体现出使外科医生成功的、针对新帧进行推理的相同原则。相反，这些方法仍基于特征匹配和关键帧上的几何优化，而这种方法已被证明在低纹理和快速光照变化等具有挑战性的内窥镜成像条件下性能会下降。在本文中，我们寻求一种替代方法，并研究了一种基于策略的内窥镜相机位姿恢复建模方式，旨在模仿专家在给定前一相机状态的条件下估计轨迹。我们的方法直接预测短时域相对运动，而无需在推理时维护显式的几何表示。因此，该方法在设计上解决了基于几何的方法中一些众所周知的挑战，例如脆弱的对应关系匹配、纹理稀疏区域的不稳定性，以及由于重建失败导致的有限位姿覆盖。我们在尸体鼻窦内窥镜检查中评估了所提出的方案。在理想状态（oracle state）约束下，我们将短时域运动预测质量与几何基准进行了比较，实现了最低的平均平移误差和具有竞争力的旋转精度。我们通过根据纹理丰富度和光照变化对预测窗口进行分组来分析鲁棒性，结果表明该方法对低纹理条件的敏感度降低。这些研究结果表明，学习到的运动策略为内窥镜相机位姿恢复提供了一种可行的替代方案。

## Abstract
In endoscopic surgery, surgeons continuously locate the endoscopic view relative to the anatomy by interpreting the evolving visual appearance of the intraoperative scene in the context of their prior knowledge. Vision-based navigation systems seek to replicate this capability by recovering camera pose directly from endoscopic video, but most approaches do not embody the same principles of reasoning about new frames that makes surgeons successful. Instead, they remain grounded in feature matching and geometric optimization over keyframes, an approach that has been shown to degrade under the challenging conditions of endoscopic imaging like low texture and rapid illumination changes. Here, we pursue an alternative approach and investigate a policy-based formulation of endoscopic camera pose recovery that seeks to imitate experts in estimating trajectories conditioned on the previous camera state. Our approach directly predicts short-horizon relative motions without maintaining an explicit geometric representation at inference time. It thus addresses, by design, some of the notorious challenges of geometry-based approaches, such as brittle correspondence matching, instability in texture-sparse regions, and limited pose coverage due to reconstruction failure. We evaluate the proposed formulation on cadaveric sinus endoscopy. Under oracle state conditioning, we compare short-horizon motion prediction quality to geometric baselines achieving lowest mean translation error and competitive rotational accuracy. We analyze robustness by grouping prediction windows according to texture richness and illumination change indicating reduced sensitivity to low-texture conditions. These findings suggest that a learned motion policy offers a viable alternative formulation for endoscopic camera pose recovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《Investigating a Policy-Based Formulation for Endoscopic Camera Pose Recovery》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在具有挑战性的内窥镜成像环境（如低纹理、光照剧烈变化、粘膜反光）中，实现鲁棒且准确的相机位姿恢复（Pose Recovery）。
*   **研究背景**：传统的视觉导航系统多依赖于几何推理（如 SfM 或 SLAM），这些方法强依赖于特征点匹配和几何优化。然而，内窥镜下的解剖结构往往缺乏纹理或具有重复性，导致特征匹配极易失效。
*   **研究动机**：受外科医生通过观察视觉变化并结合解剖知识来感知运动的启发，作者提出将位姿恢复建模为一个“基于策略的运动预测”问题，通过模仿学习（Imitation Learning）直接预测相机的相对运动，而非依赖显式的几何重建。

### 2. 方法论
*   **核心思想**：将相机视为一个智能体，将位姿估计转化为一个序列化运动推理问题。给定当前图像、目标图像和前一时刻状态，策略网络预测一段短时域内的相对运动序列。
*   **关键技术细节**：
    *   **状态表示**：以序列第一帧为锚点建立坐标系。状态包含当前图像 $I_t$、目标图像 $I_{t+k}$ 以及当前位姿 $T_t$。
    *   **网络架构**：
        *   **图像编码器**：使用共享权重的视觉编码器提取图像对特征。
        *   **位姿编码器**：独立编码前一时刻的相机位姿。
        *   **门控融合（Gated Fusion）**：通过残差门控机制融合视觉和状态特征，使模型能共同推理视觉变化与运动关系。
        *   **扩散策略（Diffusion Policy）**：采用基于 1D U-Net 的条件扩散模型作为动作解码器，预测 6D 相对位姿增量序列（平移+轴角旋转）。
    *   **流程**：输入 $(I_t, I_{t+k}, T_t) \rightarrow$ 特征融合 $\rightarrow$ 扩散模型去噪 $\rightarrow$ 输出 $\{\Delta T_{t+i}\}_{i=1}^k$。

### 3. 实验设计
*   **数据集**：使用内部采集的**尸体鼻窦内窥镜数据集**。包含 12 个标本、55 个视频序列，涵盖术前、术中和术后场景。
*   **真值获取**：通过 NDI Polaris 混合位置传感器跟踪安装在内窥镜上的红外标记，并经过手眼标定获得每帧的毫米级位姿真值。
*   **基准方法（Benchmarks）**：
    *   **SfM (SIFT)**：基于传统 SIFT 特征的 GLOMAP。
    *   **SfM (Learned Desc.)**：基于学习型特征描述符的 SfM。
    *   **OneSLAM**：基于学习型点跟踪（TAP）的先进 SLAM 系统。
*   **评价指标**：相对位姿误差（RPE，包括平移和旋转）、位姿覆盖率（Pose Coverage）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 3060 GPU**。
*   **训练参数**：Batch size 为 32，使用 AdamW 优化器，学习率 1e-4，训练 20 个 epoch。
*   **推理/模型细节**：扩散模型使用 DDIM 调度器，包含 100 个扩散步数。

### 5. 实验数量与充分性
*   **实验规模**：使用了 12 个标本中的 9 个进行训练，1 个验证，2 个测试。虽然标本数量有限，但 55 个视频序列提供了丰富的运动轨迹。
*   **鲁棒性分析**：作者专门设计了针对“低纹理”和“光照变化”的定量评估，通过 Sobel 梯度和亮度变化值对视频窗口进行分组（Top 25% vs Bottom 25%）。
*   **充分性评价**：实验设计较为客观，对比了从传统到最前沿的几何方法。但由于是在“Oracle State”（即给定前一帧真值位姿）的假设下进行的短时预测评估，尚未完全展示长时域下的累积误差表现。

### 6. 主要结论与发现
*   **精度优势**：在短时域运动预测中，该策略模型实现了**最低的平均平移误差**（3.70mm），优于所有几何基准。
*   **覆盖率**：相比于 SIFT-SfM 在挑战性序列中仅 51% 的覆盖率，该方法实现了 **100% 的位姿覆盖**，不会因特征丢失而崩溃。
*   **鲁棒性**：在低纹理条件下，该方法的性能下降幅度（$\Delta$）小于几何方法，证明了其对纹理稀疏区域的适应性。
*   **局限发现**：所有方法（包括本项目方法）在剧烈光照变化下均表现不佳，说明光照不稳定性仍是视觉位姿估计的共性难题。

### 7. 优点与亮点
*   **建模新颖**：跳出了传统的“特征匹配-几何优化”框架，将位姿恢复视为模仿专家轨迹的策略问题。
*   **无需显式地图**：在推理时不需要维护复杂的 3D 点云或深度图，降低了系统对中间几何表示质量的依赖。
*   **扩散模型的引入**：利用扩散策略处理运动预测中的多峰分布和不确定性，提高了预测的平滑度。

### 8. 不足与局限
*   **Oracle 依赖**：目前的评估基于“已知前一时刻真值位姿”，在实际应用中，如果前一帧预测错误，误差可能会在闭环反馈中迅速累积（即漂移问题）。
*   **光照敏感度**：虽然缓解了纹理问题，但对内窥镜中常见的快速光照闪烁和反光仍缺乏本质的改进手段。
*   **泛化性验证**：实验仅限于鼻窦内窥镜，尚未在其他解剖部位（如腹腔镜、支气管镜）验证其通用性。

（完）
