---
title: "Think, Then Verify: A Hypothesis-Verification Multi-Agent Framework for Long Video Understanding"
title_zh: 先思后验：一种用于长视频理解的假设-验证多智能体框架
authors: "Zheng Wang, Haoran Chen, Haoxuan Qin, Zhipeng Wei, Tianwen Qian, Cong Bai"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04977v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于长视频推理的假设验证多智能体框架
tldr: 针对长视频理解中存在的视觉冗余、长程依赖及语义漂移等挑战，本文提出VideoHV-Agent框架。该框架遵循“先思考后寻找”原则，将视频问答重构为结构化的“假设-验证”过程。通过多智能体协作，将候选答案转化为可测试假设并进行细粒度证据核实，在提升准确率的同时增强了模型的可解释性并降低了计算成本。
selection_source: fresh_fetch
motivation: 传统方法在长视频推理中容易积累语义漂移和相关性错误，因此需要一种从主动任务制定而非被动检索出发的推理机制。
method: 构建了一个包含Thinker（重写假设）、Judge（提取线索）、Verifier（细粒度验证）和Answer（整合证据）的多智能体协作框架。
result: 在三个长视频理解基准测试中均取得了SOTA性能，并表现出更强的逻辑合理性和更高的计算效率。
conclusion: 将视频问答转化为结构化的假设验证过程，能有效解决长视频推理中的复杂依赖问题并提升结果的可靠性。
---

## 摘要
长视频理解具有挑战性，原因在于密集的视觉冗余、长程时间依赖，以及思维链（chain-of-thought）和基于检索的智能体容易积累语义漂移和相关性驱动的错误。我们认为，长视频推理不应始于反应式检索，而应始于深思熟虑的任务制定：模型必须首先阐明，为了使每个候选答案成立，视频中必须满足哪些真实条件。这种“先思后找”的原则促成了 VideoHV-Agent 的诞生，该框架将视频问答重新定义为一个结构化的假设-验证过程。基于视频摘要，Thinker（思考者）将候选答案重写为可测试的假设，Judge（评判者）推导出指定必须检查哪些证据的判别性线索，Verifier（验证者）利用局部的细粒度视频内容对线索进行定位和测试，最后 Answer（回答者）智能体整合经过验证的证据以生成最终答案。在三个长视频理解基准测试上的实验表明，VideoHV-Agent 在实现最先进准确率的同时，还提供了增强的可解释性、更高的逻辑严密性以及更低的计算成本。我们的代码已在 https://github.com/Haorane/VideoHV-Agent 公开。

## 速览
**TLDR**：针对长视频理解中存在的视觉冗余、长程依赖及语义漂移问题，本文提出VideoHV-Agent框架。该框架将视频问答重构为“先思考后验证”的过程，通过多智能体协作（Thinker、Judge、Verifier、Answer）将候选答案转化为可验证的假设并进行细粒度证据核实。实验证明该方法在多个长视频基准测试上达到SOTA，且具备更强的可解释性和更低的计算成本。 \
**Motivation**：传统的检索式或思维链方法在处理长视频时易产生语义漂移和相关性驱动的错误，需要一种更具逻辑性的主动验证机制。 \
**Method**：提出一种基于假设-验证的多智能体框架，通过Thinker生成假设、Judge提取线索、Verifier定位验证、Answer整合结果。 \
**Result**：在三个长视频理解基准测试中取得了最先进的准确率，并显著提升了逻辑合理性和计算效率。 \
**Conclusion**：“先思考后寻找”的原则能有效提升长视频推理的准确性与可解释性，为复杂视频理解任务提供了新范式。

---

## Abstract
Long video understanding is challenging due to dense visual redundancy, long-range temporal dependencies, and the tendency of chain-of-thought and retrieval-based agents to accumulate semantic drift and correlation-driven errors. We argue that long-video reasoning should begin not with reactive retrieval, but with deliberate task formulation: the model must first articulate what must be true in the video for each candidate answer to hold. This thinking-before-finding principle motivates VideoHV-Agent, a framework that reformulates video question answering as a structured hypothesis-verification process. Based on video summaries, a Thinker rewrites answer candidates into testable hypotheses, a Judge derives a discriminative clue specifying what evidence must be checked, a Verifier grounds and tests the clue using localized, fine-grained video content, and an Answer agent integrates validated evidence to produce the final answer. Experiments on three long-video understanding benchmarks show that VideoHV-Agent achieves state-of-the-art accuracy while providing enhanced interpretability, improved logical soundness, and lower computational cost. We make our code publicly available at: https://github.com/Haorane/VideoHV-Agent.

---

## 论文详细总结（自动生成）

以下是对论文《Think, Then Verify: A Hypothesis–Verification Multi-Agent Framework for Long Video Understanding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：长视频理解（Long Video QA）面临视觉冗余度高、时间跨度长以及推理链条易断裂等挑战。
*   **研究背景**：现有的基于思维链（CoT）或检索的智能体（Agent）通常是“相关性驱动”的，即被动地检索与问题相关的片段。这种方式容易导致语义漂移（Semantic Drift）和错误累积，且在处理复杂逻辑（如因果、时序）时表现乏力。
*   **整体含义**：论文提出“先思后验”（Think, Then Verify）原则，主张推理应始于主动的任务制定，即先明确“如果某个答案正确，视频中必须出现什么证据”，从而将视频问答转变为结构化的假设验证过程。

### 2. 论文提出的方法论
VideoHV-Agent 框架包含三个主要阶段：
*   **上下文摘要（Context Summarization）**：将视频帧转化为文本描述，并根据问题生成简明摘要，用于初步筛选和全局感知。
*   **两步推理（Two-step Reasoning）**：
    *   **假设生成（Hypothesis Generation）**：**Thinker 智能体**将每个候选答案重写为具体的、可测试的假设（包含实体、动作、时序约束）；**Judge 智能体**提取出能区分不同假设的判别性“线索”（Clue）。
    *   **假设验证（Hypothesis Verification）**：**Verifier 智能体**根据线索在视频中定位关键片段，调用细粒度描述工具获取证据，并输出验证状态（已验证、部分验证、未验证）。
*   **自优化循环（Self-Refinement Loop）**：若验证状态为“未验证”，则触发循环，重新生成更具体或更具辨别力的假设和线索。
*   **证据整合（Evidence Integration）**：**Answer 智能体**整合摘要与验证后的证据，生成最终答案及透明的推理链。

### 3. 实验设计
*   **数据集**：
    *   **EgoSchema**：大规模第一视角长视频推理（约3分钟视频）。
    *   **NextQA**：侧重于因果和时序推理的日常视频。
    *   **IntentQA**：侧重于理解叙事视频中的角色意图。
    *   **VideoMME-L**：涵盖更广泛领域和超长时长的综合基准。
*   **对比方法**：对比了包括 VideoAgent、VideoTree、VideoMultiAgents、VideoAgent2、TS-LLAVA 等在内的多种最先进（SOTA）的零样本（Zero-shot）和监督学习方法。

### 4. 资源与算力
*   **模型后端**：所有智能体均使用 **GPT-4o** 作为大语言模型（LLM）骨干。
*   **视觉工具**：使用 LaViLa 或 CogAgent 生成初始帧级描述。
*   **算力说明**：文中未明确给出具体的 GPU 型号和训练时长（由于是基于 API 的 Agent 框架，不涉及传统意义上的模型训练）。但在效率分析中提到，处理 EgoSchema（180秒视频）的平均耗时为 123.67秒，NextQA 为 74.48秒，表现出优于同类 Agent 的推理效率。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流长视频基准测试上进行了评估，涵盖了从几分钟到几十分钟不等的视频长度。
*   **消融实验**：针对“假设生成”、“线索提取”、“验证状态机制”以及“循环次数”分别进行了消融实验，验证了各模块的必要性。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还分析了计算效率（时间成本）、不同问题类型（因果、时序、描述）下的表现，并提供了定性案例分析，实验结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：VideoHV-Agent 在 EgoSchema (81.0%)、NextQA (80.7%) 和 IntentQA (75.6%) 上均取得了零样本下的 SOTA 性能。
*   **逻辑严密**：通过将“相关性检索”转变为“假设验证”，显著减少了幻觉和逻辑错误，尤其在处理复杂的因果推理（ATP-hard 子集）时优势明显。
*   **高效性**：相比于不断扫描全片的 Agent，该方法通过线索引导精准定位，降低了处理冗余信息的计算开销。

### 7. 优点
*   **范式创新**：提出了“先思后验”的结构化推理范式，有效解决了长视频推理中的语义漂移问题。
*   **高可解释性**：每个决策步骤都有明确的假设、线索和验证记录，推理过程透明。
*   **即插即用**：作为一个多智能体框架，可以灵活更换底层的 LLM 或视觉描述模型。

### 8. 不足与局限
*   **依赖强 LLM**：框架性能高度依赖于 GPT-4o 等顶级模型，使用成本较高且受限于 API 调用。
*   **初始描述限制**：如果初始的帧级描述（Caption）丢失了关键细粒度信息，可能会影响 Thinker 智能体生成准确的假设。
*   **实时性挑战**：尽管比同类 Agent 快，但多轮对话和多次调用视觉工具仍难以满足实时视频流的处理需求。

（完）
