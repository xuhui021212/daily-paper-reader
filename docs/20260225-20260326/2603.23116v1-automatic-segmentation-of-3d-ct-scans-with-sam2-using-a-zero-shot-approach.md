---
title: Automatic Segmentation of 3D CT scans with SAM2 using a zero-shot approach
title_zh: 基于零样本方法的 SAM2 3D CT 扫描自动分割
authors: "Miquel Lopez Escoriza, Pau Amargant Alvarez"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23116v1"
tags: ["query:medvid"]
score: 9.0
evidence: 利用基于视频的记忆机制对3D CT扫描进行自动分割
tldr: 本研究探讨了在不进行微调的情况下，利用SAM2模型对3D CT扫描进行零样本自动分割。针对SAM2缺乏体积感知能力的问题，研究者通过将CT切片视为有序序列，并改进其视频记忆机制和推理流程，实现了对医疗体积数据的适配。在TotalSegmentator数据集上的实验证明，精心设计的推理策略能使冻结权重的SAM2生成连贯的3D分割结果，为医疗影像分割提供了高效的零样本方案。
selection_source: fresh_fetch
motivation: 旨在解决SAM2等基础模型在3D医疗影像中缺乏体积感知能力且难以直接应用的问题。
method: 将CT切片视为视频序列，通过改进SAM2的记忆传播机制和推理流程，在不微调权重的情况下实现3D分割。
result: 在包含2500个CT扫描的大规模数据集上验证了该方法的有效性，证明了零样本推理也能产生连贯的3D分割。
conclusion: 研究表明，通过优化推理架构，SAM2可以在完全零样本的情况下胜任复杂的体积医疗影像分割任务。
---

## 摘要
图像分割的基础模型在自然图像中表现出强大的泛化能力，但其在 3D 医学影像中的适用性仍然有限。在这项工作中，我们研究了 Segment Anything Model 2 (SAM2) 在无需任何微调或特定领域训练的情况下，对体积 CT 数据进行自动分割的零样本应用。我们分析了 SAM2 应如何应用于 CT 体积，并确定了其主要局限性：缺乏固有的体积感知能力。为了解决这个问题，我们提出了一系列仅限推理的架构和程序修改，通过将 CT 切片视为有序序列，使 SAM2 的基于视频的记忆机制适应 3D 数据。我们在 TotalSegmentator 数据集的 500 个 CT 扫描子集上进行了系统的消融研究，以评估提示策略、记忆传播方案和多轮细化。基于这些发现，我们选择了性能最佳的配置，并报告了包含 2,500 个 CT 扫描的更大规模 TotalSegmentator 数据集样本的最终结果。我们的结果表明，即使在权重冻结的情况下，只要精心构建其推理流水线，SAM2 也能生成连贯的 3D 分割，证明了全零样本方法在体积医学图像分割中的可行性。

## Abstract
Foundation models for image segmentation have shown strong generalization in natural images, yet their applicability to 3D medical imaging remains limited. In this work, we study the zero-shot use of Segment Anything Model 2 (SAM2) for automatic segmentation of volumetric CT data, without any fine-tuning or domain-specific training. We analyze how SAM2 should be applied to CT volumes and identify its main limitation: the lack of inherent volumetric awareness. To address this, we propose a set of inference-alone architectural and procedural modifications that adapt SAM2's video-based memory mechanism to 3D data by treating CT slices as ordered sequences. We conduct a systematic ablation study on a subset of 500 CT scans from the TotalSegmentator dataset to evaluate prompt strategies, memory propagation schemes and multi-pass refinement. Based on these findings, we select the best-performing configuration and report final results on a bigger sample of the TotalSegmentator dataset comprising 2,500 CT scans. Our results show that, even with frozen weights, SAM2 can produce coherent 3D segmentations when its inference pipeline is carefully structured, demonstrating the feasibility of a fully zero-shot approach for volumetric medical image segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《Automatic Segmentation of 3D CT scans with SAM2 using a zero-shot approach》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将原本为 2D 图像和视频设计的 Segment Anything Model 2 (SAM2) 有效应用于 3D 医学影像（如 CT 扫描），且无需进行昂贵的微调（Fine-tuning）或特定领域的重新训练。
*   **研究动机**：
    *   **标注瓶颈**：传统 3D 医疗分割模型（如 U-Net, UNETR）需要大量像素级标注，且难以泛化到未见类别。
    *   **维度鸿沟**：SAM 是纯 2D 的，而 SAM2 虽然引入了视频记忆机制，但视频的“时间连续性”与 CT 切片的“空间连续性”存在差异（如解剖结构的突变、拓扑变化）。
    *   **零样本潜力**：探索在保持模型权重冻结的情况下，仅通过推理侧（Inference-only）的架构调整，能否实现高质量的体积感知分割。

### 2. 方法论
核心思想是将 3D CT 体积重新解释为“伪视频”（Pseudo-video），将深度（z 轴）映射为时间维度，并针对医学影像特性修改 SAM2 的推理流水线：
*   **结构化提示选择 (SPS)**：默认 SAM2 会同等对待所有提示帧。SPS 引入空间距离阈值（如 0.3），仅保留空间邻近的提示帧在记忆库中，减少远距离无关结构的干扰。
*   **智能切片 (IS)**：分析发现 SAM2 的中间时间嵌入存在高度冗余。IS 策略仅保留最近的两帧，并分配起始和结束的时间嵌入，既提升了体积一致性又降低了运行时间。
*   **多轴传播与融合**：由于 3D 数据是各向同性的，研究者在轴状位（Axial）、冠状位（Coronal）和矢状位（Sagittal）分别独立运行 SAM2，并将得到的 Logit 体积进行平均融合（Sigmoid 激活），以消除单轴偏见。
*   **预处理**：针对骨骼组织进行 Hounsfield 单位（HU）窗口化处理和空间裁剪，以减少领域差异。

### 3. 实验设计
*   **数据集**：使用 **TotalSegmentator** 数据集，专注于 104 种解剖结构中的**骨骼结构**（因其几何形状复杂且与周围组织相似度高）。
*   **实验规模**：
    *   **消融研究**：使用 500 个 CT 体积的子集。
    *   **最终评估**：使用 2,500 个 CT 体积的大规模子集。
*   **基准与对比**：
    *   以原始 SAM2 视频推理流程为基准（Baseline）。
    *   对比了不同模型规模（Tiny, Base, Small, Large）。
    *   对比了不同的提示策略（如 First-Middle-Last 3 帧提示 vs 多帧提示）。
    *   对比了不同的预处理方法（无处理 vs 窗口化 vs CLAHE）。
*   **评价指标**：Dice 系数 (DSC)、交并比 (IoU) 和豪斯多夫距离 (HD)。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA RTX 3090 (24 GB VRAM)** 进行初步消融研究，并使用 **NVIDIA A100** 加速大规模评估。
*   **耗时**：
    *   500 个样本的评估约需 **50 分钟**。
    *   2,500 个样本的最终评估约需 **10 小时**。
*   **部署**：提到支持 CPU 运行（速度较慢）及 ONNX 部署。

### 5. 实验数量与充分性
*   **实验数量**：非常充分。论文进行了五个维度的系统消融实验：模型规模、预处理方式、提示策略、记忆库步长/容量、传播策略（单轴 vs 多轴、前向 vs 双向）。
*   **客观性与公平性**：
    *   使用了固定且平衡的子集进行评估，确保了可重复性。
    *   通过模拟地面真值（Ground-truth）生成掩码提示，排除了人为交互的不确定性，专注于评估模型传播机制的性能。

### 6. 主要结论与发现
*   **零样本可行性**：在权重冻结的情况下，通过推理优化，SAM2 在 2,500 个体积上的平均 Dice 达到 **0.841**，证明了零样本 3D 分割的可行性。
*   **记忆机制的关键**：减少记忆库中冗余的中间帧，并保留具有代表性的时间嵌入（IS 策略），能显著提升性能并降低推理延迟。
*   **结构差异性**：对于空间变化剧烈的结构（如脊椎），改进后的方法比基准提升了 **12.5%** 的 Dice 值。
*   **多轴优势**：在提示预算充足时，跨轴分布提示比单轴密集提示更有效。

### 7. 优点
*   **高效性**：无需任何训练或微调，极大地降低了计算成本和数据标注需求。
*   **通用性**：保持了基础模型的通用权重，避免了特定任务微调导致的灾难性遗忘。
*   **推理优化**：提出的 SPS 和 IS 策略不仅提升了精度，还优化了显存占用和运行速度。

### 8. 不足与局限
*   **领域局限**：实验主要集中在骨骼结构，尚未在软组织或病变（如肿瘤）上进行广泛验证。
*   **固有架构限制**：尽管进行了推理侧优化，但模型本质上仍缺乏真正的 3D 卷积或 3D 注意力，对于极度不连续的解剖结构仍可能失效。
*   **提示依赖**：性能高度依赖于初始提示的质量，在完全自动化的临床流程中可能仍需配合自动提示生成算法。

（完）
