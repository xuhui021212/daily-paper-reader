---
title: "CAST: Modeling Visual State Transitions for Consistent Video Retrieval"
title_zh: CAST：为一致性视频检索建模视觉状态转换
authors: "Yanqing Liu, Yingcheng Liu, Fanghong Dong, Budianto Budianto, Cihang Xie, Yan Jiao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08648v1"
tags: ["query:lvu"]
score: 8.0
evidence: 为长篇叙事建模视觉状态转换
tldr: 针对长视频叙事中短片组合的连贯性挑战，本文定义了“一致性视频检索”（CVR）任务并构建了基准测试。提出了 CAST 插件式适配器，通过学习视觉历史中的状态转换残差，为预训练视觉语言模型引入状态演化偏置。实验证明 CAST 在多个数据集上提升了检索一致性，并能有效辅助视频生成模型的候选重排序，增强时序连贯性。
selection_source: fresh_fetch
motivation: 现有视频检索方法多关注局部语义对齐，忽略了长视频叙事中关键的状态一致性和身份连续性。
method: 提出 CAST 适配器，利用视觉历史预测状态条件残差更新，为冻结的视觉语言空间引入显式的状态演化归纳偏置。
result: 在 YouCook2 和 CrossTask 等基准上表现优异，且在黑盒视频生成候选重排序中显著提升了时序连贯性。
conclusion: 显式建模视觉状态转换是实现一致性视频检索的有效途径，且具有良好的跨模型兼容性和生成优化潜力。
---

## 摘要
随着视频内容创作转向长篇叙事，将短视频片段组合成连贯的故事情节变得越来越重要。然而，现有的检索方案在推理时仍处于上下文无关状态，优先考虑局部语义对齐，而忽略了状态和身份的一致性。为了解决这一结构性限制，我们正式定义了一致性视频检索（CVR）任务，并引入了一个涵盖 YouCook2、COIN 和 CrossTask 的诊断基准。我们提出了 CAST（上下文感知状态转换），这是一种轻量级、即插即用的适配器，兼容多种冻结的视觉-语言嵌入空间。通过从视觉历史中预测受状态约束的残差更新（$Δ$），CAST 为潜在状态演化引入了显式的归纳偏置。大量实验表明，CAST 提高了在 YouCook2 和 CrossTask 上的性能，在 COIN 上保持了竞争力，并在多种基础骨干网络上始终优于零样本基准。此外，CAST 为黑盒视频生成候选（例如来自 Veo）提供了有用的重排序信号，从而促进生成在时间上更连贯的后续内容。

## 速览
**TLDR**：针对长视频叙事中短片组合的连贯性挑战，本文定义了“一致性视频检索”（CVR）任务并构建了基准测试。提出了 CAST 插件式适配器，通过学习视觉历史中的状态转换残差，为预训练视觉语言模型引入状态演化偏置。实验证明 CAST 在多个数据集上提升了检索一致性，并能有效辅助视频生成模型的候选重排序，增强时序连贯性。 \
**Motivation**：现有视频检索方法通常忽略上下文信息，导致在处理长视频叙事时缺乏状态和身份的一致性。 \
**Method**：提出 CAST 适配器，通过预测基于视觉历史的状态条件残差更新，为冻结的视觉语言模型引入显式的状态演化归纳偏置。 \
**Result**：在 YouCook2 和 CrossTask 等基准上显著提升了检索性能，并能有效优化黑盒视频生成模型的时序连贯性。 \
**Conclusion**：显式建模视觉状态转换是实现一致性视频检索的关键，且该轻量化方案具有良好的跨模型兼容性和实用性。

---

## Abstract
As video content creation shifts toward long-form narratives, composing short clips into coherent storylines becomes increasingly important. However, prevailing retrieval formulations remain context-agnostic at inference time, prioritizing local semantic alignment while neglecting state and identity consistency. To address this structural limitation, we formalize the task of Consistent Video Retrieval (CVR) and introduce a diagnostic benchmark spanning YouCook2, COIN, and CrossTask. We propose CAST (Context-Aware State Transition), a lightweight, plug-and-play adapter compatible with diverse frozen vision-language embedding spaces. By predicting a state-conditioned residual update ($Δ$) from visual history, CAST introduces an explicit inductive bias for latent state evolution. Extensive experiments show that CAST improves performance on YouCook2 and CrossTask, remains competitive on COIN, and consistently outperforms zero-shot baselines across diverse foundation backbones. Furthermore, CAST provides a useful reranking signal for black-box video generation candidates (e.g., from Veo), promoting more temporally coherent continuations.

---

## 论文详细总结（自动生成）

以下是对论文《CAST: Modeling Visual State Transitions for Consistent Video Retrieval》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着视频生成技术的发展，创作长篇叙事视频（如烹饪教程、组装步骤）变得日益重要。然而，现有的视频检索方法通常是“上下文无关”的，即独立地匹配文本和视频片段。
*   **核心问题**：这种独立检索会导致两种不连贯性：**身份不一致**（演员或环境突然改变）和**状态不一致**（违反因果逻辑，如先出现切好的菜再出现切菜动作）。
*   **研究目标**：论文正式定义了“一致性视频检索”（Consistent Video Retrieval, CVR）任务，旨在根据视觉历史和当前指令，检索出在语义、身份和状态上均连贯的视频片段。

### 2. 方法论
*   **核心思想**：将动作理解为潜在视觉空间的“状态转换”，而非静态的语义标签。通过预测一个残差向量（$\Delta$）来更新当前状态，从而寻找因果合理的后续片段。
*   **关键技术细节**：
    *   **CAST 适配器**：一个轻量级的插件模块，附加在冻结的预训练模型（如 CLIP）之上。
    *   **残差建模**：预测目标嵌入 $\hat{v}_t \approx v_{t-1} + \Delta$。这种加法结构允许模型通过残差分支关注“状态变化”，同时通过原始状态 $v_{t-1}$ 保留“身份信息”。
    *   **双路径预测器**：
        1.  **指令条件路径**：将当前指令与上一时刻视觉状态拼接，通过 MLP 预测局部状态演化。
        2.  **时序上下文路径**：利用交叉注意力机制，以指令为 Query，视觉历史序列为 Key/Value，捕捉长期依赖。
    *   **损失函数**：采用“类型感知对比损失”，除了全局对比外，显式引入针对“状态负样本”和“身份负样本”的局部对比损失。
    *   **推理策略**：结合语义匹配得分、视觉连续性得分和 CAST 预测得分进行综合重排序。

### 3. 实验设计
*   **数据集**：使用了三个程序化动作数据集：**YouCook2**（烹饪）、**COIN**（通用步骤）和 **CrossTask**（跨领域任务）。
*   **Benchmark 构建**：构建了 CVR 诊断基准，采用 1-vs-9 排名协议。
    *   **硬负样本设计**：包括“状态负样本”（同视频但时间点错误）和“身份负样本”（语义相似但来自不同视频/演员）。
*   **对比方法**：
    *   Zero-Shot 基础模型（CLIP 等）。
    *   后期融合（固定权重或学习权重）。
    *   前期融合（特征拼接）。
*   **跨骨干网络验证**：在 InternVideo2、VideoPrism、GME-Qwen2-VL 和 Qwen3-VL-Embedding 等多种 SOTA 模型上进行了测试。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，学习率 1e-4，Batch Size 为 512。
*   **训练时长**：在不同数据集上训练 20 到 50 个 Epoch。
*   **算力说明**：文中未明确列出具体的 GPU 型号和数量，但提到在特征提取阶段使用了多 GPU 数据并行处理。由于 CAST 是轻量级适配器且骨干网络冻结，其训练算力需求远低于全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集和 5 种不同的基础模型骨干。
*   **消融实验**：非常充分，包括残差建模 vs 直接预测、双路径架构设计、推理信号分解、上下文历史长度（L）的影响等。
*   **人工评测**：针对视频生成重排序任务，对 300 个提示词生成的候选视频进行了盲测人工评估。
*   **客观性**：通过专门设计的硬负样本（状态 vs 身份）来隔离不同的失败模式，评估指标（Acc, MnR, State Acc, Ident Acc）设计科学，能够客观反映模型在一致性上的改进。

### 6. 主要结论与发现
*   **CAST 的有效性**：在所有数据集上均显著优于上下文无关的基准，尤其在处理状态转换剧烈的场景（如 CrossTask）时优势明显。
*   **残差建模的优越性**：相比直接预测目标向量，学习残差更新能更好地平衡身份保留和状态演化。
*   **历史信息的作用**：性能在引入最近的一个片段（L=1）时提升最显著，随后随长度增加趋于饱和。
*   **通用性**：CAST 作为一个通用的适配器，在多种视觉语言嵌入空间中均能稳定提升性能。
*   **下游应用**：CAST 可以作为黑盒视频生成模型（如 Google Veo）的有效重排序工具，显著提升生成结果的物理合理性和时序逻辑。

### 7. 优点
*   **即插即用**：无需重新索引视频库，仅在查询侧增加轻量级计算，易于集成到现有检索系统。
*   **归纳偏置明确**：通过残差连接显式建模“状态演化”，符合物理世界中身份持久、状态变化的规律。
*   **诊断性强**：提出的 CVR 基准和硬负样本挖掘方法为视频理解领域提供了更细粒度的评估工具。

### 8. 不足与局限
*   **上下文窗口限制**：目前实验中上下文长度固定且较短（L=5），可能无法处理极长距离的依赖（如几十个步骤前的伏笔）。
*   **骨干网络依赖**：作为适配器，其上限受限于冻结骨干网络的表征能力；如果基础模型无法分辨细微的状态差异，CAST 也难以弥补。
*   **几何约束缺失**：虽然实验效果好，但未在潜在空间中对残差向量 $\Delta$ 施加显式的几何约束（如正交性），以确保身份和状态特征的彻底解耦。

（完）
