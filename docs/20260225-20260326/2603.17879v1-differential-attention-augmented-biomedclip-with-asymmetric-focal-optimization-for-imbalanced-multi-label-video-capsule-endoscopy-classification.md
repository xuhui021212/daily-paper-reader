---
title: Differential Attention-Augmented BiomedCLIP with Asymmetric Focal Optimization for Imbalanced Multi-Label Video Capsule Endoscopy Classification
title_zh: 采用非对称 Focal 优化的差分注意力增强 BiomedCLIP 用于不平衡多标签胶囊内镜视频分类
authors: "Podakanti Satyajith Chary, Nagarajan Ganapathy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17879v1"
tags: ["query:medvid"]
score: 10.0
evidence: 使用BiomedCLIP进行胶囊内窥镜视频分类
tldr: 本研究针对胶囊内镜（VCE）视频中极端的类别不平衡问题，提出了一种改进的BiomedCLIP多标签分类框架。通过引入差分注意力机制以抑制噪声，并结合非对称焦距损失、加权采样及时间平滑等优化策略，显著提升了对稀有病理特征的识别能力。在RARE-VISION测试集上，该方法在保持高效推理的同时，实现了较高的平均精度均值（mAP）。
selection_source: fresh_fetch
motivation: "旨在解决胶囊内镜视频中病理发现极度稀少（占比不足0.1%）导致的严重类别不平衡及多标签分类难题。"
method: 在BiomedCLIP基础上引入差分注意力机制，并整合非对称焦距损失、平方根频率加权采样、Mixup正则化及时间平滑后处理。
result: "在包含16.1万帧的测试集上，模型实现了0.2456的mAP@0.5，且单GPU全流程推理时间仅需约8.6分钟。"
conclusion: 该框架通过架构改进与优化策略的结合，有效应对了医学影像中的长尾分布问题，为高效、准确的内镜视频自动化分析提供了可行方案。
---

## 摘要
本研究提出了一种用于胶囊内镜视频（VCE）的多标签分类框架，通过结合架构和优化层面的策略，解决了 Galar 数据集固有的极端类别不平衡问题。我们的方法改进了生物医学视觉语言基础模型 BiomedCLIP，将其标准的多头自注意力替换为差分注意力机制，该机制通过计算两个 softmax 注意力图之间的差异来抑制注意力噪声。为了应对偏斜的标签分布（其中病理发现占所有标注帧的比例不足 0.1%），采用了平方根频率加权采样器、非对称 Focal Loss、Mixup 正则化以及逐类别阈值优化。在生成事件级 JSON 之前，通过中值滤波平滑和间隙合并来增强时间连贯性。在包含三次 NaviCam 检查（161,025 帧）的独立 RARE-VISION 测试集上，该流水线实现了 0.2456 的整体时间 mAP@0.5 和 0.2353 的 mAP@0.95，在单张 GPU 上的总推理时间约为 8.6 分钟。

## Abstract
This work presents a multi-label classification framework for video capsule endoscopy (VCE) that addresses the extreme class imbalance inherent in the Galar dataset through a combination of architectural and optimization-level strategies. Our approach modifies BiomedCLIP, a biomedical vision-language foundation model, by replacing its standard multi-head self-attention with a differential attention mechanism that computes the difference between two softmax attention maps to suppress attention noise. To counteract the skewed label distribution, where pathological findings constitute less than 0.1% of all annotated frames, a sqrt-frequency weighted sampler, asymmetric focal loss, mixup regularization, and per-class threshold optimization are employed. Temporal coherence is enforced through median-filter smoothing and gap merging prior to event-level JSON generation. On the held-out RARE-VISION test set comprising three NaviCam examinations (161,025 frames), the pipeline achieves an overall temporal mAP@0.5 of 0.2456 and mAP@0.95 of 0.2353, with total inference completed in approximately 8.6 minutes on a single GPU.

---

## 论文详细总结（自动生成）

### 论文总结：采用非对称 Focal 优化的差分注意力增强 BiomedCLIP 用于不平衡多标签胶囊内镜视频分类

#### 1. 核心问题与研究动机
*   **核心问题**：胶囊内镜（VCE）视频数据量巨大（单次检查 5-13 万帧），但临床相关的病理特征（如活动性出血、溃疡等）极其稀少（占比不足 0.1%）。这种**极端的类别不平衡**导致标准模型倾向于预测多数类（解剖结构），而忽略关键的少数类病理事件。
*   **研究动机**：
    1.  探索**差分注意力机制**是否能通过消除注意力噪声，提取更具辨识度的微小病理特征。
    2.  开发一套多层级的类别不平衡处理策略（涵盖采样、损失函数、增强和后处理），确保稀有事件在优化过程中获得足够的梯度信号。

#### 2. 方法论
该框架由四个阶段组成：
*   **核心架构（Differential BiomedCLIP）**：
    *   以 **BiomedCLIP**（ViT-B/16 视觉编码器 + PubMedBERT 文本编码器）为基础模型。
    *   **差分注意力（Differential Attention）**：将标准多头自注意力替换为差分注意力。其核心思想是将注意力头分为两组，计算两个 Softmax 注意力图的差值。公式上通过 $Softmax(Q_1K_1^T) - \lambda \cdot Softmax(Q_2K_2^T)$ 来抵消通用的背景噪声，放大稀有但具有诊断意义的空间信号。
*   **多任务分类路径**：
    *   **分类路径**：通过激励模块（Excitation Block，类似 SE 模块）进行通道门控，随后接线性层输出 17 类概率。
    *   **对比路径**：将图像特征与冻结的 PubMedBERT 生成的临床提示词（如“显示[标签]的胶囊内镜图像”）嵌入进行匹配。
*   **不平衡处理与优化**：
    *   **采样**：使用平方根频率加权采样器（Sqrt-frequency weighted sampler）。
    *   **损失函数**：采用**非对称 Focal Loss（ASL）**，设置 $\gamma_+=1, \gamma_-=4$，剧烈削减占主导地位的简单负样本权重。
    *   **正则化**：应用 Mixup（$\alpha=0.3$）、标签平滑（$\epsilon=0.05$）和指数移动平均（EMA）。
*   **时间后处理**：
    *   应用中值滤波（窗口大小 7）抑制孤立误报。
    *   合并小于 5 帧的间隙，并移除短于 3 帧的事件，以符合临床连续性特征。

#### 3. 实验设计
*   **数据集**：使用 **Galar 数据集**。训练集包含 60 个视频（约 49.5 万帧），验证集 10 个视频（约 8 万帧），测试集（RARE-VISION）包含 3 个 NaviCam 检查视频（约 16.1 万帧）。
*   **Benchmark**：ICPR 2026 RARE-VISION 挑战赛官方评估标准，主要指标为时间维度的 **mAP@0.5** 和 **mAP@0.95**。
*   **对比情况**：论文主要展示了其流水线在挑战赛测试集上的表现，并对比了有无正则化策略下的验证集损失收敛情况。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX PRO 6000 (96GB)** GPU。
*   **训练耗时**：训练 5 个 Epoch，总计约 **3 小时**。
*   **推理效率**：处理测试集全部 16.1 万帧数据仅需 **8.6 分钟**（约 311 帧/秒），表现出极高的临床实用潜力。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 5 个 Epoch 的训练，并在验证集上进行了逐类别的性能分析（AP、AUC、F1 等）。
*   **充分性评价**：实验涵盖了从采样、架构改进到后处理的全流程消融思考。虽然受限于竞赛论文篇幅，未展示大规模的对比实验表，但通过对 17 个类别的详细指标拆解，客观反映了模型在不同频率类别上的表现差异。

#### 6. 主要结论与发现
*   **性能指标**：在测试集上实现整体 **mAP@0.5 为 0.2456**，**mAP@0.95 为 0.2353**。
*   **边界定位精准**：mAP@0.5 与 mAP@0.95 之间的差距极小（仅 0.0103），说明时间后处理策略（中值滤波和间隙合并）能非常精准地定位病理事件的时间边界。
*   **类别差异**：解剖结构（胃、小肠、结肠）识别精度极高（AP > 0.84），但极稀有病理（如活动性出血、红斑）的 AP 仍低于 0.03，显示出极长尾分布下的学习困境。

#### 7. 优点
*   **架构创新**：将差分注意力引入生物医学基础模型，有效针对内镜图像中背景噪声多、病理特征细微的特点。
*   **策略全面**：综合运用了采样、损失加权、对比学习和时间平滑，是一个完整且针对性强的医学影像处理流水线。
*   **推理高效**：在保证精度的前提下，推理速度远超实时要求，适合大规模临床筛查。

#### 8. 不足与局限
*   **稀有类瓶颈**：对于样本数极少（少于 200 帧）的类别，模型依然难以构建鲁棒的特征表示。
*   **域偏移风险**：训练数据主要来自 Olympus 和 PillCam 系统，而测试集来自 NaviCam 系统，分辨率和色彩表现的差异可能影响泛化性能。
*   **缺乏时序建模**：目前主要基于单帧分类加后处理，未在模型内部引入循环神经网络（RNN）或时序 Transformer 来利用帧间上下文。

（完）
