---
title: Multi-View Deformable Convolution Meets Visual Mamba for Coronary Artery Segmentation
title_zh: 多视图可变形卷积结合视觉 Mamba 用于冠状动脉分割
authors: "Xiaochan Yuan, Pai Zeng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21829v1"
tags: ["query:medvid"]
score: 9.0
evidence: 从CTA图像中进行冠状动脉分割
tldr: 针对冠状动脉分割中血管细长、分支多及计算资源受限等挑战，本文提出MDSVM-UNet框架。该方法结合了多向蛇形卷积（MDSConv）以捕捉三维解剖平面的几何特征，并利用残差视觉Mamba（RVM）在保持线性复杂度的同时建模长程依赖。通过两阶段策略（粗分割引导分块提取与细分割），该框架有效提升了复杂血管结构的分割精度，平衡了性能与效率。
selection_source: fresh_fetch
motivation: 传统CNN难以捕捉长程依赖，而ViT计算开销过大，难以应对冠状动脉细长且复杂的解剖结构。
method: 提出结合多向蛇形卷积（MDSConv）与残差视觉Mamba（RVM）的两阶段分割框架，实现多视角特征融合与高效的长程建模。
result: 该方法通过粗细两阶段策略，在有效捕捉血管细微结构的同时，显著降低了计算复杂度并抑制了误报。
conclusion: MDSVM-UNet为临床环境下高效、精准的冠状动脉分割提供了一种结合形变卷积与状态空间模型的新颖解决方案。
---

## 摘要
从计算机断层扫描血管造影 (CTA) 图像中准确分割冠状动脉对于心血管疾病的诊断和治疗规划具有极其重要的临床意义。然而，由于血管系统固有的多分支和细长管状形态，加之前景血管与背景组织之间严重的类别不平衡，冠状动脉分割仍然具有挑战性。传统的基于卷积神经网络 (CNN) 的方法难以捕捉空间上相距较远的血管结构之间的长程依赖关系，而基于视觉 Transformer (ViT) 的方法则会产生极高的计算开销，阻碍了其在资源受限的临床环境中的部署。受状态空间模型 (SSM) 最近在以线性复杂度高效建模长程序列依赖方面取得成功的启发，我们提出了 MDSVM-UNet，这是一种新型的两阶段冠状动脉分割框架，它协同集成了多向蛇形卷积 (MDSConv) 与残差视觉 Mamba (RVM)。在编码阶段，我们引入了 MDSConv，这是一种可变形卷积模块，它沿着矢状面、冠状面和轴状面三个正交解剖平面学习自适应偏移，从而实现全面的多视图特征融合，准确捕捉冠状动脉血管的细长且扭曲的几何形状。在解码阶段，我们设计了一个基于 RVM 的上采样解码器块，利用选择性状态空间机制来建模切片间的长程依赖关系，同时保持线性计算复杂度。此外，我们提出了一种渐进式两阶段分割策略：第一阶段进行粗略的全图分割以引导智能分块提取，而第二阶段进行细粒度的块级分割以恢复血管细节并抑制假阳性。

## Abstract
Accurate segmentation of coronary arteries from computed tomography angiography (CTA) images is of paramount clinical importance for the diagnosis and treatment planning of cardiovascular diseases. However, coronary artery segmentation remains challenging due to the inherent multi-branching and slender tubular morphology of the vasculature, compounded by severe class imbalance between foreground vessels and background tissue. Conventional convolutional neural network (CNN)-based approaches struggle to capture long-range dependencies among spatially distant vascular structures, while Vision Transformer (ViT)-based methods incur prohibitive computational overhead that hinders deployment in resource-constrained clinical settings. Motivated by the recent success of state space models (SSMs) in efficiently modeling long-range sequential dependencies with linear complexity, we propose MDSVM-UNet, a novel two-stage coronary artery segmentation framework that synergistically integrates multidirectional snake convolution (MDSConv) with residual visual Mamba (RVM). In the encoding stage, we introduce MDSConv, a deformable convolution module that learns adaptive offsets along three orthogonal anatomical planes -- sagittal, coronal, and axial -- thereby enabling comprehensive multi-view feature fusion that faithfully captures the elongated and tortuous geometry of coronary vessels. In the decoding stage, we design an RVM-based upsampling decoder block that leverages selective state space mechanisms to model inter-slice long-range dependencies while preserving linear computational complexity. Furthermore, we propose a progressive two-stage segmentation strategy: the first stage performs coarse whole-image segmentation to guide intelligent block extraction, while the second stage conducts fine-grained block-level segmentation to recover vascular details and suppress false positives..

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MDSVM-UNet** 的新型深度学习框架，专门用于从计算机断层扫描血管造影（CTA）图像中进行冠状动脉分割。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：冠状动脉具有细长、多分支且高度扭曲的复杂几何结构，且在 CTA 图像中血管（前景）与组织（背景）之间存在严重的类别不平衡。
*   **研究动机**：
    *   **CNN 的局限性**：传统卷积采用规则采样，难以捕捉长程依赖关系，且无法很好地拟合血管的拓扑结构。
    *   **Transformer 的局限性**：虽然能建模全局信息，但其二次方计算复杂度在处理高分辨率 3D 医学影像时开销巨大。
    *   **临床需求**：自动、精准的分割对于心血管疾病（如冠心病）的诊断和手术规划至关重要。

### 2. 方法论：核心思想与关键技术
论文提出了一个**两阶段渐进式分割框架**，结合了形变卷积与状态空间模型（SSM）：
*   **核心思想**：利用多向蛇形卷积捕捉局部几何特征，利用视觉 Mamba 建模全局切片间依赖，并通过“粗到精”的策略平衡计算效率与细节恢复。
*   **关键技术细节**：
    1.  **多向蛇形卷积 (MDSConv)**：扩展了动态蛇形卷积，沿着矢状面、冠状面和轴状面三个正交解剖平面学习自适应偏移量，实现多视图特征融合，以拟合血管的细长管状形态。
    2.  **残差视觉 Mamba (RVM) 解码器**：在解码阶段引入基于 Mamba 的视觉状态空间模块（VSSM），利用其线性计算复杂度的特性来捕捉 3D 图像的长程空间依赖。
    3.  **两阶段策略**：
        *   **第一阶段**：对下采样后的全图进行粗分割，确定血管的大致位置。
        *   **第二阶段**：根据第一阶段结果提取 64×64×64 的高分辨率局部块进行精细分割，最后合并结果。
    4.  **架构优化**：采用 UNet++ 式的密集跳跃连接，减少编码器与解码器之间的语义鸿沟。

### 3. 实验设计
*   **数据集**：使用大规模公开基准数据集 **ImageCAS**，包含 1,000 例高质量标注的 3D CTA 图像（750 例用于训练，250 例用于验证和测试）。
*   **评价指标**：Dice 相似系数 (DSC)、豪斯多夫距离 (HD) 和平均豪斯多夫距离 (AHD)。
*   **对比方法 (Benchmark)**：对比了 10 种主流模型，包括 FCN、3D U-Net、TS ConvGRU (RNN类)、GCN (图卷积类)、3D U-Net++、DSU-Net (形变卷积类)、SwinUnet (Transformer类) 以及最新的 Mamba 类模型 (LightM-UNet, UltraLight)。

### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA GeForce RTX 3090 GPU (24 GB 显存)**。
*   **软件环境**：Python 3.10, PyTorch 2.1.1, CUDA 11.8。
*   **训练时长**：第一阶段训练 25 个 epoch，第二阶段训练 50 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量对比实验（单阶段 vs 两阶段）、消融实验以及定性可视化分析。
*   **充分性与客观性**：
    *   对比了不同类型的架构（CNN, Transformer, Mamba, GCN），覆盖面广。
    *   针对损失函数（Dice Loss vs Focal Loss）进行了对比。
    *   消融实验分别验证了 MDSConv 和 RVM 模块的增益，证明了两者结合的协同效应。
    *   实验基于目前最大的公开冠脉数据集，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：MDSVM-UNet 在两阶段设置下达到了 **0.8365 的 DSC**，比 ImageCAS 官方基准提升了 **5.41%**，同时在 HD 和 AHD 指标上显著下降。
*   **效率优势**：模型参数量为 **26.7M**，远低于 SwinUnet (256.3M)，在保持高性能的同时实现了更优的计算效率。
*   **策略有效性**：两阶段策略相比单阶段在 DSC 上提升了约 15%，证明了局部精细化处理对细小血管分割的必要性。

### 7. 优点（亮点）
*   **结构感知**：MDSConv 针对血管的解剖特性设计，比标准卷积更能捕捉管状结构的连续性。
*   **长程建模**：首次将视觉 Mamba 应用于冠状动脉分割，解决了 Transformer 计算量大的痛点。
*   **鲁棒性**：两阶段策略有效抑制了假阳性，提高了血管末梢分支的连接性。

### 8. 不足与局限
*   **数据集单一**：虽然 ImageCAS 规模较大，但实验仅限于该单一数据集，缺乏在其他中心或不同厂商设备数据上的泛化性验证。
*   **实时性未提及**：虽然强调了线性复杂度，但未给出具体的推理延迟（Inference Latency）数据，难以评估其在实时临床诊断中的表现。
*   **多模态缺失**：目前仅针对 CTA 图像，未探讨在 MRA 或其他血管造影模态上的适用性。

（完）
