---
title: Unified Spatiotemporal Token Compression for Video-LLMs at Ultra-Low Retention
title_zh: 极低保留率下视频大语言模型的统一时空 Token 压缩
authors: "Junhao Du, Jialong Xue, Anqi Li, Jincheng Dai, Guo Lu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21957v1"
tags: ["query:lvu"]
score: 9.0
evidence: 视频大语言模型在极低保留率下的统一时空令牌压缩
tldr: "针对Video-LLMs在超低视觉Token保留率下性能大幅下降的问题，本文提出了一种统一的时空Token压缩策略。该方法将压缩重新定义为全局分配任务，结合注意力权重和语义相似度筛选高贡献Token，并通过聚类合并未选Token以确保信息完整性。此外，引入文本感知合并进行二次压缩。该方法无需重训练，在仅保留约2% Token的情况下，仍能保持基准性能的90.1%，显著降低了计算开销和推理延迟。"
selection_source: fresh_fetch
motivation: 现有的两阶段时空压缩方法在极低保留率下会导致信息分配不均和关键视觉证据丢失。
method: 提出一种统一的选择机制，结合注意力与语义相似度进行全局Token筛选，并利用聚类合并与文本感知合并技术实现高效压缩。
result: "在多个基准测试中，仅保留约2%的视觉Token即可维持90.1%的基线性能，同时将FLOPs降低至2.6%。"
conclusion: 该统一时空压缩策略在超低Token保留率下实现了视频理解的最优性能，且具有良好的通用性和即插即用特性。
---

## 摘要
视频大语言模型（Video-LLMs）由于大量的视觉 Token 而面临高昂的计算成本。现有的 Token 压缩方法通常采用两阶段时空压缩策略，依赖于特定阶段的指标以及时空可分离性的隐式假设。然而，在极低的保留率下，此类方法往往会导致分配不均，并丢失对问答至关重要的视觉证据。我们将 Token 压缩重新定义为全局 Token 保留池中的时空分配任务。我们提出了一种统一的选择机制，结合注意力权重和语义相似度，在全局范围内选择高贡献、低冗余的 Token。未被选中的 Token 通过聚类进行合并并重新填充，从而保持信息的完整性。在 LLM 内部，我们进一步引入了文本感知合并，根据查询相关性进行二次压缩。无需重新训练，我们的方法可作为即插即用模块与现有的 Video-LLMs 兼容。实验表明，在多个基准测试中，仅保留约 2% 的视觉 Token 即可维持 90.1% 的基准性能，同时将 FLOPs 降低至约 2.6%。这些优势在不同的骨干网络中具有通用性，降低了端到端的推理延迟和内存消耗。我们的统一时空 Token 压缩策略在极低 Token 保留率下的视频理解领域达到了最先进水平。

## Abstract
Video large language models (Video-LLMs) face high computational costs due to large volumes of visual tokens. Existing token compression methods typically adopt a two-stage spatiotemporal compression strategy, relying on stage-specific metrics and an implicit assumption of spatiotemporal separability. Under extremely low retention ratios, however, such approaches often result in unbalanced allocation and loss of visual evidence essential for question answering. We reformulate token compression as a spatiotemporal allocation task within a global token retention pool. We propose a unified selection mechanism that integrates attention weights and semantic similarity to globally select tokens with high contribution and low redundancy. Unselected tokens are merged via clustering and refilled, preserving information integrity. Inside the LLM, we further introduce text-aware merging to perform secondary compression based on query relevance. Without requiring retraining, our method serves as a plug-and-play module compatible with existing Video-LLMs. Experiments show that retaining only about 2% of visual tokens preserves 90.1% of baseline performance across multiple benchmarks, while reducing FLOPs to roughly 2.6%. These benefits generalize across diverse backbones, decreasing end-to-end inference latency and memory consumption. Our unified spatiotemporal token compression strategy establishes the state-of-the-art in video understanding under ultra-low token retention.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unified Spatiotemporal Token Compression for Video-LLMs at Ultra-Low Retention》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
视频大语言模型（Video-LLMs）在处理长视频时面临巨大的计算开销，主要源于视觉 Token 数量庞大（如 32 帧视频可产生超过 6000 个 Token）。
*   **现有挑战**：目前的 Token 压缩方法多采用“先空间、后时间”的两阶段策略，这基于时空冗余可分离的假设。但在**极低保留率（如 <5%）**下，这种分离策略会导致时空资源分配不均，容易丢失关键视觉证据或保留过多重复信息。
*   **核心目标**：在保证极高压缩比（如仅保留 2% Token）的同时，最大限度保留视频的关键语义，实现高效推理。

### 2. 方法论：核心思想与关键技术
论文将 Token 压缩重新定义为**全局池化分配任务**，提出了一个内外结合的框架：

*   **外部统一时空压缩（Unified Spatiotemporal Compression）**：
    *   **保留池（Retention Pool）**：利用视觉编码器的注意力分数（衡量贡献度）和余弦相似度（衡量冗余度）进行全局筛选。只有高贡献且与已选 Token 相似度低于阈值 $\tau$ 的 Token 才能进入保留池。
    *   **回收池（Recycle Pool）**：未被选中的 Token 不直接丢弃，而是通过 **DPC-KNN（基于 K 近邻的密度峰值聚类）** 算法进行聚类合并，提取聚类中心作为语义补充重新填回保留池，确保信息完整性。
*   **内部文本感知合并（Text-Aware Merging）**：
    *   在 LLM 推理过程中，结合**文本对视觉的交叉注意力**和**语义相似度**，识别与用户查询最相关的视觉 Token。
    *   将不相关的 Token 合并到最相似的保留 Token 中，而非直接删除，以减少位置偏置（RoPE 引起）并增强查询相关性。

### 3. 实验设计
*   **数据集/基准测试**：涵盖了 6 个主流视频理解榜单：**MVBench**（综合任务）、**EgoSchema**（长视频）、**MLVU**（长视频理解）、**LongVideoBench**、**VideoMME** 以及 **ActivityNet-QA**（开放式问答）。
*   **对比方法**：对比了 5 种最先进的训练免（Training-free）方法：**FastV**、**VisionZip**、**LLaVA-Scissor**、**FastVID** 和 **HoliTom**。
*   **适配模型**：在 LLaVA-OneVision (7B/0.5B)、LLaVA-Video (7B) 和 Qwen2.5-VL (7B) 等多个骨干网络上进行了验证。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA RTX 4090** 或 **A100** GPU 上完成。
*   **训练成本**：该方法是**无需训练（Training-free）**且**即插即用**的，因此没有训练时长开销。
*   **推理效率**：在 2% 保留率下，FLOPs 降低至原始模型的 **2.6%**，吞吐量显著提升（如从 27.6 token/s 提升至 35.7 token/s）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估，涵盖了从 1% 到 100% 的多种 Token 保留率设置。
*   **消融实验**：针对注意力筛选、相似度去冗余、聚类合并、文本感知权重 $\lambda$、压缩层位置 $K$ 等关键超参数进行了详尽的消融研究。
*   **客观性**：通过跨不同架构（LLaVA 系列与 Qwen 系列）的实验证明了通用性；使用 GPT-3.5-Turbo 作为第三方评测器评估开放式问答，保证了评价的客观性。

### 6. 主要结论与发现
*   **极高压缩比下的性能**：在仅保留 **2%** 视觉 Token 的极端情况下，该方法仍能保持基准模型 **90.1%** 的性能。
*   **效率提升**：相比当前最强的 HoliTom，该方法在更低的计算开销下实现了更高的准确率。
*   **长视频优势**：通过极低保留率，模型能够处理更多帧数（如从 16 帧扩展到 128 帧），在长视频基准测试中表现优异。
*   **文本引导的重要性**：证明了在 LLM 内部引入查询相关性引导，能有效缓解注意力机制的位置偏置问题。

### 7. 优点与亮点
*   **全局优化**：打破了传统的两阶段压缩限制，在全局范围内平衡贡献度与冗余度。
*   **信息无损化处理**：通过聚类回收机制，避免了硬剪枝带来的信息彻底丢失。
*   **即插即用**：无需任何微调即可适配现有模型，部署成本极低。
*   **鲁棒性**：在超低保留率（1%-5%）下的表现远超同类方法，展示了极强的鲁棒性。

### 8. 不足与局限
*   **实时性限制**：目前主要针对离线视频处理，尚未支持实时流式视频的 Token 压缩。
*   **采样策略依赖**：依然依赖于均匀帧采样，对于极短或极长视频，固定的采样频率可能不是最优解。
*   **计算延迟偏置**：虽然大幅降低了 FLOPs，但复杂的聚类算法（DPC-KNN）在 CPU/GPU 切换或小规模计算时可能会引入额外的处理延迟（TTFT 略高于部分简单剪枝方法）。

（完）
