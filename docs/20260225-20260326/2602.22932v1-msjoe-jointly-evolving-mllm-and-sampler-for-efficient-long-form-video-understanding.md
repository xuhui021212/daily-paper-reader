---
title: "MSJoE: Jointly Evolving MLLM and Sampler for Efficient Long-Form Video Understanding"
title_zh: MSJoE：联合演化 MLLM 与采样器以实现高效长视频理解
authors: "Wenhui Tan, Xiaoyi Yu, Jiaze Li, Yijing Chen, Jianzhong Ju, Zhenbo Luo, Ruihua Song, Jian Luan"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22932v1"
tags: ["query:lvu"]
score: 10.0
evidence: 高效长视频理解与问答
tldr: 针对长视频理解中计算开销大且关键信息稀疏的挑战，本文提出MSJoE框架，通过联合进化多模态大模型（MLLM）与轻量级采样器实现高效理解。该方法利用MLLM生成多维度查询，结合CLIP相似度矩阵由采样器筛选关键帧，并采用强化学习对查询推理、帧采样和视频理解进行协同优化。实验表明，MSJoE在多个长视频基准测试中显著提升了性能。
selection_source: fresh_fetch
motivation: 旨在解决长视频中仅有少量关键帧对回答特定问题有效，而全量处理会导致计算效率低下的问题。
method: 提出一种联合进化框架，通过MLLM生成查询并配合轻量级采样器筛选关键帧，利用强化学习同步优化采样策略与模型理解能力。
result: "在VideoMME和LongVideoBench等多个榜单上，MSJoE比基础模型准确率提升了8.0%，并超越了当前最强的基准方法。"
conclusion: MSJoE证明了通过协同优化采样器与大模型的推理过程，可以实现更高效且精准的长视频内容理解。
---

## 摘要
高效理解长视频仍然是多模态大语言模型（MLLM）面临的一个根本挑战。在本文中，我们提出了 MLLM-Sampler 联合演化（MSJoE），这是一个联合演化 MLLM 和轻量级关键帧采样器的新型框架，旨在实现高效的长视频理解。MSJoE 基于一个关键假设，即对于回答视频相关的每个问题，只有一小部分关键帧子集是真正具有信息量的。具体而言，MSJoE 首先推理出若干查询（queries），这些查询描述了与问题相关的多样化视觉视角。然后，这些查询与冻结的 CLIP 模型交互，生成查询-帧相似度矩阵。最后，一个轻量级采样器根据该矩阵预测关键帧采样权重，选择一组紧凑的信息帧，并将其输入 MLLM 以生成答案。MLLM 和采样器都通过强化学习进行联合优化，从而实现了查询推理、帧采样和关键帧理解的协同适应。我们收集了一个包含 2.8K 个视频和 7K 个问答对的新长视频问答数据集，以支持训练过程。在 VideoMME、LongVideoBench、LVBench 和 MLVU 上的广泛实验表明，MSJoE 相比基础 MLLM 实现了 8.0% 的准确率提升，且比最强的基准方法高出 1.1% 的准确率。

## 速览
**TLDR**：针对长视频理解中计算效率与关键信息提取的挑战，本文提出MSJoE框架。该框架通过联合演化多模态大模型（MLLM）与轻量级关键帧采样器，利用强化学习协同优化查询推理、帧采样和关键帧理解。MSJoE仅选取少量关键帧进行推理，在VideoMME等多个基准测试上显著提升了准确率，并发布了包含2.8K视频的新数据集。 \
**Motivation**：长视频理解面临计算开销大且冗余信息多的挑战，需要一种高效的方法来识别并提取对回答特定问题真正关键的帧。 \
**Method**：提出MSJoE框架，通过MLLM生成多维度查询，结合CLIP相似度矩阵和轻量级采样器筛选关键帧，并利用强化学习对MLLM与采样器进行联合优化。 \
**Result**：在VideoMME、LongVideoBench等多个长视频基准测试中，MSJoE比基础模型准确率提升了8.0%，并超越了现有的最强基准方法。 \
**Conclusion**：联合演化MLLM与采样器是实现高效、精准长视频理解的有效途径，证明了针对性采样对提升模型性能的重要性。

---

## Abstract
Efficiently understanding long-form videos remains a fundamental challenge for multimodal large language models (MLLMs). In this paper, we present MLLM-Sampler Joint Evolution (MSJoE), a novel framework that jointly evolves the MLLM and a lightweight key-frame sampler for efficient long-form video understanding. MSJoE builds upon a key assumption that only a small subset of key-frames is truly informative for answering each question to a video. Specifically, MSJoE first reasons out several queries, which describe diverse visual perspectives relevant to the question. Then, these queries interact with a frozen CLIP model to produce a query-frame similarity matrix. Finally, a lightweight sampler predicts key-frame sampling weights from this matrix, selecting a compact set of informative frames, which are then fed into the MLLM for answer generation. Both the MLLM and sampler are jointly optimized through reinforcement learning, enabling co-adaptation of query-reasoning, frame-sampling, and key-frame understanding. A new long-video QA dataset containing 2.8K videos with 7K question-answer pairs is collected to support the training process. Extensive experiments on VideoMME, LongVideoBench, LVBench, and MLVU show that MSJoE achieves 8.0\% accuracy gain upon the base MLLM, and 1.1\% higher accuracy than strongest baseline method.

---

## 论文详细总结（自动生成）

### 论文总结：MSJoE —— 联合演化 MLLM 与采样器以实现高效长视频理解

#### 1. 核心问题与整体含义（研究动机和背景）
长视频理解是多模态大语言模型（MLLM）面临的重大挑战。现有的均匀采样方法在处理长视频时，视觉上下文长度随时长线性增加，而注意力计算呈平方级增长，导致效率低下且容易遗漏关键事件。
*   **核心假设**：对于特定的视频问答任务，视频中只有极少数关键帧是真正包含答案信息的。
*   **研究动机**：现有的采样方法（如基于 CLIP 的检索）存在三个问题：1. 仅靠问题本身不足以检索到所有相关帧（信息不足）；2. 如何将相似度分数转化为有效的采样权重（采样难题）；3. MLLM 与采样器缺乏协同优化，导致模型无法适应稀疏的关键帧输入（协作缺失）。

#### 2. 论文提出的方法论
MSJoE 框架通过**联合演化（Joint Evolution）** MLLM 和一个轻量级采样器来解决上述问题。其推理流程包含四个步骤：
*   **MLLM 引导的查询生成**：MLLM 首先观察极低分辨率的视频预览（Sparse Preview），根据问题推理出若干个描述视觉线索的查询（Queries）。
*   **相似度矩阵计算**：利用冻结的 CLIP 模型计算生成的查询与密集采样帧（1 FPS）之间的余弦相似度，形成相似度矩阵。
*   **可学习的关键帧采样器**：采用一个轻量级的 **1D U-Net** 采样器，将相似度矩阵作为输入，预测每帧的采样概率，并从中抽取 $K$ 个关键帧。
*   **答案生成**：MLLM 接收高分辨率的关键帧，结合之前的推理上下文生成最终答案。
*   **联合强化学习（Joint RL）**：使用 **GRPO 算法** 优化 MLLM，使用 **REINFORCE 算法** 优化采样器。通过准确率奖励、格式奖励和信息量奖励，使 MLLM 学会生成更好的查询，同时使采样器学会选择最有助于推理的帧。

#### 3. 实验设计
*   **数据集**：
    *   **自建数据集**：收集了 2.8k 个长视频，通过 Gemini 生成描述并构建了 7.1k 个高质量 QA 对（LongVideoQA-ALL/HARD），用于 RL 训练。
    *   **测试基准（Benchmarks）**：VideoMME、LongVideoBench、LVBench 和 MLVU。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-1.5-pro。
    *   **开源长视频模型**：VideoMind, LongVU, NVILA, Qwen-2.5-VL-7B（基座模型）。
    *   **采样算法**：Q-Frame, BOLT（启发式采样）, TSPO（可学习采样）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA H20 GPU** 的服务器上完成。
*   **训练细节**：
    *   采样器预训练：1 个 epoch，学习率 1e-5。
    *   联合 RL 训练：2 个 epoch，MLLM 学习率 1e-6，采样器学习率 1e-5。
    *   推理后端：使用 vLLM 和 VERL 框架进行分布式训练与推理。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **基准测试**：在 4 个主流长视频榜单上进行了全面评测。
*   **消融实验**：
    *   验证了 MLLM 和采样器分别在“冻结”与“训练”状态下的表现，证明了联合演化的必要性。
    *   测试了不同输入帧数（8 到 64 帧）对性能的影响。
    *   分析了奖励函数设计（信息量奖励、难度感知奖励）的有效性。
    *   探讨了预览帧数量对查询生成质量的影响。
*   **效率分析**：对比了各步骤的耗时，证明了该框架在增加少量计算开销的情况下显著提升了准确率。

#### 6. 主要结论与发现
*   **性能领先**：MSJoE 在 64 帧配置下，相比基座模型 Qwen-2.5-VL 实现了 **8.0%** 的平均准确率提升，并超越了 TSPO 等强基准方法。
*   **高效性**：MSJoE 使用 32 帧的效果甚至优于许多使用更多帧数的密集采样模型。
*   **协同效应**：实验证明，经过 RL 训练的 MLLM 能生成更具“视觉感知力”的查询，而采样器能更好地捕捉视频的叙事结构而非简单的关键词匹配。

#### 7. 优点（亮点）
*   **联合优化思想**：打破了以往“固定模型训练采样器”的局限，实现了感知（采样）与理解（推理）的同步进化。
*   **查询驱动采样**：通过 MLLM 生成多维度查询，解决了单一问题描述在检索时的信息不足问题。
*   **强化学习机制**：引入难度感知奖励和信息量奖励，稳定了采样器的训练过程，并诱导模型产生更具判别性的视觉关注点。

#### 8. 不足与局限
*   **计算开销**：虽然推理时帧数减少，但增加了“预览-查询-采样”的前置步骤，整体推理耗时比简单均匀采样增加了约 30%。
*   **依赖预训练组件**：采样质量高度依赖于 CLIP 模型的跨模态对齐能力，对于 CLIP 难以识别的极细粒度动作或抽象概念可能存在瓶颈。
*   **训练复杂性**：强化学习（尤其是 GRPO）对超参数较为敏感，且需要构建专门的高质量数据集来支撑训练。

（完）
