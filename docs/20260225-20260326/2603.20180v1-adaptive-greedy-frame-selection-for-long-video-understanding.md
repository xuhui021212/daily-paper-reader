---
title: Adaptive Greedy Frame Selection for Long Video Understanding
title_zh: 面向长视频理解的自适应贪婪帧选择
authors: "Yuning Huang, Fengqing Zhu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20180v1"
tags: ["query:lvu"]
score: 9.0
evidence: 针对长视频问答的自适应帧选择，减少视觉标记
tldr: 针对长视频理解中视觉语言模型受限于输入帧数的问题，本文提出一种自适应贪婪帧选择方法。该方法在固定帧预算下，通过联合优化查询相关性和语义代表性，克服了均匀采样易丢失关键时刻及相关性采样易导致帧冗余的缺陷。利用SigLIP和DINOv2提取特征，并结合子模函数最大化策略与问题分类器，在MLVU数据集上显著提升了长视频问答的准确率，尤其在低预算场景下表现优异。
selection_source: fresh_fetch
motivation: 传统的均匀采样或仅基于相关性的采样方法难以在有限的帧预算内平衡关键信息的捕捉与冗余的消除。
method: 提出一种基于子模函数最大化的贪婪选择算法，结合SigLIP相关性评分与DINOv2语义覆盖度，并通过文本分类器动态调整选择策略。
result: 在MLVU基准测试中，该方法在各种帧预算下均优于均匀采样和现有基准，且在极低预算下提升最为显著。
conclusion: 结合查询相关性与语义多样性的自适应帧选择是提升长视频理解效率和准确性的有效途径。
---

## 摘要
大视觉语言模型（VLM）正越来越多地应用于长视频问答，但推理过程往往受限于输入帧数及由此产生的视觉 Token 数量。朴素的稀疏采样可能会遗漏关键时刻，而纯粹由相关性驱动的选择往往会坍缩至近乎重复的帧，并牺牲对时间跨度较大的证据的覆盖。我们提出了一种问题自适应的贪婪帧选择方法，在固定帧预算下联合优化查询相关性和语义代表性。该方法构建了一个 1 FPS 的候选池（上限为 1000 帧）并进行精确的时间戳对齐，将候选帧嵌入到两个互补的空间中（用于计算问题相关性的 SigLIP 和用于计算语义相似性的 DINOv2），并通过贪婪地最大化模块化相关性项和设施选址覆盖项的加权和来选择帧。该目标函数是归一化的、单调的且子模的，从而提供了标准的 (1-1/e) 贪婪近似保证。为了兼顾与问题相关的相关性和覆盖度之间的权衡，我们引入了四种预设策略和一个轻量级的纯文本问题类型分类器，将每个查询路由至其表现最佳的预设。在 MLVU 上的实验表明，在不同的帧预算下，该方法相比均匀采样和近期强基准模型均有一致的准确率提升，且在预算紧张的情况下提升最为显著。

## 速览
**TLDR**：针对长视频理解中视觉语言模型受限于输入帧数的问题，本文提出一种自适应贪婪帧选择方法。该方法通过联合优化查询相关性和语义代表性，在固定帧预算下平衡关键瞬间捕捉与时间覆盖。通过引入子模函数最大化和基于问题类型的分类器，该方法在MLVU基准测试中显著优于均匀采样，尤其在低预算场景下表现出色。 \
**Motivation**：传统的均匀采样易丢失关键信息，而纯相关性驱动的选择常导致帧冗余且缺乏时间覆盖。 \
**Method**：提出一种贪婪选择算法，结合SigLIP计算相关性与DINOv2计算覆盖度，并利用文本分类器动态调整选择策略。 \
**Result**：在MLVU长视频问答基准上，该方法在各种帧预算下均优于均匀采样和现有基线，低预算下提升尤为显著。 \
**Conclusion**：自适应地平衡相关性与覆盖度是提升长视频理解效率和准确性的有效途径。

---

## Abstract
Large vision--language models (VLMs) are increasingly applied to long-video question answering, yet inference is often bottlenecked by the number of input frames and resulting visual tokens. Naive sparse sampling can miss decisive moments, while purely relevance-driven selection frequently collapses onto near-duplicate frames and sacrifices coverage of temporally distant evidence. We propose a question-adaptive greedy frame selection method that jointly optimizes query relevance and semantic representativeness under a fixed frame budget. Our approach constructs a 1~FPS candidate pool (capped at 1000) with exact timestamp alignment, embeds candidates in two complementary spaces (SigLIP for question relevance and DINOv2 for semantic similarity), and selects frames by greedily maximizing a weighted sum of a modular relevance term and a facility-location coverage term. This objective is normalized, monotone, and submodular, yielding a standard (1-1/e) greedy approximation guarantee. To account for question-dependent trade-offs between relevance and coverage, we introduce four preset strategies and a lightweight text-only question-type classifier that routes each query to its best-performing preset. Experiments on MLVU show consistent accuracy gains over uniform sampling and a strong recent baseline across frame budgets, with the largest improvements under tight budgets.

---

## 论文详细总结（自动生成）

### 论文总结：面向长视频理解的自适应贪婪帧选择

#### 1. 核心问题与研究动机
*   **核心问题**：长视频理解（LVU）在视觉语言模型（VLM）推理中面临计算预算和上下文窗口的限制。
*   **研究动机**：
    *   **采样困境**：朴素的均匀采样容易遗漏关键瞬间；而仅基于相关性的检索往往会导致“冗余坍缩”（选出大量相似帧）或“覆盖坍缩”（忽视了时间跨度较远的证据）。
    *   **问题差异性**：不同类型的问题对帧的需求不同。例如，总结类问题需要广泛的语义覆盖，而细节类问题则更依赖于高相关性的特定片段。

#### 2. 方法论
*   **核心思想**：提出一种自适应贪婪帧选择方法，在固定帧预算下，通过子模函数最大化（Submodular Maximization）同时优化查询相关性（Relevance）和语义代表性（Coverage）。
*   **关键技术细节**：
    *   **候选池构建**：构建 1 FPS 的候选帧池（上限 1000 帧），确保时间戳精确对齐。
    *   **双重嵌入空间**：使用 **SigLIP** 提取视觉-文本嵌入以计算查询相关性；使用 **DINOv2** 提取视觉嵌入以衡量帧间的语义相似度。
    *   **目标函数**：定义 $F(S) = \alpha R(S) + \beta C(S)$。其中 $R(S)$ 是模块化相关性项，$C(S)$ 是基于设施选址（Facility-Location）的覆盖项。
    *   **贪婪算法**：由于目标函数具有单调性和子模性，采用贪婪算法进行求解，可获得 $(1 - 1/e)$ 的近似最优保证。
    *   **自适应路由**：训练一个轻量级纯文本分类器，根据问题文本预测其类型，并从四种预设策略（相关性优先、覆盖优先等）中选择最优的权重配置（$\alpha, \beta$）。

#### 3. 实验设计
*   **数据集**：主要使用 **MLVU**（多任务长视频理解）基准测试，该数据集包含 9 类官方任务分类（如剧情问答、计数、排序、异常识别等）。
*   **对比方法（Baselines）**：
    *   **均匀采样（Uniform Sampling）**：最常用的基准。
    *   **AKS (Adaptive Keyframe Sampling)**：CVPR 2025 的强力基准方法。
*   **评估指标**：在不同帧预算（K 值）下的平均准确率。

#### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **算法效率**：作者指出该贪婪算法的时间复杂度为 $O(KN^2)$（K 为预算，N 为候选帧数），在 GPU 上高度可向量化，对于 $N \le 1000$ 的场景推理延迟极低。
*   **分类器训练**：问题类型分类器非常轻量，仅需训练 10 个 Epoch 即可收敛。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了从极低预算（5 帧）到高预算（100+ 帧）下的性能曲线。
    *   针对 MLVU 的 9 个子类别分别进行了消融实验和策略搜索。
    *   验证了问题分类器的准确率（达 97.7%）及其对最终性能的影响。
*   **充分性评价**：实验在 MLVU 这一综合性长视频基准上表现充分，涵盖了多种任务类型。但目前**缺乏在其他主流基准**（如 Video-MME, LongVideoBench）上的验证，作者将其列为未来工作。

#### 6. 主要结论与发现
*   **性能领先**：该方法在所有帧预算下均优于均匀采样和 AKS，尤其在**低预算（5-20 帧）**时提升最显著。
*   **自适应的必要性**：实验证明没有单一的策略（$\alpha, \beta$ 配置）能胜任所有问题类型，通过问题类型进行策略路由能显著提升整体表现。
*   **冗余控制**：引入 DINOv2 覆盖项能有效防止采样点在时间轴上过度聚集，保证了长视频证据的完整性。

#### 7. 优点
*   **理论完备**：利用子模性理论保证了贪婪解的质量。
*   **即插即用**：作为一种训练无关（Training-free）的帧选择模块（除极小的文本分类器外），易于集成到现有的长视频 VLM 流程中。
*   **高效平衡**：成功解决了相关性与多样性之间的权衡问题，提高了 Token 的利用效率。

#### 8. 不足与局限
*   **基准覆盖有限**：目前仅在 MLVU 一个数据集上进行了深入分析，跨数据集的泛化性有待验证。
*   **候选池上限**：1000 帧的上限对于极长视频（如数小时）可能仍显不足，可能遗漏极细粒度的瞬间。
*   **策略离散化**：目前仅使用了四种预设策略，未来可探索更连续、更精细的权重自适应调整机制。

（完）
