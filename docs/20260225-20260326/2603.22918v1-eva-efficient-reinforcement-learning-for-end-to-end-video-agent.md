---
title: "EVA: Efficient Reinforcement Learning for End-to-End Video Agent"
title_zh: EVA：面向端到端视频智能体的高效强化学习
authors: "Yaolun Zhang, Ruohui Wang, Jiahao Wang, Yepeng Tang, Xuanyu Zheng, Haonan Duan, Hao Lu, Hanming Deng, Lewei Lu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22918v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于长视频智能体推理的高效强化学习
tldr: 针对多模态大模型在长视频理解中存在的冗余和效率问题，本文提出了EVA框架。EVA是一个端到端的视频智能体，采用“先规划后感知”的策略，通过迭代的总结-规划-行动-反思机制自主决定观察内容。研究团队设计了包含SFT、KTO和GRPO的三阶段强化学习训练流程，并构建了高质量数据集。实验表明，EVA在六个基准测试中显著优于现有模型，实现了更高效、精准的查询驱动视频理解。
selection_source: fresh_fetch
motivation: 现有的视频理解模型多为被动识别或依赖手动工作流，难以高效处理长视频中的冗余信息和复杂时序依赖。
method: 提出一种基于“总结-规划-行动-反思”闭环的端到端智能体框架，并结合SFT、KTO和GRPO三阶段学习流程进行优化。
result: "在六个视频理解基准测试中，EVA比通用MLLM基线提升了6-12%，比之前的自适应智能体方法提升了1-3%。"
conclusion: EVA证明了通过强化学习训练的端到端智能体能够实现更高效的查询驱动视频理解，为长视频处理提供了新范式。
---

## 摘要
使用多模态大语言模型（MLLMs）进行视频理解仍然具有挑战性，因为视频包含长标记序列，其中存在广泛的时间依赖性和冗余帧。现有方法通常将 MLLMs 视为被动识别器，在没有自适应推理的情况下处理整个视频或均匀采样的帧。最近的基于智能体的方法引入了外部工具，但仍依赖于手动设计的工作流和感知优先策略，导致在长视频上效率低下。我们提出了 EVA，这是一个面向端到端视频智能体的高效强化学习框架，它通过迭代的“总结-计划-行动-反思”推理实现了“先计划后感知”。EVA 自主决定看什么、何时看以及如何看，实现了查询驱动且高效的视频理解。为了训练此类智能体，我们设计了一个简单而有效的三阶段学习流水线——包括监督微调（SFT）、Kahneman-Tversky 优化（KTO）和广义奖励策略优化（GRPO）——它桥接了监督模仿学习和强化学习。我们进一步为每个阶段构建了高质量的数据集，支持稳定且可重复的训练。我们在六个视频理解基准测试上评估了 EVA，展示了其全面的能力。与现有基准相比，EVA 在通用 MLLM 基准上实现了 6-12% 的显著提升，并比之前的自适应智能体方法进一步提高了 1-3%。我们的代码和模型可在 https://github.com/wangruohui/EfficientVideoAgent 获取。

## 速览
**TLDR**：针对多模态大模型在长视频理解中存在的冗余和效率问题，本文提出了EVA框架。EVA是一个端到端的视频智能体，采用“先规划后感知”的策略，通过迭代的总结-规划-行动-反思机制自主决定观察内容。研究设计了包含SFT、KTO和GRPO的三阶段强化学习训练流程，显著提升了视频理解的效率和准确性，在多个基准测试中表现优于现有模型。 \
**Motivation**：现有的多模态大模型在处理长视频时往往是被动识别或依赖手动设计的工作流，导致处理冗余信息时效率低下且缺乏灵活性。 \
**Method**：提出了EVA框架，通过迭代的推理机制实现自主视频观测，并采用SFT、KTO和GRPO三阶段流水线进行端到端强化学习训练。 \
**Result**：在六个视频理解基准测试中，EVA比通用MLLM基准提升了6-12%，比之前的自适应智能体方法提升了1-3%。 \
**Conclusion**：EVA证明了通过强化学习训练的端到端智能体能够实现更高效、更精准的查询驱动型视频理解，为长视频处理提供了新范式。

---

## Abstract
Video understanding with multimodal large language models (MLLMs) remains challenging due to the long token sequences of videos, which contain extensive temporal dependencies and redundant frames. Existing approaches typically treat MLLMs as passive recognizers, processing entire videos or uniformly sampled frames without adaptive reasoning. Recent agent-based methods introduce external tools, yet still depend on manually designed workflows and perception-first strategies, resulting in inefficiency on long videos. We present EVA, an Efficient Reinforcement Learning framework for End-to-End Video Agent, which enables planning-before-perception through iterative summary-plan-action-reflection reasoning. EVA autonomously decides what to watch, when to watch, and how to watch, achieving query-driven and efficient video understanding. To train such agents, we design a simple yet effective three-stage learning pipeline - comprising supervised fine-tuning (SFT), Kahneman-Tversky Optimization (KTO), and Generalized Reward Policy Optimization (GRPO) - that bridges supervised imitation and reinforcement learning. We further construct high-quality datasets for each stage, supporting stable and reproducible training. We evaluate EVA on six video understanding benchmarks, demonstrating its comprehensive capabilities. Compared with existing baselines, EVA achieves a substantial improvement of 6-12% over general MLLM baselines and a further 1-3% gain over prior adaptive agent methods. Our code and model are available at https://github.com/wangruohui/EfficientVideoAgent.

---

## 论文详细总结（自动生成）

### 论文总结：EVA - 面向端到端视频智能体的高效强化学习

#### 1. 论文的核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）在视频理解中面临长序列、高冗余和复杂时序依赖的挑战。
*   **核心问题**：现有方法大多将 MLLM 视为“被动识别器”（处理全量或均匀采样的帧），或者依赖手动设计的刚性工具流（感知优先），导致在处理长视频时效率低下，且容易受到无关视觉信息的干扰。
*   **研究动机**：探索如何让 MLLM 进化为主动、自适应的智能体，使其能够根据查询（Query）自主决定“看什么、何时看、怎么看”。

#### 2. 论文提出的方法论
EVA 提出了 **“先规划后感知”（Planning-before-perception）** 的范式，核心思想是通过迭代的推理闭环实现高效视频理解。
*   **核心架构**：采用“总结（Summary）- 规划（Plan）- 行动（Action）- 反思（Reflection）”的迭代循环。智能体首先根据文本查询进行推理，再调用工具获取视觉证据。
*   **关键技术细节**：
    *   **灵活工具设计**：提供 `frame_select` 工具，允许智能体动态调整四个参数：起始时间、结束时间、采样帧数（nframes）和空间缩放比例（resize）。
    *   **三阶段训练流水线**：
        1.  **SFT（冷启动）**：在 10k 高质量合成数据上微调，使模型掌握工具调用格式、交错图文推理及基本规划能力。
        2.  **KTO（策略纠正）**：利用 Kahneman-Tversky 优化，通过成功与失败的轨迹数据（11k）进行偏好对齐，减少模型在证据不足时盲目猜测的行为。
        3.  **GRPO（在线强化学习）**：基于广义奖励策略优化（Generalized Reward Policy Optimization），利用准确率奖励（MCQ 采用自验证奖励，开放式采用 ROUGE 分数）和格式奖励，实现端到端的策略演进。

#### 3. 实验设计
*   **数据集**：构建了 EVA-SFT、EVA-KTO 和 EVA-RL（包含 9.6k 开放式问答和 1.1k 多选题）三个高质量数据集。
*   **Benchmark（基准测试）**：
    *   **采样困境评估**：LSDBench。
    *   **长视频理解**：LongVideoBench, MLVU, VideoMME, LVBench。
    *   **视频推理**：Video-Holmes（零样本评估）。
    *   **幻觉评估**：ELV-Halluc。
*   **对比方法**：包括闭源模型（GPT-4o, Gemini 2.0 Flash）、静态采样模型（Qwen2.5-VL, InternVL2.5, Video-R1）以及自适应智能体方法（FrameThinker, VideoAgent）。

#### 4. 资源与算力
*   **硬件设备**：使用了 **32 台 NVIDIA H100 GPU**。
*   **训练参数**：
    *   SFT 阶段：2 epoch，学习率 2e-6。
    *   KTO 阶段：学习率 2e-6，beta 0.1。
    *   GRPO 阶段：1 epoch，Batch Size 64，每个样本生成 8 个 rollout，学习率 1e-6。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流视频理解榜单和 1 个幻觉评估榜单上进行了全面测试。
*   **消融实验**：详细对比了 SFT、KTO、GRPO 各阶段的增量贡献，并分析了 GRPO 中数据组成（仅多选、仅开放式、混合数据）对性能的影响。
*   **客观性与公平性**：实验不仅对比了准确率，还详细统计了视觉 Token 消耗量和推理轮数。结果显示 EVA 在使用极少视觉 Token（如 LSDBench 仅用 6.2k tokens）的情况下达到了领先水平，证明了其高效性。

#### 6. 主要结论与发现
*   **性能提升**：EVA 在通用 MLLM 基准上提升了 6-12%，在自适应智能体基准上进一步提升了 1-3%。
*   **策略演进**：通过强化学习，模型从“格式模仿者”转变为“战略探索者”。GRPO 训练后的模型倾向于增加推理轮数，但每轮分配更精准的视觉预算。
*   **效率优势**：相比均匀采样，EVA 能通过“先规划”有效避开视频中的噪声片段，显著降低计算开销。

#### 7. 优点
*   **范式领先**：打破了“感知优先”的传统，通过“规划优先”减少了视觉误导和 Token 浪费。
*   **训练稳健**：创新的三阶段训练（SFT+KTO+GRPO）解决了多轮对话智能体在强化学习中难以收敛和容易“奖励作弊”（Reward Hacking）的问题。
*   **高度自适应**：支持空间分辨率和时间粒度的双重调节，比现有仅支持时间范围选择的智能体更灵活。

#### 8. 不足与局限
*   **推理延迟风险**：虽然节省了视觉 Token，但多轮迭代增加了文本生成的推理次数，可能在实时性要求极高的场景下存在延迟。
*   **工具局限性**：目前仅依赖单一的帧选择工具，对于需要复杂外部知识库或更高级视觉工具（如目标检测、OCR 专用工具）的集成尚在初期。
*   **分布外鲁棒性**：对于与训练数据分布差异极大的极端长视频或噪声查询，智能体的反思机制是否能始终保持准确仍需进一步验证。

（完）
