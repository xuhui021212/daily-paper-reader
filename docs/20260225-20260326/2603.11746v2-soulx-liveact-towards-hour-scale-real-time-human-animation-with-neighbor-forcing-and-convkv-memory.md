---
title: "SoulX-LiveAct: Towards Hour-Scale Real-Time Human Animation with Neighbor Forcing and ConvKV Memory"
title_zh: SoulX-LiveAct：通过邻域强迫与 ConvKV 存储实现小时级实时人物动画
authors: "Dingcheng Zhen, Xu Zheng, Ruixin Zhang, Zhiqi Jiang, Yichao Yan, Ming Tao, Shunshun Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11746v2"
tags: ["query:lvu"]
score: 9.0
evidence: 利用记忆增强网络实现小时级实时视频生成
tldr: 本研究针对自回归扩散模型在长视频生成中的不稳定性与内存限制，提出了SoulX-LiveAct框架。通过引入Neighbor Forcing策略，在相同噪声条件下传播相邻帧，解决了学习信号不一致的问题；同时利用ConvKV内存机制压缩历史表示，实现了固定内存消耗的无限时长生成。该方法支持小时级实时人物动画，在保持高画质和口型准确度的同时，显著提升了推理效率。
selection_source: fresh_fetch
motivation: 现有的自回归扩散模型在长视频生成中面临扩散状态不匹配导致的训练不稳定以及历史缓存无限增长带来的推理瓶颈。
method: 提出了Neighbor Forcing策略以对齐扩散步骤的学习信号，并结合ConvKV内存机制将因果注意力中的键值对压缩为固定长度的结构化表示。
result: 该方法实现了20 FPS的实时流式推理，支持小时级视频生成，并在口型同步、动画质量和情感表达方面达到SOTA水平。
conclusion: SoulX-LiveAct通过优化自回归机制与内存管理，为高效、超长时长的实时人物动画生成提供了可扩展的解决方案。
---

## 摘要
自回归（AR）扩散模型通过结合扩散建模与因果推理，为视频合成等序列生成任务提供了一个极具前景的框架。尽管它们支持流式生成，但现有的 AR 扩散方法在高效扩展方面仍面临挑战。在本文中，我们识别了小时级实时人物动画中的两个关键挑战。首先，大多数强迫策略传播的是扩散状态不匹配的样本级表示，导致学习信号不一致且收敛不稳定。其次，历史表示呈无界增长且缺乏结构，阻碍了缓存状态的有效重用，并严重限制了推理效率。为了应对这些挑战，我们提出了邻域强迫（Neighbor Forcing），这是一种扩散步骤一致的 AR 公式，它在相同的噪声条件下将时间上相邻的帧作为潜邻域进行传播。该设计提供了分布对齐且稳定的学习信号，同时在整个 AR 链中保留了漂移特性。在此基础上，我们引入了一种结构化的 ConvKV 存储机制，将因果注意力中的键（keys）和值（values）压缩为固定长度的表示，从而实现常数内存推理和真正的无限视频生成，而无需依赖短期运动帧内存。广泛的实验表明，与现有的 AR 扩散方法相比，我们的方法显著提高了训练收敛性、小时级生成质量和推理效率。在数值上，LiveAct 实现了小时级实时人物动画，并支持在仅两块 NVIDIA H100 或 H200 GPU 上进行 20 FPS 的实时流式推理。定量结果表明，我们的方法在唇形同步准确度、人物动画质量和情感表达力方面达到了最先进的性能，且推理成本最低。

## Abstract
Autoregressive (AR) diffusion models offer a promising framework for sequential generation tasks such as video synthesis by combining diffusion modeling with causal inference. Although they support streaming generation, existing AR diffusion methods struggle to scale efficiently. In this paper, we identify two key challenges in hour-scale real-time human animation. First, most forcing strategies propagate sample-level representations with mismatched diffusion states, causing inconsistent learning signals and unstable convergence. Second, historical representations grow unbounded and lack structure, preventing effective reuse of cached states and severely limiting inference efficiency. To address these challenges, we propose Neighbor Forcing, a diffusion-step-consistent AR formulation that propagates temporally adjacent frames as latent neighbors under the same noise condition. This design provides a distribution-aligned and stable learning signal while preserving drifting throughout the AR chain. Building upon this, we introduce a structured ConvKV memory mechanism that compresses the keys and values in causal attention into a fixed-length representation, enabling constant-memory inference and truly infinite video generation without relying on short-term motion-frame memory. Extensive experiments demonstrate that our approach significantly improves training convergence, hour-scale generation quality, and inference efficiency compared to existing AR diffusion methods. Numerically, LiveAct enables hour-scale real-time human animation and supports 20 FPS real-time streaming inference on as few as two NVIDIA H100 or H200 GPUs. Quantitative results demonstrate that our method attains state-of-the-art performance in lip-sync accuracy, human animation quality, and emotional expressiveness, with the lowest inference cost.

---

## 论文详细总结（自动生成）

以下是对论文《SoulX-LiveAct: Towards Hour-Scale Real-Time Human Animation with Neighbor Forcing and ConvKV Memory》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自回归（AR）扩散模型在生成小时级超长视频时面临两大瓶颈：
    1.  **训练不稳定**：现有的强迫策略（如 Diffusion Forcing）在传播历史信息时，扩散状态（噪声水平）往往不匹配，导致学习信号不一致，难以实现稳定收敛。
    2.  **内存爆炸**：推理时的键值缓存（KV Cache）随帧数线性增长，导致长视频生成时内存溢出，且缺乏有效的历史信息重用机制。
*   **研究背景**：实时人物动画（如数字人直播）需要极高的口型同步性、长时间的身份一致性以及极低的推理延迟。

### 2. 方法论：核心思想与关键技术
*   **Neighbor Forcing（邻域强迫）**：
    *   **核心思想**：在相同的扩散步（Diffusion Step）下，将时间相邻帧作为潜邻域进行传播。
    *   **技术细节**：不同于以往传播“干净样本”或“异构噪声样本”的做法，该方法确保目标帧与参考帧处于同一噪声水平。这在数学上满足局部平滑性，提供了分布对齐的稳定学习信号，有效缓解了 AR 链中的漂移问题。
*   **ConvKV Memory（卷积键值存储）**：
    *   **核心思想**：引入一个轻量级的插件式压缩模块，将无限增长的 KV 缓存压缩为固定长度。
    *   **技术细节**：利用 1D 卷积对长期的 Key 和 Value 进行压缩（压缩比 $\lambda=5$），并配合 RoPE（旋转位置编码）重置操作。该机制将历史上下文总结为固定长度的表示，实现了常数级内存消耗。
*   **系统架构**：基于 DiT（Diffusion Transformer）和 Flow Matching 框架，集成音频交叉注意力机制，并采用两阶段训练（邻域强迫训练 + 存储压缩与步骤蒸馏）。

### 3. 实验设计
*   **数据集**：
    *   **HDTF**：专注于面部动态和口型同步的头像数据集。
    *   **EMTD**：包含全身动作和复杂情感表达的数据集。
*   **Benchmark 与指标**：
    *   **口型同步**：Sync-C、Sync-D。
    *   **生成质量**：FID、FVD。
    *   **综合评估**：VBench（时间质量、图像质量）、VBench-2.0（人体保真度）。
*   **对比方法**：OmniAvatar、InfiniteTalk、Live-Avatar 等当前最先进的实时/长视频生成模型。

### 4. 资源与算力
*   **硬件需求**：支持在 **2 块 NVIDIA H100 或 H200 GPU** 上实现 20 FPS 的实时流式推理。
*   **计算效率**：在 512×512 分辨率下，每帧仅需 **27.2 TFLOPs**，显著低于 Live-Avatar（39.1 TFLOPs）和双向扩散基准（50.2 TFLOPs）。
*   **训练细节**：第一阶段使用 300 小时多模态配对数据；第二阶段进行 400 步的蒸馏训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了定量对比、定性视觉展示、长视频一致性分析以及多项消融实验。
*   **消融实验**：针对 ConvKV 存储机制、情感/动作编辑模块、内存块大小（Block Size）等关键超参数进行了详细测试。
*   **充分性评价**：实验设计较为全面，不仅验证了短期的口型准确度，还重点测试了长达数十分钟甚至一小时的身份一致性，证明了模型在极端长序列下的鲁棒性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：在口型同步准确度、视频分布相似度（FID/FVD）和人体解剖学保真度上均达到 SOTA 水平。
*   **长效稳定性**：ConvKV 存储有效解决了身份漂移问题，能够保持饰品（如戒指）、服装纹理在长时间生成中的稳定性。
*   **高效推理**：通过 KV 重用和压缩机制，推理延迟大幅降低，ConvKV 模块仅增加 1.9% 的计算开销，却换取了无限时长生成的能力。

### 7. 优点
*   **理论支撑强**：在附录中提供了关于局部平滑性和步骤一致性邻域的理论证明。
*   **工程落地性高**：实现了 20 FPS 的实时性能，且对硬件要求相对较低（仅需 2 块 GPU），适合商业化直播场景。
*   **创新的存储管理**：ConvKV 机制在不增加复杂编码器的情况下，巧妙利用预训练 DiT 特征实现了高效压缩。

### 8. 不足与局限
*   **模型依赖**：性能在很大程度上依赖于预训练的非自回归底座（如 Wan2.1），若底座本身存在偏见，可能会遗传至 AR 模型。
*   **应用限制**：目前主要聚焦于人物动画（Human Animation），对于背景极其复杂或剧烈相机运动的通用视频生成场景，其压缩机制的有效性尚待进一步验证。
*   **训练成本**：虽然推理高效，但两阶段训练（尤其是涉及大规模多模态数据和蒸馏）仍需要可观的算力投入。

（完）
