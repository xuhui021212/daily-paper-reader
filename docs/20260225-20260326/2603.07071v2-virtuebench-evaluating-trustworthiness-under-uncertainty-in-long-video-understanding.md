---
title: "VirtueBench: Evaluating Trustworthiness under Uncertainty in Long Video Understanding"
title_zh: VirtueBench：评估长视频理解中不确定性下的可信度
authors: "Xueqing Yu, Bohan Li, Yan Li, Zhenheng Yang"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07071v2"
tags: ["query:lvu"]
score: 10.0
evidence: 长视频理解中评估可信度的基准测试
tldr: 针对长视频理解中视觉语言模型（VLM）因关键帧缺失而盲目猜测的问题，本文提出了 VirtueBench 基准。该基准通过设置多层级帧采样，区分视频内容是否足以回答问题，旨在评估模型在不确定性下的诚实度。研究发现现有模型在拒绝回答方面的表现差异巨大，且普遍存在过度猜测的倾向，这为开发更可靠、更具信任度的多模态模型提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的长视频理解评估体系由于忽略了输入帧缺失导致的不确定性，往往会误导模型进行盲目猜测而非诚实拒绝。
method: 引入 VirtueBench 基准，通过构建不同采样密度的视频输入，并提供区分“可回答”与“不可回答”的标准答案来评估模型的信任度。
result: "对 25 个主流模型的评估显示，拒绝准确率从 0% 到 70% 不等，且多数模型在缺乏明确指令时拒绝意愿显著下降。"
conclusion: 研究强调了在多模态理解中建立信任度评估的重要性，呼吁开发能够识别自身局限性并诚实响应的可靠 VLM。
---

## 摘要
最近的视觉语言模型（VLM）在多模态理解任务中取得了显著进展，但它们在长视频理解方面的评估仍然不可靠。由于帧输入受限，回答问题所需的关键帧可能会在模型的输入中缺失。然而，在面临此类不确定性时，如实拒绝回答的模型会被标记为错误，而那些进行猜测的模型可能会巧合地给出正确答案，从而获得具有欺骗性的高准确率，这导致了误导性的评估结果，并鼓励模型进行猜测而非诚实响应。为了解决这一问题，我们推出了 VirtueBench，这是一个专门设计用于评估不确定性下模型可信度的基准测试。VirtueBench 为每个视频构建了多个帧采样级别，并提供了区分可回答和不可回答情况的地面真值。对 25 个开源和商业 VLM 的评估揭示了不同模型系列之间截然不同的拒绝行为，拒绝准确率从表现最好模型的 70% 以上到表现最差模型的接近 0% 不等。此外，当提示词没有明确要求时，大多数模型的拒绝率会大幅下降。这些发现强调了开发用于多模态理解的可信 VLM 的必要性，并应由强调可靠性和可信度的基准测试及排行榜进行引导。

## 速览
**TLDR**：针对长视频理解中因采样限制导致关键帧缺失、模型倾向于盲目猜测而非诚实拒绝的问题，本文提出了VirtueBench基准。该基准通过构建多层级采样并区分可答与不可答情况，评估了25个主流视觉语言模型的可靠性。研究发现模型在不确定性下的拒绝表现差异巨大，且高度依赖提示词引导，强调了开发可信模型的重要性。 \
**Motivation**：现有的长视频理解评估忽略了采样不足导致的不确定性，导致模型倾向于通过猜测获取高分而非诚实回答。 \
**Method**：提出了VirtueBench基准，通过设置不同的帧采样水平并标注可答性，来评估模型在信息缺失时是否能正确拒绝回答。 \
**Result**：对25个模型的评估显示，拒绝准确率差异极大（0%到70%不等），且大多数模型在没有明确提示时拒绝意愿显著下降。 \
**Conclusion**：评估结果揭示了当前模型在处理不确定性时的不可靠性，呼吁建立更注重可靠性和诚实性的多模态评估体系。

---

## Abstract
Recent Vision-Language Models (VLMs) have made remarkable progress in multimodal understanding tasks, yet their evaluation on long video understanding remains unreliable. Due to limited frame inputs, key frames necessary for answering the question may be missing from the model's input. However, models that truthfully refuse to answer under such uncertainty are marked as incorrect, while those that guess may coincidentally produce the correct answer and thus obtain deceptively higher accuracy, leading to misleading evaluation results and encouraging models to guess rather than respond honestly. To address this issue, we introduce VirtueBench, a benchmark explicitly designed to assess model trustworthiness under uncertainty. VirtueBench constructs multiple frame-sampling levels for each video and provides ground truths that distinguish between answerable and unanswerable cases. Evaluations on 25 open-source and commercial VLMs reveal distinct refusal behaviors across different model families, with refusal accuracy ranging from over 70% in the best models to nearly 0% in the worst. Moreover, most models exhibit a substantial drop in refusal when the prompt does not explicitly require them to do so. These findings highlight the need for developing trustworthy VLMs for multimodal understanding, guided by benchmarks and leaderboards that emphasize reliability and trustworthiness.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《VirtueBench: Evaluating Trustworthiness under Uncertainty in Long Video Understanding》** 的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前视觉语言模型（VLM）在处理长视频时，受限于计算资源，通常只能采样有限的帧（如 64 或 256 帧）。这导致回答问题所需的关键信息可能在采样中丢失。
*   **研究动机**：现有的长视频评估基准（如 Video-MME）采用二元对错评价体系，忽略了“信息不足”的情况。这产生了一个负面激励：**诚实拒绝回答的模型被判错，而盲目猜测且碰巧猜对的模型获得高分**。这种评估偏差误导了模型优化方向，鼓励了不诚实（幻觉）行为。
*   **整体含义**：本文旨在建立一个衡量模型在“不确定性”下是否具备**可信度（Trustworthiness）**的基准，即模型是否能在证据不足时诚实地拒绝回答。

### 2. 论文提出的方法论
*   **核心思想**：构建一个多层级采样、动态标注可答性的基准测试集 **VirtueBench**。
*   **关键技术细节**：
    *   **多层级采样（Multi-level Sampling）**：针对同一段视频，分别提取 64、128、256、512、1024 帧五个层级的剪辑。
    *   **动态地面真值（Dynamic Ground Truth）**：为每个采样层级独立标注答案。若当前层级的帧不足以支持回答，则标准答案设为“视频未提供足够信息”。
    *   **两阶段评估流程**：
        1.  **拒绝检测（Refusal Detection）**：使用 GPT-4o 判断模型是否表达了“无法回答”。
        2.  **正确性评估（Correctness Evaluation）**：若标准答案是“不可答”，则模型必须拒绝才算对；若标准答案是具体内容，则评估语义一致性。
    *   **诚实指令提示词**：在 Prompt 中明确要求模型“仅基于提供的帧回答，若信息不足请勿猜测”。

### 3. 实验设计
*   **数据集来源**：整合了 MLVU、LVBench、Video-MME 等 6 个主流长视频基准，将其多选题转化为开放式问题。
*   **数据规模**：包含 1,328 个高质量标注实例，涵盖感知（如动作检测、属性识别）和推理（如时间推理、因果关系）两大类。
*   **对比方法**：评估了 **25 个** 主流 VLM，包括：
    *   **开源模型**：Qwen2.5-VL (7B/32B/72B)、Qwen3-VL、InternVL3.5、LLaVA-Video、Mimo-VL 等。
    *   **闭源模型**：GPT-4o、GPT-5 (o1-like)、Gemini-2.5-Flash。
*   **Benchmark 维度**：分为感知准确率、推理准确率、非拒绝子集准确率和**拒绝准确率（Refusal Accuracy）**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练模型所使用的具体 GPU 型号、数量或训练时长。
*   **推理配置**：提到在推理评估时，将最大像素限制为 512×512，以平衡视觉细节与模型处理能力。由于本文侧重于 Benchmark 的构建与评估，而非模型训练，因此未提供大规模训练算力数据。

### 5. 实验数量与充分性
*   **实验组数**：对 25 个模型在 5 个不同采样层级上进行了全面测试，并进行了 **Prompt 消融实验**（对比有无“诚实指令”对模型行为的影响）。
*   **充分性与客观性**：
    *   **充分性**：涵盖了从 7B 到 72B 不同参数规模的模型，以及经过强化学习（RL）和具备思考模式（Thinking）的模型，实验覆盖面广。
    *   **客观性**：通过人工双重校验 + 时间戳证据标注，确保了 Ground Truth 的准确性；引入 LLM-as-a-judge 减少了规则匹配的死板。

### 6. 论文的主要结论与发现
*   **性能鸿沟**：Gemini-2.5-Flash 和 Qwen3-VL-32B 是目前最可靠的模型。
*   **拒绝行为差异**：不同家族模型表现迥异。InternVL 和 LLaVA-Video 系列几乎从不拒绝（拒绝准确率接近 0%），而 Qwen 和闭源模型表现较好。
*   **规模效应与强化学习**：模型参数越大，拒绝行为越趋于诚实；经过 RL 训练或具备“思考（Thinking）”模式的模型，识别信息缺失的能力显著增强。
*   **提示词敏感性**：大多数模型并不“天生诚实”。一旦去掉 Prompt 中的诚实指令，拒绝准确率会下降近一半，说明模型默认倾向于扮演“答题者”而非“观察者”。

### 7. 优点
*   **视角独特**：首次系统性地探讨了长视频理解中“采样受限导致的不确定性”问题，纠正了现有评估体系对“瞎猜”的奖励。
*   **设计科学**：多层级采样设计能够清晰展示模型性能随信息量增加的变化曲线，比单一全量视频评估更具诊断价值。
*   **标注严谨**：不仅标注答案，还要求标注支持证据的时间戳，确保了基准测试的权威性。

### 8. 不足与局限
*   **样本规模**：1,328 条数据虽然质量高，但相比于一些数十万条规模的基准，覆盖的视频场景多样性仍有提升空间。
*   **裁判偏见风险**：依赖 GPT-4o 作为评估裁判，可能存在对特定表达风格的偏好或对复杂推理判断的误差。
*   **应用限制**：目前主要关注“信息缺失”导致的拒绝，尚未深入区分“视觉识别错误”与“逻辑推理失败”导致的不确定性。

（完）
