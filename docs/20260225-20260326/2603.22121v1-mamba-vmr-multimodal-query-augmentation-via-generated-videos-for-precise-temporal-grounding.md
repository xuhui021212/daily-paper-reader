---
title: "Mamba-VMR: Multimodal Query Augmentation via Generated Videos for Precise Temporal Grounding"
title_zh: Mamba-VMR：通过生成视频进行多模态查询增强以实现精确时序定位
authors: "Yunzhuo Sun, Xinyue Liu, Yanyang Li, Nanding Wu, Yifang Xu, Linlin Zong, Xianchao Zhang, Wenxin Liang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22121v1"
tags: ["query:lvu"]
score: 8.0
evidence: 使用Mamba在长序列中进行时间定位
tldr: 针对视频时刻检索（VMR）中长序列定位不准和计算开销大的问题，本文提出Mamba-VMR框架。该框架通过LLM匹配字幕并利用文生视频模型生成辅助视频作为时序先验，增强查询信息。随后利用多模态控制的Mamba网络高效融合生成先验与长视频序列，在减少计算开销的同时显著提升了检索精度。
selection_source: fresh_fetch
motivation: 传统VMR方法难以捕捉长视频中的隐式运动信息，且基于Transformer的架构在处理长序列时计算成本过高。
method: 采用两阶段框架，先通过LLM匹配字幕并生成辅助视频作为时序先验，再利用多模态控制的Mamba网络进行高效特征融合与噪声过滤。
result: 在TVR基准测试上的实验表明，该方法在降低计算开销的同时，显著提升了长序列视频定位的召回率。
conclusion: Mamba-VMR通过引入生成式视频先验和高效的Mamba架构，为多模态视频时刻检索提供了一种模型无关且高性能的解决方案。
---

## 摘要
文本驱动的视频时刻检索 (VMR) 仍然具有挑战性，因为在未修剪视频中捕获隐藏时序动态的能力有限，导致在长序列中的定位不精确。传统方法依赖于自然语言查询 (NLQ) 或静态图像增强，忽略了动作序列，并且在基于 Transformer 的架构中面临高昂的计算成本。现有方法未能有效整合字幕上下文和生成的时序先验，因此我们提出了一种用于增强时序定位的新型两阶段框架。在第一阶段，大语言模型 (LLM) 引导的字幕匹配从视频字幕中识别相关的文本线索，并将其与查询融合，通过文本生成视频模型生成辅助短视频，从而捕获隐式动作信息作为时序先验。在第二阶段，增强后的查询通过多模态控制的 Mamba 网络进行处理，通过视频引导的门控扩展了文本控制的选择，以便在过滤噪声的同时，高效融合生成的先验和长序列。我们的框架与基础检索模型无关，广泛适用于多模态 VMR。在 TVR 基准测试上的实验评估表明，该方法相比现有最先进方法有显著改进，包括降低了计算开销并在长序列定位中获得了更高的召回率。

## 速览
**TLDR**：针对视频时刻检索（VMR）中长序列处理效率低及缺乏动态运动先验的问题，本文提出Mamba-VMR框架。该框架首先利用大模型匹配字幕并生成辅助短视频作为增强查询，随后通过多模态受控Mamba网络高效融合时序先验并过滤噪声。实验证明该方法在TVR数据集上显著提升了检索精度，同时降低了计算开销。 \
**Motivation**：传统VMR方法难以捕捉长视频中的隐式运动信息，且Transformer架构在处理长序列时面临巨大的计算开销。 \
**Method**：采用两阶段框架，先通过LLM引导字幕匹配并生成辅助视频增强查询，再利用多模态受控Mamba网络进行高效特征融合。 \
**Result**：在TVR基准测试中，该方法在长序列定位精度和召回率上均优于现有SOTA模型，并显著降低了计算开销。 \
**Conclusion**：Mamba-VMR通过生成式视频增强和高效的Mamba架构，为多模态视频时刻检索提供了一种通用且高效的解决方案。

---

## Abstract
Text-driven video moment retrieval (VMR) remains challenging due to limited capture of hidden temporal dynamics in untrimmed videos, leading to imprecise grounding in long sequences. Traditional methods rely on natural language queries (NLQs) or static image augmentations, overlooking motion sequences and suffering from high computational costs in Transformer-based architectures. Existing approaches fail to integrate subtitle contexts and generated temporal priors effectively, we therefore propose a novel two-stage framework for enhanced temporal grounding. In the first stage, LLM-guided subtitle matching identifies relevant textual cues from video subtitles, fused with the query to generate auxiliary short videos via text-to-video models, capturing implicit motion information as temporal priors. In the second stage, augmented queries are processed through a multi-modal controlled Mamba network, extending text-controlled selection with video-guided gating for efficient fusion of generated priors and long sequences while filtering noise. Our framework is agnostic to base retrieval models and widely applicable for multimodal VMR. Experimental evaluations on the TVR benchmark demonstrate significant improvements over state-of-the-art methods, including reduced computational overhead and higher recall in long-sequence grounding.

---

## 论文详细总结（自动生成）

### 论文总结：Mamba-VMR —— 通过生成视频增强多模态查询以实现精确时序定位

#### 1. 论文的核心问题与整体含义
论文针对**视频时刻检索（Video Moment Retrieval, VMR）**中的两个核心挑战提出解决方案：
*   **语义表达不足**：传统的自然语言查询（NLQ）或静态图像增强难以捕捉复杂的动态动作序列（如“走进房间并递给某人咖啡”涉及的连续动作），导致在处理多动词查询时定位不准。
*   **计算效率瓶颈**：长视频序列在传统的 Transformer 架构下具有二次方计算复杂度，难以高效处理未修剪的长视频。
**研究动机**：利用大语言模型（LLM）的推理能力和文生视频（T2V）模型的生成能力，为查询提供动态的“时序先验”，并结合 Mamba 架构实现高效的长序列建模。

#### 2. 论文提出的方法论
论文提出了一个名为 **Mamba-VMR** 的两阶段框架：
*   **第一阶段：LLM 引导的增强与视频生成**
    *   **查询分解与字幕匹配**：使用 Llama-3.1 将复杂查询分解为基于动词的子事件，并从视频字幕中检索相关的对话线索。
    *   **时序先验生成**：将原始查询、分解后的子事件和匹配的字幕融合，输入文生视频模型（如 CogVideoX）生成一段约 6 秒的辅助短视频。这段视频包含了查询所描述的隐式运动逻辑，作为检索的“动态先验”。
*   **第二阶段：多模态受控 Mamba 网络**
    *   **特征编码**：使用 CLIP 编码器分别提取原始视频、生成视频和文本查询的嵌入向量。
    *   **视频引导门控（Video-guided Gating）**：在 Mamba 的选择性状态空间模型（SSM）基础上，设计了一个门控机制。该机制利用生成的视频先验和文本查询共同控制状态转移，从而在长序列中聚焦于与动作相关的片段并过滤噪声。
    *   **线性复杂度建模**：利用 Mamba 的线性时间复杂度特性，高效处理长视频序列。
*   **损失函数**：结合了边界分类损失（$L_{bound}$）、片段相关性损失（$L_{rel}$）和对比学习损失（$L_{cont}$），以确保生成的先验与真实时刻在特征空间对齐。

#### 3. 实验设计
*   **数据集**：
    *   **TVR**：包含 21.8K 视频和 108K 查询，具有丰富的字幕信息，是主要测试基准。
    *   **ActivityNet Captions**：约 20K 视频和 100K 查询，用于验证在无字幕场景下的泛化能力。
    *   **Charades-STA**：用于定性分析。
*   **Benchmark 与对比方法**：对比了 HERO、CONQUER、PREM、SgLFT 以及最新的多模态增强方法 ICQ。
*   **评价指标**：Recall@K (K=1, 5, 10, 100) 在 IoU 阈值为 0.5 和 0.7 下的表现。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **软件框架**：PyTorch 2.0。
*   **训练细节**：Batch size 为 32，训练 20 个 epoch，使用 AdamW 优化器，学习率为 1e-4。
*   **推理效率**：生成的视频采用离线预计算方式，平均每个查询-视频对的推理延迟约为 1.2 秒。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证，具有较高的充分性和客观性：
*   **主实验**：在两个主流数据集上均取得了 SOTA 结果。
*   **消融实验**：分别验证了 LLM 查询处理、时序先验生成、视频门控机制以及不同损失函数的贡献。
*   **深度分析**：
    *   **动词数量分析**：专门分析了 1-Verb 到 Multi-Verb 查询的表现，证明了该方法在复杂动作序列上的显著优势。
    *   **内存消耗对比**：对比了 Transformer、Longformer 与 Mamba-VMR 的显存占用，证明了其线性扩展性。
*   **对比公平性**：使用了相同的预训练特征（CLIP）和标准的评估协议。

#### 6. 主要结论与发现
*   **动态先验至关重要**：生成的短视频比静态图像（如 ICQ 方法）能提供更丰富的运动线索，显著提升了多动词查询的检索精度（在 TVR 上 R@1/IoU=0.5 提升了约 1.07%）。
*   **Mamba 架构的高效性**：在处理超过 700 帧的长视频时，Transformer 会出现显存溢出（OOM），而 Mamba-VMR 保持了稳定的线性增长。
*   **LLM 的辅助作用**：通过分解查询和匹配字幕，有效缓解了原始查询的歧义性。

#### 7. 优点
*   **创新性地引入生成式视频先验**：突破了以往仅靠文本或静态图像增强的局限，填补了运动信息缺失的空白。
*   **架构高效**：成功将 Mamba 应用于多模态 VMR 任务，解决了长视频处理的效率难题。
*   **模型无关性**：该增强框架理论上可以适配多种基础检索模型。

#### 8. 不足与局限
*   **生成质量依赖**：检索效果高度依赖于文生视频模型（CogVideoX）的生成质量，若生成视频与实际内容偏差过大，可能引入噪声。
*   **计算流程复杂**：虽然推理时视频是预生成的，但整个流水线涉及 LLM 推理、视频生成和 Mamba 检索，系统集成复杂度较高。
*   **模态覆盖**：目前主要关注视觉和文本，尚未充分利用视频中的音频（Audio）信息，这在某些对话密集的场景中可能也是关键线索。

（完）
