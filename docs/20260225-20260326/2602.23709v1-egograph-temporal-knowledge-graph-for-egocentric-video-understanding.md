---
title: "EgoGraph: Temporal Knowledge Graph for Egocentric Video Understanding"
title_zh: EgoGraph：用于第一视角视频理解的时序知识图谱
authors: "Shitong Sun, Ke Han, Yukai Huang, Weitong Cai, Jifei Song"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23709v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于超长第一视角视频理解的时间知识图谱
tldr: 针对超长第一视角视频理解中局部处理和时序建模受限的问题，本文提出 EgoGraph。这是一个无需训练的动态知识图谱构建框架，通过统一的模式提取人、物、地、事等核心实体及其交互，并利用时序关系建模策略捕捉跨实体的长期依赖，实现了跨多天的稳定记忆积累。在 EgoLifeQA 等基准测试中达到 SOTA，为超长视频理解提供了新范式。
selection_source: fresh_fetch
motivation: 现有的视频理解方法在处理跨越数天的超长第一视角视频时，由于缺乏有效的长期时序建模和跨实体依赖推理，难以应对复杂的语义理解任务。
method: 提出一种无需训练的动态知识图谱框架，通过定义的自我中心模式提取实体及其属性，并利用时序关系建模策略构建长期记忆。
result: 在 EgoLifeQA 和 EgoR1-bench 基准测试中，EgoGraph 在长视频问答任务上取得了最先进的性能表现。
conclusion: EgoGraph 证明了结构化知识图谱在处理超长视频流中的有效性，为长时程自我中心视频理解开辟了新的研究方向。
---

## 摘要
跨越数天的超长第一视角视频为视频理解带来了重大挑战。现有方法仍依赖于碎片化的局部处理和有限的时序建模，限制了它们在如此长的序列上进行推理的能力。为了解决这些局限性，我们引入了 EgoGraph，这是一个无需训练且动态的知识图谱构建框架，它显式地编码了第一视角视频流中的长期、跨实体依赖关系。EgoGraph 采用了一种新颖的第一视角模式，统一了对人、物体、地点和事件等核心实体的提取与抽象，并对其属性和交互进行结构化推理，从而产生比传统基于片段的视频模型更丰富、更连贯的语义表示。至关重要的是，我们开发了一种时序关系建模策略，能够捕捉跨实体的时序依赖关系，并在数天内积累稳定的长期记忆，从而实现复杂的时序推理。在 EgoLifeQA 和 EgoR1-bench 基准测试上的广泛实验表明，EgoGraph 在长期视频问答方面达到了最先进的性能，验证了其作为超长第一视角视频理解新范式的有效性。

## 速览
**TLDR**：针对超长第一视角视频理解中局部处理和时序建模受限的问题，本文提出EgoGraph框架。它是一种无需训练的动态知识图谱构建方法，通过统一的模式提取人、物、地、事等核心实体及其交互，并利用时序关系建模策略捕捉跨实体的长期依赖，实现了跨多日的稳定长效记忆。在EgoLifeQA等基准测试中达到SOTA性能，为超长视频理解提供了新范式。 \
**Motivation**：现有的视频理解方法依赖碎片化的局部处理，难以对跨越数日的超长第一视角视频进行有效的长期时序推理。 \
**Method**：提出一种无需训练的动态知识图谱框架，通过统一模式提取实体及其属性，并利用时序关系建模策略构建跨实体的长期记忆。 \
**Result**：在EgoLifeQA和EgoR1-bench基准测试中，EgoGraph在长时视频问答任务上取得了当前最先进的性能表现。 \
**Conclusion**：EgoGraph证明了结构化知识图谱在处理超长视频流中的有效性，为长时第一视角视频理解开辟了新的研究方向。

---

## Abstract
Ultra-long egocentric videos spanning multiple days present significant challenges for video understanding. Existing approaches still rely on fragmented local processing and limited temporal modeling, restricting their ability to reason over such extended sequences. To address these limitations, we introduce EgoGraph, a training-free and dynamic knowledge-graph construction framework that explicitly encodes long-term, cross-entity dependencies in egocentric video streams. EgoGraph employs a novel egocentric schema that unifies the extraction and abstraction of core entities, such as people, objects, locations, and events, and structurally reasons about their attributes and interactions, yielding a significantly richer and more coherent semantic representation than traditional clip-based video models. Crucially, we develop a temporal relational modeling strategy that captures temporal dependencies across entities and accumulates stable long-term memory over multiple days, enabling complex temporal reasoning. Extensive experiments on the EgoLifeQA and EgoR1-bench benchmarks demonstrate that EgoGraph achieves state-of-the-art performance on long-term video question answering, validating its effectiveness as a new paradigm for ultra-long egocentric video understanding.

---

## 论文详细总结（自动生成）

### 论文总结：EgoGraph —— 用于第一视角视频理解的时序知识图谱

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何有效理解跨越数天（Ultra-long）的超长第一视角（Egocentric）视频。
*   **研究背景**：随着可穿戴设备的普及，记录数日生活的第一视角视频数据激增。
*   **研究动机**：
    *   **现有方法局限**：传统方法（如 EgoGPT）通常将长视频切片并进行分级文本摘要（如每小时、每天）。这种方式会导致语义碎片化，难以捕捉跨越长时间跨度的实体间复杂依赖关系，且随着视频长度增加，非结构化的文本空间会导致检索效率低下。
    *   **需求**：需要一种能够像人类记忆一样，既能保持实体一致性，又能显式建模时间关联的结构化表示方式。

#### 2. 论文提出的方法论
*   **核心思想**：提出 **EgoGraph**，一个无需训练（Training-free）的动态时序知识图谱框架。它将视频流转化为由实体、属性、关系和时间戳组成的结构化图，通过图的演化来模拟长期记忆。
*   **关键技术细节**：
    *   **自我中心模式（Egocentric Schema）**：预定义了四类核心实体：**人物（Person）**、**地点（Location）**、**物体（Object）**和**事件（Event）**。每个实体包含特定的属性（如人物的习惯、物体的颜色等），确保提取信息的语义一致性。
    *   **时序建模与推理**：每个节点和边都关联一个时间戳列表 $T = \{t_1, t_2, \dots, t_n\}$。系统通过 LLM 推理实体的演化（如推断某人的习惯或物体位置的变化）。
    *   **图合并与更新**：基于文本嵌入相似度持续合并冗余节点，并将新观察到的属性和时间信息追加到现有节点上，防止图规模无限制膨胀。
    *   **QA 流程**：
        1.  **时序过滤**：根据查询时间 $t_q$ 提取子图 $G_{\le t_q}$，防止“时间泄露”。
        2.  **检索增强**：通过向量搜索检索最相关的节点、边和原始文本块。
        3.  **LLM 推理**：利用结构化 Prompt 引导 LLM 处理相对时间词（如“昨天”、“两小时前”），并要求引用时间戳以供验证。

#### 3. 实验设计
*   **数据集/场景**：
    *   **EgoLifeQA**：包含 6 个人在共享房屋中生活 7 天的视频，提供 500 个公共 QA 对。
    *   **EgoR1-Bench**：专注于超长第一视角视频推理的基准测试，包含 300 个 QA 对。
*   **对比方法**：
    *   **多模态大模型（MLLMs）**：Gemini-1.5-Pro, GPT-4o, LLaVA-OV。
    *   **专门的长视频模型**：EgoGPT（基于分级摘要）。
    *   **图检索方法**：LightRAG（通用文本图检索）、VideoRAG（针对讲座视频的静态图）。

#### 4. 资源与算力
*   **视觉前端**：使用 **InternVL-3.5-8B-Instruct** 作为视觉描述符（Captioner），将 2 分钟的视频片段转化为文本。
*   **图构建与 QA 推理**：使用 **GPT-4o** 作为核心推理引擎。
*   **嵌入模型**：使用 OpenAI 的 `text-embedding-3-small`。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或处理总时长。由于该框架是“无需训练”的，其开销主要集中在视频描述提取（推理）和调用 LLM API 的费用上。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流超长第一视角视频基准上进行了零样本（Zero-shot）评估。
*   **消融实验**：
    *   对比了“分级摘要 vs. 图结构”。
    *   验证了“自我中心模式”、“时间过滤器”和“时序推理”各组件的增益。
    *   分析了不同检索组件（节点、边、文本块）的贡献。
*   **鲁棒性分析**：测试了模型在面对 1 到 7 天不断增长的上下文时的性能稳定性，以及对查询时间与事件发生时间之间“时间跨度”的敏感性。
*   **评价**：实验设计较为全面，涵盖了定量指标、定性对比和长程压力测试，能够客观反映图结构在处理长序列时的优势。

#### 6. 论文的主要结论与发现
*   **性能领先**：EgoGraph 在 EgoLifeQA 上达到 45.8% 的准确率，显著超过了 EgoGPT（36.0%）和 Gemini-1.5-Pro（36.9%）。
*   **长程稳定性**：随着视频时长从 1 天增加到 7 天，传统纯文本方法的性能会因上下文窗口限制而崩溃，而 EgoGraph 的性能下降非常轻微，表现出极强的扩展性。
*   **结构化优势**：在涉及“实体跟踪”、“时序依赖”和“习惯推断”的任务中，图结构比分级摘要方法平均提升了 29.3% 的表现。

#### 7. 优点
*   **结构化记忆**：通过显式的实体-关系建模，解决了长视频理解中的信息碎片化问题。
*   **时序感知**：将时间作为图的一等公民，支持复杂的时间逻辑查询（如“最后一次看到某物是在什么时候”）。
*   **高效且可扩展**：通过节点合并机制保持图的简洁，避免了随着视频变长而导致的计算量爆炸。
*   **无需训练**：作为一种插件式框架，可以灵活适配不同的视觉前端和 LLM 后端。

#### 8. 不足与局限
*   **依赖视觉描述质量**：如果前端 Captioner（如 InternVL）遗漏了关键细节，图谱构建将出现不可逆的信息缺失。
*   **LLM 成本**：构建大规模图谱需要频繁调用高性能 LLM（如 GPT-4o），在处理海量视频时可能存在较高的 API 成本。
*   **实体对齐风险**：节点合并主要依赖文本相似度，在处理名称相似但实体不同的情况（或反之）时可能存在对齐错误。
*   **实时性挑战**：目前的流程涉及多个步骤（描述-提取-合并-检索-推理），可能难以满足极低延迟的实时交互需求。

（完）
