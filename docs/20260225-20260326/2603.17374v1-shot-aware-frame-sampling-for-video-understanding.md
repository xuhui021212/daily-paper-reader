---
title: Shot-Aware Frame Sampling for Video Understanding
title_zh: 视频理解中的镜头感知帧采样
authors: "Mengyu Zhao, Di Fu, Yongyu Xie, Jiaxing Zhang, Zhigang Yuan, Shirin Jalali, Yong Cao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17374v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于高效长视频理解的分镜感知帧采样
tldr: 针对长视频理解中密集输入成本高且易丢失关键瞬时事件的问题，本文提出了InfoShot，一种无需训练的任务无关型分镜感知采样器。该方法通过将视频划分为语义一致的分镜，并基于信息论目标为每个分镜选取代表性内容和异常变化的两类关键帧，从而在有限帧数下兼顾全局上下文与局部关键细节。此外，研究者还推出了SynFlash基准以评估短时异常检测能力。实验证明InfoShot在异常捕获和视频问答任务上均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的视频采样方法在极低采样率下难以平衡视频的全局覆盖度与对简短但关键事件的捕捉。
method: InfoShot将视频划分为分镜，并利用信息论目标从每个分镜中提取代表主内容和捕捉异常变化的两个互补关键帧。
result: 在提出的SynFlash基准及现有视频问答任务中，InfoShot在受限帧数下显著提升了异常命中率和预测准确性。
conclusion: InfoShot通过分镜感知的双帧采样策略，在不增加计算负担的前提下，有效提升了视觉语言模型对长视频中关键细节的感知能力。
---

## 摘要
视频帧采样对于使用视觉语言模型（VLM）进行高效的长视频理解至关重要，因为密集输入成本高昂且通常会超出上下文限制。然而，当只能保留少量帧时，现有的采样器往往难以在广泛的视频覆盖范围与简短但关键的事件之间取得平衡，这可能导致不可靠的下游预测。为了解决这一问题，我们提出了 InfoShot，这是一种用于长视频理解的任务无关、镜头感知的帧采样器。InfoShot 首先将视频划分为语义一致的镜头，然后从每个镜头中选择两个互补的关键帧：一个用于代表主要内容，另一个用于捕捉镜头内异常的变化。该设计受信息论目标的指导，鼓励采样集合保留关于镜头结构和稀疏镜头内偏差的高信息量。通过这种方式，它在无需任何重新训练的情况下，提高了保留整体视频上下文和简短决策关键时刻的概率。为了更好地评估此类短暂事件，我们进一步引入了 SynFlash，这是一个具有可控亚秒级异常模式和帧级真值（ground truth）的合成基准，并且我们还在现有的异常数据集和通用视频理解任务上评估了 InfoShot。实验表明，在帧数限制下，InfoShot 提高了异常命中率和下游视频问答（Video-QA）的准确率，同时在标准视频理解基准测试中达到或超过了强基准模型的表现。

## 速览
**TLDR**：针对长视频理解中密集采样成本高且易丢失关键短时事件的问题，本文提出了一种无需重训练的镜头感知采样框架 InfoShot。该方法通过将视频划分为语义一致的镜头，并基于信息论目标从每个镜头中提取代表性内容和异常变化的两类关键帧，从而在有限帧数下兼顾全局上下文与局部关键细节。此外，研究者还推出了 SynFlash 基准以评估短时异常检测能力。实验证明 InfoShot 在异常命中率和视频问答准确率上均优于现有基准。 \
**Motivation**：现有的视频采样方法在极低采样率下难以平衡视频的全局覆盖度与对短促但关键事件的捕捉。 \
**Method**：InfoShot 将视频划分为语义镜头，并利用信息论目标从每镜头中选取代表主内容和捕捉内部分异的两类互补关键帧。 \
**Result**：在 SynFlash 及通用视频理解基准上，InfoShot 显著提升了异常事件的命中率和下游视频问答任务的准确性。 \
**Conclusion**：InfoShot 是一种高效、通用的视频采样策略，能在不增加计算负担的前提下显著增强视觉语言模型对长视频中关键细节的感知能力。

---

## Abstract
Video frame sampling is essential for efficient long-video understanding with Vision-Language Models (VLMs), since dense inputs are costly and often exceed context limits. Yet when only a small number of frames can be retained, existing samplers often fail to balance broad video coverage with brief but critical events, which can lead to unreliable downstream predictions. To address this issue, we present InfoShot, a task-agnostic, shot-aware frame sampler for long-video understanding. InfoShot first partitions a video into semantically consistent shots, and then selects two complementary keyframes from each shot: one to represent the main content and one to capture unusual within-shot changes. This design is guided by an information-theoretic objective that encourages the sampled set to retain high information about both shot structure and sparse within-shot deviations. In this way, it improves the chance of preserving both overall video context and short decision-critical moments without requiring any retraining. To better evaluate such short-lived events, we further introduce SynFlash, a synthetic benchmark with controllable sub-second anomaly patterns and frame-level ground truth, and we also evaluate InfoShot on existing anomaly datasets and general video understanding tasks. Experiments show that InfoShot improves anomaly hit rate and downstream Video-QA accuracy under frame number constraints, while matching or outperforming strong baselines on standard video understanding benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Shot-Aware Frame Sampling for Video Understanding》的结构化深入总结：

### 1. 论文的核心问题与整体含义
在长视频理解任务中，视觉语言模型（VLM）受限于计算成本和上下文窗口，通常需要对视频进行帧采样。然而，现有的采样方法（如均匀采样或基于特定任务的采样）存在**“覆盖范围”与“关键细节”失衡**的问题：
*   **冗余性**：均匀采样会在长而静态的镜头中浪费大量配额。
*   **易丢失性**：亚秒级的关键事件（如异常行为、闪现内容）极易在稀疏采样中被漏掉，导致 VLM 给出错误判断。
论文提出了 **InfoShot**，一种任务无关、无需重训练的镜头感知采样框架，旨在有限的帧预算下，同时保留视频的全局结构和稀疏的瞬时关键信息。

### 2. 方法论：核心思想与技术细节
InfoShot 的设计基于**信息论目标**，即最大化采样帧集合对视频潜在结构（镜头变化和镜头内偏差）的信息量。其流程分为四个步骤：
*   **特征提取**：使用预训练模型（如 ResNet-50 或 SigLIP）提取视频帧的深度特征，将视频表示为特征序列。
*   **贪婪镜头分割**：采用基于窗口的**广义似然比检验（GLRT）**。通过比较相邻窗口内的内聚性与跨窗口的相似性，计算边界强度得分，并贪婪地将视频划分为语义一致的镜头。
*   **镜头内双关键帧选择**：这是该方法的核心。针对每个镜头，选择两个互补的关键帧：
    1.  **通用帧（Common Frame）**：选择在特征空间中最具代表性、最稳定的帧，用于捕捉镜头的主要语义。
    2.  **独特帧（Unique Frame）**：选择与镜头均值偏差最大、局部波动最高的帧，用于捕捉镜头内可能存在的瞬时异常或变化。
*   **VLM 推理**：将选定的帧序列输入下游 VLM（如 Qwen2.5-VL）进行视频问答或推理。

### 3. 实验设计
*   **数据集与场景**：
    *   **SynFlash（新基准）**：论文自研的合成基准，包含 8,000 个视频，涵盖四种亚秒级异常模式：伪装（Camouflage）、高饱和度闪烁（High-Saturation）、幕帘过渡（Curtain）和画中画（PiP）。
    *   **HIVAU-70k**：包含 UCF-Crime 和 XD-Violence 的真实世界异常检测数据集。
    *   **Video-MME**：通用的长视频理解基准测试。
*   **对比方法（Baselines）**：
    *   **Uniform**：均匀时间采样。
    *   **VSUMM**：基于聚类的无监督摘要方法。
    *   **TransNet V2**：基于深度学习的镜头边界检测采样。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 训练时长，因为 **InfoShot 本身是无需训练（Training-free）的即插即用模块**。
*   **推理细节**：特征提取使用了 ResNet-50，下游 VLM 使用了 Qwen2.5-VL。实验中提到在处理长视频时，为了效率会以 600 帧为块进行相似度计算。
*   **实际应用**：该方法已部署在 TikTok 的视频审核流水线中，处理每周数十亿级的短视频。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 SynFlash 上进行了 16,000 次查询测试。
    *   在 HIVAU-70k 上评估了事件覆盖率（ER）和完整覆盖率（CR）。
    *   在 Video-MME 上测试了短、中长度视频的通用理解能力。
*   **消融实验**：针对特征提取器（颜色直方图 vs. 语义特征）、采样率（0.1 到 0.5 fps）的鲁棒性进行了详细对比。
*   **评价**：实验设计较为充分且客观。通过合成数据（精确控制变量）与真实数据（验证泛化性）相结合，证明了方法在极端低帧率下的优越性。

### 6. 主要结论与发现
*   **异常捕获能力显著提升**：在 0.5 fps 的低采样率下，InfoShot 在 SynFlash 上的异常帧召回率达到 84.7%，远超 Uniform（11.8%）和 TransNet（46.3%）。
*   **下游任务准确率更高**：由于捕获了关键证据，VLM 在异常检索（AR）和时间排序（TO）任务上的准确率大幅提升。
*   **通用性强**：在 Video-MME 等通用基准上，InfoShot 保持了与均匀采样相当的性能，证明其在关注细节的同时并未破坏全局语义理解。
*   **特征质量至关重要**：语义特征（如 ResNet/SigLIP）在识别复杂异常（如画中画或伪装插入）时远优于低级颜色特征。

### 7. 优点
*   **任务无关与即插即用**：不需要针对特定任务进行微调，适用于任何 VLM 架构。
*   **双帧策略的互补性**：通过“通用+独特”的组合，有效解决了长视频中“静态背景冗余”与“瞬时事件丢失”的矛盾。
*   **理论支撑**：基于信息论的分解公式为采样策略提供了坚实的数学逻辑。
*   **实用性高**：在工业级大规模流水线中的成功部署证明了其计算效率和鲁棒性。

### 8. 不足与局限
*   **固定配额限制**：每个镜头固定分配两帧，在镜头长度差异极大或事件密度极高的情况下，可能不是最优分配。
*   **缺乏运动信息**：目前主要依赖静态特征相似度，未显式引入光流或运动矢量来捕捉快速动作。
*   **偏见风险**：虽然提升了召回率，但在审核场景下，若未配合精准的阈值校准，可能会增加误报率（False Positives）。

（完）
