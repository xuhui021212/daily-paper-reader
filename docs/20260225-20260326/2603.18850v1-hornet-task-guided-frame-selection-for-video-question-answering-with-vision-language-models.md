---
title: "HORNet: Task-Guided Frame Selection for Video Question Answering with Vision-Language Models"
title_zh: HORNet：面向视觉语言模型视频问答的任务导向型帧选择
authors: "Xiangyu Bai, Bishoy Galoaa, Sarah Ostadabbas"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18850v1"
tags: ["query:lvu"]
score: 8.0
evidence: 任务引导的帧选择用于高效视频问答
tldr: "针对视频问答中传统均匀采样无法针对任务优化的问题，本文提出HORNet。这是一种轻量级（<1M参数）的帧选择策略，利用GRPO强化学习算法训练，旨在为冻结的视觉语言模型（VLM）筛选关键帧。该方法在减少高达99%输入帧和93%处理时间的同时，显著提升了在MSVD-QA和NExT-QA等基准上的表现，证明了优化“看什么”比单纯优化“生成什么”更具效率和泛化性。"
selection_source: fresh_fetch
motivation: 现有的视频问答系统多采用启发式或均匀采样，无法根据具体问题动态选择对回答质量最关键的视频帧。
method: 提出一种基于GRPO强化学习算法训练的轻量级策略网络HORNet，通过解耦视觉筛选与推理过程，为冻结的VLM选择最优帧子集。
result: "在六个基准测试中，HORNet在大幅降低计算开销（减少93%处理时间）的同时，实现了显著的性能提升（如NExT-QA提升7.3分）。"
conclusion: 优化VLM的输入帧选择是提高视频问答效率和准确性的实用方案，且该策略具有极强的跨模型迁移能力和泛化性。
---

## 摘要
使用视觉语言模型（VLM）进行视频问答（VQA）在很大程度上取决于从输入视频中选择哪些帧，但大多数系统依赖于无法针对下游回答质量进行优化的均匀采样或启发式采样。我们提出了 HORNet，这是一种轻量级的帧选择策略，采用群体相对策略优化（GRPO）进行训练，以学习冻结的 VLM 正确回答问题所需的帧。HORNet 的可训练参数量不足 100 万，可减少高达 99% 的输入帧和 93% 的 VLM 处理时间，同时提升了短视频基准测试的回答质量（MSVD-QA 的 F1 分数提升 1.7%），并在时间推理任务上表现出色（在 NExT-QA 上比均匀采样高出 7.3 分）。我们将此形式化为“选择任意帧”（Select Any Frames, SAF）任务，该任务将视觉输入筛选与 VLM 推理相解耦，并证明了经 GRPO 训练的选择策略比监督学习和 PPO 方案具有更好的分布外泛化能力。HORNet 的策略无需重新训练即可在不同的 VLM 回答器之间迁移，在与更强大的模型配对时可获得 8.5% 的额外相对增益。通过在涵盖 341,877 个问答对和 114.2 小时视频的六个基准测试上的评估，我们的结果表明，优化 VLM “看到的内容”是优化其“生成内容”的一种实用且互补的替代方案，同时还能提高效率。

## 速览
**TLDR**：针对视频问答中传统均匀采样无法针对任务优化的问题，本文提出HORNet，一种利用GRPO强化学习训练的轻量级帧选择策略。该模型参数量不足1M，通过为冻结的视觉语言模型（VLM）筛选关键帧，在减少高达99%输入帧和93%处理时间的同时，显著提升了短视频及时间推理任务的准确率，并展现出优异的跨模型迁移能力。 \
**Motivation**：现有的视频问答系统多依赖启发式采样，无法根据具体问题动态优化输入帧，导致计算资源浪费且难以捕捉关键视觉信息。 \
**Method**：提出HORNet策略，利用GRPO算法训练轻量级选择器，在“选择任意帧”任务中学习为下游冻结的VLM挑选最相关的视频帧。 \
**Result**：在六个基准测试中，HORNet在大幅降低计算开销的同时，使MSVD-QA的F1值提升1.7%，并在NExT-QA时间推理任务上比均匀采样高出7.3分。 \
**Conclusion**：优化VLM的输入视觉内容是提升视频问答效率与性能的实用方案，且该帧选择策略具有极强的泛化性和跨模型兼容性。

---

## Abstract
Video question answering (VQA) with vision-language models (VLMs) depends critically on which frames are selected from the input video, yet most systems rely on uniform or heuristic sampling that cannot be optimized for downstream answering quality. We introduce \textbf{HORNet}, a lightweight frame selection policy trained with Group Relative Policy Optimization (GRPO) to learn which frames a frozen VLM needs to answer questions correctly. With fewer than 1M trainable parameters, HORNet reduces input frames by up to 99\% and VLM processing time by up to 93\%, while improving answer quality on short-form benchmarks (+1.7\% F1 on MSVD-QA) and achieving strong performance on temporal reasoning tasks (+7.3 points over uniform sampling on NExT-QA). We formalize this as Select Any Frames (SAF), a task that decouples visual input curation from VLM reasoning, and show that GRPO-trained selection generalizes better out-of-distribution than supervised and PPO alternatives. HORNet's policy further transfers across VLM answerers without retraining, yielding an additional 8.5\% relative gain when paired with a stronger model. Evaluated across six benchmarks spanning 341,877 QA pairs and 114.2 hours of video, our results demonstrate that optimizing \emph{what} a VLM sees is a practical and complementary alternative to optimizing what it generates while improving efficiency. Code is available at https://github.com/ostadabbas/HORNet.

---

## 论文详细总结（自动生成）

以下是对论文《HORNet: Task-Guided Frame Selection for Video Question Answering with Vision-Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频问答（VideoQA）系统通常依赖于**均匀采样**或**启发式采样**来选择视频帧。这种方式无法针对下游的回答质量进行优化，导致输入中包含大量冗余噪声，且可能丢失关键信息帧。
*   **研究动机**：
    *   **效率瓶颈**：处理长视频的所有帧或大量采样帧会带来巨大的计算开销和推理延迟。
    *   **性能鸿沟**：视觉语言模型（VLM）在图像任务上表现优异，但在视频任务（尤其是需要时间推理的任务）上表现较差，原因在于缺乏有效的视觉输入筛选机制。
    *   **小数据挑战**：在特定领域（如医疗、工业），标注数据稀缺，全量微调大模型成本极高且容易过拟合。

### 2. 方法论
HORNet 提出了 **“选择任意帧”（Select Any Frames, SAF）** 任务，将视觉输入筛选与 VLM 推理过程解耦。
*   **核心思想**：利用强化学习（GRPO）训练一个极其轻量级的策略网络，专门为冻结的 VLM 挑选“最有助于回答问题”的帧。
*   **关键技术细节**：
    1.  **视频编码器（Encoder）**：采用基于 **TimeSformer-Tiny** 的架构，通过分离的空间自注意力和时间自注意力提取每帧的时空特征。
    2.  **策略网络（MLP Policy）**：一个参数量小于 1M 的三层 MLP，输入帧特征，输出每帧被保留的概率 $p_t$。
    3.  **GRPO 训练算法**：
        *   **候选采样**：针对每个问题，策略网络生成 $K=8$ 个不同的帧子集候选。
        *   **奖励函数（Reward）**：将候选帧子集输入冻结的 VLM（如 Qwen3-VL），根据生成的答案与标准答案的相似度（Token F1 和 编辑相似度 EditSim 的加权）计算奖励。
        *   **群体相对优化**：不使用评论者网络（Critic），而是通过计算组内 $K$ 个候选的相对优势（均值和标准差归一化）来更新策略网络。
*   **算法流程**：输入视频 $\rightarrow$ 编码器提取特征 $\rightarrow$ 策略网络预测概率 $\rightarrow$ 采样帧子集 $\rightarrow$ 冻结 VLM 推理 $\rightarrow$ 根据回答质量反馈优化策略。

### 3. 实验设计
*   **数据集**：涵盖了 6 个主流基准测试，共计 341,877 个问答对和 114.2 小时视频。
    *   **短视频/开放式问答**：MSVD-QA, MSRVTT-QA。
    *   **长视频/时间推理/多选题**：NExT-QA (MCQ & Open-ended), VideoMME, ActivityNet-QA。
*   **对比方法（Baselines）**：
    *   **均匀采样（Uniform Sampling）**：大多数 VLM 的默认策略。
    *   **随机采样（Random Sampling）**。
    *   **其他训练策略**：监督微调（SFT）、近端策略优化（PPO）。
    *   **跨模型迁移**：对比了在不同规模 VLM（Qwen3-VL-2B, Qwen2.5-VL-3B）上的表现。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A100 40GB GPU** 上完成。
*   **训练规模**：在包含 223,646 个 QA 对的混合语料库上进行训练。
*   **参数量**：可训练参数量**小于 100 万（<1M）**，这使得该方法在单卡上即可高效运行。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个不同性质的数据集上进行了大规模测试，样本量充足。
*   **消融实验**：
    *   对比了不同的**训练目标**（SFT vs PPO vs GRPO），证明了 GRPO 在分布外（OOD）泛化上的优越性。
    *   对比了不同的**采样策略**，证明了在长视频中学习到的选择策略显著优于盲目采样。
    *   验证了**策略的可迁移性**，即在一个 VLM 上训练的 HORNet 可以直接用于另一个更强的 VLM。
*   **客观性**：实验涵盖了从短视频到长视频、从简单描述到复杂因果推理的多种场景，评估指标（F1-Lev, Accuracy, Runtime）全面，对比公平。

### 6. 主要结论与发现
*   **极致效率**：HORNet 可减少高达 **99%** 的输入帧和 **93%** 的 VLM 处理时间。
*   **性能提升**：在 NExT-QA 时间推理任务上，比均匀采样高出 **7.3 分**；在 MSVD-QA 上 F1 提升 **1.7%**。
*   **泛化能力**：GRPO 训练的策略比 SFT 和 PPO 具有更强的跨数据集泛化能力。
*   **即插即用**：帧选择策略具有跨模型兼容性，与更强的 VLM（如 Qwen2.5-VL-3B）结合时，性能可进一步提升 8.5%。

### 7. 优点
*   **参数极其高效**：不到 1M 的参数量，适合“小数据”和资源受限场景。
*   **解耦设计**：无需修改或微调庞大的下游 VLM，保持了基础模型的原始能力。
*   **任务导向**：通过强化学习直接优化“回答质量”，而非简单的视觉显著性，使筛选出的帧更具语义相关性。
*   **显著提速**：大幅降低了视频 VLM 的推理延迟，具有极高的工业应用价值。

### 8. 不足与局限
*   **超长视频挑战**：在小时级的视频（如 VideoMME）中，固定预算（如仅选 8 帧）会导致性能下降（-16.2%），说明对于极长视频，固定帧数可能不足以覆盖所有关键点。
*   **固定预算限制**：目前采用固定的帧选择数量，未来可能需要根据视频长度和问题复杂度动态调整采样预算。
*   **反馈闭环**：目前 VLM 是完全冻结的，如果能部分开放 VLM 的梯度反馈，可能会进一步提升协同优化效果。

（完）
