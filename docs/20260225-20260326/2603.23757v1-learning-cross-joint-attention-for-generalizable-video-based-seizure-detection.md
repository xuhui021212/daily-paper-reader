---
title: Learning Cross-Joint Attention for Generalizable Video-Based Seizure Detection
title_zh: 学习跨关节注意力以实现可泛化的基于视频的癫痫发作检测
authors: "Omar Zamzam, Takfarinas Medani, Chinmay Chinara, Richard Leahy"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23757v1"
tags: ["query:medvid"]
score: 9.0
evidence: 从长期临床视频中自动检测癫痫发作
tldr: 针对临床视频癫痫检测中模型难以泛化到新受试者的问题，本文提出一种以关节为中心的注意力模型。该方法通过提取关节中心片段来抑制背景干扰，并利用ViViT和跨关节注意力机制捕捉身体部位间的时空动态交互。实验证明，该方法在跨受试者检测任务中显著优于现有的CNN和图神经网络方法，有效提升了模型的泛化能力。
selection_source: fresh_fetch
motivation: 现有的视频癫痫检测方法因受背景偏差和受试者特定外观的影响，难以在未见过的受试者上实现良好的泛化。
method: 通过提取以关节为中心的视频片段来抑制背景，并利用ViViT和跨关节注意力机制建模身体部位间的时空交互模式。
result: 实验结果显示，该方法在跨受试者测试中一致优于现有的CNN、图神经网络和Transformer等先进模型。
conclusion: 聚焦于身体动力学而非外观特征的跨关节注意力机制，是实现高泛化性视频癫痫检测的有效途径。
---

## 摘要
从长期临床视频中进行自动癫痫发作检测可以显著减少人工复核时间，并实现实时监测。然而，由于背景偏差以及对受试者特定外观线索的依赖，现有的基于视频的方法通常难以泛化到未见过的受试者。我们提出了一种以关节为中心的注意力模型，该模型专门关注身体动力学，以提高跨受试者的泛化能力。对于每个视频片段，系统会检测身体关节并提取以关节为中心的剪辑，从而抑制背景上下文。这些以关节为中心的剪辑使用视频视觉 Transformer (ViViT) 进行标记化，并学习跨关节注意力来建模身体部位之间的时空交互，从而捕捉癫痫症状学特征性的协调运动模式。广泛的跨受试者实验表明，所提出的方法在未见过的受试者上始终优于最先进的基于 CNN、图（graph）和 Transformer 的方法。

## Abstract
Automated seizure detection from long-term clinical videos can substantially reduce manual review time and enable real-time monitoring. However, existing video-based methods often struggle to generalize to unseen subjects due to background bias and reliance on subject-specific appearance cues. We propose a joint-centric attention model that focuses exclusively on body dynamics to improve cross-subject generalization. For each video segment, body joints are detected and joint-centered clips are extracted, suppressing background context. These joint-centered clips are tokenized using a Video Vision Transformer (ViViT), and cross-joint attention is learned to model spatial and temporal interactions between body parts, capturing coordinated movement patterns characteristic of seizure semiology. Extensive cross-subject experiments show that the proposed method consistently outperforms state-of-the-art CNN-, graph-, and transformer-based approaches on unseen subjects.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“以关节为中心的跨关节注意力模型”（Joint-Centric Cross-Joint Attention Model），旨在解决临床视频癫痫检测中模型难以泛化到新受试者的问题。以下是对该论文的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现有的基于视频的癫痫检测方法在面对未见过的受试者（Cross-subject）时表现不佳。
*   **研究动机**：
    *   **背景偏差**：传统模型容易学习到病房背景、摄像头角度等无关特征。
    *   **外观依赖**：模型往往依赖受试者的具体外貌特征而非运动本质。
    *   **运动复杂性**：癫痫发作涉及复杂的全身协调运动，而现有的图卷积网络（GCN）通常使用固定的骨架连接，无法灵活捕捉不同发作类型下的动态关节关联。

### 2. 论文提出的方法论
核心思想是将视频分解为多个以关节为中心的局部动态片段，通过学习关节间的自适应注意力来识别癫痫。
*   **关节中心化表示**：
    1.  使用 **OpenPose** 检测 14 个关键关节。
    2.  在每个关节周围裁剪 $120 \times 120$ 的局部视频片段，从而彻底消除背景干扰。
*   **运动编码（ViViT）**：
    *   使用预训练的视频视觉 Transformer（**ViViT**）作为共享编码器，将每个关节的局部视频转化为一个高维运动特征向量（Motion Token）。
*   **位置与身份增强**：
    *   将关节在原图中的绝对坐标 $(x, y)$ 和关节类别身份（如“左肘”）进行线性投影，并加到运动特征上，以保留全局空间上下文。
*   **跨关节注意力机制（Cross-Joint Attention）**：
    *   利用多头自注意力（MHSA）处理所有关节的 Token。公式遵循标准注意力机制：$Attn(Q, K, V) = \text{softmax}(\frac{QK^T}{\sqrt{d}})V$。
    *   该机制允许模型根据当前的运动状态，动态地学习哪些关节的组合（如左手和右脚的同步抽搐）对判定癫痫最重要。
*   **参数高效微调（LoRA）**：
    *   为了防止在小规模临床数据集上过拟合，采用了 **LoRA** 技术对 ViViT 的最后两层进行微调，保持大部分预训练参数冻结。

### 3. 实验设计
*   **数据集**：使用公开的长期癫痫视频数据集（由 VSViG 论文引入），包含 14 名受试者的 33 段视频，涵盖局灶性发作和泛化性抽搐。
*   **实验协议**：采用严格的**按受试者划分（Subject-wise）**。9 人用于训练/验证，5 人用于测试，确保测试集完全是模型未见过的个体。
*   **基准模型（Benchmarks）**：
    *   **VSViG**：基于骨架的时空图神经网络。
    *   **Fine-tuned Hiera**：基于视频基础模型的全帧分类器。
    *   **3D-CNN**：分别基于原始视频和光流（Optical Flow）的传统三维卷积网络。
*   **评价指标**：AUROC、AUPRC（主要指标，应对类别不平衡）、Accuracy、F1-score 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **效率提示**：作者提到了使用 LoRA 进行参数高效微调，并指出该方法在计算开销极小的情况下提升了性能，暗示了该模型对算力的需求相对优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 5 次独立运行并取平均值（带标准差），增强了结果的可信度。
    *   对比了 5 种不同类型的先进基准方法。
    *   包含了消融性质的对比（如冻结特征 vs. LoRA 微调）。
*   **充分性评价**：实验设计较为客观，特别是坚持“受试者独立”的划分方式，这在医疗 AI 领域是衡量泛化能力最公平的标准。但受限于公开数据集规模，总受试者人数（14人）仍然较少。

### 6. 主要结论与发现
*   **性能领先**：提出的方法在所有指标上均显著优于 SOTA。其 AUROC 达到 0.923，远高于 VSViG (0.732) 和 Hiera (0.868)。
*   **动力学优于外观**：实验证明，通过裁剪关节片段来抑制背景，比直接处理全帧视频更能捕捉癫痫的本质特征。
*   **自适应关联的重要性**：相比于固定结构的图网络，注意力机制能更好地处理不同患者之间变异巨大的发作症状。

### 7. 优点（亮点）
*   **解耦设计**：通过“关节中心化”巧妙地实现了运动特征与背景/外观特征的解耦。
*   **动态建模**：跨关节注意力机制解决了癫痫症状学中“同病异症”的建模难题，不再受限于固定的解剖学连接。
*   **泛化性强**：在跨受试者测试中表现出的稳定性证明了其临床应用潜力。

### 8. 不足与局限
*   **发作类型覆盖有限**：该研究主要针对具有明显肢体运动的癫痫（Motor Seizures），对于失神发作等非运动性癫痫（Non-motor）或仅有微小面部抽搐的发作，该模型可能失效。
*   **隐私与细节的权衡**：由于数据集对人脸进行了模糊处理，模型无法利用眼球转动或细微面部表情等重要的临床线索。
*   **遮挡风险**：实验中提到，当医护人员遮挡患者关节时，模型会出现连续的假阴性（漏报）。
*   **样本量限制**：虽然在现有公开数据集上表现良好，但 14 个病例的样本量对于深度学习模型来说仍显单薄，需更大规模的多中心数据验证。

（完）
