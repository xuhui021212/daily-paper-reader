---
title: Learning Surgical Robotic Manipulation with 3D Spatial Priors
title_zh: 利用 3D 空间先验学习手术机器人操作
authors: "Yu Sheng, Lidian Wang, Xiaomeng Chu, Jiajun Deng, Min Cheng, Yanyong Zhang, Bei Hua, Houqiang Li, Jianmin Ji"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03798v1"
tags: ["query:medvid"]
score: 9.0
evidence: 手术机器人操作及内窥镜视觉运动策略
tldr: 本研究针对手术机器人操作中3D空间感知不足的问题，提出了Spatial Surgical Transformer (SST)。SST是一种端到端的视觉运动策略，通过直接从立体内窥镜图像中提取3D空间线索来增强机器人的感知能力。研究团队构建了包含3万对立体图像的大规模数据集Surgical3D，并利用几何Transformer提取鲁棒的3D潜表征，通过多级空间特征连接器将其与动作空间对齐。实验证明，SST在打结和离体器官解剖等复杂任务中达到了SOTA性能，为临床部署迈出了重要一步。
selection_source: fresh_fetch
motivation: 现有的手术机器人操作方法存在多阶段重建导致的误差累积，或因加装腕部摄像头而难以在临床实践中应用的问题。
method: 提出了SST模型，利用Surgical3D数据集微调几何Transformer以提取3D表征，并通过轻量级多级空间特征连接器实现端到端的动作映射。
result: 在真实机器人实验中，SST在打结和器官解剖等复杂手术任务上表现出卓越的性能和强大的空间泛化能力。
conclusion: 该研究通过挖掘内窥镜图像中的3D空间先验，为实现具备高度空间意识的自主手术机器人操作提供了有效的技术路径。
---

## 摘要
实现 3D 空间感知对于需要精确且精细操作的手术机器人操作至关重要。现有方法要么在操作前显式重建手术场景，要么通过增加腕部摄像头来补充默认的立体内窥镜，从而增强多视图特征。然而，这两种范式都存在显著局限性：前者由于其多阶段性质，容易导致误差累积并阻碍端到端优化；而后者在临床实践中很少被采用，因为腕部摄像头会干扰手术机器人臂的运动。在这项工作中，我们提出了空间手术 Transformer (SST)，这是一种端到端的视觉运动策略，通过直接挖掘内窥镜图像中嵌入的 3D 空间线索，赋予手术机器人 3D 空间感知能力。首先，我们构建了 Surgical3D，这是一个包含 3 万对具有精确 3D 几何信息的写实立体内窥镜图像对的大规模数据集，解决了手术场景中 3D 数据稀缺的问题。基于 Surgical3D，我们微调了一个强大的几何 Transformer，以从立体内窥镜图像中提取鲁棒的 3D 潜表征。随后，这些表征通过轻量级的多级空间特征连接器 (MSFC) 与机器人的动作空间无缝对齐，且全部在以内窥镜为中心的坐标系内完成。广泛的真实机器人实验表明，SST 在打结和离体器官解剖等复杂手术任务上实现了最先进的性能和强大的空间泛化能力，标志着向实际临床部署迈出了重要一步。数据集和代码将会开源。

## Abstract
Achieving 3D spatial awareness is crucial for surgical robotic manipulation, where precise and delicate operations are required. Existing methods either explicitly reconstruct the surgical scene prior to manipulation, or enhance multi-view features by adding wrist-mounted cameras to supplement the default stereo endoscopes. However, both paradigms suffer from notable limitations: the former easily leads to error accumulation and prevents end-to-end optimization due to its multi-stage nature, while the latter is rarely adopted in clinical practice since wrist-mounted cameras can interfere with the motion of surgical robot arms. In this work, we introduce the Spatial Surgical Transformer (SST), an end-to-end visuomotor policy that empowers surgical robots with 3D spatial awareness by directly exploring 3D spatial cues embedded in endoscopic images. First, we build Surgical3D, a large-scale photorealistic dataset containing 30K stereo endoscopic image pairs with accurate 3D geometry, addressing the scarcity of 3D data in surgical scenes. Based on Surgical3D, we finetune a powerful geometric transformer to extract robust 3D latent representations from stereo endoscopes images. These representations are then seamlessly aligned with the robot's action space via a lightweight multi-level spatial feature connector (MSFC), all within an endoscope-centric coordinate frame. Extensive real-robot experiments demonstrate that SST achieves state-of-the-art performance and strong spatial generalization on complex surgical tasks such as knot tying and ex-vivo organ dissection, representing a significant step toward practical clinical deployment. The dataset and code will be released.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Spatial Surgical Transformer (SST)** 的新型视觉运动策略，旨在通过 3D 空间先验增强手术机器人的自主操作能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：手术机器人操作（如缝合、解剖）需要毫米级的精度，但现有的视觉运动策略缺乏有效的 **3D 空间感知**。
*   **研究背景与动机**：
    *   **现有方法的缺陷**：传统的显式 3D 重建（如 SfM）是多阶段的，容易产生误差累积且无法端到端优化；而最近提出的增加腕部摄像头的方法在临床上不切实际，因为穿刺器（Trocar）限制了机械臂的径迹，无法容纳额外传感器。
    *   **数据稀缺**：手术领域缺乏具有精确 3D 标注的大规模数据集，导致现有的 3D 几何模型在手术场景中泛化性差。
    *   **目标**：开发一种仅依赖标准立体内窥镜、具备 3D 空间意识且能端到端执行任务的策略。

### 2. 方法论
SST 的核心思想是将预训练的几何 Transformer 与动作学习相结合，通过 3D 空间先验引导操作。
*   **Surgical3D 数据集构建**：利用 NVIDIA Omniverse 构建了包含 3 万对写实立体图像及深度图的合成数据集，并通过对真实手术视频生成“伪标签”来弥合虚实迁移（Sim-to-Real）的鸿沟。
*   **手术几何 Transformer**：基于 MASt3R 模型进行微调，输入立体图像对，提取包含丰富几何信息的 3D 潜表征（Latent Embeddings）。
*   **多级空间特征连接器 (MSFC)**：设计了一个轻量级模块，提取 Transformer 不同层级的特征（底层捕捉精细局部细节，高层捕捉全局上下文），并将其对齐到机器人的动作空间。
*   **以内窥镜为中心的策略解码器**：
    *   **动作空间**：采用以内窥镜为参考系的相对位姿表示（7自由度：平移+旋转+夹持器角度），解决了手术机器人正向运动学不精确的问题。
    *   **架构**：基于 ACT（Action Chunk Transformer）框架，预测一系列动作序列（Action Chunking）以确保运动的平滑性。

### 3. 实验设计
*   **实验场景**：在真实的 Torin 手术机器人上进行了三个代表性任务：
    1.  **Peg Pickup（套圈抓取）**：在具有不规则几何形状的硅胶肝脏模型上进行。
    2.  **Knot Tying（打结）**：涉及双臂协作和柔性缝线操作。
    3.  **Ex-vivo Gallbladder Dissection（离体胆囊解剖）**：使用真实的猪器官，模拟抓取、拉伸和切割动作。
*   **对比方法 (Benchmarks)**：
    *   **SRT**：带腕部摄像头的 SOTA 手术机器人 Transformer。
    *   **ACT**：标准的视觉运动策略（无 3D 先验）。
    *   **Diffusion Policy (DP)**：基于扩散模型的策略。

### 4. 资源与算力
*   **硬件**：论文提到在推理阶段使用了 **NVIDIA RTX-4090 GPU**。
*   **性能指标**：SST 的推理延迟约为 **56.2ms**（约 18Hz），能够满足实时部署需求。
*   **训练细节**：模型训练了 100 个 Epoch。文中未明确提及训练所用的具体 GPU 数量和总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行了 10 次独立测试。收集了 180-200 条演示轨迹用于模仿学习。
*   **消融实验**：
    *   验证了在 Surgical3D 数据集上微调的必要性（不微调则成功率大幅下降）。
    *   对比了不同特征连接器设计（LFC、MSC、MSFC）。
    *   对比了不同几何模型（MASt3R vs. VGGT）的效率。
*   **评价**：实验覆盖了从模拟环境到真实器官、从单臂到双臂的多种场景，对比了当前主流的基准模型，实验设计较为客观、公平，充分证明了 3D 先验在复杂任务中的关键作用。

### 6. 主要结论与发现
*   **SOTA 性能**：SST 在不依赖额外传感器的情况下，性能达到了甚至超过了配备腕部摄像头的 SRT 模型。
*   **空间泛化性**：SST 在处理未见过的物体位置和复杂的 3D 几何表面（如非平面的肝脏模型）时表现出极强的鲁棒性。
*   **3D 表征的重要性**：单纯增加图像编码器的强度（如 ACT/DP）无法解决手术中的空间感知问题，必须引入显式的几何引导。

### 7. 优点
*   **临床实用性强**：无需修改现有手术机器人硬件，仅靠内窥镜图像即可实现。
*   **端到端优化**：避免了显式重建带来的误差累积，同时保留了 3D 几何的解释性。
*   **数据集贡献**：填补了手术领域大规模 3D 视觉数据集的空白。
*   **多级特征融合**：MSFC 模块有效地平衡了局部精度和全局视野。

### 8. 不足与局限
*   **解剖任务成功率仍有提升空间**：在复杂的离体解剖任务中，整体成功率为 60%，主要难点在于精确定位胆囊与肝脏的交界面。
*   **虚实迁移依赖伪标签**：虽然使用了伪标签技术，但合成数据与真实人体内部环境（如出血、烟雾、反光）的差异仍可能影响极端情况下的表现。
*   **样本量限制**：虽然在机器人领域 10 次测试是常见标准，但对于医疗手术这种高风险应用，未来需要更大规模的统计验证。

（完）
