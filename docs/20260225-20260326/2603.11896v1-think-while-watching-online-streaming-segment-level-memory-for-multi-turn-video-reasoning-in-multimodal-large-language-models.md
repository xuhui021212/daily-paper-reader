---
title: "Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models"
title_zh: 边看边想：多模态大语言模型中用于多轮视频推理的在线流式片段级记忆
authors: "Lu Wang, Zhuoran Jin, Yupu Hao, Yubo Chen, Kang Liu, Yulong Ao, Jun Zhao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11896v1"
tags: ["query:lvu"]
score: 10.0
evidence: 基于记忆锚点的流式视频推理框架
tldr: 针对多模态大模型在在线视频流推理中存在的感知与生成无法并行以及长程记忆衰减问题，本文提出了“边看边想”（Think While Watching）框架。该框架通过构建片段级连续存储机制，结合三阶段多轮思维链数据集和流式因果掩码，实现了感知与思考的重叠运行。在StreamingBench等基准测试中，该方法显著提升了单轮和多轮交互的准确率，并大幅降低了推理开销。
selection_source: fresh_fetch
motivation: 现有的流式视频理解方法因交替感知生成模式导致无法并行处理且长程记忆易衰减，难以支持复杂的多轮交互。
method: 提出了基于片段级存储的流式推理框架，采用流式因果掩码和位置编码，并配合三阶段多轮思维链数据集进行训练。
result: "在StreamingBench和OVO-Bench上分别提升了2.6%和3.79%的准确率，且在多轮对话中减少了56%的输出Token。"
conclusion: 该研究通过优化存储机制和推理流程，有效解决了在线视频流中的长程依赖建模与实时交互效率问题。
---

## 摘要
多模态大语言模型（MLLMs）在离线视频理解方面表现出强大的性能，但大多数模型局限于离线推理或在线推理能力较弱，导致难以在持续到达的视频流上进行多轮交互。现有的流式方法通常采用交替的感知-生成范式，这阻碍了感知与生成的并发进行，并随着视频流的增长导致早期记忆衰减，从而损害了长程依赖建模。我们提出了“边看边想”（Think While Watching），这是一个以记忆为锚点的流式视频推理框架，能够在多轮交互过程中保留连续的片段级记忆。我们构建了一个三阶段、多轮的思维链数据集，并采用了阶段匹配的训练策略，同时通过片段级流式因果掩码和流式位置编码来强制执行严格的因果性。在推理过程中，我们引入了一种高效的流水线，使“观看”与“思考”重叠，并能自适应地选择最佳的注意力后端。在单轮和多轮流式输入协议下，我们的方法均取得了优异的结果。基于 Qwen3-VL，该方法在 StreamingBench 上的单轮准确率提升了 2.6%，在 OVO-Bench 上提升了 3.79%。在多轮设置下，它在保持性能的同时减少了 56% 的输出 token。代码可在以下网址获取：https://github.com/wl666hhh/Think_While_Watching/

## Abstract
Multimodal large language models (MLLMs) have shown strong performance on offline video understanding, but most are limited to offline inference or have weak online reasoning, making multi-turn interaction over continuously arriving video streams difficult. Existing streaming methods typically use an interleaved perception-generation paradigm, which prevents concurrent perception and generation and leads to early memory decay as streams grow, hurting long-range dependency modeling. We propose Think While Watching, a memory-anchored streaming video reasoning framework that preserves continuous segment-level memory during multi-turn interaction. We build a three-stage, multi-round chain-of-thought dataset and adopt a stage-matched training strategy, while enforcing strict causality through a segment-level streaming causal mask and streaming positional encoding. During inference, we introduce an efficient pipeline that overlaps watching and thinking and adaptively selects the best attention backend. Under both single-round and multi-round streaming input protocols, our method achieves strong results. Built on Qwen3-VL, it improves single-round accuracy by 2.6% on StreamingBench and by 3.79% on OVO-Bench. In the multi-round setting, it maintains performance while reducing output tokens by 56%. Code is available at: https://github.com/wl666hhh/Think_While_Watching/

---

## 论文详细总结（自动生成）

以下是对论文《Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管多模态大模型（MLLM）在离线视频理解上表现优异，但在**在线流式视频**场景（如直播、监控、机器人助手）中面临巨大挑战。
*   **核心痛点**：
    1.  **记忆侵蚀（Memory Erosion）**：现有的交替式感知-生成范式在处理长视频流时，早期的视觉线索和对话上下文容易被遗忘，导致多轮对话的一致性差。
    2.  **串行化瓶颈（Serialization Bottleneck）**：模型在生成文本时通常会停止视频摄取，导致感知与推理无法同步，产生严重的排队延迟。
*   **整体含义**：本文提出了 **“边看边想”（Think While Watching, TWW）** 框架，旨在通过片段级记忆机制和解耦的推理架构，实现高效、长程一致的在线多轮视频推理。

### 2. 方法论：核心思想与关键技术
*   **片段级记忆笔记（Segment-level Memory Notes）**：
    *   将视频流划分为连续片段（Segments），模型为每个片段生成简洁的“记忆笔记”（包含实体、动作、场景变化等）。
    *   这些笔记作为持久状态存储在记忆库中，后续提问时通过注意力机制隐式检索相关笔记。
*   **流式架构设计**：
    *   **片段级流式因果掩码（Streaming Causal Mask）**：确保严格的因果性，即生成的回复只能看到当前及之前的视频片段和对话历史，严禁“偷看”未来帧。
    *   **流式位置编码（Streaming Positional Encoding）**：基于 MRoPE 改进，将输入流和输出流的位置编码解耦，使得新到达的视频片段可以独立分配位置，无需等待文本解码完成。
*   **训练策略**：
    *   **三阶段训练**：
        1.  阶段1：学习编写片段笔记并回答单轮问题。
        2.  阶段2：扩展至多轮对话一致性训练。
        3.  阶段3：长程能力训练（处理长视频、不确定性处理及抗干扰学习）。
*   **推理优化**：
    *   **双 KV 缓存（Dual KV Cache）**：实现感知（Watching）与思考（Thinking）的并行化。
    *   **自适应注意力后端**：根据 Query 和 Key 的长度动态切换 Flash Attention 或 Memory-efficient Attention，以平衡速度与自定义掩码的需求。

### 3. 实验设计
*   **数据集与场景**：
    *   **在线基准**：StreamingBench（评估实时性、全源理解等）和 OVO-Bench（评估回溯、实时和前瞻能力）。
    *   **离线泛化**：Video-MME 和 LV-Bench。
*   **对比方法**：
    *   **闭源模型**：Gemini 1.5 Pro, GPT-4o。
    *   **开源流式模型**：Flash-VStream, VideoLLM-online, Dispider, StreamAgent 等。
    *   **基准模型**：Qwen3-VL (2B/4B/8B) 的 Instruct 和 Thinking 版本。
*   **评估协议**：分为单轮流式输入和多轮流式交互。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX A6000 GPU (每台 48GB 显存)**。
*   **训练细节**：采用全参数监督微调（SFT），使用 DeepSpeed ZeRO-3 优化器，bf16 精度，全局 Batch Size 为 128。
*   **模型规模**：涵盖了 Qwen3-VL 的 2B、4B 和 8B 三个量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流在线基准上进行了详尽测试，并跨越了三个模型参数量级。
    *   进行了**消融实验**，验证了记忆笔记的必要性、视觉掩码的影响以及片段划分粒度（15s 到 120s）的权衡。
    *   包含**延迟分析（TTFT）**和**注意力分布可视化**，证明了模型确实在关注长程记忆。
*   **充分性评价**：实验设计非常充分且客观。通过对比“离线 Batch”与“在线流式”的表现，揭示了现有模型在流式场景下的性能塌陷，证明了所提方法的必要性。

### 6. 主要结论与发现
*   **性能提升**：在 Qwen3-VL-4B 基础上，TWW 在 StreamingBench 上提升了 2.6% 的准确率，在 OVO-Bench 上提升了 3.79%。
*   **效率飞跃**：在多轮对话中，TWW 在保持准确率的同时，**减少了 56% 的输出 Token**（通过更高效的 CoT 记忆机制）。
*   **延迟降低**：相比离线 Batch 模式，TWW 的**首字延迟（TTFT）降低了 92.6%**。
*   **泛化性**：流式训练不仅提升了在线表现，还意外增强了模型在离线长视频任务（如 Video-MME）上的推理能力。

### 7. 优点与亮点
*   **架构创新**：成功解耦了视频感知与文本生成，解决了流式 MLLM 的“排队”难题。
*   **数据贡献**：构建并开源了首个三阶段、多轮流式思维链（CoT）数据集，填补了该领域的空白。
*   **实用性强**：片段级记忆笔记不仅减少了显存压力（相比全量 KV 缓存），还提供了可解释的中间推理过程。

### 8. 不足与局限
*   **片段划分固定**：目前采用固定的时间窗口划分片段，未来需探索基于场景切换的自适应划分。
*   **模态缺失**：尚未整合音频信号或语音转录（Speech Transcripts），这在真实直播场景中至关重要。
*   **极端长程失效**：在极长视频中，模型仍可能丢失早期细粒度的身份信息，或受到强视觉干扰（Distractors）的影响导致记忆污染。

（完）
