---
title: "VideoAtlas: Navigating Long-Form Video in Logarithmic Compute"
title_zh: VideoAtlas：以对数级计算探索长视频
authors: "Mohamed Eltahir, Ali Habibullah, Yazan Alshoibi, Lama Ayash, Tanveer Hussain, Naeemullah Khan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17948v1"
tags: ["query:lvu"]
score: 9.0
evidence: 以对数级计算复杂度导航长视频
tldr: 针对长视频理解中存在的表示损耗和长上下文处理难题，本文提出 VideoAtlas。它将视频表示为无损、可导航的层次化网格，支持递归缩放，使访问深度随视频长度对数增长。基于此构建的 Video-RLM 采用主从架构进行并行探索，实现了计算量随视频时长对数增长，并在 1-10 小时长视频基准测试中表现出极强的鲁棒性和高效的计算分配能力。
selection_source: fresh_fetch
motivation: 现有长视频理解方法依赖有损的文本转换或近似表示，导致视觉细节丢失且难以处理超长上下文。
method: 提出 VideoAtlas 层次化网格环境，并结合递归语言模型构建主从架构，通过并行探索和递归缩放实现无损视觉导航。
result: "实验表明计算量随视频时长呈对数增长，且由于结构复用实现了 30-60% 的多模态缓存命中率，在长达 10 小时的视频任务中表现稳健。"
conclusion: 结构化环境导航是实现可扩展、高保真长视频理解的一种有效且稳健的新范式。
---

## 摘要
将语言模型扩展到视频领域面临两个挑战：一是表示（representation），现有方法依赖于有损近似；二是长上下文（long-context），基于字幕或智能体的流水线常将视频压缩为文本，从而丢失了视觉保真度。为了克服这些问题，我们引入了 VideoAtlas，这是一个与任务无关的环境，将视频表示为分层网格，它同时具备无损、可导航、可扩展、无需字幕且无需预处理的特点。视频概览一目了然，任何区域都可以递归地放大，视频、中间调查过程以及智能体的记忆均统一使用相同的视觉表示，从而在端到端过程中消除了有损的文本转换。这种分层结构确保了访问深度仅随视频长度呈对数级增长。针对长上下文，递归语言模型（RLM）最近为长文本提供了强大的解决方案，但将其扩展到视觉领域需要一个可供递归的结构化环境，而 VideoAtlas 正提供了这一点。将 VideoAtlas 视为马尔可夫决策过程解锁了 Video-RLM：这是一种并行的主从（Master-Worker）架构，其中主节点协调全局探索，而从节点并发地深入分配的区域以积累无损的视觉证据。我们展示了三个关键发现：（1）计算量随视频时长呈对数级增长，且由于网格结构的重用带来了 30-60% 的多模态缓存命中率，进一步放大了这一优势；（2）环境预算（environment budgeting），通过限制最大探索深度，提供了一个有原则的计算-准确度超参数；（3）涌现出的自适应计算分配，可随问题粒度进行缩放。当从 1 小时扩展到 10 小时的基准测试时，Video-RLM 仍然是时长鲁棒性最强的方法，准确度下降极小，证明了结构化环境导航是视频理解中一种可行且可扩展的范式。

## 速览
**TLDR**：针对长视频理解中表示损失和长上下文处理难题，本文提出 VideoAtlas。它将视频表示为无损、可导航的层次化网格，允许模型以对数级计算复杂度进行递归缩放探索。基于此构建的 Video-RLM 采用主从架构并行处理，实现了从1小时到10小时视频的高效理解，在保持视觉保真度的同时显著降低了计算开销，为长视频分析提供了新范式。 \
**Motivation**：现有长视频理解方法依赖有损的文本转换或近似表示，导致视觉细节丢失且难以处理超长上下文。 \
**Method**：提出 VideoAtlas 层次化网格环境，并结合递归语言模型（Video-RLM）通过主从架构实现对视频区域的并行递归探索。 \
**Result**：该方法实现了计算量随视频长度对数增长，并在 1-10 小时视频基准测试中表现出极强的鲁棒性和高缓存命中率。 \
**Conclusion**：结构化环境导航是实现可扩展、高保真长视频理解的一种稳健且高效的范式。

---

## Abstract
Extending language models to video introduces two challenges: representation, where existing methods rely on lossy approximations, and long-context, where caption- or agent-based pipelines collapse video into text and lose visual fidelity. To overcome this, we introduce \textbf{VideoAtlas}, a task-agnostic environment to represent video as a hierarchical grid that is simultaneously lossless, navigable, scalable, caption- and preprocessing-free. An overview of the video is available at a glance, and any region can be recursively zoomed into, with the same visual representation used uniformly for the video, intermediate investigations, and the agent's memory, eliminating lossy text conversion end-to-end. This hierarchical structure ensures access depth grows only logarithmically with video length. For long-context, Recursive Language Models (RLMs) recently offered a powerful solution for long text, but extending them to visual domain requires a structured environment to recurse into, which \textbf{VideoAtlas} provides. \textbf{VideoAtlas} as a Markov Decision Process unlocks Video-RLM: a parallel Master-Worker architecture where a Master coordinates global exploration while Workers concurrently drill into assigned regions to accumulate lossless visual evidence. We demonstrate three key findings: (1)~logarithmic compute growth with video duration, further amplified by a 30-60\% multimodal cache hit rate arising from the grid's structural reuse. (2)~environment budgeting, where bounding the maximum exploration depth provides a principled compute-accuracy hyperparameter. (3)~emergent adaptive compute allocation that scales with question granularity. When scaling from 1-hour to 10-hour benchmarks, Video-RLM remains the most duration-robust method with minimal accuracy degradation, demonstrating that structured environment navigation is a viable and scalable paradigm for video understanding.

---

## 论文详细总结（自动生成）

### VideoAtlas：以对数级计算探索长视频 —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
理解长视频（如 1-10 小时）的核心挑战在于如何在海量的时间跨度中定位稀疏的关键证据。现有方法存在以下局限：
*   **表示损耗：** 均匀采样会丢失短促事件；将视频压缩为文本（Caption-based）会丢失视觉保真度。
*   **覆盖与精度的权衡：** 在固定上下文窗口内，增加帧数必然导致单帧分辨率下降。
*   **扩展性瓶颈：** 线性扫描或预处理（如离线生成字幕）的成本随视频时长线性增长，难以应对超长视频。
*   **研究动机：** 借鉴电影剪辑师使用“联络表”（Contact Sheet）快速定位并缩放的逻辑，提出一种无损、可导航且计算量随视频长度对数增长的视频表示与推理框架。

#### 2. 论文提出的方法论
论文提出了 **VideoAtlas** 环境和 **Video-RLM** 智能体架构：
*   **VideoAtlas（层次化网格环境）：**
    *   将视频表示为一个递归的 $K \times K$ 图像网格（默认 $K=8$，即 64 格）。
    *   **根网格（Root Grid）：** 提供全视频的鸟瞰图。
    *   **递归缩放（EXPAND）：** 智能体通过 `EXPAND` 动作进入某个单元格，生成更高分辨率的子网格。
    *   **对数深度：** 达到亚秒级精度所需的步骤仅为 $O(\log T)$，其中 $T$ 为视频时长。
*   **Video-RLM（主从智能体架构）：**
    *   **Master（主节点）：** 负责全局探测、分配任务给 Worker、进行不确定性分析并决定是否终止探索。
    *   **Workers（从节点）：** 并行探索分配的区域，支持深度优先（DFS，用于定位细节）或广度优先（BFS，用于理解序列）搜索。
    *   **Visual Scratchpad（视觉草稿本）：** 一种无损的多模态记忆，存储采集到的原始帧、时间戳和描述，最终以网格形式反馈给模型进行决策，完全避免了中间过程的文本损失。

#### 3. 实验设计
*   **数据集：**
    *   **LongVideoBench (LVB)：** 15-60 分钟视频。
    *   **Video-MME (VMME)：** 包含不同长度的视频（实验中去除了字幕以测试纯视觉理解）。
    *   **10小时变体：** 为了压力测试，研究者通过拼接视频构建了长达 10 小时的测试场景。
*   **对比方法：**
    *   **闭源模型：** GPT-4o, GPT-5, Gemini-1.5-Flash, Claude-3.5-Opus。
    *   **开源 VLM：** InternVL2.5-26B, GLM-4.5V-106B。
    *   **智能体/字幕方法：** MR.Video, VideoARM, DVD, LLoVi。
    *   **基准线：** 均匀采样的 Qwen3.5 (160帧)。

#### 4. 资源与算力
*   **硬件：** 使用了 **4 张 NVIDIA A100 80GB GPU**。
*   **推理框架：** 使用 **vLLM** 进行模型部署，利用其自动多模态前缀缓存（Multimodal Prefix Caching）功能。
*   **模型：** 主要使用 **Qwen3.5-35B-A3B**（混合专家模型，总 35B 参数，每次前向激活 3B）作为 Backbone，同时也测试了 Gemini-1.5-Flash 以验证框架的通用性。
*   **训练时长：** 该方法为 **Zero-shot（零样本）** 推理框架，无需针对特定视频任务进行微调训练。

#### 5. 实验数量与充分性
*   **实验规模：** 在标准长视频基准（LVB, VMME）和自定义的 10 小时极端场景上进行了全面测试。
*   **消融与分析实验：**
    *   **环境预算实验：** 测试了不同最大探索深度（Depth）对准确率的影响。
    *   **自适应计算实验：** 验证了计算量是否随问题难度（答案分布的散乱程度）自动调整。
    *   **并行扩展性实验：** 测试了 Worker 数量（1 到 7 个）对推理耗时的影响。
*   **公平性：** 实验对比了相同 Backbone 下的均匀采样和字幕方法，并使用了标准化的 Token 计数方式，实验设计较为客观、公平。

#### 6. 论文的主要结论与发现
*   **对数级计算增长：** 相比于线性增长的字幕方法，Video-RLM 的 Token 消耗随视频时长呈对数级增长，在 10 小时视频中节省了约 9.7 倍的 Token。
*   **时长鲁棒性：** 在从 1 小时扩展到 10 小时视频时，Video-RLM 的准确率下降极小（如在 VMME 上仅下降 0.7%），而均匀采样和字幕方法大幅退化。
*   **高缓存命中率：** 由于网格结构的重复利用，实现了 **30-60% 的多模态缓存命中率**，显著降低了实际 GPU 计算开销。
*   **自适应分配：** 系统会自动为分布散乱的答案分配更多计算资源（Token 消耗增加约 40%）。

#### 7. 优点
*   **无损表示：** 始终保持原始视觉保真度，不依赖中间文本转换。
*   **高效导航：** 层次化网格设计使得搜索效率极高，且天然支持并行化。
*   **无需预处理：** 无需离线解码或生成字幕，可直接在原始视频流上操作。
*   **环境预算化：** 通过限制深度 $d$ 提供了一个清晰的“计算-准确度”权衡杠杆。

#### 8. 不足与局限
*   **感知瓶颈：** 系统的上限受限于底层 VLM 的视觉感知能力（如误读文字、视觉混淆）。
*   **无锚点搜索开销：** 如果根网格中没有明显的视觉线索（锚点），智能体可能需要多轮探索才能找到相关区域。
*   **评估范围：** 目前主要验证了多选题（QA），在时间定位（Grounding）和摘要生成等任务上的表现尚待更多实证。
*   **纯零样本：** 目前仅使用零样本推理，尚未引入强化学习（RL）来优化探索策略。

（完）
