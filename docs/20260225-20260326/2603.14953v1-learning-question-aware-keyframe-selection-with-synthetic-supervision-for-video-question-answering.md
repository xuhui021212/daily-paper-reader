---
title: Learning Question-Aware Keyframe Selection with Synthetic Supervision for Video Question Answering
title_zh: 面向视频问答的合成监督下问题感知关键帧选择学习
authors: "Minchan Kwon, Hyounguk Shon, Junmo Kim"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14953v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于视频问答的问题感知关键帧选择
tldr: 针对视频问答（VideoQA）中推理成本高和信息稀释的问题，本文提出了一种感知问题的关键帧选择框架。该方法利用大模型生成的伪标签提供合成监督，并引入覆盖正则化以确保关键帧的多样性与互补性。在NExT-QA数据集上的实验证明，该方法显著提升了模型在时序和因果推理方面的表现，证明了可学习关键帧选择模块的有效性。
selection_source: fresh_fetch
motivation: 现有的VideoQA方法面临推理成本高、信息稀释以及关键帧选择缺乏有效监督和存在冗余的问题。
method: 提出一种结合大模型生成伪标签进行合成监督，并利用覆盖正则化促进时序证据多样性的关键帧选择框架。
result: 在NExT-QA数据集上显著提高了准确率，特别是在处理复杂的时序和因果关系问题上表现优异。
conclusion: 关键帧选择是一个有效且可学习的模块，能够通过合成监督和多样性约束显著增强VideoQA的推理能力。
---

## 摘要
大规模多模态模型（LMMs）近期在视频问答（VideoQA）中展现了卓越的性能，但由于高昂的推理成本和稀释的信息，视频推理仍然具有挑战性。关键帧选择能够提高效率并实现更精准的推理，但仅依赖图文相似度时，往往面临监督稀疏和帧选择冗余的问题。我们提出了一个问题感知的关键帧选择框架，包含两个核心组件：一是源自 LMMs 的伪关键帧标签，用于提供信息丰富的监督；二是覆盖正则化，旨在促进跨时间维度的多元化和互补性证据。在 NExT-QA 数据集上的实验表明，我们的方法显著提高了准确率，特别是在时间型和因果型问题上，从而确立了关键帧选择作为视频问答中一个有效且可学习模块的地位。

## Abstract
Large multimodal models (LMMs) have recently demonstrated remarkable performance in video question answering (VideoQA), yet reasoning over video remains challenging due to high inference cost and diluted information. Keyframe selection offers efficiency and sharper reasoning but suffers from sparse supervision and redundant frame choices when relying only on image-text similarity. We present a question-aware keyframe selection framework with two components: pseudo keyframe labels derived from LMMs that provide informative supervision and a coverage regularization that promotes diverse, complementary evidence across time. Experiments on NExT-QA show that our method significantly improves accuracy, especially for temporal and causal question types, establishing keyframe selection as an effective and learnable module for VideoQA.

---

## 论文详细总结（自动生成）

### 论文总结：面向视频问答的合成监督下问题感知关键帧选择学习

#### 1. 核心问题与研究动机
*   **核心问题**：在视频问答（VideoQA）任务中，大语言模型（LMMs）面临推理成本高昂和信息稀释（冗余帧干扰）的挑战。
*   **研究动机**：
    *   **监督稀疏性**：现有的 VideoQA 数据集通常只提供“问题-答案”对，缺乏哪些帧是关键帧的标注。
    *   **选择冗余性**：传统的基于 CLIP 相似度的选择方法往往会选中时间上相邻的重复帧，缺乏多样性。
    *   **推理局限性**：静态的图文匹配忽略了视频中的时序和因果逻辑，难以回答“为什么”或“之后发生了什么”等复杂问题。

#### 2. 核心方法论
论文提出了一个**问题感知的关键帧选择框架**，主要包含以下核心组件：
*   **高斯生成器（Gaussian Generator）**：继承自 GCG 框架，利用 CLIP 图像编码器和文本编码器提取特征，输出 $K$ 个高斯掩码来表示关键帧的概率分布。
*   **合成关键帧监督（SKS）**：
    *   利用强大的 LMM（如 Qwen2.5-VL）作为“教师”，通过精心设计的 Prompt（包含信息优先、非冗余、时序逻辑等规则）让其输出回答问题所需的关键帧索引。
    *   将这些索引转化为伪标签，通过均方误差（MSE）损失函数指导轻量级选择器的训练，从而引入高层的语义和逻辑关联。
*   **问题感知覆盖正则化（QCCR）**：
    *   在推理阶段引入覆盖正则化项，通过调节强度参数 $\lambda$ 来平衡“帧得分”与“时间分布均匀性”。
    *   **自适应调节**：利用 LMM 主干网络预先判断问题类型（描述性、时序性、因果性），并根据类型动态调整 $\lambda$，确保选中的帧既相关又具有互补性。

#### 3. 实验设计
*   **数据集**：主要在 **NExT-QA** 数据集上进行评估，该数据集包含 5.4k 个视频和 52k 个问答对，涵盖描述（Descriptive）、时序（Temporal）和因果（Causal）三种问题类型。
*   **Benchmark 与对比方法**：
    *   对比了包括 HCRN、VGT、Raformer、TranSTR 等传统方法。
    *   对比了 GCG（直接基准）和 SeViLA（强力 LMM 基准）。
*   **模型配置**：使用 InstructBLIP 作为 LMM 主干，EVA-CLIP 作为视觉-文本编码器，Qwen2.5-VL 用于生成合成标签。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **模型规模**：提到使用了 InstructBLIP 和 Qwen2.5-VL（后者通常参数量较大），但其核心选择器模块（高斯生成器）被描述为轻量级的线性层。

#### 5. 实验数量与充分性
*   **实验规模**：在 NExT-QA 这一主流且具挑战性的数据集上进行了全面测试。
*   **消融实验**：分别验证了移除 QCCR（覆盖正则化）和 SKS（合成监督）后的性能变化，证明了各组件的必要性。
*   **定性分析**：通过可视化对比了 CLIP 评分与 VLM 排序的差异，直观展示了方法在处理时序/因果问题上的优势。
*   **充分性评价**：实验设计较为客观，对比了多种不同架构的 SOTA 模型，但在数据集的多样性覆盖上（如长视频数据集）略显单一。

#### 6. 主要结论与发现
*   **性能提升**：该方法在 NExT-QA 上达到了 **73.3%** 的平均准确率，超越了 SeViLA 等强力基准。
*   **类型优势**：在**因果（Causal）**和**描述（Descriptive）**类型问题上提升显著，证明了合成监督能有效传递复杂的语义关系。
*   **效率与精度平衡**：通过仅选择 4 个关键帧，模型在大幅降低 LMM 推理负担的同时，保持甚至提升了推理质量。

#### 7. 优点
*   **解决监督难题**：巧妙利用强 LMM 生成伪标签，解决了 VideoQA 缺乏时序标注的痛点。
*   **自适应性强**：QCCR 模块能够根据问题语义动态调整采样策略，避免了单一启发式规则的局限。
*   **轻量化集成**：选择器部分非常轻量，易于集成到现有的 LMM 架构中。

#### 8. 不足与局限
*   **数据集覆盖有限**：实验仅集中在 NExT-QA，未在 AGQA 或 Ego4D 等其他大规模或更长视频的数据集上验证泛化性。
*   **依赖教师模型**：合成监督的效果高度依赖于所选 LMM（如 Qwen2.5-VL）的质量，若教师模型存在偏见或错误，会直接影响选择器的性能。
*   **超参数敏感性**：QCCR 中的 $\lambda$ 参数针对不同问题类型的预设值可能需要精细调优，缺乏完全自动化的学习机制。

（完）
