---
title: "PanORama: Multiview Consistent Panoptic Segmentation in Operating Rooms"
title_zh: PanORama：手术室中多视图一致的全景分割
authors: "Tuna Gürbüz, Ege Özsoy, Tony Danjun Wang, Nassir Navab"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19920v1"
tags: ["query:medvid"]
score: 9.0
evidence: 手术室手术流程的多视图一致全景分割
tldr: "针对手术室复杂、动态且遮挡严重的场景，本文提出了首个具有多视图一致性的全景分割模型 PanORama。该方法通过在骨干网络内部进行跨视图特征交互，实现了单次前向传播下的视图一致性，无需相机标定或后期优化。在 MM-OR 和 4D-OR 数据集上，该模型在全景质量（PQ）上超过 70%，优于现有技术，并能泛化至未见过的相机视角，显著提升了手术室的空间理解能力。"
selection_source: fresh_fetch
motivation: 手术室环境复杂且遮挡严重，稀疏多视图下的全景分割常因局部视野受限导致各相机预测不一致。
method: 在骨干网络中直接建模跨视图特征交互，实现无需相机参数、无需后期处理的端到端多视图一致性分割。
result: "在 MM-OR 和 4D-OR 数据集上达到超过 70% 的 PQ 性能，显著超越了现有最先进方法。"
conclusion: PanORama 通过增强多视图一致性提升了手术室空间感知，为手术辅助和感知系统提供了新的可能性。
---

## 摘要
手术室（OR）是杂乱、动态且高度遮挡的环境，在复杂的手术工作流中，可靠的空间理解对于态势感知至关重要。从稀疏的多视图图像中实现全景分割的空间理解面临着根本性的挑战，因为部分视图中的有限可见性通常会导致不同摄像机之间的预测错误。为此，我们推出了 PanORama，这是首个在设计上就具备多视图一致性的手术室全景分割方法。通过在单次前向传播中在骨干网络内部的特征层级建模跨视图交互，视图一致性是直接产生的，而非通过事后细化。我们在 MM-OR 和 4D-OR 数据集上进行了评估，实现了超过 70% 的全景质量（PQ）性能，并超越了之前的最先进技术。重要的是，PanORama 无需校准，不需要相机参数，并且在推理时可以泛化到任何多视图配置中未见过的相机视角。通过显著增强多视图分割并进而提升手术室的空间理解，我们相信我们的方法为手术感知和辅助开辟了新的机遇。代码将在论文接收后发布。

## Abstract
Operating rooms (ORs) are cluttered, dynamic, highly occluded environments, where reliable spatial understanding is essential for situational awareness during complex surgical workflows. Achieving spatial understanding for panoptic segmentation from sparse multiview images poses a fundamental challenge, as limited visibility in a subset of views often leads to mispredictions across cameras. To this end, we introduce PanORama, the first panoptic segmentation for the operating room that is multiview-consistent by design. By modeling cross-view interactions at the feature level inside the backbone in a single forward pass, view consistency emerges directly rather than through post-hoc refinement. We evaluate on the MM-OR and 4D-OR datasets, achieving >70% Panoptic Quality (PQ) performance, and outperforming the previous state of the art. Importantly, PanORama is calibration-free, requiring no camera parameters, and generalizes to unseen camera viewpoints within any multiview configuration at inference time. By substantially enhancing multiview segmentation and, consequently, spatial understanding in the OR, we believe our approach opens new opportunities for surgical perception and assistance. Code will be released upon acceptance.

---

## 论文详细总结（自动生成）

以下是对论文《PanORama: Multiview Consistent Panoptic Segmentation in Operating Rooms》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
手术室（OR）环境极其复杂，具有高动态性、严重遮挡以及大量细小或透明的手术器械。为了实现手术辅助和态势感知，系统需要具备深度的空间理解能力。
*   **核心挑战**：单视图感知容易受遮挡影响，导致信息缺失；而现有的多视图方法通常依赖精确的相机标定（参数），或者需要复杂的后期处理来对齐不同视角的预测结果。
*   **研究目标**：提出一种**无需相机标定**、在模型设计上即具备**多视图一致性**的全景分割框架，能够从稀疏的多个视角中提取全局相干的场景表示。

### 2. 论文提出的方法论
PanORama 采用端到端的架构，主要由三个核心组件组成：
*   **共享视觉骨干网络 (Shared Backbone)**：使用预训练的 DINOv2 提取各视图的补丁级（patch-level）特征 Token。所有视图共享同一套权重，以保持特征空间的统一。
*   **多视图特征聚合器 (Multiview Feature Aggregator)**：这是核心创新点。借鉴了 VGGT 的设计，通过交替使用**局部注意力**（仅在单视图内进行自注意力）和**全局注意力**（跨所有视图的 Token 进行自注意力）来建模跨视图交互。这种设计允许模型在不依赖相机位姿或深度信息的情况下，自动学习视图间的空间对应关系。
*   **全景分割头 (Panoptic Segmentation Head)**：采用粗到精（coarse-to-fine）的解码器，通过卷积细化和跳跃连接（skip connections）将聚合后的多视图感知 Token 逐步上采样，最终生成每个视角的像素级语义评分图。
*   **训练损失**：结合了二元交叉熵损失（BCE）和 Dice 损失，支持在训练时随机化视图顺序以增强鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **MM-OR**：模拟机器人膝关节置换手术数据集，包含 5 个外部 RGB 视角（其中 3 个有标注）。
    *   **4D-OR**：模拟膝关节置换手术数据集，包含 6 个外部 RGB 视角（其中 3 个有标注）。
*   **基准模型 (Benchmark)**：对比了当前最先进的单视图全景分割模型 **DVIS++**。
*   **评估指标**：全景质量（PQ），并针对时间维度评估了 1、4、8 帧连续窗口下的性能。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA RTX 3090 (24 GB)** GPU。
*   **训练细节**：采用 AdamW 优化器，学习率为 $1 \times 10^{-5}$，权重衰减为 0.05。
*   **时长/规模**：训练 21 个 epoch，每个 mini-batch 包含 5-6 个样本。

### 5. 实验数量与充分性
论文进行了多维度的实验验证，具有较高的客观性：
*   **主实验**：在两个主流手术室数据集上验证了性能，PQ 指标提升显著（比 DVIS++ 高出约 7%）。
*   **消融实验**：对比了单视图版 PanORama 与多视图版，证明了跨视图特征聚合的有效性。
*   **未标注视图利用实验**：验证了模型即使在部分视角缺乏标注的情况下，也能通过引入这些视角提升整体分割精度。
*   **新视角泛化实验**：通过在训练中剔除某一相机视角并在推理时重新引入，证明了模型学习到了全局一致的表示，而非简单的视角记忆。

### 6. 主要结论与发现
*   **性能领先**：PanORama 在 MM-OR 和 4D-OR 上均达到了 SOTA 水平，PQ 超过 70%。
*   **一致性涌现**：通过在解码前进行特征级交互，视图间的一致性是自然产生的，无需后期处理。
*   **无需标定**：模型在完全不知道相机参数的情况下，依然能有效整合多视角信息。
*   **泛化能力强**：模型能够很好地适应推理阶段新增的或未见过的相机视角。

### 7. 优点
*   **设计优雅**：将多视图一致性集成在骨干网络内部，实现了单次前向传播完成所有视角的预测。
*   **实用性高**：无需相机标定（Calibration-free），这在频繁变动的手术室环境中极具应用价值。
*   **灵活性**：支持推理时输入任意数量的同步视图。
*   **鲁棒性**：在严重遮挡和物体仅部分可见的情况下，能利用其他视角的互补信息进行修复。

### 8. 不足与局限
*   **时间建模缺失**：目前主要关注空间一致性，虽然在时间窗口评估中表现良好，但模型内部缺乏显式的时序动力学建模。
*   **数据局限性**：实验主要基于模拟手术数据集（MM-OR 和 4D-OR），在真实临床手术室环境中的表现仍需进一步验证。
*   **计算开销**：随着输入视图数量和分辨率的增加，全局注意力机制的计算复杂度（$O(N^2)$）可能会成为实时处理的瓶颈。

（完）
