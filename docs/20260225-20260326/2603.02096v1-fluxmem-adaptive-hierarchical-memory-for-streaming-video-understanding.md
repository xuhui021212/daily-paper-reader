---
title: "FluxMem: Adaptive Hierarchical Memory for Streaming Video Understanding"
title_zh: FluxMem：用于流式视频理解的自适应分层记忆
authors: "Yiweng Xie, Bo He, Junke Wang, Xiangyu Zheng, Ziyi Ye, Zuxuan Wu"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02096v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于流式视频理解的自适应分层记忆
tldr: FluxMem是一个无需训练的流式视频理解框架，旨在解决长视频处理中的冗余和效率问题。它通过分层设计实现自适应内存压缩：首先利用时间相邻选择（TAS）消除帧间冗余，再通过空间域整合（SDC）合并帧内重复区域。该框架引入自适应机制，根据场景统计数据自动调整压缩率。实验表明，FluxMem在多个基准测试中达到SOTA水平，显著降低了延迟和显存占用，同时保持了优秀的离线理解能力。
selection_source: fresh_fetch
motivation: 针对流式视频理解中视觉内存冗余导致的计算开销大和实时性差的问题，提出高效的自适应压缩方案。
method: 采用分层架构，结合时间相邻选择（TAS）和空间域整合（SDC），并利用自适应机制根据场景动态调整压缩率。
result: "在StreamingBench和OVO-Bench上刷新SOTA，延迟降低约70%，显存减少约35%，且在MLVU上以极少Token保持高性能。"
conclusion: FluxMem通过自适应的分层内存管理，在不牺牲理解精度的情况下，大幅提升了流式视频处理的效率和实时性。
---

## 摘要
本文提出了 FluxMem，这是一个用于高效流式视频理解的免训练框架。FluxMem 通过一种分层的两阶段设计自适应地压缩冗余视觉记忆：(1) 时间相邻选择 (TAS) 模块移除相邻帧之间的冗余视觉 token；(2) 空间域整合 (SDC) 模块进一步将每帧内空间重复的区域合并为紧凑的表示。为了有效适应动态场景，我们在 TAS 和 SDC 中引入了一种自适应 token 压缩机制，该机制根据场景的内在统计数据自动确定压缩率，而非手动调节。大量实验表明，FluxMem 在现有的在线视频基准测试中取得了新的 SOTA 结果，在实时设置下，StreamingBench 达到 76.4，OVO-Bench 达到 67.2，同时在 OVO-Bench 上将延迟降低了 69.9%，峰值 GPU 显存占用降低了 34.5%。此外，它还保持了强大的离线性能，在 MLVU 上达到 73.1，同时减少了 65% 的视觉 token 使用量。

## 速览
**TLDR**：FluxMem是一个无需训练的流式视频理解框架，通过分层设计解决视觉内存冗余。它利用TAS和SDC模块分别消除帧间和帧内的冗余Token，并引入自适应机制根据场景动态调整压缩率。该框架在多个基准测试中取得SOTA，显著降低了延迟和显存占用，实现了高效且精准的实时视频理解。 \
**Motivation**：针对流式视频理解中视觉内存冗余导致的计算效率低下和显存压力问题，提出一种高效的自适应压缩方案。 \
**Method**：采用分层架构，通过TAS模块去除帧间冗余并利用SDC模块合并帧内空间重复区域，结合自适应机制动态调整压缩率。 \
**Result**：在StreamingBench和OVO-Bench上刷新SOTA记录，延迟降低69.9%，峰值显存减少34.5%，且在MLVU上表现强劲。 \
**Conclusion**：FluxMem通过自适应的分层内存管理，在不牺牲理解能力的前提下，显著提升了流式视频理解的效率和实时性。

---

## Abstract
This paper presents FluxMem, a training-free framework for efficient streaming video understanding. FluxMem adaptively compresses redundant visual memory through a hierarchical, two-stage design: (1) a Temporal Adjacency Selection (TAS) module removes redundant visual tokens across adjacent frames, and (2) a Spatial Domain Consolidation (SDC) module further merges spatially repetitive regions within each frame into compact representations. To adapt effectively to dynamic scenes, we introduce a self-adaptive token compression mechanism in both TAS and SDC, which automatically determines the compression rate based on intrinsic scene statistics rather than manual tuning. Extensive experiments demonstrate that FluxMem achieves new state-of-the-art results on existing online video benchmarks, reaching 76.4 on StreamingBench and 67.2 on OVO-Bench under real-time settings, while reducing latency by 69.9% and peak GPU memory by 34.5% on OVO-Bench. Furthermore, it maintains strong offline performance, achieving 73.1 on MLVU while using 65% fewer visual tokens.

---

## 论文详细总结（自动生成）

以下是对论文《FluxMem: Adaptive Hierarchical Memory for Streaming Video Understanding》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大模型（MLLMs）在处理实时流式视频时面临巨大挑战。传统的离线模型预加载全量视频，而流式应用（如机器人、自动驾驶）要求模型能实时处理连续输入、记忆长时上下文并产生因果响应。
*   **研究动机**：视频中存在大量时空冗余。现有的 Token 压缩方法通常采用全局统一的剪枝策略，忽略了记忆的时间效用（近期帧需要高密度保留以供定位，远期历史可高度压缩）。此外，手动设置压缩阈值难以适应动态变化的场景。

### 2. 方法论：FluxMem 架构
FluxMem 是一个**免训练（Training-free）**的框架，核心思想是构建**自适应分层记忆结构**：
*   **分层记忆设计**：
    *   **短期记忆 (Short-term)**：保留所有原始 Token，确保对当前查询的精确感知。
    *   **中期记忆 (Mid-term)**：通过**时间相邻选择 (TAS)** 模块，对比相邻帧，移除空间对齐的冗余 Token，仅保留语义变化显著的部分。
    *   **长期记忆 (Long-term)**：通过**空间域整合 (SDC)** 模块，将每一帧内的局部重复区域聚类为代表性的“锚点（Anchors）”，进一步压缩空间冗余。
*   **关键技术细节**：
    *   **TAS 逻辑**：计算当前 Token 与前后帧 3x3 窗口内 Token 的最小余弦距离，识别“新颖”Token。
    *   **SDC 逻辑**：在保留的 Token 间构建稀疏图，利用并查集（Union-Find）合并空间邻近且相似的区域。
    *   **自适应阈值（核心亮点）**：引入图像处理中的 **Otsu 算法**，根据每帧特征的统计分布动态计算压缩阈值，无需人工调参，自动适应高速运动或静态场景。
    *   **主动触发机制**：复用 TAS 的统计数据检测场景切换，自动触发模型响应。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **在线/流式任务**：OVO-Bench（时间戳理解、主动响应）、StreamingBench（实时视觉感知与推理）。
    *   **离线长视频任务**：VideoMME、MLVU、LongVideoBench。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 1.5 Pro。
    *   **开源离线模型**：LLaVA-Video, InternVL2, Qwen2.5-VL。
    *   **开源在线模型**：VideoLLM-Online, TimeChat-Online, StreamForest 等（涵盖训练型和免训练型）。

### 4. 资源与算力
*   **算力设备**：所有实验均在 **8 × NVIDIA A100 GPU** 上进行。
*   **训练时长**：由于该方法是**免训练**的，因此不存在训练阶段，直接在推理阶段插入预训练模型（如 Qwen2.5-VL-7B）中使用。
*   **推理开销**：每帧更新的额外开销仅为 4.1ms，极具实时性优势。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流视频理解基准测试，涉及 OCR、动作识别、因果推理等数十个子维度。
*   **消融实验**：
    *   验证了短/中/长期记忆各部分的贡献。
    *   对比了不同 Token 减少策略（随机、均匀、FIFO 等）。
    *   深入分析了自适应阈值与固定阈值的性能差异。
*   **充分性评价**：实验设计非常充分且客观。通过在不同长度（短、中、长）和不同性质（在线、离线）的任务上进行测试，证明了该方法的通用性和鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在 StreamingBench (76.4) 和 OVO-Bench (67.2) 上刷新了 SOTA 记录。
*   **极高效率**：在 OVO-Bench 上将**延迟降低了 69.9%**，**峰值显存降低了 34.5%**。
*   **Token 压缩率**：在保持甚至提升精度的前提下，丢弃了 60%–70% 的视觉 Token，在极端情况下（丢弃 85%）仍能维持较高性能。
*   **自适应优势**：Otsu 算法生成的动态阈值在不同运动强度的视频中表现远超固定阈值。

### 7. 优点与亮点
*   **即插即用**：无需任何微调即可提升现有 MLLM 的流式处理能力。
*   **时空解耦压缩**：TAS 关注时间动态，SDC 关注空间结构，分层级联的设计符合视频数据的内在特性。
*   **数据驱动的自适应性**：通过统计学方法自动确定压缩强度，解决了流式视频场景多变、难以预设参数的痛点。
*   **因果一致性**：严格遵循流式处理的因果律，不依赖未来帧信息。

### 8. 不足与局限
*   **基础模型依赖**：作为免训练插件，其最终性能上限仍受限于底层预训练 MLLM（如 Qwen2.5-VL）的能力。
*   **超参数 γ 的敏感性**：虽然压缩阈值是自适应的，但主动响应触发的敏感度参数 γ 仍需手动设置。
*   **极端长时记忆**：虽然设有长期记忆层，但对于数小时以上的超长视频，简单的均值锚点（SDC）是否会丢失关键细节仍有待进一步验证。

（完）
