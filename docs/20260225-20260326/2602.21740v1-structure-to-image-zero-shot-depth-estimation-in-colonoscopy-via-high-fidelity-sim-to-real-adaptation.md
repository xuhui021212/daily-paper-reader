---
title: "Structure-to-Image: Zero-Shot Depth Estimation in Colonoscopy via High-Fidelity Sim-to-Real Adaptation"
title_zh: 结构到图像：通过高保真仿真到现实适配实现结肠镜检查中的零样本深度估计
authors: "Juan Yang, Yuyan Zhang, Han Jia, Bing Hu, Wanzhong Song"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21740v1"
tags: ["query:medvid"]
score: 9.0
evidence: 结肠镜视频中的零样本深度估计
tldr: 针对结肠镜单目深度估计中模拟与真实图像间的领域差异问题，本文提出一种“结构到图像”生成范式。该方法将深度图从被动约束转变为主动生成基础，并首次引入相位一致性与跨层级结构约束，以协同优化几何结构与血管纹理等细节。在零样本评估中，该方法显著提升了深度估计精度，为结肠镜检查提供了高保真的仿真到真实适配方案。
selection_source: fresh_fetch
motivation: 现有的图像翻译方法在处理结肠镜模拟与真实域差异时，难以平衡图像真实感与结构一致性，导致深度估计出现结构畸变。
method: 提出一种结构到图像（Structure-to-Image）范式，利用相位一致性和跨层级结构约束来协同优化宏观几何与微观纹理生成。
result: "在公开幻像数据集的零样本评估中，该方法训练的模型相比现有竞争对手在均方根误差（RMSE）上最高降低了44.18%。"
conclusion: 该研究证明了将深度图作为主动生成基础并结合相位一致性约束，能有效解决结肠镜深度估计中的域迁移难题。
---

## 摘要
结肠镜检查的单目深度估计（MDE）受到模拟图像与真实图像之间领域差异的阻碍。现有的图像到图像翻译方法将深度作为后验约束，由于无法平衡真实感与结构一致性，往往会产生结构失真和镜面高光。为了解决这一问题，我们提出了一种“结构到图像”范式，将深度图从被动约束转变为主动生成基础。我们首次将相位一致性引入结肠镜领域适配，并设计了一种跨层级结构约束，以共同优化几何结构和血管纹理等细粒度细节。在公开仿体数据集上进行的零样本评估中，基于我们生成的数据进行微调的 MDE 模型与竞争方法相比，均方根误差（RMSE）最大降低了 44.18%。我们的代码可在 https://github.com/YyangJJuan/PC-S2I.git 获取。

## Abstract
Monocular depth estimation (MDE) for colonoscopy is hampered by the domain gap between simulated and real-world images. Existing image-to-image translation methods, which use depth as a posterior constraint, often produce structural distortions and specular highlights by failing to balance realism with structure consistency. To address this, we propose a Structure-to-Image paradigm that transforms the depth map from a passive constraint into an active generative foundation. We are the first to introduce phase congruency to colonoscopic domain adaptation and design a cross-level structure constraint to co-optimize geometric structures and fine-grained details like vascular textures. In zero-shot evaluations conducted on a publicly available phantom dataset, the MDE model that was fine-tuned on our generated data achieved a maximum reduction of 44.18% in RMSE compared to competing methods. Our code is available at https://github.com/YyangJJuan/PC-S2I.git.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Structure-to-Image (S2I)** 的新范式，旨在解决结肠镜单目深度估计（MDE）中模拟数据与真实场景之间的领域差异（Domain Gap）问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：结肠镜检查中，由于缺乏真实世界的深度真值（Ground Truth），MDE 模型通常在模拟数据上训练。然而，模拟图像在纹理和光照上的低保真度导致模型在真实临床场景中泛化性极差。
*   **研究动机**：现有的图像翻译方法（如 CycleGAN 变体）通常将深度作为“后验约束”，试图在保持结构的同时增加真实感。但这往往导致**结构畸变**（如肠道褶皱变形）和**镜面伪影**，严重影响下游深度估计任务的准确性。
*   **整体含义**：论文试图通过改变生成逻辑，将深度信息从“被动约束”提升为“主动生成的基石”，从而生成既具有高真实感又严格遵循几何结构的训练数据。

### 2. 论文提出的方法论
*   **核心思想**：提出 **Structure-to-Image (S2I)** 策略。不再是简单的“图像到图像”翻译，而是训练一个生成器，以深度图（结构先验）为基础主动生成匹配的真实外观。
*   **关键技术细节**：
    *   **统一生成框架**：构建了一个包含“图像到深度（I2D）”和“深度到图像（D2I）”的双向分支框架，解决了原始模拟深度图存在的“阶梯状”数值不连续问题。
    *   **相位一致性损失 ($L_{PC}$)**：这是本文的核心创新。首次将相位一致性（Phase Congruency）引入结肠镜领域适配。相比传统的边缘检测（如 Canny、Sobel），PC 在频率域操作，对对比度不敏感，能同时精准定位宏观几何（肠腔、褶皱）和微观结构（血管纹理）。
    *   **法向一致性损失 ($L_n$)**：通过约束模拟深度与重建深度的法向量一致，进一步强化细微几何结构的对齐。
    *   **跨层级结构约束**：结合 $L_{PC}$ 和 $L_n$，协同优化图像的真实感与结构一致性。

### 3. 实验设计
*   **数据集**：
    *   **模拟/仿体**：SimCol（模拟数据集）、C3VD（高保真仿体数据集，用于零样本评估）。
    *   **真实世界**：Colon10K（公开真实数据集）、Colon-Ours（自建真实数据集，含 2,083 帧）。
*   **Benchmark 与对比方法**：
    *   **图像生成对比**：XDCycleGAN、Struct-Preserve、Sim2Real。
    *   **深度估计对比**：以 **DepthAnythingV2-small** 作为下游基础模型，对比在不同生成数据微调后的性能。
*   **评价指标**：PSNR、SSIM、IS（图像质量）；RMSE、MAE、SqRel（深度精度）。

### 4. 资源与算力
*   **硬件环境**：使用了一张 **NVIDIA RTX 4090 GPU**。
*   **训练细节**：Batch size 为 6，学习率为 $2 \times 10^{-4}$，总计训练 200 个 epoch。
*   **软件环境**：Python 3.12, PyTorch 2.5.1, CUDA 12.4。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **生成质量实验**：对比了 6 种方法（含消融版本）在 PSNR/SSIM/IS 上的表现。
    *   **零样本深度生成**：在 C3VD 数据集上直接测试生成模型的 I2D 能力。
    *   **下游任务验证**：将生成数据用于微调 SOTA 模型（DepthAnythingV2），并在 C3VD 和 Colon10K 上进行零样本推理。
    *   **消融实验**：分别验证了“相位一致性”和“法向一致性”的独立贡献。
*   **充分性评价**：实验设计较为充分，涵盖了从底层生成质量到高层临床应用任务的全流程验证。使用了第三方公开的仿体数据集（C3VD）进行零样本测试，保证了评估的客观性。

### 6. 主要结论与发现
*   **精度大幅提升**：在 C3VD 仿体数据集的零样本评估中，使用该方法生成数据微调的模型，其 **RMSE 相比 Baseline 降低了 25.95%**，相比其他竞争方法（如 Sim2Real）**最高降低了 44.18%**。
*   **结构保持能力强**：定性结果显示，该方法能有效消除镜面高光带来的结构误判，准确捕捉哈斯特拉褶皱（Haustral folds）等关键解剖结构。
*   **稳定性**：相比 XDCycleGAN，该方法在不同训练条件下表现出更强的稳定性，不易产生结构异常。

### 7. 优点
*   **范式创新**：将深度图从辅助约束转变为生成基础，从根本上缓解了 Sim-to-Real 过程中的结构失真问题。
*   **特征提取精准**：引入相位一致性（PC）是一个亮点，解决了结肠镜图像中血管等微细结构难以在跨域翻译中保持的痛点。
*   **即插即用**：生成的高保真数据可以显著提升现有单目深度估计基础模型的临床适用性。

### 8. 不足与局限
*   **预测偏差风险**：该方法在推理时依赖于预测的深度图作为先验，如果初始预测存在较大偏差，可能会引入生成误差。
*   **纹理控制力**：虽然能捕捉血管结构，但对于血管纹理的生成尚不具备完全的可控性（如特定病理特征的生成）。
*   **计算开销**：虽然文中未详述推理时间，但复杂的跨层级约束和双向分支在训练阶段可能比基础 CycleGAN 耗时更长。

（完）
