---
title: ResNet-50 with Class Reweighting and Anatomy-Guided Temporal Decoding for Gastrointestinal Video Analysis
title_zh: 结合类别重加权与解剖结构引导时间解码的 ResNet-50 胃肠道视频分析
authors: "Romil Imtiaz, Dimitris K. Iakovidis"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17784v1"
tags: ["query:medvid"]
score: 10.0
evidence: 具有解剖引导时序解码的胃肠道视频分析
tldr: 本研究提出了一种用于胃肠道视频分析的多标签识别流水线，采用ResNet-50作为基础分类器。针对严重的类别不平衡问题，引入了带裁剪的类别加权损失函数；在时序处理阶段，通过解剖结构引导的投票平滑、病理门控和迟滞解码器，解决了预测碎片化问题。该方案在挑战赛中显著提升了时序平均精度，证明了结合解剖先验与时序一致性约束的有效性。
selection_source: fresh_fetch
motivation: 旨在解决胃肠道视频分析中极端的类别不平衡以及帧级预测直接转换为事件时产生的碎片化不匹配问题。
method: 使用ResNet-50进行多标签分类，结合带裁剪的类别正样本加权损失，并采用解剖结构引导的时序解码策略进行后处理。
result: 在挑战赛测试集上，该方法将时序平均精度（mAP）从0.3801显著提升至0.4303。
conclusion: 通过类别重加权和融合解剖学先验的时序解码，能有效优化多标签内窥镜视频的分析性能和时序稳定性。
---

## 摘要
我们开发了一种基于 ResNet-50 帧分类器及后续解剖结构引导时间事件解码的多标签胃肠道视频分析流水线。该系统可从缩放至 336x336 的视频帧中预测 17 个标签，包括 5 个解剖结构类别和 12 个病理类别。主要的挑战在于严重的类别不平衡，尤其是罕见的病理标签。为解决此问题，我们在训练损失函数中采用了截断的类别正样本加权（clipped class-wise positive weighting），在保持优化稳定性的同时提升了对罕见类别的学习能力。在时间处理阶段，我们发现直接将帧转换为事件会导致与官方标准答案（ground truth）产生碎片化的不匹配。因此，最终提交方案结合了 GT 风格的逐帧事件合成、解剖结构投票平滑、基于解剖结构的病理门控以及保守的滞后解码器（hysteresis decoder）。这一设计使挑战赛测试集上的最终时间 mAP 从 0.3801 提升至 0.4303。

## Abstract
We developed a multi-label gastrointestinal video analysis pipeline based on a ResNet-50 frame classifier followed by anatomy-guided temporal event decoding. The system predicts 17 labels, including 5 anatomy classes and 12 pathology classes, from frames resized to 336x336. A major challenge was severe class imbalance, particularly for rare pathology labels. To address this, we used clipped class-wise positive weighting in the training loss, which improved rare-class learning while maintaining stable optimization. At the temporal stage, we found that direct frame-to-event conversion produced fragmented mismatches with the official ground truth. The final submission therefore combined GT-style framewise event composition, anatomy vote smoothing, and anatomy-based pathology gating with a conservative hysteresis decoder. This design improved the final temporal mAP from 0.3801 to 0.4303 on the challenge test set.

---

## 论文详细总结（自动生成）

以下是对论文《ResNet-50 with Class Reweighting and Anatomy-Guided Temporal Decoding for Gastrointestinal Video Analysis》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在胃肠道（GI）内窥镜视频分析中，如何解决**极端类别不平衡**（罕见病理样本极少）以及**帧级预测与时序事件边界不一致**的问题。
*   **研究背景**：自动化的视频分析对辅助诊断和病变筛查至关重要。然而，现有的模型往往在单帧识别上表现尚可，但在需要精确定位事件起止时间的时序评估（如 ICPR 2026 RARE-VISION 挑战赛）中表现不佳，容易产生碎片化的预测结果。

### 2. 方法论
该研究提出了一套从帧级分类到时序解码的完整流水线：
*   **核心架构**：采用 **ResNet-50** 作为骨干网络，输入分辨率为 336×336，输出 17 个标签（5 类解剖结构，12 类病理）。
*   **类别重加权（Clipped Class-wise Weighting）**：
    *   针对罕见病理，在加权二元交叉熵损失（Weighted BCE）中引入正样本权重 $w_c$（负样本数/正样本数）。
    *   为防止梯度爆炸和优化不稳定，对权重进行**截断处理**（范围限制在 1.0 到 50.0 之间）。
*   **解剖结构引导的时序解码**：
    *   **解剖投票平滑**：在局部窗口内进行多数投票，消除解剖结构预测的瞬时跳变。
    *   **解剖病理门控**：利用医学先验知识，若当前帧预测的解剖部位不可能出现某种病理（如食管病理出现在大肠），则强制将该病理概率置零。
    *   **GT 风格事件合成**：模拟官方标注逻辑，当活跃标签集发生变化时即开启新事件，减少预测碎片化。
    *   **迟滞解码器（Hysteresis Decoder）**：使用保守的阈值策略将概率序列转换为时间段。

### 3. 实验设计
*   **数据集**：ICPR 2026 RARE-VISION 挑战赛数据集。
*   **任务类型**：多标签视频事件检测（17 类）。
*   **Benchmark**：以直接进行帧到事件转换的基准流水线作为对比对象。
*   **评估指标**：mAP@0.5 和 mAP@0.95（时间平均精度）。
*   **对比场景**：在 5 折交叉验证上评估帧级性能，并在三个特定的测试视频（ukdd navi 00051/00068/00076）上评估时序性能。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量、训练时长或内存消耗等硬件细节。

### 5. 实验数量与充分性
*   **实验规模**：进行了 5 折交叉验证以确保帧级分类的稳健性（达到 0.8605 mAP）。
*   **消融/对比实验**：对比了基准流水线与加入“解剖平滑+病理门控+GT风格合成”后的性能差异。
*   **充分性评价**：实验设计较为客观，针对挑战赛的特定评估机制（IoU 匹配）进行了深入的调试分析，证明了各模块对最终 mAP 提升的贡献。但由于是竞赛论文，对比的方法主要局限于自身流水线的改进，缺乏与其他先进时序模型（如 SlowFast 或 Transformer-based 视频模型）的横向对比。

### 6. 主要结论与发现
*   **帧级与时序的脱节**：强大的帧级分类器（0.86 mAP）并不等同于强大的时序检测器（初始仅 0.38 mAP），时序一致性和边界对齐是关键。
*   **解剖先验的价值**：通过解剖结构对病理预测进行“门控”约束，能有效减少解剖学上不可能出现的假阳性。
*   **稳定性胜过激进**：相比于复杂的 HMM/Viterbi 解码，结合了医学先验的保守迟滞解码器在未见视频上表现出更好的泛化性。

### 7. 优点
*   **实用性强**：针对医疗视频的领域特性（解剖结构约束）设计了专门的后处理逻辑。
*   **解决不平衡问题**：通过截断权重平衡了罕见类别的召回率与训练稳定性。
*   **性能提升显著**：在测试集上将时序 mAP 从 0.3801 提升至 0.4303，提升幅度达 13% 以上。

### 8. 不足与局限
*   **时序建模深度不足**：主要依赖后处理和手工规则（门控、投票），而非端到端的时序特征学习（如使用 3D 卷积或长短期记忆网络）。
*   **超参数依赖**：权重截断范围（1-50）和投票窗口大小等超参数可能需要针对不同数据集重新调整。
*   **应用限制**：解剖门控依赖于准确的解剖分类，如果解剖结构预测错误，会连锁导致病理预测被错误抑制。

（完）
