---
title: "Symphony: A Cognitively-Inspired Multi-Agent System for Long-Video Understanding"
title_zh: Symphony：一种受认知启发的长视频理解多智能体系统
authors: "Haiyang Yan, Hongyun Zhou, Peng Xu, Xiaoxue Feng, Mengyi Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17307v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于长视频理解与推理的多智能体系统
tldr: 针对长视频理解（LVU）中信息密度高和时间跨度大的挑战，本文提出了受人类认知启发的名为 Symphony 的多智能体系统。该系统通过细粒度的任务分解、增强反思的深度推理协作机制，以及基于视觉语言模型（VLM）的定位方法，有效提升了对复杂长视频的推理和关键片段定位能力。实验证明 Symphony 在多个主流长视频基准测试中达到 SOTA 水平，显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有的多模态大模型智能体在处理长视频时，简单的任务分解和嵌入式检索难以应对复杂的长链推理及隐性意图定位。
method: 提出 Symphony 系统，通过模拟人类认知模式进行细粒度任务分解，并结合反思机制的深度推理协作与基于 VLM 的视频片段定位。
result: "在 LVBench、LongVideoBench、VideoMME 和 MLVU 等多个基准测试中取得领先，其中在 LVBench 上比现有最优方法提升了 5.0%。"
conclusion: Symphony 证明了结合认知启发式分解与增强型定位机制能显著提升长视频理解任务的性能。
---

## 摘要
尽管多模态大语言模型（MLLM）智能体发展迅速且应用广泛，但在处理具有高信息密度和长跨度时间特征的长视频理解（LVU）任务时仍面临挑战。最近关于 LVU 智能体的研究表明，简单的任务分解和协作机制不足以应对长链推理任务。此外，通过基于嵌入的检索直接减少时间上下文可能会丢失复杂问题的关键信息。在本文中，我们提出了 Symphony，一个多智能体系统，旨在缓解这些局限性。通过模拟人类认知模式，Symphony 将 LVU 分解为细粒度的子任务，并结合了由反思增强的深度推理协作机制，有效提升了推理能力。此外，Symphony 提供了一种基于视觉语言模型（VLM）的定位方法来分析 LVU 任务并评估视频片段的相关性，这显著增强了定位具有隐性意图和长跨度时间复杂问题的能力。实验结果表明，Symphony 在 LVBench、LongVideoBench、VideoMME 和 MLVU 上均达到了最先进的性能，在 LVBench 上比之前的最优方法提升了 5.0%。代码可在 https://github.com/Haiyang0226/Symphony 获取。

## 速览
**TLDR**：针对长视频理解（LVU）中信息密度高、时间跨度大及推理链条长等挑战，本文提出了一种受人类认知启发的名为Symphony的多智能体系统。该系统通过细粒度的任务分解、增强反思的深度推理协作机制以及基于VLM的定位方法，有效提升了对复杂长视频的理解与推理能力，在多个主流基准测试中达到SOTA水平。 \
**Motivation**：现有的多模态大模型智能体在处理长视频时，简单的任务分解和基于嵌入的检索难以应对复杂的长链推理及关键信息定位。 \
**Method**：提出Symphony系统，通过模拟人类认知模式进行细粒度子任务分解，并结合反思机制的深度推理协作与基于VLM的视频片段定位。 \
**Result**：在LVBench、LongVideoBench、VideoMME和MLVU等多个榜单上取得领先，其中在LVBench上比此前最优方法提升了5.0%。 \
**Conclusion**：Symphony通过认知启发的协作与精准定位机制，显著增强了智能体在长视频理解任务中的推理深度与准确性。

---

## Abstract
Despite rapid developments and widespread applications of MLLM agents, they still struggle with long-form video understanding (LVU) tasks, which are characterized by high information density and extended temporal spans. Recent research on LVU agents demonstrates that simple task decomposition and collaboration mechanisms are insufficient for long-chain reasoning tasks. Moreover, directly reducing the time context through embedding-based retrieval may lose key information of complex problems. In this paper, we propose Symphony, a multi-agent system, to alleviate these limitations. By emulating human cognition patterns, Symphony decomposes LVU into fine-grained subtasks and incorporates a deep reasoning collaboration mechanism enhanced by reflection, effectively improving the reasoning capability. Additionally, Symphony provides a VLM-based grounding approach to analyze LVU tasks and assess the relevance of video segments, which significantly enhances the ability to locate complex problems with implicit intentions and large temporal spans. Experimental results show that Symphony achieves state-of-the-art performance on LVBench, LongVideoBench, VideoMME, and MLVU, with a 5.0% improvement over the prior state-of-the-art method on LVBench. Code is available at https://github.com/Haiyang0226/Symphony.

---

## 论文详细总结（自动生成）

以下是对论文《Symphony: A Cognitively-Inspired Multi-Agent System for Long-Video Understanding》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：长视频理解（LVU）面临信息密度极高、时间跨度大以及推理链条长等挑战。现有的多模态大模型（MLLM）在处理超长输入时，指令遵循和推理能力会显著下降。
*   **研究背景**：
    *   **检索局限性**：传统的基于嵌入（Embedding）的检索（RAG）在处理复杂、含糊或需要多步推理的问题时，往往无法精准定位关键片段。
    *   **推理瓶颈**：单智能体系统在任务复杂度超过模型上限时，倾向于采取简单行动而非深度推理。
    *   **协作效率**：现有的多智能体系统多采用线性流水线，限制了对复杂解空间的探索。

### 2. 论文提出的方法论
Symphony 是一个受人类认知启发的多智能体系统，其核心思想是将复杂的视频理解任务分解为功能化的认知维度。
*   **核心架构（五大智能体）**：
    1.  **规划智能体 (Planning Agent)**：作为核心协调者，负责全局任务规划、智能体调度和信息整合。
    2.  **定位智能体 (Grounding Agent)**：模拟“注意力”机制。利用 LLM 对查询进行意图识别和语义扩展，并结合 VLM 进行相关性评分（而非仅依赖 CLIP 检索），精准识别关键视频段。
    3.  **字幕智能体 (Subtitle Agent)**：负责语言处理，进行实体识别、情感分析和主题建模。
    4.  **视觉感知智能体 (Visual Perception Agent)**：提供帧检查、全局摘要和多片段对比分析工具。
    5.  **反思智能体 (Reflection Agent)**：模拟“自我监督”。对推理轨迹进行回顾性评估，发现逻辑矛盾或证据不足时，生成批评建议并触发新一轮推理。
*   **协作机制**：采用“反思增强的动态协作”流程（类似 Actor-Critic 框架）。规划智能体动态调用其他智能体积累证据，反思智能体验证结果，通过多轮迭代优化答案。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **LVBench**：平均时长 68 分钟，侧重时间定位、总结和推理。
    *   **LongVideoBench**：评估细粒度信息检索和跨片段逻辑推理。
    *   **Video-MME (Long)**：评估时空复合推理能力。
    *   **MLVU**：涵盖推理、总结、识别等 9 类任务。
*   **对比方法**：
    *   **商业模型**：Gemini-1.5-Pro, GPT-4o, OpenAI o3。
    *   **开源模型**：InternVL2.5-78B, Qwen2.5-VL-72B。
    *   **智能体框架**：VideoTree, VideoAgent, DVD, VideoDeepResearch, LvAgent 等。

### 4. 资源与算力
*   **模型使用**：
    *   规划与反思智能体：**DeepSeek R1**。
    *   字幕智能体：**DeepSeek V3**。
    *   视觉感知与定位智能体：**Doubao Seed 1.6 VL**。
*   **算力/成本说明**：文中未明确给出具体的 GPU 训练时长（因为该系统主要基于现有的基础模型进行推理协作）。但在成本分析中提到，在 LVBench 上平均每条查询消耗约 22 万个 Token，成本约为 **0.124 美元**，比使用 OpenAI o3 的系统（0.213 美元）降低了 41.8%。

### 5. 实验数量与充分性
*   **实验规模**：在四个主流长视频基准测试上进行了全面评估。
*   **消融实验**：针对每个智能体（规划、字幕、视觉、反思）的贡献、定位策略（VLM 评分 vs. CLIP 检索）、采样方法（FPS、片段长度）以及投票策略进行了详细的对比实验。
*   **充分性评价**：实验设计非常充分且客观。通过在不同基础模型（如 Qwen2.5-VL, Seed 1.6VL）上运行 Symphony，证明了性能提升源于系统架构设计而非单一基础模型的强大。

### 6. 主要结论与发现
*   **性能领先**：Symphony 在所有基准测试中均达到 SOTA。在最难的 LVBench 上，比之前的最优方法（DVD）提升了 **5.0%**。
*   **定位关键**：基于 VLM 的相关性评分显著优于传统的 CLIP 检索，尤其在处理抽象概念和长跨度动作时。
*   **反思有效**：引入独立的反馈/反思智能体比智能体自我反思能多带来 2.5% 的性能提升，有效缓解了模型的“过度自信”问题。

### 7. 优点
*   **认知启发式设计**：将任务按功能维度（而非模态维度）解耦，降低了单个智能体的认知负荷，减少了模块间的耦合。
*   **动态协作**：相比线性流水线，动态调度和反思机制允许系统更灵活地探索解空间。
*   **精准定位**：通过 LLM 扩展查询意图并结合 VLM 深度理解，解决了长视频中“大海捞针”的难题。

### 8. 不足与局限
*   **推理延迟**：由于涉及多智能体多轮交互和 VLM 并行评分，系统的端到端推理时间可能较长（虽然文中通过并行化进行了优化）。
*   **成本挑战**：尽管比闭源模型便宜，但每条查询消耗 22 万个 Token 对于大规模实时应用仍具有较高的成本门槛。
*   **依赖性**：系统的表现高度依赖于底层推理模型（如 DeepSeek R1）的逻辑能力和视觉模型（如 Seed 1.6 VL）的感知精度。

（完）
