---
title: "StreamReady: Learning What to Answer and When in Long Streaming Videos"
title_zh: StreamReady：在长流式视频中学习回答内容与时机
authors: "Shehreen Azad, Vibhav Vineet, Yogesh Singh Rawat"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08620v1"
tags: ["query:lvu"]
score: 9.0
evidence: 流式长视频理解的就绪感知公式
tldr: 针对长流式视频理解中回答时机的重要性，本文提出了Answer Readiness Score (ARS) 评价指标，通过非对称惩罚机制衡量回答的及时性。同时，开发了StreamReady框架，利用轻量级就绪机制判断证据是否充分以决定回答时机。此外，推出了ProReady-QA基准测试。实验表明，StreamReady在多个长视频基准上表现优异，实现了准确性与时效性的平衡。
selection_source: fresh_fetch
motivation: 现有的流式视频理解模型缺乏对回答时机的考量，导致回答可能因过早而流于猜测或因过晚而失去实时价值。
method: 提出了衡量回答时机准确性的ARS指标，并构建了包含轻量级就绪判断机制的StreamReady框架。
result: StreamReady在ProReady-QA及其他8个长视频基准测试中均取得了优异表现，证明了其强大的泛化能力。
conclusion: 该研究通过引入就绪感知机制，成功实现了流式视频中时间推理与准时回答的统一，提升了模型的实时理解效能。
---

## 摘要
流式视频理解通常涉及时间敏感的场景，模型需要在支持性视觉证据出现时准确地做出回答：在证据出现前回答反映了推测，而在证据过后回答则会降低实时效用。为了捕捉这种行为，我们引入了一种具备就绪意识（readiness-aware）的流式视频理解形式化方法，并提出了回答就绪分数（ARS），这是一种具有非对称早到和迟到惩罚的时间感知目标。当与正确性结合时，ARS 定义了一种有效准确率，不仅衡量模型是否正确，还衡量其是否在合适的时刻回答。基于这一形式化方法，我们提出了 StreamReady 框架，通过一种轻量级的就绪机制将时间推理与准时回答统一起来，该机制在响应前判断是否已观察到足够的证据。为了评估这一能力，我们进一步引入了 ProReady-QA 基准，它包含标注的回答证据窗口，以及涵盖局部和全局上下文的主动多轮问题。StreamReady 在 ProReady-QA 上取得了优异的性能，并在另外八个流式和离线长视频基准测试中一致优于现有方法，展示了鲁棒且具有广泛泛化能力的视频理解能力。

## 速览
**TLDR**：针对长流式视频理解中回答时机的重要性，本文提出了Answer Readiness Score (ARS) 评价指标，通过非对称惩罚机制衡量回答的及时性。同时，开发了StreamReady框架，利用轻量级就绪机制判断证据是否充分以决定回答时机。此外，推出了ProReady-QA基准测试。实验表明，StreamReady在多个长视频基准上表现优异，实现了准确性与时效性的平衡。 \
**Motivation**：现有的流式视频理解模型往往忽略了回答时机的重要性，过早回答会导致猜测，过晚则降低实时实用性。 \
**Method**：提出了ARS评分标准和StreamReady框架，通过轻量级机制判断观察到的证据是否足以支撑回答。 \
**Result**：StreamReady在ProReady-QA及其他8个长视频基准测试中均取得了领先性能，展现了强大的泛化能力。 \
**Conclusion**：该研究通过引入就绪感知机制，有效统一了流式视频中的时间推理与准时回答，提升了实时视频理解的实用价值。

---

## Abstract
Streaming video understanding often involves time-sensitive scenarios where models need to answer exactly when the supporting visual evidence appears: answering before the evidence reflects speculation, answering after it has passed reduces real-time utility. To capture this behavior, we introduce a readiness-aware formulation of streaming video understanding with the Answer Readiness Score (ARS), a timing-aware objective with asymmetric early and late penalties. When combined with correctness, ARS defines an effective accuracy that measures not just whether a model is right, but whether it answers at the appropriate moment. Building on this formulation, we introduce StreamReady, a framework to unify temporal reasoning with on-time answering through a lightweight readiness mechanism that decides if sufficient evidence has been observed before responding. To evaluate this capability, we further introduce ProReady-QA, a benchmark with annotated answer evidence windows and proactive multi-turn questions across local and global contexts. StreamReady achieves superior performance on ProReady-QA, and consistently outperforms prior methods across eight additional streaming and offline long-video benchmarks, demonstrating robust and broadly generalizable video understanding capability.

---

## 论文详细总结（自动生成）

以下是对论文《StreamReady: Learning What to Answer and When in Long Streaming Videos》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在流式长视频理解中，模型不仅需要知道“回答什么”（准确性），还需要知道“何时回答”（时效性）。
*   **研究背景**：现有的多模态大模型（MLLM）多为离线处理，或在流式设置下仅关注回答的正确性。在实时场景（如监控、机器人、辅助系统）中，问题往往先于证据出现（主动推理）。过早回答会导致凭空猜测（幻觉），过晚回答则失去实时价值。
*   **动机**：缺乏一种能够同时评估回答正确性和时机准确性的机制，以及缺乏具备这种“就绪感知”能力的模型框架。

### 2. 方法论
*   **核心思想**：引入“就绪感知”（Readiness-aware）公式，通过轻量级机制监控证据流，仅在证据充分时触发回答。
*   **关键技术细节**：
    *   **分层存储（Visual Memory Tree）**：构建三层视觉记忆（原始帧、质心、原型），通过自适应聚类压缩冗余，保留长程细粒度细节。
    *   **上下文记忆库（Contextual Memory Bank）**：存储历史问答对的语义表示，支持多轮对话推理。
    *   **查询感知推理**：采用双分支 Q-Former，通过“粗到细”的检索机制，根据当前问题从记忆树中提取相关证据。
    *   **就绪机制（Readiness Mechanism）**：在推理模块中嵌入一个可学习的 `<RDY>` 标记，由轻量级“就绪头”监控。当该标记的得分超过阈值时，触发 LLM 生成答案。
*   **评价指标（ARS）**：
    *   **Answer Readiness Score (ARS)**：采用非对称惩罚机制。**早到惩罚（Early Penalty）**较严厉，打击无证据的猜测；**迟到惩罚（Late Penalty）**较温和，容忍轻微延迟。
    *   **有效准确率（Effective Accuracy）**：定义为原始准确率与 ARS 的乘积。

### 3. 实验设计
*   **数据集与基准**：
    *   **ProReady-QA（本文提出）**：包含 5000 个主动式问答对，涵盖 5 类任务（步骤识别、重复事件计数、线索触发响应、因果检测、目标状态检测），并标注了精确的证据时间窗口。
    *   **流式基准**：StreamingBench, OVOBench, OVBench, VStream-QA。
    *   **离线长视频基准**：VideoMME, MLVU, MVBench, EgoSchema。
*   **对比方法**：包括离线模型（InternVL2, LLaVA-OneVision, Qwen-2-VL 等）和在线流式模型（VideoLLM-online, StreamBridge, ViSpeak, InfiniPot-V 等）。

### 4. 资源与算力
*   **模型骨干**：使用 Qwen-2 VL (7B) 作为基础模型。
*   **训练配置**：微调推理模块和就绪头，训练 5 个 epoch，学习率为 2e-5，使用 AdamW 优化器。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到了推理延迟和内存占用随视频长度增加保持稳定，具有良好的扩展性。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个不同的基准测试（5 个流式，4 个离线）上进行了广泛评估。
*   **消融实验**：针对记忆库组成、就绪机制设计、`<RDY>` 标记位置、惩罚因子敏感度等进行了详尽的消融研究。
*   **客观性**：通过引入 ARS 指标，填补了以往研究中对“回答时机”评估的空白，对比实验涵盖了当前最先进的开源模型，评估过程较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：StreamReady 在 ProReady-QA 上显著优于现有方法，ARS 得分提升约 9%，有效准确率大幅领先。
*   **泛化能力**：在不考虑回答时机的传统流式和离线长视频任务中，StreamReady 依然保持了顶尖的准确率，证明其分层记忆和检索机制的有效性。
*   **效率优势**：相比于需要重复压缩或维护庞大 KV 缓存的方法，StreamReady 的固定大小分层记忆显著降低了长视频处理的延迟和显存压力。

### 7. 优点与亮点
*   **首创性**：首次正式定义了流式视频理解中的“就绪感知”问题，并提出了科学的非对称评价指标 ARS。
*   **设计优雅**：`<RDY>` 标记与推理过程深度耦合，确保了“何时回答”是基于“看到了什么证据”做出的决策。
*   **实用性强**：分层记忆结构解决了长视频流处理中的计算瓶颈，适合实时部署。

### 8. 不足与局限
*   **不可回答性（Unanswerability）**：虽然论文简要测试了模型对无关问题的抑制能力，但尚未建立完善的“不可回答”处理机制。
*   **监督信号依赖**：就绪机制的训练依赖于伪标签（基于推理表示与记忆的相似度），虽然有效，但在极端复杂场景下可能存在噪声。
*   **模态局限**：目前主要关注视觉流，未充分整合音频流等其他实时模态信息。

（完）
