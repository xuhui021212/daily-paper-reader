---
title: "VISTA: Validation-Guided Integration of Spatial and Temporal Foundation Models with Anatomical Decoding for Rare-Pathology VCE Event Detection"
title_zh: VISTA：结合解剖学解码的验证引导型时空基础模型集成，用于罕见病理 VCE 事件检测
authors: "Bo-Cheng Qiu, Yu-Fan Lin, Yu-Zhe Pien, Chia-Ming Lee, Fu-En Yang, Yu-Chiang Frank Wang, Chih-Chung Hsu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18343v1"
tags: ["query:medvid"]
score: 10.0
evidence: 利用时序模型在长胶囊内窥镜视频流中进行事件检测
tldr: 针对胶囊内镜（VCE）中罕见病理事件检测的挑战，本文提出VISTA框架。该方法将RARE-VISION任务定义为度量对齐的事件检测问题，通过集成EndoFM-LV（时序）和DINOv3（空间）两个基础模型，并结合验证引导的分层融合与解剖感知的时间解码技术，有效解决了长视频中病理稀疏且异构的问题，显著提升了事件级检测精度。
selection_source: fresh_fetch
motivation: 胶囊内镜视频中病理发现稀疏且视觉异构，传统逐帧分类难以满足实际的事件级评估需求。
method: 结合时空基础模型，利用验证集引导的权重融合及解剖约束的时间解码器，实现从帧预测到稳定事件生成的转化。
result: "在官方隐藏测试集上，该方法实现了0.3530的mAP@0.5和0.3235的mAP@0.95，验证了各模块的有效性。"
conclusion: 通过整合多模态基础模型与解剖学先验，VISTA为长视频中的罕见医疗事件检测提供了一种鲁棒且高效的解决方案。
---

## 摘要
胶囊内镜事件检测具有挑战性，因为具有诊断相关性的发现非常稀疏、视觉异质性强，且嵌入在冗长、多噪声的视频流中，而评估是在事件层面而非仅通过帧准确率进行的。因此，我们将 RARE-VISION 任务表述为一个指标对齐的事件检测问题，而非纯粹的逐帧分类任务。我们的框架结合了两个互补的主干网络：用于局部时序上下文的 EndoFM-LV 和用于强帧级视觉语义的 DINOv3 ViT-L/16，随后采用了多样化头部集成、验证引导的分层融合以及解剖感知时序事件解码。融合阶段使用基于验证集的类别级模型加权、主干加权和概率校准；解码阶段则应用时序平滑、解剖约束、阈值细化和逐标签事件生成，以产生稳定的事件预测。验证消融实验表明，互补的主干网络、验证引导的融合以及解剖感知时序解码均对事件级性能有所贡献。在官方隐藏测试集上，所提方法实现了 0.3530 的总体时序 mAP@0.5 和 0.3235 的时序 mAP@0.95。

## Abstract
Capsule endoscopy event detection is challenging because diagnostically relevant findings are sparse, visually heterogeneous, and embedded in long, noisy video streams, while evaluation is performed at the event level rather than by frame accuracy alone. We therefore formulate the RARE-VISION task as a metric-aligned event detection problem instead of a purely frame-wise classification task. Our framework combines two complementary backbones, EndoFM-LV for local temporal context and DINOv3 ViT-L/16 for strong frame-level visual semantics, followed by a Diverse Head Ensemble, Validation-Guided Hierarchical Fusion, and Anatomy-Aware Temporal Event Decoding. The fusion stage uses validation-derived class-wise model weighting, backbone weighting, and probability calibration, while the decoding stage applies temporal smoothing, anatomical constraints, threshold refinement, and per-label event generation to produce stable event predictions. Validation ablations indicate that complementary backbones, validation-guided fusion, and anatomy-aware temporal decoding all contribute to event-level performance. On the official hidden test set, the proposed method achieved an overall temporal mAP@0.5 of 0.3530 and temporal mAP@0.95 of 0.3235.

---

## 论文详细总结（自动生成）

以下是对论文《VISTA: Validation-Guided Integration of Spatial and Temporal Foundation Models with Anatomical Decoding for Rare-Pathology VCE Event Detection》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：胶囊内镜（VCE）视频中的病理发现具有**稀疏性**（极少出现）、**视觉异构性**（受气泡、光照、模糊干扰）以及**长视频背景**。
*   **研究动机**：传统的逐帧分类方法在处理此类任务时，往往会导致预测结果在时间上破碎、解剖学逻辑不一致。此外，RARE-VISION 挑战赛采用的是基于**时间 mAP（事件级）**而非帧准确率的评估标准。因此，需要一种能够对齐评估指标、利用时空上下文并遵循解剖学约束的检测框架。

### 2. 方法论
VISTA 框架将任务定义为“指标对齐的事件检测问题”，主要包含以下四个阶段：
*   **双主干特征提取（Dual-Backbone）**：
    *   **EndoFM-LV**：提取 4 帧片段的局部时序上下文特征。
    *   **DINOv3 ViT-L/16**：提取强力的帧级全局视觉语义特征。
*   **多样化头部集成（DHE）**：在每个主干上训练 5 个轻量化分类头，采用不同的架构（Linear/MLP）和损失函数（BCE、Focal Loss、Asymmetric Loss）以应对类别不平衡。
*   **验证引导的分层融合（VGHF）**：
    *   **模型级融合**：根据验证集上的类别平均精度（AP）分配权重。
    *   **主干级融合**：根据验证集帧级 mAP 进行加权。
    *   **概率校准**：通过温度缩放（Temperature Scaling）优化置信度。
*   **解剖感知时序事件解码（ATED）**：
    *   **时序平滑**：对解剖类和病理类应用不同窗口的平滑处理。
    *   **解剖约束**：强制执行解剖区域互斥原则，并遵循消化道单向传输顺序（口→结肠），抑制不合理的反向跳转。
    *   **事件生成**：采用逐标签（Per-label）解码而非元组分割，以符合时间 IoU 评估逻辑。

### 3. 实验设计
*   **数据集**：使用官方提供的 Galar 数据集，包含 80 个带标注的开发视频和 3 个隐藏测试视频。
*   **Benchmark**：ICPR 2026 RARE-VISION 挑战赛官方评估协议。
*   **对比实验**：
    *   对比了单主干（仅 EndoFM 或仅 DINOv3）与双主干融合的效果。
    *   对比了不同融合策略（均匀加权 vs. 验证引导加权）。
    *   对比了不同解码策略（无处理 vs. 元组分割 vs. 逐标签解码）。
*   **评估指标**：时间 mAP@0.5 和 mAP@0.95。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练时长。
*   **实现细节**：提到了使用预提取的 DINOv3 特征和在线提取的 EndoFM 特征，并提供了 GitHub 代码仓库链接。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了 7 组主要的消融实验（见表 1），涵盖了主干选择、融合策略和解码逻辑的组合。
*   **充分性**：实验设计较为合理，通过消融实验清晰地展示了每个模块（尤其是解剖约束和验证引导融合）对最终 mAP 的提升作用。
*   **客观性**：使用了官方隐藏测试集进行最终验证，结果具有一定的客观性，但也暴露出验证集与测试集之间存在性能差距（Gap）。

### 6. 主要结论与发现
*   **时空互补性**：DINOv3 提供了强大的视觉特征，而 EndoFM 补充了必要的时序动态信息，两者结合优于单一模型。
*   **推理设计至关重要**：在事件级检测任务中，后处理（时序平滑、解剖约束）对性能的提升甚至超过了模型本身的改进。
*   **解剖学先验的价值**：引入消化道传输的单向性约束能有效过滤解剖学上不可能出现的误报。

### 7. 优点
*   **指标对齐**：针对事件级评估指标（mAP）设计了逐标签解码逻辑，而非传统的语义分割逻辑。
*   **解剖学集成**：将医学常识（如器官顺序、互斥性）转化为硬性约束，增强了模型的鲁棒性。
*   **基础模型应用**：成功将最新的视觉基础模型（DINOv3）和内镜专用模型（EndoFM）集成到罕见病检测任务中。

### 8. 不足与局限
*   **泛化风险**：融合参数和阈值高度依赖于特定的验证集划分，可能导致在面对分布偏移较大的新数据时性能下降（如测试集表现明显低于验证集）。
*   **时序建模较弱**：消融实验显示 DINOv3（空间）的表现远优于 EndoFM（时序），说明目前的时序特征提取仍有提升空间。
*   **端到端性不足**：框架包含多个手工设计的后处理步骤和阈值搜索过程，系统复杂度较高，不够简洁。

（完）
