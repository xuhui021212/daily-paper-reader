---
title: CT-Guided Spatially-varying Regularization for Voxel-Wise Deformable Whole-Body PET Registration
title_zh: CT引导的空间变分正则化用于体素级可变形全身PET配准
authors: "Xiangcen Wu, Ruohua Chen, Sichun Li, Qianye Yang, Sheng Liu, Jianjun Liu, Zhaoheng Xie"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22905v1"
tags: ["query:medvid"]
score: 9.0
evidence: 全身PET配准的空间变化正则化
tldr: 针对全身PET图像配准中解剖结构异质性导致的变形不合理问题，本文提出一种CT引导的空间可变正则化策略。该方法利用配对的CT图像构建体素级正则化图，取代传统的全局权重，使骨骼等刚性结构获得强约束，而软组织保持灵活性。在296名患者的跨示踪剂PET/CT数据集上的实验表明，该方法显著提升了全身及器官层面的配准精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22905v1/fig-001.webp\", \"caption\": \"Fig. 3. Left: PET images; right: corresponding segmentation masks. The fixed image is the PSMA-PET scan, and the moving image is the FDG-PET scan. Warped moving results from the baseline and proposed methods are shown for visual comparison. Insets (a)–(c) highlight representative regions of interest.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.22905v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the training process of the proposed framework. Blue and green boxes denote the 18F-PSMA and 18F-FDG PET/CT images and corresponding segmentation masks, respectively. Images sharing the same blue or green border are spatially aligned. Red dashed lines indicate operations used only during training. During testing, only PET images are used as input.\", \"page\": 4, \"index\": 2, \"width\": 725, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.22905v1/fig-003.webp\", \"caption\": \"Fig. 4. Boxplots of organ segmentation mask Dice scores after registration for different methods. Statistical significance markers compare γ = 2 against the baseline: ** denotes p < 0.001 and * denotes p < 0.05.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2604.22905v1/fig-004.webp\", \"caption\": \"Table 1. Comparison of baseline and proposed methods across different µr values (mean ± std, n = 80). Proposed results are shown in bold only when they are both statistically significant (paired t-test, p < 0.05) and better than the baseline.\", \"page\": 7, \"index\": 4, \"width\": 721, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.22905v1/fig-005.webp\", \"caption\": \"Fig. 1. Axial views from the same patient with different tracer PET/CT scans (18FPSMA: blue, 18F-FDG: green). PET and CT are spatially aligned within each scan session, but the two PET/CT are not aligned to each other due to scan anatomical variation.\", \"page\": 3, \"index\": 5, \"width\": 725, \"height\": 184}]"
motivation: 传统的全局正则化权重无法兼顾全身配准中刚性骨骼的稳定性与软组织的高柔性需求。
method: 利用PET/CT中的CT图像生成体素级正则化图，为不同解剖区域提供自适应的变形场约束强度。
result: 在包含296例患者的跨示踪剂临床数据集上，该方法在全身配准性能和器官对齐方面均优于弱监督基准模型。
conclusion: CT引导的空间可变正则化能有效处理全身解剖异质性，是提升跨示踪剂PET图像配准质量的有效手段。
---

## 摘要
全身正电子发射断层扫描（PET）配准对于多参数肿瘤表征和转移性疾病进展评估至关重要。在基于深度学习的可变形配准中，密集位移场（DDF）正则化项对于稳定优化和防止大型3D体积中出现不切实际的变形至关重要。全身可变形配准的一个关键挑战是解剖异质性，刚性结构（如骨骼）应接受更强的正则化，而软组织则需要更灵活的变形和更弱的约束。在这项工作中，我们提出了一种简单而有效的CT引导空间变分正则化策略，用于全身跨示踪剂可变形PET配准。其核心思想是利用PET/CT采集中的配对CT体积来构建DDF的体素级正则化图，取代传统的单一全局正则化权重。这在刚性结构和软组织之间产生了具有解剖适应性的正则化强度。该方法在包含18F-PSMA和18F-FDG的296名患者的真实临床跨示踪剂PET/CT数据集上进行了评估，结果表明，该方法在全身配准性能和器官级对齐方面均比弱监督配准基准取得了显著的统计学改进。

## Abstract
Whole-body Positron Emission Tomography (PET) registration is essential for multi-parametric tumor characterization and assessment of metastatic disease progression. In deep learning-based deformable registration, the dense displacement field (DDF) regularizer is crucial for stabilizing optimization and preventing unrealistic deformations in large 3D volumes. A key challenge in whole-body deformable registration is anatomical heterogeneity, rigid structures (e.g., bones) should undergo stronger regularization, whereas soft tissues require more flexible deformation and weaker constraints. In this work, we propose a simple yet effective CT-guided spatially-varying regularization strategy for whole-body cross-tracer deformable PET registration. The key idea is to use the paired CT volume from the PET/CT acquisition to construct a voxel-wise regularization map for the DDF, replacing the conventional single global regularization weight. This yields anatomy-adaptive regularization strength across rigid and soft tissues. The proposed method is evaluated on a real clinical cross-tracer PET/CT dataset of 296 patients involving 18F-PSMA and 18F-FDG, showing that the proposed method achieves statistically significant improvements over weakly-supervised registration baseline in both whole-body registration performance and organ-wise alignment.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对全身跨示踪剂 PET 图像配准的新方法，重点解决了全身解剖结构异质性带来的变形约束难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：全身 PET 图像的可变形配准（Deformable Registration）面临两大挑战：
    1.  **示踪剂差异**：不同示踪剂（如 $^{18}$F-FDG 和 $^{18}$F-PSMA）的摄取模式完全不同，导致传统的基于强度的相似性度量（如 MSE）失效。
    2.  **解剖异质性**：全身包含刚性结构（骨骼）和柔性结构（软组织）。传统的配准方法通常使用**全局统一**的正则化权重，这会导致骨骼发生不切实际的变形，或者限制了软组织的必要灵活性。
*   **研究动机**：利用 PET/CT 扫描中“PET 与 CT 空间对齐”的天然属性，引入 CT 信息来引导配准过程，实现空间自适应的变形约束。

### 2. 核心方法论
*   **核心思想**：利用配对的 CT 图像作为解剖参考，构建一个**体素级（Voxel-wise）的空间变分正则化图**，动态调整变形场（DDF）的平滑度约束。
*   **关键技术细节**：
    1.  **弱监督学习框架**：由于跨示踪剂 PET 强度不一致，训练时利用配对的 CT 图像计算 Dice 相似度损失，并利用自动分割生成的 128 个器官掩码（Mask）进行解剖监督。
    2.  **CT 引导的正则化图 ($w(x)$)**：
        *   将移动图像的 CT 亨氏单位（HU）值归一化并进行非线性映射（通过指数 $\gamma$ 控制对比度）。
        *   高 HU 值区域（如骨骼）被赋予更高的正则化权重，强制其保持刚性；低 HU 值区域（如软组织）权重较低，允许更大程度的变形。
        *   公式化表示为：$L_{reg} = \sum w(x) \|\nabla \mu(x)\|^2$，其中 $w(x)$ 根据 CT 强度在预设的均值 $\mu_r$ 和偏差 $\delta$ 之间波动。
    3.  **推理阶段**：模型仅需输入两幅 PET 图像即可预测位移场，无需推理时的额外 CT 或掩码。

### 3. 实验设计
*   **数据集**：使用了来自 296 名患者的真实临床数据集，每位患者均包含 $^{18}$F-FDG PET/CT 和 $^{18}$F-PSMA PET/CT 两组扫描。
*   **基准模型（Benchmark）**：以使用全局统一正则化权重的弱监督配准模型（基于 Swin Transformer-V2）作为 Baseline。
*   **对比与评估**：
    *   评估指标包括互信息（MI）、Dice 分数和目标配准误差（TRE）。
    *   进行了消融实验，测试了不同全局权重 $\mu_r$、非线性指数 $\gamma$ 以及偏差 $\delta$ 对结果的影响。

### 4. 资源与算力
*   **模型架构**：使用了 Swin Transformer-V2 作为骨干网络。
*   **训练参数**：Batch size 为 2，训练 350 个 epoch，使用 AdamW 优化器。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量，但考虑到处理的是 128x128x384 的 3D 高分辨率体积数据，通常需要具备大显存的工业级 GPU（如 A100 或 V100）。

### 5. 实验数量与充分性
*   **实验规模**：训练集 216 例，测试集 80 例。对于医学影像配准任务而言，这是一个相对较大的临床数据集。
*   **充分性**：
    *   实验涵盖了 128 个解剖结构的器官级分析，并展示了不同正则化强度下的性能曲线。
    *   通过统计学检验（paired t-test）验证了改进的显著性。
    *   **客观性**：对比了多种超参数组合，证明了该方法在较宽的参数范围内均优于基准线，实验设计较为公平。

### 6. 主要结论与发现
*   **性能提升**：在最佳参数下，该方法将平均 Dice 从 0.52 提升至 0.58，TRE 从 8.37mm 降低至 6.04mm，MI 显著提高。
*   **非线性映射的重要性**：实验发现 $\gamma=2$ 的效果优于 $\gamma=1$，说明对 CT 强度进行非线性增强能更好地区分刚性和柔性组织的约束需求。
*   **鲁棒性**：空间变分正则化在适中的全局约束强度下表现最稳定，有效缓解了全身配准中的解剖扭曲问题。

### 7. 优点与亮点
*   **简单高效**：不需要学习复杂的体素级正则化场，而是直接利用现有的 CT 解剖先验，计算开销小且易于实现。
*   **临床实用性强**：针对跨示踪剂 PET 配准这一实际临床痛点，利用了 PET/CT 设备的固有物理特性。
*   **解剖感知**：成功实现了“骨骼强约束、软组织弱约束”的直观物理逻辑。

### 8. 不足与局限
*   **数据集局限**：使用的是私有临床数据集，缺乏在公开数据集（如 TotalSegmentator 相关公开数据）上的交叉验证。
*   **依赖 CT 质量**：正则化图直接源自 CT，如果 CT 存在严重伪影（如金属伪影），可能会导致错误的变形约束。
*   **超参数敏感性**：虽然表现优于基准，但 $\mu_r$ 和 $\delta$ 等参数仍需根据具体任务手动调优，未实现完全的自动化参数搜索。

（完）
