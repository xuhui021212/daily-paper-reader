---
title: "OCR-Memory: Optical Context Retrieval for Long-Horizon Agent Memory"
title_zh: OCR-Memory：面向长时程智能体记忆的光学上下文检索
authors: "Jinze Li, Yang Zhang, Xin Yang, Jiayi Qu, Jinfeng Xu, Shuo Yang, Junhua Ding, Edith Cheuk-Han Ngai"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26622v1"
tags: ["query:lvu"]
score: 9.0
evidence: 视觉模态作为长程智能体记忆的高密度表示
tldr: 针对自主LLM智能体在长程交互任务中面临的文本上下文限制和信息丢失问题，本文提出OCR-Memory框架。该方法将历史轨迹渲染为带有视觉标识符的高密度图像，通过“定位并转录”范式检索经验，避免了传统摘要带来的信息损失和幻觉。实验证明，OCR-Memory在严格上下文限制下显著提升了智能体的记忆容量和证据恢复的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26622v1/fig-001.webp\", \"caption\": \"Table 1: Main Results on Long-Horizon Agent Tasks. We report fine-grained metrics for Mind2Web, including Element Accuracy, Action F1 Score, Step Success Rate, and Task Success Rate. For AppWorld, we report Success Rates across three difficulty levels and the overall Average. OCR-Memory consistently outperforms baselines, achieving significant gains in metrics requiring precise structural grounding.\", \"page\": 6, \"index\": 1, \"width\": 808, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.26622v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the OCR-Memory. The system enables long-horizon agent memory by storing interaction histories as compressed multi-resolution images (left). To retrieve information, we employ a Locate-and-Transcribe paradigm: the model scans the visual history annotated with Set-of-Mark (SoM) visual anchors (center) to predict the index of relevant segments. Finally, the verbatim text corresponding to the selected index is deterministically fetched (right), avoiding generation-based hallucinations and minimizing token usage.\", \"page\": 3, \"index\": 2, \"width\": 824, \"height\": 464}]"
motivation: 现有的智能体记忆系统受限于文本上下文预算，存储原始轨迹成本高，而文本摘要又会导致信息丢失和碎片化。
method: 提出OCR-Memory框架，将历史轨迹渲染为带视觉锚点的图像，并采用“定位并转录”模式检索精确的原始文本。
result: 在长程智能体基准测试中，该方法在严格的上下文限制下实现了性能的持续提升，并有效减少了幻觉。
conclusion: OCR-Memory通过视觉编码显著增强了智能体的有效记忆容量，为长程任务提供了一种高效且保真的记忆检索方案。
---

## 摘要
自主大语言模型（LLM）智能体越来越多地在长时程、交互式环境中运行，其成功取决于对长期积累经验的重复利用。然而，现有的智能体记忆系统从根本上受到文本上下文预算的限制：存储或回顾原始轨迹的 Token 成本极高，而摘要和纯文本检索则以信息丢失和证据碎片化为代价来节省 Token。为了解决这一局限性，我们提出了光学上下文检索记忆（OCR-Memory），这是一种利用视觉模态作为智能体经验高密度表示的记忆框架，能够在检索时以极低的提示词开销保留任意长度的历史记录。具体而言，OCR-Memory 将历史轨迹渲染为带有唯一视觉标识符标注的图像。OCR-Memory 通过“定位并转录”（locate-and-transcribe）范式检索存储的经验，该范式通过视觉锚点选择相关区域并检索相应的逐字文本，从而避免自由生成并减少幻觉。在长时程智能体基准测试上的实验表明，在严格的上下文限制下，该方法取得了持续的性能提升，证明了光学编码在增加有效记忆容量的同时，保留了忠实的证据恢复能力。

## Abstract
Autonomous LLM agents increasingly operate in long-horizon, interactive settings where success depends on reusing experience accumulated over extended histories. However, existing agent memory systems are fundamentally constrained by text-context budgets: storing or revisiting raw trajectories is prohibitively token-expensive, while summarization and text-only retrieval trade token savings for information loss and fragmented evidence. To address this limitation, we propose Optical Context Retrieval Memory (OCR-Memory), a memory framework that leverages the visual modality as a high-density representation of agent experience, enabling retention of arbitrarily long histories with minimal prompt overhead at retrieval time. Specifically, OCR-Memory renders historical trajectories into images annotated with unique visual identifiers. OCR-Memory retrieves stored experience via a \emph{locate-and-transcribe} paradigm that selects relevant regions through visual anchors and retrieves the corresponding verbatim text, avoiding free-form generation and reducing hallucination. Experiments on long-horizon agent benchmarks show consistent gains under strict context limits, demonstrating that optical encoding increases effective memory capacity while preserving faithful evidence recovery.

---

## 论文详细总结（自动生成）

以下是对论文《OCR-Memory: Optical Context Retrieval for Long-Horizon Agent Memory》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：自主 LLM 智能体在处理长时程（Long-horizon）任务时，需要依赖历史经验。然而，现有的记忆系统面临“上下文预算”的瓶颈：
    *   **存储原始文本**：Token 消耗极大，容易超出 LLM 的上下文窗口。
    *   **文本摘要/压缩**：虽然节省了空间，但会导致关键细节（如错误信息、结构化布局）丢失，且容易产生幻觉。
*   **核心目标**：利用视觉模态（图像）作为高密度、保真的信息载体，通过光学编码大幅压缩记忆体积，同时保持对原始细节的精确检索能力。

### 2. 方法论
OCR-Memory 框架将记忆的存储与检索从文本域转移到了图像域，核心流程如下：

*   **视觉记忆构建（Visual Memory Bank）**：
    *   将智能体的交互轨迹（推理链、工具调用、环境反馈）渲染成图像。
    *   **视觉锚点（Set-of-Mark, SoM）**：在图像中为每个文本段落添加红色边界框和唯一的数字索引 ID。
*   **定位并转录（Locate-and-Transcribe）检索范式**：
    *   **定位**：给定查询 $q$，光学检索模型（基于 DeepSeek-OCR）扫描图像，预测相关片段的索引，而非直接生成文本内容。
    *   **转录**：根据预测的索引，从外部原始日志中“确定性地”提取逐字文本。这种设计解耦了“理解”与“生成”，彻底消除了检索阶段的文本幻觉。
*   **多分辨率与主动召回（Multi-Resolution & Active Recall）**：
    *   **动态老化**：模拟人类记忆，较旧的记忆以低分辨率（如 512x512）存储，大幅减少视觉 Token 消耗。
    *   **主动召回**：一旦低分辨率图像中的某个片段被判定为相关，系统立即将其上采样恢复至高分辨率（1024x1024），确保后续推理获得最清晰的细节。
*   **训练策略**：使用 HotpotQA 数据集改造为判别式检索任务，通过加权二元交叉熵损失（Weighted BCE）微调模型，使其偏好高召回率。

### 3. 实验设计
*   **数据集/场景**：
    *   **Mind2Web**：跨域 Web 导航任务，评估智能体在复杂网页结构中的操作能力。
    *   **AppWorld**：API 交互任务，侧重于评估智能体在长历史背景下的回溯和纠错能力。
*   **Benchmark 指标**：
    *   Mind2Web：元素准确率（Ele Acc）、动作 F1 值、步骤成功率（Step SR）、任务成功率（Task SR）。
    *   AppWorld：不同难度（Easy, Med, Hard）下的成功率。
*   **对比方法**：
    *   **Zero-Shot**：无记忆辅助。
    *   **Text-RAG**：基于向量相似度的标准文本检索。
    *   **MemoryBank**、**AWM**（智能体工作流记忆）、**ACON**（上下文压缩优化）。

### 4. 资源与算力
*   **模型架构**：检索器采用 **DeepSeek-OCR (3B)**，推理智能体采用 **GPT-4** 或 **Qwen3-32B**。
*   **微调细节**：使用 LoRA 技术微调解码器（Rank=16），训练 3 个 Epoch。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量。仅提到全局 Batch Size 为 128，采用了余弦学习率调度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流长时程基准上进行了主实验。
    *   针对 SoM 机制、多分辨率策略、Token 限制（1k 到 8k）进行了多组消融实验。
    *   引入了 **NIAH（大海捞针）** 测试来评估长达 32k Token 的检索鲁棒性。
    *   进行了**检索质量评估**（Recall@K, MRR）和**忠实度（Faithfulness）**测试。
*   **充分性评价**：实验设计非常全面且客观。通过更换推理后端（GPT-4 到 Qwen3）证明了方法的通用性；通过严格限制 Token 预算展示了该方法在极端资源受限下的优势。

### 6. 主要结论与发现
*   **性能领先**：OCR-Memory 在所有指标上均优于现有的文本记忆方法，尤其在 AppWorld 的“Hard”任务中，成功率比 AWM 高出 3.6%。
*   **高压缩比**：光学编码实现了超过 **10 倍** 的上下文压缩率，同时在 32k 长度下仍保持 94.1% 的检索准确率。
*   **零幻觉检索**：通过“索引选择+原始文本提取”模式，实现了 100% 的内容检索忠实度。
*   **Token 效率**：在 Mind2Web 任务中，单步注入推理模型的 Token 数从 3980 降至 596，降幅达 6.7 倍。

### 7. 优点
*   **高密度表示**：巧妙利用视觉 Token 的信息承载能力，解决了长历史轨迹导致的“上下文爆炸”问题。
*   **确定性恢复**：Locate-and-Transcribe 机制有效解决了多模态模型在读取微小文字时的幻觉问题。
*   **生物启发式设计**：多分辨率老化与主动召回机制在节省算力与保持精度之间取得了极佳平衡。

### 8. 不足与局限
*   **训练成本**：与无需训练的 RAG 相比，该方法需要专门微调一个光学检索模型。
*   **存储与延迟开销**：图像渲染和存储的磁盘占用远高于纯文本（每集 1.47 MB vs 18 KB），且检索延迟（1.7s）高于简单文本检索（0.3s）。
*   **部署复杂性**：系统需要同时运行视觉编码器和大型语言模型，增加了显存占用。

（完）
