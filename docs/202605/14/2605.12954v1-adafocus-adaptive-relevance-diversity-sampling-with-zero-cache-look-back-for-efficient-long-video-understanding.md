---
title: "AdaFocus: Adaptive Relevance-Diversity Sampling with Zero-Cache Look-back for Efficient Long Video Understanding"
title_zh: AdaFocus：基于零缓存回溯的自适应相关性-多样性采样，助力高效长视频理解
authors: "Xiao Yang, Yingzhe Ma, Haoxuan Yu, Zixin Li, Ning Qin"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.12954v1"
tags: ["query:lvu"]
score: 10.0
evidence: 通过渐进式证据获取实现高效长视频理解
tldr: 针对长视频理解中密集编码成本高和稀疏压缩丢失细节的问题，本文提出AdaFocus框架。该框架将视频理解视为渐进式证据获取过程，通过AdaRD采样器生成兼顾相关性与多样性的紧凑预览，并引入不确定性触发的零缓存回溯机制，在模型不确定时从磁盘按需检索高分辨率证据。实验证明，AdaFocus在显著降低计算开销和内存占用的同时，提升了长视频任务的准确率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12954v1/fig-001.webp\", \"caption\": \"Table 6: Backbone generalization. Δ denotes the absolute gain of using ckpt-3000 over Original under the same inference method, measuring RL training’s contribution.\", \"page\": 6, \"index\": 1, \"width\": 508, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.12954v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the AdaFocus framework. (a) Coarse Preview Stage: the input video is processed at 1 fps through a CLIP encoder and the AdaRD sampler to produce a compact keyframe set K ≪ 𝑁 , which is fed to the backbone LVLM as visual tokens. (b) Confidence Gating: the frozen LVLM generates an answer; if the length-normalized log-probability confidence 𝐶 ≥ 𝜏 the answer is accepted immediately, otherwise an agentic look-back is triggered. (c) Zero-Cache Refinement: the target timestamp is grounded via regex parsing or cross-attention fallback, and a high-resolution temporal window [𝑡 − Δ𝑤, 𝑡 + Δ𝑤] is decoded directly from disk via zero-cache I/O and fused into the evidence set for re-generation, achieving ∼33× token savings over dense oracle encoding.\", \"page\": 2, \"index\": 2, \"width\": 1002, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2605.12954v1/fig-003.webp\", \"caption\": \"Figure 2: Detailed methodology of AdaFocus. (a) Relevance-Diversity Balancing: each candidate frame is scored by cosine similarity 𝑟𝑖 = 𝑆𝑟𝑒𝑙 (𝑣𝑖 ) with a temporal diversity penalty; adaptive routing switches to global clustering when 𝑔(V,Q) < 𝜏𝑔𝑙𝑜𝑏𝑎𝑙 , producing a compressed preview K . (b) Uncertainty Mechanism: the backbone LVLM generates tokens whose log-probabilities are monitored against a length-calibrated threshold 𝛾 (𝐿) = 𝛾0 − 𝛽 log(𝐿); falling below the threshold triggers refinement (𝑧 = 1). (c) Zero-Cache Grounding & Fallback: the target timestamp is first extracted via regex parsing (𝑡𝑟𝑒𝑔𝑒𝑥 ); if unavailable, a crossattention fallback selects the most-attended frame (𝑡𝑎𝑡𝑡𝑛 = argmax𝑖 𝐴𝑖 ). The retrieved window is fused into the evidence set E𝑓 𝑖𝑛𝑎𝑙 = EK ∪ ER without any VRAM pre-cache.\", \"page\": 3, \"index\": 3, \"width\": 1002, \"height\": 440}]"
motivation: 现有的长视频理解方法在密集编码的高昂成本与稀疏压缩导致的信息丢失之间难以平衡。
method: 提出AdaRD采样器生成自适应预览，并结合零缓存回溯机制，在模型置信度低时从磁盘动态检索关键细节。
result: 在多个基准测试上取得领先，相比密集编码减少了约33倍的视觉Token消耗，并显著提升了VideoMME和Charades-STA的性能。
conclusion: 渐进式预览结合零缓存证据细化是实现高效、可扩展长视频推理的一种极具潜力的范式。
---

## 摘要
长视频理解受到僵化的单次处理范式（one-shot paradigm）的严重制约：现有方法要么以极高的内存和延迟成本对视频进行密集编码，要么将其过度压缩为稀疏帧集，从而不可逆地丢弃了下游推理所需的细粒度证据。因此，当前模型难以同时平衡时间覆盖范围、视觉细节和计算效率。我们提出了 AdaFocus，这是一个高效的框架，它将长视频理解重新构思为渐进式的证据获取，而非单次编码。AdaFocus 依赖于两个紧密耦合的组件。首先，查询感知的自适应相关性-多样性采样器（AdaRD）生成一个紧凑且信息丰富的视频预览，当查询缺乏可靠的局部定位时，它会自动切换到全局聚类。其次，AdaFocus 引入了一种由不确定性触发的细化机制，而不是在内存中缓存详尽的帧序列。它仅在模型不置信时执行有针对性的回溯，通过零缓存 I/O 设计直接从磁盘检索高分辨率证据。这使得被丢弃的视觉细节从不可逆的损失转变为按需可恢复的证据，且无需支付详尽预加载的成本。在七个标准长视频基准测试上的实验表明，AdaFocus 提供了比强基准模型显著更好的效率-准确度权衡。与传统的密集编码相比，AdaFocus 实现了更高的任务性能（例如，在 VideoMME 上准确率提升 2.59，在 Charades-STA 上 mIoU 较单次推理提升 8.39），同时将视觉 token 消耗降低了约 33 倍，并通过其零缓存磁盘检索设计消除了对内存中帧预缓存的需求。这些发现表明，渐进式预览结合零缓存证据细化是实现可扩展多媒体推理的一种高效范式。

## Abstract
Long video understanding is heavily bottlenecked by a rigid one-shot paradigm: existing methods either densely encode videos at prohibitive memory and latency costs, or aggressively compress them into sparse frame sets that irreversibly discard fine-grained evidence needed for downstream reasoning. Consequently, current models struggle to simultaneously balance temporal coverage, visual details, and computational efficiency.   We propose AdaFocus, an efficient framework that rethinks long-video understanding as progressive evidence acquisition rather than one-pass encoding. AdaFocus relies on two tightly coupled components. First, a Query-Aware Adaptive Relevance-Diversity sampler (AdaRD) produces a compact yet informative video preview, adaptively switching to global clustering when the query lacks reliable local grounding. Second, instead of caching exhaustive frame sequences in memory, AdaFocus introduces an uncertainty-triggered refinement mechanism. It performs targeted look-back only when the model is not confident, retrieving high-resolution evidence directly from disk via a zero-cache I/O design. This turns discarded visual details from an irreversible loss into on-demand recoverable evidence without paying the cost of exhaustive preloading.   Experiments on seven standard long-video benchmarks show that AdaFocus delivers a substantially better efficiency-accuracy trade-off than strong baselines. Compared with conventional dense encoding, AdaFocus achieves improved task performance (e.g., +2.59 accuracy on VideoMME, +8.39 mIoU on Charades-STA over single-pass inference) while reducing visual token consumption by ~33x and eliminating the need for in-memory frame pre-caching through its zero-cache disk retrieval design. These findings suggest that progressive preview combined with zero-cache evidence refinement is a highly effective paradigm for scalable multimedia reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《AdaFocus: Adaptive Relevance-Diversity Sampling with Zero-Cache Look-back for Efficient Long Video Understanding》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：长视频理解目前面临“两难”境地：**密集编码**（Dense Encoding）虽然保留了细节，但计算成本和内存占用极高，且易导致注意力稀释；**稀疏采样**（Sparse Sampling）虽然高效，但会不可逆地丢弃关键的细粒度证据。
*   **核心问题**：如何在不预先加载全量视频到内存/显存的前提下，实现对长视频的高效、精准理解，并平衡时间覆盖范围与视觉细节。
*   **整体含义**：论文将长视频理解从传统的“单次处理”范式重构为“**渐进式证据获取**”范式，通过初步预览和按需回溯，实现了效率与准确性的双重提升。

### 2. 方法论
AdaFocus 框架由三个核心阶段组成：
*   **查询感知的自适应相关性-多样性采样 (AdaRD)**：
    *   利用冻结的 CLIP 编码器计算视频帧与文本查询的相关性分数。
    *   通过一个带有时间惩罚项的贪婪算法，在选择相关帧的同时确保时间上的多样性，避免冗余。
    *   **最大激活路由**：若查询属于全局性问题（相关性分数普遍较低），自动切换到基于 K-Means 的时间聚类模式，以保证宏观覆盖。
*   **内生置信度门控 (Confidence Gating)**：
    *   模型生成初步答案时，监控其长度归一化的对数概率（Log-probabilities）。
    *   若置信度低于动态阈值，则触发“回溯（Look-back）”机制。
*   **零缓存按需细化 (Zero-Cache Refinement)**：
    *   **时间戳定位**：通过正则表达式从模型回复中提取时间戳；若失败，则利用交叉注意力权重（Cross-attention）寻找模型最关注的帧。
    *   **零缓存 I/O**：直接从磁盘（使用 OpenCV）解码目标时间点附近的短窗口（±1.5s）高分辨率帧，并将其注入上下文进行二次推理。这种设计无需在显存中预存全量视频。

### 3. 实验设计
*   **数据集与基准**：
    *   **视频 QA 与推理**：VideoMME, VideoMMMU, MVBench, LongVideoBench, MMVU。
    *   **时间定位 (Grounding)**：Charades-STA, ActivityNet-TVG。
*   **对比方法**：
    *   基础模型：Qwen2.5-VL-7B-Instruct。
    *   同类 SOTA：Video-R1, VideoChat-R1.5, VideoAuto-R1。
    *   消融基准：单次推理（Baseline）、仅 CoT 推理（CoT-only）、密集编码参考（Dense Oracle）。

### 4. 资源与算力
*   **训练算力**：使用了 **8 张 NVIDIA A100 GPU** 进行 GRPO（群组相对策略优化）对齐训练。
*   **训练时长**：训练 3000 步，耗时约 **8 小时**（总计约 64 A100 GPU-hours）。
*   **推理算力**：推理过程在单张 A100 上完成，强调了其对硬件的友好性（零缓存设计显著降低了显存需求）。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个主流长视频基准上进行了全面测试，涵盖了从感知到认知、从短视频剪辑到长视频理解的多种场景。
*   **消融实验**：非常充分。包括了组件贡献分析（AdaRD vs. Agent）、随机检索对比（证明了定位的有效性）、帧预算敏感度、置信度阈值影响、回溯窗口大小分析以及缓存策略对比。
*   **客观性**：通过与“密集编码”这一理论上限进行对比，客观展示了该方法在处理长序列时的优势（甚至超过了密集编码，因为后者存在注意力稀释问题）。

### 6. 主要结论与发现
*   **性能提升**：AdaFocus 在所有基准上均显著优于单次推理。在 Charades-STA 上 mIoU 提升了 **8.39**，在 VideoMME 上准确率提升了 **2.59**。
*   **极高效率**：相比密集编码，AdaFocus 仅消耗了约 **1/33** 的视觉 Token，却获得了更好的效果。
*   **协同效应**：RL 训练（提升推理质量）与 AdaFocus 模块（提升视觉证据质量）具有加性增益，两者结合效果最佳。
*   **零缓存可行性**：磁盘 I/O 带来的延迟（<0.5s）远低于模型推理延迟，证明了“磁盘换显存”在长视频场景下的实用性。

### 7. 优点
*   **系统级创新**：引入“零缓存”设计，打破了长视频必须预加载到显存的固有思维，极大地扩展了模型处理视频长度的上限。
*   **自适应采样**：AdaRD 能够根据问题类型动态调整策略，比固定的均匀采样更具智能性。
*   **模块化与无损性**：推理模块无需额外微调即可移植到其他多模态模型上，且实验证明零缓存检索与全缓存检索在精度上是无损的。

### 8. 不足与局限
*   **推理延迟**：由于采用了多轮迭代（最多 3 次回溯），平均推理时间约为单次推理的 2 倍，最坏情况下可达 4.4 倍。
*   **定位依赖**：回溯的精度高度依赖于模型生成时间戳的能力或交叉注意力的准确性，如果模型初始判断完全偏离目标，回溯也难以挽回。
*   **应用限制**：目前主要针对 7B 规模的模型进行了验证，在更大规模模型或更极端长度（如数小时）视频上的表现仍需进一步探索。

（完）
