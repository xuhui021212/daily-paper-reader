---
title: "BVI-Mamba: Video Enhancement Using a Visual State-Space Model for Low-Light and Underwater Environments"
title_zh: BVI-Mamba：基于视觉状态空间模型的低光照与水下环境视频增强
authors: "Guoxi Huang, Ruirui Lin, Yini Li, David R. Bull, Nantheera Anantrasirichai"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23655v1"
tags: ["query:lvu"]
score: 8.0
evidence: Visual Mamba框架旨在减少视频处理中的内存使用和计算时间
tldr: 针对低光照和水下视频的噪声与色彩失衡问题，本文提出基于视觉状态空间模型（VSS）的增强框架BVI-Mamba。该框架结合特征对齐模块与基于VSS块的UNet架构，在降低计算开销的同时实现高效去噪与亮度调节。实验表明，其性能优于传统的卷积和Transformer模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23655v1/fig-001.webp\", \"caption\": \"Figure 3: Low-light enhancement comparison (left to right): the original frame, results of PCDUNet, STASUNet, BVI-CDM and BVI-Mamba\", \"page\": 6, \"index\": 1, \"width\": 1008, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2604.23655v1/fig-002.webp\", \"caption\": \"Figure 2: Diagram of the proposed BVI-Mamba for video enhancement.\", \"page\": 4, \"index\": 2, \"width\": 1008, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.23655v1/fig-003.webp\", \"caption\": \"Figure 1: Diagram of 2D-Selective-Scan (SS2D). SS2D contains three steps, including cross-scan, SSM processing and the merge operation.\", \"page\": 3, \"index\": 3, \"width\": 998, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.23655v1/fig-004.webp\", \"caption\": \"Figure 4: Underwater enhancement comparison (left to right): the original frame, results of Zero-TIG, and BVI-Mamba\", \"page\": 7, \"index\": 4, \"width\": 1008, \"height\": 239}]"
motivation: 旨在解决低光照和水下视频增强中AI模型计算开销大、内存占用高以及画质退化严重的问题。
method: 提出包含特征对齐和基于VSS块的UNet增强模块的框架，利用状态空间模型优化时空特征处理。
result: 实验证明该方法在低光照和水下视频增强任务中，性能优于现有的Transformer和卷积神经网络模型。
conclusion: BVI-Mamba为高效、高质量的极端环境视频增强提供了一种兼顾性能与计算效率的新方案。
---

## 摘要
在低光照和水下条件下拍摄的视频通常会受到噪声、低对比度、色彩失衡和模糊等失真的影响。这些问题不仅限制了可见性，还降低了检测等自动化任务的性能。通常需要进行后处理，但这可能非常耗时。与基于图像的方法相比，基于人工智能的视频增强工具也需要更多的计算资源。本文介绍了一种名为 Visual Mamba 的新型框架，旨在通过利用视觉状态空间（VSS）模型来减少内存使用和计算时间。该框架由两个模块组成：(i) 特征对齐模块，在特征空间中注册输入帧之间的时空位移；(ii) 增强模块，使用类 UNet 架构执行去噪和亮度调节，其中所有卷积层均被 VSS 块替换。实验结果表明，Visual Mamba 技术在低光照和水下视频增强任务中均优于 Transformer 和基于卷积的模型。代码可在 https://github.com/russellllaputa/BVI-Mamba 在线获取。

## Abstract
Videos captured in low-light and underwater conditions often suffer from distortions such as noise, low contrast, color imbalance, and blur. These issues not only limit visibility but also degrade automatic tasks like detection. Post-processing is typically required but can be time-consuming. AI-based tools for video enhancement also demand significantly more computational resources compared to image-based methods. This paper introduces a novel framework, Visual Mamba, designed to reduce memory usage and computational time by leveraging the Visual State Space (VSS) model. The framework consists of two modules: (i) a feature alignment module, where spatio-temporal displacement between input frames is registered in the feature space, and (ii) an enhancement module, where noise removal and brightness adjustment are performed using a UNet-like architecture, with all convolutional layers replaced by VSS blocks. Experimental results show that the Visual Mamba technique outperforms Transformer and convolution-based models in both low-light and underwater video enhancement tasks. Code is available on line at https://github.com/russellllaputa/BVI-Mamba.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **BVI-Mamba** 的新型视频增强框架，旨在解决低光照和水下环境下的视频质量退化问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在低光照和水下环境中拍摄的视频通常存在高噪声、低对比度、色彩失衡和模糊等问题。
*   **研究动机**：
    *   **计算开销大**：现有的基于 AI（如 CNN 或 Transformer）的视频增强方法计算资源消耗极高，难以处理长序列或高分辨率视频。
    *   **长程依赖建模**：传统卷积在处理大尺寸图像块和长程依赖时存在局限，而 Transformer 的计算复杂度随序列长度呈平方级增长。
    *   **环境复杂性**：水下成像受光散射和吸收影响，导致严重的色彩偏差和对比度下降。

### 2. 方法论
BVI-Mamba 结合了状态空间模型（SSM）的线性效率与类 UNet 的多尺度特征处理能力。
*   **核心思想**：将 Mamba（一种具有线性复杂度的状态空间模型）引入视频增强领域，替代传统的卷积层或 Swin Transformer 块。
*   **关键技术细节**：
    1.  **特征对齐模块**：采用 PCD（金字塔、级联和可变形卷积）对齐模块，在特征空间中注册相邻帧之间的时空位移，确保时间一致性，减少闪烁。
    2.  **2D 选择性扫描（SS2D）**：为了让 1D 的 SSM 处理 2D 图像数据，该模块从四个方向（左到右、上到下、右到左、下到上）展开图像块进行扫描，增强了模型对 2D 结构的感知能力。
    3.  **增强模块（VSS 块）**：采用类 UNet 架构，但将所有卷积层替换为 **VSS（Visual State Space）块**。每个块包含层归一化（LN）、SS2D 模块和前馈网络（FFN），能够同时捕获局部细节和全局上下文。
    4.  **水下色彩校正**：针对水下视频，在送入增强模型前先进行基于色度适应（Chromatic Adaptation）的色彩平衡调整。

### 3. 实验设计
*   **数据集**：
    *   **DID**：包含 413 对动态场景视频。
    *   **BVI-RLV**：全注册的高清低光视频数据集，包含 40 个场景。
    *   **BVI-Coral**：用于水下视频测试（深海低光场景，无 Ground Truth）。
*   **Benchmark 与对比方法**：
    *   **PCDUNet**（基于 CNN）
    *   **STA-SUNet**（基于 Transformer）
    *   **BVI-CDM**（基于扩散模型 Diffusion Model）
    *   **Zero-TIG**（用于水下视频对比）
*   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似性）。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算系统，配备 **65 块 Nvidia P100 GPU**（32 个节点，每节点 2 块卡，外加 1 个登录节点）。
*   **软件环境**：Python、PyTorch、CUDA，并集成了 C++ 编写的可变形卷积组件。
*   **训练细节**：对于缺乏详细训练参数的模型，默认运行 **200k 次迭代**。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流低光数据集（DRV 和 BVI-RLV）上进行了定量对比，并在水下数据集上进行了定性对比。
*   **充分性**：实验涵盖了当前主流的三大技术路线（CNN、Transformer、Diffusion），对比具有代表性。
*   **客观性**：使用了全注册数据集（BVI-RLV）进行评估，减少了因对齐不准导致的指标偏差。但论文中未见详细的消融实验（例如验证 SS2D 四向扫描的必要性）。

### 6. 主要结论与发现
*   **性能领先**：BVI-Mamba 在 PSNR 和 SSIM 指标上显著优于 CNN 和 Transformer 模型。例如在 DRV 数据集上，其 PSNR 达到 22.95dB，远高于 STA-SUNet 的 14.19dB。
*   **视觉效果**：该方法在去噪和锐化图像的同时，能更好地保留精细细节，且在处理水下色彩失衡方面表现出色。
*   **效率优势**：通过引入 Mamba 架构，模型在处理长序列视频时表现出比 Transformer 更好的计算效率。

### 7. 优点
*   **架构创新**：首次将 Visual Mamba 引入低光和水下视频增强任务，利用了 SSM 的线性复杂度优势。
*   **时空一致性**：结合了成熟的 PCD 对齐技术，有效解决了视频增强中常见的亮度波动和伪影问题。
*   **多场景适用**：不仅针对陆地低光，还通过预处理步骤扩展到了极具挑战性的深海视频增强。

### 8. 不足与局限
*   **水下评估受限**：由于水下视频缺乏 Ground Truth，该部分的评估仅停留在主观视觉比较，缺乏定量数据支持。
*   **数据集缺陷**：论文提到 DID 数据集本身存在对齐问题和镜头污垢，这可能影响了在该数据集上的训练效果。
*   **算力门槛**：虽然 Mamba 理论上更高效，但实验中使用了多达 65 块 P100 GPU，对于普通研究者来说，复现或训练该模型的硬件成本依然较高。

（完）
