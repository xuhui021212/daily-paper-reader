---
title: "Persistent Visual Memory: Sustaining Perception for Deep Generation in LVLMs"
title_zh: 持久视觉记忆：在大型视觉语言模型（LVLMs）中维持深度生成的感知能力
authors: "Siyuan Huang, Xiaoye Qu, Yafu Li, Tong Zhu, Zefeng He, Muxin Fu, Daizong Liu, Wei-Long Zheng, Yu Cheng"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00814v1"
tags: ["query:lvu"]
score: 8.0
evidence: 用于在LVLM中维持感知的持久视觉记忆
tldr: 针对自回归多模态大模型在长文本生成中面临的“视觉信号稀释”问题，即视觉注意力随序列增长而衰减的现象，本文提出了持久视觉记忆（PVM）机制。PVM是一个与前馈网络并行的轻量级模块，通过建立距离无关的检索路径，为模型提供持续且按需的视觉感知能力。在Qwen3-VL模型上的实验证明，PVM能以极小的参数开销显著提升复杂推理任务的准确率，并有效抵抗生成长度增加导致的信号衰减。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00814v1/fig-001.webp\", \"caption\": \"Figure 4: Overview of the Persistent Visual Memory (PVM) framework. PVM is integrated parallel to the frozen FFN to shield active visual retrieval from sequential dilution during autoregressive reasoning. It treats the hidden state as a query to retrieve specific visual contexts via a parameter-efficient bottleneck adapter (consisting of Projection, Cross-Attn, FFN, and Restoration). The module employs a Silencing Mask to activate selectively during text generation, injecting the retrieved visual signal into the main stream through a learnable gate to enhance visual grounding.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2605.00814v1/fig-002.webp\", \"caption\": \"Figure 1: Visual Memory Mechanisms. Unlike Standard LVLMs that degrade via visual dilution or Injection methods that cause serial interference, our Persistent Visual Memory (PVM) establishes an independent retrieval path, preserving visual intensity without disrupting the autoregressive flow.\", \"page\": 2, \"index\": 2, \"width\": 744, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2605.00814v1/fig-003.webp\", \"caption\": \"Table 1: Main Results on General and Reasoning Benchmarks. We report 4-run average accuracy (%) across 8 benchmarks, with the best and second-best performances highlighted in bold and underlined. The numbers in subscripts denote the absolute improvement (↑) or decline (↓) rela-\", \"page\": 8, \"index\": 3, \"width\": 832, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2605.00814v1/fig-004.webp\", \"caption\": \"Figure 8: Layer-wise Distribution of Mean Visual Attention Mass. The bar chart visualizes the aggregate attention weight assigned to visual tokens at each layers. We observe a characteristic \\\"Rise-Peak-Decay\\\" pattern, guiding our data-driven injection strategies.\", \"page\": 19, \"index\": 4, \"width\": 492, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2605.00814v1/fig-005.webp\", \"caption\": \"Table 4: Detailed Hyperparameter Settings. We report the specific configurations for both the SFT alignment phase and the GRPO reinforcement learning phase.\", \"page\": 17, \"index\": 5, \"width\": 577, \"height\": 480}]"
motivation: 解决自回归多模态大模型在长序列生成过程中，由于文本历史累积导致的视觉注意力逐渐衰减（视觉信号稀释）的问题。
method: 引入一种轻量级的持久视觉记忆（PVM）模块作为FFN的并行分支，通过距离无关的检索路径直接为模型提供视觉嵌入。
result: 在Qwen3-VL的4B和8B规模模型上，PVM显著提升了多模态任务的平均准确率，特别是在需要持久视觉感知的复杂推理任务中表现优异。
conclusion: PVM通过结构化手段有效缓解了深度生成中的信号抑制，增强了模型对视觉信息的长期维持能力并加速了内部预测的收敛。
---

## 摘要
尽管自回归大型视觉语言模型（LVLMs）在多模态任务中表现出卓越的能力，但它们面临着“视觉信号稀释”现象，即文本历史的累积扩大了注意力配分函数，导致视觉注意力随生成序列长度的增加而呈反比例衰减。为了应对这一问题，我们提出了持久视觉记忆（Persistent Visual Memory, PVM），这是一种轻量级的可学习模块，旨在确保持续且按需的视觉感知。PVM 作为 LVLMs 中前馈网络（FFN）旁侧的一个并行分支，建立了一个与距离无关的检索路径，直接为精确的视觉感知提供视觉嵌入，从而在结构上缓解了深度生成中固有的信号抑制。在 Qwen3-VL 模型上进行的大量实验表明，PVM 在参数开销极小的情况下带来了显著的改进，在 4B 和 8B 规模上均实现了持续的平均准确率提升，特别是在需要持久视觉感知的复杂推理任务中。此外，深入分析表明，PVM 能够抵抗长度诱导的信号衰减，并加速内部预测的收敛。

## Abstract
While autoregressive Large Vision-Language Models (LVLMs) demonstrate remarkable proficiency in multimodal tasks, they face a "Visual Signal Dilution" phenomenon, where the accumulation of textual history expands the attention partition function, causing visual attention to decay inversely with generated sequence length. To counteract this, we propose Persistent Visual Memory (PVM), a lightweight learnable module designed to ensure sustained, on-demand visual perception. Integrated as a parallel branch alongside the Feed-Forward Network (FFN) in LVLMs, PVM establishes a distance-agnostic retrieval pathway that directly provides visual embeddings for precise visual perception, thereby structurally mitigating the signal suppression inherent to deep generation. Extensive experiments on Qwen3-VL models demonstrate that PVM brings notable improvements with negligible parameter overhead, delivering consistent average accuracy gains across both 4B and 8B scales, particularly in complex reasoning tasks that demand persistent visual perception. Furthermore, in-depth analysis reveals that PVM can resist length-induced signal decay and accelerate internal prediction convergence.

---

## 论文详细总结（自动生成）

这是一份关于论文《Persistent Visual Memory: Sustaining Perception for Deep Generation in LVLMs》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **核心问题**：论文揭示并解决了自回归大型视觉语言模型（LVLMs）中的**“视觉信号稀释”（Visual Signal Dilution）**现象。
*   **研究背景**：在长文本生成或复杂推理（如长链条 CoT）过程中，随着生成的文本历史不断累积，注意力机制的配分函数（分母）不断增大，导致模型分配给初始视觉 Token 的注意力权重随序列长度增加呈反比例（$O(t^{-1})$）衰减。
*   **研究动机**：现有的“视觉注入”方法往往直接干扰自回归序列，可能破坏逻辑推理的连贯性。本文旨在寻找一种既能维持视觉感知，又不干扰推理逻辑的结构化方案。

### 2. 论文提出的方法论
*   **核心思想**：提出**持久视觉记忆（Persistent Visual Memory, PVM）**模块。它作为一个轻量级的并行分支集成在 Transformer 的 FFN（前馈网络）旁，建立一条与距离无关的独立检索路径。
*   **关键技术细节**：
    *   **并行架构**：将 PVM 与冻结的 FFN 并行放置。FFN 负责逻辑推理，PVM 负责从原始视觉嵌入中检索信息。
    *   **瓶颈适配器（Bottleneck Adapter）**：为了节省参数，将隐藏状态和视觉特征投影到低维潜空间（如 512 维）进行交叉注意力计算。
    *   **独立注意力归一化**：PVM 的注意力计算仅限于视觉 Token 集合，不受增长的文本上下文影响，从而在数学上消除了信号稀释。
    *   **门控融合与视觉静默掩码**：使用可学习的门控参数 $\lambda$ 融合 PVM 输出，并通过掩码确保该模块仅在生成文本 Token 时激活，不干扰视觉 Token 自身的表示。
*   **训练流程**：采用两阶段策略：
    1.  **视觉记忆对齐（SFT）**：冻结主干，训练 PVM 模块建立语义映射。
    2.  **策略精炼（GRPO）**：使用强化学习（GRPO）微调主干和 PVM，强化模型在复杂推理中主动检索视觉信息的能力。

### 3. 实验设计
*   **基准模型**：Qwen3-VL-Instruct（4B 和 8B 版本）。
*   **数据集/场景**：涵盖 8 个主流多模态评测集，包括通用理解（MMMU, MMBench, MMStar, MMT）和科学/数学推理（MathVerse, MathVision, AI2D）。
*   **对比方法**：
    *   **内部基线**：原始模型、SFT、LoRA、RL 增强版。
    *   **视觉注入方法**：MemVR、ICoT、CoMemo。
    *   **RL 推理模型**：Euclid-8B、PEARL-8B、OneThinker-8B。
*   **特殊分析实验**：
    *   **“盲眼画家”压力测试**：强制模型生成极长描述以观察注意力衰减。
    *   **LogitLens 分析**：探究中间层预测的收敛速度。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H200 GPU**（每张 141GB 显存）。
*   **软件框架**：PyTorch, Hugging Face `transformers`, `trl`, DeepSpeed (ZeRO-2/3), FlashAttention-2。
*   **参数开销**：PVM 模块仅引入约 **27.92M** 额外参数，仅占 8B 模型总量的 **0.32%**。
*   **推理开销**：解码吞吐量仅下降约 4.6%，每 Token 延迟增加约 1.18ms。

### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个不同维度的基准测试上进行了评估，并对 4B 和 8B 两种规模的模型进行了验证。
*   **消融实验**：非常充分，包括：
    *   检索来源（原始视觉嵌入 vs. 处理后的隐藏状态）。
    *   注入层位置的选择策略（峰值注意力层 vs. 最大衰减层 vs. 跨步分布层）。
    *   潜空间维度（d'）的敏感性分析。
    *   **等参数量控制实验**：对比了相同参数量的普通 MLP 分支，证明了 PVM 交叉注意力机制的独特性。
*   **客观性**：采用 `lmms-eval` 标准化工具，报告 4 次运行的平均值，实验设计严谨且具有统计意义。

### 6. 论文的主要结论与发现
*   **视觉稀释是真实存在的**：通过数学推导和实证分析，确认了视觉注意力随文本增长而衰减的 $O(t^{-1})$ 规律。
*   **PVM 显著提升性能**：在 8B 模型上平均提升 **4.8%**，在 4B 上提升 **4.4%**，超越了现有的视觉注入和 RL 微调模型。
*   **长序列优势更明显**：在 MathVerse 的长输出样本中，PVM 相比基线实现了高达 **27.3%** 的相对提升，证明了其在深度生成中的稳定性。
*   **加速收敛**：LogitLens 分析显示，PVM 帮助模型更早地在中间层确定预测结果，减轻了主干网络的感知负担。

### 7. 优点
*   **理论与实证结合**：不仅提出了方法，还给出了视觉信号稀释的数学证明和层级分布分析。
*   **轻量且高效**：以极小的参数代价（<1%）换取了显著的性能增益，且推理延迟增加极低。
*   **结构化解耦**：通过并行分支避免了对自回归逻辑流的直接干扰，解决了“视觉注入”导致的逻辑混乱问题。

### 8. 不足与局限
*   **模型覆盖范围**：实验主要集中在 Qwen3-VL 系列，虽然理论上具有通用性，但尚未在其他架构（如基于 InternVL 或 LLaVA 的模型）上广泛验证。
*   **理论假设**：定理 4.1 的证明依赖于“局部固定查询”假设，这在极长程的全局解码动态中可能存在简化。
*   **应用场景限制**：目前主要针对静态图像，尚未扩展到视频理解等动态流式输入场景。
*   **训练依赖**：虽然 PVM 模块轻量，但仍需要两阶段的微调（SFT+RL）才能达到最佳效果。

（完）
