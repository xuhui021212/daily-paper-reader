---
title: Membership Inference Attacks Against Video Large Language Models
title_zh: 针对视频大语言模型的成员推理攻击
authors: "Wei Song, Yuxin Cao, Ziqi Ding, Yi Liu, Gelei Deng, Yuekang Li"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27002v1"
tags: ["query:lvu"]
score: 8.5
evidence: 针对视频大语言模型的隐私攻击
tldr: 本研究针对视频大语言模型（VideoLLM）提出了首个黑盒成员推理攻击（MIA）框架，旨在识别特定视频是否被用于模型训练。针对视频特征与成员信号难以区分的挑战，该方法利用温度扰动下的生成文本语义漂移，并结合视频难度特征进行判定。实验证明VideoLLM存在明显的隐私泄露风险，为未来多模态模型的隐私保护研究提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27002v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of TempVideo-MIA.\", \"page\": 7, \"index\": 1, \"width\": 725, \"height\": 292}]"
motivation: 随着VideoLLM在海量异构视频数据上进行训练，评估其是否会泄露训练数据中特定视频的隐私信息变得至关重要。
method: 该方法通过在不同解码温度下查询模型并测量生成文本的语义漂移，结合视频的运动复杂度和时长等难度特征来推断成员身份。
result: 在LLaVA-Video-7B模型上的测试达到了0.68的AUC和0.63的准确率，验证了黑盒攻击的可行性。
conclusion: 研究揭示了VideoLLM在成员推理攻击面前的脆弱性，强调了系统性评估和缓解多模态模型隐私风险的紧迫性。
---

## 摘要
视频大语言模型（VideoLLMs）越来越多地在从异构来源收集的大规模视频-文本语料库上进行训练或指令微调，这引发了一个紧迫的隐私问题：外部审计员能否确定某个特定视频是否被用于训练？虽然成员推理攻击（MIAs）已在分类器以及最近的文本和图像生成模型中得到了广泛研究，但 VideoLLM 领域仍未被探索。这一设置具有挑战性，因为黑盒审计员只能观察到生成的文本，而成员信号与视频特定因素（如运动复杂度和时间跨度）纠缠在一起。在本文中，我们提出了一种针对 VideoLLMs 的黑盒 MIA，它将温度扰动生成与视频感知难度特征相结合。我们的核心直觉是，成员样本在不同解码温度下往往会诱导出更尖锐、更脆弱的生成行为，并且这一信号应与查询视频的内在难度共同解释。具体而言，我们在低和高温度下查询目标模型，并测量所得文本之间的语义漂移。我们针对 LLaVA-Video-7B-Qwen2-Video-Only 评估了该攻击，并实现了 0.68 的成员推理 AUC 和 0.63 的准确率。这些结果表明 Video-LLMs 容易受到黑盒成员推理攻击的影响，凸显了社区系统评估和减轻 VideoLLMs 隐私风险的迫切需求。

## Abstract
Video large language models (VideoLLMs) are increasingly trained or instruction-tuned on large-scale video--text corpora collected from heterogeneous sources, raising an immediate privacy question: can an external auditor determine whether a particular video was used during training? While membership inference attacks (MIAs) have been studied extensively for classifiers and, more recently, for text and image generation models, the VideoLLM setting remains unexplored. This setting is challenging because black-box auditors observe only generated text, whereas the membership signal is entangled with video-specific factors such as motion complexity and temporal span. In this paper, we present a black-box MIA targeting VideoLLMs that couples temperature-perturbed generation with video-aware difficulty features. Our key intuition is that member samples tend to induce sharper, more brittle generation behavior across decoding temperatures, and that this signal should be interpreted jointly with the intrinsic difficulty of the queried video. Concretely, we query the target model at low and high temperatures, measure the semantic drift between the resulting texts. We evaluate the attack against \texttt{LLaVA-Video-7B-Qwen2-Video-Only} and achieve a member inference AUC of 0.68 and accuracy of 0.63. These results demonstrate that Video-LLMs are vulnerable to black-box membership inference attacks, highlighting an urgent need for the community to systematically evaluate and mitigate privacy risks in VideoLLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Membership Inference Attacks Against Video Large Language Models》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着视频大语言模型（VideoLLM）在海量、异构的视频-文本数据集上进行预训练和指令微调，隐私风险日益凸显。核心问题是：外部审计员能否在仅拥有黑盒访问权限的情况下，判断某个特定视频是否被包含在模型的训练集中。
*   **背景挑战**：与文本或图像模型不同，视频模型的成员推理（MIA）面临“信号纠缠”问题。模型输出的不稳定性可能源于视频本身的复杂性（如剧烈运动、长时长），而非模型对数据的记忆。因此，如何从视频特征中解耦出成员身份信号是本文的研究重点。

### 2. 论文提出的方法论：TempVideo-MIA
*   **核心思想**：利用“温度扰动”观察生成行为的脆性。研究假设：如果模型见过某个样本，其生成的响应会高度集中在记忆的模式上；当解码温度升高时，成员样本的语义漂移（Semantic Drift）会比非成员样本更剧烈。
*   **关键技术细节**：
    1.  **温度扰动查询**：对同一视频使用相同提示词，分别在低温度（$\tau_{low}=0$）和高温度（$\tau_{high}=0.8$）下获取两段生成文本。
    2.  **语义相似度测量**：使用 CLIP 嵌入空间计算生成文本与参考文本之间的余弦相似度，得到 `sim_low` 和 `temp_diff`（两者之差）。
    3.  **视频感知难度特征**：
        *   **运动复杂度**：利用 SpyNet 提取光流，计算平均运动幅度的对数。
        *   **视频时长**：记录时长的对数，以消除采样稀疏性带来的干扰。
    4.  **交互特征**：将 `temp_diff` 分别与运动复杂度和时长相乘，捕捉非线性关联。
*   **特征向量**：最终构建一个 6 维特征向量（相似度、温度差、复杂度、时长及两个交互项），输入到二分类器中。

### 3. 实验设计
*   **目标模型**：LLaVA-Video-7B-Qwen2-Video-Only。
*   **数据集**：
    *   **成员样本**：随机抽取自 LLaVA-Video-178K（该模型的官方指令微调集）。
    *   **非成员样本**：抽取自 VideoChatGPT 数据集。
*   **Benchmark 构建**：共 700 个样本（350 成员 / 350 非成员）。为了防止模型通过视频时长这一“捷径”进行学习，实验采用了**长度匹配采样**，确保两组样本的时长分布一致。
*   **对比方法**：在特征提取后，对比了四种经典的分类器：逻辑回归 (LR)、随机森林 (RF)、支持向量机 (SVM) 和多层感知机 (MLP)。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **消耗评估**：由于该攻击属于黑盒攻击，且特征提取（光流计算和文本嵌入）相对轻量，主要的算力消耗在于对目标 VideoLLM 的重复推理查询。实验中进行了 100 次独立运行以确保统计稳定性。

### 5. 实验数量与充分性
*   **实验规模**：针对 700 个平衡样本进行了 100 次随机划分的重复实验。
*   **充分性评价**：
    *   **客观性**：通过 100 次重复运行并报告平均 AUC 和标准差，有效降低了单一数据划分带来的偶然性。
    *   **公平性**：采用了长度匹配协议，排除了时长分布不均导致的实验偏差。
    *   **局限性**：目前仅针对一个主流模型（LLaVA-Video）进行测试，尚未覆盖更多架构或更大规模的模型。

### 6. 主要结论与发现
*   **攻击有效性**：TempVideo-MIA 达到了 **0.68 的 AUC** 和 **0.63 的准确率**。这证明了 VideoLLM 确实存在可被利用的隐私泄露信号。
*   **信号来源**：实验证实，成员样本在温度变化时表现出更明显的语义不稳定性，且这种不稳定性在结合视频难度特征后更具辨识度。
*   **黑盒可行性**：即使不接触 Logits 或 Token 概率，仅凭生成的文本内容也能实现非随机的成员推理。

### 7. 优点
*   **首创性**：填补了视频大语言模型成员推理攻击的研究空白。
*   **解耦设计**：巧妙地引入运动复杂度和时长特征，解决了视频领域特有的混淆变量问题。
*   **现实性强**：采用纯黑盒设置，不依赖模型内部参数或概率分布，符合实际审计场景。

### 8. 不足与局限
*   **性能上限**：AUC 处于 0.68 左右，属于中等强度攻击，尚不能达到完美识别，说明隐私泄露信号在视频模态中相对隐蔽。
*   **覆盖范围**：实验仅限于视频描述（Captioning）任务，未探讨视频问答（VQA）或其他交互任务下的表现。
*   **防御缺失**：论文主要侧重于攻击的实现，尚未深入探讨如何通过数据去重或差分隐私等手段有效防御此类攻击。

（完）
