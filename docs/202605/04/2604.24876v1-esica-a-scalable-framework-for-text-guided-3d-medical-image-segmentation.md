---
title: "ESICA: A Scalable Framework for Text-Guided 3D Medical Image Segmentation"
title_zh: ESICA：一种可扩展的文本引导 3D 医学图像分割框架
authors: "Yu Xin, Gorkem Can Ates, Jun Ma, Sumin Kim, Ying Zhang, Kaleb E Smith, Kuang Gong, Wei Shao"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24876v1"
tags: ["query:medvid"]
score: 8.0
evidence: 文本引导的3D医学图像分割框架
tldr: 针对现有文本引导的3D医学图像分割模型计算开销大、语义对齐弱及细节捕捉不足的问题，本文提出ESICA框架。该框架通过相似性矩阵掩码预测、高效分解解码器及两阶段细化策略，实现了高精度的体积分割。在多模态基准测试中，ESICA达到了SOTA性能，且其轻量化版本在保持性能的同时显著减少了参数量，提升了临床部署的实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24876v1/fig-001.webp\", \"caption\": \"Table 3: Quantitative results on the CVPR-BiomedSegFM validation set, broken down by imaging modality. Metrics are reported for semantic segmentation (DSC, NSD) and instance segmentation (F1, DSC-TP). “N/A” indicates metrics not applicable for a given modality. Best results are in bold, and second-best are underlined.\", \"page\": 7, \"index\": 1, \"width\": 498, \"height\": 730}, {\"url\": \"assets/figures/arxiv/2604.24876v1/fig-002.webp\", \"caption\": \"Figure 2: Qualitative segmentation results across modalities on the CVPR-BiomedSegFM validation set.\", \"page\": 8, \"index\": 2, \"width\": 868, \"height\": 866}, {\"url\": \"assets/figures/arxiv/2604.24876v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the proposed ESICA framework. A 3D volume is partitioned into patches and embedded by an image encoder, while free-text prompts are processed by a parallel text encoder. The core transformer module fuses these multimodal features and enables iterative refinement via a prompt encoder and mask token. Patch-wise masks are reconstructed by the decoder and aligned with text features through a similarity matrix. Final full-volume predictions are obtained by aggregating the patch-level outputs.\", \"page\": 4, \"index\": 3, \"width\": 1033, \"height\": 324}]"
motivation: 现有的文本引导3D医学图像分割方法存在计算成本高、文本与体积特征对齐弱以及难以捕捉精细解剖细节的问题。
method: 提出了一种基于相似性矩阵的掩码预测机制，结合带有适配器模块的高效分解解码器和两阶段细化策略。
result: 在涵盖CT、MRI等五种模态的BiomedSegFM基准测试中，ESICA取得了最先进的分割精度，且轻量化版本表现出极高的效率。
conclusion: ESICA框架为高效、可扩展且临床可部署的文本引导3D医学图像分割系统提供了先进的解决方案。
---

## 摘要
文本引导的 3D 医学图像分割允许用户直接使用自然语言指定感兴趣区域，为基于类别和基于空间提示的模型提供了一种灵活的替代方案。这种范式避免了对预定义标签集的依赖，减少了歧义输出，并更自然地契合临床工作流程。然而，现有的文本引导框架通常计算开销大，文本与体素特征对齐较弱，且无法捕捉精细的解剖细节。我们提出了 ESICA，这是一个轻量级且可扩展的框架，通过三项创新解决了这些挑战：(1) 基于相似性矩阵的掩码预测公式，增强了语义对齐；(2) 带有适配器模块的高效分解解码器，用于精确的体素解码；(3) 双阶段细化策略，用于锐化边界并解决不确定区域。为了提高训练稳定性和泛化能力，ESICA 采用了由仅正样本预训练和平衡微调组成的双阶段方案。在涵盖五种成像模态（CT、MRI、PET、超声和显微镜）的 CVPR BiomedSegFM 基准测试中，ESICA 达到了最先进的分割精度，而紧凑的 ESICA4 Lite 变体在参数量大幅减少的情况下实现了相似的分割性能，从而获得了卓越的效率与精度权衡。我们的框架推动了文本引导分割向高效、可扩展且可临床部署的系统迈进。代码将在 https://github.com/mirthAI/ESICA 公开。

## Abstract
Text guided 3D medical image segmentation offers a flexible alternative to class based and spatial prompt based models by allowing users to specify regions of interest directly in natural language. This paradigm avoids reliance on predefined label sets, reduces ambiguous outputs, and aligns more naturally with clinical workflows. However, existing text guided frameworks are often computationally expensive, exhibit weak text volume feature alignment, and fail to capture fine anatomical details. We propose ESICA, a lightweight and scalable framework that addresses these challenges through three innovations: (1) a similarity matrix based mask prediction formulation that enhances semantic alignment, (2) an efficient decomposed decoder with adapter modules for accurate volumetric decoding, and (3) a two pass refinement strategy that sharpens boundaries and resolves uncertain regions. To improve training stability and generalization, ESICA adopts a two stage scheme consisting of positive only pretraining followed by balanced fine tuning. On the CVPR BiomedSegFM benchmark spanning five imaging modalities (CT, MRI, PET, ultrasound, and microscopy), ESICA achieves state of the art segmentation accuracy, while the compact ESICA4 Lite variant attains similar segmentation performance with substantially fewer parameters, yielding a superior efficiency accuracy trade off. Our framework advances text guided segmentation toward efficient, scalable, and clinically deployable systems. Code will be made publicly available at https://github.com/mirthAI/ESICA.

---

## 论文详细总结（自动生成）

### ESICA: 文本引导 3D 医学图像分割的可扩展框架论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：传统的 3D 医学图像分割依赖于预定义的类别标签或繁琐的空间提示（如点、框），这限制了模型在处理未见解剖结构、罕见病变及跨模态任务时的灵活性。
*   **研究动机**：虽然文本引导分割提供了更自然的交互方式，但现有框架面临三大挑战：
    1.  **计算开销巨大**：难以在资源受限的临床环境中部署。
    2.  **语义对齐薄弱**：文本描述与复杂的 3D 体积特征之间缺乏精确的映射。
    3.  **细节捕捉不足**：难以处理细小或不规则的解剖边界。

#### 2. 方法论
ESICA 框架通过以下核心技术实现高效、精确的分割：
*   **相似性矩阵掩码预测**：不再直接回归掩码，而是计算上采样后的图像特征与文本嵌入向量之间的点积（Similarity Matrix），从而强制实现强语义一致性，减少了对复杂辅助损失函数的依赖。
*   **DCFormerV2 图像编码器**：在分解卷积（将 3D 卷积分解为 H、W、D 三个 1D 分支）的基础上，增加了一个 3×3×3 的标准卷积分支，以兼顾全局上下文和局部精细细节。
*   **高效解码器与适配器**：基于 EffiDec3D 设计，引入适配器模块（Adapter）处理跳跃连接特征，在保持低参数量的同时提升特征融合质量。
*   **两阶段细化策略（Two-pass Refinement）**：模型进行两次前向传播。第一遍生成初步预测，第二遍将初步掩码作为空间反馈输入提示编码器，通过迭代锐化边界。
*   **双阶段训练方案**：
    1.  **仅正样本预训练**：建立稳定的图像-文本对齐。
    2.  **平衡微调**：引入负样本（不包含目标结构的图像/文本对），提升模型的判别能力和泛化性。

#### 3. 实验设计
*   **数据集**：使用 **CVPR-BiomedSegFM** 基准数据集，包含 35,792 个 3D 体积数据，涵盖 68 个子数据集。
*   **场景/模态**：覆盖五种成像模态：CT、MRI、PET、3D 超声（3DUS）和显微镜图像（Microscopy）。
*   **对比方法**：与当前最先进的文本引导模型进行对比，包括 **CAT**、**SAT** 和 **Text3DSAM**。
*   **评估指标**：
    *   语义分割：Dice 系数 (DSC)、归一化表面 Dice (NSD)。
    *   实例分割：F1 分数、真阳性 Dice (DSC-TP)。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA B200 GPU**。
*   **算力配置**：96 核 CPU，1024 GB 系统内存。
*   **训练时长**：
    *   第一阶段（预训练）：约 24 小时（30 个 Epoch）。
    *   第二阶段（微调）：约 4 小时（5 个 Epoch）。
    *   总计训练时间约为 28 小时。
*   **模型规模**：标准版 ESICA 为 252M 参数，轻量版 ESICA-Lite 仅为 130M 参数。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 5 种模态上分别进行了详细的定量评估，并提供了全指标的平均性能对比。
*   **消融实验**：针对训练策略（有无负样本微调、有无预训练）、架构组件（有无相似性掩码、有无 DCFormerV2）以及细化迭代次数（1次 vs 2次 vs 3次）进行了深入分析。
*   **充分性评价**：实验设计非常充分，涵盖了从宏观性能到微观组件的全面验证。通过跨模态的大规模数据测试，证明了模型在异构数据上的鲁棒性，对比实验选取了同类最新的 SOTA 模型，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先**：ESICA 在所有评估指标上均达到了 SOTA 水平，尤其在 MRI 和显微镜模态下表现突出。
*   **效率卓越**：ESICA-Lite 在参数量远少于 SAT 的情况下，实现了优于 CAT 的性能，证明了高效架构设计的有效性。
*   **对齐机制有效**：相似性矩阵公式显著提升了文本与图像的对齐精度，尤其对细小结构的定位效果明显。
*   **迭代必要性**：两次细化迭代是精度与效率的最佳平衡点，过多的迭代（如3次）反而可能导致误差累积。

#### 7. 优点
*   **高效率与可扩展性**：通过分解卷积和轻量化解码器，显著降低了 3D 处理的计算负担。
*   **创新的对齐方式**：相似性矩阵预测机制简化了训练目标，提高了语义一致性。
*   **临床实用性**：支持自然语言交互，且提供了轻量化版本，更易于集成到实际临床工作流中。
*   **训练稳定性**：提出的两阶段课程学习方案有效解决了大规模多模态训练中的不稳定性问题。

#### 8. 不足与局限
*   **实例检测挑战**：尽管分割精度高，但实例级别的检测性能（F1 分数）仍有提升空间，表明识别物体“是否存在”比“勾勒边界”更具挑战性。
*   **负样本挖掘**：目前的负采样策略相对简单，未来可能需要更针对性的硬负样本挖掘（Hard Negative Mining）来进一步减少误报。
*   **语言复杂性**：对于极其复杂或组合式的长文本指令，模型的理解能力和鲁棒性尚未进行极端压力测试。

（完）
