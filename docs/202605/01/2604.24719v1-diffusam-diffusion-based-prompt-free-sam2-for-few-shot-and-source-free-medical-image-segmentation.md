---
title: "DiffuSAM: Diffusion-Based Prompt-Free SAM2 for Few-Shot and Source-Free Medical Image Segmentation"
authors: "Tal Grossman, Noa Cahan, Lev Ayzenberg, Hayit Greenspan"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24719v1"
tags: ["query:medvid"]
score: 9.0
evidence: 基于扩散模型的SAM2医学图像分割适配
tldr: 本研究提出DiffuSAM，一种基于扩散模型的SAM2改进框架，旨在解决医疗影像分割中对专家提示词的依赖及跨域迁移难题。通过轻量化扩散先验从冻结的SAM2特征中生成掩码嵌入，并结合空间一致性约束，实现了在少样本和无源无监督域自适应设置下的高效、无提示分割，在BTCV和CHAOS数据集上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24719v1/fig-001.webp\", \"caption\": \"Fig. 3: Diffusion inference steps. Top: CT; Bottom: MRI.\", \"page\": 3, \"index\": 1, \"width\": 486, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.24719v1/fig-002.webp\", \"caption\": \"Table 1: Few-shot settings: (a) model requirements and (b) Dice results per organ on BTCV dataset.\", \"page\": 4, \"index\": 2, \"width\": 502, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.24719v1/fig-003.webp\", \"caption\": \"Fig. 1: An outline of our proposed method. (a) In our proposed network, we add a diffusion prior (in Green) to generate memory embeddings from the image embeddings, thereby removing the need for a user prompt (in Red). During training, the SAM2 components all remain frozen. (b) Diffusion Prior model. During training, a diffusion model takes a memory embedding to which noise has been added, the matching image embedding, an embedding of the current time step, and a label. The system learns to use the image embedding to remove the noise in successive time steps. In inference, it generates a memory embedding by starting with pure noise and an image embedding and removing noise iteratively according to that embedding.\", \"page\": 2, \"index\": 3, \"width\": 477, \"height\": 513}]"
motivation: 针对SAM2在医疗领域迁移能力有限且高度依赖专家提示词的问题，探索无需提示的自动化分割方案。
method: 利用轻量化扩散先验从冻结的SAM2图像特征中合成掩码嵌入，并结合前序切片的空间一致性信息输入解码器。
result: 在BTCV和CHAOS数据集的CT与MRI影像上，DiffuSAM在少样本和无源域自适应任务中均取得了极具竞争力的性能。
conclusion: DiffuSAM证明了结合扩散模型与SAM2架构可以有效实现高效、精准且无需人工干预的医疗影像分割。
---

## Abstract
Segmentation models such as Segment Anything Model (SAM) and SAM2 achieve strong prompt-driven zero-shot performance. However, their training on natural images limits domain transfer to medical data. Consequently, accurate segmentation typically requires extensive fine-tuning and expert-designed prompts. We propose DiffuSAM, a diffusion-based adaptation of SAM2 for prompt-free medical image segmentation. Our framework synthesizes SAM2-compatible segmentation mask-like embeddings via a lightweight diffusion-prior from off-the-shelf frozen SAM2 image features. The generated embeddings are integrated into SAM2's mask decoder to produce accurate segmentations, thereby eliminating the need for user prompts. The diffusion prior is further conditioned on previously segmented slices, enforcing spatial consistency across volumes. Evaluated on the BTCV and CHAOS datasets for CT and MRI under Source-Free Unsupervised Domain Adaptation (SF-UDA) and Few-Shot settings, DiffuSAM achieves competitive performance with efficient training and inference. Code is available upon request from the corresponding author.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DiffuSAM** 的新框架，旨在解决基础模型 SAM2 在医学图像分割中对人工提示（Prompts）的依赖以及跨领域迁移难的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然 SAM 和 SAM2 在自然图像分割上表现优异，但在医学影像领域面临两大挑战：
    1.  **领域差异**：医学图像的纹理、对比度和解剖结构与自然图像迥异，导致零样本性能下降。
    2.  **提示依赖**：SAM2 依赖用户提供的点或框（Prompts）来引导分割，这在临床工作流中效率低下且依赖专家经验。
*   **研究动机**：开发一种无需人工提示（Prompt-free）、能够利用 SAM2 强大的特征提取能力、并能适应少样本（Few-shot）和无源域自适应（SF-UDA）场景的医学分割模型。

### 2. 方法论
DiffuSAM 的核心思想是利用**扩散模型（Diffusion Model）**作为先验，从冻结的 SAM2 图像特征中自动合成“掩码嵌入（Memory Embeddings）”，从而替代人工提示。
*   **扩散先验（Diffusion Prior）**：
    *   使用一个轻量级的 UNet 扩散模型。
    *   **训练阶段**：输入冻结的 SAM2 图像编码器提取的特征（$z_{img}$）和类别标签，学习生成对应的 SAM2 存储嵌入（$z_{mem}$）。
    *   **损失函数**：结合了扩散先验损失（$L_{prior}$）和分割损失（$L_{seg}$），确保生成的嵌入既符合分布又能产生准确的掩码。
*   **空间一致性（Volumetric Consistency）**：
    *   为了处理 3D 医学体积数据，模型在生成当前切片的嵌入时，会参考相邻切片的分割预测。
    *   通过跨切片调节（Cross-slice conditioning），确保了解剖结构在空间上的连续性。
*   **推理流程**：在推理时，模型从纯噪声开始，仅需 2 步扩散迭代即可生成存储嵌入，随后通过 SAM2 的解码器输出分割结果，全程无需人工干预。

### 3. 实验设计
*   **数据集**：
    *   **BTCV**：包含 30 个腹部 CT 扫描卷。
    *   **CHAOS**：包含 20 个 T2-SPIR MRI 扫描卷。
    *   **目标器官**：脾脏、右肾、左肾、肝脏。
*   **实验场景与 Benchmark**：
    1.  **少样本学习（Few-shot）**：仅使用 BTCV 数据集的 10%（3 个卷）进行训练，在剩余 90% 上测试。对比方法包括 UNet、UNETR、SAM、SAM2、FATE-SAM。
    2.  **无源无监督域自适应（SF-UDA）**：在 BTCV（CT）上训练，直接在 CHAOS（MRI）上测试，无需访问源域数据。对比方法包括 DPL、DFG。
*   **评价指标**：Dice 系数。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A5000** GPU。
*   **显存占用**：训练和推理阶段均**小于 5 GB**，体现了极高的轻量化特性。
*   **训练细节**：训练 7800 次迭代，Batch Size 为 4，使用 Adam 优化器。
*   **推理效率**：推理时仅需 **2 个扩散步数**，显著提升了生成速度。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 CT 和 MRI 两种模态，针对少样本和跨模态域自适应两个关键任务进行了验证。
*   **消融实验**：专门对比了有无“3D 空间一致性（Volumetric Consistency）”的效果（DiffuSAM vs DiffuSAM-no 3D），证明了空间约束对 Dice 分数的显著提升（约 4.5% 到 4.8%）。
*   **客观性**：通过 t-SNE 可视化展示了生成的嵌入与真实嵌入的分布接近程度，实验对比了经典的监督学习模型和最新的基于 SAM 的变体，结果较为客观。

### 6. 主要结论与发现
*   **性能优异**：在少样本设置下，DiffuSAM 的平均 Dice 达到 87.24%，优于所有对比方法（包括需要人工提示的 SAM2）。
*   **跨模态能力强**：在 CT 到 MRI 的 SF-UDA 任务中，DiffuSAM 达到了 85.2% 的 Dice 分数，与当前最先进的 DFG 方法持平。
*   **扩散先验的有效性**：证明了扩散模型可以有效地在 SAM2 的潜在空间内学习解剖分布，从而消除对人工提示的依赖。

### 7. 优点
*   **无需提示（Prompt-free）**：真正实现了 SAM2 在医学领域的自动化应用。
*   **轻量且高效**：显存需求极低（<5GB），且推理步数极短（2步），适合临床实时应用。
*   **空间感知**：通过跨切片调节解决了医学影像中常见的 3D 连续性问题。
*   **通用性**：在少样本和无源域自适应场景下均表现稳健。

### 8. 不足与局限
*   **对比范围有限**：论文承认未与 MedSAM（在海量医学数据上全量微调的模型）进行直接对比，虽然 DiffuSAM 胜在无需全量微调和提示，但在绝对精度上可能仍有差距。
*   **解剖结构局限**：实验主要集中在腹部器官，尚未在更复杂的病灶（如肿瘤）或更细微的结构（如血管）上验证。
*   **依赖预训练权重**：模型性能高度依赖于 SAM2 原始图像编码器的特征提取质量，如果原始编码器在某些极端医学模态下失效，扩散先验也难以补救。

（完）
