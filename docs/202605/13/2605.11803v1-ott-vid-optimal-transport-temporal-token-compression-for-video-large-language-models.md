---
title: "OTT-Vid: Optimal Transport Temporal Token Compression for Video Large Language Models"
title_zh: OTT-Vid：面向视频大语言模型的最优传输时序 Token 压缩
authors: "Minseok Kang, Minhyeok Lee, Jungho Lee, Minjung Kim, Donghyeong Kim, Dayeon Lee, Heeseung Choi, Ig-jae Kim, Sangyoun Lee"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11803v1"
tags: ["query:lvu"]
score: 9.0
evidence: 针对长视频扩展的Video-LLM时间令牌压缩技术
tldr: "针对视频大语言模型推理成本随视频长度增加而激增的问题，本文提出OTT-Vid，一种基于最优传输的免训练时间令牌压缩框架。该方法通过空间剪枝识别关键内容，并利用非均匀令牌质量的最优传输来动态分配压缩预算，有效平衡了令牌重要性与匹配成本。实验证明，在仅保留10%令牌的情况下，OTT-Vid在多项基准测试中显著优于现有方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11803v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of temporal compression strategies, where white-shaded regions denote compressed tokens. For simplicity, only compression of stationary object regions is depicted. Similaritydriven compression merges stationary objects away due to high cross-frame similarity, losing persistence evidence. OTT-Vid jointly considers token importance and similarity, preserving semantically representative tokens across frames.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.11803v1/fig-002.webp\", \"caption\": \"Table 1: Main results on Qwen2.5-VL-7B across four VQA and two VTG benchmarks. VQA scores are reported as accuracy and VTG scores as mIoU. Avg. (%) denotes the mean performance retention relative to the uncompressed baseline. Best results per retention ratio are in bold.\", \"page\": 8, \"index\": 2, \"width\": 810, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2605.11803v1/fig-003.webp\", \"caption\": \"Table 2: Cross-backbone evaluation on LLaVA-OneVision-7B and LLaVA-Video-7B (VQA only, as these models do not support temporal grounding). Avg. (%) denotes the mean performance retention relative to the uncompressed baseline. Best results per setting are in bold.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2605.11803v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of OTT-Vid. (Sec 3.2) Each frame t is encoded into visual tokens Ft, spatially pruned to retained tokens St, and converted into a mass vector mt from per-token importance. (Sec 3.3) Sinkhorn OT computes a transport plan Pt and transport difficulty Wt for each neighboring frame pair. (Sec 3.4) Compression budgets Bt are allocated in inverse proportion to transport difficulty, and the transport plan determines which tokens to compress.\", \"page\": 4, \"index\": 4, \"width\": 818, \"height\": 433}]"
motivation: 现有的视频令牌压缩方法忽略了令牌的语义角色及帧间压缩性的差异，导致推理效率与性能难以兼得。
method: 提出两阶段框架，先进行空间剪枝，再通过非均匀令牌质量和位置感知成本的最优传输算法动态分配帧间压缩预算。
result: "在六个基准测试中，OTT-Vid在仅保留10%令牌的情况下，保留了95.8%的VQA性能和73.9%的VTG性能。"
conclusion: OTT-Vid通过最优传输理论实现了高效且自适应的视频令牌压缩，为长视频理解提供了低成本且高性能的解决方案。
---

## 摘要
随着视频大语言模型（Video-LLMs）扩展到更长、更复杂的视频，由于跨帧累积的大量视觉 Token，其推理成本迅速增长。免训练的 Token 压缩已成为解决这一瓶颈的实用方案。然而，现有的时序压缩方法主要依赖于跨帧 Token 相似性或分割启发式方法，忽略了每个 Token 在其帧内的语义角色，且未能根据每对帧的可压缩性调整压缩强度。在这项工作中，我们提出了 OTT-Vid，一种基于传输分配的时序 Token 压缩框架。我们的方法包含两个阶段：空间剪枝识别每帧内的代表性内容，随后在相邻帧之间求解最优传输（OT）以估计时序可压缩性。我们将此 OT 公式化为非均匀 Token 质量，从而保护具有重要语义的 Token 免受过度压缩，并采用感知局部性的代价函数来捕捉特征和空间差异。由此产生的传输方案共同平衡了 Token 重要性和匹配代价，而其总代价定义了每对帧的传输难度，我们利用该难度动态分配压缩预算。在涵盖视频问答（VQA）和时序定位（VTG）的六个基准测试上的实验表明，OTT-Vid 在仅保留 10% Token 的情况下，保留了 95.8% 的 VQA 性能和 73.9% 的 VTG 性能，持续优于现有的最先进免训练压缩方法。

## Abstract
As Video Large Language Models (Video-LLMs) scale to longer and more complex videos, their inference cost grows rapidly due to the large volume of visual tokens accumulated across frames. Training-free token compression has emerged as a practical solution to this bottleneck. However, existing temporal compression methods rely primarily on cross-frame token similarity or segmentation heuristics, overlooking each token's semantic role within its frame and failing to adapt compression strength to the compressibility of each frame pair. In this work, we propose OTT-Vid, a transport-derived allocation framework for temporal token compression. Our approach consists of two stages: spatial pruning identifies representative content within each frame, and optimal transport (OT) is then solved between neighboring frames to estimate temporal compressibility. We formulate this OT with non-uniform token mass, which protects semantically important tokens from aggressive compression, and a locality-aware cost that captures both feature and spatial disparities. The resulting transport plan jointly balances token importance and matching cost, while its total cost defines the transport difficulty of each frame pair, which we use to allocate compression budgets dynamically. Experiments on six benchmarks spanning video question answering and temporal grounding show that OTT-Vid preserves 95.8% of VQA and 73.9% of VTG performance while retaining only 10% of tokens, consistently outperforming existing state-of-the-art training-free compression methods.

---

## 论文详细总结（自动生成）

以下是对论文《OTT-Vid: Optimal Transport Temporal Token Compression for Video Large Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着视频大语言模型（Video-LLMs）处理的视频长度和复杂度增加，视觉 Token 的激增导致推理成本（尤其是 Prefill 阶段）呈平方级增长。现有的免训练 Token 压缩方法存在两个主要缺陷：
*   **忽略语义角色：** 仅依赖跨帧相似性进行压缩，容易误删虽然高度相似但具有关键语义的持久性对象（如静止的物体），导致模型丢失关于持续时间、连续性和事件边界的证据。
*   **缺乏自适应预算分配：** 视频不同片段的冗余度不同，现有方法通常采用统一的压缩强度，无法针对每对帧的可压缩性动态调整预算。

### 2. 方法论：核心思想与关键技术
OTT-Vid 提出了一个基于**最优传输（Optimal Transport, OT）**的分配框架，将时序压缩分解为“Token 保护优先级”和“跨帧传输难度”两个问题。

*   **空间剪枝与质量分配（Spatial Pruning & Mass Assignment）：**
    *   首先对单帧进行空间压缩，利用“显著性加权覆盖”目标保留代表性 Token。
    *   **核心创新：** 为每个保留的 Token 分配“质量（Mass）”。为了保护重要 Token，采用**反向设计**：语义越重要的 Token 分配的质量越小，从而在 OT 求解中更难被传输（即更难被压缩）；不重要的 Token 质量大，更容易被传输合并。
*   **时序最优传输（Temporal OT）：**
    *   在相邻帧之间建立传输代价矩阵（结合语义距离和空间距离）。
    *   通过 Sinkhorn 算法求解传输方案（Transport Plan）。
*   **自适应预算分配（Budget Allocation）：**
    *   计算每对帧的“传输难度”（总传输代价）。难度越低，说明冗余度越高。
    *   根据难度通过负 Softmax 动态分配压缩预算，实现非均匀压缩。
*   **执行压缩：** 根据传输方案和分配的预算，对 Token 进行合并（Merge）或剪枝（Prune）。

### 3. 实验设计
*   **数据集/基准：**
    *   **视频问答（VQA）：** MVBench, VideoMME, LongVideoBench, MLVU。
    *   **时序定位（VTG）：** Charades-TimeLens, ActivityNet-TimeLens（VTG 任务对时序证据的保留要求极高）。
*   **对比方法：** HoliTom (NeurIPS'25), FastVID (NeurIPS'25), FlashVID (ICLR'26), UniComp (CVPR'26) 等最先进的免训练方法。
*   **模型骨干：** Qwen2.5-VL-7B, LLaVA-OneVision-7B, LLaVA-Video-7B。

### 4. 资源与算力
*   **硬件：** 使用单张 **NVIDIA RTX Pro 5000 (Blackwell)** GPU。
*   **软件环境：** Python 3.10, PyTorch 2.7.0, CUDA 12.8。
*   **效率表现：** 在 10% Token 保留率下，OTT-Vid 实现了约 **2.0倍的 TTFT（首字生成时间）加速**，峰值显存占用约为 18.22 GB，与现有高效方法相当。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 6 个主流基准测试，并在 3 个不同的 Video-LLM 家族上验证了泛化性。
*   **消融实验：** 针对核心组件（重要性质量、自适应预算）、超参数（温度系数、时序占比 $\gamma$、代价阈值等）以及不同的空间剪枝策略进行了详尽的对比。
*   **客观性：** 实验在相同的评估协议（lmms-eval）下进行，对比了不同保留率（10% 到 25%）下的性能曲线，结果显示在极端压缩（10%）下优势尤为明显，具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越：** 在仅保留 10% Token 的情况下，OTT-Vid 保留了 95.8% 的 VQA 性能和 73.9% 的 VTG 性能。
*   **VTG 任务突破：** 相比于传统相似性驱动的方法在 VTG 任务上的大幅溃败（性能保留仅 50%-60%），OTT-Vid 展现了极强的时序结构保护能力。
*   **自适应性：** 证明了非均匀分配预算比统一分配更能有效利用有限的 Token 容量。

### 7. 优点与亮点
*   **理论优雅：** 将 Token 压缩问题巧妙地映射为最优传输问题，利用 OT 的边际约束自然地处理了 Token 的重要性。
*   **免训练：** 插件式设计，无需对预训练模型进行任何微调。
*   **任务覆盖全面：** 不仅关注 VQA，还深入探讨了对时序敏感的定位任务，填补了现有压缩研究的评价空白。

### 8. 不足与局限
*   **并行计算限制：** 为了效率，OTT-Vid 在初始空间剪枝的基础上并行计算所有帧对的传输方案，这忽略了前序压缩对后续帧表示的累积影响。
*   **计算开销：** 虽然比 LLM 推理快得多，但 Sinkhorn 迭代和 leave-one-out 质量分配仍带来了一定的额外 CPU/GPU 计算开销（约 48ms）。
*   **极端长视频：** 尽管在长视频基准上表现良好，但对于超长视频（如数小时），两帧之间的 OT 建模可能不足以捕捉更长程的时序依赖。

（完）
