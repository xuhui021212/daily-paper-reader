---
title: "TCP-SSM: Efficient Vision State Space Models with Token-Conditioned Poles"
title_zh: TCP-SSM：具有令牌条件极点的高效视觉状态空间模型
authors: "Sara Shoouri, Morteza Tavakoli Taba, Hun-Seok Kim"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11563v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于长程视觉任务的高效视觉状态空间模型
tldr: 本研究针对视觉状态空间模型（SSM）中递归动力学隐式且难以控制的问题，提出了TCP-SSM框架。该方法通过引入显式的实数和复共轭稳定极点，并结合令牌条件的半径与角度调制，实现了对记忆行为的动态调整。通过分组极点共享和轻量级低秩输入路径，TCP-SSM在显著降低计算复杂度的同时，增强了模型的可解释性与性能，为长程视觉任务提供了高效的替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11563v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of the dominant pole-derived memory horizon at layers 13, 20, and 22 (of 24) for an example image. Brighter regions denote larger τ (slower decay, longer memory). T1, T2, and T3 highlight the longest memory, fastest decay, and strongest radius-weighted oscillation (ρmaxθ/π). Across depth, the pole-conditioned recurrence evolves from coarse object-level memory toward discriminative regions such as the face and eyes.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2605.11563v1/fig-002.webp\", \"caption\": \"Table 1: Comparison on ImageNet-1K. SSM FLOPs are reported for models with an SSM branch.\", \"page\": 7, \"index\": 2, \"width\": 781, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2605.11563v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of TCP-SSM Model. Left: TCP-SSM is inserted into the Vision Mamba model. Right: For token xt, a shared grouped pole bank defines a stable base denominator Qg(z −1), while token-conditioned radius and angle modulation produce the local denominator Qt,g(z −1). A lightweight low-rank numerator forms the driving signal ηt, and the grouped recurrent branch outputs yt. The final output ot combines recurrent memory with a learned diagonal direct path.\", \"page\": 3, \"index\": 3, \"width\": 818, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2605.11563v1/fig-004.webp\", \"caption\": \"Table 2: Comparison on ADE20K semantic segmentation. * denotes our produced results.\", \"page\": 8, \"index\": 4, \"width\": 694, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.11563v1/fig-005.webp\", \"caption\": \"Figure 3: Accuracy-computation trade-off on EVM-B.\", \"page\": 9, \"index\": 5, \"width\": 457, \"height\": 298}]"
motivation: 现有视觉状态空间模型（SSM）的递归动力学通常是隐式的，导致长路径下的记忆行为难以控制且效率有待提升。
method: 提出TCP-SSM框架，通过实数和复共轭极点显式建模递归动力学，并利用令牌条件下的半径和角度调制实现动态记忆调整。
result: "在图像分类、语义分割和目标检测任务中，该方法在保持或提升精度的同时，将SSM计算复杂度降低了高达44%。"
conclusion: TCP-SSM通过引入可解释且稳定的极点机制，为构建高效、可控的视觉状态空间模型提供了一种新颖且有效的方案。
---

## 摘要
状态空间模型（SSMs）已成为长程视觉任务中注意力模型的一个极具吸引力的替代方案，提供了具有线性复杂度的输入依赖型递归。然而，大多数高效的 SSM 变体通过修改扫描路径、分辨率或遍历模式来降低计算成本，而在很大程度上使递归动力学保持隐式。因此，模型的状态依赖记忆行为难以控制，特别是在紧凑的主干网络中，长扫描路径可能会超过有效记忆范围。我们提出了令牌条件极点 SSM（TCP-SSM），这是一种结构化的选择性 SSM 框架，在提高效率的同时，通过稳定极点使递归动力学变得显式且可解释。TCP-SSM 使用以下部分构建每个扫描算子：1) 建模单调或正负交替衰减的实极点，以及 2) 捕捉阻尼振荡响应的共轭复极点。通过使用有界半径和角度调制，TCP-SSM 将共享的基础极点转换为令牌依赖极点，使每个扫描步骤能够根据当前的视觉令牌调整其记忆行为，同时保持极点稳定性。为了实现实际的可扩展性，我们将分组极点共享与轻量级低秩输入路径相结合，从而产生了一个保持线性时间扫描效率的高效扫描算子。在图像分类、语义分割和目标检测任务中，TCP-SSM 在 Vision Mamba 风格的模型中将 SSM 的计算复杂度降低了高达 44%，同时保持或超过了基准精度。

## Abstract
State Space Models (SSMs) have emerged as a compelling alternative to attention models for long-range vision tasks, offering input-dependent recurrence with linear complexity. However, most efficient SSM variants reduce computation cost by modifying scan routes, resolutions, or traversal patterns, while largely leaving the recurrent dynamics implicit. Consequently, the model's state-dependent memory behavior is difficult to control, particularly in compact backbones where long scan paths can exceed the effective memory horizon. We propose Token-Conditioned Poles SSM (TCP-SSM), a structured selective SSM framework that improves efficiency while making recurrence dynamics explicit and interpretable through stable poles. TCP-SSM builds each scan operator with 1) real poles that model monotone or sign-alternating decay, and 2) complex-conjugate poles that capture damped oscillatory responses. Using bounded radius and angle modulation, TCP-SSM converts shared base poles into token-dependent poles, allowing each scan step to adapt its memory behavior to the current visual token while preserving pole stability. For practical scalability, we integrate grouped pole sharing with a lightweight low-rank input pathway, yielding an efficient scan operator that preserves linear-time scan complexity. Across image classification, semantic segmentation, and object detection, TCP-SSM reduces SSM computation complexity up to 44% in Vision Mamba-style models while maintaining or surpassing baseline accuracy.

---

## 论文详细总结（自动生成）

这篇论文提出了 **TCP-SSM**（Token-Conditioned Poles SSM），旨在通过显式控制递归动力学来提升视觉状态空间模型（SSM）的效率和可解释性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：虽然 SSM（如 Mamba）在处理长程依赖方面具有线性复杂度，但在视觉任务中，其递归动力学通常是“隐式”的。
*   **核心问题**：在紧凑型模型中，长扫描路径往往超过了模型的有效记忆范围，导致信息丢失。现有的改进多集中在“扫描路径”的设计上，而忽略了“递归结构”本身。
*   **整体含义**：论文从控制理论出发，认为应该通过显式的“极点（Poles）”配置来管理记忆衰减和振荡行为，从而使模型在减少计算量的同时，具备更强的自适应记忆能力。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用 $z$ 域传递函数的极点来参数化递归过程。极点的半径决定记忆衰减速度，角度决定振荡频率。
*   **关键技术细节**：
    *   **稳定分组基础分母**：将通道分为 $G$ 组，每组共享一组基础极点。包括实极点（模拟单调衰减）和复共轭极点（模拟阻尼振荡）。通过约束确保极点始终在单位圆内，保证系统稳定性。
    *   **令牌条件调制（TCP）**：根据当前令牌 $x_t$ 动态预测半径缩放因子 $s^{(\rho)}$ 和角度缩放因子 $s^{(\theta)}$，从而实时调整记忆行为。
    *   **轻量级低秩分子**：为了降低计算开销，使用低秩分解（Rank-$r_f$）来构建输入驱动信号，避免了传统 SSM 中昂贵的密集矩阵运算。
    *   **特征蒸馏训练**：采用预训练的原始 SSM（如 Vim）作为教师模型，通过中间特征层蒸馏引导学生模型（TCP-SSM）学习。

### 3. 实验设计
*   **数据集**：
    *   **图像分类**：ImageNet-1K。
    *   **语义分割**：ADE20K（使用 UPerNet 框架）。
    *   **目标检测与实例分割**：COCO 2017（使用 Cascade Mask R-CNN 框架）。
*   **基准模型（Benchmark）**：Vision Mamba (Vim) 和 EfficientVMamba (EVM)。
*   **对比方法**：包括 ViT, Swin Transformer, DeiT, 以及多种 SSM 变体（如 VMamba, LocalMamba, PlainMamba 等）。

### 4. 资源与算力
*   **硬件设备**：使用了 **6 台 NVIDIA RTX PRO 6000 Blackwell GPU**。
*   **训练时长**：
    *   Vim-T 变体训练了 600 个 epoch。
    *   EVM-T 训练了 700 个 epoch，EVM-B 训练了 250 个 epoch。
*   **超参数**：学习率通常设为 $1 \times 10^{-4}$，采用 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了计算机视觉的三大核心任务（分类、分割、检测），并针对 Vim 和 EVM 两种不同的 SSM 架构进行了验证。
*   **消融实验**：非常充分。作者研究了蒸馏损失的影响、令牌条件调制的必要性、极点分组数 $G$ 的影响，以及精度与计算量（FLOPs）之间的权衡曲线。
*   **客观性**：通过在相同训练方案下重新运行基准模型（如 Vim-T*），确保了对比的公平性。

### 6. 主要结论与发现
*   **高效性**：在保持或提升精度的前提下，TCP-SSM 将 Vim 的 SSM 模块计算量降低了约 **40.6%**，将 EVM-Base 的计算量降低了高达 **70.6%**（仅损失极小精度）。
*   **性能提升**：在 ADE20K 分割任务上，TCP-Vim-T1 以更低的计算量实现了 41.1% 的 mIoU，优于原始 Vim-T 的 40.6%。
*   **可解释性**：通过可视化极点导出的记忆范围（Memory Horizon），发现模型在深层会自动将记忆聚焦于判别性区域（如人脸、眼睛）。

### 7. 优点
*   **理论扎实**：将控制理论中的极点概念引入深度学习，提供了明确的数学解释性和稳定性保证。
*   **通用性强**：作为一种插件式的递归算子，可以轻松集成到现有的各种视觉 SSM 架构中。
*   **精度-效率平衡极佳**：在大幅削减 FLOPs 的同时，性能几乎无损甚至有所增强。

### 8. 不足与局限
*   **超参数敏感性**：引入了极点阶数、低秩维度、分组数等额外超参数，可能需要针对不同任务进行精细调优。
*   **未解决扫描顺序问题**：该方法优化的是“如何记忆”，但并未直接解决 2D 图像展平为 1D 序列时固有的空间结构破坏问题（仍依赖于外部扫描策略）。
*   **训练依赖蒸馏**：实验表明，如果没有教师模型的蒸馏引导，TCP 结构的收敛效果会受到一定影响。

（完）
