---
title: "Hi-GaTA: Hierarchical Gated Temporal Aggregation Adapter for Surgical Video Report Generation"
title_zh: Hi-GaTA：用于手术视频报告生成的层级门控时间聚合适配器
authors: "Kedi Sun, Chaohui Dang, Yue Feng, James Glasbey, Theodoros N. Arvanitis, Le Zhang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11208v1"
tags: ["query:medvid"]
score: 10.0
evidence: 手术过程的自动评估报告及长序列的时间适配器
tldr: 针对手术视频报告生成中时空对齐难和高质量数据稀缺的问题，本文提出了Hi-GaTA框架。该框架包含一个在4万分钟手术视频上预训练的编码器Sur40k，以及一个分层门控时间聚合适配器，能将长视频高效压缩为LLM可理解的视觉前缀。通过在包含214个模拟手术视频的新基准数据集上进行实验，该方法在生成临床级报告方面优于现有大模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11208v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of our proposed method. Left: The Perception–Alignment–Reasoning pipeline for surgical video report generation. Right: Detailed architecture of the HiGaTA module.\", \"page\": 4, \"index\": 1, \"width\": 725, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2605.11208v1/fig-002.webp\", \"caption\": \"Table 1. Joint comparison of video encoders and LLM backbones (mean ± std).\", \"page\": 7, \"index\": 2, \"width\": 725, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.11208v1/fig-003.webp\", \"caption\": \"Fig. 2. Qualitative comparison of generated reports. Our Hi-GaTA approach produces more clinically accurate and comprehensive descriptions than LLaVA-Med-v1.5-7B [8] and Qwen2.5-VL-7B [2], closer to the ground truth.\", \"page\": 8, \"index\": 3, \"width\": 725, \"height\": 206}]"
motivation: 旨在解决手术视频与语言推理对齐困难以及高质量、隐私保护的手术报告数据集匮乏的问题。
method: 提出了Hi-GaTA适配器，结合预训练的Sur40k编码器，通过分层时间金字塔和文本条件双交叉注意力机制实现视频特征压缩与对齐。
result: 实验表明该方法在手术视频报告生成任务上取得了最佳性能，显著优于现有的多模态大模型基准。
conclusion: 该研究通过专用编码器和高效的时间聚合适配器，为自动化、临床级的手术程序评估报告生成提供了有效的解决方案。
---

## 摘要
针对手术过程的自动化、临床级评估报告可以减轻文档记录负担并提供客观反馈，但由于难以将密集时空视频表示与基于语言的推理对齐，以及缺乏高质量、保护隐私的数据集，这仍然具有挑战性。为了填补这一空白，我们建立了一个基准，包含 214 个高质量模拟手术视频，并配有外科医生撰写的评估报告。基于这一资源，我们提出了一种用于手术视频报告生成的“感知-对齐-推理”框架，其核心是 Hi-GaTA，这是一种新型轻量级时间适配器，通过短程到长程的时间聚合，将长视频序列高效压缩为紧凑的、与大语言模型（LLM）兼容的视觉前缀标记。为了实现稳健的视觉感知，我们在 40,000 分钟的公开手术视频上预训练了 Sur40k（一种手术专用的 ViViT 风格视频编码器），以捕获细粒度的时空过程先验。Hi-GaTA 采用具有文本调节双重交叉注意力的时间金字塔，并通过跨层级门控融合和深度递增策略提高多尺度一致性。最后，我们使用 LoRA 对 LLM 骨干网络进行微调，以便在有限的监督下生成连贯且风格一致的手术报告。实验表明，我们的方法实现了最佳的整体性能，在强大的多模态大语言模型（MLLM）基线基础上取得了持续的提升。消融研究进一步验证了所提各组件的有效性。

## Abstract
Automated, clinician-grade assessment reports for surgical procedures could reduce documentation burden and provide objective feedback, yet remain challenging due to the difficulty of aligning dense spatio-temporal video representations with language-based reasoning and the scarcity of high-quality, privacy-preserving datasets. To address this gap, we establish a benchmark comprising 214 high-quality simulated surgical videos paired with surgeon-authored evaluation reports. Building on this resource, we propose a Perception-Alignment-Reasoning framework for surgical video report generation, featuring Hi-GaTA, a novel lightweight temporal adapter that efficiently compresses long video sequences into compact, LLM-compatible visual prefix tokens through short-to-long-range temporal aggregation. For robust visual perception, we pretrain Sur40k, a surgical-specific ViViT-style video encoder on 40,000 minutes of public surgical videos to capture fine-grained spatio-temporal procedural priors. Hi-GaTA employs a temporal pyramid with text-conditioned dual cross-attention, and improves multi-scale consistency through cross-level gated fusion and an increasing-depth strategy. Finally, we fine-tune the LLM backbone using LoRA to enable coherent and stylistically consistent surgical report generation under limited supervision. Experiments show our approach achieves the best overall performance, with consistent gains over strong Multimodal Large Language Model (MLLM) baselines. Ablation studies further validate the effectiveness of each proposed component.

---

## 论文详细总结（自动生成）

### 论文总结：Hi-GaTA 用于手术视频报告生成的层级门控时间聚合适配器

#### 1. 核心问题与整体含义（研究动机和背景）
手术过程的自动化评估和报告生成对于减轻临床医生负担、提供客观反馈具有重要意义。然而，该领域面临三大挑战：
*   **时空对齐困难**：手术视频具有长程时间依赖性，现有模型难以捕捉全局工作流与局部精细动作之间的复杂关系。
*   **计算冗余与噪声**：直接将密集视频特征输入大语言模型（LLM）会导致计算开销巨大，且冗余信息会干扰模型的推理能力。
*   **高质量数据稀缺**：缺乏配有专业医生标注的高质量、隐私保护的手术视频报告数据集。
本文旨在通过提出一个“感知-对齐-推理”框架，并引入专门的视频编码器和时间适配器来解决上述问题。

#### 2. 论文提出的方法论
论文提出了一个三阶段框架：
*   **感知阶段（Sur40k 编码器）**：基于 ViViT 架构，在 40,000 分钟的公开手术视频上通过自监督学习（InfoNCE 损失）进行预训练，以捕获手术特有的时空先验知识。
*   **对齐阶段（Hi-GaTA 适配器）**：这是核心创新点，一种轻量级插件式适配器。
    *   **时间金字塔池化（TPP）**：通过不同窗口大小提取多尺度时间特征。
    *   **双交叉注意力（DCA）**：使可学习的查询（Queries）同时与视觉特征和文本提示（Prompt）对齐。
    *   **跨层级门控融合**：通过 Sigmoid 门控机制将短程上下文注入长程摘要，确保多尺度语义一致性。
    *   **深度递增策略**：在层级结构中增加计算深度，强化长程抽象能力。
*   **推理阶段（LLM + LoRA）**：将压缩后的视觉前缀标记（Visual Prefix Tokens）输入 LLM，利用 LoRA 进行微调，生成风格一致的临床报告。

#### 3. 实验设计
*   **数据集**：建立了一个包含 **214 个高质量模拟手术视频**（1-9 分钟）的基准数据集，每个视频配有外科医生撰写的结构化评分（OSATS 风格）和叙述性反馈。
*   **基准测试（Benchmark）**：
    *   **编码器对比**：对比了 Dinov3、ViViT、XCLIP 等通用编码器。
    *   **LLM 骨干对比**：对比了 Gemma2-2B、Llama3.2 (1B/3B)、Qwen2.5 (1.5B/3B)。
    *   **多模态基线对比**：对比了 LLaVA-Med-v1.5-7B 和 Qwen2.5-VL-7B。
*   **评价指标**：BLEU, ROUGE-L, METEOR, MedBERTScore, CIDEr。

#### 4. 资源与算力
*   **硬件**：所有实验均在 **单张 40G NVIDIA A100 GPU** 上完成。
*   **训练细节**：
    *   Sur40k 预训练：使用 AdamW 优化器，余弦退火学习率。
    *   报告生成训练：分为两个阶段（先训练适配器，再通过 LoRA 微调 LLM），每阶段各 30 个 epoch。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了编码器消融、LLM 骨干消融、Hi-GaTA 组件消融（门控融合、深度递增）以及与主流 MLLM 的对比实验。
*   **充分性评价**：实验设计较为全面，涵盖了从视觉感知到语言生成的各个环节。虽然 214 个样本在深度学习领域不算大，但在获得专家标注的医疗视频领域属于合理规模。通过 MedBERTScore 等语义指标评估，增强了结果的客观性。

#### 6. 主要结论与发现
*   **领域专用感知至关重要**：Sur40k 在所有指标上均显著优于通用视频编码器，证明了手术领域预训练的必要性。
*   **Hi-GaTA 的有效性**：通过将长视频压缩为仅 16 个视觉前缀标记，模型不仅降低了计算量，还通过层级聚合提升了对复杂手术流程的理解。
*   **模型规模并非唯一决定因素**：较小的 Qwen2.5-1.5B 在配合 Sur40k 和 Hi-GaTA 时，表现优于某些参数量更大的模型。

#### 7. 优点
*   **高效的压缩机制**：Hi-GaTA 适配器解决了长视频输入 LLM 时的冗余问题，实现了“轻量级且即插即用”。
*   **临床对齐**：通过与外科医生合作建立基准，并使用 GPT-5 辅助整合报告，确保了生成内容的专业性和风格一致性。
*   **多尺度建模**：门控融合机制有效地结合了局部动作细节和全局手术阶段。

#### 8. 不足与局限
*   **数据场景受限**：数据集基于模拟手术（物理模型），而非真实的人体临床手术，可能存在领域差距（Domain Gap）。
*   **样本量较小**：214 个视频的规模可能限制了模型的泛化能力，未来需在多中心、多类型手术数据上验证。
*   **评估依赖性**：参考报告的合成使用了 GPT-5，虽然保留了医生风格，但可能引入自动化工具的潜在偏见。

（完）
