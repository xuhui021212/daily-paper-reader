---
title: Contrastive Learning under Noisy Temporal Self-Supervision for Colonoscopy Videos
title_zh: 结肠镜视频中噪声时间自监督下的对比学习
authors: "Luca Parolari, Pietro Gori, Lamberto Ballan, Carlo Biffi, Loic Le Folgoc"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12320v1"
tags: ["query:medvid"]
score: 9.0
evidence: 结肠镜视频中基于时序结构的自监督关联
tldr: 本研究针对结肠镜视频中息肉轨迹表示学习的难题，提出了一种基于噪声时间自监督的对比学习方法。由于手动标注息肉关联成本高昂，该方法利用视频的时间结构自动生成关联，并引入噪声感知对比损失来处理潜在的错误关联。实验表明，该轻量级模型仅需27个视频训练，在息肉检索、重识别、大小估计和组织学分类等任务上均优于现有基线，甚至媲美大型基础模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12320v1/fig-001.webp\", \"caption\": \"Fig. 1: Our method. (Top) Tracklets detected in a colonoscopy video are used to derive the self-supervision and produce bags of tracklets for contrastive learning. At the beginning of training (c ≈ 0), bags are constituted by temporally adjacent tracklets, yielding low-variation views that are likely to correspond to the same polyp. Later (c → 1), bags progressively include more distant tracklets, increasing diversity but also the chance of incorrect associations. (Bottom) The model learns from within-bag associations while limiting noisy ones. In the example, the loss maximizes the similarity between tracklet 18, 15 and 21 while down-weighting the contribution of noisy associations such as tracklet 24.\", \"page\": 3, \"index\": 1, \"width\": 708, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2605.12320v1/fig-002.webp\", \"caption\": \"Table 1: Comparison with State-of-the-Art. All metrics are in percentage. Best, second best; shared bolding denotes no statistical difference (p > 0.05). *We note that EndoFM [31] is trained on SUN-SEG [17], a superset of SUN [22] annotated with segmentation masks, leading to an overlap with our test set.\", \"page\": 7, \"index\": 2, \"width\": 714, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2605.12320v1/fig-003.webp\", \"caption\": \"Table 2: Ablation study. (Top) Impact of the exponential rank-based sampling used to construct bags, and curriculum strategy that controls the trade-off between safer and more diverse associations during training. (Bottom) Effect of applying the proposed loss on frame or tracklet embeddings, or both.\", \"page\": 8, \"index\": 3, \"width\": 712, \"height\": 234}]"
motivation: 传统的监督对比学习依赖昂贵的息肉关联标注，限制了结肠镜视频表示学习的规模化应用。
method: 利用结肠镜视频的时间结构提取自监督关联，并设计了一种噪声感知对比损失函数以应对时间关联中的错误。
result: 在息肉检索、重识别、大小估计和组织学分类等多项下游任务中，该方法显著优于自监督和监督基线。
conclusion: 该研究证明了通过结合时间结构与噪声感知机制，可以在极小规模的数据集上训练出高性能且鲁棒的医疗影像表示模型。
---

## 摘要
学习息肉轨迹段（tracklets）的鲁棒表示是实现多种人工智能辅助结肠镜检查应用的关键，涵盖从息肉特征描述到自动报告和检索的各个方面。监督对比学习是学习此类表示的一种有效方法，但它通常依赖于正确的正负样本定义。收集这些标签需要将视频中描绘同一底层息肉实体的轨迹段关联起来，这不仅成本高昂，而且需要专门的临床专业知识。在这项工作中，我们利用结肠镜检查程序的顺序工作流，从时间结构中推导出自监督关联。由于从时间上推导出的关联不能保证完全正确，我们引入了一种噪声感知对比损失来处理噪声关联。我们展示了所学表示在多个下游任务中的有效性，包括息肉检索与重识别、大小估计和组织学分类。我们的方法优于先前的自监督和监督基准模型，并且在所有任务中都达到或超过了最近的基础模型，而仅使用了在27个视频上训练的轻量级编码器。代码可在 https://github.com/lparolari/ntssl 获取。

## Abstract
Learning robust representations of polyp tracklets is key to enabling multiple AI-assisted colonoscopy applications, from polyp characterization to automated reporting and retrieval. Supervised contrastive learning is an effective approach for learning such representations, but it typically relies on correct positive and negative definitions. Collecting these labels requires linking tracklets that depict the same underlying polyp entity throughout the video, which is costly and demands specialized clinical expertise. In this work, we leverage the sequential workflow of colonoscopy procedures to derive self-supervised associations from temporal structure. Since temporally derived associations are not guaranteed to be correct, we introduce a noise-aware contrastive loss to account for noisy associations. We demonstrate the effectiveness of the learned representations across multiple downstream tasks, including polyp retrieval and re-identification, size estimation, and histology classification. Our method outperforms prior self-supervised and supervised baselines, and matches or exceeds recent foundation models across all tasks, using a lightweight encoder trained on only 27 videos. Code is available at https://github.com/lparolari/ntssl.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对结肠镜视频中息肉轨迹表示学习的新框架，旨在通过利用视频的时间结构来消除对昂贵的人工标注（息肉实体关联）的依赖。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在结肠镜视频分析中，学习息肉轨迹段（Tracklets）的鲁棒表示对于息肉重识别、大小估计和病理分类至关重要。
*   **研究动机**：
    *   **标注成本高**：传统的监督对比学习需要将视频中不同时间点出现的同一息肉进行关联，这需要极高的临床专业知识且耗时耗力。
    *   **自监督挑战**：结肠镜环境视觉复杂（视角变化、遮挡、形变、光照不均），现有的自监督方法与监督方法之间仍存在性能差距。
    *   **时间结构潜力**：结肠镜检查具有顺序性，时间上接近的观察结果极大概率指向同一息肉实体，这可以作为一种天然的自监督信号。

### 2. 核心方法论
论文提出了 **NTSSL (Noisy Temporal Self-Supervision Learning)** 框架，包含以下关键技术：
*   **基于时间结构的自监督关联**：
    *   **Bag（包）构建**：对于每个锚点轨迹段，根据时间距离对同一视频中的其他轨迹段进行排序（Rank）。
    *   **指数采样**：不直接选取最近的样本，而是通过指数分布采样构建包含 $K$ 个候选正样本的“包”。这增加了样本的多样性。
    *   **课程学习 (Curriculum Learning)**：训练初期只采样时间极近的样本（噪声低、多样性低），随着训练进行，逐渐增大采样范围（噪声增加、多样性增加）。
*   **噪声感知对比损失 (Noise-Aware Contrastive Loss)**：
    *   **核心思想**：承认时间关联可能存在错误（即“包”中可能混入不同息肉的轨迹）。
    *   **公式逻辑**：利用 `log-sum-exp` 算子对正样本进行平滑近似最大化。损失函数不要求包内所有样本都与锚点相似，只要其中**至少一个**最匹配的样本比其他负样本更相似即可。这种松弛机制允许模型自动忽略包内的噪声关联。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：REAL-Colon（仅使用 27 个视频，85 个息肉）。
    *   **测试/下游任务集**：REAL-Colon 测试集、SUN database、PolypSize、PolypsSet（共涉及约 195 个视频，210 个息肉实体）。
*   **下游任务 (Benchmarks)**：
    1.  **检索 (Retrieval)**：衡量嵌入空间的聚类能力。
    2.  **重识别 (ReID)**：判断两段轨迹是否为同一息肉。
    3.  **大小估计 (Size Estimation)**：二分类（≤5mm vs >5mm）。
    4.  **组织学分类 (Histology)**：二分类（腺瘤 vs 非腺瘤）。
*   **对比方法**：
    *   **自监督基准**：TPC, MVE, SFE。
    *   **全监督基准**：Sup, TA（使用真实实体标签）。
    *   **基础模型 (Foundation Models)**：通用视觉模型 (DinoV2/V3, V-JEPA2) 和内窥镜专用模型 (EndoFM, SurgeNet)。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA A100 GPU (64GB)，8核 CPU，64GB RAM。
*   **训练细节**：
    *   编码器基于 ResNet-50 和 Transformer Encoder。
    *   轨迹长度 $L=8$。
    *   仅在 27 个视频的小规模数据集上进行训练。
    *   文中未明确给出具体的总训练时长，但提到非线性探测（下游任务）训练了 20 个 epoch。

### 5. 实验充分性与客观性
*   **实验数量**：涵盖了 4 类不同的临床相关任务，并在 4 个公开数据集上进行了验证，实验设计较为全面。
*   **消融实验**：详细分析了指数采样、课程学习以及损失函数作用层级（帧级 vs 轨迹级）的影响，证明了各组件的有效性。
*   **公平性**：所有基准模型均在相同的数据分割下重新训练或评估，并提供了参数量和计算量 (FLOPs) 的对比，确保了比较的客观性。

### 6. 主要结论与发现
*   **性能卓越**：该方法在所有任务上均显著优于先前的自监督方法。
*   **超越监督学习**：在无需实体标签的情况下，性能甚至超过了使用真实标签的监督对比学习方法（如在检索 mAP 上提升明显）。
*   **以小博大**：使用轻量级架构（约 25M 参数）在极小数据集（27个视频）上训练的结果，媲美甚至超过了拥有 11 亿参数、在数亿张图像上预训练的 DinoV2-giant 模型。

### 7. 优点
*   **临床实用性强**：利用结肠镜检查的自然工作流（顺序性）构建监督信号，无需额外人工标注。
*   **鲁棒性高**：创新的噪声感知损失函数有效解决了时间关联中的“假阳性”问题。
*   **高效轻量**：相比于庞大的基础模型，该模型更易于集成到实时的手术辅助系统中。

### 8. 不足与局限
*   **依赖检测质量**：实验基于 Ground-truth 的检测框构建轨迹，实际应用中若前端检测器效果差，可能会影响表示学习。
*   **训练数据规模**：虽然在小数据集上表现优异，但尚未在大规模未标注结肠镜视频库上验证其扩展性（Scaling Law）。
*   **特定场景限制**：如果医生在检查过程中频繁反复观察多个息肉（非线性工作流），时间关联的噪声可能会大幅增加，挑战损失函数的容错极限。

（完）
