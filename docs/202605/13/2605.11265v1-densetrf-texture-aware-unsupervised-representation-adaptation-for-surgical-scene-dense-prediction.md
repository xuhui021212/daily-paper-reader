---
title: "DenseTRF: Texture-Aware Unsupervised Representation Adaptation for Surgical Scene Dense Prediction"
title_zh: DenseTRF：用于手术场景密集预测的纹理感知无监督表示自适应
authors: "Guiqiu Liao, Matjaž Jogan, Daniel A. Hashimoto"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11265v1"
tags: ["query:medvid"]
score: 9.0
evidence: 手术场景稠密预测与手术区域预测
tldr: 针对手术场景中密集预测任务面临的分布偏移和泛化性差的问题，本文提出了DenseTRF框架。该框架利用基于纹理中心注意力的槽注意机制学习不变的视觉结构，并通过无监督方式将表示适配到目标分布。通过在槽注意力上进行密集预测调节和模型合并策略，DenseTRF在多种手术程序的跨分布泛化实验中显著优于现有先进模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11265v1/fig-001.webp\", \"caption\": \"Table 2. Comparison with foundation model-based adaptation under low-data regime.\", \"page\": 7, \"index\": 1, \"width\": 718, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2605.11265v1/fig-002.webp\", \"caption\": \"Fig. 1. Architecture of the proposed network, where the dense head is conditioned on object-centric representations learned via Slot Attention and optimized using both reconstruction and supervised dense prediction objectives.\", \"page\": 3, \"index\": 2, \"width\": 721, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2605.11265v1/fig-003.webp\", \"caption\": \"Fig. 2. (a) Slot Attention (SA) adaptation via periodic model merging, which anchors the adapted model to the base representation during specialization. (b) Example dense prediction tasks across Thoracic, POEM, and Cholec surgical datasets.\", \"page\": 4, \"index\": 3, \"width\": 713, \"height\": 275}]"
motivation: 手术计算机视觉模型在部署时常因训练数据无法覆盖实际场景的多样性而面临分布偏移，导致泛化性能下降。
method: 提出一种基于纹理中心注意力的自监督表示适配框架，利用槽注意力捕获不变纹理特征，并结合模型合并策略进行无监督适配。
result: 在多种手术程序的实验中，该方法在跨分布泛化性能上显著优于现有的最先进分割模型和测试时适配方法。
conclusion: DenseTRF通过提取纹理感知的不变表示，有效提升了手术场景密集预测任务在面对领域偏移时的鲁棒性和泛化能力。
---

## 摘要
手术计算机视觉中的密集预测任务（如分割和手术区域预测）可以为腹腔镜和机器人手术提供有价值的指导。然而，由于训练数据集很少能涵盖部署过程中遇到的变异性，这些模型经常受到分布偏移的影响，导致泛化能力较差。我们提出了 DenseTRF，这是一个基于以纹理为中心的注意力的自监督表示自适应框架。我们的方法利用槽注意力（slot attention）来学习捕获不变视觉结构的纹理感知表示。通过在无监督的情况下将这些表示自适应到目标分布，DenseTRF 显著提高了对领域偏移的鲁棒性。该框架通过将密集预测以槽注意力为条件以及模型合并策略来实现。在多种手术程序上的实验表明，与最先进的分割模型和用于密集预测任务的测试分布自适应方法相比，该方法提高了跨分布的泛化能力。

## Abstract
Dense prediction tasks in surgical computer vision, such as segmentation and surgical zone prediction, can provide valuable guidance for laparoscopic and robotic surgery. However, these models often suffer from distribution shifts, as training datasets rarely cover the variability encountered during deployment, leading to poor generalization. We propose DenseTRF, a self-supervised representation adaptation framework based on texture-centric attention. Our method leverages slot attention to learn texture-aware representations that capture invariant visual structures. By adapting these representations to the target distribution without supervision, DenseTRF significantly improves robustness to domain shifts. The framework is implemented through conditioning dense prediction on slot attention and model merging strategies. Experiments across multiple surgical procedures demonstrate improved cross-distribution generalization in comparison to state-of-the-art segmentation models and test-distribution adaptation methods for dense prediction tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DenseTRF** 的框架，旨在解决手术场景中密集预测任务（如组织分割、手术区域预测）在面对领域偏移（Domain Shift）时泛化性差的问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：手术视频分析中的密集预测模型在实际部署时，常因解剖结构差异、光照变化、视角切换及手术阶段不同而面临严重的**分布偏移**。
*   **研究背景**：
    *   现有的监督学习方法依赖大量标注，但在医疗领域，高质量的密集标注成本极高且难以跨机构获取。
    *   现有的视觉大模型（Foundation Models）虽然泛化性强，但在特定手术任务上仍需针对性适配。
    *   手术场景中，组织的**纹理（Texture）**通常比多变的几何形状（Shape）更具统计不变性，但现有方法未能充分利用这一特性。

### 2. 核心方法论
DenseTRF 的核心思想是结合**槽注意力（Slot Attention）**与**无监督表示自适应**，提取纹理感知的视觉结构。

*   **关键技术细节**：
    1.  **纹理感知表示学习**：利用槽注意力机制将图像特征分解为多个“槽（Slots）”。为了强化纹理特征并削弱空间几何偏见，引入了**补丁顺序排列（Patch Order Permutation）**，迫使模型根据外观而非位置进行特征分组。
    2.  **无监督测试分布自适应**：
        *   **双分支结构**：维护一个在多领域数据上训练的“基础分支（Base Branch）”和一个仅在无标签目标域数据上训练的“目标分支（Target Branch）”。
        *   **周期性模型合并（Periodic Model Merging）**：通过加权平均两个分支的参数，防止模型在目标域过度拟合（灾难性遗忘），同时实现表示的平滑迁移。
    3.  **密集预测调节**：密集预测头（Dense Head）不仅使用编码器特征，还级联了（i）适配后的特征、（ii）重建特征、（iii）槽注意力掩码（SA masks），从而利用对象中心（Object-centric）的信息。
*   **算法流程**：首先在多域数据上预训练槽注意力模型，然后在部署阶段利用无标签目标数据进行自适应调整，最后在极少量标注数据（1%-5%）下微调预测头。

### 3. 实验设计
*   **数据集/场景**：
    1.  **Thoracic**：机器人辅助肺癌手术视频（胸外科）。
    2.  **POEM**：经口内镜下肌切开术视频（内镜手术）。
    3.  **Cholec80/CholecSeg8K**：腹腔镜胆囊切除术视频。
*   **Benchmark 与对比方法**：
    *   **SOTA 分割模型**：Mask2Former, SegMan, Swin-UNETR, UNet+ResNet, UNet+MIT。
    *   **大模型适配方法**：SAM, DINO-v1, DINO-v3, CLIP, 以及专门的测试时适配方法 Slot-TTA。
*   **评估指标**：Dice 系数、交并比 (IoU) 和豪斯多夫距离 (HD)。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到了使用 DINO-v3 作为预训练编码器，训练迭代次数为 5000 次，采用了早停机制（Early Stopping）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三种完全不同的手术类型（胸外科、内镜、腹腔镜），验证了跨领域的通用性。
    *   设置了 1% 到 5% 的极低数据标注比例实验，模拟现实中的数据匮乏场景。
    *   **消融实验**：分别验证了槽注意力（SA）、自适应策略（Adaptation）和多源特征融合（Concatenation）的有效性。
*   **客观性**：所有实验均独立运行 5 次并报告标准差，使用了 Bonferroni 校正进行显著性检验（p < 0.05），实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能领先**：在所有数据集和标注比例下，DenseTRF 均显著优于现有的分割模型和大模型适配方法。例如在 Cholec 数据集上，Dice 分数比最强的基准（UNet+MIT）高出约 7.84%。
*   **边界精度高**：DenseTRF 在豪斯多夫距离（HD）指标上表现优异，说明其生成的分割边缘更贴合解剖结构。
*   **自适应的必要性**：消融实验显示，如果不进行自适应（w/o ada），直接引入槽注意力有时甚至会降低性能，证明了模型合并策略在处理领域偏移中的关键作用。

### 7. 优点
*   **创新性视角**：将手术场景理解从传统的“形状驱动”转向“纹理驱动”，更符合手术视频的视觉特性。
*   **数据效率极高**：在仅有 1% 标注数据的情况下仍能保持稳健性能，具有极强的临床应用潜力。
*   **架构灵活**：通过模型合并而非复杂的对抗训练实现自适应，易于集成到现有的手术导航系统中。

### 8. 不足与局限
*   **算力成本未知**：虽然模型头是轻量级的，但槽注意力的迭代计算和双分支合并可能会增加推理前后的计算开销。
*   **实时性探讨不足**：论文未提及推理延迟（FPS），对于需要实时反馈的手术导航任务，这一指标至关重要。
*   **超参数敏感性**：模型合并的频率和权重（λ）可能需要针对不同手术类型进行手动调优，缺乏自动化的参数选择机制。

（完）
