---
title: "LDDR: Linear-DPP-Based Dynamic-Resolution Frame Sampling for Video MLLMs"
title_zh: LDDR：基于线性 DPP 的视频 MLLM 动态分辨率帧采样
authors: "Jingfeng Chen, Jiawen Qian, Wendi Deng, Yinuo Guo, Jiaqi Yu, Sicong Leng, Raghuveer Thirukovalluru, Bhuwan Dhingra"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11477v1"
tags: ["query:lvu"]
score: 9.5
evidence: 在有限的视觉Token预算下从长视频中选择信息丰富的帧
tldr: 针对视频大模型在有限Token预算下难以高效提取关键帧的问题，本文提出LDDR框架。该框架无需训练且即插即用，利用线性行列式点过程（DPP）在任务相关空间内进行查询感知的帧选择，并结合Group DPP重要性指标实现动态分辨率分配。LDDR在提升处理速度的同时，显著增强了模型对长短视频的理解能力，在多个基准测试中均优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11477v1/fig-001.webp\", \"caption\": \"Table 1: Main results across four video understanding benchmarks and five MLLM backbones under matched visual-token budgets. * denotes Model supporting dynamic resolution, † denotes LD, which applies linear DPP frame selection, and ‡ denotes LDDR, which adds GD-Guided dynamic resolution. LD improves over baselines, while LDDR achieves further gains on dynamic-resolution models.\", \"page\": 6, \"index\": 1, \"width\": 781, \"height\": 984}, {\"url\": \"assets/figures/arxiv/2605.11477v1/fig-002.webp\", \"caption\": \"Table 5: Ablation study on the density-prior exponent τ . τ = 0 removes the density prior, while τ = 1 is used as the default setting in all main experiments.\", \"page\": 14, \"index\": 2, \"width\": 741, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.11477v1/fig-003.webp\", \"caption\": \"Figure 1: LDDR Overview. LDDR first extracts frame and query embeddings, then applies Linear Feature-space DPP to select a diverse and query-relevant candidate frame set. GD-importance-based dynamic resolution further allocates visual tokens adaptively across selected frames.\", \"page\": 3, \"index\": 3, \"width\": 818, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2605.11477v1/fig-004.webp\", \"caption\": \"Figure 2: Sampling runtime under different total numbers of input frames on Video-MME. Left: comparison across different frame sampling methods. Right: comparison between our Linear DPP implementation and the original DPP. The runtime of Linear DPP scales approximately linearly as the total number of frames increases.\", \"page\": 8, \"index\": 4, \"width\": 821, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2605.11477v1/fig-005.webp\", \"caption\": \"Figure 3: Phases latency breakdown, including LongCLIP processing and Frame Sampling time.\", \"page\": 8, \"index\": 5, \"width\": 813, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2605.11477v1/fig-006.webp\", \"caption\": \"Figure 4: Qualitative examples of LDDR on video question answering tasks.\", \"page\": 9, \"index\": 6, \"width\": 817, \"height\": 371}]"
motivation: 现有的视频采样方法在处理长视频时，往往无法兼顾全局依赖性、计算效率和Token预算的灵活分配。
method: 提出基于线性DPP的查询感知采样，并利用Group DPP重要性度量来指导帧的保留与动态分辨率分配。
result: 在四个视频基准测试中，LDDR在低预算和高预算场景下分别比次优方法提升了2.5和1.6个百分点，且运行速度比标准DPP提升3倍。
conclusion: LDDR是一种高效、通用的视频采样方案，能有效平衡信息丰富度与计算开销，显著提升多模态大模型的视频理解性能。
---

## 摘要
多模态大语言模型（MLLM）中的视频理解需要在有限的视觉 token 预算下，从冗长且冗余的视频中选择具有信息量的帧。现有方法通常依赖于均匀采样、逐点相关性评分、分块选择或智能体探索，这些方法要么忽略了全局依赖关系，要么引入了巨大的额外开销。我们提出了 LDDR（基于线性 DPP 的动态分辨率），这是一个无需训练、即插即用且具备预算感知能力的视频帧采样框架。LDDR 在任务条件特征空间中执行查询感知的行列式点过程（DPP）帧选择，与标准 DPP 基准相比，实现了 3 倍的运行速度提升。它进一步引入了组 DPP（Group DPP）重要性指标来指导帧保留和动态分辨率分配，为具有信息量且非冗余的帧分配更多 token，同时对用处较小的帧进行降采样或剪枝。在涵盖短、中、长视频的四个视频基准测试中，LDDR 始终优于次优基准，在预算受限的情况下提升了 2.5 分，在高预算场景下提升了 1.6 分。这些改进在包括开源和闭源模型在内的多个 MLLM 骨干网络中均得到了一致体现。定性分析证实，相关帧被选中并分配了更高的预算，从而促进了视频理解能力的提升。

## Abstract
Video understanding in multimodal large language models requires selecting informative frames from long, redundant videos under limited visual-token budgets. Existing methods often rely on uniform sampling, point-wise relevance scoring, chunk-wise selection, or agentic exploration, which either miss global dependencies or introduce substantial overhead. We propose LDDR (Linear DPP-Based Dynamic Resolution), a training-free, plug-and-play, and budget-aware video frame sampling framework. LDDR performs query-aware Determinantal Point Process (DPP) frame selection in a task-conditioned feature space, achieving a 3x runtime speedup over standard DPP baselines. It further introduces a Group DPP importance metric to guide frame retention and dynamic resolution allocation, assigning more tokens to informative, non-redundant frames while downscaling or pruning less useful ones. Across four video benchmarks spanning short-, medium-, and long-range videos, LDDR consistently outperforms the next-best baselines, achieving gains of 2.5 points under budget-constrained settings and 1.6 points in high-budget scenarios. These improvements are consistently observed across multiple MLLM backbones, including both open- and closed-source models. Qualitative analysis confirms that relevant frames are selected and allocated a higher budget, facilitating improved video understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《LDDR: Linear-DPP-Based Dynamic-Resolution Frame Sampling for Video MLLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在多模态大语言模型（MLLM）处理长视频时，面临的核心矛盾是**海量的视频帧冗余**与**有限的视觉 Token 预算**之间的冲突。
*   **研究动机**：现有的采样方法（如均匀采样、Top-K 相关性评分）要么忽略了帧间的全局依赖（导致选出的帧高度相似），要么计算开销巨大（如 Agent 探索或标准 DPP 的 $O(T^2)$ 复杂度），或者无法灵活分配不同帧的 Token 权重。
*   **核心目标**：开发一种**无需训练、即插即用、计算高效**的采样框架，既能根据查询（Query）选择最相关的多样化帧，又能根据信息量动态分配分辨率。

### 2. 方法论：核心思想与关键技术
LDDR 框架由两个核心模块组成：

*   **线性特征空间 DPP（Linear Feature-space DPP）**：
    *   **核心思想**：传统的行列式点过程（DPP）在核矩阵（Kernel Matrix）空间计算，复杂度随视频长度 $T$ 呈平方增长。LDDR 将其转到特征空间（Feature-space）进行贪婪 MAP 推断。
    *   **技术细节**：通过将查询相关的权重融入帧特征 $\Phi = \text{diag}(\tilde{r})\hat{F}$，直接在 $d$ 维特征空间进行正交化更新。这证明了与标准 DPP 在数学上等价，但将复杂度从 $O(T^2d + TK^2)$ 降低到 $O(KTd)$，实现了相对于视频长度的线性扩展。
*   **基于 Group DPP (GD) 重要性的动态分辨率分配**：
    *   **核心思想**：并非所有选中的帧都同等重要。LDDR 利用 GD 重要性分数来衡量某一帧对已选集合的“边际贡献”。
    *   **技术细节**：GD 分数定义为包含该帧与不包含该帧时集合行列式的比值，几何上代表该帧在特征空间中的“残差唯一性”。
    *   **流程**：根据 GD 分数对候选帧排序，通过二分查找确定在总 Token 预算 $C_{total}$ 下可保留的最优帧数和每帧的分辨率（Token 数在 256 到 1024 之间动态调整）。

### 3. 实验设计
*   **数据集/基准测试**：涵盖了短、中、长不同维度的四个主流基准：**Video-MME**、**LongVideoBench**、**LVBench** 和 **MLVU**。
*   **对比方法**：
    *   基础方法：Uniform Sampling（均匀采样）。
    *   SOTA 采样策略：AKS（二分分区相关性）、Q-frame（查询感知动态分辨率）、FOCUS（最优臂选择）、MDP3（分块 DPP）。
*   **骨干模型**：测试了 Qwen2.5-VL-7B、Qwen3-VL-8B、LLaVA-OneVision-1.5-8B、InternVL3.5 (4B/8B) 以及闭源的 **GPT-4o-mini**（文中称为 GPT-5-mini）。

### 4. 资源与算力
*   **算力设备**：所有实验均在单张 **NVIDIA RTX A6000 GPU** 上完成。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练时长。
*   **推理效率**：LDDR 相比标准 DPP 实现了 **3 倍以上的运行速度提升**，且采样延迟随输入帧数呈线性增长，非常适合长视频实时处理。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个开源模型和 1 个闭源模型上进行了广泛测试，覆盖了从 8 帧到 32 帧等不同预算场景。
*   **消融实验**：
    *   验证了“全局选择”优于“分块选择”（尤其在长视频中）。
    *   验证了 DPP 中“相关性”与“多样性”缺一不可。
    *   测试了不同视觉编码器（CLIP, SigLIP, LongCLIP）对采样效果的影响。
*   **充分性评价**：实验设计非常全面，不仅有量化指标，还有定性分析和效率对比，客观地展示了 LDDR 在不同预算和模型下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在低预算设置下，LDDR 比次优基准平均高出 **2.5 分**；在高预算下高出 **1.6 分**。
*   **长视频优势**：视频越长，LDDR 的全局线性 DPP 优势越明显，因为它能有效捕捉长程依赖并消除冗余。
*   **动态分辨率的价值**：通过 GD 重要性分配 Token，比固定分辨率或简单的相关性分配更有效，能显著提升模型对关键细节的捕捉能力。
*   **通用性**：作为一种预处理手段，它能显著提升闭源模型（如 GPT 系列）的视频理解表现。

### 7. 优点
*   **高效性**：解决了 DPP 在长视频处理中的计算瓶颈，使其具备实际应用价值。
*   **理论完备**：提供了线性 DPP 与标准 DPP 等价性的数学证明，以及 GD 重要性的几何解释。
*   **即插即用**：不需要修改 MLLM 的权重或结构，适配性极强。
*   **预算感知**：能够精准控制总 Token 消耗，适合对推理成本敏感的场景。

### 8. 不足与局限
*   **依赖外部编码器**：采样的质量高度依赖于预编码器（如 LongCLIP）的特征质量，如果编码器无法对齐查询和视频，采样效果会受限。
*   **下游模型限制**：虽然采样是通用的，但最终效果仍受限于下游 MLLM 处理动态分辨率和长上下文的能力。
*   **超参数敏感性**：虽然文中提到 $\tau=1$ 是默认值，但密度先验等参数在极端分布的视频中可能仍需微调。

（完）
