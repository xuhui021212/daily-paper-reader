---
title: Improving Temporal Action Segmentation via Constraint-Aware Decoding
title_zh: 通过约束感知解码改进时序动作分割
authors: "Yeo Keat Ee, Debaditya Roy, Chen Li, Hao Zhang, Basura Fernando"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10149v1"
tags: ["query:lvu"]
score: 8.0
evidence: 使用结构先验和维特比解码对未修剪视频进行时序动作分割
tldr: 针对时序动作分割中动作多变和边界模糊的挑战，本文提出一种轻量级的约束感知解码框架。该框架通过提取转移置信度、动作边界和类别持续时间等统计结构先验，并将其集成到改进的Viterbi解码算法中，实现了在无需重新训练的情况下对预测结果的推理时优化。该方法能有效纠正结构性错误，显著提升了全监督和半监督TAS模型的性能与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10149v1/fig-001.webp\", \"caption\": \"Table 1: Performance results on both semi-supervised (5% of labeled data) and fully supervised settings. As we compare our method with the counterparts under different settings, we follow the work [21] in semi-supervised setting and using the C2F-TCN [22] model as TAS backbone, whereas we follow [5] for fully-supervised setting and using ASFormer [?] as backbone. For the KARI* method, we reproduced the segmentation results using the given induced grammar rules.\", \"page\": 8, \"index\": 1, \"width\": 846, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2605.10149v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of the proposed constraint-aware temporal action segmentation. Structural constraints (valid starts/ends, action transitions, and duration bounds) are enforced within Viterbi decoding, eliminating invalid transitions and guiding backtracking to the optimal path. Red arrows denote low-probability transitions, black arrows valid transitions, and green arrows the final backtracking flow, yielding consistent and interpretable segmentation. Best viewed in color.\", \"page\": 2, \"index\": 2, \"width\": 550, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.10149v1/fig-003.webp\", \"caption\": \"Figure 4: Qualitative examples from the semi-supervised experiments demonstrate that our approach produces segmentation outputs with better alignment to the ground truth (GT), after training with constraint-based refined pseudo-labels during iterative learning. The top row shows the output from the TAS model without any constraint-aware refinement, which contains invalid action segments. Best viewed in color.\", \"page\": 9, \"index\": 3, \"width\": 850, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2605.10149v1/fig-004.webp\", \"caption\": \"Figure 5: Comparison of edit score improvements across ICC iterations in a semi-supervised setting using different amounts of labeled data (1%, 5%, and 10%). Our constraint-based method consistently outperforms the baseline (w/o constraints) across all iterations and data proportions. Best viewed in color.\", \"page\": 10, \"index\": 4, \"width\": 943, \"height\": 309}]"
motivation: 旨在解决现有动作分割方法在处理复杂动作结构时存在的边界模糊及语法解析复杂度高、扩展性差的问题。
method: 提出一种改进的Viterbi解码算法，将从数据中提取的转移概率、边界集和持续时间等统计约束直接应用于推理阶段。
result: 实验表明该方法在不增加模型复杂度的前提下，显著改善了多种TAS模型在全监督和半监督场景下的分割表现。
conclusion: 约束感知解码为提升时序动作分割性能提供了一种高效、通用的后处理手段，证明了统计先验在纠正结构预测错误中的重要性。
---

## 摘要
时序动作分割（TAS）将未修剪的视频划分为带标签的动作片段。尽管全监督方法推动了该领域的发展，但动作变异性、模糊边界以及高昂的标注成本等挑战依然存在，特别是在新领域或低资源领域。基于语法的方法利用结构先验改进了分割效果，但依赖于复杂的解析过程，限制了其可扩展性。在这项工作中，我们提出了一个轻量级的、基于约束的细化框架，通过整合统计结构先验（如转移置信度、动作边界集和每类持续时间）来增强 TAS 预测，这些先验可以直接从标注数据中提取。这些约束被集成到改进的维特比（Viterbi）解码算法中，从而在无需重新训练或增加模型复杂度的情况下实现推理阶段的细化。我们的方法通过纠正结构预测错误，在保持高效的同时改进了全监督和半监督 TAS 模型。代码可在 https://github.com/LUNAProject22/CAD 获取。

## Abstract
Temporal action segmentation (TAS) divides untrimmed videos into labeled action segments. While fully supervised methods have advanced the field, challenges such as action variability, ambiguous boundaries, and high annotation costs remain, especially in new or low-resource domains. Grammar-based approaches improve segmentation with structural priors but rely on complex parsing limiting scalability. In this work, we propose a lightweight, constraint-based refinement framework that enhances TAS predictions by integrating statistical structural priors such as transition confidence, action boundary sets, and per-class duration, that can be directly extracted from annotated data. These constraints are integrated into a modified Viterbi decoding algorithm, allowing inference-time refinement without retraining or added model complexity. Our approach improves both fully and semi-supervised TAS models by correcting structural prediction errors while maintaining high efficiency. Code is available at https://github.com/LUNAProject22/CAD

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Temporal Action Segmentation via Constraint-Aware Decoding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
时序动作分割（TAS）旨在将长视频划分为有标签的动作片段。尽管深度学习模型取得了进展，但仍面临**过度分割（over-segmentation）**、**动作边界模糊**以及**动作序列逻辑不合理**（如不符合常理的动作转换）等挑战。
*   **研究动机**：现有的基于语法（Grammar-based）的方法虽然能引入结构先验，但通常依赖复杂的递归解析算法，计算开销大且难以扩展。
*   **核心目标**：提出一种轻量级的、基于约束的细化框架，通过在推理阶段引入统计结构先验，在不增加模型复杂度和无需重新训练的前提下，纠正预测中的结构性错误。

### 2. 核心方法论
论文提出了**约束感知维特比解码（Constraint-Aware Viterbi Decoding, CAD）**。
*   **核心思想**：将动作分割视为一个受限优化问题，利用从训练数据中提取的统计规律作为硬约束，指导维特比算法寻找全局最优且逻辑合理的路径。
*   **关键技术细节**：
    1.  **结构约束提取**：从标注数据中统计：
        *   **转移置信度**：动作 A 到 B 发生的频率。
        *   **合法起始/结束动作**：哪些动作通常出现在视频开头或结尾。
        *   **归一化持续时间边界**：每个动作类别相对于视频总长度的最小/最大时长比例。
    2.  **改进的维特比算法**：
        *   **初始化**：仅允许合法起始动作拥有初始分值。
        *   **前向传递**：在计算状态转移时，增加约束检查。只有当当前动作满足最小持续时间且转移在合法集合中时，才允许切换动作；若超过最大持续时间，则强制切换。
        *   **终止与回溯**：在视频末尾仅从合法结束动作中选择最优路径进行回溯。

### 3. 实验设计
*   **数据集**：使用了两个主流的 TAS 基准数据集：**Breakfast**（早餐制作，1712个视频，48类动作）和 **50Salads**（沙拉制作，50个视频，19类动作）。
*   **实验场景**：
    *   **半监督学习**：在仅有 1%、5%、10% 标注数据的情况下，利用该方法优化伪标签。
    *   **全监督学习**：作为后处理步骤，直接优化强基准模型的输出。
*   **对比方法**：
    *   **基准模型**：ASFormer, C2F-TCN, FACT。
    *   **竞争对手**：KARI（基于语法的最新方法）、ICC（半监督基准）。
*   **评价指标**：F1@{10, 25, 50}（重叠率阈值下的 F1 分数）、Edit（编辑距离）和 Acc（帧准确率）。

### 4. 资源与算力
*   **硬件支持**：论文在进行推理时间分析时明确提到了使用 **NVIDIA A5000 GPU**。
*   **训练/推理时长**：
    *   该方法本身是**训练无关（Training-free）**的，仅需在推理时运行。
    *   **效率对比**：在 20 个视频的测试中，该方法耗时仅 **2.81 秒**，而基于语法的 KARI 方法需要 148.9 秒，速度提升了约 50 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了全监督和半监督两种主流设置，并在两个不同规模的数据集上进行了验证。
*   **消融实验**：非常充分。作者分别验证了：
    1.  不同约束组件（起始/结束、转移、持续时间）的贡献。
    2.  硬约束与软约束的效果对比。
    3.  不同标注比例（1%, 5%, 10%）下的性能提升。
    4.  在不同模型骨干（ASFormer, FACT）上的通用性。
*   **公平性**：对比实验采用了相同的骨干网络和评价标准，确保了结果的客观性。

### 6. 主要结论与发现
*   **性能提升**：该方法在所有指标上均一致优于基准模型，尤其在 **Edit（编辑距离）** 指标上提升显著，说明它有效减少了过度分割。
*   **半监督优势**：在极低标注（1%）情况下，该方法通过优化伪标签显著加速了模型的收敛并提高了最终精度。
*   **效率优势**：相比复杂的语法解析，改进的维特比解码具有线性时间复杂度 $O(CT)$，更适合大规模应用。

### 7. 优点
*   **即插即用**：无需修改原始模型架构，无需重新训练，可直接作为后处理插件。
*   **轻量高效**：计算开销极低，推理速度极快。
*   **可解释性强**：约束条件（如“动作持续时间”或“动作顺序”）具有明确的语义含义，易于调试和手动调整。

### 8. 不足与局限
*   **数据依赖性**：约束是从训练集中统计得到的。如果测试视频中出现了训练集未涵盖的罕见动作顺序，硬约束可能会导致预测错误（即“长尾分布”问题）。
*   **硬约束风险**：论文采用硬约束（Hard Constraints），虽然效率高，但在处理高度灵活或随机的动作序列时，可能不如软约束或概率模型灵活。
*   **应用限制**：主要适用于具有一定结构化流程的动作（如烹饪、工业操作），对于完全随机、无序的动作序列提升可能有限。

（完）
