---
title: "QueST: Persistent Queries as Semantic Monitors for Drift Suppression in Long-Horizon Tracking"
title_zh: QueST：将持久查询作为语义监测器以抑制长时程追踪中的漂移
authors: "Mayank Anand, Mohammad Saqlain, Kyan Mahajan, Priya Shukla, Gora Chand Nandi, Andrew Melnik"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09513v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于长时程视频跟踪中漂移抑制的持久查询
tldr: "针对长时视频跟踪中由于逐帧匹配导致的语义漂移问题，本文提出了QueST框架。该框架将交互实体视为持久的语义查询，通过全局时空特征注意力机制建立稳定的语义锚点，并结合轻量级3D物理约束来抑制遮挡下的轨迹漂移。在PartNet-Mobility数据集上的实验表明，QueST显著降低了终端漂移，相比TAP-Net在绝对点误差上提升了67.7%，实现了更可靠的长时跟踪。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09513v1/fig-001.webp\", \"caption\": \"Figure 1: The Reliability Crisis. (A) Re-initialization breaks identity; (B) Markovian trackers (e.g., CoTracker) accumulate drift ∑ ϵt because they propagate local errors; (C) QueST maintains global anchors via persistent learnable queries and 3D physical grounding, suppressing drift.\", \"page\": 2, \"index\": 1, \"width\": 829, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2605.09513v1/fig-002.webp\", \"caption\": \"Figure 4: Single-joint interaction sequences (Phase 1) used for short-horizon training, with multiview RGB-D frames and pixel-level affordance annotations.\", \"page\": 7, \"index\": 2, \"width\": 748, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.09513v1/fig-003.webp\", \"caption\": \"Figure 5: Multi-category, multi-step interaction sequences (Level 4) with cumulative joint actuation, designed to evaluate long-horizon temporal consistency under articulated motion and occlusion.\", \"page\": 7, \"index\": 3, \"width\": 748, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2605.09513v1/fig-004.webp\", \"caption\": \"Figure 2: QueST System Architecture. Video features Ft are extracted via a ViT encoder. Persistent learnable queries Q attend globally across frames to maintain semantic identity. The resulting 2D trajectories are lifted to 3D world coordinates xt using depth backprojection for physical grounding.\", \"page\": 3, \"index\": 4, \"width\": 790, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2605.09513v1/fig-005.webp\", \"caption\": \"Figure 6a demonstrates that QueST adapts its tracking behavior based on the specific query intent. Given the same input video, different queries (e.g., “Open” vs. “Lift”) induce distinct, stable trajectories.\", \"page\": 9, \"index\": 5, \"width\": 822, \"height\": 446}]"
motivation: 传统的逐帧点跟踪在面对关节运动、遮挡和视角变化时容易产生累积误差且难以自我纠正。
method: 提出一种基于监测设计的框架，将实体作为持久语义查询进行全局时空特征检索，并引入3D物理接地约束轨迹。
result: "在长时关节运动序列评估中，QueST相比TAP-Net将绝对点误差降低了67.7%，并能更好地保持实体身份。"
conclusion: 将语义监测直接嵌入感知过程能有效抑制分布偏移下的漂移，显著提升长时跟踪的可靠性。
---

## 摘要
视频中的点追踪通常被表述为帧间对应关系，即每个点被局部匹配到下一帧。虽然这在短时程内有效，但在关节运动、遮挡和视角变化下，误差会不断累积，导致现有追踪器无法检测或纠正的隐性语义漂移。在这项工作中，我们从监测的角度重新审视了长时程追踪，并引入了 QueST，这是一个“监测即设计”的框架，它将与交互相关的实体视为持久的语义查询，而非瞬时的点轨迹。与局部传播不同，每个查询在每个时间步都会对时空视频特征进行全局注意力计算，从而提供跨时间的稳定语义锚点。我们进一步利用轻量级的 3D 物理接地来约束查询轨迹，利用几何合理性来抑制遮挡下的无界漂移。我们在 SAPIEN 中 PartNet-Mobility 的长时程关节运动序列上评估了 QueST，并与 RAFT-3D、CoTracker 和 TAP-Net 进行了比较。QueST 显著减少了终端漂移，与 TAP-Net 相比，绝对点误差（APE）提升了 67.7%，同时在更长的时程内能更好地保持身份一致性。我们的结果表明，将语义监测直接嵌入到感知中，可以在分布偏移下实现更可靠的长时程追踪。

## Abstract
Tracking points in videos is typically formulated as frame-to-frame correspondence, where each point is matched locally to the next frame. While this works over short horizons, errors accumulate under articulation, occlusion, and viewpoint change, leading to silent semantic drift that existing trackers cannot detect or correct. In this work, we revisit long-horizon tracking from a monitoring perspective and introduce QueST, a monitoring-by-design framework that treats interaction-relevant entities as persistent semantic queries rather than transient point tracks. Instead of local propagation, each query attends globally over spatio-temporal video features at every time-step, providing a stable semantic anchor across time. We further constrain query trajectories with lightweight 3D physical grounding, using geometric plausibility to suppress unbounded drift under occlusion. We evaluate QueST on long-horizon articulated sequences from PartNet-Mobility in SAPIEN and compare against RAFT-3D, CoTracker, and TAP-Net. QueST substantially reduces terminal drift achieving a 67.7% Absolute Point Error (APE) improvement over TAP-Net while better preserving identity over extended horizons. Our results show that embedding semantic monitoring directly into perception enables more reliable long-horizon tracking under distribution shift.

---

## 论文详细总结（自动生成）

这是一份关于论文《QueST: Persistent Queries as Semantic Monitors for Drift Suppression in Long-Horizon Tracking》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长时程（Long-horizon）视频点追踪中的“隐性语义漂移”（Silent Semantic Drift）。
*   **研究背景**：现有的追踪器（如 CoTracker, TAP-Net）大多依赖马尔可夫假设，即通过局部外观线索进行帧间传播。在面对物体关节运动（如开门）、遮挡和视角大幅切换时，微小的局部误差会不断累积，导致追踪点逐渐偏离目标语义（例如从“把手”漂移到“背景”），且系统无法自我察觉或纠正。
*   **研究动机**：在机器人操作等具身智能场景中，错误的感知状态会导致不安全的动作。因此，需要一种能够实时监测并抑制漂移的追踪机制。

### 2. 方法论
QueST 提出了“监测即设计”（Monitoring-by-design）的框架，核心包含两个支柱：
*   **持久语义查询（Persistent Semantic Queries）**：
    *   不再使用局部像素传播，而是将追踪目标表示为一组可学习的持久查询（Queries）。
    *   **全局注意力机制**：使用 ViT 编码器提取视频特征，通过 Transformer 解码器让查询在整个时空特征卷中进行全局交叉注意力计算。这使得查询像“语义锚点”一样，在每一帧都重新定位目标，从而消除累积误差。
*   **3D 物理接地（3D Physical Grounding）**：
    *   将 2D 预测点通过深度图反投影到 3D 空间。
    *   **运动约束**：引入物理一致性损失函数，惩罚不符合运动学规律（如速度或加速度突变）的轨迹。如果视觉编码器漂移到背景，其 3D 轨迹通常会违反运动平滑性，从而触发修正信号。
*   **损失函数**：结合了定位误差（$L_{aff}$）、平滑度约束（$L_{vel}, L_{acc}$）以及流预测损失。

### 3. 实验设计
*   **数据集/场景**：使用 SAPIEN 仿真环境中的 **PartNet-Mobility** 数据集。该数据集包含大量具有关节运动的物体（如柜子、洗碗机、笔记本电脑）。
*   **测试基准**：设计了不同复杂度的压力测试，包括单关节运动（Phase 1）和多关节连续运动（Level 4，长达 240 帧）。
*   **对比方法**：与当前主流的追踪算法进行对比，包括 **RAFT-3D**、**CoTracker** 和 **TAP-Net**。
*   **评估指标**：绝对点误差（APE）、终端漂移（Drift@100）和身份保持准确率（Identity Accuracy）。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA RTX 6000 GPU**。
*   **性能**：推理速度约为 **30 FPS**，能够满足实时监测的需求。
*   **训练细节**：采用两阶段训练（感知监测阶段和流预测适配阶段），使用 AdamW 优化器，训练周期为 50 个 epoch，时间窗口 $T=4$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 4 个复杂等级的关节运动测试、噪声鲁棒性测试（0%-5% 高斯噪声）以及消融实验。
*   **充分性**：实验设计较为全面，通过对比不同时间窗口长度（$T=2, 4, 8$）和查询数量（$K=4, 8, 16$）验证了超参数的影响。
*   **客观性**：使用了标准的 3D 真实值（Ground Truth）进行定量分析，并展示了在极端视角变化下的定性结果，对比实验具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：QueST 在长时程追踪中显著优于基准模型，相比 TAP-Net，其 **绝对点误差（APE）降低了 67.7%**。
*   **抑制漂移**：基准模型（如 CoTracker）的误差随时间呈近线性增长，而 QueST 的误差曲线保持平稳且有界。
*   **身份保持**：在复杂的关节运动和遮挡下，QueST 的身份保持准确率达到 86.5%，而对比方法均低于 22%。
*   **鲁棒性**：在 5% 的高斯噪声下，QueST 仍能保持 96% 以上的准确率，证明了全局语义查询比局部像素匹配更稳定。

### 7. 优点
*   **架构创新**：将追踪问题转化为“语义监测”问题，利用全局注意力打破了马尔可夫链的误差累积。
*   **物理约束**：巧妙地利用 3D 运动学先验作为正则项，增强了模型在遮挡期间的预测合理性。
*   **实时性**：在保持高精度的同时达到了工业级的实时处理速度。

### 8. 不足与局限
*   **极端遮挡限制**：当目标被遮挡超过 80% 且持续时间超过 30 帧时，全局注意力机制可能会漂移到视觉相似的邻近物体上。
*   **对称性歧义**：在具有多个完全相同部件（如一排相同的储物柜把手）的物体上，查询可能会在等效的语义目标之间发生切换。
*   **环境局限**：目前主要在 SAPIEN 仿真环境中验证，虽然理论上可扩展，但尚未在复杂的真实世界非结构化环境中进行大规模实测。

（完）
