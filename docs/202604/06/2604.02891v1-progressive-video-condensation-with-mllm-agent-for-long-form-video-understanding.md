---
title: Progressive Video Condensation with MLLM Agent for Long-form Video Understanding
title_zh: 基于 MLLM 智能体的渐进式视频浓缩，用于长视频理解
authors: "Yufei Yin, Yuchen Xing, Qianke Meng, Minghao Chen, Yan Yang, Zhou Yu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02891v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于长视频理解和查询相关信息提取的渐进式视频压缩代理
tldr: 针对长视频理解中计算成本高与视觉细节丢失的矛盾，本文提出ProVCA，一种基于多模态大模型（MLLM）的渐进式视频压缩智能体。该方法通过从粗到细的三个阶段（片段定位、片段选择、关键帧精炼）迭代筛选关键帧，在显著减少输入帧数的同时保留了关键视觉信息。实验证明，ProVCA在EgoSchema等多个长视频问答基准测试中取得了SOTA性能，实现了高效且精准的视频推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02891v1/fig-001.webp\", \"caption\": \"TABLE I: Comparison with other training-free methods on EgoSchema, NExT-QA, and IntentQA.\", \"page\": 5, \"index\": 1, \"width\": 1076, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-002.webp\", \"caption\": \"TABLE II: Ablation experiments for ProVCA. All results are on the val subset of NExT-QA. Best results are bolded, and\", \"page\": 5, \"index\": 2, \"width\": 1093, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of ProVCA in video understanding based on MLLM.\", \"page\": 3, \"index\": 3, \"width\": 1076, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.02891v1/fig-004.webp\", \"caption\": \"Fig. 1: Conceptual comparisons of three Video understanding methods based on LLM (MLLM). Existing approaches either (a) directly reason with pre-trained video-based MLLMs at high computational cost, or (b) use LLM agents that convert frames to captions and rely on VLMs (e.g., CLIP) to filter key frames, discarding fine-grained visual cues. In contrast, our ProVCA (c) progressively condenses videos by using an MLLM to narrow down query-relevant content from coarse segments to fine frames, then feeds the selected key frames into the MLLM to generate the final answer.\", \"page\": 1, \"index\": 4, \"width\": 518, \"height\": 356}]"
motivation: 现有的长视频理解方法在保留细粒度视觉细节与控制多模态大模型的计算开销之间难以兼得。
method: 提出一种渐进式压缩智能体，通过片段定位、片段选择和关键帧精炼三个层级，由粗到细地为模型筛选关键视觉信息。
result: 在EgoSchema、NExT-QA和IntentQA等数据集上取得了SOTA零样本准确率，且相比以往免训练方法使用了更少的帧数。
conclusion: 渐进式视频压缩策略能有效平衡长视频理解的效率与精度，证明了通过智能体筛选关键帧是提升MLLM长序列处理能力的有效途径。
---

## 摘要
理解长视频需要在有限的计算预算下，从长序列中提取与查询相关的信息。现有的“先文本后 LLM”流水线会丢失细粒度的视觉线索，而基于视频的多模态大语言模型（MLLMs）虽然能保留视觉细节，但对帧数需求过高且计算开销巨大。在这项工作中，我们旨在利用 MLLMs 实现高效的视频理解。我们提出了 ProVCA，一种渐进式视频浓缩智能体，它在多个粒度上迭代定位关键视频帧。ProVCA 首先采用片段定位模块来识别与查询相关的视频片段，然后通过片段选择模块基于相似度选择重要片段，最后利用关键帧精炼模块在这些片段中精准定位特定的关键帧。通过将范围从粗粒度片段逐步缩小到细粒度帧，ProVCA 识别出一组少量的关键帧，用于基于 MLLM 的推理。ProVCA 在 EgoSchema 上达到了 69.3%、在 NExT-QA 上达到了 80.5%、在 IntentQA 上达到了 77.7% 的最先进零样本准确率，同时使用的帧数少于以往的免训练方法。

## Abstract
Understanding long videos requires extracting query-relevant information from long sequences under tight compute budgets. Existing text-then-LLM pipelines lose fine-grained visual cues, while video-based multimodal large language models (MLLMs) can keep visual details but are too frame-hungry and computationally expensive. In this work, we aim to harness MLLMs for efficient video understanding. We propose ProVCA, a progressive video condensation agent that iteratively locates key video frames at multiple granularities. ProVCA first adopts a segment localization module to identify the video segment relevant to the query, then a snippet selection module to select important snippets based on similarity, and finally a keyframe refinement module to pinpoint specific keyframes in those snippets. By progressively narrowing the scope from coarse segments to fine frames, ProVCA identifies a small set of keyframes for MLLM-based reasoning. ProVCA achieves state-of-the-art zero-shot accuracies of 69.3\% on EgoSchema, 80.5\% on NExT-QA, and 77.7\% on IntentQA, while using fewer frames than previous training-free methods.

---

## 论文详细总结（自动生成）

### 论文结构化总结：ProVCA 渐进式视频浓缩智能体

#### 1. 核心问题与研究动机
*   **核心问题**：如何在有限的计算预算下，实现对长视频的深度理解。
*   **研究背景**：
    *   **现有路径 A（Text-then-LLM）**：将视频转为文本描述再交给 LLM。优点是计算省，缺点是丢失了细粒度的视觉特征。
    *   **现有路径 B（Video-based MLLM）**：直接输入多帧图像。优点是保留细节，缺点是帧数过多导致内存爆炸、冗余严重且推理效率低。
*   **动机**：模仿人类“先看大纲，再读细节”的阅读理解模式，开发一种能够自动筛选查询相关（Query-relevant）关键帧的智能体，平衡精度与效率。

#### 2. 方法论：ProVCA 框架
ProVCA 采用**由粗到细（Coarse-to-Fine）**的三个阶段来浓缩视频：
*   **阶段一：片段定位（Segment Localization）**
    *   将视频划分为 $n$ 个大段，提取每段起始帧。
    *   利用 MLLM 的语义推理能力，从全局视野中定位出与问题最相关的粗粒度时间区间（Segment）。
*   **阶段二：片段选择（Snippet Selection）**
    *   **时序感知聚类**：在定位到的片段内，利用 Caption 模型生成描述，通过计算文本嵌入的余弦相似度进行动态聚类。
    *   **动态阈值**：根据片段长度动态调整相似度阈值 $t$，将相似帧归为一个小片（Snippet）。
    *   **重要性评估**：MLLM 评估每个小片的代表帧，给出相关性评分和置信度。
*   **阶段三：关键帧精炼（Keyframe Refinement）**
    *   针对低置信度的小片，回归视觉领域。
    *   将小片内的所有原始帧按序输入 MLLM，通过结构化 Prompt 让模型精准锁定最关键的单帧。
*   **最终推理**：将筛选出的极少量关键帧（通常仅 4-7 帧）输入 MLLM，配合思维链（CoT）生成最终答案。

#### 3. 实验设计
*   **数据集**：
    *   **NExT-QA**：侧重因果和时序动作解释（平均 44 秒）。
    *   **EgoSchema**：长达 3 分钟的第一视角视频理解。
    *   **IntentQA**：侧重视频中的意图推理。
*   **Benchmark 与对比方法**：
    *   对比了免训练（Training-free）的 SOTA 方法，包括 **VideoTree**、**VideoAgent**、**LVNet**、**IG-VLM** 等。
    *   涵盖了基于开源模型（如 LLaVA）和闭源模型（如 GPT-4o）的多种方案。

#### 4. 资源与算力
*   **模型使用**：核心推理引擎为 **GPT-4o** (gpt-4o-2024-08-06)。
*   **辅助工具**：使用 LaViLa 和 CogAgent 作为视觉描述生成器（Captioner）。
*   **算力说明**：论文**未明确指出**具体的 GPU 硬件型号和数量。由于该方法属于**免训练（Training-free）**范畴，主要开销在于调用 MLLM API 的 Token 消耗和 Caption 模型的推理。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流长视频理解基准上进行了全面测试，包括 EgoSchema 的全量榜单测试。
*   **消融实验**：非常充分。作者分别验证了：
    1.  不同 MLLM 底座（GPT-4o, LLaVA-OV 等）的影响。
    2.  输入模态（仅文本、图像网格、图像+文本）的效果。
    3.  三个核心模块（定位、选择、精炼）各自的贡献。
    4.  聚类策略（K-means vs. 时序感知聚类）的优劣。
*   **客观性**：通过在相同底座（GPT-4o）下对比 VideoTree 等方法，证明了性能提升源于算法框架而非模型溢价。

#### 6. 主要结论与发现
*   **性能领先**：在 EgoSchema 全量集上达到 74.2% 准确率，比之前最好的 VideoTree 高出 8.2%。
*   **高效浓缩**：ProVCA 仅需平均 **4.2 到 7.3 帧** 即可完成高质量推理，远少于 LVNet 等方法使用的 12 帧或更多。
*   **模块协同**：消融实验显示，缺少任何一个阶段（定位、选择或精炼）都会导致准确率下降 1.4%~2.6%，证明了渐进式筛选的必要性。

#### 7. 优点（亮点）
*   **认知启发式设计**：模拟人类处理长信息的逻辑，逻辑清晰且易于解释。
*   **即插即用**：作为一种 Agent 框架，不需要昂贵的重新训练或微调，可直接适配现有的强力 MLLM。
*   **精度与效率双赢**：在大幅减少输入数据量的同时，反而提升了推理精度，解决了长视频冗余信息干扰推理的问题。

#### 8. 不足与局限
*   **依赖外部工具**：流程中需要预先生成 Caption，这增加了系统复杂性，且 Caption 模型的质量会直接影响后续的聚类效果。
*   **API 成本**：虽然帧数减少了，但由于是多阶段推理，需要多次调用 MLLM API，可能存在较高的累计延迟和调用成本。
*   **实时性挑战**：渐进式的多次迭代意味着该方法目前更适合离线长视频分析，难以满足超低延迟的实时交互需求。

（完）
