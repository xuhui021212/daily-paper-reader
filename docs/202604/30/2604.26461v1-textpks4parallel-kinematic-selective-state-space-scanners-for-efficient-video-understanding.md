---
title: "$\\text{PKS}^4$:Parallel Kinematic Selective State Space Scanners for Efficient Video Understanding"
authors: "Lingjie Zeng, Hailun Zhang, Xiwen Wang, Qijun Zhao"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26461v1"
tags: ["query:lvu"]
score: 10.0
evidence: 用于长视频时间建模的线性复杂度高效状态空间模型
tldr: 针对长视频理解中时空注意力计算开销大及现有SSM模型破坏空间结构的问题，本文提出PKS^4。该方法在保留2D视觉骨干网络的基础上，通过运动先验编码器提取局部位移和运动边界，并利用并行运动选择性状态空间扫描器在时间维度进行线性复杂度扫描。PKS^4在保持空间结构的同时显著降低了计算成本，在多个动作识别基准上达到SOTA，且训练效率提升了约10倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26461v1/fig-001.webp\", \"caption\": \"Table 4: Performance comparison on K400. Views = #frames × #temporal clips × #spatial crops. “e” denotes the number of pre-training epochs for VideoMamba.\", \"page\": 7, \"index\": 1, \"width\": 477, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.26461v1/fig-002.webp\", \"caption\": \"Table 3: Comparison with the state-of-the-art on temporal-heavy dataset SSV2. Views = #frames × #temporal clips × #spatial crops. “e” denotes the number of pre-training epochs for VideoMamba.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 722}, {\"url\": \"assets/figures/arxiv/2604.26461v1/fig-003.webp\", \"caption\": \"Figure 3: Overview of PKS4. Given intermediate token features from a ViT layer, we first split CLS and patch tokens. The Kinematic Prior Encoder process patch tokens through a temporal sliding window. The correlation and variation operators are sequentially applied to explicitly capture rich kinematic priors. To preserve innate spatial structures, we deploy parallel scanning along the temporal dimension for each spatial location. EachKinematic Selective State Space Scanner (KS4) dynamically generates data-dependent parameters to adaptively modulate the update and decay processes, achieving linear-complexity temporal modeling. The updated tokens are concatenated and fed back to the next ViT layer.\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 250}]"
motivation: 解决传统视频模型计算复杂度高以及现有SSM模型在处理视频时破坏空间结构且训练成本高昂的问题。
method: 提出一种即插即用的PKS^4模块，结合运动先验编码器与并行时间扫描器，在保留2D空间语义的同时实现线性复杂度的时序建模。
result: 在多个动作识别基准上取得领先性能，且仅需20个epoch即可收敛，训练计算量比纯视频SSM模型降低约10倍。
conclusion: PKS^4为高效视频理解提供了一种新范式，证明了结合运动先验与并行SSM扫描能有效平衡计算效率与时空建模能力。
---

## Abstract
Temporal modeling remains a fundamental challenge in video understanding, particularly as sequence lengths scale. Traditional video models relying on dense spatiotemporal attention suffer from quadratic computational costs for long videos. To circumvent these costs, recent approaches adapt image models for videos via Parameter-Efficient Fine-Tuning (PEFT) methods such as adapters. However, deeply inserting these modules incurs prohibitive activation memory overhead during back-propagation. While recent efficient State Space Models (SSMs) introduce linear complexity, they disrupt 2D spatial relationships and rely on extensive masked pre-training to recover spatial awareness.   To overcome these limitations, we propose Parallel Kinematic Selective State Space Scanners (PKS$^4$). We retain a standard 2D vision backbone for spatial semantics and insert a single plug-and-play PKS$^4$ module with linear-complexity temporal scanning, avoiding temporal attention and multi-layer adapters. We first extract kinematic priors via a Kinematic Prior Encoder, which captures local displacements and motion boundaries through inter-frame correlations and differences. These priors drive linear-complexity SSMs to track underlying kinematic states, adaptively modulating update speeds and read-write strategies at each time step.   Instead of global scanning, we deploy parallel scanners along the temporal dimension for each spatial location, preserving spatial structures while reducing overhead. Experiments on spatial-heavy and temporal-heavy action recognition benchmarks show that PKS$^4$ achieves state-of-the-art performance. Remarkably, our method converges in merely $20$ epochs, achieving approximately $10\times$ lower training compute than pure video SSMs, establishing a new paradigm for efficient video understanding.

---

## 论文详细总结（自动生成）

### 论文总结：$\text{PKS}^4$: Parallel Kinematic Selective State Space Scanners for Efficient Video Understanding

#### 1. 核心问题与整体含义（研究动机和背景）
视频理解的核心挑战在于如何高效地建模长距离时序依赖。当前主流方法存在以下瓶颈：
*   **计算开销大**：传统的时空注意力机制（Spatiotemporal Attention）具有序列长度的平方复杂度，处理长视频时计算成本极高。
*   **显存瓶颈**：基于参数高效微调（PEFT）的方法（如 Adapter）虽然减少了参数量，但由于需要深度插入多层模块，在反向传播时会产生巨大的激活显存开销。
*   **空间结构破坏**：新兴的状态空间模型（SSM，如 Mamba）通常将 3D 视频展平为 1D 序列进行扫描，这破坏了固有的 2D 空间关系，导致模型需要昂贵的从头预训练（From-scratch Pre-training）来重新学习空间感。

**研究动机**：提出一种既能保持线性计算复杂度，又能保留 2D 空间结构，且训练成本极低的视频理解框架。

#### 2. 论文提出的方法论
论文提出了 **PKS$^4$（并行运动选择性状态空间扫描器）**，其核心思想是解耦空间和时序建模：
*   **空间骨干保留**：保留标准的 2D 预训练 ViT 提取空间语义，仅在中间层插入单个即插即用的 PKS$^4$ 模块。
*   **运动先验编码器 (Kinematic Prior Encoder, KPE)**：在扫描前显式提取运动特征。
    *   **相关性算子 (Correlation Operator)**：通过局部搜索窗口计算帧间对应关系，捕捉位移一致性。
    *   **变化算子 (Variation Operator)**：通过帧间差分建模，强调运动边界和时间变化。
*   **并行时间扫描 (Parallel Scanning)**：不进行全局 1D 展平，而是针对每个空间位置（Patch）构建独立的时间序列，并在时间维度上进行并行扫描。
*   **运动选择性 SSM (KS$^4$)**：利用 KPE 提取的运动先验驱动 SSM 的参数生成（如步长 $\Delta$、读写矩阵 $B, C$），使状态演化更符合视频的运动动力学。

#### 3. 实验设计
*   **数据集**：
    *   **Something-Something V2 (SSV2)**：时序敏感型数据集，侧重于精细动作和物体交互。
    *   **Kinetics-400 (K400)**：外观敏感型数据集，侧重于日常人类行为。
*   **Benchmark 与对比方法**：
    *   **时空注意力模型**：TimeSformer, ViViT, Mformer, Swin Transformer。
    *   **PEFT 方法**：ST-Adapter, AIM, DualPath, EVL, Side4Video。
    *   **SSM 模型**：VideoMamba 及其变体。

#### 4. 资源与算力
*   **训练效率**：PKS$^4$ 表现出极高的效率，仅需 **20 个 epoch** 即可收敛。
*   **计算量对比**：
    *   PKS$^4$-B/16 的总训练计算量约为 **2.95 EFLOPs**。
    *   相比之下，纯视频 SSM（如 VideoMamba）需要约 **31.54 EFLOPs**（含预训练）。
    *   PKS$^4$ 实现了约 **10 倍** 的训练计算量削减。
*   **硬件说明**：文中未明确给出具体的 GPU 型号和数量，但通过显存占用对比图（Fig 2a）展示了其在相同 Batch Size 下比 Adapter 方法更省显存。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准上进行了详尽测试，并提供了 ViT-B 和 ViT-L 两种规模的实验结果。
*   **消融实验**：非常充分，涵盖了：
    1.  各子模块（KPE, KS$^4$）的有效性。
    2.  PKS$^4$ 模块在 ViT 骨干中的插入位置（第 8 层效果最好）。
    3.  单向扫描与双向扫描的对比（单向性价比更高）。
    4.  KPE 的上下文帧数影响。
    5.  CLS Token 的辅助时序路由设计。
*   **客观性**：对比实验均在相同的测试视角（Views）和预训练背景下进行，评价较为公正。

#### 6. 主要结论与发现
*   **性能领先**：在 SSV2 上，PKS$^4$-L/14 达到了 74.3% 的 Top-1 准确率，优于现有的 PEFT 和 SSM 方法。
*   **训练极快**：由于保留了 2D 空间结构和图像预训练知识，无需大规模视频预训练即可快速收敛。
*   **显存友好**：通过单层插入而非多层深度注入，显著降低了反向传播时的激活显存压力。
*   **运动驱动的重要性**：实验证明，由运动先验驱动的 SSM 状态转移比直接处理原始展平 Token 更有效。

#### 7. 优点
*   **架构优雅**：即插即用，无需修改 ViT 内部结构，对现有图像模型极其友好。
*   **效率极高**：线性复杂度 + 并行扫描 + 极短的训练周期，是目前最高效的视频微调方案之一。
*   **归纳偏置合理**：通过并行扫描保留空间结构，解决了 SSM 在视频领域“空间感缺失”的痛点。

#### 8. 不足与局限
*   **外观主导场景优势减弱**：在 K400 等对时序要求不高的场景下，性能提升不如 SSV2 显著，说明运动先验的收益与任务的时序复杂度正相关。
*   **动态采样缺失**：目前对所有空间位置进行等量扫描，未来可引入动态门控机制，对静态背景区域减少扫描以进一步优化。
*   **长视频上限**：虽然理论上是线性复杂度，但在极长视频（如数小时）中的表现尚未在文中得到验证。

（完）
