---
title: "OmniVTG: A Large-Scale Dataset and Training Paradigm for Open-World Video Temporal Grounding"
title_zh: OmniVTG：面向开放世界视频时序定位的大规模数据集与训练范式
authors: "Minghang Zheng, Zihao Yin, Yi Yang, Yuxin Peng, Yang Liu"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25276v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于视频时间定位的大规模数据集和训练范式
tldr: 针对视频时序定位在开放世界中因数据规模和语义多样性不足导致的性能瓶颈，本文提出了大规模数据集OmniVTG及自我修正思维链（CoT）训练范式。通过语义扩展流水线构建高质量数据，并利用多模态大模型的理解能力对定位结果进行自我修正。实验证明该方法在多个基准测试中取得了SOTA零样本性能，显著提升了模型的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25276v1/fig-001.webp\", \"caption\": \"Figure 2. The visualizations and comparisons of our OmniVTG dataset.\", \"page\": 2, \"index\": 1, \"width\": 1022, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2604.25276v1/fig-002.webp\", \"caption\": \"Table 2. Zero-shot performance comparison with multimodal large language models on video temporal grounding benchmarks.\", \"page\": 7, \"index\": 2, \"width\": 926, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.25276v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative comparison with Time-R1.\", \"page\": 8, \"index\": 3, \"width\": 435, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.25276v1/fig-004.webp\", \"caption\": \"Table 1. Statistics of video temporal grounding datasets.\", \"page\": 4, \"index\": 4, \"width\": 860, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.25276v1/fig-005.webp\", \"caption\": \"Figure 3. (a) Our dataset collection pipeline. The Target Words Identification identifies underrepresented words in existing datasets. The Interactive Video Collection collects videos that are more likely to contain the target word. The Automated Annotation reformulates the grounding tasks to dense caption tasks and prompts MLLMs to generate timestamps and captions using the target word. (b) Our model training pipeline. The Supervised Finetuning stage enhances basic temporal localization and the specific understanding skills needed for self-correction. The Self-Correction CoT Finetuning stage explicitly teaches the model the ‘predict-correct’ reasoning path. The Reinforcement Learning stage further enhances the model’s complex reasoning ability.\", \"page\": 4, \"index\": 5, \"width\": 818, \"height\": 504}]"
motivation: 现有VTG数据集规模和语义多样性有限，导致模型在开放世界中处理稀有概念时性能受限。
method: 引入了OmniVTG数据集及一种结合监督微调、CoT微调和强化学习的自我修正训练范式。
result: 该方法在OmniVTG上表现出色，并在四个主流VTG基准测试中实现了最先进的零样本性能。
conclusion: 通过大规模高质量数据和自我修正机制，可以有效增强多模态大模型在复杂开放场景下的视频定位能力。
---

## 摘要
视频时序定位（Video Temporal Grounding, VTG）是指根据文本查询定位视频片段的任务，由于数据集规模和语义多样性有限，该任务在开放世界设置中面临挑战，导致常见概念与罕见概念之间存在性能差距。为了克服这些局限性，我们引入了 OmniVTG，这是一个用于开放世界 VTG 的新型大规模数据集，并结合了旨在增强多模态大语言模型（MLLM）定位能力的自我修正思维链（Self-Correction Chain-of-Thought, CoT）训练范式。我们的 OmniVTG 通过一种新颖的语义覆盖迭代扩展流水线构建，该流水线首先识别现有数据集词汇表中的空白，并收集极有可能包含这些目标概念的视频。为了实现高质量标注，我们利用现代 MLLM 在密集描述方面优于直接定位的见解，设计了一个以描述为中心的数据引擎，以促使 MLLM 生成带有时间戳的密集描述。除了数据集之外，我们观察到简单的监督微调（SFT）是不够的，因为罕见概念与常见概念之间的性能差距依然存在。我们发现 MLLM 的视频理解能力显著超过其直接定位能力。基于此，我们提出了一种自我修正思维链（CoT）训练范式。我们训练 MLLM 首先进行预测，然后利用其理解能力对自己的预测进行反思和优化。这种能力通过 SFT、CoT 微调和强化学习的三阶段流水线进行培养。广泛的实验表明，我们的方法不仅在 OmniVTG 数据集的开放世界定位中表现出色，而且在四个现有 VTG 基准测试中也实现了最先进的零样本性能。代码可在 https://github.com/oceanflowlab/OmniVTG 获取。

## Abstract
Video Temporal Grounding (VTG), the task of localizing video segments from text queries, struggles in open-world settings due to limited dataset scale and semantic diversity, causing performance gaps between common and rare concepts. To overcome these limitations, we introduce OmniVTG, a new large-scale dataset for open-world VTG, coupled with a Self-Correction Chain-of-Thought (CoT) training paradigm designed to enhance the grounding capabilities of Multimodal Large Language Models (MLLMs). Our OmniVTG is constructed via a novel Semantic Coverage Iterative Expansion pipeline, which first identifies gaps in the vocabulary of existing datasets and collects videos that are highly likely to contain these target concepts. For high-quality annotation, we leverage the insight that modern MLLMs excel at dense captioning more than direct grounding and design a caption-centric data engine to prompt MLLMs to generate dense, timestamped descriptions. Beyond the dataset, we observe that simple supervised finetuning (SFT) is insufficient, as a performance gap between rare and common concepts still persists. We find that MLLMs' video understanding ability significantly surpasses their direct grounding ability. Based on this, we propose a Self-Correction Chain-of-Thought (CoT) training paradigm. We train the MLLM to first predict, then use its understanding capabilities to reflect on and refine its own predictions. This capability is instilled via a three-stage pipeline of SFT, CoT finetuning, and reinforcement learning. Extensive experiments show our approach not only excels at open-world grounding in our OmniVTG dataset but also achieves state-of-the-art zero-shot performance on four existing VTG benchmarks. Code is available at https://github.com/oceanflowlab/OmniVTG.

---

## 论文详细总结（自动生成）

# OmniVTG: 面向开放世界视频时序定位的大规模数据集与训练范式

## 1. 核心问题与整体含义（研究动机和背景）
视频时序定位（VTG）旨在根据文本查询在长视频中精确定位起止时间。现有的 VTG 研究在**开放世界场景**下表现不佳，主要瓶颈在于：
*   **数据集局限性**：现有数据集（如 ActivityNet, Charades-STA）规模较小，且语义覆盖范围窄，多集中于日常活动，缺乏对罕见、抽象或特定领域概念的覆盖。
*   **性能鸿沟**：模型在处理常见概念时表现尚可，但在面对罕见概念（Rare Concepts）时性能大幅下降。
*   **能力错位**：研究发现，多模态大模型（MLLM）的视频理解能力（如判断片段是否匹配）远强于其直接生成精确时间戳的定位能力。

## 2. 论文提出的方法论
论文提出了 **OmniVTG**，包含一个大规模数据集和一种全新的训练范式。

### 核心思想
利用 MLLM 强大的描述和理解能力来辅助定位，并通过“预测-修正”的思维链（CoT）提升定位精度。

### 关键技术细节
1.  **语义覆盖迭代扩展流水线**：
    *   **目标词识别**：对比现有数据集与 BERT 词表，找出未覆盖的罕见词。
    *   **定向视频收集**：利用 LLM 将罕见词转化为具体的搜索关键词（如将“meticulous”转化为“钟表匠精密组装”），从互联网抓取视频。
    *   **以描述为中心的数据引擎**：利用 MLLM（如 Gemini-2.5-Pro）生成带时间戳的密集描述（Dense Captioning），而非直接定位，因为实验证明 MLLM 生成描述时的时间戳更准确。
2.  **自我修正思维链（Self-Correction CoT）训练范式**：
    *   **阶段一：监督微调（SFT）**：在 OmniVTG 上进行多任务训练（定位、描述、匹配、状态分类），增强基础能力。
    *   **阶段二：CoT 微调**：构建“先预测一个粗略/错误区间，再通过反思修正为精确区间”的训练数据，教会模型自我纠偏。
    *   **阶段三：强化学习（RL）**：使用 GRPO 算法，通过 IoU 奖励和格式奖励，进一步优化模型在复杂样本上的推理逻辑。

## 3. 实验设计
*   **数据集**：自建 **OmniVTG**（2124小时视频，35.9万个查询），其词汇量远超现有数据集总和。
*   **Benchmark**：在四个主流公开集上进行**零样本（Zero-shot）**评估：ActivityNet Captions, Charades-STA, QVHighlights, TVGBench。
*   **对比方法**：包括传统的 VTG 模型以及最新的 MLLM 模型，如 TimeChat, UniTime, Time-R1, Qwen2.5-VL 等。
*   **评估指标**：Recall@1 (R1) 在不同 IoU 阈值（0.3, 0.5, 0.7）下的表现。

## 4. 资源与算力
*   **基座模型**：Qwen2.5-VL-7B。
*   **微调技术**：SFT 和 CoT 阶段使用 **LoRA**（rank=8, alpha=8）；RL 阶段进行**全参数微调**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但考虑到 7B 模型的大规模 RL 训练，通常需要数十块 A100/H100 级别的显卡。

## 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 4 个公开数据集的零样本对比、OmniVTG 内部测试集（包含全集与罕见子集）的性能分析。
*   **消融实验**：针对训练阶段的必要性（SFT vs CoT vs RL）、数据规模的影响（10% vs 50% vs 100%）、推理策略（有无自我修正）进行了详细对比。
*   **充分性评价**：实验设计较为全面，特别是针对“罕见概念”的专项评估，有力地证明了模型在开放世界下的鲁棒性。对比基准涵盖了当前最先进的同类模型，实验结果具有较高的说服力。

## 6. 主要结论与发现
*   **数据集质量**：通过 MLLM 密集描述生成的自动化标注质量极高，93.8% 的标注与人工修正后的 IoU 大于 0.5。
*   **SOTA 性能**：OmniVTG 在所有测试的公开基准上均取得了最先进的零样本性能，显著超过了之前的 SOTA 模型 Time-R1。
*   **自我修正的有效性**：引入 CoT 和 RL 后的自我修正机制，能有效缩小模型在处理罕见概念与常见概念之间的性能差距。

## 7. 优点
*   **数据构建思路巧妙**：避开了昂贵的人工标注，利用“描述优于定位”的特性，实现了大规模、高质量、高语义多样性的数据自动化生产。
*   **训练范式符合直觉**：借鉴了人类“先看大概，再细看局部”的认知过程，通过自我修正机制提升了 MLLM 的时序推理深度。
*   **泛化能力强**：仅在 OmniVTG 上训练，即可在多个未见过的领域（如烹饪、新闻、Vlog）中表现出色。

## 8. 不足与局限
*   **依赖闭源模型**：数据标注阶段高度依赖 Gemini-2.5-Pro 等高性能闭源模型，这可能限制了该流水线在完全开源环境下的复现成本。
*   **计算开销**：RL 阶段的全参数微调对于 7B 规模的模型来说计算资源消耗巨大，普通研究者难以负担。
*   **幻觉风险**：虽然有自我修正机制，但在极度复杂的长视频中，模型仍可能产生逻辑幻觉，将不相关的片段强行关联。

（完）
