---
title: "STRIVE: Structured Spatiotemporal Exploration for Reinforcement Learning in Video Question Answering"
title_zh: STRIVE：视频问答中强化学习的结构化时空探索
authors: "Emad Bahrami, Olga Zatsarynna, Parth Pathak, Sunando Sengupta, Juergen Gall, Mohsen Fayyaz"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01824v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于视频问答的时空强化学习
tldr: 本研究针对视频问答中强化学习奖励信号方差低、不稳定的问题，提出了STRIVE框架。该框架通过构建视频的时空变体并进行跨文本与视觉的联合归一化，扩展了组内比较的维度。结合重要性感知采样机制，STRIVE在确保语义一致性的同时增强了探索的鲁棒性。在VideoMME等六个基准测试中，该方法显著提升了大型多模态模型的视频推理性能，为稳定多模态强化学习提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01824v1/fig-001.webp\", \"caption\": \"Table 1: Performance on six video understanding benchmarks. To ensure a fair comparison, models are evaluated with a maximum of 32 frames. Our proposed STRIVE framework outperforms the standard GRPO baseline across both Qwen2.5-VL-7B and Qwen3-VL-8B architectures. Results marked with * are evaluated using publicly available model weights.\", \"page\": 6, \"index\": 1, \"width\": 975, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2604.01824v1/fig-002.webp\", \"caption\": \"Figure 1: STRIVE prevents advantage collapse in video reinforcement learning. (a) Standard policy optimization samples multiple textual responses from a single, fixed video representation. When these generated responses lack diversity, the reward variance can drop to zero (Var(r) = 0), stalling the gradient of the objective J(θ). (b) Our proposed STRIVE framework employs Variant Construction to systematically generate multiple spatiotemporal visual variants of the input video. By computing joint advantages across both textual generations and varied visual perspectives, STRIVE empirically maintains a consistently positive reward variance (Var(r) > 0), delivering informative updates to the policy J(θ).\", \"page\": 2, \"index\": 2, \"width\": 943, \"height\": 588}, {\"url\": \"assets/figures/arxiv/2604.01824v1/fig-003.webp\", \"caption\": \"Table 2: Ablation study of STRIVE variant construction strategies. We evaluate the three broad categories of transformations introduced in our method: deterministic temporal grouping, stochastic grouping, and importance-based semantic grouping. For the importance-based approach, we compare cross-modal similarity (Sim) and gradient-based attribution (Grad) scoring. Finally, we show the impact of adding spatial augmentations to the best-performing temporal strategy.\", \"page\": 7, \"index\": 3, \"width\": 973, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.01824v1/fig-004.webp\", \"caption\": \"Figure 3: Empirical analysis of optimization dynamics. (a) Standard deviation of rewards, demonstrating that STRIVE maintains consistently higher variance than the GRPO baseline. (b) Fraction of zero-advantage updates, illustrating STRIVE’s ability to prevent gradient starvation. (c) Gradient norm over training steps. Semi-transparent lines show the raw gradient values at each step to highlight the severe optimization instability and chaotic spikes of standard GRPO, while bold solid lines denote the smoothed trajectory.\", \"page\": 7, \"index\": 4, \"width\": 961, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.01824v1/fig-005.webp\", \"caption\": \"Figure 4: Qualitative comparison between STRIVE and the standard GRPO baseline. (Top) STRIVE successfully isolates the relevant driving frames to correctly answer a question about a person’s mode of transport, whereas GRPO is distracted by irrelevant walking scenes. (Bottom) STRIVE accurately determines the strict temporal sequence of objects, demonstrating how question-guided variant construction improves fine-grained spatiotemporal reasoning and prevents reliance on flawed priors.\", \"page\": 10, \"index\": 5, \"width\": 974, \"height\": 827}, {\"url\": \"assets/figures/arxiv/2604.01824v1/fig-006.webp\", \"caption\": \"Table 3: Generalization of the STRIVE framework. We compare the standard GRPO and Dr.GRPO algorithms with their STRIVE-enhanced counterparts. Incorporating our spatiotemporal variant construction consistently improves the average performance regardless of the underlying policy optimization baseline.\", \"page\": 8, \"index\": 6, \"width\": 971, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.01824v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of our STRIVE Framework. Given a video input v and question q, a set of transformations {Ti}Mi=1 is applied to generate video variants {ṽi}Mi=1. Each variant is paired with the question to form input tuples (ṽi, q). The policy model produces G textual responses for each of the M variants, resulting in a G ×M matrix of responses {oi,j}G,M\", \"page\": 4, \"index\": 7, \"width\": 971, \"height\": 288}]"
motivation: 现有的多模态强化学习方法在处理相似正确度的响应时，常因奖励方差过低导致优势估计不稳定，限制了策略更新的有效性。
method: 提出一种结构化时空探索机制，通过生成视频的多样化时空变体并结合重要性感知采样，实现文本生成与视觉变体的联合奖励归一化。
result: 在VideoMME、TempCompass等六个主流视频推理基准测试中，STRIVE相较于强强化学习基线模型展现出持续的性能提升。
conclusion: 结构化的时空探索是稳定多模态强化学习并提升模型视频推理能力的有效且原则性的机制。
---

## 摘要
我们提出了 STRIVE（基于重要性感知变体探索的时空强化学习），这是一个用于视频问答的结构化强化学习框架。虽然基于组的策略优化方法在大型多模态模型中展现出应用前景，但当响应表现出相似的正确性时，它们往往面临奖励方差低的问题，导致优势估计微弱或不稳定。STRIVE 通过为每个输入视频构建多个时空变体，并在文本生成和视觉变体之间执行联合归一化，从而解决了这一局限性。通过将组比较从语言多样性扩展到结构化视觉扰动，STRIVE 丰富了奖励信号，并促进了更稳定且更具信息量的策略更新。为了确保探索在语义上保持合理，我们引入了一种重要性感知采样机制，该机制在保留时间覆盖范围的同时，优先处理与输入问题最相关的视频帧。这种设计鼓励在互补的视觉视角下进行鲁棒推理，而不是过拟合于单一的时空配置。在包括 VideoMME、TempCompass、VideoMMMU、MMVU、VSI-Bench 和 PerceptionTest 在内的六个具有挑战性的视频推理基准测试上的实验表明，在多个大型多模态模型中，该方法相较于强大的强化学习基线均有一致的提升。我们的结果强调了结构化时空探索作为稳定多模态强化学习和提高视频推理性能的一种原则性机制的作用。

## Abstract
We introduce STRIVE (SpatioTemporal Reinforcement with Importance-aware Variant Exploration), a structured reinforcement learning framework for video question answering. While group-based policy optimization methods have shown promise in large multimodal models, they often suffer from low reward variance when responses exhibit similar correctness, leading to weak or unstable advantage estimates. STRIVE addresses this limitation by constructing multiple spatiotemporal variants of each input video and performing joint normalization across both textual generations and visual variants. By expanding group comparisons beyond linguistic diversity to structured visual perturbations, STRIVE enriches reward signals and promotes more stable and informative policy updates. To ensure exploration remains semantically grounded, we introduce an importance-aware sampling mechanism that prioritizes frames most relevant to the input question while preserving temporal coverage. This design encourages robust reasoning across complementary visual perspectives rather than overfitting to a single spatiotemporal configuration. Experiments on six challenging video reasoning benchmarks including VideoMME, TempCompass, VideoMMMU, MMVU, VSI-Bench, and PerceptionTest demonstrate consistent improvements over strong reinforcement learning baselines across multiple large multimodal models. Our results highlight the role of structured spatiotemporal exploration as a principled mechanism for stabilizing multimodal reinforcement learning and improving video reasoning performance.

---

## 论文详细总结（自动生成）

### STRIVE：视频问答中强化学习的结构化时空探索论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：在视频问答（VideoQA）的强化学习（RL）后训练中，现有的组相对策略优化（如 GRPO）通常只针对单一固定视频输入采样多个文本回答。当这些回答的正确性趋同（全对或全错）时，奖励方差会趋于零，导致**优势估计（Advantage Estimation）失效或不稳定**（即“优势坍塌”），从而阻碍模型策略的有效更新。
*   **研究背景**：视频具有丰富的时空结构，单一的采样方式无法充分利用视频的多样性。现有的改进方法多集中在文本端或奖励函数设计上，忽略了从视觉输入端引入结构化多样性的潜力。

#### 2. 方法论
STRIVE 框架的核心思想是将多样性从“仅输出端”扩展到“输入+输出端”，构建一个双重分组的优化空间。
*   **核心思想**：为同一个视频-问题对构建 $M$ 个时空视觉变体，并为每个变体生成 $G$ 个文本响应，形成一个 $M \times G$ 的响应矩阵。
*   **关键技术细节**：
    *   **联合优势计算（Joint Advantage Computation）**：在整个 $M \times G$ 的响应池中进行奖励的归一化处理。公式上，优势 $A_{i,j}$ 取决于该响应奖励与全组均值及标准差的关系，这能有效维持健康的奖励方差。
    *   **重要性感知采样（Importance-aware Sampling）**：为了防止随机采样丢失关键信息，STRIVE 计算视频帧与问题的语义相关性分数（通过跨模态嵌入相似度或梯度归因）。
    *   **分段随机探索**：将视频分为 $K$ 个时段，在每个时段内根据相关性分数进行温度缩放的 Softmax 采样。这既保证了覆盖关键事件，又通过随机性维持了探索空间。
    *   **隐式一致性正则化**：通过跨变体比较，迫使模型在不同的视觉视角下保持回答的一致性，减少对语言先验的依赖。

#### 3. 实验设计
*   **数据集与基准测试**：在 6 个具有挑战性的视频推理基准上进行了评估，涵盖时空推理、专业知识和基础感知：
    *   **VideoMME**（综合评估）、**TempCompass**（时间逻辑）、**VideoMMMU**（多学科知识）、**MMVU**（专家级理解）、**VSI-Bench**（空间智能）、**PerceptionTest**（基础感知）。
*   **对比方法**：
    *   **基线模型**：Qwen2.5-VL-7B、Qwen3-VL-8B。
    *   **RL 算法**：标准 GRPO、Dr.GRPO、ArrowRL、Video-R1。
*   **公平性设置**：为了公平对比，STRIVE 设置 $M=2, G=4$，而标准 GRPO 设置 $M=1, G=8$，确保总计算预算（8 次前向传播）一致。

#### 4. 资源与算力
*   **训练细节**：模型在 Video-R1 数据集的子集上训练了 **1,000 个 RL 步数**。
*   **算力说明**：论文中**未明确列出具体的 GPU 型号和数量**，但提到由于“计算资源限制”，将 RL 训练步数限制在 1,000 步，并使用了 0.5B 的轻量化模型（如 LLaVA-OV-0.5B）作为辅助评分器以降低开销。

#### 5. 实验数量与充分性
*   **实验规模**：在两个不同代际的开源模型（Qwen2.5 和 Qwen3）上验证了有效性，并在 6 个主流榜单上取得了提升。
*   **消融实验**：非常充分。作者对比了：
    1.  不同的变体构建策略（确定性、随机、基于重要性）。
    2.  不同的评分模型（SigLIP-2、LLaVA-OV、策略模型自身）。
    3.  视觉变体数 $M$ 与文本生成数 $G$ 的权衡。
    4.  在不同 RL 算法（GRPO vs Dr.GRPO）上的泛化性。
*   **客观性**：通过固定计算预算进行对比，并选择客观的多选题（MCQA）作为评估指标，避免了 GPT-4 评分的主观偏差。

#### 6. 主要结论与发现
*   **解决坍塌**：STRIVE 显著降低了“零优势更新”的比例，使训练过程中的梯度更加平滑稳定。
*   **性能提升**：在 Qwen2.5-VL-7B 上，平均性能从 57.6 提升至 59.8；在 Qwen3-VL-8B 上也有显著增益，证明了策略的可扩展性。
*   **视觉锚定**：定性分析显示，STRIVE 能帮助模型更好地定位关键帧（如区分开车还是走路），减少了幻觉。

#### 7. 优点
*   **原理性创新**：从输入侧解决 RL 训练不稳定的问题，为多模态 RL 提供了一个新的维度。
*   **即插即用**：作为一种变体构建框架，可以轻松集成到现有的各种组优化 RL 算法中。
*   **效率平衡**：通过重要性采样和轻量化评分器，在不增加总推理次数的前提下提升了训练质量。

#### 8. 不足与局限
*   **训练时长较短**：受限于资源，仅训练了 1,000 步，长期训练的收敛特性尚待观察。
*   **评分器依赖**：重要性采样依赖于预先计算的评分，虽然有轻量化方案，但仍增加了训练流水线的复杂性。
*   **任务局限**：主要针对视频问答任务，对于长视频生成或非问答类任务的适用性未做探讨。

（完）
