---
title: "BoxTuning: Directly Injecting the Object Box for Multimodal Model Fine-Tuning"
title_zh: BoxTuning：直接注入目标框用于多模态模型微调
authors: "Zekun Qian, Ruize Han, Wei Feng"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11136v1"
tags: ["query:lvu"]
score: 9.0
evidence: 视频问答的对象级时空理解
tldr: "针对多模态大模型在视频问答中缺乏细粒度物体感知的问题，本文提出 BoxTuning。该方法摒弃了高成本的文本坐标序列化方式，直接在视频帧上渲染彩色边界框和运动轨迹作为视觉提示。这种方法显著降低了 87-93% 的文本 Token 开销，保留了完整的时间分辨率和运动信息，在多个视频 QA 基准测试中表现优于传统方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11136v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of the BoxTuning framework. Visually prompted frames, produced by rendering colored bounding boxes and trajectory trails from an off-the-shelf detector (YOLO-World [5]) and tracker (SAM 2 [29]), are fed into the vision encoder together with a concise text legend mapping colors to object classes and the question. The vision encoder is kept frozen, while the vision projector (STC connector) is fully fine-tuned and the LLM is adapted via LoRA [8].\", \"page\": 7, \"index\": 1, \"width\": 575, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.11136v1/fig-002.webp\", \"caption\": \"Fig. 1: Comparison of object-centric representation strategies for video MLLMs. Left : ObjectMLLM [31] serializes per-frame bounding box coordinates as text tokens (e.g ., “(Object 0) adult: frame 0 [73 30 92 100] ...”), producing 1,218 tokens for a single video. The original keyframes lack intermediate dynamics, leading to action misinterpretation. Right : BoxTuning renders colored bounding boxes and trajectory trails directly on the video frames, with only a concise color-mapped text legend (e.g ., “(1) Red box: Adult; (2) Blue box: Elephant; ...”, 182 tokens). The trajectory-augmented keyframes capture continuous dynamics, enabling accurate temporal reasoning. Overall, BoxTuning achieves 85% token savings (in this case) while bridging temporal gaps that text-coordinate methods miss.\", \"page\": 3, \"index\": 2, \"width\": 721, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.11136v1/fig-003.webp\", \"caption\": \"Fig. 3: Visual prompt examples on CLEVRER video frames. (a) Bounding boxes alone provide spatial localization of each object. (b) Adding trajectory trails encodes motion direction and speed across intermediate frames, aiding temporal reasoning. The text legend (not shown) maps each color to its object class.\", \"page\": 8, \"index\": 3, \"width\": 666, \"height\": 220}]"
motivation: 现有的文本坐标序列化方法存在模态不匹配、Token 开销巨大且迫使时间采样率降低的问题。
method: 通过在视频帧上直接绘制彩色边界框和轨迹线，并仅在文本端保留颜色与物体的映射关系，将空间位置信息注入视觉模态。
result: 在五个视频 QA 基准测试中，BoxTuning 在空间任务上超越了基线模型，并大幅减少了推理任务中的性能衰减。
conclusion: 视觉提示是一种比文本坐标更自然、更高效的向视频多模态大模型传递物体信息的方式。
---

## 摘要
物体级时空理解对于视频问答（Video QA）至关重要，然而现有的多模态大语言模型（MLLMs）通常对视频帧进行整体编码，缺乏细粒度物体定位（object grounding）的显式机制。近期的研究通过将边界框（bounding box）坐标序列化为文本 token 来解决这一问题，但这种“文本-坐标”范式存在根本性的模态不匹配：物体信息本质上是视觉的，而将其编码为文本会产生高昂的 token 成本，迫使模型进行剧烈的时间下采样。我们提出了 BoxTuning，通过将物体的时空信息直接注入视觉模态来解决这一不匹配问题。彩色边界框和轨迹线被作为视觉提示（visual prompts）渲染到视频帧上，仅保留简洁的“颜色-物体”图例作为文本。这显著降低了 token 成本，在实践中实现了 87-93% 的文本 token 削减。它还保留了完整的时间分辨率，其中轨迹线在每个关键帧内进一步编码了帧间运动方向和速度，恢复了文本-坐标方法被迫丢弃的细粒度动态信息。在五个视频问答基准测试（CLEVRER、Perception Test、STAR、NExT-QA、IntentQA）上的实验结果表明，BoxTuning 在空间导向型任务上超越了文本-坐标基准线，并几乎消除了在推理中心型任务中观察到的准确率下降，证明了视觉提示是将物体信息传递给视频 MLLM 的一种更自然、更高效的范式。

## Abstract
Object-level spatial-temporal understanding is essential for video question answering, yet existing multimodal large language models (MLLMs) encode frames holistically and lack explicit mechanisms for fine-grained object grounding. Recent work addresses this by serializing bounding box coordinates as text tokens, but this text-coordinate paradigm suffers from a fundamental modality mismatch: object information is inherently visual, yet encoding it as text incurs a high token cost that forces aggressive temporal downsampling. We propose BoxTuning, which resolves this mismatch by injecting object spatial-temporal information directly into the visual modality. Colored bounding boxes and trajectory trails are rendered onto video frames as visual prompts, with only a concise color-to-object legend retained as text. This reduces the token cost significantly, achieving 87-93% text token reduction in practice. It also preserves full temporal resolution, where the trajectory trails further encode inter-frame motion direction and speed within each keyframe, recovering fine-grained dynamics that text-coordinate methods are forced to discard. Experimental results on five video QA benchmarks (CLEVRER, Perception Test, STAR, NExT-QA, IntentQA) show that BoxTuning surpasses text-coordinate baselines on spatially oriented tasks and nearly eliminates the accuracy degradation observed on reasoning-centric tasks, establishing visual prompting as a more natural and efficient paradigm for conveying object information to video MLLMs.

---

## 论文详细总结（自动生成）

以下是对论文《BoxTuning: Directly Injecting the Object Box for Multimodal Model Fine-Tuning》的结构化总结：

### 1. 核心问题与整体含义（研究动机）
视频问答（Video QA）要求模型理解物体间的复杂交互和时空动态。现有的多模态大模型（MLLM）通常对视频帧进行全局编码，缺乏细粒度的物体感知。
*   **现有方案的缺陷**：近期研究（如 ObjectMLLM）将物体坐标序列化为文本 Token。这种“文本-坐标”范式存在**模态不匹配**（视觉信息强行转文本）、**Token 开销巨大**（导致上下文窗口过载）以及**时间采样受限**（为节省 Token 必须大幅降低采样率，丢失运动细节）等问题。
*   **研究目标**：提出一种更自然、高效的物体信息注入方式，在降低 Token 成本的同时保留完整的时间动态信息。

### 2. 方法论：BoxTuning
BoxTuning 的核心思想是将物体时空信息直接渲染在视觉模态中，而非转化为文本。
*   **物体信息获取**：
    *   使用 **YOLO-World** 进行开放词汇目标检测。
    *   使用 **SAM 2** 进行跨帧掩码跟踪，获取精确的边界框。
    *   采用迭代检测策略处理新进入画面的物体。
*   **视觉提示构建（核心技术）**：
    *   **彩色边界框**：为每个跟踪对象分配唯一颜色，直接在视频帧上绘制。
    *   **运动轨迹线（Trajectory Trails）**：在当前帧绘制连接前序若干帧中心点的折线。轨迹的长度是自适应的（对应采样间隔），从而在单帧内编码了物体的运动方向和速度。
*   **简洁文本图例**：仅在文本输入中提供极简的颜色映射（如“红色框：成年人”），将 Token 复杂度从 $O(N \times T \times C)$ 降至 $O(N \times C')$，其中 $T$（帧数）被完全消除。
*   **微调策略**：基于 VideoLLaMA2-7B，冻结视觉编码器，全量微调时空卷积（STC）连接器，并对 LLM 进行 LoRA 微调。

### 3. 实验设计
*   **数据集**：涵盖 5 个主流基准测试：
    *   **CLEVRER**（合成碰撞推理）、**Perception Test**（感知诊断）、**STAR**（情境推理）、**NExT-QA**（因果/时间推理）、**IntentQA**（人类意图推理）。
*   **对比方法**：
    *   **基线模型**：VideoLLaMA2-7B。
    *   **SOTA 对比**：ObjectMLLM（文本坐标法）、SeViLA、ViLA、LLaVA-NeXT-Video 等。
*   **公平性保证**：使用与 ObjectMLLM 相同的检测/跟踪流水线，确保性能提升仅源于表示方式的改进。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：采用 AdamW 优化器，学习率为 $2 \times 10^{-4}$，Batch Size 为 16，使用了 LoRA（Rank=128）。
*   **时长**：文中未明确给出总训练时长，但提到了余弦学习率调度和 100 步预热。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同性质的视频 QA 数据集上进行了全面测试。
*   **消融实验**：
    1.  **组件贡献**：分别验证了边界框、文本图例、轨迹线的作用。
    2.  **轨迹长度**：对比了固定长度与自适应长度的效果。
    3.  **线宽敏感度**：测试了不同渲染粗细对模型感知的影响。
*   **充分性评价**：实验设计客观且充分，通过跨领域的多个数据集验证了方法的通用性，尤其是针对“推理中心型”任务中 Token 溢出问题的分析具有说服力。

### 6. 主要结论与发现
*   **性能领先**：BoxTuning 在所有 5 个基准测试中均优于 ObjectMLLM。在空间导向任务（如 CLEVRER）上提升显著。
*   **Token 效率**：相比文本坐标法，BoxTuning 实现了 **87%–93% 的 Token 削减**（平均从 1000 Token 降至 88 Token）。
*   **推理保护**：在 NExT-QA 等复杂推理任务中，文本坐标法常因 Token 过多干扰 LLM 导致性能下降，而 BoxTuning 几乎消除了这种负面影响，保持了模型原有的推理能力。
*   **运动恢复**：视觉轨迹线能有效补偿时间下采样带来的动态信息损失。

### 7. 优点
*   **范式创新**：将物体信息回归视觉模态，符合多模态模型的直觉，解决了长视频处理中的 Token 焦虑。
*   **即插即用**：不需要修改模型架构，通过视觉提示工程和轻量化微调即可实现。
*   **时空融合**：通过轨迹线在静态帧中巧妙地引入了时间维度的运动矢量信息。

### 8. 不足与局限
*   **视觉遮挡风险**：在物体极度密集的场景下，过多的彩色框和轨迹线可能会遮挡原始视频的细粒度特征。
*   **依赖外部工具**：性能高度依赖于前端检测器（YOLO-World）和跟踪器（SAM 2）的准确性，若检测漏掉关键物体，模型无法补救。
*   **颜色容量**：虽然预设了 32 种颜色，但在极端多目标的场景下，颜色的区分度和模型的辨色能力可能面临挑战。

（完）
