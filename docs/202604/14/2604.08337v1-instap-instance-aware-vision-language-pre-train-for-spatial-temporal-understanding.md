---
title: "InstAP: Instance-Aware Vision-Language Pre-Train for Spatial-Temporal Understanding"
title_zh: InstAP：面向时空理解的实例感知视觉语言预训练
authors: "Ashutosh Kumar, Rajat Saini, Jingjing Pan, Mustafa Erdogan, Mingfang Zhang, Betty Le Dem, Norimasa Kobori, Quan Kong"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08337v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于时空理解的视觉语言预训练
tldr: 针对当前视觉语言预训练模型在实例级推理方面的不足，本文提出InstAP框架。该框架通过将文本描述与特定的时空区域关联，实现了全局场景与细粒度实例的联合优化。同时，研究者构建了大规模双粒度数据集InstVL，包含200万张图像和5万段视频。实验证明，InstAP在实例检索和全局视频理解任务上均表现优异，有效提升了模型的时空感知能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08337v1/fig-001.webp\", \"caption\": \"Figure 3. Our instance-aware alignment mechanism. Instance features (Query Q) from a Trajectory RoI Encoder (fθ) are fused with global context (Key K, Value V ) via an Attention Pool to create an instance-aware embedding. This embedding is contrasted with text features (eϕ). The loss forces the model to match positive pairs (V1T1) while contrasting against negatives from different videos (V2T1/V2T2) and masking potential false-negative pairs from the same video (V1T2), enforcing fine-grained discrimination (Eq. 8).\", \"page\": 4, \"index\": 1, \"width\": 932, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2604.08337v1/fig-002.webp\", \"caption\": \"Figure 2. Illustration of our InstVL dataset. We display sampled frames with color-coded, temporally-consistent instance trajectories (e.g., ID: 0, ID: 1). The top text provides the fine-grained instance captions grounded to these trajectories; the bottom text provides the holistic global caption for the entire scene.\", \"page\": 3, \"index\": 2, \"width\": 496, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.08337v1/fig-003.webp\", \"caption\": \"Table 1. Comparison of SOTA models and our InstAP on the InstVL test set. We report T2V/V2T R@1 on the instance and global splits across InstVL(img), InstVL(img-zero), and InstVL(video). UMT-L (InstVL; g/g+i) baselines use the same full training corpus as InstAP, trained with only InstVL’s global captions (g) or with all InstVL captions treated as global (g+i).\", \"page\": 7, \"index\": 3, \"width\": 910, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2604.08337v1/fig-004.webp\", \"caption\": \"Table 2. Zero-shot text-to-video retrieval (R@1 / R@5 / R@10) on standard benchmarks. UMT-L (InstVL; g) and UMT-L (InstVL; g+i) are baselines trained on the full corpus as InstAP.\", \"page\": 7, \"index\": 4, \"width\": 630, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.08337v1/fig-005.webp\", \"caption\": \"Figure 5. InstAP consistently retrieves correct fine-grained descriptions, whereas the global baseline [29] is confounded by semantic distractors and mismatches the query.\", \"page\": 8, \"index\": 5, \"width\": 466, \"height\": 509}]"
motivation: 现有的视觉语言预训练模型主要依赖全局监督，导致其在处理细粒度实例推理和时空定位时表现欠佳。
method: 提出InstAP框架，通过在InstVL数据集上联合优化全局视觉文本对齐与细粒度的实例级对比学习。
result: InstAP在实例级检索上显著超越现有模型，并在MSR-VTT等多个视频基准测试中实现了领先的零样本性能。
conclusion: 引入实例感知的预训练目标能显著增强模型对局部细节的捕捉能力，进而全面提升全局场景的理解效果。
---

## 摘要
当前的视觉语言预训练（VLP）范式在全局场景理解方面表现出色，但由于仅采用全局监督，在实例级推理方面面临挑战。我们提出了 InstAP，这是一种实例感知预训练框架，通过将文本提及（textual mentions）关联到特定的时空区域，共同优化全局视文对齐和细粒度的实例级对比对齐。为了支持这一框架，我们推出了 InstVL，这是一个包含 200 万张图像和 5 万个视频的大规模数据集，具有双粒度标注：整体场景描述和密集的、关联到区域的实例描述。在 InstVL 基准测试中，InstAP 在实例级检索方面显著优于现有的 VLP 模型，并且也超过了在完全相同的数据语料库上训练的强 VLP 基准模型，从而验证了我们实例感知目标的有效性。此外，以实例为中心的预训练提升了全局理解能力：InstAP 在包括 MSR-VTT 和 DiDeMo 在内的多个视频基准测试中取得了具有竞争力的零样本性能。定性可视化进一步表明，InstAP 能将文本提及定位到正确的实例，而仅采用全局监督的模型则表现出更弥散的场景级注意力。

## Abstract
Current vision-language pre-training (VLP) paradigms excel at global scene understanding but struggle with instance-level reasoning due to global-only supervision. We introduce InstAP, an Instance-Aware Pre-training framework that jointly optimizes global vision-text alignment and fine-grained, instance-level contrastive alignment by grounding textual mentions to specific spatial-temporal regions. To support this, we present InstVL, a large-scale dataset (2 million images, 50,000 videos) with dual-granularity annotations: holistic scene captions and dense, grounded instance descriptions. On the InstVL benchmark, InstAP substantially outperforms existing VLP models on instance-level retrieval, and also surpasses a strong VLP baseline trained on the exact same data corpus, isolating the benefit of our instance-aware objective. Moreover, instance-centric pre-training improves global understanding: InstAP achieves competitive zero-shot performance on multiple video benchmarks, including MSR-VTT and DiDeMo. Qualitative visualizations further show that InstAP localizes textual mentions to the correct instances, while global-only models exhibit more diffuse, scene-level attention.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **InstAP**，一个旨在增强视觉语言预训练（VLP）模型在时空理解和实例级推理能力的框架。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉语言预训练模型（如 CLIP 及其变体）主要关注视频/图像与文本之间的**全局对齐**。这种“粗粒度”的监督导致模型在处理需要精确时空定位和区分特定实体的任务（如“男孩扔球”与“狗跳跃”同时发生时的区分）时表现不佳。
*   **研究动机**：
    *   缺乏大规模、高质量且包含时空轨迹标注的视频-文本数据集。
    *   现有的实例级方法通常是“事后修补”（如依赖预训练检测器或辅助头），而非将实例感知作为核心表征能力进行训练。
    *   作者认为，通过显式地将文本提及（Mentions）与时空区域（Trajectories）对齐，不仅能提升细粒度推理，还能反哺全局场景的理解。

### 2. 方法论
*   **核心思想**：通过双粒度标注的数据集，同时优化全局视文对齐和实例级的对比对齐。
*   **关键技术细节**：
    *   **InstVL 数据集构建**：利用 GroundingDINO 进行检测，SAM2 进行实例跟踪，并使用大语言模型（如 GPT-4o）生成全局描述和针对特定轨迹的实例描述。包含 200 万张图像和 5 万段视频。
    *   **InstAP 架构**：
        *   **教师-学生框架**：采用 ViT-L 作为学生网络，通过掩码视频建模（MVM）回归教师网络（CLIP）的高层特征。
        *   **全局-局部交叉注意力（Global-Local Cross Attention）**：将实例裁剪区域（Crop）的特征作为 Query，与全图特征（Key, Value）进行交叉注意力融合，从而为实例特征注入全局上下文。
        *   **联合优化目标**：
            1.  **全局损失**：包括视频-文本对比（VTC）、匹配（VTM）和掩码语言建模（MLM）。
            2.  **实例感知损失**：在实例级别同样应用 VTC、VTM 和 MLM。特别地，在实例对比学习中，会屏蔽来自同一视频的其他实例，以避免伪负样本干扰。
    *   **算法流程**：先进行自监督掩码视频建模预训练，再在 InstVL 及其他通用数据集上进行双粒度对齐训练。

### 3. 实验设计
*   **数据集/场景**：
    *   **预训练**：K710、HDVILA、WebVid、CC3M/12M、ShareGPT4V 等。
    *   **内部基准**：InstVL-1K/10K（包含图像、视频及跨分布的 zero-shot 测试集）。
    *   **外部基准（Zero-shot 检索）**：MSR-VTT、DiDeMo、MSVD、LSMDC、ActivityNet。
*   **对比方法**：包括 VideoPrism、CLIP4Clip、UMT-L、SigLIP、InternVid (ViCLIP) 等 SOTA 模型。
*   **消融实验**：验证了实例损失（$L_{inst}$）、学习温度、损失权重、标题采样策略以及视频轨迹数据对性能的贡献。

### 4. 资源与算力
论文明确列出了庞大的算力消耗：
*   **第一阶段（掩码视频建模）**：使用了 **320 台 NVIDIA H100 GPU**。
*   **第二阶段（对齐学习）**：使用了 **200 台 NVIDIA B200 GPU**（每张显卡 180GB 显存）。
*   **训练时长**：第一阶段 800 个 epoch，第二阶段 15 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。不仅在 5 个标准的第三方视频检索基准上进行了测试，还构建了包含不同规模（1K/10K）和分布（COYO 来源的 zero-shot 分支）的内部测试集。
*   **客观性与公平性**：
    *   作者设置了严谨的基准线：**UMT-L (InstVL; g/g+i)**。即在完全相同的数据集上，分别只用全局标题或将实例标题视为全局标题进行训练。
    *   结果显示 InstAP 显著优于这些基准，证明了性能提升源于**算法框架**而非仅仅是数据量的增加。

### 6. 主要结论与发现
*   **实例感知反哺全局理解**：在 MSR-VTT (41.1 R@1) 和 DiDeMo (54.0 R@1) 上刷新了零样本检索的记录，证明了细粒度对齐能增强模型对整体场景的鲁棒性。
*   **定位能力显著提升**：定性分析显示 InstAP 能精确关注到文本描述对应的时空区域，而传统模型注意力较为弥散。
*   **框架有效性**：在 InstVL 视频实例检索任务上，InstAP 将 T2V R@1 从基准的 40% 左右提升至 60.63%。

### 7. 亮点
*   **数据集贡献**：InstVL 是首个大规模、通用领域、包含自由文本描述和完整时空轨迹的双粒度数据集。
*   **端到端集成**：将实例感知直接嵌入预训练目标，而非作为插件，使模型表征具有原生细粒度判别力。
*   **性能强劲**：在保持全局理解领先的同时，极大地增强了局部定位和推理能力。

### 8. 不足与局限
*   **复杂场景失效**：在严重遮挡、目标极小或背景极其复杂的场景下，模型仍会出现多实例混淆（约占错误类型的 44.6%）。
*   **算力门槛极高**：使用了数百张 H100 和 B200，这种规模的预训练对于大多数学术机构和中小型企业来说难以复现。
*   **数据依赖**：性能高度依赖于由 GPT-4o 等高级模型生成的标注质量，可能存在模型幻觉传播的风险。

（完）
