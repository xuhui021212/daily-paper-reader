---
title: "Mosaic: Cross-Modal Clustering for Efficient Video Understanding"
title_zh: Mosaic：用于高效视频理解的跨模态聚类
authors: "Tuowei Wang, He Zhou, Chengru Song, Qiushi Li, Ju Ren"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10060v1"
tags: ["query:lvu"]
score: 10.0
evidence: 流式长视频理解的高效集群驱动VLM推理
tldr: 针对流式长视频理解中大视觉语言模型（VLM）面临的KVCache管理难题，本文提出了Mosaic系统。现有方法因Token级管理导致高开销和数据移动碎片化，Mosaic利用VLM KVCache中隐含的跨模态聚类特性（视觉连贯性与语义相关性），以聚类为基本单位进行组织和检索。实验表明，Mosaic相比现有技术实现了高达1.38倍的加速，显著提升了长视频推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10060v1/fig-001.webp\", \"caption\": \"Fig. 1. Comparison of token-level and cluster-level KVCache management.\", \"page\": 2, \"index\": 1, \"width\": 522, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.10060v1/fig-002.webp\", \"caption\": \"Fig. 2. Architecture of the vision-language model.\", \"page\": 2, \"index\": 2, \"width\": 520, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.10060v1/fig-003.webp\", \"caption\": \"Fig. 12. Performance comparison across models and datasets with different video lengths (Qwen denotes Qwen2.5-VL-7B).\", \"page\": 10, \"index\": 3, \"width\": 1064, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2604.10060v1/fig-004.webp\", \"caption\": \"Fig. 3. (a) Accuracy under sparse frame sampling (16–128 frames), with only minor degradation as fewer frames are used. (b) Index search time as a percentage of attention time across video durations, ranging from 17% to 28%. (c) I/O time decreases with larger block sizes, with a 30% reduction when increasing from 1 to 64 frames per block.\", \"page\": 3, \"index\": 4, \"width\": 511, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.10060v1/fig-005.webp\", \"caption\": \"Fig. 7. Performance comparison (normalized) between ReKV, LiveVLM and MOSAIC across models and datasets. Lines indicate average values.\", \"page\": 8, \"index\": 5, \"width\": 1063, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.10060v1/fig-006.webp\", \"caption\": \"Fig. 5. Overview of MOSAIC.\", \"page\": 4, \"index\": 6, \"width\": 521, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.10060v1/fig-007.webp\", \"caption\": \"Fig. 6. Nested cross-modal clustering and two-stage retrieval indexing.\", \"page\": 5, \"index\": 7, \"width\": 524, \"height\": 283}]"
motivation: 现有的Token级KVCache管理方法在处理流式长视频时存在高昂的管理开销和碎片化的数据移动问题。
method: 提出Mosaic系统，利用跨模态聚类作为KVCache组织、维护和检索的基本单元，以利用视觉连贯性和语义相关性。
result: 实验结果显示，Mosaic在长视频理解任务中优于现有基准，实现了最高1.38倍的推理加速。
conclusion: 通过将KVCache管理从Token级提升到聚类级，Mosaic有效解决了流式长视频推理中的内存和计算瓶颈。
---

## 摘要
大型视觉语言模型 (VLM) 正在实现交互式视频推理，从而催生了流式长视频理解。在这种场景下，视频帧连续到达，系统在保持长期上下文的同时，需在严格的延迟限制下生成响应。一个核心挑战是 KVCache 管理：随着视频流的增长，KVCache 迅速膨胀，增加了计算和内存开销。现有的基于检索的方法利用注意力稀疏性，并将非活跃的 KVCache 从 GPU 卸载到 CPU 内存，但其 Token 级的设计导致了高昂的管理开销和碎片化的数据移动。我们提出了 Mosaic，这是首个用于流式长视频理解的聚类驱动型 VLM 推理系统。我们的核心见解是，VLM 的 KVCache 表现出一种隐式的跨模态聚类结构：检索到的 KV 状态形成的群体由视觉连贯性和语义相关性共同塑造。基于这一观察，Mosaic 将跨模态聚类作为 KVCache 组织、维护和检索的基本单位。评估结果表明，Mosaic 的性能优于最先进的基准方法，实现了高达 1.38 倍的加速。

## Abstract
Large vision-language models (VLMs) are enabling interactive video reasoning, giving rise to streaming long-video understanding. In this setting, frames arrive continuously, while the system preserves long-term context and generates responses under strict latency constraints. A central challenge is KVCache management: as video streams grow, KVCache expands rapidly, increasing computation and memory overhead. Existing retrieval-based approaches exploit attention sparsity and offload inactive KVCache from GPU to CPU memory, but their token-level design causes high management overhead and fragmented data movement. We present Mosaic, the first cluster-driven VLM inference system for streaming long-video understanding. Our key insight is that VLM KVCache exhibits an implicit cross-modal clustering structure: retrieved KV states form groups jointly shaped by visual coherence and semantic relevance. Based on this observation, Mosaic uses cross-modal clusters as the basic unit of KVCache organization, maintenance, and retrieval. Evaluations show that Mosaic outperforms state-of-the-art baselines, achieving up to 1.38x speedup.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MOSAIC** 的系统，旨在解决大视觉语言模型（VLM）在处理流式长视频时的推理效率问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在“流式长视频理解”场景下，视频帧连续到达，系统需要维护极长的上下文（KVCache）。现有的基于检索的 KVCache 管理方法（如 ReKV）主要在 **Token 级别**进行操作。
*   **研究动机**：
    *   **管理开销高**：逐帧进行 Token 级别的评分、索引和选择会产生显著的计算和簿记成本。
    *   **I/O 碎片化**：Token 级别的检索导致在 CPU 和 GPU 之间频繁传输细碎的数据块，带宽利用率低，延迟高。
    *   **观察发现**：作者发现 VLM 的 KVCache 存在隐式的“跨模态聚类”结构，即被检索的 KV 状态通常在视觉上连贯且在语义上相关，这为以“聚类”为单位管理 KVCache 提供了可能。

### 2. 方法论：核心思想与关键技术
MOSAIC 提出了以**跨模态聚类**作为 KVCache 组织、维护和检索的基本单元。
*   **跨模态构建器 (Cross-Modal Constructor)**：
    *   **嵌套聚类**：首先在视觉空间（由视觉编码器诱导）进行粗粒度分区，然后在每一层的语义空间（由 Transformer 的 Key 诱导）进行细粒度细化。
    *   **分级索引**：在 GPU 上维护轻量级的聚类中心向量（Centroids）作为索引，支持两阶段检索（先视觉后语义）。
*   **自适应维护器 (Self-Adaptive Maintainer)**：
    *   **内聚性追踪**：通过计算聚类内方差（Variance）监控质量。
    *   **延迟分裂 (Deferred Splitting)**：当新帧导致聚类质量下降时，不立即在 CPU/GPU 间移动数据进行重聚类，而是先存入 GPU 缓冲区，待该聚类被正常检索时再合并分裂，减少无效 I/O。
*   **高性能执行器 (High-Performance Executor)**：
    *   **批处理执行**：将细碎的单帧操作转化为矩阵运算，提高硬件利用率。
    *   **跨层预取 (Overlap-Aware Prefetch)**：利用相邻层 Query 的相似性，在当前层计算时异步预取下一层可能需要的聚类，掩盖 I/O 延迟。

### 3. 实验设计
*   **数据集**：使用了 5 个主流长视频基准测试：MLVU、LongVideoBench、VideoMME、RVS-ego、RVS-movie，以及一个来自快手（Kuaishou）的真实直播流数据集。
*   **对比方法 (Baselines)**：
    *   **NoCache**：均匀采样帧并重新计算。
    *   **ReKV**：Token 级别的 KVCache 检索。
    *   **LiveVLM**：结合 Token 检索与 KVCache 压缩。
    *   **StreamMem**：查询无关的内存管理方法。
*   **评估模型**：LLaVA-OneVision-7B、Qwen2.5-VL-7B、Qwen2.5-VL-3B。

### 4. 资源与算力
*   **硬件平台**：
    *   **平台 A**：单台 NVIDIA H800 GPU (80GB)，配 Intel Platinum 8468V CPU。
    *   **平台 B**：8 台 NVIDIA A40 GPU (48GB)，用于扩展性测试。
*   **训练/推理时长**：论文未明确提及训练时长（因为 MOSAIC 是推理优化系统，通常无需重新训练模型），主要关注推理阶段的延迟（TTFT）和吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同架构和规模的模型，5 个公开数据集加 1 个工业级真实数据集，实验设置较为全面。
*   **消融实验**：针对嵌套聚类、延迟分裂、批处理、跨层预取等核心组件均做了消融分析，验证了各模块的贡献。
*   **客观性**：通过在不同 GPU 架构（H800 和 A40）上进行测试，并对比了多种最先进的基准，实验结果具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **性能提升**：MOSAIC 相比 ReKV 和 LiveVLM 实现了高达 **1.38 倍**的加速。
*   **内存优化**：在处理 1024 帧视频时，相比 ReKV 减少了约 **19%** 的显存占用（相比原始模型减少更多，避免了 OOM）。
*   **准确率**：在保持或略微提升推理准确率的同时，显著降低了延迟，证明了聚类级检索能有效捕捉关键上下文。
*   **扩展性**：在多 GPU 环境下表现出近乎线性的吞吐量增长。

### 7. 优点与亮点
*   **范式创新**：首次提出将 KVCache 管理从 Token 级提升到聚类级，利用了 VLM 特有的跨模态结构特性。
*   **算法-系统协同设计**：不仅有聚类算法的改进（如延迟分裂），还有系统层面的优化（如跨层预取），完整解决了 I/O 瓶颈。
*   **即插即用**：作为推理系统，不需要对原始模型进行重新训练或微调，易于部署。

### 8. 不足与局限
*   **超参数依赖**：自适应维护中的方差阈值（Threshold）可能需要针对不同模型或任务进行微调，虽然文中提出了自适应公式，但在极端分布的数据下可能仍需人工干预。
*   **应用限制**：主要针对 Transformer 架构的 VLM，对于非 Transformer 架构（如基于线性循环的模型）的适用性未讨论。
*   **实时性挑战**：虽然实现了加速，但在极高帧率或超大规模并发场景下，聚类维护的计算开销是否会成为新的瓶颈仍有待观察。

（完）
