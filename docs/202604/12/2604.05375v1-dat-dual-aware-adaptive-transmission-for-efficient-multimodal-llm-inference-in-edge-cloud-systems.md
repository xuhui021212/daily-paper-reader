---
title: "DAT: Dual-Aware Adaptive Transmission for Efficient Multimodal LLM Inference in Edge-Cloud Systems"
title_zh: DAT：面向边缘云系统高效多模态大语言模型推理的双感知自适应传输
authors: "Qi Guo, Zheming Yang, Yunqing Hu, Chang Zhao, Wen Ji"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05375v1"
tags: ["query:lvu"]
score: 9.0
evidence: 连续视频流上的高效多模态大模型推理
tldr: 针对边缘云系统中多模态大模型（MLLM）处理连续视频流时面临的高计算和带宽开销问题，本文提出了DAT框架。该框架通过边云协作的小大模型级联、视觉引导的微调策略以及语义与带宽感知的自适应传输优化，实现了高质量语义生成、低延迟事件告警和有效的视觉证据补充。实验证明，DAT在保证高准确率的同时，显著降低了告警延迟并提升了传输效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05375v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of Traditional and DAT Service Pipelines.\", \"page\": 2, \"index\": 1, \"width\": 506, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.05375v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of DAT.\", \"page\": 3, \"index\": 2, \"width\": 506, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.05375v1/fig-003.webp\", \"caption\": \"Table 3: Overall Performance of Semantics-Bandwidth Aware Multi-Stream Adaptive Transmission\", \"page\": 8, \"index\": 3, \"width\": 514, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.05375v1/fig-004.webp\", \"caption\": \"Figure 3: Workflow of DAT: Semantic-Bandwidth Aware Multi-Stream Transmission for Efficient Multimodal LLM Inference in Edge-Cloud Systems.\", \"page\": 4, \"index\": 4, \"width\": 1056, \"height\": 379}]"
motivation: 旨在解决多模态大模型在带宽受限的边缘云系统中处理视频流时，计算开销大、通信延迟高以及视觉证据交付困难的问题。
method: 提出一种由边缘侧轻量级模型过滤帧并触发云端大模型推理的级联架构，并结合视觉引导微调策略与语义带宽感知的多流自适应传输优化。
result: "实验显示DAT达到了98.83%的识别准确率，在严重拥塞下将语义告警延迟降低了77.5%，并能在0.5秒内交付98.33%的视觉证据。"
conclusion: 通过联合优化级联推理与弹性传输，DAT有效提升了边缘云环境下多模态大模型推理的效率、准确性和实时性。
---

## 摘要
多模态大语言模型（MLLM）在语义理解和视觉推理方面展现出强大的能力，但在带宽受限的边缘云系统中，将其应用于连续视频流会产生极高的计算和通信开销，并阻碍低延迟警报和有效的视觉证据交付。为了应对这一挑战，我们提出了 DAT，旨在实现高质量的语义生成、低延迟的事件警报和有效的视觉证据补充。为了减少不必要的深度推理成本，我们提出了一种协作式的大小模型级联方案。轻量级的边缘侧小模型充当门控模块，用于过滤非目标事件帧并执行目标检测，仅针对可疑帧触发 MLLM 推理。在此基础上，我们引入了一种带有视觉引导和语义提示的高效微调策略，提高了结构化事件理解、目标检测和输出一致性。为了确保在带宽限制下实现低延迟语义警报和有效的视觉证据补充，我们进一步设计了一种感知语义和带宽的多流自适应传输优化方法。实验结果表明，DAT 实现了 98.83% 的识别准确率和 100% 的输出一致性。在严重拥塞的情况下，它将加权语义警报延迟降低了高达 77.5%，并在 0.5 秒内交付了 98.33% 的视觉证据，证明了联合优化级联推理和弹性传输的有效性。

## Abstract
Multimodal large language models (MLLMs) have shown strong capability in semantic understanding and visual reasoning, yet their use on continuous video streams in bandwidth-constrained edge-cloud systems incurs prohibitive computation and communication overhead and hinders low-latency alerting and effective visual evidence delivery. To address this challenge, we propose DAT to achieve high-quality semantic generation, low-latency event alerting, and effective visual evidence supplementation. To reduce unnecessary deep reasoning costs, we propose a collaborative small-large model cascade. A lightweight edge-side small model acts as a gating module to filter non-target-event frames and perform object detection, triggering MLLM inference only for suspicious frames. Building on this, we introduce an efficient fine-tuning strategy with visual guidance and semantic prompting, which improves structured event understanding, object detection, and output consistency. To ensure low-latency semantic alerting and effective visual evidence supplementation under bandwidth constraints, we further devise a semantics and bandwidth-aware multi-stream adaptive transmission optimization method. Experimental results show that DAT achieves 98.83% recognition accuracy and 100% output consistency. Under severe congestion, it reduces weighted semantic alert delay by up to 77.5% and delivers 98.33% of visual evidence within 0.5 s, demonstrating the effectiveness of jointly optimizing cascade inference and elastic transmission.

---

## 论文详细总结（自动生成）

### 论文总结：DAT——面向边缘云系统高效多模态大语言模型推理的双感知自适应传输

#### 1. 核心问题与整体含义
随着多模态大语言模型（MLLM）在视觉推理领域的崛起，将其应用于连续视频流监控（如交通监控）成为可能。然而，在边缘云协作系统中，面临两大核心挑战：
*   **计算开销巨大**：MLLM 参数量大，对所有视频帧进行深度推理会导致极高的计算延迟和成本。
*   **带宽瓶颈**：将海量高清视频实时上传至云端会造成上行链路拥塞，导致关键事件告警延迟，且低价值背景帧占据了大量带宽。
**DAT 框架**旨在通过“推理侧级联”与“传输侧自适应优化”的联合设计，实现高准确度的结构化语义生成、极低延迟的事件告警以及高效的视觉证据补充。

#### 2. 方法论
DAT 采用了“双感知”（语义优先级感知与带宽动态感知）的架构，包含以下关键技术：
*   **大小模型协作级联推理**：
    *   **边缘侧小模型门控**：使用轻量级模型（如 YOLOv12s）对视频帧进行初步筛选和目标检测。只有当检测到目标且置信度超过阈值时，才触发 MLLM 推理。
    *   **视觉引导与语义提示微调**：将小模型生成的检测框（Boxed image）和 ROI 区域作为 MLLM 的输入，并结合任务特定的 Prompt。通过 LoRA 策略对 MLLM 进行高效微调，使其稳定输出结构化的 JSON 结果（包含事件类别、严重程度、坐标等）。
*   **语义与带宽感知的多流自适应传输**：
    *   **词典序优化问题（Lexicographic Optimization）**：将传输目标建模为多目标优化。首要目标是最小化加权语义告警延迟（确保 JSON 告警最快到达），次要目标是最大化有效视觉证据（ROI 或检测框图像）的交付率。
    *   **在线分层贪婪调度算法**：根据实时带宽预算，优先调度高优先级事件的轻量级 JSON 数据；在带宽允许的残余空间内，再根据优先级补充视觉证据。

#### 3. 实验设计
*   **数据集**：
    *   **Accidents Detection Dataset**：主要用于交通事故检测与理解。
    *   **Accident Detection From CCTV Footage**：提供负样本用于训练正则化。
    *   **5G Traffic Datasets**：使用真实的 5G 带宽轨迹（Zoom1 轨迹）模拟动态网络环境。
*   **Benchmark 与对比方法**：
    *   **推理对比**：对比了原始 Qwen2.5-VL-7B、GPT-5（闭源基准）以及经过 LoRA 微调后的模型。
    *   **传输对比**：对比了固定框上传（Fixed Box）、固定 ROI 上传、仅带宽感知调度、仅优先级感知调度等 5 种基准策略。
*   **场景设置**：模拟了低负载、中等负载和突发流量（Burst）三种模式，并应用了不同的带宽缩放因子（1.0x, 0.5x, 0.25x）。

#### 4. 资源与算力
论文明确指出了实验所用的硬件资源：
*   **云端服务器**：NVIDIA A100 (40GB) GPU。
*   **边缘节点**：NVIDIA RTX 5090 (32GB) GPU。
*   **模型配置**：小模型为 YOLOv12s，大模型为 Qwen2.5-VL-7B-Instruct。
*   **训练细节**：使用 LoRA 进行微调，学习率 5e-5，序列长度 4096。

#### 5. 实验数量与充分性
实验设计较为充分且客观：
*   **多维度评估**：涵盖了语义理解准确度（Count EM, SevAcc）、结构化输出合规性（JSON 解析率）、传输性能（加权告警延迟、0.5s/1s 交付率）等。
*   **消融实验与参数分析**：对小模型门控阈值、视觉有效截止时间（$D_{vis}$）以及不同优先级来源（模型预测 vs. 真值）进行了深入分析。
*   **极端环境测试**：特别测试了在严重网络拥塞（0.25x 带宽）和突发流量下的表现，验证了系统的鲁棒性。

#### 6. 主要结论与发现
*   **高准确性**：DAT 在事故严重程度识别上达到了 98.83% 的准确率，在定位精度（mIoU）上显著优于未微调的大模型和 GPT-5。
*   **极低延迟**：在严重拥塞环境下，DAT 将加权语义告警延迟降低了 **77.5%**。
*   **高效证据交付**：在 0.5 秒内成功交付了 **98.33%** 的视觉证据，平均视觉延迟比其他自适应方法降低了 49.8%–75.4%。
*   **输出一致性**：通过微调，模型实现了 100% 的结构化 JSON 格式合规性。

#### 7. 优点
*   **联合优化思想**：打破了以往研究中“只管推理”或“只管传输”的局限，将 MLLM 的语义输出直接用于指导底层的传输调度。
*   **实用性强**：采用 LoRA 微调和级联架构，使得在边缘设备上部署 7B 规模的 MLLM 变得具有可行性。
*   **调度算法高效**：提出的分层贪婪调度算法复杂度低（$O(N \log N)$），非常适合资源受限的边缘节点实时运行。

#### 8. 不足与局限
*   **场景单一性**：实验主要集中在交通事故检测，虽然具有代表性，但在更复杂的通用场景（如多目标行为识别、长时序因果推理）下的泛化能力有待验证。
*   **多摄像头协同**：论文目前侧重于单边缘节点对单/多流的处理，未深入探讨大规模跨摄像头追踪与联合传输的复杂冲突。
*   **能耗考量**：虽然降低了计算频率，但未量化 RTX 5090 等高端显卡在边缘侧的实际功耗表现。

（完）
