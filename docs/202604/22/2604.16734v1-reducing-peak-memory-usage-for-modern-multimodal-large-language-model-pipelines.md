---
title: Reducing Peak Memory Usage for Modern Multimodal Large Language Model Pipelines
title_zh: 降低现代多模态大语言模型流水线的峰值显存占用
authors: "Junwan Kim, Hyunkyung Bae"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16734v1"
tags: ["query:lvu"]
score: 9.0
evidence: 减少多模态大模型处理长视频序列时的峰值显存
tldr: 针对多模态大语言模型在处理高分辨率图像和长视频时显存占用过高的问题，本文提出了一种顺序输入压缩机制。该方法利用视觉Token的结构规律性和冗余性，在预填充阶段即执行结构感知的KV缓存压缩，从而将显存控制在固定预算内。该研究显著降低了推理过程中的峰值显存，同时保持了优异的生成性能，为高效多模态推理提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16734v1/fig-001.webp\", \"caption\": \"Table 1: Performance under fixed KV-cache budgets. Best results are in bold. ∆ denotes the difference from the full-cache baseline. Our method maintains stable performance across compression settings, with minimal degradation even at a budget of 1024 (∼ 90% compress)\", \"page\": 4, \"index\": 1, \"width\": 447, \"height\": 333}]"
motivation: 现有压缩方法多在输入处理完成后才执行，导致预填充阶段峰值显存巨大，成为长序列推理的主要瓶颈。
method: 提出一种顺序输入压缩机制，在预填充过程中利用结构规律性对KV缓存进行实时压缩，以维持固定的内存预算。
result: 该方法在大幅减少峰值显存使用的同时，仅导致极小的模型生成性能损失。
conclusion: 通过在推理全过程中动态控制内存增长，该研究有效提升了多模态大模型在资源受限环境下的实用性。
---

## 摘要
多模态大语言模型 (MLLM) 近期在理解多样化视觉输入（包括高分辨率图像和长视频序列）并生成响应方面展现出了强大的能力。随着这些模型扩展到更丰富的视觉表示，推理过程越来越依赖于在键值 (KV) 缓存中存储大量的视觉 token，使得显存消耗成为核心瓶颈。现有方法通过识别视觉 token 中的冗余并压缩缓存来解决这一问题，但此类压缩通常仅在处理完所有输入后才进行，导致预填充 (prefill) 阶段的峰值显存占用过高。在本研究中，我们展示了 MLLM 表现出固有的结构规律性和表示冗余，可用于在整个推理过程中控制显存增长。基于这一见解，我们提出了一种顺序输入压缩机制，通过在预填充过程中执行结构感知的键值缓存压缩，来强制执行固定的显存预算。该方法在显著降低峰值显存占用的同时，仅以极小的性能损失维持了生成表现，从而实现了更具实用性且显存高效的多模态推理。

## Abstract
Multimodal large language models (MLLMs) have recently demonstrated strong capabilities in understanding and generating responses from diverse visual inputs, including high-resolution images and long video sequences. As these models scale to richer visual representations, inference increasingly relies on storing large numbers of vision tokens in the key-value (KV) cache, making memory consumption a central bottleneck. Existing methods address this issue by identifying redundancy in vision tokens and compressing the cache, but such compression is typically applied only after all inputs are processed, resulting in high peak memory usage during the prefill stage. In this work, we show that MLLMs exhibit inherent structural regularities and representational redundancy that can be exploited to control memory growth throughout inference. Based on this insight, we propose a sequential input-compression mechanism that enforces a fixed memory budget by performing structure-aware key-value cache compression during the prefill process. This approach substantially reduces peak memory usage while maintaining generative performance with only minimal degradation, enabling more practical and memory-efficient multimodal inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reducing Peak Memory Usage for Modern Multimodal Large Language Model Pipelines》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现代多模态大语言模型（MLLM）通过处理高分辨率图像和长视频序列来提升推理能力，但这导致视觉 Token 数量激增。
*   **核心问题**：现有的键值（KV）缓存压缩技术通常在**完整输入处理完成后**才进行压缩。这意味着在“预填充（Prefill）”阶段，系统必须先承载完整的、巨大的 KV 缓存，导致显存出现极高的峰值，经常引发显存溢出（OOM），限制了长序列的处理能力。
*   **整体含义**：本文旨在打破“先处理、后压缩”的传统模式，提出一种“边预填充、边压缩”的框架，在输入处理的全过程中维持固定的显存预算。

### 2. 论文提出的方法论
核心思想是**顺序结构感知（Structure-aware）的 KV 缓存压缩**。
*   **分块预填充（Block-wise Prefill）**：将输入序列（如图像切片或视频帧）划分为连续的块。模型按顺序处理每个块，每处理完一块就将其 KV 对添加到缓存中。
*   **实时驱逐机制（KV Eviction）**：一旦缓存超过预设预算 $M$，立即执行驱逐算法。
    *   **查询感知驱逐（Query-Aware）**：利用文本提示（Prompt）作为代理查询，计算缓存 Token 的重要性得分，保留最相关的视觉区域。
    *   **查询无关驱逐（Query-Agnostic）**：在多轮对话等缺乏明确查询的情况下，通过保留与平均表示差异最大的“离群”Token 来维持表示的多样性。
*   **结构对齐**：将分块边界与视觉输入（如图像的 Tile 或视频的帧组）的物理结构对齐，以保留语义完整性。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像领域**：使用 `ImageNeedleInHaystack`（测试长上下文检索）和 `V*`（测试密集视觉定位）。
    *   **视频领域**：使用 `MLVU` 和 `Video-MME`（长视频设置）。
    *   **通用任务**：使用 `MMMU` 和 `POPE` 验证通用多模态能力。
*   **Benchmark 与对比对象**：
    *   **基准模型**：InternVL3.5 (8B/14B) 和 Qwen2.5-VL (7B/32B)。
    *   **对比方法**：全缓存（Full Cache）基准，以及 SnapKV 和 KeyDiff 的变体。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A100 GPU** 上进行。
*   **算力细节**：论文未明确提及具体的训练时长（因为该方法主要针对推理阶段的优化，无需重新训练模型）。实验展示了在处理高达 12,800 个视觉 Token 时，该方法如何避免 OOM。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 不同参数规模的模型，验证了方法的通用性。
*   **消融实验**：
    *   对比了查询感知与查询无关策略的优劣。
    *   分析了不同分块大小（Block Size）对性能的影响。
    *   对比了“预填充压缩”与直接“降低输入分辨率”的效果。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入分析了显存-延迟权衡（Memory-Latency Trade-off），证明了在极端压缩（约 90%）下性能的稳定性，实验结果客观且具有说服力。

### 6. 论文的主要结论与发现
*   **显存控制**：该方法能使预填充阶段的峰值显存保持在近乎恒定的水平，成功解决了高分辨率输入导致的 OOM 问题。
*   **性能保持**：在压缩率高达 90%（预算仅 1024 Token）的情况下，模型在多数基准测试中的性能下降极小，甚至在某些视频任务中因减少了冗余信息而略有提升。
*   **结构重要性**：实验发现，分块大小若能与模型原生的视觉 Token 化结构（如 Qwen2.5-VL 的 28x28 网格）对齐，压缩效果最佳。

### 7. 优点
*   **解决痛点**：直接针对 MLLM 推理中最脆弱的“预填充峰值显存”问题，具有极强的工程实用价值。
*   **即插即用**：作为一种推理侧的优化方案，不需要对模型进行重新训练或微调。
*   **结构感知**：充分利用了视觉数据的空间和时间冗余特性，比纯文本的 KV 压缩更适合多模态场景。

### 8. 不足与局限
*   **推理延迟**：由于采用了顺序分块处理而非并行处理，首个 Token 生成时间（TTFT）会有所增加，存在显存换延迟的权衡。
*   **对齐依赖**：压缩效果高度依赖于分块边界与视觉结构的对齐，对于不同 tokenization 机制的模型可能需要手动调优。
*   **任务局限**：在极小预算下，对于需要全局极细微细节的任务（如超长序列中的微小目标定位）仍可能存在性能风险。

（完）
