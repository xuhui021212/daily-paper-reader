---
title: "RS-SSM: Refining Forgotten Specifics in State Space Model for Video Semantic Segmentation"
title_zh: RS-SSM：在状态空间模型中细化被遗忘的细节信息以用于视频语义分割
authors: "Kai Zhu, Zhenyu Cui, Zehua Zang, Jiahuan Zhou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24295v2"
tags: ["query:medvid"]
score: 9.0
evidence: 用于视频语义分割的状态空间模型
tldr: 针对视频语义分割中状态空间模型（SSM）因固定状态空间导致像素级细节信息丢失的问题，本文提出RS-SSM模型。该模型通过通道振幅感知器（CwAP）提取特征分布，并利用遗忘门信息精炼器（FGIR）反转并补偿压缩过程中丢失的时空细节。实验证明，RS-SSM在保持高效计算的同时，在四个主流VSS基准测试上达到了SOTA性能，显著提升了时空一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24295v2/fig-001.webp\", \"caption\": \"Figure 1. Existing SSM-based VSS methods lose spatiotemporal specifics when perform state space compression, limiting the model’s segmentation accuracy. In contrast, our proposed RSSSM method guides the model to focus on forgotten spatiotemporal specifics, thereby improving the pixel-level semantic segmentation performance.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2603.24295v2/fig-002.webp\", \"caption\": \"Table 1. The comparison with existing methods on the VSPW [37], NYUv2 [52] and CamVid [1] dataset. Our RS-SSM achieves state-ofthe-art performance while maintaining high efficiency. The best and second-best results are highlighted in RED and BLUE, respectively. We calculate GFLOPs and FPS with an input resolution of 480 × 853.\", \"page\": 6, \"index\": 2, \"width\": 940, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2603.24295v2/fig-003.webp\", \"caption\": \"Figure 2. The pipeline of our RS-SSM. Following previous work [22, 61], we use an image encoder to extract feature maps for each frame. After linear projection, we extract spectrum features through the CwAP module to quantify the channel distribution of specific information. Then our FGIR module adaptively inverts and refines the forgetting gate from the SSM θ2, thereby encouraging SSM θ1 to perform complementary refining of forgotten spatiotemporal specifics.\", \"page\": 3, \"index\": 3, \"width\": 1036, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2603.24295v2/fig-004.webp\", \"caption\": \"Figure 4. Visualization of segmentation results on VSPW dataset [37]. Compared to the existing SSM-based method TV3S [22], our RS-SSM produces more accurate and detailed segmentation results by effectively refining specific information in videos.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 499}, {\"url\": \"assets/figures/arxiv/2603.24295v2/fig-005.webp\", \"caption\": \"Figure 3. Visualization of the updating gate Bd as mentioned in Eq 2. Influenced by the forgetting gate, the updating gate reflects the amount of new information introduced at each time step. The bottom row visualization reveals that the vanilla SSM θ2 suffers from information loss of specifics during the state space compression. Conversely, as illustrated in the top row, the SSM θ1 effectively refines these forgotten specifics after inverting and refining the forgetting gate.\", \"page\": 5, \"index\": 5, \"width\": 1036, \"height\": 361}]"
motivation: 现有的SSM在压缩状态空间时会不可避免地遗忘特定的像素级细节，限制了其在视频语义分割中的建模能力。
method: 提出RS-SSM，包含用于提取特征分布的CwAP模块和通过反转遗忘门矩阵来补偿丢失信息的FGIR模块。
result: 在四个视频语义分割基准数据集上的实验结果表明，该方法在保持高计算效率的同时实现了最先进的性能。
conclusion: RS-SSM通过有效找回被遗忘的时空细节，增强了SSM在视频像素级分割任务中的表现。
---

## 摘要
最近，状态空间模型通过线性复杂度的状态空间压缩展示了高效的视频分割性能。然而，视频语义分割（VSS）需要像素级的时空建模能力，以保持语义对象分割的时间一致性。虽然状态空间模型在状态空间压缩过程中可以保留通用的语义信息，但固定大小的状态空间不可避免地会遗忘特定的细节信息，这限制了模型进行像素级分割的能力。为了解决上述问题，我们提出了一种用于视频语义分割的细化细节状态空间模型（RS-SSM）方法，该方法对遗忘的时空细节进行互补细化。具体而言，设计了一个通道级幅度感知器（CwAP），用于提取并对齐状态空间中细节信息的分布特征。此外，提出了一种遗忘门信息细化器（FGIR），根据细节信息分布自适应地反转并细化状态空间模型中的遗忘门矩阵。因此，我们的 RS-SSM 利用反转的遗忘门来互补地细化状态空间压缩过程中遗忘的细节信息，从而增强了模型的时空像素级分割能力。在四个 VSS 基准数据集上的广泛实验表明，我们的 RS-SSM 在保持高计算效率的同时，实现了最先进的性能。代码可在 https://github.com/zhoujiahuan1991/CVPR2026-RS-SSM 获取。

## Abstract
Recently, state space models have demonstrated efficient video segmentation through linear-complexity state space compression. However, Video Semantic Segmentation (VSS) requires pixel-level spatiotemporal modeling capabilities to maintain temporal consistency in segmentation of semantic objects. While state space models can preserve common semantic information during state space compression, the fixed-size state space inevitably forgets specific information, which limits the models' capability for pixel-level segmentation. To tackle the above issue, we proposed a Refining Specifics State Space Model approach (RS-SSM) for video semantic segmentation, which performs complementary refining of forgotten spatiotemporal specifics. Specifically, a Channel-wise Amplitude Perceptron (CwAP) is designed to extract and align the distribution characteristics of specific information in the state space. Besides, a Forgetting Gate Information Refiner (FGIR) is proposed to adaptively invert and refine the forgetting gate matrix in the state space model based on the specific information distribution. Consequently, our RS-SSM leverages the inverted forgetting gate to complementarily refine the specific information forgotten during state space compression, thereby enhancing the model's capability for spatiotemporal pixel-level segmentation. Extensive experiments on four VSS benchmarks demonstrate that our RS-SSM achieves state-of-the-art performance while maintaining high computational efficiency. The code is available at https://github.com/zhoujiahuan1991/CVPR2026-RS-SSM.

---

## 论文详细总结（自动生成）

以下是对论文《RS-SSM: Refining Forgotten Specifics in State Space Model for Video Semantic Segmentation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于状态空间模型（SSM，如 Mamba）的视频语义分割（VSS）方法在进行状态空间压缩时，虽然能保留通用的语义信息（如全局结构），但由于状态空间大小固定，不可避免地会**遗忘像素级的时空细节信息**（如边界、纹理、局部变化）。
*   **研究背景**：VSS 要求极高的时空一致性和像素级精度。传统的 CNN 或 Transformer 方法要么计算成本高（Transformer 的二次复杂度），要么难以处理长序列。SSM 虽具备线性复杂度和长程建模能力，但在处理精细分割任务时存在“细节丢失”的瓶颈。

### 2. 方法论
论文提出了 **RS-SSM（细化细节状态空间模型）**，核心思想是通过双路径 SSM 结构，利用其中一条路径专门补偿另一条路径遗忘的细节。
*   **通道级幅度感知器 (CwAP)**：
    *   利用 **2D 快速傅里叶变换 (FFT)** 将特征图转换到频域。
    *   通过频率半径划分频带，将高频分量定义为“细节信息”。
    *   计算每个通道的高频能量占比，生成**光谱特征 (Spectrum Features)**，用以量化各通道包含细节信息的程度。
*   **通道信息损失 ($L_{ci}$)**：
    *   引入余弦相似度损失，旨在对齐不同样本间细节信息的通道分布，促使模型将细节信息集中在特定的通道子集中，便于后续增强。
*   **遗忘门信息精炼器 (FGIR)**：
    *   **自适应反转**：根据 CwAP 提取的光谱特征，对主路径 SSM 的遗忘门矩阵进行反转。原本压缩严重的通道（细节丢失处）在反转后变为低压缩状态。
    *   **互补细化**：利用反转后的遗忘门指导第二条 SSM 路径，专门提取和恢复被遗忘的时空细节，最后将两条路径的特征融合。

### 3. 实验设计
*   **数据集**：在四个主流 VSS 基准上进行了验证：
    1.  **VSPW**：大规模野外视频场景解析数据集。
    2.  **NYUv2**：室内环境视频数据集。
    3.  **CamVid**：城市驾驶场景数据集。
    4.  **Cityscapes**：城市街道场景数据集。
*   **对比方法**：包括基于 Transformer 的 SOTA 方法（如 Segformer, CFFM++, MRCFA, Mask2Former, MPVSS）以及最新的基于 SSM 的方法（如 TV3S）。
*   **骨干网络**：采用了 MiT (B1, B2, B5) 和 Swin-Transformer (T, S) 作为 Encoder。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **软件框架**：基于 MMSegmentation 框架实现。
*   **训练细节**：使用 AdamW 优化器，初始学习率为 $6 \times 10^{-5}$，采用聚类学习率策略（poly policy）。输入尺寸根据数据集不同设定（如 480x480 或 512x1024）。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从轻量级（MiT-B1）到大规模（MiT-B5, Swin-S）不同参数量的对比实验。
*   **消融实验**：针对单路径 SSM、双路径普通 SSM、无 CwAP 引导的反转等多种变体进行了对比，验证了 CwAP 和 FGIR 模块的必要性。
*   **客观性**：实验不仅对比了精度（mIoU），还详细列出了计算量（GFLOPs）、参数量（Params）和推理速度（FPS），证明了该方法在性能与效率之间的平衡。

### 6. 主要结论与发现
*   **性能领先**：RS-SSM 在所有数据集上均达到了 SOTA。在 VSPW 上，使用 MiT-B5 骨干网络时，比之前的 SSM 领先者 TV3S 提升了 **1.8 mIoU**。
*   **效率优势**：在提升精度的同时，RS-SSM 保持了 SSM 的线性复杂度。相比 TV3S，在相同骨干网络下，RS-SSM 的 GFLOPs 更低且 FPS 更高（例如在 MiT-B5 下 FPS 从 14.0 提升至 20.2）。
*   **细节恢复**：可视化结果证实，RS-SSM 在物体边缘和细小结构的分割上明显优于现有方法。

### 7. 优点
*   **创新性地引入频域分析**：通过 FFT 量化 SSM 通道中的细节信息，为解决 SSM 的信息遗忘问题提供了新视角。
*   **轻量化设计**：CwAP 和 FGIR 模块设计精巧，通过共享线性投影权重，在不显著增加计算负担的前提下实现了性能飞跃。
*   **强通用性**：在室内、室外、驾驶等多种场景下均表现稳健，且兼容多种主流骨干网络。

### 8. 不足与局限
*   **超参数敏感性**：频域划分的带数 $K$ 和高频带选择 $k_h$ 等超参数可能需要针对不同分辨率的数据集进行微调。
*   **双路径开销**：虽然比 Transformer 快，但双路径 SSM 相比单路径 SSM 仍存在一定的显存占用增加。
*   **应用限制**：目前主要针对语义分割，对于需要极高实时性（如 >60 FPS）的超高分辨率视频处理，可能仍需进一步优化。

（完）
