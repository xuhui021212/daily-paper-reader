---
title: "AdaptToken: Entropy-based Adaptive Token Selection for MLLM Long Video Understanding"
title_zh: AdaptToken：基于熵的多模态大语言模型长视频理解自适应 Token 选择
authors: "Haozhe Qi, Kevin Qu, Mahdi Rad, Rui Wang, Alexander Mathis, Marc Pollefeys"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28696v1"
tags: ["query:lvu"]
score: 10.0
evidence: 多模态大模型长视频理解的自适应 Token 选择
tldr: 针对多模态大模型在长视频理解中面临的内存开销和上下文限制，本文提出AdaptToken框架。该方法无需训练，利用模型自身的响应熵作为全局控制信号，通过跨模态注意力对视频组内的Token进行排序，并根据熵值动态分配全局Token预算。此外，AdaptToken-Lite支持提前停止机制，在模型获得足够确定性时跳过后续处理。实验表明，该方法在多个长视频基准测试中显著提升了准确率，并大幅降低了推理时间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28696v1/fig-001.webp\", \"caption\": \"Fig. 4: Real-data entropy experiments on based on InternVL2.5 8B.\", \"page\": 6, \"index\": 1, \"width\": 596, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2603.28696v1/fig-002.webp\", \"caption\": \"Fig. 5: Visualization of AdaptToken token selection. Two frame groups are presented side by side. For each clip, we first estimate intra-group token relevance via cross-modal attention (heatmaps in the second row), and group-level relevance via response entropy, which measures the model’s answer confidence. Based on these signals, we perform global-aware token selection, adaptively allocating a larger token budget to groups that are more relevant to the text prompt (colored masks in the third row). The resulting token set is compact yet information-dense, improving both accuracy and inference efficiency for long-video understanding.\", \"page\": 7, \"index\": 2, \"width\": 725, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.28696v1/fig-003.webp\", \"caption\": \"Table 1: Comprehensive evaluation across long-video benchmarks. Results are grouped by model family. Best performance within each block is shown in bold. The first two blocks report representative closed-source and open-source MLLMs as reference points. Subsequent blocks list each base MLLM followed by the corresponding integrated methods.\", \"page\": 10, \"index\": 3, \"width\": 685, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2603.28696v1/fig-004.webp\", \"caption\": \"Fig. 2: Overall pipeline of AdaptToken. AdaptToken processes long videos by dividing them into frame groups and selecting informative tokens within each group based on group relevance estimated from response entropy. It progressively gathers evidence across groups and stops processing once sufficient information has been collected.\", \"page\": 3, \"index\": 4, \"width\": 725, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2603.28696v1/fig-005.webp\", \"caption\": \"Fig. 6: Entropy distributions across benchmarks. Qwen2.5-VL is used as the base MLLM.\", \"page\": 20, \"index\": 5, \"width\": 567, \"height\": 427}]"
motivation: 现有长视频理解方法缺乏跨远距离视频片段的关联性比较机制，且无法在获取足够证据后自动停止处理。
method: 提出一种基于熵的自适应Token选择框架，利用模型不确定性进行全局预算分配，并结合跨模态注意力进行Token排序。
result: 在VideoMME等四个基准测试中，该方法显著提升了Qwen2.5-VL等模型的准确率，且AdaptToken-Lite在保持性能的同时减少了约一半的推理时间。
conclusion: AdaptToken通过引入熵信号实现了高效且精准的长视频Token管理，为大模型处理超长视频输入提供了有效的无训练解决方案。
---

## 摘要
由于高昂的内存成本和上下文长度限制，长视频理解对于多模态大语言模型（MLLMs）而言仍然具有挑战性。先前的方法通过对短片段内的帧/Token 进行评分和选择来缓解这一问题，但它们缺乏一种原则性的机制来 (i) 比较远距离视频片段之间的相关性，以及 (ii) 在收集到足够证据后停止处理。我们提出了 AdaptToken，这是一个无需训练的框架，它将 MLLM 的自我不确定性转化为长视频 Token 选择的全局控制信号。AdaptToken 将视频划分为组，提取跨模态注意力以对每组内的 Token 进行排序，并利用模型的响应熵来估计每组与提示词的相关性。这种熵信号实现了跨组的全局 Token 预算分配，并进一步支持提前停止（AdaptToken-Lite），即当模型变得足够确定时跳过剩余组。在四个长视频基准测试（VideoMME、LongVideoBench、LVBench 和 MLVU）以及多个基础 MLLM（7B-72B）上，AdaptToken 持续提高了准确率（例如，在 Qwen2.5-VL 7B 上平均提高 +6.7），并能持续从极长输入（高达 10,000 帧）中获益，而 AdaptToken-Lite 在保持相当性能的同时将推理时间减少了约一半。项目页面：https://haozheqi.github.io/adapt-token

## Abstract
Long video understanding remains challenging for Multi-modal Large Language Models (MLLMs) due to high memory costs and context-length limits. Prior approaches mitigate this by scoring and selecting frames/tokens within short clips, but they lack a principled mechanism to (i) compare relevance across distant video clips and (ii) stop processing once sufficient evidence has been gathered. We propose AdaptToken, a training-free framework that turns an MLLM's self-uncertainty into a global control signal for long-video token selection. AdaptToken splits a video into groups, extracts cross-modal attention to rank tokens within each group, and uses the model's response entropy to estimate each group's prompt relevance. This entropy signal enables a global token budget allocation across groups and further supports early stopping (AdaptToken-Lite), skipping the remaining groups when the model becomes sufficiently certain. Across four long-video benchmarks (VideoMME, LongVideoBench, LVBench, and MLVU) and multiple base MLLMs (7B-72B), AdaptToken consistently improves accuracy (e.g., +6.7 on average over Qwen2.5-VL 7B) and continues to benefit from extremely long inputs (up to 10K frames), while AdaptToken-Lite reduces inference time by about half with comparable performance. Project page: https://haozheqi.github.io/adapt-token

---

## 论文详细总结（自动生成）

以下是对论文《AdaptToken: Entropy-based Adaptive Token Selection for MLLM Long Video Understanding》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大语言模型（MLLM）在处理长视频（如数小时的视频）时，面临严重的**内存开销**和**上下文长度限制**。
*   **研究动机**：现有的帧/Token选择方法通常只在短片段内部进行局部评分，存在两个主要缺陷：
    1.  **缺乏全局视角**：无法比较远距离视频片段之间的相对重要性，导致Token预算分配不均。
    2.  **缺乏停止机制**：即使模型已经获得了足够的信息来回答问题，仍会处理完所有采样帧，造成计算资源浪费。
*   **整体含义**：本文提出了一种无需训练的框架 **AdaptToken**，利用模型自身的“响应熵”（Response Entropy）作为全局信号，实现自适应的Token选择和推理提前停止。

### 2. 论文提出的方法论
AdaptToken 包含四个核心步骤，旨在将模型的不确定性转化为全局控制信号：
*   **基于响应熵的组相关性估计**：将长视频分为多个帧组（Groups）。模型对每个组进行初步推理，计算输出概率分布的熵。**低熵意味着模型更确定**，表明该组包含更多与提示词（Prompt）相关的证据。
*   **熵引导的全局 Token 选择**：
    1.  **组内评分**：利用 MLLM 内部层的跨模态注意力（Cross-modal Attention）对组内 Token 进行重要性排序。
    2.  **全局分配**：根据各组的熵值动态分配 Token 预算。相关性高（熵低）的组获得更多的 Token 配额。
*   **位置感知的全局 Token 去重**：计算 Token 间的特征相似度和时间距离，迭代移除冗余 Token，以确保选出的 Token 集合具有高多样性和广覆盖度。
*   **提前停止机制（AdaptToken-Lite）**：在按顺序处理视频组时，如果连续有多个组（如 3 个）的响应熵低于设定阈值（0.75），则认为已收集足够证据，跳过剩余视频组的计算。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **VideoMME**：涵盖短、中、长视频的综合评估。
    *   **MLVU**：侧重多任务长视频理解（3分钟到2小时）。
    *   **LongVideoBench**：强调长上下文中的交错视频语言理解。
    *   **LVBench**：针对极长视频（平均 1.1 小时）的基准测试。
*   **对比方法**：包括基础模型（InternVL2.5, Qwen2.5-VL, Qwen3-VL）以及多种最先进的选择方法（如 FlexSelect, AdaReTAKE, ZoomV, SeViCES 等）。
*   **评估指标**：准确率（Accuracy）和推理耗时（Inference Time）。

### 4. 资源与算力
*   **硬件设备**：主要评估在 **8 台 NVIDIA H100 GPU** 上完成，使用了 LMMS-Eval 框架。
*   **特定测试**：针对 Qwen3-VL 的显存测试在 **H200 GPU** 上进行。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不涉及训练时长，仅涉及推理阶段的计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流长视频基准上进行了测试。
    *   涵盖了从 7B 到 72B 不同参数规模的模型。
    *   进行了详尽的消融实验，包括组件有效性、Token 移除率、提前停止阈值、不同确定性度量指标（熵 vs 置信度 vs KL散度）的对比。
*   **充分性与公平性**：实验设计较为充分，通过在相同 Token 预算下对比多种 SOTA 方法，证明了其优越性。同时，针对 10,000 帧超长输入的测试展示了该方法的扩展性，这在同类研究中较为罕见。

### 6. 论文的主要结论与发现
*   **性能提升**：AdaptToken 在所有基准上均优于基础模型和现有的选择方法。例如，在 Qwen2.5-VL 7B 上平均准确率提升了 **+6.7**。
*   **效率优化**：AdaptToken-Lite 变体在保持几乎相同准确率的前提下，将平均推理时间缩短了约 **50%**。
*   **超长输入支持**：该方法能够有效处理高达 **10,000 帧** 的输入，且性能随帧数增加持续提升，证明了全局 Token 管理的有效性。
*   **模型通用性**：在 InternVL 和 Qwen 等不同架构的模型上均表现稳健。

### 7. 优点
*   **无需训练**：作为一种即插即用的插件，不需要昂贵的微调过程。
*   **全局感知**：通过响应熵解决了长视频处理中各片段相关性无法量化比较的难题。
*   **推理高效**：引入了原则性的提前停止机制，显著降低了处理长视频的冗余计算。
*   **可扩展性强**：能够处理极长视频，突破了传统 MLLM 的上下文瓶颈。

### 8. 不足与局限
*   **组划分策略**：目前的帧组划分和遍历顺序（最大边距顺序）虽然有效，但可能不是最优的，对于某些非均匀分布关键信息的视频，可能会影响提前停止的准确性。
*   **组内评分依赖**：Token 评分仍依赖于跨模态注意力，如果模型本身的注意力机制在某些复杂场景下失效，Token 选择的质量也会受限。
*   **首轮开销**：虽然提前停止减少了后续计算，但为了估计熵，仍需对部分视频组进行初步的完整前向传播。

（完）
