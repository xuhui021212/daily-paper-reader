---
title: Query-Conditioned Evidential Keyframe Sampling for MLLM-Based Long-Form Video Understanding
title_zh: 基于查询条件的证据性关键帧采样，用于基于多模态大语言模型的长视频理解
authors: "Yiheng Wang, Lichen Zhu, Yueqian Lin, Yudong Liu, Jingyang Zhang, Hai \"Helen\" Li, Yiran Chen"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01002v1"
tags: ["query:lvu"]
score: 10.0
evidence: 基于多模态大模型的长视频问答关键帧采样
tldr: 针对多模态大模型在长视频理解中面临的上下文限制和计算开销问题，本文提出了一种基于信息瓶颈理论的证据驱动关键帧采样框架。该方法将关键帧选择建模为最大化所选帧与查询之间的条件互信息，并将其分解为高效的单帧评分任务。通过引入对比学习训练的查询条件证据评分网络，该方法在严格的 Token 预算下显著提升了长视频问答的性能和训练效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01002v1/fig-001.webp\", \"caption\": \"Fig. 1: Semantic relevance vs. evidential usefulness in keyframe selection. Frames with the highest semantic similarity (blue) predicted by CLIP [12], describing poem content (left) or general match scenes (right), are not always informative for answering the query. In contrast, frames with the highest evidence scores (red) predicted by our method better capture answer-critical information. The green regions indicate the temporal segments containing the key evidence needed for answering.\", \"page\": 2, \"index\": 1, \"width\": 994, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2604.01002v1/fig-002.webp\", \"caption\": \"Table 1: Comparison with the state-of-the-art methods on LVBench [16]. ER, EU, KIR, TG, Rea, and Sum stand for entity recognition, event understanding, key information retrieval, temporal grounding, reasoning, and summarization subtasks. † denotes our reproduced results.\", \"page\": 8, \"index\": 2, \"width\": 700, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.01002v1/fig-003.webp\", \"caption\": \"Table 3: Ablation study on pretrained vision-language encoders. We evaluate the impact of different encoders within our sampling framework across MLLMs. Our approach consistently surpasses uniform sampling, while the choice of encoder has a moderate impact on the final performance.\", \"page\": 9, \"index\": 3, \"width\": 640, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.01002v1/fig-004.webp\", \"caption\": \"Table 2: Comparison with existing keyframe sampling methods on VideoMME [29]. † denotes our reproduced results.\", \"page\": 9, \"index\": 4, \"width\": 583, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2604.01002v1/fig-005.webp\", \"caption\": \"Fig. 2: Overview of the query-conditioned evidence scoring network. The input video is uniformly sampled into frames, which are encoded into frame embeddings and scored to obtain frame-level evidence scores conditioned on the query. Frames with the highest evidence scores are then selected and fed into an MLLM to generate the final response.\", \"page\": 5, \"index\": 5, \"width\": 1003, \"height\": 837}]"
motivation: 现有的长视频关键帧采样方法难以捕捉关键证据线索，且面临组合优化效率低下的挑战。
method: 基于信息瓶颈理论，将关键帧选择转化为最大化条件互信息，并利用其结构分解为独立的帧级评分任务。
result: 在长视频理解基准测试中，该方法在严格的 Token 限制下优于现有采样策略，并大幅提高了训练效率。
conclusion: 该研究为长视频理解提供了一种理论完备且高效的关键帧采样方案，有效平衡了模型性能与计算成本。
---

## 摘要
多模态大语言模型（MLLMs）在视频问答中展现出强大的性能，但其在长视频中的应用受到有限上下文长度和计算成本的限制，使得关键帧采样变得至关重要。现有方法通常依赖于语义相关性或强化学习，前者无法捕捉证据性线索，后者则面临低效的组合优化问题。在这项工作中，我们提出了一种基于信息瓶颈理论的证据驱动关键帧采样框架。我们将关键帧选择建模为最大化所选帧与查询之间的条件互信息，提供了一个能够反映每一帧对回答问题贡献的有原则的目标函数。为了使该目标易于处理，我们利用其结构推导出了分解优化方案，将子集选择简化为独立的帧级评分。我们还引入了一个采用对比学习目标训练的查询条件证据评分网络，以高效估计证据重要性。在长视频理解基准测试上的实验表明，在严格的 Token 预算下，我们的方法始终优于先前的采样策略，同时显著提高了训练效率。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong performance on video question answering, but their application to long-form videos is constrained by limited context length and computational cost, making keyframe sampling essential. Existing approaches typically rely on semantic relevance or reinforcement learning, which either fail to capture evidential clues or suffer from inefficient combinatorial optimization. In this work, we propose an evidence-driven keyframe sampling framework grounded in information bottleneck theory. We formulate keyframe selection as maximizing the conditional mutual information between selected frames and the query, providing a principled objective that reflects each frame's contribution to answering the question. To make this objective tractable, we exploit its structure to derive a decomposed optimization that reduces subset selection to independent frame-level scoring. We further introduce a query-conditioned evidence scoring network trained with a contrastive objective to estimate evidential importance efficiently. Experiments on long-form video understanding benchmarks show that our method consistently outperforms prior sampling strategies under strict token budgets, while significantly improving training efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Query-Conditioned Evidential Keyframe Sampling for MLLM-Based Long-Form Video Understanding》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在处理长视频时，受限于上下文窗口长度和高昂的计算成本，必须进行关键帧采样。
*   **研究动机**：
    *   **现有方法缺陷**：传统的采样方法要么依赖“语义相关性”（如 CLIP 评分），但相关帧不一定包含回答问题所需的“证据”；要么依赖“强化学习（RL）”，但搜索空间巨大且奖励稀疏，导致训练效率极低且不稳定。
    *   **核心挑战**：如何在极低的 Token 预算下，精准识别并提取对回答特定查询（Query）具有决定性作用的证据帧。

### 2. 方法论
*   **核心思想**：基于**信息瓶颈（Information Bottleneck）理论**，将关键帧选择建模为最大化所选帧集合 $S$ 与答案 $O$ 在给定查询 $Q$ 下的**条件互信息** $I(S; O | Q)$。
*   **关键技术细节**：
    *   **目标分解**：利用互信息目标的单调次模性（Submodularity），通过其模块化上界将复杂的组合优化问题简化为独立的**帧级证据评分**。
    *   **证据评分网络 ($g_\theta$)**：
        *   **时间证据聚合器**：使用因果窗口注意力机制捕捉局部时间上下文。
        *   **查询引导门控**：通过门控机制过滤掉与查询无关的视觉通道。
        *   **多子空间评分**：在多个语义子空间内计算视觉特征与查询特征的方向对齐度。
    *   **对比学习训练**：采用 **InfoNCE 损失函数**进行训练。将标注的证据片段内的帧视为正样本，片段外的帧为负样本。这种方式使采样网络与下游 MLLM 解耦，无需 MLLM 参与训练循环，极大提升了效率。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Seek-173K 中的 LLaVA-Video 子集（包含证据片段标注）。
    *   **测试基准**：**LVBench**（平均时长 4101 秒的超长视频基准）和 **Video-MME**（综合视频理解基准）。
*   **对比方法**：
    *   **免训练方法**：均匀采样、AKS、FOCUS、Q-Frame、BOLT 等。
    *   **可训练/RL 方法**：TSPO、MLLM-Selector、FrameThinker、ReTaKe 等。
*   **下游模型**：Qwen2-VL-7B、Qwen2.5-VL-7B、LLaVA-Video-7B。

### 4. 资源与算力
*   **训练算力**：使用 **8 台 NVIDIA RTX A5000 GPU**。
*   **训练时长**：在 264K 样本上仅需 **0.6 小时**（相比之下，RL 方法 TSPO 在 10K 样本上需 78 小时）。
*   **推理算力**：评估在 NVIDIA L40S GPU 上完成。
*   **参数量**：评分网络中可学习部分仅约 10M 参数（Vision Encoder 保持冻结）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流长视频基准，测试了三种不同的主流 MLLM 后端。
*   **消融实验**：
    *   测试了不同预训练编码器（CLIP, SigLIP, SigLIP2）的影响。
    *   测试了不同帧预算（8, 16, 32, 64 帧）下的性能表现。
    *   进行了定性分析，对比了采样成功与失败的案例。
*   **充分性评价**：实验设计较为充分，通过跨模型、跨数据集的对比验证了方法的泛化性；通过效率对比突出了该方法在工业应用中的潜力。

### 6. 主要结论与发现
*   **性能领先**：在相同帧预算下，该方法显著优于现有采样策略。在 LVBench 上，为 Qwen2.5-VL-7B 带来了 **10.1%** 的准确率提升。
*   **效率极高**：由于解耦了 MLLM 训练，训练速度比 RL 方法快两个数量级以上。
*   **证据覆盖**：分析表明，该方法能将证据帧的覆盖率从均匀采样的 33.6% 提升至 50.3%（32 帧预算下），有效减少了 MLLM 的幻觉现象。

### 7. 优点（亮点）
*   **理论完备**：从信息论角度出发，为关键帧采样提供了坚实的数学基础。
*   **训练高效**：通过对比学习规避了 RL 的信用分配难题和昂贵的 MLLM 推理开销。
*   **即插即用**：作为前端采样器，可以适配任何现有的 MLLM，无需对下游模型进行微调。

### 8. 不足与局限
*   **音频缺失**：目前仅基于视觉帧评分，无法处理依赖音频（如对话、背景音乐）的查询。
*   **时间戳定位弱**：未显式建模绝对时间戳，在需要精确时间区间定位的任务上表现一般。
*   **全局理解偏差**：在需要全局总结（Summarization）的任务中，由于采样倾向于聚集在局部证据点，其表现有时不如覆盖面更广的均匀采样。

（完）
