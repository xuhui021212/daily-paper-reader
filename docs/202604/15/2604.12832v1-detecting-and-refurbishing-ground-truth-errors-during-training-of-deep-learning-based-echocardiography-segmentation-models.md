---
title: Detecting and refurbishing ground truth errors during training of deep learning-based echocardiography segmentation models
title_zh: 基于深度学习的超声心动图分割模型训练过程中真值错误的检测与修复
authors: "Iman Islam, Bram Ruijsink, Andrew J. Reader, Andrew P. King"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12832v1"
tags: ["query:medvid"]
score: 9.0
evidence: 超声心动图分割与地面真值错误检测
tldr: 本研究针对超声心动图分割中人工标注存在的随机误差和系统偏差，探讨了深度学习模型的鲁棒性，并提出一种在训练过程中检测并修复错误标注的新策略。研究利用CAMUS数据集模拟了三种误差类型，对比了基于损失和梯度方差（VOG）的检测方法，并结合伪标签技术进行标注修复。结果表明，VOG能有效识别错误标注，且该方法在标注质量较差的高误差场景下显著提升了分割性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12832v1/fig-001.webp\", \"caption\": \"Fig. 2. Experiment 3 - Random GT label errors. Test set results when training with random GT label errors. Box plots display the Dice coefficients for each segmented structure and the overall mean for each model. The orange stars represent cases where the refurbished model performs better than the baseline model with a statistically significant difference by a Wilcoxon signed rank test (p-value<0.05).\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2604.12832v1/fig-002.webp\", \"caption\": \"Fig. 3. Experiment 3 - Systematic GT errors. Test set results when training with systematic GT label errors. Box plots display the Dice coefficients for each segmented structure and the overall mean for each model. The orange stars represent cases where the refurbished model performs better than the baseline model with a statistically significant difference by a Wilcoxon signed rank test (p-value<0.05).\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 450}]"
motivation: 针对超声心动图分割中人工标注易产生随机误差和系统偏差，从而影响深度学习模型准确性的问题。
method: 提出一种基于梯度方差（VOG）检测错误标注并利用伪标签进行在线修复的训练策略。
result: 实验发现VOG能高效识别错误标注，且该修复方法在标注错误率较高时能显著改善模型表现。
conclusion: 该研究证明了通过动态检测与修复标注错误，可以有效提升医疗影像分割模型在低质量标注数据上的训练效果。
---

## 摘要
基于深度学习的医学图像分割通常依赖于通过手动标注获取的真值（GT）标签，但这些标签容易出现随机错误或系统性偏差。本研究探讨了深度学习模型在超声心动图（echo）分割中对此类错误的鲁棒性，并评估了一种在模型训练期间检测和修复错误标签的新策略。利用 CAMUS 数据集，我们模拟了三种错误类型，随后将基于损失的 GT 标签错误检测方法与基于梯度方差（VOG）的方法进行了比较。我们还提出了一种伪标签方法来修复疑似错误的 GT 标签。我们评估了所提方法在不同错误水平下的性能。结果表明，VOG 在训练过程中标记错误 GT 标签方面非常有效。然而，标准的 U-Net 在随机标签错误和中等水平的系统性错误（最高达 50%）下仍保持了强大的性能。检测与修复方法提升了模型性能，尤其是在高错误率条件下。

## Abstract
Deep learning-based medical image segmentation typically relies on ground truth (GT) labels obtained through manual annotation, but these can be prone to random errors or systematic biases. This study examines the robustness of deep learning models to such errors in echocardiography (echo) segmentation and evaluates a novel strategy for detecting and refurbishing erroneous labels during model training. Using the CAMUS dataset, we simulate three error types, then compare a loss-based GT label error detection method with one based on Variance of Gradients (VOG). We also propose a pseudo-labelling approach to refurbish suspected erroneous GT labels. We assess the performance of our proposed approach under varying error levels. Results show that VOG proved highly effective in flagging erroneous GT labels during training. However, a standard U-Net maintained strong performance under random label errors and moderate levels of systematic errors (up to 50%). The detection and refurbishment approach improved performance, particularly under high-error conditions.

---

## 论文详细总结（自动生成）

这篇论文针对超声心动图分割任务中人工标注存在的质量问题，提出了一套在训练过程中自动检测并修复错误标注的框架。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习模型通常假设训练数据的真值（Ground Truth, GT）是完美的“金标准”，但在医学影像（尤其是复杂的超声心动图）中，人工标注常伴随随机误差（如疏忽）或系统性偏差（如标注风格不一）。
*   **研究动机**：目前关于标注错误的研究多集中在分类任务，分割任务（尤其是多类别分割）的研究较少。本文旨在探究 SOTA 分割模型对标注错误的鲁棒性，并开发一种无需重新标注即可在线优化训练数据的自动化方案。

### 2. 方法论
该论文提出了一套“检测+修复”的迭代流水线：
*   **错误检测（VOG 方法）**：采用**梯度方差（Variance of Gradients, VOG）**作为识别指标。其核心思想是：如果一个样本的标注是错误的，模型在训练过程中对该样本的梯度激活会表现出剧烈的波动。通过计算样本在连续多个 epoch 间的梯度方差，并利用四分位距（IQR）检测离群值，从而锁定疑似错误标注。
*   **标签修复（Refurbishment）**：
    *   **热身期**：前 10 个 epoch 正常训练，使模型具备初步判别能力。
    *   **伪标签替换**：每隔 5 个 epoch 进行一次修复。对于被 VOG 标记为错误的样本，将其原始 GT 替换为模型过去 5 个 epoch 预测结果的平均值（即伪标签）。
*   **算法流程**：训练 -> 记录梯度 -> 计算 VOG -> 识别离群样本 -> 更新伪标签 -> 继续训练。

### 3. 实验设计
*   **数据集**：使用 **CAMUS 公开数据集**，包含 500 组四腔心（A4C）舒张末期（ED）的超声图像，涵盖左心室（LV）、左心肌（LVM）和左心房（LA）三个类别。
*   **错误模拟**：人工合成了三种符合临床实际的错误：
    1.  **不完整标注**：部分结构缺失。
    2.  **边界扭曲**：标签的膨胀或侵蚀。
    3.  **标签合并**：不同结构类别混淆。
*   **实验场景**：分为**随机错误**（模拟标注者分心）和**系统性错误**（模拟特定标注偏见），错误比例设定为 12.5%、25% 和 50%。
*   **基准（Benchmark）**：标准的 **U-Net** 模型，以及基于“训练损失（Loss-based）”的传统错误检测方法。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用 Adam 优化器，学习率为 0.001，Batch Size 为 4，总计训练 100 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **实验 1**：对比 VOG 与 Loss-based 方法在三种错误类型下的检测准确率、灵敏度和特异性。
    *   **实验 2**：评估修复后的标签与原始正确标签的 Dice 系数提升情况。
    *   **实验 3**：在不同错误比例（12.5% - 50%）下对比 Baseline U-Net 与本文方法的最终分割性能。
*   **充分性评价**：实验设计较为全面，涵盖了从检测精度到最终模型性能的闭环验证，并使用了 Wilcoxon 符号秩检验进行统计显著性分析（p < 0.05），结果具有较强的说服力。

### 6. 主要结论与发现
*   **VOG 的优越性**：在识别错误标注方面，基于梯度方差（VOG）的方法在准确率和灵敏度上显著优于传统的基于损失（Loss）的方法。
*   **模型鲁棒性**：令人惊讶的是，标准的 U-Net 对**随机标注错误**具有极强的鲁棒性，即使有 50% 的错误，性能下降也有限。
*   **修复效果**：在**系统性错误**和**高比例错误**场景下，本文提出的修复策略能显著提升 Dice 分数，有效缓解了错误标注对模型性能的负面影响。

### 7. 优点
*   **轻量化与即插即用**：VOG 检测方法不需要修改模型架构，也不需要额外的辅助网络，计算开销较小。
*   **现实意义强**：模拟的错误类型（如边界侵蚀、结构合并）非常贴合超声心动图临床标注的实际痛点。
*   **多类别覆盖**：不同于以往只关注二值分割的研究，本文验证了多类别心脏结构分割下的有效性。

### 8. 不足与局限
*   **数据集规模较小**：仅使用了 500 例样本的 CAMUS 数据集，在更大规模或更多样化的临床数据上的泛化性有待验证。
*   **合成错误的局限**：虽然模拟了真实错误，但实际临床中的标注错误可能更加复杂且具有隐蔽性，合成错误可能无法完全覆盖真实分布。
*   **架构单一**：实验仅基于 U-Net 展开，未探讨 Transformer 或其他更先进架构在相同策略下的表现。

（完）
