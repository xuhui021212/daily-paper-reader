---
title: Efficient Spatial-Temporal Focal Adapter with SSM for Temporal Action Detection
title_zh: 基于 SSM 的高效时空焦点适配器用于时序动作检测
authors: "Yicheng Qiu, Keiji Yanai"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09164v1"
tags: ["query:lvu"]
score: 10.0
evidence: 使用 SSM 进行长程时间视频建模的高效架构
tldr: 本研究针对时序动作检测中CNN和Transformer处理长视频时存在的特征冗余及全局依赖建模不足的问题，提出了一种基于状态空间模型（SSM）的新型框架。核心贡献是引入了高效时空焦点适配器（ESTF Adapter），其中包含专门设计的时序边界感知SSM（TB-SSM），旨在实现高效的空间特征处理与强大的全局时序推理。实验证明该方法在多个基准测试中显著提升了动作定位的准确性与系统鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09164v1/fig-001.webp\", \"caption\": \"TABLE I: Comparison with previous methods on THUMOS14 and ActivityNet-1.3 Datasets.\", \"page\": 4, \"index\": 1, \"width\": 906, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.09164v1/fig-002.webp\", \"caption\": \"Fig. 2: Qualitative results of our proposed method and previous method on THUMOS14 Dataset for Video Action Detection task.\", \"page\": 5, \"index\": 2, \"width\": 492, \"height\": 551}, {\"url\": \"assets/figures/arxiv/2604.09164v1/fig-003.webp\", \"caption\": \"Fig. 1: The architecture of the proposed TAD framework. We integrate ESTF Adapters into the frozen pre-trained backbone layers to adapt representations for temporal action detection. For each adapter, features are processed by two branches: (1) The Spatial Branch that extracts localized structural cues using convolutions, and (2) The Temporal Branch that employs the TB-SSM to capture asymmetric state dynamics for precise boundary regression. Finally, fused features are fed into the detection head.\", \"page\": 2, \"index\": 3, \"width\": 994, \"height\": 420}]"
motivation: 传统架构在长视频时序动作检测中面临特征冗余和长程依赖建模困难的挑战。
method: 引入集成时序边界感知SSM（TB-SSM）的高效时空焦点适配器，优化预训练层的特征提取。
result: 在多个主流基准测试中，该方法在定位精度和鲁棒性方面均优于现有的SSM及其他结构模型。
conclusion: 证明了结合SSM的时空适配器能有效提升长视频理解性能，为高效视频分析提供了新思路。
---

## 摘要
时序人体动作检测旨在识别并定位未修剪视频中的动作片段，是视频理解中的一项关键任务。尽管 CNN 和 Transformer 等先前架构已取得进展，但在应用于长视频序列时，它们仍面临特征冗余和全局依赖建模能力下降的问题。这些局限性严重制约了它们在现实世界视频分析中的可扩展性。状态空间模型 (SSMs) 凭借线性长程建模和强大的全局时序推理能力，提供了一种极具前景的替代方案。通过重新思考 SSM 在时序建模中的应用，本研究构建了一种用于视频人体动作检测的新型框架。具体而言，我们在预训练层中引入了高效时空焦点 (ESTF) 适配器。该模块结合了我们提出的时序边界感知 SSM (TB-SSM) 在时序特征建模方面的优势，以及对空间特征的高效处理。我们在多个基准数据集上进行了全面且定量的分析，将所提方法与先前基于 SSM 的方法及其他结构化方法进行了对比。大量实验表明，我们的改进策略显著增强了定位性能和鲁棒性，验证了所提方法的有效性。

## Abstract
Temporal human action detection aims to identify and localize action segments within untrimmed videos, serving as a pivotal task in video understanding. Despite the progress achieved by prior architectures like CNN and Transformer models, these continue to struggle with feature redundancy and degraded global dependency modeling capabilities when applied to long video sequences. These limitations severely constrain their scalability in real-world video analysis. State Space Models (SSMs) offer a promising alternative with linear long-term modeling and robust global temporal reasoning capabilities. Rethinking the application of SSMs in temporal modeling, this research constructs a novel framework for video human action detection. Specifically, we introduce the Efficient Spatial-Temporal Focal (ESTF) Adapter into the pre-trained layers. This module integrates the advantages of our proposed Temporal Boundary-aware SSM(TB-SSM) for temporal feature modeling with efficient processing of spatial features. We perform comprehensive and quantitative analyses across multiple benchmarks, comparing our proposed method against previous SSM-based and other structural methods. Extensive experiments demonstrate that our improved strategy significantly enhances both localization performance and robustness, validating the effectiveness of our proposed method.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Spatial-Temporal Focal Adapter with SSM for Temporal Action Detection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**时序动作检测（Temporal Action Detection, TAD）**任务，即在未修剪的视频中识别动作的类别并精准定位其起止时间。
*   **研究动机**：传统的 CNN 架构受限于感受野，难以建模长程依赖；而 Transformer 架构虽然具备全局建模能力，但其二次方级计算复杂度（$O(N^2)$）在处理长视频序列时会产生严重的内存瓶颈和特征冗余。
*   **核心挑战**：如何在保持全局时序推理能力的同时，降低计算成本，并克服状态空间模型（SSM）在全局建模时容易出现的“过度平滑（Oversmoothing）”问题，从而保留对动作边界至关重要的细粒度特征。

### 2. 方法论
论文提出了一种基于 **ESTF（Efficient Spatial-Temporal Focal）适配器** 的新框架，核心思想是将 SSM 的线性效率与高效的空间特征处理相结合。
*   **核心架构**：将 ESTF 适配器插入冻结的预训练视频骨干网络（如 VideoMAE v2）中，通过参数高效微调（PEFT）实现下游任务适配。
*   **关键技术细节**：
    *   **空间分支（Spatial Branch）**：利用深度可分离卷积（DW-Conv 2d）提取局部结构线索，并通过下采样减少冗余。
    *   **时序分支（Temporal Branch）**：引入 **TB-SSM（时序边界感知 SSM）**。
    *   **TB-SSM 机制**：不同于传统的双向 SSM，TB-SSM 为前向和后向扫描分别参数化了独立的决策状态转移矩阵（$A_f$ 和 $A_b$）。这种**非对称状态动力学**设计旨在更好地捕捉动作开始和结束边界的内在差异。
    *   **时空融合**：通过解耦空间和时序建模，并在模块内部进行特征交互与融合，使模型既能捕捉长程依赖，又能保留细粒度的边界信息。

### 3. 实验设计
*   **数据集**：
    *   **THUMOS14**：包含 20 类动作，常用于评估精准的时序定位。
    *   **ActivityNet-1.3**：大规模基准，包含 200 类日常活动，视频较长。
    *   **Charades**：室内日常活动，具有频繁的动作共存和复杂的时序结构。
*   **Benchmark 与对比方法**：
    *   对比了基于 Transformer 的模型（如 ActionFormer, TadTR, AdaTAD）。
    *   对比了最新的基于 SSM 的模型（如 VideoMamba, ActionMamba, MambaTAD）。
    *   评估指标采用不同 tIoU 阈值下的平均精度均值（mAP）。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 GPU** 进行训练。
*   **软件环境**：CUDA 12.8，PyTorch 2.8。
*   **训练细节**：采用 AdamW 优化器，学习率 1e-4，Batch Size 为 8。视频采样长度固定为 768 帧，输入分辨率为 224x224。
*   **算力消耗**：文中提到其端到端显存占用约为 **28.6G**，低于同类端到端模型（如 MambaTAD 的 30.7G 和 AdaTAD++ 的 30.9G），体现了较好的显存效率。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了性能对比，并针对 THUMOS14 和 ActivityNet-1.3 进行了详细的消融实验。
*   **消融实验**：
    *   验证了空间分支、时序分支、时空融合机制以及 TB-SSM 各自的贡献。
    *   对比了不同时序建模策略（如 Transformer vs. 不同变体的 SSM）。
*   **充分性评价**：实验设计较为全面，涵盖了从短视频到长视频、从简单场景到复杂共存场景的评估。通过对比不同 tIoU 阈值下的表现，客观地证明了该方法在边界回归精度上的优势。

### 6. 主要结论与发现
*   **性能领先**：ESTF-SSM 在 THUMOS14 上的平均 mAP 达到 75.3%，在 ActivityNet-1.3 上达到 43.9%，均优于现有的 SSM 和 Transformer 基准模型。
*   **边界敏感性**：TB-SSM 的非对称建模显著提升了在高 tIoU 阈值下的表现，证明其对动作起止点的定位更加精准。
*   **效率优势**：在提升精度的同时，该方法保持了较低的内存占用和线性计算复杂度，更适合处理长视频。

### 7. 优点
*   **参数高效**：通过适配器（Adapter）模式利用预训练模型，避免了昂贵的从头训练成本。
*   **非对称建模创新**：针对动作边界的非对称性设计 TB-SSM，解决了传统 SSM 在时序定位中特征过度平滑的问题。
*   **时空解耦**：有效地平衡了局部空间细节和全局时序上下文的提取。

### 8. 不足与局限
*   **骨干网络依赖**：虽然适配器很轻量，但仍需依赖强大的预训练骨干网络（如 VideoMAE v2），这在一定程度上限制了其在极低功耗设备上的部署。
*   **在线检测缺失**：目前的研究主要针对离线（Offline）未修剪视频，尚未探讨其在实时在线（Online）动作检测任务中的表现。
*   **超参数敏感性**：文中未详细讨论适配器插入层数、降维比例（Rank）等超参数对不同类型视频的敏感程度。

（完）
