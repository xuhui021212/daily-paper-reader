---
title: Online Reasoning Video Object Segmentation
title_zh: 在线推理视频对象分割
authors: "Jinyuan Liu, Yang Wang, Zeyu Zhao, Weixin Li, Song Wang, Ruize Han"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11411v1"
tags: ["query:lvu"]
score: 9.0
evidence: 具有时间接地引用的在线推理视频对象分割
tldr: 本文针对现有推理视频目标分割（RVOS）方法依赖离线全局信息、不符合实时应用需求的问题，提出了在线推理视频目标分割（ORVOS）任务。该任务要求模型仅利用当前及过去帧进行增量推理，并处理指代转移。作者推出了包含因果标注和指代转移标签的基准测试集ORVOSB，并提出了一种基于持续更新提示和结构化时间令牌库的基线模型，为实时推理分割研究奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11411v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of ORVOSB data construction and annotation pipeline.\", \"page\": 6, \"index\": 1, \"width\": 650, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.11411v1/fig-002.webp\", \"caption\": \"Fig. 2. Illustrative examples of the five reasoning query types in ORVOSB.\", \"page\": 7, \"index\": 2, \"width\": 723, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.11411v1/fig-003.webp\", \"caption\": \"Fig. 4. Qualitative comparison of segmentation results on ORVOSB and ReVOS.\", \"page\": 14, \"index\": 3, \"width\": 721, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.11411v1/fig-004.webp\", \"caption\": \"Fig. 3. Overview of the proposed online reasoning video object segmentation framework. At each time step, the model processes context frames and the current frame together with a dynamic token reservoir. The multimodal large language model produces segmentation tokens, which are adaptively fused to form a history-aware prompt for mask prediction. The fused token features are written back to the token reservoir, enabling structured long-term reasoning under strict causal constraints.\", \"page\": 8, \"index\": 4, \"width\": 650, \"height\": 225}]"
motivation: 现有RVOS方法多采用离线模式，无法满足现实世界中需要严格因果关系和逐帧决策的在线部署需求。
method: 提出了ORVOSB基准测试集，并开发了一个结合持续更新分割提示与结构化时间令牌库的基线模型，以实现有限计算下的长时程推理。
result: 实验结果显示，现有方法在严格因果约束和指代转移场景下表现挣扎，而本文提出的基线模型建立了稳健的性能基础。
conclusion: 该研究通过定义在线推理分割新任务并提供配套基准与基线，为未来实时视频理解与推理研究指明了方向。
---

## 摘要
推理视频对象分割根据自然语言查询预测视频中的像素级掩码，这些查询可能涉及隐式和具有时间基础的引用。然而，现有方法是在离线模式下开发和评估的，即在推理时可以获取整个视频，并利用未来帧进行回顾性消歧，这偏离了需要严格因果、逐帧决策的真实世界部署场景。我们研究了在线推理视频对象分割（ORVOS），在这种模式下，模型必须仅利用过去和当前帧增量式地解释查询，且不能重新访问之前的预测，同时还要处理随事件展开而发生的指代偏移。为了支持评估，我们引入了 ORVOSB，这是一个具有帧级因果标注和指代偏移标签的基准测试，包含 210 个视频、12,907 个标注帧以及跨越五个推理类别的 512 个查询。我们进一步提出了一个基准模型，该模型具有持续更新的分割提示和结构化时间令牌库，用于在有限计算资源下进行长时程推理。实验表明，现有方法在严格的因果关系和指代偏移下表现不佳，而我们的基准模型为未来的研究奠定了坚实的基础。

## Abstract
Reasoning video object segmentation predicts pixel-level masks in videos from natural-language queries that may involve implicit and temporally grounded references. However, existing methods are developed and evaluated in an offline regime, where the entire video is available at inference time and future frames can be exploited for retrospective disambiguation, deviating from real-world deployments that require strictly causal, frame-by-frame decisions. We study Online Reasoning Video Object Segmentation (ORVOS), where models must incrementally interpret queries using only past and current frames without revisiting previous predictions, while handling referent shifts as events unfold. To support evaluation, we introduce ORVOSB, a benchmark with frame-level causal annotations and referent-shift labels, comprising 210 videos, 12,907 annotated frames, and 512 queries across five reasoning categories. We further propose a baseline with continually-updated segmentation prompts and a structured temporal token reservoir for long-horizon reasoning under bounded computation. Experiments show that existing methods struggle under strict causality and referent shifts, while our baseline establishes a strong foundation for future research.

---

## 论文详细总结（自动生成）

这篇论文针对推理视频对象分割（Reasoning Video Object Segmentation, RVOS）在实际应用中的局限性，提出了**在线推理视频对象分割（ORVOS）**这一新任务，并提供了相应的基准测试和基线模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 RVOS 方法大多处于“离线模式”，即模型在推理时可以访问整个视频。这导致了两个主要问题：
    1.  **回顾性消歧（Retrospective Disambiguation）**：模型可能利用未来帧的信息来修正对过去帧的判断（例如，根据视频中段车辆开始移动，倒推视频开头静止的车辆为“移动的车辆”），这在实时场景中是不可能的。
    2.  **应用受限**：离线模式无法直接部署于机器人、具身智能或实时视频监控等需要逐帧、因果决策的场景。
*   **研究动机**：提出一种符合**严格因果律（Strict Causality）**且能处理**指代转移（Referent Shifts，即查询目标随事件发展而改变）**的在线推理框架。

### 2. 方法论：核心思想与关键技术
论文提出了一个结合多模态大模型（MLLM）与动态存储机制的在线框架：
*   **持续更新的分割提示（CUSP）**：
    *   模型在每个时间步 $t$ 接收当前帧、短滑动窗口内的上下文帧以及历史记忆令牌。
    *   **亲和力引导的自适应融合（Affinity-guided Adaptive Fusion）**：计算当前目标令牌与历史分割令牌之间的余弦相似度，通过加权融合将历史证据注入当前提示，使分割提示随时间演进。
*   **结构化时间令牌库（TR）**：
    *   **稠密到稀疏的保留策略（Dense-to-sparse Retention）**：为了在有限计算预算下处理长视频，算法对近期帧保留稠密特征，对远期帧进行非线性采样（越远越稀疏）。
    *   **基于查询的聚合（Query-based Aggregation）**：使用轻量级 Transformer 编码器，通过可学习的查询令牌对历史特征进行压缩和提取。
*   **提示引导的掩码解码**：将融合后的提示特征输入分割头（如 SAM-2 解码器），结合当前帧视觉特征生成掩码。

### 3. 实验设计
*   **新基准测试集 ORVOSB**：
    *   包含 210 个视频、12,907 个标注帧、512 个推理查询。
    *   **五大推理类别**：属性、空间关系、动作/状态变化、交互、外部知识。
    *   **特点**：标注了严格的因果边界和指代转移标签（平均每个查询包含 3.66 次目标切换）。
*   **对比方法**：
    *   **视频模型**：VISA, VRS-HQ, GLUS, VideoLISA。
    *   **图像模型**：LISA, READ。
*   **评估指标**：区域相似度（$\mathcal{J}$）和轮廓准确度（$\mathcal{F}$）的平均值 $\mathcal{J\&F}$。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：
    *   基于 Chat-UniVi-7B-v1.5 模型，使用 LoRA 插件进行微调。
    *   训练迭代次数为 9,000 次，每台 GPU 的 Batch Size 为 1，配合 16 步梯度累积（等效 Batch Size 为 128）。
    *   使用了 DeepSpeed 优化库。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 **ORVOSB**（在线）和 **ReVOS**（离线通用）两个数据集上进行了全面对比。
    *   进行了详细的**消融实验**，验证了 CUSP 模块、自适应融合、令牌库（TR）以及采样策略（稠密到稀疏）的有效性。
*   **充分性与客观性**：实验涵盖了从图像级到视频级的多种 SOTA 方法，并针对在线场景特有的“指代转移”进行了定性分析。实验设计考虑了不同推理类型，结果显示该方法在所有类别上均有显著提升，具有较强的说服力。

### 6. 主要结论与发现
*   **现有方法的局限**：传统的 RVOS 方法在面对严格因果约束和目标切换时性能大幅下降（如 VISA 仅 33.3% $\mathcal{J\&F}$），因为它们依赖全局信息或固定的目标跟踪。
*   **在线推理的优势**：本文提出的方法在 ORVOSB 上达到了 **61.3% $\mathcal{J\&F}$**，比最强的对比方法（READ）高出 9.8%。
*   **泛化能力**：即使在传统的离线基准 ReVOS 上，该在线模型依然保持了极具竞争力的性能（54.3% $\mathcal{J\&F}$），证明了其时序建模的鲁棒性。

### 7. 优点
*   **任务定义前瞻性**：首次明确定义了在线推理分割任务，更贴近机器人等实际工业应用。
*   **内存效率**：通过“稠密到稀疏”的存储机制，解决了长视频推理中内存爆炸的问题。
*   **基准质量高**：ORVOSB 填补了具备“指代转移”标注的视频推理数据集空白。

### 8. 不足与局限
*   **计算延迟**：虽然是“在线”处理，但基于 7B 参数量的 MLLM 逐帧推理，在嵌入式设备上的实时性（FPS）可能仍面临挑战（文中未详细给出推理耗时）。
*   **指代转移的复杂性**：对于极短时间内频繁切换的目标，模型可能仍存在一定的滞后性。
*   **应用限制**：目前主要针对单目标指代，对于涉及多个复杂目标的并行推理能力尚待进一步验证。

（完）
