---
title: "One Token per Highly Selective Frame: Towards Extreme Compression for Long Video Understanding"
authors: "Zheyu Zhang, Ziqi Pang, Shixing Chen, Xiang Hao, Vimal Bhat, Yu-Xiong Wang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14149v1"
tags: ["query:lvu"]
score: 10.0
evidence: 针对长视频理解的极端视频标记压缩
tldr: 本文针对长视频理解中视觉标记过多导致LLM上下文受限的问题，提出X-Comp模型以实现“每帧一个标记”的极致压缩。该模型结合了可学习的逐层标记压缩（LP-Comp）和基于问题引导的帧级筛选（QC-Comp），通过局部注意力机制缓解位置偏差，使模型能处理多出2-4倍的视频帧。实验表明，X-Comp在仅需极少微调数据的情况下，显著提升了长视频基准测试的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14149v1/fig-001.webp\", \"caption\": \"Figure 4: Question-Conditioned Compression. To reduce the visual redundancy and improve long video understanding performance, we split a video into individual segments and assign question-conditioned relevance scores to video frames. The frames with lower relevance scores are discarded so that the LLM only concentrates on the informative frames.\", \"page\": 6, \"index\": 1, \"width\": 317, \"height\": 588}, {\"url\": \"assets/figures/arxiv/2604.14149v1/fig-002.webp\", \"caption\": \"Figure 1: Left: We present XComp that explores using the LLM layers to progressively compress the video tokens towards the extreme of one token per frame. Right: Such capabilities enable the model to better improve itself with denser input frames without significantly increasing video tokens.\", \"page\": 2, \"index\": 2, \"width\": 771, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2604.14149v1/fig-003.webp\", \"caption\": \"Table 1: Long video understanding comparison. Our method XComp built upon VideoChatFlash-2B achieves state-of-the-art results among 2B-scale models, demonstrating the effectiveness of extreme compression in long video understanding. Note that the performance of proprietary models and 3∼9B-scale VLMs are provided for reference.\", \"page\": 7, \"index\": 3, \"width\": 776, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.14149v1/fig-004.webp\", \"caption\": \"Figure 5: LongVideoBench case analysis. XComp leverages QC-Comp to divide a long video into short segments, mitigating attention bias. Local attention highlights key frames relevant to the question and correct answer, enabling effective filtering of irrelevant content.\", \"page\": 8, \"index\": 4, \"width\": 816, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.14149v1/fig-005.webp\", \"caption\": \"Figure 2: Overview. Our XComp comprises of two parts to achieve extreme compression in long video understanding. (1) At the token level, we propose the supervised compression tuning that enables the LLM to compress every video frame into one compact token in a learnable and progressive manner, namely, LP-Comp (Sec. 3.2). (2) At the frame level, we utilize the internal attention mechanism to select the frames relevant to the questions, which is question-conditioned compression, namely, QC-Comp (Sec. 3.3). Both aspects combined lead to our objective of extreme compression in long video understanding: one token per highly selective frame.\", \"page\": 4, \"index\": 5, \"width\": 814, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.14149v1/fig-006.webp\", \"caption\": \"Figure 3: Learnable and Progressive Compression (LP-Comp). With supervised compression tuning, the LLM layers learn to condense the visual tokens progressively into a concise set of tokens until reaching the extreme of one token per frame.\", \"page\": 5, \"index\": 6, \"width\": 765, \"height\": 339}]"
motivation: 现有模型因每帧生成的标记过多，受限于LLM上下文长度而不得不稀疏采样视频，导致关键时序信息丢失。
method: 提出可学习的逐层标记压缩（LP-Comp）与基于问题引导的帧级筛选（QC-Comp），并利用局部注意力优化长序列处理。
result: "在LVBench上将准确率从42.9%提升至46.2%，且仅需2.5%的监督微调数据即可实现性能飞跃。"
conclusion: 通过标记级和帧级的双重极致压缩，X-Comp在保持极高计算效率的同时，大幅增强了模型对长视频的理解能力。
---

## Abstract
Long video understanding is inherently challenging for vision-language models (VLMs) because of the extensive number of frames. With each video frame typically expanding into tens or hundreds of tokens, the limited context length of large language models (LLMs) forces the VLMs to perceive the frames sparsely and lose temporal information. To address this, we explore extreme video token compression towards \emph{one token per frame} at the final LLM layer. Our key insight is that heuristic-based compression, widely adopted by previous methods, is prone to information loss, and this necessitates supervising LLM layers into \emph{learnable} and \emph{progressive} modules for \emph{token-level compression} (LP-Comp). Such compression enables our VLM to digest 2x-4x more frames with improved performance. To further increase the token efficiency, we investigate \emph{frame-level compression}, which selects the frames most relevant to the queries via the internal attention scores of the LLM layers, named \emph{question-conditioned compression} (QC-Comp). As a notable distinction from previous studies, we mitigate the position bias of LLM attention in long contexts, \emph{i.e.}, the over-concentration on the beginning and end of a sequence, by splitting long videos into short segments and employing local attention. Collectively, our combined \emph{token-level} and \emph{frame-level} leads to an e\textbf{x}treme compression model for long video understanding, named \textbf{\name}, achieving a significantly larger compression ratio and enabling denser frame sampling. Our \name is finetuned from VideoChat-Flash with a data-efficient \emph{supervised compression tuning} stage that only requires 2.5\% of the supervised fine-tuning data, yet boosts the accuracy from 42.9\% to 46.2\% on LVBench and enhances multiple other long video benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《One Token per Highly Selective Frame: Towards Extreme Compression for Long Video Understanding》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：长视频理解面临的主要瓶颈是视觉标记（Tokens）数量巨大。现有的视觉语言模型（VLMs）在处理每一帧时通常会产生数十甚至数百个标记，这迅速填满了大语言模型（LLM）的上下文窗口，迫使模型不得不进行稀疏采样，从而丢失了关键的时序信息和视觉细节。
*   **核心问题**：如何在不损失关键信息的前提下，实现极高比例的视频标记压缩，从而允许模型处理更长、更密集的视频帧？

### 2. 论文提出的方法论：XComp
论文提出了 **XComp** 框架，从标记级和帧级两个维度进行极致压缩：
*   **标记级压缩：可学习且渐进式压缩 (LP-Comp)**
    *   **核心思想**：放弃传统的启发式（如池化或固定采样）方法，通过“监督压缩微调（SCT）”让 LLM 层学会将每帧信息浓缩。
    *   **技术细节**：采用**余弦调度（Cosine Schedule）**在 LLM 的 L 个层之间平滑地减少标记数量，最终在最后一层实现“每帧一个标记”的极致目标。
    *   **后缀保留 (Suffix-Preservation)**：由于 Decoder-only LLM 的因果注意力特性，模型被设计为保留每帧的最后一个标记，使其能够吸收前面标记的信息。
*   **帧级压缩：问题引导压缩 (QC-Comp)**
    *   **核心思想**：根据用户的问题，动态筛选最相关的视频帧。
    *   **分段局部注意力 (Segmented Local Attention)**：为了克服长文本中 LLM 常见的“位置偏差”（容易忽略中间信息），将长视频切分为短片段，在片段内计算问题与视频帧的注意力分数，从而更公平地评估每一帧的相关性。

### 3. 实验设计
*   **数据集/场景**：
    *   **长视频基准**：LongVideoBench, MLVU, VideoMME (Long), LVBench。这些视频时长从几分钟到一小时不等。
    *   **其他任务**：CLEVRER（因果推理）、VDC（视频描述）、MME-VideoOCR（文字识别）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：以 VideoChat-Flash-2B 为主要 Baseline。
    *   **对比对象**：包括闭源模型（GPT-4V, GPT-4o, Gemini-1.5-Pro）以及开源模型（InternVL3, Qwen2.5VL, mPLUG-Owl3, Eagle2.5 等 2B 至 9B 规模的模型）。

### 4. 资源与算力
*   **训练算力**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：整个微调过程大约耗时 **24 小时**。
*   **推理效率**：在单张 NVIDIA H200 上测试，相比 Baseline，XComp 在处理 1k-4k 帧时，LLM 计算量（TFLOPs）减少了约 **53%-58%**，延迟降低了 **45%-58%**。
*   **数据效率**：仅使用了 VideoChat-Flash 原始 SFT 数据的 **2.5%**（约 7.2 万条数据）进行微调。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流长视频榜单上进行了全面测试，并针对 LLaVA-Next-Video 进行了泛化性验证。
*   **消融实验**：非常充分。作者对比了：
    1.  可学习压缩 vs. 训练前压缩（验证了 SCT 的必要性）。
    2.  渐进式压缩 vs. 步进式压缩（验证了平滑调度的优越性）。
    3.  后缀保留 vs. 均匀丢弃（验证了与因果注意力匹配的重要性）。
    4.  局部注意力 vs. 全局注意力（验证了缓解位置偏差的效果）。
*   **客观性**：通过在相同参数规模（2B）下对比多个 SOTA 模型，证明了其在极致压缩下的领先地位。

### 6. 主要结论与发现
*   **极致压缩可行性**：证明了将每帧压缩至 1 个标记是可行的，且通过这种方式，模型可以处理比以往多 2-4 倍的帧数。
*   **性能随帧数增长**：与许多模型在帧数增加后性能下降不同，XComp 展现出随着输入帧数增加，准确率持续提升的趋势。
*   **位置偏差缓解**：通过分段局部注意力计算相关性分数，能显著提高长视频中关键帧的召回率。

### 7. 优点（亮点）
*   **极高的数据效率**：仅需极少量的微调数据即可实现显著的性能提升。
*   **架构兼容性强**：LP-Comp 和 QC-Comp 思想可以轻松迁移到其他基于 Transformer 的 VLM 架构中（如 LLaVA-Next）。
*   **推理成本大幅降低**：在保持甚至提升精度的同时，显著减少了显存占用和推理延迟。

### 8. 不足与局限
*   **细粒度感知损失**：在 MME-VideoOCR 等需要极高空间分辨率的任务上，由于极致的标记压缩，性能相比原始模型有轻微下降。
*   **任务类型局限**：目前主要在多选题（Multiple-choice）形式的基准上表现优异，对于需要长篇幅生成的复杂叙事任务探讨较少。
*   **联合优化空间**：目前的帧筛选（QC-Comp）是在推理阶段进行的，尚未在训练阶段与 LLM 层进行端到端的联合优化。

（完）
