---
title: "LiquidTAD: Efficient Temporal Action Detection via Parallel Liquid-Inspired Temporal Relaxation"
title_zh: LiquidTAD：通过并行液体启发式时间松弛实现高效的时序动作检测
authors: "Zepeng Sun, Naichuan Zheng, Hailun Xia, Junjie Wu, Liwei Bao, Xiaotai Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18274v2"
tags: ["query:lvu"]
score: 10.0
evidence: 长未修剪视频序列中的高效时序动作检测
tldr: 针对时序动作检测（TAD）中现有模型参数量大、计算开销高且难以跨平台部署的问题，本文提出LiquidTAD框架。该方法借鉴液体神经网络（LNN）的指数松弛先验，将其转化为一种并行的时序算子，通过向量化非递归公式避免了复杂的ODE求解。结合层级衰减率共享策略，LiquidTAD在保持高精度的同时显著降低了模型复杂度，为高效、硬件无关的动作检测提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18274v2/fig-001.webp\", \"caption\": \"Fig. 1: Performance versus parameter complexity on THUMOS-14. The horizontal axis shows FLOPs, the vertical axis shows average mAP, and the size of each point represents the model parameter count. LiquidTAD achieves competitive mAP with a substantially smaller detection head, highlighting its efficiency-accuracy trade-off.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.18274v2/fig-002.webp\", \"caption\": \"TABLE I: State-of-the-art comparison on THUMOS-14 and ActivityNet-1.3. Performance is measured by mAP at different IoU thresholds and average mAP. † denotes methods built upon a stronger detection framework.\", \"page\": 6, \"index\": 2, \"width\": 1014, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.18274v2/fig-003.webp\", \"caption\": \"Fig. 2: The overall architecture of LiquidTAD. The input video features are processed through a feature pyramid equipped with Liquid Temporal Stacks, utilizing Parallel Liquid-inspired Relaxation and a Hierarchical Decay-Rate Sharing Strategy to efficiently capture multi-scale temporal dynamics without relying on sequential ODE solvers.\", \"page\": 4, \"index\": 3, \"width\": 968, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.18274v2/fig-004.webp\", \"caption\": \"Fig. 3: Qualitative visualization of action detection results. We compare the predicted action boundaries of LiquidTAD (LTAD) against the strong baseline ActionFormer [11] and the Ground Truth (Truth). Across various action types (e.g., JavelinThrow, HammerThrow, BaseballPitch), LiquidTAD produces more coherent temporal boundaries and reduces fragmented predictions.\", \"page\": 5, \"index\": 4, \"width\": 529, \"height\": 423}]"
motivation: 旨在解决现有TAD模型因参数冗余、计算量大及依赖特殊算子而导致的部署困难问题。
method: 提出并行液体启发式松弛机制，通过标准神经操作实现线性复杂度的时序建模，并引入层级衰减率共享策略优化特征金字塔。
result: "在THUMOS-14上达到69.46%的平均mAP，参数量仅为10.82M，比ActionFormer减少了60%以上。"
conclusion: LiquidTAD在保证检测精度的前提下，大幅提升了模型的轻量化程度和硬件适配性。
---

## 摘要
时序动作检测（TAD）要求在长的、未剪辑的视频序列中精确地定位动作边界。虽然当前的高性能方法取得了很强的准确率，但它们通常具有过多的参数量、巨大的计算开销，并且依赖于阻碍在不同硬件平台上部署的专用算子。本文提出了 LiquidTAD，这是一个将液体神经动力学（Liquid Neural Dynamics）的指数松弛先验提取为并行时间算子的框架，而不是复制完整的液体神经网络（LNN）动力学。通过引入一种并行液体启发式松弛（Parallel Liquid-inspired Relaxation）机制，通过完全向量化、非递归的公式（完全基于标准神经操作构建）避免了顺序 ODE 求解，从而实现了在时间长度上具有线性复杂度的硬件无关部署。一种互补的分层衰减率共享策略（Hierarchical Decay-Rate Sharing Strategy）进一步在特征金字塔层级间调整这种松弛先验，稳定了优化并隐式补偿了深层的时间压缩。在 THUMOS-14 和 ActivityNet-1.3 上的实验评估表明，LiquidTAD 在大幅降低模型占用的同时，实现了与强基准模型相当的准确率。具体而言，在 THUMOS-14 上，LiquidTAD 仅用 10.82M 参数和 27.17G FLOPs 就实现了 69.46% 的平均 mAP，与 ActionFormer 相比，参数量减少了 60% 以上。

## Abstract
Temporal Action Detection (TAD) requires precise localization of action boundaries within long, untrimmed video sequences. While current high-performing methods achieve strong accuracy, they are often characterized by excessive parameter counts, substantial computational overhead, and a reliance on specialized operators that hinder deployment across diverse hardware platforms. This paper presents LiquidTAD, a framework that distills the exponential relaxation prior of liquid neural dynamics into a parallel temporal operator, rather than reproducing full Liquid Neural Network (LNN) dynamics. By introducing a Parallel Liquid-inspired Relaxation mechanism, sequential ODE solving is avoided through a fully vectorized, non-recursive formulation built entirely upon standard neural operations, enabling hardware-agnostic deployment with linear complexity with respect to the temporal length. A complementary Hierarchical Decay-Rate Sharing Strategy further adapts this relaxation prior across feature pyramid levels, stabilizing optimization and implicitly compensating for temporal compression in deeper layers. Experimental evaluations on THUMOS-14 and ActivityNet-1.3 demonstrate that LiquidTAD achieves accuracy competitive with strong baselines while substantially lowering the model footprint. Specifically, on THUMOS-14, LiquidTAD achieves 69.46\% average mAP with only 10.82M parameters and 27.17G FLOPs, reducing the parameter count by over 60\% compared with ActionFormer.

---

## 论文详细总结（自动生成）

### LiquidTAD：通过并行液体启发式时间松弛实现高效时序动作检测

#### 1. 核心问题与整体含义（研究动机和背景）
时序动作检测（TAD）旨在长视频中精确定位动作的起始与结束边界。当前主流的高性能模型（如基于 Transformer 或 Mamba 的架构）面临三大挑战：
*   **计算开销巨大**：参数量多且 FLOPs 高，难以在资源受限设备上运行。
*   **硬件部署受限**：高度依赖专用算子（如自注意力机制或 Mamba 的自定义 CUDA 内核），在通用 CPU 或非特定加速器上性能骤降。
*   **时序建模缺乏物理先验**：传统模型多通过离散 Token 聚合上下文，缺乏对时序持久性和衰减模式（物理演化过程）的显式建模。

本文提出 **LiquidTAD**，旨在将液体神经网络（LNN）的指数松弛先验提取为一种**并行化、硬件无关**的时间算子，以实现精度与效率的最优平衡。

#### 2. 方法论：核心思想与关键技术
LiquidTAD 的核心是将 LNN 的连续时间动力学简化为可并行计算的标准神经操作：
*   **并行液体启发式松弛（Parallel Liquid-inspired Relaxation）**：
    *   **原理**：将传统的顺序 ODE 求解过程转化为非递归的向量化更新。
    *   **公式化**：定义衰减率 $\lambda$ 和时间步 $\Delta t$，计算保留系数 $\alpha = \exp(-\lambda \Delta t)$。
    *   **更新机制**：输出 $out = \alpha X + (1 - \alpha) \cdot S(X)$，其中 $X$ 是当前特征（参考状态），$S(X)$ 是通过深度卷积和门控机制生成的“时间刺激”。这种设计保留了 LNN 的指数衰减特性，同时允许沿时间维度并行计算。
*   **层级衰减率共享策略（Hierarchical Decay-Rate Sharing Strategy）**：
    *   在特征金字塔的每一层，将 $\lambda$ 约束为单个可学习的标量并由所有通道共享。
    *   **作用**：稳定了优化过程，并使模型能自动适应不同金字塔层级的时间压缩（即不同尺度下的动作持续时间）。
*   **架构集成**：将上述机制集成到 Liquid Parallel Temporal Block (LPTB) 中，构建多尺度特征金字塔。

#### 3. 实验设计
*   **数据集**：在两个主流 Benchmark 上进行评估：**THUMOS-14**（短视频、动作密集）和 **ActivityNet-1.3**（长视频、动作稀疏）。
*   **对比方法**：
    *   **Transformer 类**：ActionFormer, TadTR, TALLFormer。
    *   **CNN 类**：TriDet, TemporalMaxer, G-TAD。
    *   **SSM 类**：MambaTAD, VideoMambaSuite。
*   **评估指标**：不同 IoU 阈值下的 mAP（平均精度）以及平均 mAP。

#### 4. 资源与算力
*   **训练效率**：文中明确对比了不同后端的训练耗时。在 THUMOS-14 上，LiquidTAD 的并行后端每轮（epoch）训练仅需 **12.96 秒**，而传统的 ODE 后端需 3663.83 秒，CfC 后端需 1683.08 秒。
*   **推理延迟**：在 **Intel Xeon Gold 6240 CPU** 上进行单线程测试，LiquidTAD 的延迟为 782ms，约为 ActionFormer 和 TriDet 的一半。
*   **GPU 信息**：文中提到在“单个 GPU”上测量训练和推理时间，但**未明确说明具体的 GPU 型号**（如 A100 或 V100）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 SOTA 对比、模型复杂度分析（参数量 vs. FLOPs）、跨平台（CPU）延迟测试以及详尽的消融实验。
*   **消融实验**：针对并行后端有效性、衰减率参数化策略、时间步 $\Delta t$ 的取值、特征金字塔深度等关键变量进行了 4 组以上的专项测试。
*   **客观性**：基于 OpenTAD 统一框架实现，确保了特征提取（I3D/TSP）和评估流程的公平性。实验结果显示其在大幅减小模型规模的同时保持了极具竞争力的精度。

#### 6. 主要结论与发现
*   **高效性**：在 THUMOS-14 上，LiquidTAD 仅需 **10.82M 参数**（比 ActionFormer 减少 63%）即可达到 69.46% 的平均 mAP。
*   **硬件友好**：由于不依赖自定义 CUDA 内核，其在 CPU 上的推理速度比基于 Mamba 的模型快约 **73 倍**。
*   **物理先验的价值**：实验证明，相比于纯动态的时间常数，静态但层级共享的衰减率在 TAD 任务中更稳定且有效。

#### 7. 优点
*   **部署灵活性**：完全基于标准 PyTorch 算子，无需任何硬件特定的优化库即可在各种平台上高效运行。
*   **线性复杂度**：计算复杂度随序列长度线性增长，适合处理超长视频。
*   **物理启发式设计**：引入指数松弛先验，为时序特征的保留与更新提供了明确的物理含义，减少了预测的碎片化。

#### 8. 不足与局限
*   **绝对精度上限**：虽然效率极高，但在 THUMOS-14 上的绝对 mAP 略低于一些参数量巨大的重型模型（如 CausalTAD）。
*   **对特征提取器的依赖**：实验主要基于预提取特征，未探讨端到端训练对液体算子性能的影响。
*   **超参数敏感性**：消融实验显示时间步 $\Delta t$ 的设置对结果有一定影响，可能需要针对不同采样率的视频进行微调。

（完）
