---
title: "ELSA: Exact Linear-Scan Attention for Fast and Memory-Light Vision Transformers"
authors: "Chih-Chung Hsu, Xin-Di Ma, Wo-Ting Liao, Chia-Ming Lee"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23798v1"
tags: ["query:lvu"]
score: 8.5
evidence: 用于长序列快速且轻量化处理的精确线性扫描注意力机制
tldr: ELSA是一种新型的精确线性扫描注意力机制，旨在解决现有加速器依赖特定硬件或在长序列FP32计算中效率低下的问题。它通过将在线Softmax更新重新表述为结合律幺半群上的前缀扫描，实现了对数级的并行深度和线性内存消耗。ELSA不依赖Tensor Core，可作为即插即用的替代方案，在A100和Jetson等多种硬件上均表现出显著的加速效果，且保持了精确的Softmax语义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23798v1/fig-001.webp\", \"caption\": \"Figure 4. Latency and peak memory at 1K, 4K, and 16K tokens (FP16/FP32). In FP32, ELSA reduces latency over MathSDPA and ME-SDPA while maintaining low memory. In FP16, ELSA achieves the lowest memory among exact kernels and remains competitive with FA2/FA3 at longer sequences; at 16K tokens, standard softmax attention fails with OOM while ELSA remains stable.\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.23798v1/fig-002.webp\", \"caption\": \"Table 9. Vectorized equivalence (oracle-based). 95th-percentile drift metrics against a high-precision (float64) reference, across all batches, heads, and tokens. All three scenarios show nearmachine-precision agreement.\", \"page\": 14, \"index\": 2, \"width\": 506, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.23798v1/fig-003.webp\", \"caption\": \"Figure 1. Integral-image analogy and ELSA computational flow. (a) Just as a prefix-sum table [6] enables O(1) range queries by composing boundary values, the softmax state (m,S,W ) supports exact blockwise composition via ⊕ (Proposition 1). (b) ME-SDPA [5] chains tokens sequentially via the online softmax recurrence [29], incurring O(n) depth that becomes prohibitive at long sequences. (c) ELSA reduces depth to O(logn) via a two-level prefix scan: Hillis–Steele [16] within blocks (all threads active, maximizing sharedmemory bandwidth) and Blelloch [1] across blocks (work-optimal, minimizing global memory traffic)—while preserving exact FP32 semantics, O(n) memory, and hardware agnosticism: ⊕ requires only scalar max/exp and vector fma, with no Tensor Core dependency.\", \"page\": 4, \"index\": 3, \"width\": 822, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.23798v1/fig-004.webp\", \"caption\": \"Table 11. VGGT baseline. ELSA-FP32 achieves 1.46–2.34× speedup over xFormers-FP32 while matching its memory footprint. Speedup is relative to xFormers-FP32; shading intensity reflects speedup magnitude.\", \"page\": 16, \"index\": 4, \"width\": 475, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2604.23798v1/fig-005.webp\", \"caption\": \"Table 12. FastVGGT scaling. ELSA-FP32 sustains 1.18–1.38× speedup at long sequences with identical memory overhead. Math baselines fail beyond 30 frames (OOM); shading intensity reflects speedup magnitude, red denotes below-baseline performance.\", \"page\": 16, \"index\": 5, \"width\": 462, \"height\": 952}]"
motivation: 现有的注意力加速方案往往牺牲Softmax精度、依赖特定硬件指令或在处理长序列FP32计算时存在并行深度限制。
method: 将在线Softmax计算重构为结合律幺半群上的前缀扫描算法，从而实现硬件无关的并行加速并保证数值精确性。
result: 在A100上FP32性能提升1.3-3.5倍，在Jetson TX2等边缘设备上也实现了显著的加速和吞吐量提升。
conclusion: ELSA提供了一种高效、硬件无关且保持全精度的注意力机制实现，是跨平台高精度推理的理想统一内核。
---

## Abstract
Existing attention accelerators often trade exact softmax semantics, depend on fused Tensor Core kernels, or incur sequential depth that limits FP32 throughput on long sequences. We present \textbf{ELSA}, an algorithmic reformulation of online softmax attention that (i)~preserves exact softmax semantics in real arithmetic with a \emph{provable} $\mathcal{O}(u\log n)$ FP32 relative error bound; (ii)~casts the online softmax update as a prefix scan over an associative monoid $(m,S,W)$, yielding $O(n)$ extra memory and $O(\log n)$ parallel depth; and (iii)~is Tensor-Core independent, implemented in Triton and CUDA C++, and deployable as a \emph{drop-in replacement} requiring no retraining or weight modification. Unlike FlashAttention-2/3, which rely on HMMA/GMMA Tensor Core instructions and provide no compatible FP32 path, ELSA operates identically on A100s and resource-constrained edge devices such as Jetson TX2 -- making it the only hardware-agnostic exact-attention kernel that reduces parallel depth to $O(\log n)$ at full precision. On A100 FP32 benchmarks (1K--16K tokens), ELSA delivers $1.3$--$3.5\times$ speedup over memory-efficient SDPA and $1.97$--$2.27\times$ on BERT; on Jetson TX2, ELSA achieves $1.5$--$1.6\times$ over Math (64--900 tokens), with $17.8$--$20.2\%$ throughput gains under LLaMA-13B offloading at $\ge$32K. In FP16, ELSA approaches hardware-fused baselines at long sequences while retaining full FP32 capability, offering a unified kernel for high-precision inference across platforms. Our code and implementation are available at https://github.com/ming053l/ELSA.