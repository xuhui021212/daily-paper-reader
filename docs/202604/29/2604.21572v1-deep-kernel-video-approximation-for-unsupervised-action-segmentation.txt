Title: Deep kernel video approximation for unsupervised action segmentation

URL Source: https://arxiv.org/pdf/2604.21572v1

Published Time: Fri, 24 Apr 2026 00:48:25 GMT

Number of Pages: 15

Markdown Content:
# Deep kernel video approximation for unsupervised action segmentation 

S.L. Pintea 1[0000 −0002 −2356 −2140] and J. Dijkstra 2[0000 −0002 −8666 −3731]  

> 1

Department of Intellgient Systems (DIS), Tilburg University, Tilburg, NL  

> 2

Division of Image Processing, LUMC, Leiden, NL 

Abstract. This work focuses on per-video unsupervised action segmen-tation, which is of interest to applications where storing large datasets is either not possible, or nor permitted. We propose to segment videos by learning in deep kernel space, to approximate the underlying frame dis-tribution, as closely as possible. To define this closeness metric between the original video distribution and its approximation, we rely on maxi-mum mean discrepancy (MMD) which is a geometry-preserving metric in distribution space, and thus gives more reliable estimates. Moreover, un-like the commonly used optimal transport metric, MMD is both easier to optimize, and faster. We choose to use neural tangent kernels (NTKs) to define the kernel space where MMD operates, because of their improved descriptive power as opposed to fixed kernels. And, also, because NTKs sidestep the trivial solution, when jointly learning the inputs (video ap-proximation) and the kernel function. Finally, we show competitive re-sults when compared to state-of-the-art per-video methods, on six stan-dard benchmarks. Additionally, our method has higher F1 scores than prior agglomerative work, when the number of segments is unknown. 

Keywords: Unsupervised action segmentation · MMD · infinite NTK 

## 1 Introduction 

Could you find the unitary action segments in a random video? What if you are told beforehand how many segments there are? This is the task of unsuper-vised action segmentation, where videos are segmented into mutually exclusive segments, i.e. actions. This task is essential in healthcare and assisted living, for activity tracking. However, in these applications, storing large datasets is not permitted due to privacy concerns. Here, we focus on such data-constrained scenarios where only one video is available at a time, with no access to large sets of training videos, and no annotations. Prior unsupervised action segmentation works [1,2,3,4] do not model the underlying video distribution. Therefore, they do not take into account the un-derlying geometry of the video space. In consequence, these methods do not rely on metrics of convergence in distribution space, and thus, are unstable with respect to deformations of the distribution supports [5]. To date, two classes of commonly used distances take the geometry of the underlying distribution              

> arXiv:2604.21572v1 [cs.CV] 23 Apr 2026 2S.L Pintea and J. Dijkstra
> Fig. 1. We learn a small video approximation evm∼Q(in green) such that its distri-bution is "as close as possible" to the distribution of the real video frames vi∼P(in orange). To define "as close as possible" we rely on a geometry preserving metric – MMD (maximum mean discrepancy) which minimizes the distance between the kernel means μQand μP.

into account: OT (optimal transport) and MMD (maximum mean discrepancy). While OT has been previously used for unsupervised action segmentation [6,7], it is computationally expensive and hard to optimize. On the other hand, MMD defines geometry-aware distances, and is more straight-forward to optimize than OT [5]. Moreover, MMD can be simply optimized in batches over the data. This work proposes learning a video approximation in a deep kernel formula-tion by optimizing a geometry-preserving metric, namely MMD. By definition, MMD works in kernel space [8]. Although using fixed kernels (such as defined by the exponential family) is better suited for cases such as ours where we do not rely on a training set, these kernels are limited in descriptive power. Wenliang et al. 

[9] show that deep network kernels are more informative than the exponential-family kernels. Here, we want to get the best of both worlds, by choosing to use NTKs (neural tangent kernels). NTKs combine the non-parametric flexibility of kernel methods, with the descriptive power of deep neural networks. Specifically, here we rely on infinite NTKs over fully-connected networks, which consistently outperform finite NTKs [10]. Concretely, our method learns to approximate a set of video frames {vi}i∈{ 1,..,N } by a small video approximation {evm}m∈{ 1,..,M }

with M ≪N . Our goal is to learn a distribution over the video approximation 

ev∼Q that is as close as possible to the true video distribution v∼P. To define “as close as possible" we minimize a geometry-preserving metric between the real video distribution and its approximation – MMD, as in Fig. 1. To summarize, this work is the first to propose learning a video approxi-mation in a deep kernel formulation by optimizing via a geometry-preserving metric – MMD (maximum mean discrepancy). Specifically, (i) we focus on per-video unsupervised action segmentation where we learn a small set of synthetic frames that closely approximate the original video; (ii) to this end, we aim to make the distribution of the video approximation as close as possible to the real video frames distribution; (iii) we show experimentally that the proposed method is more precise in terms of F1 scores, than prior agglomerative work, when the number of segments is not known; and our method has competitive results when benchmarked on standard datasets. Our complete code is available at: https://github.com/SilviaLauraPintea/video_approx. Deep kernel video approximation for unsupervised action segmentation 3

## 2 Related work 

For an overview of the state-of-the-art action segmentation, we direct the readers to [11]. Here, we focus on unsupervised temporal segmentation of actions. 

Clustering-based action segmentation. Unsupervised methods rely on prob-abilistic mixture models [12,13,14]. Using a stacked Dirichlet mixture process [12] pioneers unsupervised action segmentation. Similarly, a mixture of Gaussians can be used for coarse-to-fine clustering of frames [13], or over U-Net embeddings [15]. Also from a probabilistic perspective, a Generalized Mallows Model [14] is used to alternate between learning action appearance and temporal model-ing. A parallel research trend has focused on clustering self-supervised features [16,17,18]. Rather than clustering embeddings, learning hierarchical codebooks in an encoder-decoder architecture is effective [3]. Graph embeddings [16] or simple MLP embeddings [17] can be clustered. Vice versa, starting from a given video clustering, it is effective to further group frames based on visual similarity [19]. While network embeddings are informative, a training-free temporally-weighted hierarchical clustering [2] is both effective and fast. We are dissimilar to the above methods, as our video approximation is not defined by cluster centers, but learned, such that it follows the underlying video-frame distribution. 

Action segmentation by labeling. Rather than clustering, detecting action boundaries based on frame similarities also gives promising results [1,4]. Action boundaries can be detected as discrepancies between predicted future frames and true future frames [20,21]. Alternatively, an auto-encoder with an attention module trained using a contrastive loss is effective at finding discriminative la-tent codes [22]. Differently, generating labels and recovering the action ordering through expectation-maximization over HMM (Hidden Markov Model) [23] or OT (optimal transport) [6,24,7] has been previously proposed. Rather than gen-erating labels per frames using self-supervision or optimal transport, here, we aim to learn a video approximation in deep kernel space. For this, our method relies on a geometry preserving metric — MMD (maximum mean discrepancy), which unlike OT, has improved computational and sample complexity and does not suffer from the curse of dimensionality. Moreover, because we train per video rather than over a complete dataset of videos, we opt for a computationally ef-ficient deep kernel model – an infinite NTK (neural tangent kernel). 

Learning pseudo-inputs. Approximating a large dataset with a small set of pseudo-samples, called inducing points , has been studied in the Gaussian Process literature [25,26,27]. More recently, this idea has been adopted in dataset dis-tillation literature [28] in combination with NTKs (neural tangent kernels) [29]. Kernel ridge regression using a kernel defined as NTK, approximates infinitely wide neural networks [30,29]. Here, rather than learning the video approxima-tion in a supervised manner in a Gaussian Process formulation, we rely on MMD (maximum mean discrepancy). MMD in kernel space was first defined in [8,31], to test if two groups of samples come from the same distribution. More recently, NTKs have been extended to approximate characteristic kernels, and thus be 4 S.L Pintea and J. Dijkstra 

useful for hypothesis testing [32,33,34]. We build on prior work, and use infinite NTKs [10] to learn a video approximation that minimizes the MMD with respect to the real video frame distribution. 

## 3 Learning video approximations in deep kernel space 

Given a set of video frames vi∈{ 1,..,N }, we want to learn a small set of synthetic frames evm∈{ 1,..,M }, M ≪N that closely approximate the video. During the opti-mization, to objectively measure the closeness between the original video and its approximation, we need a metric. Here, we opt for a geometry-preserving metric, specifically MMD (maximum mean discrepancy). 

Geometry preserving metric: MMD. We want the distribution of the video approximation ev to be as close as possible to the distribution of the real video v.Specifically, we assume that the real video frames are drawn from P: v∼P, and the video approximation from Q: ev∼Q. And we optimize ev such that Q ≈ P.By definition, MMD [8] operates in the RKHS (Reproducing Kernel Hilbert Space) [8] denoted by H. In RKHS, starting from a mapping function kv:V→H ,

kv(·)= k(v, ·), we can define a kernel function k:V×V→ R as an inner product 

k(vi, vj )= ⟨k(vi, ·), k (vj , ·)⟩. In RKHS we can measure the distance between two distributions by measuring the distance between their mean embeddings [8]: MMD (P, Q, H)= sup k∈H (Ev∼P[k(v, ·)] −Eev∼Q[k(ev, ·)]) (1) The empirical estimate of MMD is given by the distance between the kernel means, estimated over samples from the data distributions P and Q:MMD (P, Q, H) = μP − μQ H = 1

N

X

> v∼P

k(v, ·) − 1

M

X

> ev∼Q

k(ev, ·)

> H

(2) Gretton et al. [8] show that this can be rewritten in a simple form that only depends on kernel values: MMD 2(P, Q, H)= 1

N 2

X

> i,j

k(vi, vj )+ 1

M 2

X

> n,m

k(evn, evm)− 2

N M 

X

> i,m

k(vi, evm) (3) MMD has the nice property that it does not need to make any assumption about the underlying data distributions, P and Q. However, we note here that M M D ,as defined in [8], assumes that the samples v∼P and ev∼Q are i.i.d samples. While this assumption may hold for unitary action prototypes ev, this does not hold for video frames, v. To compensate for this, we learn the video approximation ev by minimizing the Eq. (3) using shuffled batches of the data of size NM . Additionally, we regularize the norm of evm, to constrain the learning. Ideally, when optimizing M M D 2(P, Q, H), the kernels should satisfy two other properties: i.e. that the mapping k(vi, ·) is smooth, and that the ker-nel is characteristic . For mapping smoothness, we use kernels defined by con-tinuous functions, discussed below. For the second property, this depends on Deep kernel video approximation for unsupervised action segmentation 5

the kernel choice. A characteristic kernel has a mean embedding that fully de-scribes the data distribution. Therefore, for these kernels, it holds that when 

M M D (P, Q, H)=0 this implies P=Q. However, the standard characteristic ker-nels ( e.g. the exponential family) have limited representation power [9]. There-fore, we choose to combine an exponential kernel with a deep learning kernel.     

> Fig. 2. We learn the video approximation ev
> by optimizing the M M D 2loss over a prod-uct of NTK and Gaussian kernels.

Non-parametric yet descriptive deep kernels: infinite-NTKs. MMD relies on kernel functions k(·, ·).Learning the kernel function k(·, ·) to-gether with the video approximation 

ev leads to a trivial solution. There-fore, we need to rely on fixed ker-nels, such as the exponential fam-ily kernels. However, Wenliang et al. 

[9], shows that the exponential fam-ily kernels are less informative than deep network kernels. Thus, to have a fixed kernel and yet a deep kernel, we choose to use NTKs (neural tan-gent kernels). NTKs [26,27] are kernels defined over the gradients of wide deep networks. Particularly, we choose to use infinite NTKs, which consistently out-perform finite NTKs [10]. The NTK, at two samples vi, vj for a neural network f (·, θ), is the sum over layers l of inner products between the gradients of the network output with respect to the layer parameters θl, evaluated at samples vi, vj :

kntk (vi, vj ) = X

> l

*

∂f (vi, θ)

∂θl

f (vj , θ)

∂θl

+

. (4) The NTK in Eq. (4) is not a characteristic kernel. To ameliorate this, we combine the kntk (·, ·) with a characteristic kernel, namely a Gaussian kernel: 

kG (vi, vj ). This combination has the added benefit that it allows us to minimize the distance between the distributions both in input image space ( i.e. Gaussian kernel) and in the neural network gradient space ( i.e. NTK). To combine the ker-nels, we simply take the product. Because the two kernels have different ranges, we define a rescaling α= med (kG (vi,vj ))    

> med (kntk (vi,vj))

, where med (·) denotes the median, to bring them in the same range: 

k(vi, vj ) = αk ntk (vi, vj )kG (vi, vj ), (5) where kG (vi, vj )= exp 



− ∥vi−vj ∥22

> λ2



is the Gaussian kernel with length-scale λ.Note that when the length-scale λ→∞ , the Gaussian kernel kG (·, ·)→0. There-fore, jointly learning the length-scale λ and the ev, leads to a trivial solution. To avoid this, inspired by Gretton et al. [35], we set λ = med i,j (∥vj − vi∥22), where med (·) is the median function. Rather than using the median of the distances as 6 S.L Pintea and J. Dijkstra 

in [35], we use the median of the squared distances, as it works better in com-bination with an NTK, and acts as a regularization that enforces smoothness. Fig. 2 shows a schematic representation of our video approximation learning. Our overall kernel in Eq. (5) is not a characteristic kernel, as kntk (·, ·) is not characteristic [34]. Projecting the inputs on a hypersphere Sd−1 where d is the dimension of the inputs can make NTK similar to a Laplace kernel [33], which is characteristic. Alternatively, using random cosine projections in the first layer [34] also makes NTK approximate a characteristic kernel. In the experimental section we analyze these kernel alternatives. 

Action segmentation. We use the kernel in Eq. (5) and learn a video approx-imation that minimizes the distribution distance in kernel space, as defined by the M M D 2(P, Q, H) metric, between the real video v and its approximation ev.Given that the kernel can be interpreted as a similarity between samples, we use this to assign the real video frames to the synthetic frames in the approxima-tion, and thus segment the actions. Concretely, we assign a video frame vi to an action indexed by m∗, by taking the maximum kernel similarity across all evm:

m∗ = arg max 

> m

k(vi, evm). (6) Following prior work [2,1,7], we use Hungarian matching to map the indexes 

m∈{ 1, .., M } to the ground truth action classes in the dataset. In our method, aligning the video approximation with the original video causes severe over-segmentation when the features are not smoothed over time. Therefore, we smooth the input video frames using a Gaussian, and discuss in the experiments the impact of the smoothing hyperparameter. 

Theoretical time/complexity analysis. Kernel mean embeddings, as used in the MMD (Eq. (3)) have the benefit that they can model higher order moments of the distributions, while being simple to optimize and fast [5,36]. When com-pared to optimal transport – an alternative geometry-preserving metric, MMD is provably more efficient. MMD’s computational complexity is O(n2) [8] while for optimal transport this is O(n3 log( n)) [37], where n is the number of sam-ples. Additionally, MMD is independent of data dimension and has a sample complexity of O

 1√n



, while optimal transport suffers from the curse of di-mensionality with a sample complexity of O



n− 1

> d



[37], where d is the data dimension. In practice, we can improve the speed of MMD by optimizing it over batches of data. Thus, allowing us to process extremely long videos. Moreover, using batches gives better results than optimizing over the complete video. 

## 4 Experimental analysis 

Datasets overview. We compare with state-of-the-art on six standard segmen-tation benchmarks: Breakfast [38], 50 Salads [39], YTI (YouTube Instructional) 

[40], Desktop Assembly [24], Hollywood Extended [41] and MPII Cooking 2 Deep kernel video approximation for unsupervised action segmentation 7Avg. frames Videos Avg. segments Features 50 Salads [39] 11788 50 19 64 D IDT [43] MPII Cooking 2 [42] 10555 273 17 64 D IDT [43] Breakfast [38] 2099 1712 6 64 D IDT [43] Hollywood extended [41] 835 937 3 64 D IDT [43] Desktop assembly [24] 779 76 23 512 D-features [24] YTI [40] 520 150 9 3k D-features [17]  

> Table 1. Datasets overview. We use the same features as prior work [1,2,7].

[42]. We follow previous work [1,2,7], and for Breakfast [38], 50 Salads [39], 

Hollywood Extended [41] and MPII Cooking 2 [42] we use 64-dimensional IDT (improved dense trajectory) features [43]. Also as in previous work, we use 3000-dimensional features provided by [17] for YTI [40], while for Desktop Assembly 

[24] we use 512-dimensional features provided by [24]. Tab. 1 shows an overview of the datasets. We do not exclude any action class during training. On YTI , at evaluation time we ignore the background class, as in [1,7]. 

Metrics and baselines. Our method computes a video approximation, and aligns each video with its learned approximation. Therefore, we can only compare with unsupervised methods that perform the segmentation per video, rather than across videos, such as: TW-FINCH [2], ABD [1] and ASOT [7] and CLOT [44]. Unfortunately, we cannot compare with the recent HVQ [3] and VASOT [6], as they use the full dataset to train the segmentation, rather than individual videos. To compare with the baselines, we use the standard metrics: MoF (mean accuracy over frames); IoU (intersection over union) and F 1 from [2]. 

Hyperparameters. In all our experiments we use the NTK initialization with hyperparameters: W∼N (0 , 2) , b∼N (0 , 0.1) (where W, b represent the MLP weights and biases, respectively) [45,46]. And we use a learning rate set to 5e−2

and a weight decay of 1e−3, for all datasets. We use an infinite-width NTK, defined by a 1-hidden layer MLP with ReLU activations implemented with the 

neural tangents library [47] in JAX . We initialize the video approximation with the mean frames of uniform segments. For each video, we set the number of segments M to the average number of actions per activity/dataset, as in previous work [2,7]. We minimize the M M D 2 over 100 epochs, and use a batch size of NM ,where N is the number of video frames. Because we perform the optimization over data batches, our method can be effectively applied to long videos. We ran all experiments on a small NVIDIA RTX A1000 Laptop GPU. 

4.1 Hypothesis: the video approximation learns actions Experimental setup. To evaluate our hypothesis that our method can learn good video approximations, we use a controlled setting. Concretely, we design a 

Moving5 dataset containing 50 videos per set (training/validation/test) and 5 classes, where videos contain ≈150 frames. We select the digits {1, 3, 5, 7, 9} to represent actions. Each digit moves for a variable number of frames and with a speed proportional to the number of frames, across a fixed trajectory: i.e. 8 S.L Pintea and J. Dijkstra MoF ( ↑) IoU ( ↑) F1 (↑)K-means 59.2% 42.2% 54.9% Uniform 68.2% 54.8% 67.3% Kernel (K-means) 60.4% 44.0% 56.6% Kernel (Uniform) 72.5% 59.3% 70.3%     

> Video approx. (Ours) 73.3% 59.9% 70.5%
> Ground truth average frame
> Video approximation

Table 2. Hypothesis testing. Left: Aligning the video approximation in kernel space improves over using L2 distances for aligning cluster centers with video frames. Right: 

Examples of learned video approximations, versus ground truth average frame per class. The learned video approximation aligns better with the video than the uniform segment centers, or K-means centers. 

top-to-bottom, diagonal, inverse-diagonal, right-to-left, left-to-right. To make it challenging, action "3" can repeat up to 3 times per video. To visualize the learned video approximation, we define ev in the RGB image domain of size 

M ×28 ×28 ×3. We normalize each frame by its L2 norm. We do not smooth the videos over time. To avoid overfitting, we train for 10 epochs. We select these hyperparameters on a separate validation set. 

Is learning the video approximation effective? Tab. 2 shows that aligning videos with their approximation, in the kernel space (using Eq. (6)) is more effective than using L2 distances. Each video segment length can vary between 5 and 30 frames. Thus, the uniform video splitting has an error proportional to this variance. While fixing the video approximation to uniform initialization works well, learning the video approximation further improves the results. 

Finding unitary action boundaries. Here, we test if our method can find unitary action boundaries. For this, we predict a number of segments randomly varied with an offset δ=M ±δ, where M is the true number of actions, and 

δ ∈ { 1, 2, .., 5}. Because the data is noisy: i.e. the required number of segments does not match the underlying data, we use 20 epochs here and a weight decay of 

1e−4. We evaluate with respect to the true number of segments. We estimate the boundary accuracy as the percentage of action boundaries correctly detected. We compare with TW-FINCH [2], as a representative of agglomerative clustering. Tab. 3 shows that our method can better detect action transitions than ag-glomerative clustering when the required number of segments does not match the underlying data structure. Moreover, unlike agglomerative clustering meth-ods [2,1], our method can deal with repeated actions, as it does not explicitly use the time information for clustering. Our advantage is due to our method not strictly enforcing a given number of segments: although we learn M synthetic frames in our video approximation, some of them may not align well with any video frame in kernel space (using Eq. (6)). 

4.2 Analysis of model choices 

Given that we use infinite NTK, there are no kernel parameters that need to be selected. For the initialization of the underlying MLP weights, there is al-ready extensive research, and we follow the standard NTK initialization [46]. Deep kernel video approximation for unsupervised action segmentation 9True # actions Random # actions MoF ( ↑) Boundary acc. ( ↑) MoF ( ↑) Boundary acc. ( ↑)TW-FINCH [2] 72.5% 64.4% TW-FINCH [2] 60.3% 58.8% Ours 73.3% 51.2% Ours 60.5% 62.4% (a) Segmentation with true classes and random number of classes 

(b) True number of segments: 5 th video (left) and 35 th video (right). 

(c) Random number of segments: more (5 th video) and less (35 th video). 

Table 3. Hypothesis testing. (a) While TW-FINCH [2] can give precise boundaries when knowing the ground truth number of segments, it fails to do so for a random number of segments. (b) Two examples when predicting the true number of segments. (c) Tow examples when the desired number of segments is randomly varied. Our model can better predict repeated actions, and can better detect the boundaries. 

The actual seed used plays no role, as the networks are infinitely wide. And, for the Gaussian kernel, we use the median of the squared distances between the samples. Here, we consider the effect of the kernel choice and the data smoothing. 

Effect of the kernel choice. To test the effect of the kernel function, we consider seven kernel options: NNGP – the neural network Gaussian Process kernel [48] which is a characteristic kernel, NTK – the neural tangent kernel [30] which is not characteristic, NTK sphere – the neural tangent kernel with inputs projected on the Sd−1 sphere, where d is the dimension of the inputs [33], 

SNTK – our implementation of the shift-invariant NTK proposed in [34], Gauss 

– the simple Gaussian kernel with fixed length-scale, which is also characteris-tic, Gauss ×NTK sphere – using Eq. (5) to combine the NTK sphere and the Gaussian kernel, and Gauss ×NTK (Ours) – the combination of the Gaussian kernel and the NTK using Eq. (5). For the Gauss kernel we set the length-scale parameter to λ=1 e+1 , as that gave the best results, here. Fig. 3(a) shows that while the characteristic kernels such as Gauss , NNGP and NTK sphere have a higher F1 and IoU scores than the rest, the combination of the Gaussian kernel with the NTK achieves the best segmentation accuracy. 

Effect of input smoothing. Here, we test the effect of feature smoothing over time, on three datasets with varying video sizes: 50 Salads (≈10 K frames) [39], 

Desktop Assembly (≈700 frames ) [24] and YTI [40] ( ≈500 frames). We smooth the videos over time with a Gaussian filter over window sizes of approximately 10 S.L Pintea and J. Dijkstra MoF ( ↑) IoU ( ↑) F1 (↑)Gauss 62.1% 46.0% 56.7% NNGP [48] 67.7% 46.2% 56.6% NTK [30] 69.1% 42.3% 52.6% NTK sphere [33] 67.1% 46.6% 57.3% SNTK [34] 1 62.7% 45.7% 56.5% Gauss ×NTK sphere 69.8% 44.6% 55.0% Gauss ×NTK (Ours) 70.1% 44.9% 55.3% 0.5 1.0 1.5 2.0 2.5 3.0 3.5 Smoothing hyperparameter   

> 52.5
> 55.0
> 57.5
> 60.0
> 62.5
> 65.0
> 67.5
> 70.0 MoF
> 50 Salads Desktop Assembly YouTube Instructional
> (a) Kernel choices (b) Input smoothing s

Fig. 3. Model choices: (a) Kernel choice on 50 Salads [39]. The combination of the Gaussian kernel and NTK, as given by the Eq. (5), is the most accurate. (b) Input smoothing σ. We consider three datasets with varying video lengths 50 Salads [39] – long, Desktop Assembly [24] – medium, and YTI [40] – short. The choice of the smoothing hyperparameter s has effect, especially on long videos. 

s NM and we vary the smoothing hyperparameter s∈{ 0.5, 1, 1.5, 2, 2.5, 3, 3.5}. In Fig. 3(b) smoothing has a pronounced effect in terms of MoF scores, especially for long videos, as in 50 Salads . This is because smoothing adds information about the neighboring actions. In all experiments we set s=2 .5 for datasets containing long videos ( i.e. e.g. 50 Salads , MPII cooking , Breakfast ), and set 

s=1 .5 for datasets containing short videos ( i.e. YTI , Hollywood Extended and 

Desktop Assembly ). 

4.3 Unsupervised action segmentation per video Average number of classes for segmentation. We compare our method with 

all prior work we could find, performing unsupervised action segmentation per video and reporting on these standard benchmarks. Our method and the consid-ered baselines segment each video independently, without relying on contextual information from large video datasets. We urge the reader to keep this setting in mind, while judging the completeness of our benchmarking. We do not add 

SaM [4] because although their method is strikingly similar to ABD [1], they report results using the ground truth number of segments, while standardly the average number of segments per dataset/activity is used [2,7]. Tab. 4 shows the results across all datasets. Overall, there is no winning method across all datasets. Interestingly, TW-FINCH remains a strong base-line, especially for the datasets containing a high granularity level of annota-tions, like YTI and Desktop Assembly . Our method obtains competitive results, outperforming ASOT [7] on 50 Salads . Our method cannot perform well on the Desktop Assembly because of the high granularity annotation level: e.g. "tighten_screw1" , "tighten_screw2" , "tighten_screw3" , "tighten_screw4" . On the Hollywood Extended and Breakfast we only learn 3 and 6 synthetic frames in the video approximation, respectively. Such a low number of synthetic frames may not be sufficient to learn a good video approximation. On the datasets with short videos, such as YTI , Hollywood Extended our method gains little in    

> 1Our implementation of SNTK proposed in [34].

Deep kernel video approximation for unsupervised action segmentation 11 Hollywood Extended Breakfast MPII Cooking 2 MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑)TW-FINCH [2] (CVPR’21) 55.0% 35.0% 45.7% 62.7% 42.3% 49.8% 41.5% 22.4% 28.1% ABD [1] (CVPR’22) 60.7% 36.0% 57.1% 64.0% — 52.3% — — —ASOT [7] (CVPR’24) — — — 63.3% 35.9% 53.5% — — —CLOT [44] (ICCV’25) — — — 66.3% 37.1% 55.9% — — —Ours (uniform init) 54.1% 35.2% 46.4% 57.9% 39.5% 47.5% 35.3% 19.3% 25.1% Ours 55.1% 35.5% 46.5% 61.5% 42.3% 49.8% 39.7% 19.1% 25.4% YTI Desktop Assembly 50 Salads (Mid) MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑)TW-FINCH [2] (CVPR’21) 1 72.5% 46.0% 58.2% 73.3% 57.8% 67.7% 66.7% 47.3% 57.1% ABD [1] (CVPR’22) 2 67.2% — 49.2% — — — 71.8% — 59.2% ASOT [7] (CVPR’24) 71.2% 47.8% 63.3% 73.4% 47.6% 68.0% 64.3% 33.4% 51.1% CLOT [44] (ICCV’25) 69.3% 48.2% 60.8% 73.5% 52.4% 75.2% 69.4% 45.0% 63.8% Ours (uniform init) 70.1% 45.1% 56.0% 67.9% 48.4% 59.5% 65.2% 46.0% 56.7% Ours 71.3% 46.0% 56.7% 71.0% 52.4% 62.8% 70.1% 44.9% 55.3% 

Table 4. Benchmarking: average number of classes for segmentation. Results on six standard benchmarks compared to all baselines, performing unsupervised video segmentation per video. While TW-FINCH remains a strong baseline, our method shows competitive results, comparing positively to ASOT (CVPR’24) and CLOT 

(ICCV’25) on 50 Salads and ABD (CVPR’22) on YTI .

learning the video approximation on top of the uniform initialization. This may indicate that the models are overfitting here. On the other hand, we observe a large gain in segmentation accuracy (MoF) for long videos such as 50 Salads and 

MPII Cooking 2 , when compared to the uniform initialization. On average, our method learns a video approximation that align well with the original video in the deep kernel space. 

Random number of classes for segmentation. Here, we test the hypoth-esis that hierarchical clustering creates arbitrary segment boundaries that do not respect the underlying video data. This is a realistic setting, as often the true underlying number of segments present in the video is not known. For this, we rerun our code and the code of FW-FINCH [2] on all datasets using 

M =M ±U (−M , M ), where M is the average number of actions annotated in each dataset. We use the same random seed for both methods. Similar to the synthetic data, to better deal with the noisy number of segments, we train for 200 iterations with a weight decay of 1e−4. We cannot compare with ABD [1] as the code is not published and our efforts to replicate the author’s results did not succeed. We do not consider ASOT [7] and CLOT [44] as they do not do hierarchical clustering. Tab. 5 shows the results when the ideal number of segments is not known. While TW-FINCH shows competitive results in Tab. 4 when the number of segments is on average the true one, it is less competitive when the required number of segments largely deviates from the true one. Our method consistently outperforms the TW-FINCH baseline in F1 scores, except for the MPII Cooking    

> 1Where missing, we report TW-FINCH [2] results as run by us.
> 2ABD [1] do not provide code and we failed to reproduce their results.

12 S.L Pintea and J. Dijkstra Hollywood Extended Breakfast MPII Cooking 2 MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑)TW-FINCH [2](CVPR’21) 56.4% 33.0% 36.7% 58.4% 41.3% 38.0% 40.5% 24.4% 21.9% Ours 53.3% 34.7% 39.3% 57.9% 41.4% 38.6% 39.3% 22.3% 20.8% YTI Desktop Assembly 50 Salads (Mid) MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑) MoF ( ↑) IoU ( ↑) F1 (↑)TW-FINCH [2](CVPR’21) 67.3% 44.5% 47.3% 60.8% 46.9% 42.9% 57.2% 41.3% 38.2% Ours 66.5% 43.6% 47.5% 61.2% 41.8% 43.1% 60.3% 38.7% 41.8%                

> 5th video example from Hollywood Extended 5th video example from Breakfast
> 5th video example from MPII Cooking 2 5th video example from YTI
> 5th video example from Desktop Assembly 5th video examples from 50 Salads (Mid)

Table 5. Benchmarking: random number of classes for segmentation. Our pro-posed method repeatedly outperforms the TW-FINCH baseline in terms of F1 scores, when the ideal number of segments is not known, which is a realistic setting. We also show example predictions for the 5th video of each dataset. 

2 dataset, where our smooth predictions cannot detect the dense background. This improvement is due to our method not forcing a fixed number of segments. By assigning each frame to its closest approximation in kernel space, it can happen that some synthetic frames do have any video frames assigned to them. Therefore, our method more closely follows the underlying data distribution when segmenting the videos. We also illustrate under Tab. 5 a randomly chosen example (the predictions for the 5 th video) from each dataset. 

## 5 Method limitations and conclusions 

While being effective at learning video approximations, especially for long videos, our method’s descriptive power is bounded. Concretely, our method learns a sin-gle synthetic frame per action, which may be over-simplistic for long and complex actions. Another limitation of our proposed method is that it fails to correctly segment the video if the actions are too similar, as in the case of Desktop As-sembly (e.g. "tighten_screw_1" , "tighten_screw_2" , etc. ) in Fig. 4(a). Another failure case is when actions that are dissimilar are annotated together under a single name, e.g. the background action in MPII Cooking 2 , as in Fig. 4(b). Ad-ditionally, our method is sensitive to the smoothing hyperparameter, s. Learning Deep kernel video approximation for unsupervised action segmentation 13 Tighten_screw Tighten_screw_4 Tighten_screw_1 Tighten_screw_2 Tighten_screw_3 0 100 200 300 400 500 600 0 100 200 300 400 500 600 0 2000 4000 6000 8000 10000 12000 14000 0 2000 4000 6000 8000 10000 12000 14000        

> Background Background
> (a) Failures on Desktop Assembly (b) Failures on MPII Cooking 2
> (video 2020-06-03_18-01-32 )(video s08-d04-cam-002 )

Fig. 4. Failure cases. Our method fails to correctly segment very similar actions, as in the case of Desktop Assembly . Another failure is when distinct actions are grouped together, such as the background class on the challenging MPII Cooking 2 dataset. 

the smoothing hyperparameter together with the video approximation is possi-ble, however this cannot be directly used in combination with batched data. And batched data is, in practice, better than using the complete video. To conclude, we propose a simple method for learning video approximations and aligning these with the original video in kernel space, for unsupervised action segmentation. We show that the proposed method works in practice, and that the learned video approximation aligns well with the original video. Our method has the added benefit that it respects the underlying structure of the data when the true number of actions is not known. 

Acknowledgements. This work is supported by the IWISH project (ITEA 20044) and funded by RVO (Netherlands Enterprise Agency). 

## References 

1. Z. Du, X. Wang, G. Zhou, and Q. Wang, “Fast and unsupervised action boundary detection for action segmentation,” in CVPR , 2022. 2. S. Sarfraz, N. Murray, V. Sharma, A. Diba, L. Van Gool, and R. Stiefelhagen, “Temporally-weighted hierarchical clustering for unsupervised action segmenta-tion,” in CVPR , 2021. 3. F. Spurio, E. Bahrami, G. Francesca, and J. Gall, “Hierarchical vector quantization for unsupervised action segmentation,” in AAAI , 2025. 4. Z. Xing and W. Zhao, “Unsupervised action segmentation via fast learning of se-mantically consistent actoms,” in AAAI , 2024. 5. J. Feydy, T. Séjourné, F.-X. Vialard, S.-i. Amari, A. Trouvé, and G. Peyré, “In-terpolating between optimal transport and mmd using sinkhorn divergences,” in 

ICAIS , 2019. 6. A. S. Ali, S. A. Mahmood, M. Saeed, A. Konin, M. Z. Zia, and Q.-H. Tran, “Joint self-supervised video alignment and action segmentation,” in ICCV , 2025. 7. M. Xu and S. Gould, “Temporally consistent unbalanced optimal transport for unsupervised action segmentation,” in CVPR , 2024. 8. A. Gretton, K. Borgwardt, M. Rasch, B. Schölkopf, and A. Smola, “A kernel method for the two-sample-problem,” NeurIPS , 2006. 9. L. Wenliang, D. J. Sutherland, H. Strathmann, and A. Gretton, “Learning deep kernels for exponential family densities,” in ICML , 2019. 10. J. Lee, S. Schoenholz, J. Pennington, B. Adlam, L. Xiao, R. Novak, and J. Sohl-Dickstein, “Finite versus infinite neural networks: an empirical study,” NeurIPS ,2020. 14 S.L Pintea and J. Dijkstra 11. G. Ding, F. Sener, and A. Yao, “Temporal action segmentation: An analysis of modern techniques,” TPAMI , 2023. 12. K. M. Kitani, T. Okabe, Y. Sato, and A. Sugimoto, “Fast unsupervised ego-action learning for first-person sports videos,” in CVPR , 2011. 13. F. Nater, H. Grabner, and L. Van Gool, “Temporal relations in videos for unsu-pervised activity analysis.,” in BMVC , 2011. 14. F. Sener and A. Yao, “Unsupervised learning and segmentation of complex activi-ties from video,” in CVPR , 2018. 15. R. G. VidalMata, W. J. Scheirer, A. Kukleva, D. Cox, and H. Kuehne, “Joint visual-temporal embedding for unsupervised learning of actions in untrimmed sequences,” in WACV , 2021. 16. M. Dimiccoli and H. Wendt, “Learning event representations for temporal segmen-tation of image sequences by dynamic graph embedding,” TIP , 2020. 17. A. Kukleva, H. Kuehne, F. Sener, and J. Gall, “Unsupervised learning of action classes with continuous temporal embedding,” in CVPR , 2019. 18. Z. Wang, H. Chen, X. Li, C. Liu, Y. Xiong, J. Tighe, and C. Fowlkes, “Sscap: Self-supervised co-occurrence action parsing for unsupervised temporal action seg-mentation,” in WACV , 2022. 19. E. Bueno-Benito, B. T. Vecino, and M. Dimiccoli, “Leveraging triplet loss for un-supervised action segmentation,” in CVPR , 2023. 20. S. N. Aakur and S. Sarkar, “A perceptual prediction framework for self supervised event segmentation,” in CVPR , 2019. 21. A. Garcia del Molino, J.-H. Lim, and A.-H. Tan, “Predicting visual context for unsupervised event segmentation in continuous photo-streams,” in ICM , 2018. 22. S. Swetha, H. Kuehne, Y. S. Rawat, and M. Shah, “Unsupervised discriminative embedding for sub-action learning in complex activities,” in ICIP , 2021. 23. J. Li and S. Todorovic, “Action shuffle alternating learning for unsupervised action segmentation,” in CVPR , 2021. 24. S. Kumar, S. Haresh, A. Ahmed, A. Konin, M. Z. Zia, and Q.-H. Tran, “Unsuper-vised action segmentation by joint representation learning and online clustering,” in CVPR , 2022. 25. L. Csató and M. Opper, “Sparse on-line gaussian processes,” Neural computation ,2002. 26. A. Smola and P. Bartlett, “Sparse greedy gaussian process regression,” NeurIPS ,2000. 27. E. Snelson and Z. Ghahramani, “Sparse gaussian processes using pseudo-inputs,” 

NeurIPS , 2005. 28. T. Nguyen, Z. Chen, and J. Lee, “Dataset meta-learning from kernel ridge-regression,” in ICLR , 2020. 29. A. Jacot, F. Gabriel, and C. Hongler, “Neural tangent kernel: Convergence and generalization in neural networks,” NeurIPS , 2018. 30. S. Arora, S. S. Du, W. Hu, Z. Li, R. R. Salakhutdinov, and R. Wang, “On exact computation with an infinitely wide neural net,” NeurIPS , 2019. 31. A. J. Smola, A. Gretton, and K. Borgwardt, “Maximum mean discrepancy,” in 

ICONIP , 2006. 32. X. Cheng and Y. Xie, “Neural tangent kernel maximum mean discrepancy,” 

NeurIPS , 2021. 33. A. Geifman, A. Yadav, Y. Kasten, M. Galun, D. Jacobs, and B. Ronen, “On the similarity between the laplace and neural tangent kernels,” NeurIPS , 2020. 34. S. Jia, E. Nezhadarya, Y. Wu, and J. Ba, “Efficient statistical tests: A neural tangent kernel approach,” in ICML , 2021. Deep kernel video approximation for unsupervised action segmentation 15 35. A. Gretton, K. M. Borgwardt, M. J. Rasch, B. Schölkopf, and A. Smola, “A kernel two-sample test,” JMLR , 2012. 36. K. Muandet, K. Fukumizu, B. Sriperumbudur, B. Schölkopf, et al. , “Kernel mean embedding of distributions: A review and beyond,” Foundations and Trends in Machine Learning , 2017. 37. A. Genevay, L. Chizat, F. Bach, M. Cuturi, and G. Peyré, “Sample complexity of sinkhorn divergences,” in ICAIS , PMLR, 2019. 38. H. Kuehne, A. Arslan, and T. Serre, “The language of actions: Recovering the syntax and semantics of goal-directed human activities,” in CVPR , 2014. 39. S. Stein and S. J. McKenna, “Combining embedded accelerometers with computer vision for recognizing food preparation activities,” in International Joint Confer-ence on Pervasive and Ubiquitous Computing , 2013. 40. J.-B. Alayrac, P. Bojanowski, N. Agrawal, J. Sivic, I. Laptev, and S. Lacoste-Julien, “Unsupervised learning from narrated instruction videos,” in CVPR , 2016. 41. P. Bojanowski, R. Lajugie, F. Bach, I. Laptev, J. Ponce, C. Schmid, and J. Sivic, “Weakly supervised action labeling in videos under ordering constraints,” in ECCV ,2014. 42. M. Rohrbach, A. Rohrbach, M. Regneri, S. Amin, M. Andriluka, M. Pinkal, and B. Schiele, “Recognizing fine-grained and composite activities using hand-centric features and script data,” IJCV , 2016. 43. H. Wang and C. Schmid, “Action recognition with improved trajectories,” in ICCV ,2013. 44. E. Bueno-Benito and M. Dimiccoli, “Clot: Closed loop optimal transport for unsu-pervised action segmentation,” in ICCV , pp. 10719–10729, 2025. 45. G. Yang and E. J. Hu, “Feature learning in infinite-width neural networks,” Corr ,2020. 46. J. Sohl-Dickstein, R. Novak, S. S. Schoenholz, and J. Lee, “On the infinite width limit of neural networks with a standard parameterization,” CoRR , 2020. 47. R. Novak, L. Xiao, J. Hron, J. Lee, A. A. Alemi, J. Sohl-Dickstein, and S. S. Schoenholz, “Neural tangents: Fast and easy infinite neural networks in python,” in ICLR , 2020. 48. J. Lee, Y. Bahri, R. Novak, S. S. Schoenholz, J. Pennington, and J. Sohl-Dickstein, “Deep neural networks as gaussian processes,” ICLR , 2018.