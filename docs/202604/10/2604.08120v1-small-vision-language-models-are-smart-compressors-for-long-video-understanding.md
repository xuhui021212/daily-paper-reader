---
title: Small Vision-Language Models are Smart Compressors for Long Video Understanding
title_zh: 小型视觉语言模型是长视频理解的智能压缩器
authors: "Junjie Fei, Jun Chen, Zechun Liu, Yunyang Xiong, Chong Zhou, Wei Wen, Junlin Han, Mingchen Zhuge, Saksham Suri, Qi Qian, Shuming Liu, Lemeng Wu, Raghuraman Krishnamoorthi, Vikas Chandra, Mohamed Elhoseiny, Chenchen Zhu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08120v1"
tags: ["query:lvu"]
score: 10.0
evidence: 高效的查询感知框架，压缩长视频以进行下游理解
tldr: 针对长视频理解中多模态大模型受限于上下文窗口和视觉冗余的问题，本文提出Tempo框架。该框架利用小型视觉语言模型（SVLM）作为局部时间压缩器，通过自适应令牌分配（ATA）机制，根据查询意图动态分配带宽。在保持全局故事情节的同时，重点保留关键片段信息，实现了极高压缩率下的高性能，在长视频基准测试中超越了GPT-4o和Gemini 1.5 Pro。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08120v1/fig-001.webp\", \"caption\": \"Figure 1 Tempo achieves SOTA long video understanding via query-aware Adaptive Token Allocation (ATA). (a) Motivation:\", \"page\": 2, \"index\": 1, \"width\": 981, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.08120v1/fig-002.webp\", \"caption\": \"Figure 3 Scaling behavior of Tempo. We investigate the interplay between maximum frame capacity (fmax) and total visual token budgets. (Left) On Video-MME (Long), a strict 4K budget acts as an optimal sweet spot by aggressively filtering redundancy, whereas larger budgets (8K/12K) yield marginal noise. (Right) On the extreme-long LVBench, restrictive budgets eventually limit the achievable performance, whereas expansive capacities (e.g., 12K) monotonically unlock new peaks at higher frame densities, proving the necessity of scaled context for hour-long video understanding.\", \"page\": 10, \"index\": 2, \"width\": 964, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2604.08120v1/fig-003.webp\", \"caption\": \"Figure 2 Overview of the Tempo framework. Our unified architecture casts long video understanding as an end-to-end, query-aware compression process. The Local Compressor (Left). For each segment, a Small Vision-Language Model (SVLM) acts as a semantic temporal compressor. Under causal attention, learnable memory tokens M inherently distill the preceding visual tokens Xi and user query Q. Inference-Only Bypass (Middle). During a single forward pass, an Adaptive Token Allocation (ATA) controller intercepts the hidden state hrel i to compute a zero-shot relevance score si. This enables an O(1) dynamic head truncation, allocating dense bandwidth to query-critical segments while compressing redundancies into minimal temporal anchors to strictly satisfy a global budget Bmax. The Global Decoder (Right). The compressed memory tokens are assembled into a highly sparse, time-aware sequence using explicit temporal tags (e.g., <t=2.0s>). A global LLM synthesizes this condensed multimodal context to generate the final response.\", \"page\": 4, \"index\": 3, \"width\": 986, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.08120v1/fig-004.webp\", \"caption\": \"Table 2 Ablation studies on Tempo’s core components. We decompose our framework across five dimensions: (A) progressive training curriculum, (B) segment-level budget allocation, (C) intra-segment token reduction scheme, (D) relevance scoring source, and (E) temporal continuity. Unless otherwise specified, all variants process videos uniformly sampled at 2 FPS up to a maximum of 1024 frames, strictly bounded by an 8K visual token budget for fair comparison.\", \"page\": 9, \"index\": 4, \"width\": 978, \"height\": 739}]"
motivation: 现有的长视频理解方法（如稀疏采样）会盲目丢弃关键帧，且密集的视觉流容易导致模型超出上下文预算并产生信息丢失。
method: 提出Tempo框架，利用SVLM进行跨模态蒸馏压缩，并引入无需训练的ATA动态路由机制，根据查询相关性为不同视频片段分配差异化的令牌预算。
result: 在LVBench长视频测试中，Tempo以8K视觉预算取得52.3分，优于GPT-4o和Gemini 1.5 Pro，且在极高压缩比下仍保持领先。
conclusion: 真正的长视频理解应依赖于意图驱动的高效压缩，而非盲目扩大上下文窗口。
---

## 摘要
将多模态大语言模型（MLLMs）应用于长达一小时的视频时，受限于上下文长度。密集的视觉流会耗尽 token 预算，并加剧“迷失在中间”（lost-in-the-middle）现象。现有的启发式方法（如稀疏采样或均匀池化）盲目地牺牲了保真度，丢弃了关键时刻，并在无关背景上浪费了带宽。我们提出了 Tempo，这是一个高效的查询感知框架，用于压缩长视频以进行下游理解。Tempo 利用小型视觉语言模型（SVLM）作为局部时间压缩器，将 token 缩减视为一种早期的跨模态蒸馏过程，通过单次前向传播生成紧凑且符合意图的表示。为了在不破坏因果关系的情况下执行严格的预算，我们引入了自适应 Token 分配（ATA）。利用 SVLM 的零样本相关性先验和语义前载，ATA 充当了一个无需训练的 $O(1)$ 动态路由器。它为查询关键片段分配高带宽，同时将冗余内容压缩为极小的时间锚点，以维持全局故事情节。大量实验表明，我们的 6B 架构在激进的动态压缩（0.5-16 tokens/帧）下实现了最先进的性能。在超长视频基准 LVBench（4101秒）上，Tempo 在严格的 8K 视觉预算下获得了 52.3 分，优于 GPT-4o 和 Gemini 1.5 Pro。扩展到 2048 帧时，分数达到 53.7。至关重要的是，Tempo 将长达一小时的视频压缩到远低于理论极限的水平，证明了真正的长视频理解依赖于意图驱动的效率，而非贪婪填充的上下文窗口。

## Abstract
Adapting Multimodal Large Language Models (MLLMs) for hour-long videos is bottlenecked by context limits. Dense visual streams saturate token budgets and exacerbate the lost-in-the-middle phenomenon. Existing heuristics, like sparse sampling or uniform pooling, blindly sacrifice fidelity by discarding decisive moments and wasting bandwidth on irrelevant backgrounds. We propose Tempo, an efficient query-aware framework compressing long videos for downstream understanding. Tempo leverages a Small Vision-Language Model (SVLM) as a local temporal compressor, casting token reduction as an early cross-modal distillation process to generate compact, intent-aligned representations in a single forward pass. To enforce strict budgets without breaking causality, we introduce Adaptive Token Allocation (ATA). Exploiting the SVLM's zero-shot relevance prior and semantic front-loading, ATA acts as a training-free $O(1)$ dynamic router. It allocates dense bandwidth to query-critical segments while compressing redundancies into minimal temporal anchors to maintain the global storyline. Extensive experiments show our 6B architecture achieves state-of-the-art performance with aggressive dynamic compression (0.5-16 tokens/frame). On the extreme-long LVBench (4101s), Tempo scores 52.3 under a strict 8K visual budget, outperforming GPT-4o and Gemini 1.5 Pro. Scaling to 2048 frames reaches 53.7. Crucially, Tempo compresses hour-long videos substantially below theoretical limits, proving true long-form video understanding relies on intent-driven efficiency rather than greedily padded context windows.

---

## 论文详细总结（自动生成）

这是一份关于论文《Small Vision-Language Models are Smart Compressors for Long Video Understanding》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大语言模型（MLLM）在处理长视频（如一小时以上）时面临严重的**上下文窗口瓶颈**。
*   **研究动机**：
    *   **视觉冗余与饱和**：密集的视频流会迅速填满 LLM 的输入配额，导致“迷失在中间”（lost-in-the-middle）现象，使模型难以检索关键信息。
    *   **现有方案的缺陷**：稀疏采样（Sparse Sampling）容易漏掉瞬时关键帧；均匀池化（Uniform Pooling）则是查询无关的，会模糊关键细节并在无关背景上浪费计算资源。
*   **整体含义**：论文提出了一种名为 **Tempo** 的框架，主张长视频理解不应盲目追求超长上下文，而应通过“查询感知”的智能压缩，将带宽分配给真正相关的片段。

### 2. 方法论：Tempo 框架
*   **核心思想**：将视频压缩视为一个**早期跨模态蒸馏过程**。利用一个小型视觉语言模型（SVLM）作为局部压缩器，根据用户查询动态决定每个视频片段的保留信息量。
*   **关键技术细节**：
    1.  **两级架构**：由基于 SVLM 的“局部压缩器”（如 Qwen3-VL-2B）和基于 LLM 的“全局解码器”（如 Qwen3-4B）组成。
    2.  **查询感知压缩**：将用户查询 $Q$ 与视频片段 $X_i$ 一起输入 SVLM，通过因果注意力机制将语义信息蒸馏到少量的“内存令牌”（Memory Tokens）中。
    3.  **自适应令牌分配（ATA）**：
        *   **零样本相关性先验**：利用 SVLM 预训练中自带的能力，通过计算“Yes/No”回答的 Logit 差异生成相关性分数 $s_i$。
        *   **动态路由**：根据 $s_i$ 为相关片段分配高带宽（最高 16 tokens/帧），为无关片段分配极低带宽（约 0.5 tokens/帧），形成“时间锚点”以维持因果连续性。
        *   **语义前载与头部截断**：研究发现 SVLM 会将关键信息集中在前面的内存令牌中，因此通过简单的 $O(1)$ 头部截断即可实现高效压缩，无需复杂的空间池化。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **LongVideoBench**：评估长上下文交错视频理解。
    *   **MLVU**：多任务长视频理解基准。
    *   **Video-MME**：全面的视频分析评估，包含长视频子集。
    *   **LVBench**：针对超长视频（平均 4101 秒）的极端压力测试。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 1.5 Pro。
    *   **开源通用模型**：InternVL3.5, Qwen2.5-VL, Molmo2。
    *   **专用长视频模型**：VideoChat-Flash, LongVA, LongVU, Storm 等。

### 4. 资源与算力
*   **硬件设备**：使用了 **64 张 NVIDIA H100 (80GB) GPU** 组成的计算集群。
*   **训练流程**：采用四阶段渐进式课程学习：
    1.  阶段 0：模态对齐（LCS-558K 数据集）。
    2.  阶段 1：大规模预训练（约 3.5M 样本）。
    3.  阶段 2：多样化指令微调（SFT）。
    4.  阶段 3：长上下文 SFT（专门针对长序列外推进行微调）。
*   **算力细节**：使用了 FSDP（全分片数据并行）和混合精度（bfloat16）训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流长视频基准上进行了全面测试。
    *   **消融实验非常充分**：涵盖了训练课程阶段、片段预算分配策略（均匀 vs 随机 vs ATA）、令牌减少方案（头部截断 vs 尾部截断 vs Token Merging）、相关性来源等 5 个维度的深度对比。
*   **客观性与公平性**：
    *   在相同的 8K 视觉令牌预算下对比了多种 SOTA 模型。
    *   不仅报告了最高性能，还详细分析了不同帧数（fmax）和令牌预算（Bmax）下的扩展行为（Scaling Behavior）。

### 6. 主要结论与发现
*   **“少即是多”（Less is More）**：在某些任务中，4K 预算的表现甚至优于 8K，证明了严格的压缩能起到“去噪”作用，减轻 LLM 的注意力稀释。
*   **性能领先**：仅 6B 参数的 Tempo 在 LVBench 上得分 52.3，显著超过了 GPT-4o (30.8) 和 Gemini 1.5 Pro (33.1)。
*   **意图驱动的效率**：Tempo 实际消耗的令牌数往往远低于设定的上限，证明其能根据语义必要性自主分配资源。

### 7. 优点
*   **高效性**：通过 $O(1)$ 的张量切片（头部截断）实现压缩，无额外推理开销。
*   **端到端统一**：将相关性评分与特征提取集成在 SVLM 的单次前向传播中，避免了额外的检索模块。
*   **因果保留**：通过“时间锚点”机制，即使是无关片段也保留极少量令牌，确保了模型不会丢失时间定位感。

### 8. 不足与局限
*   **多轮对话限制**：目前的查询感知压缩是针对单次查询优化的。如果用户在对话中改变意图，可能需要重新对视频进行压缩处理，增加了交互延迟。
*   **推理开销**：虽然压缩了输入 LLM 的令牌，但前端 SVLM 仍需处理原始视频帧，对于极长视频，前端处理的计算量依然存在。
*   **训练依赖**：高性能依赖于高质量的四阶段课程学习，训练过程对数据配比和阶段转换较为敏感。

（完）
