---
title: "UniversalVTG: A Universal and Lightweight Foundation Model for Video Temporal Grounding"
title_zh: UniversalVTG：一种用于视频时序定位的通用且轻量级的基础模型
authors: "Joungbin An, Agrim Jain, Kristen Grauman"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08522v1"
tags: ["query:lvu"]
score: 9.0
evidence: 针对长视频上下文的高效定位头
tldr: UniversalVTG是一个通用且轻量级的视频时刻定位（VTG）基础模型。针对现有模型跨域迁移能力差以及多模态大模型（MLLM）计算成本高、长视频处理受限的问题，该研究通过大规模跨数据集预训练，引入离线查询统一器标准化异构查询，并结合高效定位头。该模型在多个基准测试中刷新了SOTA记录，且参数量比MLLM小100倍以上，实现了性能与效率的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08522v1/fig-001.webp\", \"caption\": \"Table 3: Effect of joint training and language unification. All settings use the same visual encoder and grounding head (HieraMamba); only the training regime differs. Performance is reported as Avg. R@1 (higher is better). ∆ denotes relative change compared to dataset-specific training.\", \"page\": 8, \"index\": 1, \"width\": 730, \"height\": 784}, {\"url\": \"assets/figures/arxiv/2604.08522v1/fig-002.webp\", \"caption\": \"Fig. 2: UniversalVTG Framework. A single, lightweight model generalizes across heterogeneous video domains and query styles. (Left) Diverse videos are mapped to a Shared Representation via an efficient backbone, while a Query Unifier canonicalizes multi-style text inputs into a Unified Semantic Space. (Right) With both modalities standardized, one grounding head localizes events across varied viewpoints (ego/exo), durations (short/long), and linguistic forms (e.g., questions, declarations). UniversalVTG achieves real-time inference (∼10ms/query) suitable for long-form deployment.\", \"page\": 5, \"index\": 2, \"width\": 719, \"height\": 277}]"
motivation: 旨在解决现有VTG模型难以跨领域迁移以及大语言模型在处理长视频时刻定位时计算开销过大的问题。
method: 通过离线查询统一器将异构查询标准化到共享声明空间，并采用轻量级架构进行大规模跨数据集统一监督训练。
result: 在GoalStep、Ego4D-NLQ等五个主流基准测试中均达到SOTA，且以百分之一的参数量匹配或超越了基于MLLM的方法。
conclusion: UniversalVTG证明了通过统一查询空间和大规模预训练，轻量级模型也能在通用视频时刻定位任务中展现出极强的竞争力和实用性。
---

## 摘要
视频时序定位（VTG）通常采用针对特定数据集的模型来解决，这些模型在跨领域和查询样式时的迁移能力较差。最近为克服这一限制，研究者尝试将大型多模态语言模型（MLLM）应用于 VTG，但其高昂的计算成本和有限的视频上下文仍阻碍了长视频定位。与之不同，我们在保持模型轻量化的同时，扩展了统一监督的规模。我们提出了 UniversalVTG，这是一个通过大规模跨数据集预训练得到的单一 VTG 模型。一个离线查询统一器（Query Unifier）将异构的查询格式规范化为共享的陈述性空间，从而减少了语言不匹配，并防止了在朴素联合训练中观察到的负迁移现象。结合高效的定位头，UniversalVTG 可以扩展到长且未经剪辑的视频。在 GoalStep-StepGrounding、Ego4D-NLQ、TACoS、Charades-STA 和 ActivityNet-Captions 等多个基准测试中，单个 UniversalVTG 检查点在与专用 VTG 模型的对比中均实现了最先进的性能。此外，尽管 UniversalVTG 的参数量比最近基于 MLLM 的方法小 100 倍以上，但它在多个基准测试上的准确率与之持平甚至更高，为参数密集的 MLLM 提供了一个实用的替代方案。

## Abstract
Video temporal grounding (VTG) is typically tackled with dataset-specific models that transfer poorly across domains and query styles. Recent efforts to overcome this limitation have adapted large multimodal language models (MLLMs) to VTG, but their high compute cost and limited video context still hinder long-video grounding. We instead scale unified supervision while keeping the model lightweight. We present UniversalVTG, a single VTG model trained with large-scale cross-dataset pretraining. An offline Query Unifier canonicalizes heterogeneous query formats into a shared declarative space, reducing linguistic mismatch and preventing the negative transfer observed under naïve joint training. Combined with an efficient grounding head, UniversalVTG scales to long, untrimmed videos. Across diverse benchmarks-GoalStep-StepGrounding, Ego4D-NLQ, TACoS, Charades-STA, and ActivityNet-Captions-one UniversalVTG checkpoint achieves state-of-the-art performance versus dedicated VTG models. Moreover, despite being $>100\times$ smaller than recent MLLM-based approaches, UniversalVTG matches or exceeds their accuracy on multiple benchmarks, offering a practical alternative to parameter-heavy MLLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniversalVTG: A Universal and Lightweight Foundation Model for Video Temporal Grounding》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：视频时序定位（VTG）旨在根据自然语言查询定位视频中的起止时间。
*   **核心痛点**：
    1.  **领域碎片化**：现有模型多针对特定数据集设计，跨领域（如第一人称 vs 第三人称）和跨查询样式（如提问 vs 描述）的迁移能力极差。
    2.  **MLLM 的局限性**：虽然多模态大模型（MLLM）具有泛化性，但其参数量巨大、计算成本高，且受限于上下文窗口，难以处理长达数十分钟的未经剪辑视频。
*   **研究目标**：开发一个单一、轻量级且通用的基础模型，能够同时处理不同领域、不同长度的视频，并保持极高的推理效率。

### 2. 方法论
*   **核心思想**：通过“语义规范化”解决跨数据集训练中的“负迁移”问题，并结合高效的线性复杂度架构处理长视频。
*   **关键技术细节**：
    1.  **查询统一器（Query Unifier）**：引入离线 LLM（如 GPT-5 或 Llama 3.1）将异构的查询（如疑问句、祈使句、短语碎片）统一转换为标准的陈述性过去时态空间。这消除了语言风格歧义，使模型能专注于核心的时空对齐。
    2.  **视觉编码器**：采用 PerceptionEncoder，这是一种轻量级图像级编码器，在保持低计算量（比 InternVideo 低 7.6 倍）的同时提供强大的每帧语义。
    3.  **时序定位头（HieraMamba）**：基于状态空间模型（SSM）的架构，具有线性计算复杂度，能够高效处理长视频序列。
    4.  **两阶段训练流程**：
        *   **阶段 I（大规模预训练）**：在包含 116 万个查询-片段对的混合数据集（NaQ, Momentor 等）上进行预训练。
        *   **阶段 II（联合微调）**：在五个目标基准数据集上使用统一的权重进行联合训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **长视频**：GoalStep（平均 26 分钟）、Ego4D-NLQ（第一人称）、TACoS（烹饪场景）。
    *   **短视频**：Charades-STA（室内活动）、ActivityNet-Captions（开放领域）。
*   **Benchmark 与对比方法**：
    *   **专用 VTG 模型**：2D-TAN, Moment-DETR, SnAG, DeCafNet, HieraMamba 等。
    *   **基于 MLLM 的方法**：Qwen2.5-VL-7B, TimeChat-7B, UniTime-7B, ED-VTG-7B 等。
*   **评价指标**：Recall@1 和 Recall@5 在不同 IoU 阈值下的表现。

### 4. 资源与算力
*   **训练算力**：
    *   **阶段 I**：使用 8 张 GPU 进行大规模预训练。
    *   **阶段 II**：仅需 1 张 GPU 进行联合微调。
*   **推理效率**：在单张 NVIDIA A40 GPU 上，处理 500 秒视频的定位头推理时间仅为 ~0.09 秒，查询统一耗时约 0.34 秒（使用 Qwen3-4B）。
*   **模型规模**：参数量仅为 60M（0.06B），比主流 MLLM（7B）小 100 倍以上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流基准测试，进行了详尽的跨数据集迁移矩阵实验（Table S3）。
*   **消融实验**：针对查询统一器的必要性、预训练的影响、不同 LLM 作为统一器的效果进行了多组对比。
*   **充分性评价**：实验设计非常充分且客观。通过“跨数据集评估矩阵”清晰地展示了传统模型在未见领域上的崩溃，以及 UniversalVTG 如何通过统一监督克服这一问题。对比实验涵盖了从传统小模型到最新大模型的所有主流路线，具有很强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：单个 UniversalVTG 权重在所有五个基准测试中均达到或超过了针对特定数据集训练的专家模型。
*   **以小博大**：60M 参数的模型在长视频定位任务上优于或持平 7B 参数的 UniTime 和 Qwen2.5-VL。
*   **解决负迁移**：实验证明，简单的联合训练会导致性能下降，而通过“查询统一器”进行语义规范化是解锁跨域泛化能力的关键。
*   **长视频优势**：由于采用了线性复杂度的定位头，该模型在处理 20 分钟以上的视频（如 GoalStep）时表现出极强的鲁棒性，而许多 MLLM 因内存限制无法处理此类视频。

### 7. 优点（亮点）
*   **极高的实用性**：模型轻量、推理快（~10ms/query），非常适合实时部署和长视频检索。
*   **创新的数据处理**：提出“查询统一器”这一离线预处理步骤，巧妙地利用 LLM 的语言能力解决了视觉任务中的多模态冲突，且不增加在线推理的视觉计算负担。
*   **真正的通用性**：打破了“一个数据集一个模型”的僵局，实现了单一检查点适配多种视角和时长。

### 8. 不足与局限
*   **状态连续性挑战**：在处理跨镜头剪辑（Camera Cuts）时，模型有时会丢失目标的身份一致性，导致定位片段断裂。
*   **复杂推理限制**：对于需要深层逻辑推理（如理解动作的前因后果或复杂的程序性步骤）而非单纯视觉匹配的任务，模型仍有提升空间。
*   **离线依赖**：虽然查询统一器开销小，但仍依赖于 LLM 的预处理，对于完全无法接入 LLM 的极端嵌入式环境可能存在限制。

（完）
