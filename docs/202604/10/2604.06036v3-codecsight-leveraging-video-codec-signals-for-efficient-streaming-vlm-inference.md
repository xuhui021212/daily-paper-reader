---
title: "CodecSight: Leveraging Video Codec Signals for Efficient Streaming VLM Inference"
title_zh: CodecSight：利用视频编解码信号实现高效的流式 VLM 推理
authors: "Yulin Zou, Yan Chen, Wenyan Chen, JooYoung Park, Shivaraman Nitin, Luo Tao, Francisco Romero, Dmitrii Ustiugov"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06036v3"
tags: ["query:lvu"]
score: 9.0
evidence: 利用视频编解码信号进行高效流式 VLM 推理
tldr: 针对视频大模型（VLM）流式推理成本高昂且现有冗余消除方法开销大的问题，本文提出CodecSight。该系统创新性地利用视频编解码器自带的元数据（如运动矢量）作为低成本运行时信号，统一优化视频解码、视觉处理和LLM预填充。通过编解码引导的补丁剪枝和选择性KV缓存刷新，CodecSight实现了全在线的端到端加速，无需离线训练，在保持高精度的同时显著提升了推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06036v3/fig-001.webp\", \"caption\": \"Figure 8. System architecture overview of CodecSight.\", \"page\": 6, \"index\": 1, \"width\": 501, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.06036v3/fig-002.webp\", \"caption\": \"Figure 9. Illustration of the motion-guided token pruning policy. Each (2 × 2) group of neighboring patches is projected into a vision token. A token is retained if any of its constituent patches are marked dynamic, otherwise it will be discarded.\", \"page\": 7, \"index\": 2, \"width\": 502, \"height\": 266}]"
motivation: 现有的视频冗余消除技术计算开销高且缺乏端到端优化，难以满足实时视频大模型推理的需求。
method: 利用视频压缩过程中产生的编解码元数据来指导ViT补丁剪枝和LLM的KV缓存刷新，实现高效的流式推理。
result: "实验表明该系统可提升高达3倍的吞吐量，减少87%的GPU计算开销，且准确率损失极小。"
conclusion: CodecSight通过深度结合编解码信号与模型推理过程，为高效、实时的视频大模型服务提供了新路径。
---

## 摘要
视频流分析是视觉语言模型（VLM）服务中的关键工作负载，但多模态推理的高昂成本限制了其可扩展性。现有系统通过利用视频流中的时空冗余来降低推理成本，但它们往往仅针对视觉 Transformer (ViT) 或大语言模型 (LLM) 进行局部优化，未能充分挖掘端到端的优化潜力。此外，现有方法在识别冗余时会产生显著开销，无论是通过离线分析和训练，还是通过昂贵的在线计算，这使得它们难以适应动态的实时视频流。我们提出了 CodecSight，这是一个由编解码器引导的流式视频分析系统。该系统基于一个关键观察：视频编解码器在压缩过程中已经作为副产品提取了每个视频流的时空结构。CodecSight 将这些编解码器元数据视为低成本的运行时信号，从而统一了视频解码、视觉处理和 LLM 预填充（prefilling）阶段的优化；同时，直接在压缩比特流上操作也带来了减少传输开销的固有优势。这驱动了 ViT 编码前的编解码器引导式补丁剪枝（patch pruning），以及 LLM 预填充期间的选择性键值（KV）缓存刷新。这两种机制都是完全在线的，且无需离线训练。实验表明，与最先进的基准方法相比，CodecSight 的吞吐量提升了高达 3 倍，GPU 计算量减少了高达 87%，同时保持了具有竞争力的准确率，F1 分数仅下降 0~8%。

## Abstract
Video streaming analytics is a crucial workload for vision-language model serving, but the high cost of multimodal inference limits scalability. Prior systems reduce inference cost by exploiting temporal and spatial redundancy in video streams, but they target either the vision transformer (ViT) or the LLM with a limited view, leaving end-to-end opportunities untapped. Moreover, existing methods incur significant overhead to identify redundancy, either through offline profiling and training or costly online computation, making them ill-suited for dynamic real-time streams.   We present CodecSight, a codec-guided streaming video analytics system, built on a key observation that video codecs already extract the temporal and spatial structure of each stream as a byproduct of compression. CodecSight treats this codec metadata as a low-cost runtime signal to unify optimization across video decoding, visual processing, and LLM prefilling, with transmission reduction as an inherent benefit of operating directly on compressed bitstreams. This drives codec-guided patch pruning before ViT encoding and selective key-value cache refresh during LLM prefilling, both of which are fully online and do not require offline training. Experiments show that CodecSight achieves an improvement in throughput of up to 3$\times$, and a reduction of up to 87% in GPU compute over state-of-the-art baselines, maintaining competitive accuracy with only 0$\sim$8% F1 drop.

---

## 论文详细总结（自动生成）

这是一份关于论文《CodecSight: Leveraging Video Codec Signals for Efficient Streaming VLM Inference》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着城市监控（CCTV）数量爆炸式增长，使用视觉语言模型（VLM）进行实时视频流分析面临巨大的计算成本挑战。
*   **核心问题**：
    *   **时空冗余**：视频流中相邻帧及滑动窗口之间存在大量重复内容，导致重复计算。
    *   **现有系统局限**：目前的优化方法（如针对 ViT 或 LLM 的局部优化）往往孤立存在，且依赖昂贵的离线分析或在线计算来识别冗余，难以应对动态变化的实时流。
*   **整体含义**：论文提出 **CodecSight**，旨在利用视频编解码器（Codec）在压缩过程中产生的“免费”元数据（如运动矢量和残差），作为低成本信号来统一优化视频解码、视觉编码（ViT）和语言解码（LLM）的全流程。

### 2. 方法论：核心思想与关键技术
CodecSight 的核心思想是将编解码信号转化为模型推理的优化指令，实现“编解码引导”的推理。
*   **硬件加速编解码处理**：利用 GPU 硬件解码器（如 NVIDIA NVDEC）进行单次解码并提取元数据，避免滑动窗口导致的重复解码，并直接在 GPU 内存中进行预处理。
*   **运动矢量引导的补丁剪枝（Token Pruning）**：
    *   **原理**：提取 P 帧中的运动矢量（MV）和残差。若某区域 MV 较小且残差低，则视为静态/可预测区域。
    *   **操作**：将块级 MV 映射到 ViT 的补丁（Patch）网格上，生成动态掩码。在 GOP（图像组）内累积掩码，仅对动态补丁进行 ViT 编码，大幅减少视觉 Token 数量。
*   **选择性 KV 缓存（KVC）刷新**：
    *   **挑战**：滑动窗口重叠部分虽然图像相同，但由于上下文和位置变化，直接复用 KV 缓存会导致语义漂移。
    *   **方案**：利用编解码帧类型（I/P 帧）进行选择性刷新。将 I 帧作为“锚点 Token”进行重新计算（Refresh），而对 P 帧 Token 进行复用（Reuse）。
    *   **位置校正**：对复用的 Token 应用基于 RoPE 的旋转位置编码校正，确保其在不同窗口位置的语义一致性。

### 3. 实验设计
*   **数据集**：**UCF-Crime**（包含 1900 个真实世界监控视频，涵盖多种异常场景和运动特征）。
*   **模型（Benchmark）**：
    *   **InternVL3-14B**（基于 Qwen2.5-14B）。
    *   **Qwen3-VL-32B-Instruct**。
*   **对比方法（Baselines）**：
    *   **Full Comp**：无优化的标准 vLLM 推理。
    *   **Déjà Vu**：针对 ViT 编码优化的视频查询引擎。
    *   **CacheBlend**：针对 RAG 任务优化的 KVC 管理方案。
    *   **VLCache**：针对多模态输入优化的缓存复用框架。

### 4. 资源与算力
*   **硬件环境**：配备 4 张 **NVIDIA A100 (40GB, SXM4)** GPU 的服务器，使用 NVLink 互联。
*   **配置细节**：
    *   InternVL3 使用 2 张 GPU（张量并行 TP=2）。
    *   Qwen3-VL 使用 4 张 GPU（张量并行 TP=4）。
*   **算力消耗**：文中强调了 CodecSight 显著降低了算力需求，例如在 InternVL3 上减少了约 **87% 的 FLOPs**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **端到端性能测试**：对比了不同模型下的延迟加速比和准确率（Precision, Recall, F1）。
    *   **消融实验**：分别验证了“补丁剪枝”和“KVC 复用”对性能和精度的贡献。
    *   **敏感性分析**：测试了滑动窗口步长（Stride）、运动矢量阈值（MV Threshold）和 GOP 大小对结果的影响。
    *   **不同运动强度分析**：将视频分为低、中、高运动等级，验证系统的鲁棒性。
*   **充分性评价**：实验设计较为全面，涵盖了从底层算力（FLOPs）到顶层语义（F1 分数）的多个维度，且在不同架构的 VLM 上均有验证，客观性较强。

### 6. 主要结论与发现
*   **吞吐量提升**：相比基准线，实现了高达 **3 倍** 的端到端延迟缩减（即吞吐量提升 3 倍）。
*   **算力节省**：GPU 计算开销减少了 **87%**，传输开销降低了约 2.12 倍。
*   **精度保持**：在大幅加速的同时，F1 分数仅下降了 **0~8%**，在实际监控任务中具有极高的可用性。
*   **低开销**：CodecSight 自身的优化逻辑（剪枝决策和缓存管理）仅占总延迟的约 4% 左右。

### 7. 优点（亮点）
*   **全在线且无需训练**：不需要昂贵的离线 Profile 或针对特定数据集的重新训练，具有极强的通用性和即插即用性。
*   **端到端协同**：打破了以往只优化 ViT 或只优化 LLM 的孤岛模式，将编解码信号贯穿整个推理流水线。
*   **硬件友好**：直接利用现有的硬件解码单元（NVDEC），充分发挥了异构计算的优势。

### 8. 不足与局限
*   **编解码器依赖**：目前原型主要针对 H.264 和 NVIDIA 硬件，虽然理论上可移植到 H.265 或 AV1，但不同编解码器的元数据提取接口可能存在差异。
*   **高动态场景挑战**：在极高运动强度的视频中，由于冗余度降低，剪枝和复用的收益会收缩（实验显示高运动视频加速比降至 2.49x）。
*   **语义敏感度**：对于某些极细微的动作（如远距离微小位移），单纯依靠编解码器的运动矢量可能会产生误剪枝，导致精度损失。

（完）
