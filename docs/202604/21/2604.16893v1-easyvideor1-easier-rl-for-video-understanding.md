---
title: "EasyVideoR1: Easier RL for Video Understanding"
title_zh: EasyVideoR1：更简便的视频理解强化学习框架
authors: "Chuanyu Qin, Chenxu Yang, Qingyi Si, Naibin Gu, Dingyu Yao, Zheng Lin, Peng Fu, Nan Duan, Jiaqi Wang"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16893v1"
tags: ["query:lvu"]
score: 9.0
evidence: 用于视频理解和推理能力的强化学习
tldr: 本研究针对大语言模型在视频理解任务中应用强化学习（RLVR）面临的计算开销大、任务多样及评估难等挑战，提出了EasyVideoR1框架。该框架通过离线预处理与张量缓存技术提升了1.47倍吞吐量，设计了涵盖11种任务的奖励系统，并结合离线-在线混合训练与图文联合训练模式。实验表明，该框架在22个主流视频理解基准测试中表现优异，显著降低了视频RL训练的门槛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16893v1/fig-001.webp\", \"caption\": \"Figure 3 Training efficiency comparison between cache-based loading and on-the-fly video decoding. (a) Per-step time breakdown by pipeline phase. Cache-based loading reduces rollout generation time by 1.5× and reference model forward time by 2.9×, while actor update time remains unchanged. (b) Overall efficiency: cache-based loading achieves a 1.47× speedup in both wall-clock time per step and token throughput.\", \"page\": 10, \"index\": 1, \"width\": 960, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2604.16893v1/fig-002.webp\", \"caption\": \"Table 2 Video understanding benchmarks supported by the evaluation framework.\", \"page\": 8, \"index\": 2, \"width\": 840, \"height\": 773}, {\"url\": \"assets/figures/arxiv/2604.16893v1/fig-003.webp\", \"caption\": \"Figure 1 Overview of the EasyVideoR1 training pipeline. Videos are preprocessed offline into .pt cache files. During training, each worker loads cached frames locally.\", \"page\": 4, \"index\": 3, \"width\": 873, \"height\": 605}, {\"url\": \"assets/figures/arxiv/2604.16893v1/fig-004.webp\", \"caption\": \"Figure 2 Benchmark performance comparison. The number above each blue bar indicates the accuracy change relative to the Instruct baseline. Background colors denote benchmark categories. EasyVideoR1 training yields an average improvement of +2.3 points, with the largest gains on reasoning (+6.6 on Video-Holmes) and mathematical (+6.7 on VideoMathQA) tasks.\", \"page\": 9, \"index\": 4, \"width\": 959, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.16893v1/fig-005.webp\", \"caption\": \"Table 1 Supported task types and their accuracy scoring methods.\", \"page\": 5, \"index\": 5, \"width\": 648, \"height\": 300}]"
motivation: 现有的强化学习框架缺乏针对视频模态的高效优化，且视频理解任务存在计算开销大和评估困难等问题。
method: 提出了包含离线预处理、任务感知奖励系统、混合训练范式及图文联合训练的完整视频RL训练框架。
result: 实现了1.47倍的训练吞吐量提升，并在22个主流视频理解基准测试上达到了与官方报告一致的高精度。
conclusion: EasyVideoR1为大视觉语言模型在视频理解领域的强化学习训练提供了高效、全面且可复现的系统化解决方案。
---

## 摘要
基于可验证奖励的强化学习（RLVR）在提升大语言模型推理能力方面表现出显著成效。随着模型演进为原生多模态架构，将 RLVR 扩展到视频理解领域变得日益重要，但由于视频任务类型的多样性、重复解码和预处理高维视觉输入的计算开销，以及在众多敏感超参数下进行可重复评估的难度，这一领域在很大程度上仍未被充分探索。现有的开源强化学习训练框架为文本和图像场景提供了坚实的基础设施，但缺乏针对视频模态的系统性优化。在这项工作中，我们提出了 EasyVideoR1，这是一个完整且高效的强化学习框架，专门为在视频理解任务上训练大型视觉语言模型而设计。EasyVideoR1 的主要贡献包括：（1）一个完整的视频强化学习训练流水线，通过离线预处理和张量缓存消除了冗余的视频解码，使吞吐量提升了 1.47 倍；（2）一个全面的任务感知奖励系统，涵盖了 11 种不同的视频和图像问题类型，并具有统一的路由和模块化扩展；（3）一种混合离线-在线数据训练范式，将精选的高质量轨迹与同策略（on-policy）探索相结合，有利于更具挑战性任务的学习；（4）支持独立配置像素预算的图像-视频联合训练，使两种模态能够相互增强；（5）一个涵盖 22 个主流视频理解基准测试的异步多基准评估框架，其复现的准确率与官方报告的分数高度一致。

## Abstract
Reinforcement learning from verifiable rewards (RLVR) has demonstrated remarkable effectiveness in improving the reasoning capabilities of large language models. As models evolve into natively multimodal architectures, extending RLVR to video understanding becomes increasingly important yet remains largely unexplored, due to the diversity of video task types, the computational overhead of repeatedly decoding and preprocessing high-dimensional visual inputs, and the difficulty of reproducible evaluation across numerous sensitive hyperparameters. Existing open-source RL training frameworks provide solid infrastructure for text and image scenarios but lack systematic optimizations tailored for video modality. In this work, we present \textbf{EasyVideoR1}, a complete and efficient reinforcement learning framework specifically designed for training large vision-language models on video understanding tasks. EasyVideoR1 makes the following contributions: (1) a full video RL training pipeline with offline preprocessing and tensor caching that eliminates redundant video decoding and yields a 1.47 $\times$ throughput improvement; (2) a comprehensive, task-aware reward system covering 11 distinct video and image problem types with unified routing and modular extension; (3) a mixed offline-online data training paradigm that combines curated high-quality trajectories with on-policy exploration, benefiting the learning of more challenging tasks; (4) joint image-video training with independently configurable pixel budgets, allowing the two modalities to mutually reinforce each other; and (5) an asynchronous multi-benchmark evaluation framework covering 22 mainstream video understanding benchmarks, with reproduced accuracy closely aligned with officially reported scores.

---

## 论文详细总结（自动生成）

以下是对论文《EasyVideoR1: Easier RL for Video Understanding》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效、系统地将基于可验证奖励的强化学习（RLVR，如 DeepSeek-R1 模式）扩展到视频理解领域。
*   **研究动机**：
    *   **计算瓶颈**：视频数据维度高，重复的解码和预处理（缩放、采样）在 RL 训练的多个阶段（Rollout、Actor 训练、参考模型前向）造成了巨大的 CPU/IO 负担。
    *   **任务复杂性**：视频任务涵盖选择题、时空定位、目标追踪等多种类型，缺乏统一且模块化的奖励计算系统。
    *   **评估困难**：视频评估对超参数（如 FPS、分辨率）极度敏感，现有框架缺乏能复现官方精度的标准化、高吞吐评估工具。
    *   **现有框架不足**：如 EasyR1 或 OneThinker 在视频模态上缺乏针对性优化，导致训练效率低下。

### 2. 方法论：核心思想与关键技术
EasyVideoR1 在 EasyR1 和 veRL 的基础上进行了系统性扩展：
*   **视频缓存机制（Video Caching）**：将视频预先解码、采样并调整分辨率后存储为 `.pt` 张量文件。训练时直接加载张量，避免了每步重复解码，通过元数据一致性确保各阶段处理逻辑统一。
*   **混合模态流水线适配**：
    *   **Dummy Tensors**：解决 FSDP 在处理混合图像-视频微批次时，因模态分支不活跃导致的梯度同步失败问题。
    *   **独立分辨率预算**：允许为图像和视频分别设置不同的像素上限（如图像追求高分辨率，视频追求多帧数）。
*   **任务感知奖励系统**：内置 11 种任务模块（如 MCQ、时空定位、数学、OCR 等），通过统一路由根据问题类型自动分配评分逻辑。
*   **混合离线-在线训练**：支持在单次迭代中混合使用预收集的高质量轨迹（离线）和模型自主生成的响应（在线），缓解 RL 训练初期的“冷启动”问题。
*   **异步评估框架**：基于 vLLM 的 `AsyncLLMEngine`，实现数据加载、Prefill 和 Decode 的全异步流水线，并结合分块 Prefill（Chunked Prefill）防止长视频垄断 GPU。

### 3. 实验设计
*   **基座模型**：主要使用 **Qwen3-VL-8B-Instruct**。
*   **训练数据**：约 10 万条视频样本，涵盖多个开源 RL 数据集，并经过 Pass-rate 过滤（保留模型处于“学习边界”的样本）。
*   **Benchmark**：
    *   **训练评估**：选取了 10 个代表性基准（如 Video-MME, MVBench, LVBench, VideoMathQA 等）。
    *   **框架支持**：评估框架共集成了 **22 个** 主流视频理解基准。
*   **对比对象**：Instruct 基座模型、官方发布的 Thinking 变体（Qwen3-VL-8B-Think）。

### 4. 资源与算力
*   **硬件配置**：使用了 **32 台 NVIDIA H200 GPU**。
*   **训练时长**：约 **20 小时**（完成 200 个 GRPO 训练步数）。
*   **吞吐量提升**：相比实时解码模式，缓存机制带来了 **1.47 倍** 的整体吞吐量提升。

### 5. 实验数量与充分性
*   **实验规模**：论文在 10 个核心基准上验证了性能提升，并在 32 卡规模下进行了详细的吞吐量对比实验。
*   **充分性**：实验覆盖了从通用理解、长视频到 STEM 知识和推理等多个维度，结果显示 RL 训练在推理和数学任务上提升最显著（+6.6 到 +6.7 分）。
*   **客观性**：通过与官方 Thinking 模型对比，证明了该框架能让 Instruct 模型在不增加推理开销的情况下达到甚至超过专门推理模型的水平。

### 6. 主要结论与发现
*   **RL 显著增强视频推理**：通过可验证奖励，模型在视频逻辑推理和数学计算上的能力大幅提升。
*   **效率优化至关重要**：离线缓存和异步评估是实现大规模视频 RL 训练的前提，能显著降低 GPU 等待 CPU 解码的时间。
*   **Instruct 潜力巨大**：经过 EasyVideoR1 训练的 8B 模型在多个指标上超越了官方的 Thinking 版本，且保持了标准推理模式的简洁性。

### 7. 优点与亮点
*   **系统性优化**：不仅关注算法，更深入底层 IO 和预处理，解决了视频 RL 训练中最实际的“慢”问题。
*   **高度集成与易用**：一键式支持 22 个基准测试，且复现精度高，极大降低了社区研究门槛。
*   **灵活的训练范式**：支持图文联合训练和离线数据引导，适应了当前多模态数据分布不均的现状。

### 8. 不足与局限
*   **存储开销**：虽然提升了速度，但将视频存储为未压缩张量会占用巨大的磁盘空间（相比压缩视频格式高出几个数量级）。
*   **奖励局限性**：目前仍依赖于“可验证奖励”（如客观题、公式、代码），对于主观性较强的开放式视频描述任务，奖励建模依然困难。
*   **模型覆盖**：虽然支持 Qwen 系列，但对于其他架构（如基于 Transformer-World Model 的模型）的适配性尚未广泛验证。

（完）
