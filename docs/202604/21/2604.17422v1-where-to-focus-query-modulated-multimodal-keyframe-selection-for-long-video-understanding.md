---
title: "Where to Focus: Query-Modulated Multimodal Keyframe Selection for Long Video Understanding"
title_zh: 聚焦何处：面向长视频理解的查询调制多模态关键帧选择
authors: "Shaoguang Wang, Weiyu Guo, Ziyang Chen, Xuming Hu, Hui Xiong"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17422v1"
tags: ["query:lvu"]
score: 10.0
evidence: 长视频理解中的高效关键帧选择
tldr: 针对长视频理解中关键帧选择效率低且易受模态噪声干扰的问题，本文提出Q-Gate框架。该框架无需训练，通过解耦视觉定位、全局匹配和上下文对齐三个专家流，并利用大语言模型动态评估查询意图，实现查询调制的门控机制。Q-Gate能智能分配权重并抑制无关模态噪声，在LongVideoBench等基准测试中显著提升了多模态大模型的长视频推理性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17422v1/fig-001.webp\", \"caption\": \"Figure 3: Impact of Softmax temperature 𝜏 on LongVideoBench. Performance peaks at 𝜏 = 0.5. This\", \"page\": 7, \"index\": 1, \"width\": 466, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.17422v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of the Q-Gate framework. Given a video and a user query, Q-Gate first computes multi-granularity scores\", \"page\": 4, \"index\": 2, \"width\": 1046, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2604.17422v1/fig-003.webp\", \"caption\": \"Figure 5: Qualitative visualization of Q-Gate’s dynamic strategy. Top: For a detail-oriented query, Q-Gate assigns high weight to\", \"page\": 8, \"index\": 3, \"width\": 1052, \"height\": 476}, {\"url\": \"assets/figures/arxiv/2604.17422v1/fig-004.webp\", \"caption\": \"Figure 6: Internal fusion dynamics for a subtitle-anchored query. Bottom Row: Guided by dynamic weights (𝑤𝑐𝑜𝑛𝑡𝑒𝑥𝑡 = 0.50,𝑤𝑔𝑟𝑜𝑢𝑛𝑑𝑖𝑛𝑔 = 0.40), the final fusion effectively suppresses\", \"page\": 8, \"index\": 4, \"width\": 492, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2604.17422v1/fig-005.webp\", \"caption\": \"Figure 7: Trade-off between end-to-end processing time and overall accuracy on LongVideoBench (𝐾 = 8 on GPT4o). Q-\", \"page\": 8, \"index\": 5, \"width\": 439, \"height\": 332}]"
motivation: 现有的关键帧选择方法多依赖单一视觉指标或静态融合，难以应对复杂剧情查询且易引入模态噪声。
method: 提出Q-Gate框架，利用LLM作为门控机制动态调节视觉定位、全局匹配和上下文对齐三个专家流的权重。
result: 在LongVideoBench和Video-MME等多个基准测试中，Q-Gate在多种模型底座上均显著优于现有最先进方法。
conclusion: Q-Gate通过动态模态路由有效抑制了噪声，为可扩展的长视频推理提供了一种鲁棒且可解释的即插即用方案。
---

## 摘要
长视频理解对于多模态大语言模型（MLLMs）而言仍是一项艰巨挑战，原因在于处理密集帧序列的计算成本极高。现有的解决方案通常选择关键帧子集，它们往往依赖于单一的以视觉为中心的指标（如 CLIP 相似度）或启发式评分的静态融合。这种“一刀切”的范式经常失效：仅视觉指标对于情节驱动的叙事查询无效，而无差别地引入文本评分则会为纯视觉任务引入严重的“模态噪声”。为了突破这一瓶颈，我们提出了 Q-Gate，这是一个即插即用且无需训练的框架，它将关键帧选择视为一个动态模态路由问题。我们将检索过程解耦为三个轻量级专家流：用于局部细节的视觉定位（Visual Grounding）、用于场景语义的全局匹配（Global Matching）以及用于字幕驱动叙事的上下文对齐（Contextual Alignment）。至关重要的是，Q-Gate 引入了一种查询调制门控机制，该机制利用大语言模型（LLM）的上下文推理能力来评估查询意图，并动态地在专家之间分配注意力权重。该机制智能地激活必要的模态，同时“静音”无关模态，从而最大化信噪比。在 LongVideoBench 和 Video-MME 上针对多种 MLLM 主干网络进行的大量实验表明，Q-Gate 显著优于最先进的基准模型。通过有效抑制特定模态的噪声，它为可扩展的视频推理提供了一种鲁棒且具有高度可解释性的解决方案。

## Abstract
Long video understanding remains a formidable challenge for Multimodal Large Language Models (MLLMs) due to the prohibitive computational cost of processing dense frame sequences. Prevailing solutions, which select a keyframe subset, typically rely on either a single visual-centric metric (e.g., CLIP similarity) or a static fusion of heuristic scores. This ``one-size-fits-all'' paradigm frequently fails: visual-only metrics are ineffective for plot-driven narrative queries, while indiscriminately incorporating textual scores introduces severe ``modal noise'' for purely visual tasks. To break this bottleneck, we propose Q-Gate, a plug-and-play and training-free framework that treats keyframe selection as a dynamic modality routing problem. We decouple the retrieval process into three lightweight expert streams: Visual Grounding for local details, Global Matching for scene semantics, and Contextual Alignment for subtitle-driven narratives. Crucially, Q-Gate introduces a Query-Modulated Gating Mechanism that leverages the in-context reasoning of an LLM to assess the query's intent and dynamically allocate attention weights across the experts. This mechanism intelligently activates necessary modalities while ``muting'' irrelevant ones, thereby maximizing the signal-to-noise ratio. Extensive experiments on LongVideoBench and Video-MME across multiple MLLM backbones demonstrate that Q-Gate substantially outperforms state-of-the-art baselines. By effectively suppressing modality-specific noise, it provides a robust, highly interpretable solution for scalable video reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Where to Focus: Query-Modulated Multimodal Keyframe Selection for Long Video Understanding》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：长视频理解面临巨大的计算开销，多模态大模型（MLLM）难以处理数千帧的输入。现有的关键帧选择方法（如基于 CLIP 的视觉相似度或静态的多模态融合）存在“一刀切”的问题：
    *   **视觉局限性**：仅靠视觉指标难以捕捉依赖字幕或复杂剧情的查询。
    *   **模态噪声**：无差别地引入文本（字幕）评分，会在纯视觉任务中引入干扰噪声，降低检索精度。
*   **研究背景**：在长视频问答中，不同的查询（Query）对视觉细节、全局语义和对话上下文的需求各不相同。

### 2. 方法论（Q-Gate 框架）
Q-Gate 是一个**无需训练、即插即用**的框架，其核心思想是将关键帧选择视为一个**动态模态路由**问题。
*   **三个专家流（Expert Streams）**：
    1.  **视觉定位（Visual Grounding）**：提取查询中的实体，计算局部实体与视频帧的 CLIP 相似度，关注局部细节。
    2.  **全局匹配（Global Matching）**：计算完整查询语句与视频帧的全局 CLIP 相似度，捕捉场景语义。
    3.  **上下文对齐（Contextual Alignment）**：利用文本嵌入模型（如 Sentence-BERT）计算查询与视频字幕（Subtitles）的相似度，处理叙事逻辑。
*   **查询调制门控机制（Query-Modulated Gating）**：
    *   利用 LLM（如 GPT-4o-mini）作为决策器，分析用户查询的意图。
    *   LLM 为上述三个专家流动态分配权重（$w_{grounding}, w_{global}, w_{context}$），权重总和为 1。
    *   **公式化表达**：最终得分 $S = \sum (w_i \cdot \text{Softmax}(S_i / \tau))$，其中 $\tau$ 是温度系数，用于调节得分分布的平滑度。
*   **流程**：输入视频和查询 -> LLM 生成权重 -> 计算加权融合得分 -> 选取 Top-K 关键帧 -> 送入 MLLM 进行推理。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **LongVideoBench (LVB)**：涵盖多种长视频任务（1-60分钟）。
    *   **Video-MME**：目前最全面的多模态视频评估基准。
*   **对比方法**：
    *   **启发式方法**：随机采样（Random）、均匀采样（Uniform）。
    *   **视觉检索方法**：基于 CLIP 的相似度检索。
    *   **最先进方法（SOTA）**：如 MA-LMM 等现有的关键帧选择策略。
*   **底层模型（Backbones）**：测试了包括 GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro 以及开源模型 LLaVA-OneVision 等多种模型。

### 4. 资源与算力
*   **训练算力**：论文明确指出该方法是 **Training-free（无需训练）** 的，因此没有模型训练阶段的算力消耗。
*   **推理算力**：
    *   使用了轻量级模型（如 GPT-4o-mini 或 Llama-3）作为门控器，推理开销极低。
    *   特征提取使用预训练的 CLIP 和文本嵌入模型。
    *   文中未具体列出 GPU 型号和具体耗时，但强调了其相对于全量帧处理的极高效率提升。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流长视频基准上进行了广泛测试，涵盖了从闭源商业模型到开源前沿模型的多种组合。
*   **消融实验**：
    *   验证了三个专家流各自的贡献。
    *   研究了温度系数 $\tau$ 对性能的影响。
    *   对比了动态权重与静态权重（如 1:1:1 融合）的效果。
*   **客观性**：通过可视化分析（Case Study）展示了门控机制如何根据不同类型的查询（如细节查询 vs. 剧情查询）调整权重，证明了其逻辑的合理性。

### 6. 主要结论与发现
*   **动态路由的必要性**：静态融合往往会导致性能下降，而 Q-Gate 通过“静音”无关模态，显著提升了信噪比。
*   **性能提升**：在 LongVideoBench 上，Q-Gate 配合 GPT-4o 达到了 64.1% 的准确率，显著超过了均匀采样（55.4%）和纯视觉检索。
*   **鲁棒性**：该方法在不同长度的视频和不同能力的底层模型上均表现出一致的改进。

### 7. 优点
*   **即插即用**：不需要对现有的 MLLM 进行任何微调，易于集成到现有管线中。
*   **可解释性强**：通过观察 LLM 分配的权重，可以直观理解模型为何选择这些特定帧。
*   **高效抑制噪声**：解决了长视频中字幕信息与视觉信息可能存在的冲突问题。

### 8. 不足与局限
*   **依赖门控 LLM 的质量**：如果作为门控器的 LLM 无法准确理解查询意图，可能会分配错误的权重。
*   **字幕依赖性**：对于缺乏字幕或字幕质量极差（如 OCR 错误严重）的视频，上下文对齐流的效果会受限。
*   **实时性挑战**：虽然比处理全量帧快，但引入额外的 LLM 调用和多流计算在极端实时场景下仍有优化空间。

（完）
