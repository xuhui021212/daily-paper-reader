---
title: "LiquidTAD: An Efficient Method for Temporal Action Detection via Liquid Neural Dynamics"
title_zh: LiquidTAD：一种基于液体神经动力学的高效时序动作检测方法
authors: "Zepeng Sun, Naichuan Zheng, Hailun Xia, Junjie Wu, Liwei Bao, Xiaotai Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18274v1"
tags: ["query:lvu"]
score: 9.0
evidence: 通过液体动力学在未修剪视频中进行高效时序动作检测
tldr: 针对时序动作检测（TAD）中Transformer架构计算复杂度高、参数冗余的问题，本文提出LiquidTAD框架。该方法首次将液体神经网络（LNN）引入TAD领域，通过闭式连续时间（CfC）公式实现并行化，以O(N)线性复杂度捕捉复杂时序依赖。LiquidTAD在保持高精度的同时大幅减少了参数量，在THUMOS-14等数据集上实现了优异的性能与效率平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18274v1/fig-001.webp\", \"caption\": \"TABLE V: Ablation results for (a) temporal pyramid levels and (b) discretization step ∆t on THUMOS-14. Default settings are in bold.\", \"page\": 4, \"index\": 1, \"width\": 516, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.18274v1/fig-002.webp\", \"caption\": \"TABLE III: Complexity and performance analysis across THUMOS-14, ActivityNet-1.3, and Ego4D datasets.\", \"page\": 3, \"index\": 2, \"width\": 516, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.18274v1/fig-003.webp\", \"caption\": \"TABLE I: State-of-the-art comparison on the THUMOS-14 and ActivityNet-1.3 datasets. We report mAP (%) at various tIoU thresholds and the average mAP. The benchmarks are grouped by their respective backbones and architectures.\", \"page\": 3, \"index\": 3, \"width\": 1042, \"height\": 321}]"
motivation: 现有的Transformer架构在处理长视频时面临计算复杂度高和参数冗余的挑战，限制了其在资源受限环境下的部署。
method: 提出ActionLiquid模块，利用闭式连续时间动力学实现并行化计算，并通过可学习的时间常数自适应调节对不同动作时长的敏感度。
result: "在THUMOS-14上达到69.46%的mAP，参数量仅为10.82M（比ActionFormer减少63%），并在多个基准测试中展现出极佳的鲁棒性。"
conclusion: LiquidTAD通过引入并行化液体神经动力学，成功打破了TAD任务中精度与效率的权衡，为高效视频理解提供了新思路。
---

## 摘要
未剪辑视频中的时序动作检测（TAD）目前由基于 Transformer 的架构主导。尽管性能优异，但其二次计算复杂度和显著的参数冗余限制了在资源受限环境中的部署。在本文中，我们提出了 LiquidTAD，这是一种新型的参数高效框架，它使用并行化的 ActionLiquid 模块取代了繁琐的自注意力层。与受限于顺序执行瓶颈的传统液体神经网络（LNNs）不同，LiquidTAD 利用闭式连续时间（CfC）公式，使模型能够重新表述为可并行化的算子，同时保留了连续时间动力学的内在物理先验。该架构以 $O(N)$ 的线性复杂度捕捉复杂的时序依赖关系，并通过学习到的时间常数（$τ$）自适应地调节时序敏感性，为处理不同持续时间的动作提供了一种鲁棒机制。据我们所知，这项工作首次将基于并行化 LNN 的架构引入 TAD 领域。在 THUMOS-14 数据集上的实验结果表明，LiquidTAD 仅需 10.82M 参数即可达到 69.46% 的极具竞争力的平均 mAP，与 ActionFormer 基准相比参数量减少了 63%。在 ActivityNet-1.3 和 Ego4D 基准上的进一步评估证实，LiquidTAD 实现了最优的准确率-效率权衡，并对时序采样变化表现出卓越的鲁棒性，推进了现代 TAD 框架的帕累托前沿。

## Abstract
Temporal Action Detection (TAD) in untrimmed videos is currently dominated by Transformer-based architectures. While high-performing, their quadratic computational complexity and substantial parameter redundancy limit deployment in resource-constrained environments. In this paper, we propose LiquidTAD, a novel parameter-efficient framework that replaces cumbersome self-attention layers with parallelized ActionLiquid blocks. Unlike traditional Liquid Neural Networks (LNNs) that suffer from sequential execution bottlenecks, LiquidTAD leverages a closed-form continuous-time (CfC) formulation, allowing the model to be reformulated as a parallelizable operator while preserving the intrinsic physical prior of continuous-time dynamics. This architecture captures complex temporal dependencies with $O(N)$ linear complexity and adaptively modulates temporal sensitivity through learned time-constants ($τ$), providing a robust mechanism for handling varying action durations. To the best of our knowledge, this work is the first to introduce a parallelized LNN-based architecture to the TAD domain. Experimental results on the THUMOS-14 dataset demonstrate that LiquidTAD achieves a highly competitive Average mAP of 69.46\% with only 10.82M parameters -- a 63\% reduction compared to the ActionFormer baseline. Further evaluations on ActivityNet-1.3 and Ego4D benchmarks confirm that LiquidTAD achieves an optimal accuracy-efficiency trade-off and exhibits superior robustness to temporal sampling variations, advancing the Pareto frontier of modern TAD frameworks.

---

## 论文详细总结（自动生成）

这是一份关于论文《LiquidTAD: An Efficient Method for Temporal Action Detection via Liquid Neural Dynamics》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
时序动作检测（TAD）旨在从未剪辑的视频中精确定位动作的起始时间并识别类别。当前该领域由基于 Transformer 的架构主导，虽然性能卓越，但存在两个核心痛点：
*   **计算复杂度高**：自注意力机制具有二次方复杂度 $O(T^2)$，处理长视频时计算量激增。
*   **参数冗余严重**：为了捕捉多尺度时序上下文，Transformer 需要堆叠大量重型模块，导致参数量庞大（通常超过 30M），难以在资源受限的嵌入式设备上部署。
**研究动机**：探索一种能够打破“参数墙”、具备线性复杂度且能自适应不同动作时长的轻量化高效架构。

### 2. 方法论：核心思想与关键技术
LiquidTAD 首次将**液体神经网络（LNN）**引入 TAD 领域，将时序建模视为一个连续时间动力系统。

*   **核心思想**：利用**闭式连续时间（CfC）动力学**取代传统的自注意力机制。通过并行化的算子实现高效训练，同时保留连续时间建模的物理先验。
*   **ActionLiquid 模块**：
    *   **局部混合与门控**：首先通过轻量级的 1D 深度可分离卷积提取局部上下文，并使用数据依赖的门控机制（Gating）调节信息流。
    *   **闭式 LTC 更新公式**：状态演化遵循公式：
        $x_{out} = x \odot e^{-\tau \Delta t} + M(\tilde{x}) \odot (1 - e^{-\tau \Delta t}) \odot G(\tilde{x}) \odot M$
        其中，$e^{-\tau \Delta t}$ 是**保留因子**（保留历史信息），$(1 - e^{-\tau \Delta t})$ 是**创新率**（吸收新信息）。
    *   **可学习时间常数 ($\tau$)**：$\tau$ 决定了系统对输入的响应速度。模型在特征金字塔的不同层级学习不同的 $\tau$，使其能自适应地处理高频短动作和低频长活动。
*   **并行化设计**：不同于传统 ODE 需要顺序求解，LiquidTAD 将动力学方程重新表述为可并行化的张量操作，确保了训练效率。

### 3. 实验设计
*   **数据集**：
    *   **THUMOS-14**：侧重于短而密集的动作定位。
    *   **ActivityNet-1.3**：侧重于长持续时间的动作。
    *   **Ego4D (Moment Queries)**：侧重于第一视角（Egocentric）视频的复杂场景。
*   **Benchmark 与对比方法**：
    *   对比了包括 ActionFormer (Transformer 基准)、TriDet、CausalTAD、TadTR、DiffTAD 等在内的多种 SOTA（州际领先）方法。
    *   使用了 I3D、TSP 和 InternVideo 等主流预训练特征提取器。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到基于 OpenTAD 统一框架实现，使用 AdamW 优化器。
*   **效率指标**：明确给出了推理时的计算量（FLOPs）和参数量。例如在 THUMOS-14 上为 27.17 G FLOPs，参数量为 10.82M。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个性质截然不同的主流数据集上进行了验证，涵盖了短动作、长动作和第一视角视频，具有较强的普适性。
*   **消融实验**：
    *   验证了 $\tau$ 的共享策略（单标量 vs 逐通道）。
    *   分析了特征金字塔深度对性能的影响。
    *   **鲁棒性测试**：专门测试了在推理阶段改变采样步长 $\Delta t$ 时的表现。
*   **客观性**：实验采用了统一的特征提取器和 OpenTAD 评测协议，对比数据较为客观、公平。

### 6. 主要结论与发现
*   **极高的参数效率**：LiquidTAD 在 THUMOS-14 上以 **10.82M** 参数达到了 **69.46%** 的 mAP，相比 ActionFormer 参数量减少了 **63%**，且精度更高。
*   **线性复杂度优势**：随着视频长度增加，LiquidTAD 的计算量呈线性增长，有效解决了 Transformer 的二次方瓶颈。
*   **时序鲁棒性**：由于引入了 $\Delta t$ 相关的物理先验，模型对采样频率的变化具有极强的弹性。即使推理时的 $\Delta t$ 与训练时不一致，性能下降也远小于离散模型。
*   **自适应性**：学习到的 $\tau$ 能够自动补偿不同的动作尺度，证明了连续动力学在视频理解中的有效性。

### 7. 优点（亮点）
*   **首创性**：首次将并行化液体神经网络应用于 TAD 任务，开辟了非 Transformer 架构的新路径。
*   **帕累托前沿突破**：在准确率-效率权衡（Accuracy-Efficiency Trade-off）上表现卓越，非常适合移动端或实时部署。
*   **物理可解释性**：通过时间常数 $\tau$ 和步长 $\Delta t$ 的耦合，赋予了模型处理非均匀采样视频的内在能力。

### 8. 不足与局限
*   **精度上限**：虽然效率极高，但在绝对精度上与一些极其庞大的 SOTA 模型（如 CausalTAD，52M+ 参数）相比仍有微小差距（约 0.3% mAP）。
*   **特征依赖**：实验主要依赖于预取的离线特征，未讨论端到端训练（End-to-End）时的表现。
*   **算力细节缺失**：未提供具体的训练环境和耗时数据，不便于其他研究者评估训练成本。

（完）
