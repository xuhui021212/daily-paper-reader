---
title: Sink-Token-Aware Pruning for Fine-Grained Video Understanding in Efficient Video LLMs
title_zh: 高效视频大语言模型中面向细粒度视频理解的汇聚标记感知剪枝
authors: "Kibum Kim, Jiwan Kim, Kyle Min, Yueqi Wang, Jinyoung Moon, Julian McAuley, Chanyoung Park"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20937v1"
tags: ["query:lvu"]
score: 9.0
evidence: 高效视频大模型与视频理解中的令牌剪枝
tldr: 针对视频大语言模型推理延迟高的问题，本文研究了现有剪枝方法在细粒度理解任务上的性能崩溃现象。研究发现“汇聚令牌”是导致该问题的关键，它们虽无语义信息却占据大量注意力。为此，作者提出了SToP方法，通过引入汇聚评分来抑制这些令牌，在保留极少量令牌的情况下显著提升了模型在幻觉评估和复杂推理等任务中的表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20937v1/fig-001.webp\", \"caption\": \"Table 2: Performance comparison on multiple-choice question answering (MCQA) benchmarks. The Performance Drop Rate is computed as in Tab. 1.\", \"page\": 11, \"index\": 1, \"width\": 679, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.20937v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison on benchmarks requiring fine-grained video understanding. Performance Drop Rate denotes the macro-average of performance degradation relative to the Vanilla model across all categories. Desc., Consist., Temp., Act., YC are Description, Consistency, Temporal, ActivityNet, and YouCook2, respectively.\", \"page\": 10, \"index\": 2, \"width\": 679, \"height\": 396}]"
motivation: 现有的视频令牌剪枝方法在细粒度理解任务中表现不佳，主要原因是无语义的“汇聚令牌”干扰了模型对视觉证据的提取。
method: 提出SToP方法，通过计算汇聚评分来量化令牌的汇聚倾向，并将其应用于现有的时空剪枝框架以抑制无效令牌。
result: "在多个基准测试中，SToP显著提升了主流剪枝方法的性能，即使在剪除90%视觉令牌的情况下依然保持高效的理解能力。"
conclusion: 识别并抑制汇聚令牌是实现高效且精准的视频大语言模型推理的关键路径。
---

## 摘要
视频大语言模型（Video LLMs）由于向 LLM 提供了大量的视觉标记（visual tokens），导致推理延迟较高。为了解决这一问题，免训练的视觉标记剪枝已成为降低计算成本的一种方案；然而，现有方法主要在多项选择题解答（MCQA）基准测试上进行验证，而这些基准通常仅需粗粒度线索即可。在这项工作中，我们揭示了这些方法在需要精确视觉定位（visual grounding）的细粒度理解任务（如幻觉评估）上会出现剧烈的性能下降。为了探索这一差距，我们进行了系统分析，并将汇聚标记（sink tokens）——即吸引了过多注意力但语义信息匮乏的标记——识别为细粒度视频理解的关键障碍。当这些汇聚标记在剪枝中幸存时，它们会扭曲模型的视觉证据并阻碍细粒度理解。受此启发，我们提出了汇聚标记感知剪枝（SToP），这是一种简单且有效的即插即用方法。它引入了汇聚分数来量化每个标记表现为汇聚标记的倾向，并将该分数应用于现有的空间和时间剪枝方法以抑制这些标记，从而增强视频理解。为了验证 SToP 的有效性，我们将其应用于最先进的剪枝方法（VisionZip、FastVid 和 Holitom），并在涵盖幻觉、开放式生成、组合推理和 MCQA 的多种基准测试中进行了评估。我们的结果表明，即使在剪枝掉高达 90% 的视觉标记时，SToP 也能显著提升性能。

## Abstract
Video Large Language Models (Video LLMs) incur high inference latency due to a large number of visual tokens provided to LLMs. To address this, training-free visual token pruning has emerged as a solution to reduce computational costs; however, existing methods are primarily validated on Multiple-Choice Question Answering (MCQA) benchmarks, where coarse-grained cues often suffice. In this work, we reveal that these methods suffer a sharp performance collapse on fine-grained understanding tasks requiring precise visual grounding, such as hallucination evaluation. To explore this gap, we conduct a systematic analysis and identify sink tokens--semantically uninformative tokens that attract excessive attention--as a key obstacle to fine-grained video understanding. When these sink tokens survive pruning, they distort the model's visual evidence and hinder fine-grained understanding. Motivated by these insights, we propose Sink-Token-aware Pruning (SToP), a simple yet effective plug-and-play method that introduces a sink score to quantify each token's tendency to behave as a sink and applies this score to existing spatial and temporal pruning methods to suppress them, thereby enhancing video understanding. To validate the effectiveness of SToP, we apply it to state-of-the-art pruning methods (VisionZip, FastVid, and Holitom) and evaluate it across diverse benchmarks covering hallucination, open-ended generation, compositional reasoning, and MCQA. Our results demonstrate that SToP significantly boosts performance, even when pruning up to 90% of visual tokens.

---

## 论文详细总结（自动生成）

### 论文总结：高效视频大语言模型中面向细粒度视频理解的汇聚标记感知剪枝（SToP）

#### 1. 核心问题与整体含义
视频大语言模型（Video LLMs）在处理长视频时，由于视觉标记（Visual Tokens）数量庞大，面临极高的推理延迟和计算成本。虽然现有的“免训练视觉标记剪枝”技术能有效减少计算量，但作者发现这些方法在多项选择题（MCQA）基准上表现尚可，但在需要精确视觉定位的**细粒度理解任务**（如幻觉评估、复杂动作推理）中会出现严重的性能崩溃。
**研究动机：** 识别并解决导致剪枝后细粒度理解能力下降的关键障碍——“汇聚标记”（Sink Tokens）。

#### 2. 方法论
作者提出了 **SToP (Sink-Token-aware Pruning)**，一种即插即用的免训练剪枝增强方法。
*   **核心思想：** 识别那些在空间位置上持续拥有高注意力分、但语义信息匮乏的“汇聚标记”，并在剪枝过程中显式地抑制它们，从而为真正有意义的视觉标记腾出配额。
*   **关键技术细节：**
    1.  **汇聚评分（Sink Score, $s_i$）：** 通过累加所有帧中同一位置标记的注意力分数，并利用幂函数进行分布锐化和归一化，量化每个标记成为“汇聚点”的倾向。
    2.  **STSP（汇聚感知空间剪枝）：** 在帧内剪枝时，从原始注意力分数中减去汇聚评分项，降低汇聚标记的优先级。
    3.  **STTP（汇聚感知时间剪枝）：** 在帧间剪枝时，将汇聚评分引入相似度阈值判断，促使模型更容易剪掉跨帧重复出现的无意义背景/汇聚标记。

#### 3. 实验设计
*   **数据集与场景：**
    *   **细粒度任务：** EventHallusion（幻觉评估）、VideoComp（组合推理）、VCG-Bench（开放式生成）。
    *   **MCQA任务：** MVBench、VideoMME、NextQA、LongVideoBench、MLVU。
*   **基准模型（Backbones）：** LLaVA-OneVision-7B、LLaVA-Video-7B、Qwen2.5-VL。
*   **对比方法：** VisionZip、FastVid、HoliTom、PruneVid、FlashVid 等最先进的剪枝框架。
*   **评估指标：** 准确率（Accuracy）、GPT-5 评分（针对开放式生成）。

#### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA GeForce A6000 (48GB) GPU。
*   **训练时长：** 该方法为**免训练（Training-free）**，仅在推理阶段实施，因此不涉及大规模训练算力消耗。
*   **配置：** 默认处理 32 帧视频，测试了 10%、15% 和 20% 的标记保留率。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 8 个主流基准测试，跨越了 3 个不同的模型架构，实验设计非常全面。
*   **消融实验：** 详细验证了 STSP 和 STTP 模块的各自贡献，并对超参数（如惩罚权重 $\mu_s$、锐化因子 $w$）进行了敏感性分析。
*   **客观性：** 通过对比“硬剪枝”和“注意力重分布”等朴素方案，证明了 SToP 的优越性。实验覆盖了从 16 帧到 64 帧的不同输入规模，验证了其在极端预算（剪掉 90% 标记）下的鲁棒性，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **汇聚标记是元凶：** 视觉编码器中的汇聚标记虽然吸引大量注意力，但几乎不含语义，保留它们会挤占关键视觉证据的配额，导致模型产生幻觉。
*   **时间剪枝的副作用：** 传统的时间剪枝之所以比空间剪枝更鲁棒，是因为它在去除冗余时“顺便”抑制了部分汇聚标记。
*   **性能提升显著：** SToP 能显著缓解剪枝带来的性能下降。例如，使用 SToP 处理 16 帧视频的效果甚至优于未优化前处理 64 帧视频的效果，极大地优化了精度与速度的权衡。

#### 7. 优点
*   **即插即用：** 无需重新训练模型，可直接集成到现有的各种剪枝算法中。
*   **针对性强：** 首次深入探讨了视觉汇聚标记对视频 LLM 细粒度理解的影响，填补了领域空白。
*   **高效性：** 在极低保留率（10%）下依然能保持较高的理解精度，显著降低了长视频处理的计算门槛。

#### 8. 不足与局限
*   **超参数依赖：** 汇聚评分的惩罚权重（$\mu_s, \mu_t$）在不同模型架构上可能需要微调以达到最优效果。
*   **特征剪枝适配：** 目前主要针对基于注意力机制的剪枝方法，对于完全基于特征聚类（如 DPC-KNN）且不参考注意力权重的剪枝路径，其直接应用效果受限（尽管文中提出了改进思路）。
*   **评估成本：** 部分实验依赖 GPT-5 进行评分，这在复现和大规模验证时可能存在成本和 API 访问限制。

（完）
