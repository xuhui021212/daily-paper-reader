---
title: "BTS-rPPG: Orthogonal Butterfly Temporal Shifting for Remote Photoplethysmography"
title_zh: BTS-rPPG：用于远程光电容积脉搏波测量的正交蝴蝶时间偏移
authors: "Ba-Thinh Nguyen, Thi-Duyen Ngo, Thanh-Trung Huynh, Thanh-Ha Le, Huy-Hieu Pham"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01679v1"
tags: ["query:medvid"]
score: 9.0
evidence: 面部视频远程光电容积脉搏波描记术的时间建模
tldr: 针对远程光电容积脉搏波（rPPG）测量中现有深度学习方法受限于局部时间建模、感受野有限的问题，本文提出BTS-rPPG框架。该框架引入受FFT启发的正交蝴蝶时间偏移（BTS）机制，通过XOR配对实现跨帧信息的高效传播，并结合正交特征传输（OFT）减少冗余。实验证明，该方法显著增强了长程时间建模能力，在多个基准数据集上达到了领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01679v1/fig-001.webp\", \"caption\": \"Table 1. Intra-dataset evaluation results on PURE [21], UBFC-rPPG [1], and MMPD [22]. Lower is better for MAE, MAPE, and RMSE, while higher is better for Pearson correlation r. Best results are highlighted in bold and second-best results are underlined.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.01679v1/fig-002.webp\", \"caption\": \"Table 2. Cross-dataset test results. PURE→UBFC-rPPG means the model is trained on PURE and tested on UBFC-rPPG. Lower is better for MAE/MAPE/RMSE, while higher is better for Pearson correlation r.\", \"page\": 6, \"index\": 2, \"width\": 1036, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.01679v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the proposed BTS-rPPG. I) The framework comprises (a) an input representation module, (b) a BTS-enhanced Swin Transformer backbone, and (c) a predictor head. RGB frames and their normalized difference frames are fused and embedded into patch-level tokens, which are then processed by Swin Transformer stages equipped with BTS and finally regressed to the target rPPG waveform. II) OFT suppresses redundant transferred information by preserving only the feature component orthogonal to the target context, thereby promoting complementary temporal exchange. III) BTS establishes a butterfly-inspired hierarchical communication pattern, where temporal interactions are progressively expanded across stages to enable efficient propagation from short-range to long-range dependencies.\", \"page\": 3, \"index\": 3, \"width\": 965, \"height\": 669}]"
motivation: 现有的rPPG方法多依赖局部时间偏移或卷积，导致时间感受野受限，难以捕捉长程生理信号动态。
method: 提出基于XOR配对的蝴蝶时间偏移（BTS）以扩大感受野，并结合正交特征传输（OFT）机制过滤冗余信息，实现高效的跨帧交互。
result: 在多个主流rPPG基准数据集上的实验结果表明，BTS-rPPG在生理参数估计精度上优于现有的时间建模策略。
conclusion: BTS-rPPG通过结构化的蝴蝶模式和正交特征过滤，有效提升了非接触式生理感知的长程时间建模效率和准确性。
---

## 摘要
远程光电容积脉搏波测量（rPPG）通过分析血液循环引起的细微外观变化，实现了从面部视频中进行非接触式生理感知。然而，对这些信号的时间动态进行建模仍然具有挑战性，因为许多深度学习方法依赖于时间偏移或卷积算子，这些算子主要聚合来自相邻帧的信息，导致建模主要局限于局部时间范围，且时间感受野有限。为了解决这一局限性，我们提出了 BTS-rPPG，这是一个基于正交蝴蝶时间偏移（BTS）的时间建模框架。受快速傅里叶变换（FFT）中蝴蝶通信模式的启发，BTS 通过基于异或（XOR）的蝴蝶配对调度建立结构化的帧交互，逐步扩大时间感受野，并实现信息在远距离帧之间的高效传播。此外，我们引入了一种正交特征传输机制（OFT），在时间偏移之前根据目标上下文对源特征进行过滤，仅保留用于跨帧传输的正交分量。这减少了冗余的特征传播，并促进了互补的时间交互。在多个基准数据集上的广泛实验表明，BTS-rPPG 改善了生理动态的远程时间建模，并在 rPPG 估计方面始终优于现有的时间建模策略。

## Abstract
Remote photoplethysmography (rPPG) enables contactless physiological sensing from facial videos by analyzing subtle appearance variations induced by blood circulation. However, modeling the temporal dynamics of these signals remains challenging, as many deep learning methods rely on temporal shifting or convolutional operators that aggregate information primarily from neighboring frames, resulting in predominantly local temporal modeling and limited temporal receptive fields. To address this limitation, we propose BTS-rPPG, a temporal modeling framework based on Orthogonal Butterfly Temporal Shifting (BTS). Inspired by the butterfly communication pattern in the Fast Fourier Transform (FFT), BTS establishes structured frame interactions via an XOR-based butterfly pairing schedule, progressively expanding the temporal receptive field and enabling efficient propagation of information across distant frames. Furthermore, we introduce an orthogonal feature transfer mechanism (OFT) that filters the source feature with respect to the target context before temporal shifting, retaining only the orthogonal component for cross-frame transmission. This reduces redundant feature propagation and encourages complementary temporal interaction. Extensive experiments on multiple benchmark datasets demonstrate that BTS-rPPG improves long-range temporal modeling of physiological dynamics and consistently outperforms existing temporal modeling strategies for rPPG estimation.

---

## 论文详细总结（自动生成）

以下是对论文《BTS-rPPG: Orthogonal Butterfly Temporal Shifting for Remote Photoplethysmography》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：远程光电容积脉搏波（rPPG）测量依赖于捕捉面部视频中微弱的血液循环引起的光学变化。现有的深度学习方法（如基于卷积或简单时间偏移的方法）主要关注相邻帧之间的局部时间交互，导致**时间感受野受限**，难以有效捕捉具有长程周期性特征的生理信号动态。
*   **研究背景**：rPPG 信号具有短程连续性和长程周期性。传统的局部建模方法在面对光照变化、头部运动等噪声时鲁棒性不足，因此需要一种既能保持计算效率又能实现长程时间建模的机制。

### 2. 提出的方法论
论文提出了 **BTS-rPPG** 框架，其核心包含两个关键技术模块：
*   **蝴蝶时间偏移（BTS, Butterfly Temporal Shifting）**：
    *   **核心思想**：受快速傅里叶变换（FFT）中蝴蝶通信模式的启发。
    *   **技术细节**：采用基于异或（XOR）的配对调度算法。在网络的第 $l$ 阶段，帧 $t$ 与帧 $t \oplus 2^{l-1}$ 进行信息交换。
    *   **效果**：随着网络层数加深，时间偏移量呈指数级增长（1, 2, 4, 8...），使模型能够以对数级的阶段数覆盖极长的时间跨度，实现从局部到全局的层次化建模。
*   **正交特征传输（OFT, Orthogonal Feature Transfer）**：
    *   **核心思想**：减少跨帧传输时的信息冗余。
    *   **技术细节**：在进行时间偏移前，将源帧特征投影到目标帧特征的正交空间中。通过 Gram-Schmidt 正交化思路，仅保留源帧中与目标帧上下文互补的成分（正交分量），过滤掉重复信息。
*   **整体架构**：以 Swin Transformer 为骨干网络，输入端融合了原始 RGB 帧和归一化差分帧（NDF），通过多阶段 BTS 增强的 Transformer 块提取特征，最后由预测头回归出 rPPG 波形。

### 3. 实验设计
*   **数据集**：
    *   **PURE**：受控环境下的运动鲁棒性测试。
    *   **UBFC-rPPG**：包含自然光和数学任务诱导的心率变化。
    *   **MMPD**：大规模多域数据集，涵盖不同肤色、光照和活动类型，极具挑战性。
*   **Benchmark 与对比方法**：
    *   **传统方法**：CHROM, POS, ICA 等。
    *   **深度学习方法**：DeepPhys, PhysNet, TS-CAN, PhysFormer, EfficientPhys, RhythmFormer 等。
*   **评估指标**：平均绝对误差 (MAE)、平均绝对百分比误差 (MAPE)、均方根误差 (RMSE) 和皮尔逊相关系数 ($r$)。

### 4. 资源与算力
*   **硬件设备**：使用单张 **NVIDIA GeForce RTX 3090 GPU** 进行训练。
*   **软件环境**：PyTorch 2.0.0, CUDA 11.8。
*   **训练细节**：
    *   MMPD 数据集训练 10 个 epoch，PURE 和 UBFC-rPPG 训练 30 个 epoch。
    *   采用 Adam 优化器，初始学习率 $1 \times 10^{-4}$，使用 OneCycle 学习率调度。
    *   推理效率：在 NVIDIA T4 GPU 上可达 2.05 Kfps，具备实时处理能力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **内部测试**：在三个主流数据集上进行了详尽的性能对比。
    *   **跨数据集测试**：验证了模型在 PURE $\to$ UBFC 等多种组合下的泛化能力。
    *   **消融实验**：分别验证了 BTS 和 OFT 的增益、OFT 的通道折叠比例（Fold Ratio）以及不同的时间配对调度（线性、反向蝴蝶等）。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集验证证明了模型并非仅仅拟合特定数据集的视觉统计特性，而是捕捉到了通用的生理特征。

### 6. 主要结论与发现
*   **性能领先**：BTS-rPPG 在所有基准测试中均显著优于现有方法。在 UBFC-rPPG 上，MAE 降低了约 67%。
*   **长程建模的重要性**：实验证明，逐步扩大时间感受野的“蝴蝶模式”比单纯的局部建模或线性增长建模更符合生理信号的层次化结构。
*   **冗余抑制有效**：OFT 机制通过保留正交特征，在不增加参数量的情况下提升了特征交互的效率。

### 7. 优点
*   **创新性强**：巧妙地将 FFT 的蝴蝶通信模式引入时间偏移算子，解决了长程建模难题。
*   **高效性**：相比于全注意力机制，BTS 是一种稀疏且参数高效的交互方式，推理速度快。
*   **鲁棒性**：在多肤色、复杂光照的 MMPD 数据集上表现优异，证明了其在现实场景中的潜力。

### 8. 不足与局限
*   **理论解释尚浅**：虽然蝴蝶模式在实验上非常有效，但论文中对于“为什么这种特定的阶段性时间通信模式最适合 rPPG 信号”缺乏深层的数学或生理学理论证明。
*   **输入依赖**：模型性能仍受限于面部检测的准确性，对于极端遮挡或剧烈运动导致的面部丢失情况，文中未做深入讨论。
*   **超参数敏感性**：OFT 的通道折叠比例需要针对不同数据集进行微调以达到最优效果。

（完）
