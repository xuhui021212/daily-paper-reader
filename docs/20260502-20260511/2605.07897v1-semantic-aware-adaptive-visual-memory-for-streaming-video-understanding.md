---
title: Semantic-Aware Adaptive Visual Memory for Streaming Video Understanding
title_zh: 语义感知自适应视觉记忆用于流式视频理解
authors: "Hang Wu, Sherin Mary Mathews, Yujun Cai, Ming-Hsuan Yang, Yiwei Wang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07897v1"
tags: ["query:lvu"]
score: 10.0
evidence: 流式视频理解的语义感知自适应视觉记忆
tldr: 针对在线流式视频理解中内存管理与语义感知不足的问题，本文提出SAVEMem框架。该框架无需训练，通过双阶段策略实现语义感知的内存生成与自适应检索。第一阶段利用伪问题库引入语义先验构建三层存储；第二阶段根据查询动态调整检索范围并筛选关键帧。在Qwen2.5-VL上实验显示，该方法显著提升了多个基准测试性能，并大幅降低了显存占用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07897v1/fig-001.webp\", \"caption\": \"Table 1 Comparison with state-of-the-art Methods on OVO-Bench. Bold indicates the best and underline indicates the second best among all open-source models. † indicates the reproduced results.\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2605.07897v1/fig-002.webp\", \"caption\": \"Figure 1 Overview of SAVEMem. A two-stage pipeline decouples query-agnostic semantic memory generation from query-aware retrieval, yielding consistent gains over Qwen2.5-VL backbones across multiple streaming benchmarks.\", \"page\": 2, \"index\": 2, \"width\": 986, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2605.07897v1/fig-003.webp\", \"caption\": \"Table 2 Comparison with Other Training-free Methods on OVO-Bench. Bold indicates the best and underline indicates the second best within each backbone group. † indicates the reproduced results.\", \"page\": 7, \"index\": 3, \"width\": 976, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.07897v1/fig-004.webp\", \"caption\": \"Figure 2 Framework of SAVEMem. Stage 1 builds a three-tier streaming memory query-agnostically through a Recency Anchor, Temporal Semantic Pruning, and Spatial Semantic Selection guided by a pseudo-question prior; Stage 2 retrieves query-relevant frames query-awarely via an anchor-conditioned recency gate and late interaction between query and memory tokens.\", \"page\": 4, \"index\": 4, \"width\": 982, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2605.07897v1/fig-005.webp\", \"caption\": \"Figure 4 Semantic score distributions from the pseudo-question bank at the frame level (left) and token level within a single frame (right).\", \"page\": 9, \"index\": 5, \"width\": 683, \"height\": 317}]"
motivation: 现有的流式视频理解方法在内存压缩时缺乏语义信号，且检索与压缩阶段难以协同，导致长视频处理效率与准确性受限。
method: 提出SAVEMem框架，通过伪问题库引导的语义显著性构建三层流式内存，并利用锚点条件门控实现查询感知的自适应检索。
result: "在Qwen2.5-VL模型上，SAVEMem将OVO-Bench总分从52.27提升至62.69，并在128帧下减少了48%的峰值显存。"
conclusion: SAVEMem通过引入语义感知和自适应检索，为长流式视频理解提供了一种高效且无需训练的内存管理方案。
---

## 摘要
在线流式视频理解要求模型实时处理连续的视觉输入并响应用户查询，其中无限的流和不可预测的查询时机使内存管理成为核心挑战。现有方法通常通过视觉相似性启发式方法压缩视觉标记（tokens），或通过键值缓存（KV-cache）级别的检索来增强压缩。然而，压缩决策很少包含语义信号，且检索通常在压缩完成后才添加，导致这两个阶段难以协调。我们提出了 SAVEMem，这是一个无需训练的双阶段框架，它将语义感知引入记忆生成，并让检索范围根据每个查询进行自适应。在第一阶段，SAVEMem 在恒定的内存预算下在线构建三层流式记忆。一个固定的伪问题库提供了轻量级的语义先验，使得长期保留由语义显著性而非仅由视觉相似性决定。在第二阶段，SAVEMem 对该记忆执行查询感知检索。一个基于锚点条件的近期门控（anchor-conditioned recency gate）根据查询目标是当前还是遥远的过去，将检索范围从短期调整到中长期记忆。在此范围内，查询与记忆标记之间的后期交互（late interaction）选择候选帧进行回答。在无需训练的情况下应用于 Qwen2.5-VL，SAVEMem 将 OVO-Bench 的总分从 52.27 提高到 62.69，并在 StreamingBench 和 ODV-Bench 上取得了一致的提升，同时在 128 帧时比基干模型减少了 48% 的峰值 GPU 显存。

## Abstract
Online streaming video understanding requires models to process continuous visual inputs and respond to user queries in real time, where the unbounded stream and unpredictable query timing turn memory management into a central challenge. Existing methods typically compress visual tokens via visual similarity heuristics, or augment compression with KV-cache-level retrieval. However, compression decisions rarely incorporate semantic signals, and retrieval is often added after compression is finalized, making the two stages hard to coordinate. We present SAVEMem, a training-free dual-stage framework that brings semantic awareness into memory generation and lets the retrieval scope adapt per query. In Stage~1, SAVEMem builds a three-tier streaming memory online under a constant memory budget. A fixed pseudo-question bank provides a lightweight semantic prior, so that long-term retention is shaped by semantic salience rather than visual similarity alone. In Stage~2, SAVEMem performs query-aware retrieval over this memory. An anchor-conditioned recency gate adapts the retrieval scope from short-term to mid- and long-term memory based on whether the query targets the present or the distant past. Within this scope, late interaction between query and memory tokens selects candidate frames for answering. Applied to Qwen2.5-VL without training, SAVEMem improves the OVO-Bench overall score from 52.27 to 62.69 and yields consistent gains on StreamingBench and ODV-Bench, while reducing peak GPU memory by 48\% at 128 frames over the backbone.

---

## 论文详细总结（自动生成）

### 论文总结：SAVEMem - 语义感知自适应视觉记忆用于流式视频理解

#### 1. 核心问题与整体含义（研究动机和背景）
在线流式视频理解（Streaming Video Understanding）要求模型在视频流不断涌入且查询（Query）随机发生的情况下，实时处理视觉输入并给出回答。其核心挑战在于：
*   **内存受限**：视频流理论上是无限的，但 GPU 显存有限，必须对历史信息进行压缩或丢弃。
*   **语义缺失**：现有方法多基于视觉相似性（如帧间差异）进行压缩，容易忽略视觉上不显著但语义上重要的细节。
*   **检索与压缩脱节**：压缩通常是查询无关的，而检索往往在压缩后才独立进行，两者缺乏协同。
**SAVEMem** 的研究动机是引入语义先验来引导内存压缩，并根据查询内容动态调整检索范围，实现高效且精准的流式理解。

#### 2. 方法论：核心思想与关键技术
SAVEMem 是一个**无需训练（Training-free）**的双阶段框架，其核心思想是将“查询无关的语义压缩”与“查询感知的自适应检索”解耦并协同。

*   **阶段 1：语义记忆生成（Query-agnostic）**
    *   **三层记忆架构**：构建短期（Recency Anchor）、中期（Mid-term）和长期（Long-term）三层存储。
    *   **伪问题库（Pseudo-question Bank）**：预设一组通用的视觉探测问题（如“场景中有哪些物体？”、“发生了什么动作？”），利用这些问题与视觉 Token 的相似度（MaxSim）作为语义显著性评分。
    *   **选择性遗忘**：
        *   **时间语义剪枝**：在移入中期记忆时，保留语义评分高或处于场景边界的帧。
        *   **空间语义选择**：在移入长期记忆时，根据语义评分在空间维度筛选 Token，保留关键信息。
*   **阶段 2：自适应记忆检索（Query-aware）**
    *   **锚点条件近期门控（Recency Gate）**：计算查询与短期记忆的亲和力。如果查询针对的是“当下”，则直接跳过中长期检索；如果亲和力低，则触发对历史记忆的检索。
    *   **后期交互（Late Interaction）**：借鉴 ColBERT 机制，在 Token 级别计算查询与候选帧的匹配得分，从而筛选出最相关的候选帧输入大模型（MLLM）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **OVO-Bench**：核心基准，包含实时视觉感知（OCR、动作、属性等）和回溯追踪（情节记忆、动作序列等）。
    *   **StreamingBench**：评估流式输入下的实时视觉理解。
    *   **ODV-Bench**：针对自动驾驶场景的在线视频理解（静态与动态目标）。
*   **基干模型（Backbone）**：主要基于 **Qwen2.5-VL**（3B 和 7B 版本）。
*   **对比方法**：包括闭源模型（Gemini 1.5 Pro, GPT-4o）、开源离线模型（LLaVA-Video, InternVL2）、以及最新的流式模型（FluxMem, HERMES, ViSpeak, StreamForest 等）。

#### 4. 资源与算力
*   **算力需求**：论文明确指出该方法在**单张 NVIDIA GPU** 上运行。
*   **训练时长**：由于是 **Training-free（无需训练）** 框架，因此不存在训练时长，可直接应用于预训练的视觉语言模型。
*   **效率表现**：在处理 128 帧视频时，相比基干模型减少了 **48% 的峰值 GPU 显存**，且显存增长随视频长度呈亚线性趋势。
*   **未明确信息**：文中未具体指明所使用的 GPU 型号（如 A100 或 H100）。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流流式视频基准上进行了详尽测试，涵盖了从通用场景到自动驾驶场景的多种任务。
*   **消融实验**：设计了三组消融实验，分别验证了“双阶段解耦”、“伪问题库先验”以及“近期门控机制”的有效性。
*   **客观性与公平性**：实验采用了统一的 1 FPS 采样率和相同的 Token 预算进行对比，并复现了基干模型的结果，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能提升**：在 OVO-Bench 上，SAVEMem 将 Qwen2.5-VL-7B 的总分从 52.27 提升至 **62.69**，超越了许多需要微调的流式模型。
*   **实时感知优势**：在 OCR、空间理解等细粒度任务上提升尤为显著，证明了语义先验在保留关键细节方面的作用。
*   **显存优化**：通过三层记忆管理，成功将长视频处理从“长度受限”转变为“预算受限”，极大地提高了长视频流的处理能力。

#### 7. 优点（亮点）
*   **无需训练**：具有极强的通用性，可作为插件直接提升现有 VLM 的流式处理能力。
*   **语义引导压缩**：突破了传统仅依赖视觉相似性的局限，利用伪问题库巧妙地引入了“常识性”语义重要性。
*   **自适应检索范围**：近期门控机制能够根据查询意图自动平衡“关注当下”与“回溯历史”，提高了检索效率。

#### 8. 不足与局限
*   **复杂推理挑战**：在需要极高时间覆盖度的任务（如精确计数、长程因果推理）中，受限于有限的内存预算，仍存在信息丢失的风险。
*   **伪问题库的局限性**：目前使用的是一组固定的通用问题，对于某些极度专业化（如医疗、工业监控）的视频领域，可能需要定制化的语义先验。
*   **两阶段协同上限**：由于无需训练，压缩和检索阶段无法进行端到端的联合优化，性能可能存在进一步提升的空间。

（完）
